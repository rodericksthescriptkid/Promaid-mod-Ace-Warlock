# Promaid 上传 CurseForge / Modrinth 发布指南

> 本文是**操作手册**，回答"点哪里、填什么"。两个平台都是**先提交、再人工审核**，新项目第一次提交会进审核队列（CurseForge 常见几小时到 3 天，Modrinth 常见 1~2 天，周末更慢）。
> 版本：v1.2.1 · 对应 jar：`patched/promaid-1.2.1.jar`（Forge）与 `patched/promaid-1.2.0-neoforge-1.21.1.jar`（NeoForge）

---

## 0. 已就绪的素材

| 素材 | 位置 | 说明 |
| --- | --- | --- |
| 方形图标（1024 / 512） | `桌面\promaid-icon\platform_icon_1024.png`、`platform_icon_512.png` | 从你给的竖图裁出的正方形，给两个平台上传 |
| 图标备选裁法 | `桌面\promaid-icon\A_头部特写.png`（脸大、缩到 64px 最清楚）、`B_上方方形.png`（就是上面那张，含面具+脸+矿石方块）、`C_中部方形.png`（最满、不推荐） | 每张都带 `_对照.png`，右边两格是"缩小到 64px 之后的样子"，用来判断小尺寸可读性 |
| 图标已接进 jar | `promaid_src/assets/promaid/icon.png`（128px）+ 两个 `mods.toml` 的 `logoFile` | 游戏内"模组列表"从此显示这张图（此前没有图标） |
| 项目说明（长文） | 本文件第 4 节 | 直接复制粘贴 |
| 版本更新说明 | 本文件第 4.3 节 | 填到"该版本的 changelog"字段 |
| 许可证 | 仓库根 `LICENSE`（MIT） | 两个平台都要选许可证 |
| 前置依赖 | Touhou Little Maid | Modrinth 项目名 `touhou-little-maid`（loaders 已含 forge + neoforge、版本含 1.20.1 / 1.21.1）；CurseForge 上叫 Touhou Little Maid |

**图标那条为什么要方形**：两个平台的图标位都是正方形展示，竖图会被自动裁切或压扁；而且**平台图标显示尺寸很小**（列表里可能只有 64px），所以判断标准只有一个——"缩到 64px 还认得出这是什么"。上面每张候选图右侧就是给你看这个的。

---

## 1. 你需要自己准备的两样东西

**① 4~6 张游戏内截图**（平台都建议至少 3 张，缺图是常见驳回理由）。建议按这个清单拍：

1. 空袭女仆在天上放烟花俯冲打怪（最能代表 1.2 系列）；
2. 远程空袭空中盘旋开火；
3. 建造系统工地（红色区块框 + 橙色幽灵方块投影）；
4. 排班表界面（列表 + 时段高亮）；
5. 指标石锁定方块 + 橙色幽灵路径；
6. 配置面板两级菜单 / 手册首页（体现"内置 32 章手册 + 一键跳转配置"）。

拍完放一起，两个平台都复用同一批。截图里尽量**不要出现其它模组的付费内容或他人作品**。

**② 确认素材授权**（审核员问到要能答上来，这三项是你包里"非自写代码"的部分）：

- **图标/宣传图**：确认这张角色图你是从哪来的（自绘 / 委托 / 生成式工具）。两个平台的共同底线是"你上传的东西你得有权用"。若为生成式工具产出，如实说明即可。
- **内置日语语音包（122 条 .ogg）**：确认**参考音源**你有权再分发（若是从别人的语音包/作品里取音，这是唯一有实际风险的一项，建议换成自己的录音或明确可商用的音源）。
- **随包蓝图（`.litematic`）**：文件名里标注"已授权"的那些，确认授权范围包含"再分发"。建议在描述里写一句来源说明（第 4.2 节已留了位置）。

---

## 2. Modrinth（建议先做，流程短、字段少）

网址：<https://modrinth.com> → 右上头像 → **Creator dashboard** → **Projects** → **Create a project**

### 2.1 建项目（Create project）

| 字段 | 填什么 |
| --- | --- |
| Name | `Promaid（更智能的车万女仆）` |
| URL / slug | `promaid`（若被占用就 `promaid-tlm`） |
| Summary | 见 4.1（一行，256 字符内） |
| Project type | `Mod` |
| Description | 贴第 4.2 节的 Markdown |
| Categories | 勾 `Mobs`、`Adventure`、`Utility`、`Game mechanics` |
| Environment → Client | **Required** |
| Environment → Server | **Required** |
| License | `MIT License`（选 "Custom" 也行，URL 填仓库 LICENSE 地址） |
| Links → Source | `https://github.com/fadersketch/Promaid-mod` |
| Links → Issues | `https://github.com/fadersketch/Promaid-mod/issues` |
| Icon | 上传 `platform_icon_512.png` |
| Gallery | 传你拍的截图（拖拽排序，第一张最重要） |

> Client / Server 都选 Required 是如实填写：本模组有客户端渲染（鞘翅图层、冷却 HUD、配置面板）也有服务端逻辑，少装一边会显示异常。

### 2.2 传版本（左侧 Versions → Create version）

**要传两次**（同一个 1.2.1，两个加载器）：

| 字段 | 第 1 次（Forge） | 第 2 次（NeoForge） |
| --- | --- | --- |
| Version name | `v1.2.1 (Forge 1.20.1)` | `v1.2.1 (NeoForge 1.21.1)` |
| Version number | `1.2.1` | `1.2.1` |
| Release channel | `Release` | `Release` |
| Loaders | **只勾 Forge** | **只勾 NeoForge** |
| Game versions | **只勾 1.20.1** | **只勾 1.21.1** |
| File | `promaid-1.2.0.jar` | `promaid-1.2.1-neoforge-1.21.1.jar` |
| Dependencies | 添加 **Required** → `Touhou Little Maid` | 同上 |
| Changelog | 贴第 4.3 节 | 贴第 4.3 节 |

> 千万别一个文件勾两个加载器——Forge 包放进 NeoForge 环境会直接启动失败，这是投诉最多的来源。
> 文件名建议改成 `promaid-1.2.1-forge-1.20.1.jar` / `promaid-1.2.1-neoforge-1.21.1.jar` 再传（纯为了下载者一眼能分辨，不影响加载）。

### 2.3 提交审核

两边的字段填完 → 项目页右上 **Submit for review**。之后项目对你是可见的、对外不可见，审核通过才会公开。被驳回会在 dashboard 里给理由，改完再提交即可。

---

## 3. CurseForge

网址：<https://authors.curseforge.com/> → 用你的 CurseForge 账号登录 → **Create Project**

### 3.1 建项目

| 字段 | 填什么 |
| --- | --- |
| Project type | **Minecraft** → **Mods** |
| Name | `Promaid（更智能的车万女仆）` |
| Summary | 见 4.1 |
| Project Avatar | 上传 `platform_icon_1024.png`（正方形） |
| Description | 贴第 4.2 节（编辑器支持 Markdown，可切到 Markdown 模式直接贴） |
| Category | 勾 **Mobs**、**Adventure and RPG**、**Utility & QoL**（三个都勾一点，分类只是检索用） |
| Game version / Modloader | 建项目时若要求填，随便选一个占位即可——**以每次上传文件时选的为准** |
| License | `MIT License`（填上仓库 LICENSE 的 URL） |
| Links → Source / Issues | 填 GitHub 仓库与 Issues 地址 |
| Relations | 添加 **Required Dependency** → `Touhou Little Maid` |

### 3.2 传文件（Files → Upload File）

同样**传两次**：

| 字段 | 第 1 次 | 第 2 次 |
| --- | --- | --- |
| File | `promaid-1.2.0.jar` | `promaid-1.2.1-neoforge-1.21.1.jar` |
| Display name | `Promaid 1.2.1 — Forge 1.20.1` | `Promaid 1.2.1 — NeoForge 1.21.1` |
| Release type | **Release** | **Release** |
| Game version | `1.20.1` | `1.21.1` |
| Modloader | **Forge** | **NeoForge** |
| Changelog | 贴第 4.3 节 | 贴第 4.3 节 |

传完把 Forge 那个标成主文件（Mark as primary file），这样"Download"按钮对上 1.20.1 用户。

### 3.3 提交审核

文件上传后状态是 `Under Review`（项目本身也会 `Pending Approval`）。审核期间项目对外不可见。通过后自动公开；被驳回会给具体条目，改完重传。

> CurseForge 的 `Manage Project → Distribution` 里不要勾任何"限制下载/仅限特定整合包"之类的选项，保持默认可公开下载。

---

## 4. 可直接粘贴的文案

### 4.1 项目名与简介

**项目名**

```
Promaid（更智能的车万女仆）
```

**简介（中文，两个平台通用）**

```
《车万女仆》(TLM) 的扩展模组：让女仆会飞（两种空袭打法）、会建造、会挖矿伐木烧制酿造、会排班干活、会记事儿说话、还会在你遇险时保你的命。全部功能游戏内逐项开关 + 内置 32 章手册。
```

**Summary（英文版，Modrinth 用户以英文为主，可二选一或并用）**

```
A Touhou Little Maid addon: flying air-raid combat, blueprint building, mining, farming, scheduling, AI memory & chat, and self-preservation. 31-chapter in-game guide, everything toggleable.
```

### 4.2 完整描述（Markdown，两个平台通用，直接整段粘贴）

````markdown
# Promaid（更智能的车万女仆）

为《车万女仆》(Touhou Little Maid, TLM) 制作的扩展模组，让女仆真正"活"起来——会飞、会建造、会干活、会记事儿、会说话，还会在危险时保你的命。

**纯独立前置**：只要求原版车万女仆（TLM），不对任何其他模组做专属联动。不过武器判定基于原版攻击力属性，所以 tacz / 卓越前线 / 拔刀剑 / 史诗战斗 / 万法皆通等模组的武器**天然可用**，无需专门适配。

> 开发过程中使用了 AI 辅助编程工具；作者并非专业程序员，代码中可能存在缺陷。开发者平时只玩单人模式，未针对多人服务器做优化，有此类问题请多加谅解，也欢迎到 Issues 反馈。

---

## 一、空袭 —— 会飞的女仆（v1.2.0 主线）

给女仆穿上**鞘翅**、带上**武器**和**烟花火箭**，她自己飞起来打怪——不再贴地追着砍。装备不齐她不会飞，而且切到任务当场就报缺哪一件。

| 模式 | 怎么打 | 武器位 |
|---|---|---|
| **近战空袭** | 爬到敌人上方 → 收翅俯冲 → 触底前一记近战 → 再起飞，循环 | 任意近战武器（剑/斧/三叉戟/重锤/模组近战） |
| **远程空袭** | 在天上盘旋，用手里的远程武器持续开火 | 任意远程武器（弓/弩/御币/三叉戟/模组弹射/枪械） |

- **三件套**：鞘翅（切任务后她自己穿到胸甲槽）+ 武器 + 烟花火箭（放背包就行）；远程空袭额外需要弹药，御币 / 三叉戟不消耗弹药；**不响应自主切换**（不会被"主动参战"改任务、也不会自己换掉你给的武器）；
- **收翅俯冲那一击**：1.21.1 手持**重锤**时按玩家同款的**重锤下落加成**结算（含风爆的原地爆炸与自我顶起），而且她按**本轮最高点**算落差——飞得越高这一锤越狠，不设上限；其它武器按原版**暴击 ×1.5** + 附魔。命中率有三道保险（判定距离 4 格 / 收翅 2 tick 即可出手 / 对身边 2.5 格内的其他合法敌对目标补一记，女仆一律跳过、**绝不误伤主人与宠物**，且要过视线）；
- **远程空袭**：绕目标约 10 格盘旋、维持在目标上方约 3.5 格；掉高 3 格才补烟花；怪物贴到 3 格内时**弹开她自己**（只弹她不弹怪）；开火走 TLM 自己的通道，附魔照常生效，御币的扇形分档规则照搬原版，装了 TACZ / 卓越前线的女仆能**拿枪在空中开火**；
- **索敌**：以她自己为圆心、**半径 50 格的球**，锁定即持续瞄准；视线只作为**新锁定**的门槛，"隔墙不出手"依然成立；
- **空袭牵引绳**：空袭期间以她为圆心、半径 **100 格**（可调，0 = 关闭）内找不到主人，就**立刻把她传送回你身边**（强制生效、无视地块、可以空中传送）——防的是"她飞太高把目标打死后自己回不来"；距离按 3D 算，飞太高本身也会触发；
- **空袭期间的强制限制**：禁止被传送（跟随拉回 / 一键集合 / 跨维跟随 / 死亡归位都让位，唯一例外是上面那条牵引绳）、不触发自保（但**不免疫真实伤害**，照样会被打下来）、滑翔 / 悬空 / 俯冲时不搭方块、落地自己放水 / 细雪接住；
- **默认开着「空袭免疫摔落伤害」**：空袭整条链路是"高空盘旋 + 收翅俯冲"，落地缓冲万一没接住（背包没桶 / 落点被占 / 被打断）代价是十几点伤害甚至摔死，想按原版吃摔伤可以在配置里关掉。

## 二、三叉戟：近战 / 远程 / 激流旋转冲击

三叉戟在本模组里有三条明确分工，**近战与空袭两条线都能用**：

- **当近战武器**：攻击模式 / 近战空袭都认它（原版近战伤害与附魔照常结算），她在攻击模式下会拿它贴脸打，不会站着原地转圈；
- **当远程武器**：远程空袭里它可以投掷，而且**不消耗弹药**（免检，不会误报"缺弹药"）。注意**附了激流就投不出去**——那是原版"水里突进"的设定；
- **激流旋转冲击**（默认开，可关）：主手拿着**带激流附魔的三叉戟**时，她那一记普通挥砍会被**替换成旋转冲击**——把玩家那套"激流突进"搬成了女仆的**独立攻击链路**：
  - **触发就是她的攻击时机**：近身 **4 格**内替换（地面要站在地上）；空袭的**收翅俯冲那一记在空中也替换**（那一下本来就在空中，实战价值更大），**空中旋转期间不会自我摔伤**；起不了手就照常挥砍；
  - 旋转 **16 tick**，与玩家同款（平躺 + 高速自转），撞到谁就结算一次伤害（攻击力 + 附魔），**同一目标每次突进只打一下**；
  - 原来那一记普通挥砍由这条链路**取代**（走路、索敌、排班一律不变）；关掉「激流三叉戟旋转冲击」即恢复普通三叉戟挥砍；
  - 视觉上照抄了官方那圈"激流特效"（是个独立的渲染图层），旋转枢轴也抬高过——不会看起来像嵌进地里。

## 三、其它新玩法

- **指标石**（9 块平滑石头 3×3 合成）：手持它，视线指到的方块显示绿色框 → 右键锁定成红色框 → 右击你自己的女仆绑定 → 从她脚下到锁定点之间所有空气方块变成橙色幽灵方块，她立刻一格一格填过去。材料先从她背包拿、不够从你的背包拉；创造模式视为无限；
- **床铺互通**：女仆能睡原版 16 色床，**玩家也能睡女仆床并把它设为重生点**；
- **投喂认模组食物**：判定改为「有食物属性 + 不在黑名单」（与 TLM 自己"女仆饿了吃"完全同口径），三明治 / 模组料理 / 模组水果全都认；配置面板里是**勾选面板**，点图标切换能不能吃，支持搜索翻页；默认不喂腐肉、生肉、金苹果那几类；
- **女仆支援喂食 = 她自己吃**：喂给另一个女仆的食物，回血量与她**自己吃逐行同源**；
- **不得把方块搭在主人身上**：目标格被主人的碰撞箱真正占着时不搭方块（防把她挤住、卡住或盖住头）；判据取"碰撞箱真正交叠"，所以主人站在方块上不会被误判，正常垫脚搭路不受影响。

## 四、核心系统（不需要 LLM 也能用）

- **排班表**（纸 + 墨囊合成）：全女仆日程调度——早班 / 晚班 / 全天、把工作时段切成 6 段排不同任务、当前时段整行高亮、一键集合、自动在家模式、大范围活动与空闲散步；
- **建造系统**：女仆按蓝图**一块一块真实搭出来**，材料真实消耗、缺料会暂停并告诉你补什么。四种蓝图来源：**随包附赠 81 套已授权蓝图**、LLM 对话现场生成、外部图纸文件（`.nbt/.snbt/.litematic/.schem/.json`）、AI 设计器；工地有金色框预览 + 青色幽灵投影（**Z 键旋转 90°**）、工头模式、进度持久化、缺料自动替代（机械蓝图按六层自下而上）；
- **挖矿 / 伐木**：矿表白名单 + 价值评分选矿、连锁采集、透视感知（默认关）、搭方块防掉落与自动回收、**发呆看门狗**；伐木自动识别模组原木、随手补种、骨粉催熟；
- **烧制 / 酿造 / 农场 / 宰杀**：什么能烧就烧、多女仆各占一个炉子；酿造有批量与定向两种模式（缺料会停下等补料）；农场连锁收获 / 批量种植、耕地标记制、锄地索敌、作物骨粉；宰杀按种类计数阈值自动宰杀，不误杀驯服宠物；
- **战斗与自保**：被动保命（低血回血 / 垫高 / 逃跑、环境险境自救）、回血阶梯、逃生手段、致死自动回魂符、**女仆自动复活**（重生点不可用就直接在主人所在位置复活，强制无视地块）、主动切换战斗并自动还原；
- **主人免伤三层防护**：伤害事件总闸 + 全部攻击注入点过滤 + 定期清仇恨——雪地打雪仗只会扔雪球，绝不挥刀；
- **单兵战术 / 贴身辅助 / 武器自动切换**：PVP 式走位、跳劈暴击、放风筝、举盾格挡；自动投喂与治疗；武器按 **DPS（最终伤害 × 攻速）+ 附魔 + 耐久**评分自动换（**判定全基于原版属性，没有武器黑名单**）；
- **被动技能**：落地水 / 落地雪、岩浆逃生放水、主人死亡传送、搭路（背包有方块时朝你铺桥/垫高，方块自动回收）；
- **女仆安全与区块保载**：有主女仆所在区块持续保持加载；寻路主动绕开岩浆 / 火 / 仙人掌等危险方块；被困基岩顶层或掉出世界底部自动救援。

## 五、AI 部分（需要自备 LLM 站点，全部可关）

- **AI 记忆**：重要事实、关系三元组、主人画像、工作笔记 + 日 / 3日 / 周 / 月四级记忆日记，睡一觉自动整理并沉淀长期记忆，带人格种子与情绪快照；
- **主动对话 / 工作播报 / 自主决策**：她会自己开口（气泡 + 语音），关心你重伤、夜晚困倦、好感升级，也会在空闲时自己判断该去干什么；
- **17 个 AI 工具**：直接对她说"过来我身边 / 给我钻石 / 去打那只怪 / 记住我喜欢喝红茶 / 造一个 5x5 现代小屋"；
- **感知与情绪**：纯规则的感知系统（敌人出现/离开、主人受伤/低血、天气变化）+ PAD 三维情绪模型；
- **TTS 与语音**：内置**日语语音包（开箱即用，不联网）**，也可接自己的 TTS；系统消息可朗读。

> **需要联网的地方只有一处**：AI 对话 / 记忆 / 工具 / 蓝图生成需要你**自己填一个 OpenAI 兼容 API 站点**（模型、地址、密钥都在游戏内配置面板填写）。不填 = 这些功能自动不工作，模组其余全部功能照常，**不会偷偷发任何请求**。所有主动发言共用日配额（默认 40 次/天）控成本。

## 六、安装

1. 装好对应版本的 Minecraft 与加载器：**1.20.1 + Forge 47.4.x**，或 **1.21.1 + NeoForge 21.1.250**；
2. 装前置《车万女仆》TLM（1.20.1 用 Forge 版，1.21.1 用 NeoForge 版）；
3. 把本模组的 jar 放进 `mods` 文件夹（**注意选对加载器的那个包**）；
4. 进游戏会收到一本《Promaid 手册》（丢了用 `/give @p maid_smart:blueprint_book`，或工作台「书 + 纸」合成），里面有 **32 章保姆式教学**；
5. 想用 AI 功能，按手册第 3 章把 API 站点配好。

**升级注意**：装新 jar 前请删掉 `mods` 里的旧版本 jar（modId 相同，同时加载会启动失败）。旧存档兼容：物品 / 任务 / 蓝图 / 记忆等持久化数据沿用 `maid_smart` 命名空间。

## 七、已知情况

- **只玩单人**：开发者不玩多人，未做服务端优化。多人服务器上若有异常，欢迎反馈但请多谅解；
- **TLM 版本**：实测 1.5.3（1.20.1 Forge / 1.21.1 NeoForge）。其他版本未必兼容；
- **1.20.1 与 1.21.1 的差异**：1.21.1 多一块**重锤**相关内容（重锤猛击、风刃、专属落地水），1.20.1 没有重锤，其余功能对齐。
- **与「是，史蒂夫模型」(YSM) 共存**：不会崩——v1.2.0（实测五百四十八）修掉了导致**黑屏进不去游戏**的 `ClassCastException`；但**鞘翅与激流旋转特效在 YSM 模型上不绘制**（YSM 模型没有 Gecko 骨骼表，硬画只会把翅膀堆在原点，所以那两个图层在 YSM 渲染器下主动跳过），女仆的背部物品/鞘翅由 TLM 自己的 YSM 图层负责。两树同步修复。

## 八、致谢与素材说明

- 前置模组 **Touhou Little Maid** 及其作者；
- 随包附赠的 `.litematic` 蓝图（文件名标注"已授权"者为取得授权后随包分发）；
- 内置日语语音为使用开源语音合成工具生成；
- 图标与宣传图由作者提供。

## 九、许可证

**MIT** —— 源码与问题反馈：<https://github.com/fadersketch/Promaid-mod>
````

### 4.3 v1.2.1 版本更新说明（填到"该版本的 changelog"字段）

```markdown
## 本版主题：空袭（会飞的女仆）

**新玩法**
- 近战空袭 / 远程空袭两种飞行攻击模式（鞘翅 + 武器 + 烟花火箭）：收翅俯冲、空中盘旋开火、
  球面索敌（半径 50 格、锁定即持续瞄准）
- **空袭牵引绳**：空袭期间离主人超出半径（默认 100 格，可调 / 可关）就立刻传送回主人身边，
  防止她飞太高把目标打死后自己回不来
- 1.21.1：重锤按玩家同款下落加成结算（按本轮最高点算落差，飞得越高越狠），含风爆
- **指标石**：视线锁定 + 绑定女仆后由她逐格铺路搭墙
- **激流三叉戟旋转冲击**：地面近身与空袭收翅俯冲均可触发

**修复与调整**
- 自动复活：去掉内置退避 CD；重生点不可用（床被拆 / 重生锚没电 / 维度不允许）时直接在主人
  所在位置复活（强制、无视地形）
- 排班表的人工传送改为强制 + 无视地块 + 可空中传送
- 女仆支援喂食对齐"她自己吃"的回血口径
- 投喂判定改为"有食物属性 + 黑名单"，新增食物勾选面板；金苹果仍走治疗链
- 空袭链路的批量修复：鞘翅渲染与挂点、附魔生效、出手视线门控、落地缓冲循环、副手被烟花占用、
  飞高丢目标、弩射烟花、御币弹药误判、拔刀剑适配等
- **装了「是，史蒂夫模型」(YSM) 时黑屏进不去游戏**：鞘翅/旋转特效图层的渲染器类型写死导致
  `ClassCastException`，实体渲染器整批建不起来 → 资源重载被中止 = 黑屏；现已放宽类型并在
  YSM 渲染器下跳过这两个图层（1.20.1 与 1.21.1 同步修复）
- **本版起模组图标不再空白**（游戏内模组列表显示图标）

**完整逐条更新日志**（本版 99 条）见仓库 `CHANGELOG.md`。
```

---

## 5. 审核会被卡住的点（照着自查）

1. **图标不是正方形 / 分辨率太小** → 用第 0 节生成好的方形图；
2. **描述太空、缺安装说明与前置声明** → 第 4.2 节已包含前置、安装、版本差异、许可证；
3. **没写依赖** → 两个平台都要把 Touhou Little Maid 标成 Required；
4. **加载器填错**（Forge 包勾了 NeoForge）→ 一个文件只勾一个加载器；
5. **截图缺失或与内容无关** → 至少 3 张、真实游戏画面；
6. **许可证没选** → MIT；
7. **外部下载链接**：描述里不要出现"必须去某网盘下载"之类，所有文件都从平台上传（GitHub 源码链接是允许的）；
8. **素材来源说不清**（语音包 / 蓝图 / 图标）→ 第 1 节那三项自己先确认一遍，描述里已留出处说明段落。

## 6. 上线之后

1. 把两个平台的项目链接加回仓库：`README.md` 顶部已留了"仓库"一行，可再加 CurseForge / Modrinth 徽章；
2. 之后每次发版：**先**在本地重新构建两个 jar（`build_promaid.py` / `build_promaid_neo.py`）→ 部署本地 → 传两个平台的新版本 → 再在 GitHub 发 Release（顺序反了会出现"平台上的包和仓库里的不一致"）；
3. 平台上的版本号与 `mods.toml` 里的 `version` 保持一致（当前两边都是 `1.2.1`）。
