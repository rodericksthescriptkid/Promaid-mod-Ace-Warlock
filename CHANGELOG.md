## 实测五百六十二【空袭·法术层：用飞行武器的同时顺手放法术（需装《车万女仆：魔法》）】

### 需求

需求原文："……女仆能使用近战/远程空袭的默认武器（近战：鞘翅+重锤，远程：鞘翅+弓/枪械mods枪械）
的同时进行法术释放。"

补充口径（需求方补充）：法术书不是武器、是**饰品栏位装备**，所以它**不该参与"近战/远程 DPS"
那套自动模式选择**（此前那条按 DPS 选模式的路子对法术攻击模式无效）。因此这一版做的是
**叠加层**：不新增任务、不占武器位、不改三件套激活口径（还是鞘翅 + 武器 + 烟花；法术书不是第四件必需品）。

### 两侧接口（反编译实证：车万女仆：魔法 1.8.4-neoforge + TLM 1.5.3-neoforge）

- **唯一施法入口链**：任务行为 → `SimplifiedSpellCaster.melee_tick/far_tick`
  → `SpellBookManager#castSpell` → `ISpellBookProvider#castSpell`
  → `IronsSpellbooksProvider.initiateCasting/actualCasting`（forceLookAtTarget → setupSpellTargetData
  → checkPreCastConditions → MagicData.initiateCast → onServerPreCast → setCasting）。
- **空中无门槛**：整条链路（provider / manager / ISS 侧）**没有任何 isFallFlying / onGround /
  isPassenger 判定**——鞘翅滑翔中照样能放（唯一的 isPassenger 是雷步落点的 stopRiding，不是门禁）。
- **不看主手**：ISS 路径只要求女仆身上（**背包 / curios 饰品栏 / 主手任一**）有
  `ISpellContainer.isSpellContainer(item)` 为真的物品 → 法术书放**饰品栏**完全可用。
- **女仆没有法力**：她走自有 `MagicData(false)`，ISS 的 `canBeCastedBy`（查蓝）与
  `castSpell`（扣蓝）都不在这条链上——只剩"**法术冷却 + 黑名单**"两道闸，且两道闸都在
  `initiateCasting` 里判（我们调 `castSpell` 时它自己会筛掉冷却中的法术）。
- **续施法不依赖任务**：法术模组自己挂在 TLM `MaidTickEvent` 上每 tick 调
  `SpellBookManager#tick()` 推进吟唱/连续施法/冷却 → "她的任务是不是法术任务"对施法链路没有影响。

### 改法（promaid_src_neo，1.21.1 专属）

1. **新增 `MaidSpellCompat`**（`com/maidsmart/combat/`）：全反射软兼容层，与
   `GunCompat`（枪械）/ `SlashBladeCompat`（拔刀剑）同范式——没装 / 换版本 / 内部改名
   都只是"这层不生效"，不会让本模组起不来。探针拿的是
   `SpellBookManager.getOrCreateManager` / `getProviders` / `stopAllCasting` 与
   `ISpellBookProvider.setTarget` / `castSpell` / `isCasting` —— **全部是公开方法，不需要 mixin**。
   **刻意不用它的 `SimplifiedSpellCaster.melee_tick/far_tick`**：那两个方法在放法术之外还会替女仆
   挥一记 `doHurtTarget`，而空袭的伤害结算有自己的口径（收翅俯冲那一记 + 无敌帧清零），
   多出来的这一挥会打乱命中判定；需求也是"用武器打的同时施法"，所以只触发法术那一半。
2. **`MaidFlightCombatBehavior` 新增 `tryCastSpell`** + 两个调用点 + 两张节流表
   （`SPELL_NEXT_CAST` 发起节奏 / `SPELL_LAST_LOG` 日志限频，随 `forget`/`pauseRound`/`clearAll` 清理）。
   命中判定先过自己的三道闸：开关、3D 距离（默认 24 格，与法术模组自己的 `maxSpellRange` 对齐——
   我们直连 provider，它不替我们拦距离）、`SelfPreservationBehavior.hasSight`（隔墙不施法）。
3. **三个配置项**（`MaidSmartConfig` + `PromaidConfigScreen` + 中英 lang）：`flightSpellCast`（默认开）、
   `flightSpellCastInterval`（默认 20 tick）、`flightSpellCastRange`（默认 24 格）。
4. **手册新增第七节**（`GuideContent.flightGuide`）：法术书放哪儿、放哪个法术、什么时候放、
   三个开关、以及"怎么确认她在放法术"（运行日志搜「空袭·法术」）。

**没有动的**：三件套激活口径、空袭状态机、伤害结算、自主切换排除名单、落地缓冲。

### 为什么只在这两个相位发起施法（关键设计）

法术模组在**吟唱期间每 tick** 把女仆的朝向拧向目标（`forceLookAtTarget` 直写 yaw/pitch），
而 TLM 的 `MaidTickEvent` 在 `EntityMaid.tick()` 里**早于 brain tick** 发出
（字节码实证：先 post 事件、再 `super.tick()`）——所以**吟唱期间我们写的盘旋/爬升朝向会被它覆盖**，
而鞘翅滑翔的转向力来自视线方向（`LivingEntity.travel` 滑翔分支把水平速度往视线拽），
硬顶就是两种朝向互相打架、飞行轨迹被掰歪。

于是只在**本来就该面向目标**的两段发起：
- **远程空袭**：`tickRangedAir` 的"开火"之前（与枪械开火同一位置——理由同款：开火会拧朝向，
  放在朝向逻辑之前才能被本 tick 的盘旋朝向盖回去）；
- **近战空袭**：已经爬到目标上方、正要压低俯冲那一刻（`faceTarget` 之前）。

**爬升段刻意不施法**（那一段要"背离敌人 + 抬头"把烟花推力吃满），**收翅俯冲那一段也不施法**
（本轮唯一的致命一击，不能让吟唱抢朝向）。

### 验证（专用服务器实测，2026-09-18）

环境：NeoForge 21.1.250 + TLM 1.5.3-neoforge + 车万女仆：魔法 1.8.4-neoforge +
Iron's Spells 'n Spellbooks 1.21.1-3.16.3（+ geckolib / curios / playeranimator / irons_lib）+ 本模组；
无头服务器 + RCON 驱动场景（`/summon` 直接带 `MaidTask` 与 `MaidInventory` NBT 生成女仆，
背包放鞘翅/重锤或弓/烟花/法术书，旁边放一只带抗性的目标）。

- **近战空袭**：日志出现 `[空袭·法术] 测试女仆 空中施法（目标 23.4 格，近战空袭）`（多轮）；
- **远程空袭**：同上，`远战测试 … 远程空袭`；
- **法术确实生效**（决定性证据）：给她一本只带 `irons_spellbooks:oakskin` 的法术书，
  实测她身上挂出 **`irons_spellbooks:oakskin`（amplifier 2）且持续时间不断刷新**
  （386 → 266 → 341 → 221 → 300 → 380 → …）——说明 trigger → castSpell → initiateCasting → onCast
  全链路通，不是"只是喊了一声"；
- 编译：`promaid_src_neo` 249 个源文件 0 错；打包 650 条目通过。

**待实测**：画面观感（她施法时的朝向与飞行动作是否自然、盘旋/爬升受多少影响）需在客户端确认。

### 已知限制 / 后续

- 吟唱期间朝目标由法术模组接管 → 盘旋与爬升轨迹会受一点影响。已用"只在面向目标的相位发起"
  压到最小；若实机观感仍不佳，下一步的选项是"吟唱期间进入固定盘旋姿态"或"爬升前主动中止施法"
  （`stopAllCasting` 会给她正在吟唱的法术记冷却但不结算伤害，即"打断成功、法术作废"）。
- 需求方另提的**第二项未做**：让她智能使用带冲刺能力的法术（如「烈焰冲锋」= ISS 的
  `burning_dash`）来给飞行加速。那条路要的是"**指定法术**"的施法路径
  （`MaidIronsSpellData` + `MagicData.initiateCast` 自己结算 onCast/onServerCastComplete），
  与本次"随机挑一个不在冷却的"路径不同，留待下一轮。
## 实测五百六十三【Sable 同装时：全图扫描被拒查 → 一大片功能静默失效 + 日志刷爆】

**现象**：装了 Sable（Create Aeronautics 的物理引擎）后 `latest.log` 被
「Aborting entity get for abnormally large AABB」刷爆（实测 ~1 小时 19 万条 / 500MB+）。

**根因**（Sable 2.0.5 字节码实证）：`SubLevelInclusiveLevelEntityGetter#get(AABB, Consumer)`
开头即 `if (box.getSize() > 100000.0) { logError(box); return; }`——本模组用了多年的
"有限值全世界 AABB"（±131072/±4096，getSize≈3.7e5）**一个实体都不会交付**。

**影响面**（在 Sable 环境里等于彻底失效，不只是刷日志）：农场锄地/骨粉催熟、钓鱼座椅走位、
指标石找女仆（含客户端预览连线）、自主战斗还原扫描、仇恨清除、中立威胁驱动、宠物免疫、
home 巡逻/走动、险境脱离、危险减益、排班扫描、搭路状态清理。

**改法**：14 处"有限值全世界 AABB"统一改 `level.getAllEntities()`（客户端那处改
`entitiesForRendering()`）——不传 AABB，同时继续绕开早期两个老坑（`EntityMaid.class`
桶 bug、±∞ 经 `blockToSection` 溢出）。

**验证**：本地无头服 + Sable 2.0.5 A/B —— 旧 jar 45 秒 406 条报错、农场女仆的 `till diag`
诊断 0 行；修复后同场景 0 条、4 行（扫描确实重新看到了女仆）。

## 实测五百六十四【空袭女仆被困在远处/异维度：三条自动召回链全被"无界让位"挡死】

**现象**：女仆击败 BOSS 后"凭空消失"，日志里连着几十分钟每 60 秒一条
「[跨维] … 重锤跃起中，跨维度跟随不传」。

**根因**：三处"把她拉回主人身边"的判定都被同一类**无界让位闸**挡住——
`followIfCrossDimension` 的 `isAirborne`、`trySameDimPull` 的 `isAirborne` 与
`isFlightAirborne`；而空袭女仆**整个作战期间都在空中**，让位因此变成永久禁传
（空袭牵引绳又刻意不跨维度）。

**改法**：让位**有界**——确认"确实该拉"后开始计时，持续空中超过 15 秒就作废这一轮让位
（先收翅 + 清空袭静态状态再强拉）；落地即清零；近处的正常扑击不参与计时。
新增日志「[传送] … 持续空中让位超过 15 秒（距离/维度已超线）→ 放弃本轮让位，强制拉回」。

## 实测五百六十五【客户端实体丢失（服务端照打、客户端没有她）→ 重同步兜底】

**现象**：女仆战斗中"客户端消失"——F3+B 看不到碰撞箱、客户端实体列表里没有她；
但服务端一切正常（照常攻击、`/tp` 找得到、凋灵按稳定间隔掉血）；**退出重进立刻恢复**。

**根因**（源码级）：法术模组 `MaidSpellEventHandler.onEntityLeaveLevel` 在"该释放区块加载"
的移除原因下会发 `MaidClientRemovalGuardMessage` **让客户端删掉她的实体**；而它自己的
客户端恢复（`MaidEntityRestoreMessage`）**只对带锚核的女仆生效**——没带锚核的女仆被删掉后
没有任何一方补回来（她随后被加回同一 level 时，原版追踪表未必再发一次生成包）。

**改法（兜底）**：新增 `/maid_smart resync`（`/maid_smart resync all` / `resync <UUID>`）
强制给主人客户端补"删+生成+数据+装备"四包；并在女仆**离场与入世界**两个时点各登记一次
延迟补包（队列按 UUID 全维度查找，仅"她还活着 + 主人同维度"才补）。另记「离场」日志
（含 removalReason）用于定位。

**验证**：实测现象发生时执行一次 `resync` 即恢复可见（输出同时显示
`Sable: 不在 sub-level 内（追踪位置=原坐标）`，据此排除了 Sable 追踪误判这条支线）。

## 实测五百六十一【修：点「保存并返回」卡死 / 下次进游戏卡在 mod 加载界面（配置自触发写盘风暴）】

反馈原文："promaid 配置书在最新版也开始不稳定，一旦点保存有大概率导致游戏崩溃，然后再次进入时
mc 卡在 mod 加载页面，触发了两次卡死，退回到 1.10 版本恢复正常。"

**根因（不是配置书本身，是配置的写盘时机）**：1.2.0 起 NeoForge 侧在收到配置事件后多做了一次
显式 `save()`（当时是为了让默认值迁移落盘）。而 NeoForge 用文件监听器盯着 `config/*.toml`：

```
我们事件里 save()  →  文件被写  →  监听器触发  →  又发一次 ModConfigEvent.Reloading
   ↑                                                                      │
   └──────────────────────  处理器再 save()  ←───────────────────────────┘
```

**实测复现**（NeoForge 专用服务器，外部把 `config/promaid-common.toml` 改一次）：
20 秒内该文件被重写 **38 次**（约 2 次/秒，停不下来），并留下**截断的**
`promaid-common.new.tmp.toml`（16 KB 半截文件，写盘被下一次写盘打断）。

两个症状由此完全对得上：

- **点「保存并返回」= 一次写盘 → 风暴起来 → 客户端卡死/崩**（"一保存就崩、大概率高"）；
- **下次进游戏时 NeoForge 发现旧键要修正**（日志里的
  `Configuration file ... is not correct. Correcting`——玩家 toml 里残留着 1.2.0 删掉的
  `build.realGhostBlocks / ghostBlockCap / placementRange`，这类修正本身也要写盘）
  → 同样踩进风暴 → **卡在 mod 加载界面**，且每次重开都会重演。

**修法**：配置事件处理里**一个字节都不写**（只记录 ModConfig 引用），并加了一道
`inConfigEvent` 闸——事件回调期间任何落盘请求一律拒绝，防止以后有人再加回来。
默认值迁移改由**服务端启动时**跑一次（本来就有这条兜底链路），且**只在真的改过值时才落盘一次**；
那一次在配置回调之外，监听器触发的 Reloading 进来只会记个引用，风暴断掉。

**验证**（同一套复现脚本、同一台机器）：

| | 修前 | 修后 |
| --- | --- | --- |
| 外部改一次配置后 20 秒内文件被重写次数 | **38** | **0** |
| 残留截断的 .new.tmp.toml | 有（16 KB） | 无 |

两树同步修改（Forge 侧原本没写盘，一并收紧到同一口径）；两个专用服务器回归照常 PASS。

## 实测五百六十【友军风免：女仆的法术 / 风弹不再震开主人与同主女仆】

反馈原文："玩家和其他女仆免疫女仆释放的风暴/风弹效果，不会被震开。当前版本免疫伤害，但是会被震风。
导致从高空攻击的时候会直接把主人也打到空中。"

**伤害早就免了，漏的是击退**：主人免伤有本模组的「主人/友军免伤总闸」，《万法皆通》自己也有
盟友判定——两条都在攻击 / 受伤 / 结算三层把"女仆→友军"的伤害取消掉了。但击退**不是伤害事件**：

- 原版 `Explosion.explode()` 在实体循环里先 `hurt()`（被上面两层拦掉），随后**无条件**执行
  `entity.setDeltaMovement(旧速度 + 击退向量)`——伤害结算返回 false 也照推不误。铁魔法的火球
  （`MagicFireball`）正是**用女仆本人当爆炸来源**构造的原版爆炸，所以"女仆在高空打、火球在主人
  脚边炸"就会把主人顶上天，与反馈完全吻合。
- 铁魔法「呼啸之风」（`GustCollider`）更彻底：一次伤害都不发，直接给对方
  `setDeltaMovement(旧速度 + 推力)`，所以三层免伤防护对它完全无效。

**做法**：不去逐个模组打补丁（写死第三方类名 = 无底洞），而是抓住所有外力位移的**唯一出口**
`Entity.setDeltaMovement(Vec3)`（各重载最终都汇到这里），再解决**归因**——"这一下是谁推的"：
新增"当前正在 tick 的实体"记录（`Entity.tick` 的 HEAD），而法术 / 风弹 / 火球推人时调用栈必定
围着效果实体自己的 tick（铁魔法在自己的 tick 里射线命中后推、火球在自己的 tick 里爆炸、瞬发范围
法术在女仆本人的 tick 里结算、风弹 collider 由法书在施法瞬间手动 tick 也在这条链上），
于是推人者 = 那个实体本身是女仆、或它的主人是女仆（投射物 / 召唤物 / 载具）。
爆炸另有一条独立归因（`Explosion` 的直系 / 间系来源），第三方模组的爆炸也照这条走。

**判据三条**：① 推人者是女仆方、且不是她自己（她自己的烟花推进 / 风弹自起跳一字不改）；
② 受害者是玩家或女仆、且与她友军（优先用《万法皆通》自己的盟友口径，保证与它对伤害的判定一致；
没装则用本模组口径）；③ 只拦**明显的外力位移**（速度变化 ≥ 0.2 格/tick，走路 / 重力 / 互相挤开
都远小于它）。

**对主人手感的额外一层**：爆炸对玩家有两条施加通道——服务端改速度，以及把击退向量塞进爆炸包让
**客户端自己推自己的玩家**。所以除了拦服务端那一下，还会在发包前把友军从"受击玩家击退表"里剔掉，
否则本地仍会被"推一下、再被服务器拉回"。两条通道都在服务端处理，客户端不掺和。

配置：`combat.friendlyWindImmune`（战斗与自保 → 单兵战术 → 友军风免，默认开）。

## 实测五百五十九【万法皆通（法术附属）：法术模式的自动换武器 / 自主参战收紧】

《车万女仆：万法皆通》（`touhou_little_maid_spell`，前置 铁魔法 与 新生魔艺）给女仆加了
「近战法术 / 远程法术」两个任务。查兼容时发现一件关键事实：

**这两个任务的 `isWeapon(maid, stack)` 恒返回 true**（javap 反汇编实证：方法体只有
`iconst_1; ireturn`）。而本模组所有"武器"判定都走"任务自带判据"，于是对这两个任务等于
**万物皆武器**：

- **自动换武器**：主手空/快坏时会把背包里**最高 DPS 的冷兵器**（剑/斧）塞进法术模式的主手；
- **自主参战**：法术任务只凭"背包里有任意一件非原版物品"就进候选池（实测三百七十九的兜底）——
  等于"带个模组食物也算会用魔法"。

### 收紧规则（本次改动）

1. 自动换武器遇到法术任务时：**她得真的带着法术装备**（法术书/法器在主手、副手、背包或饰品栏，
   或者附属数据里已经有她的法术书）才开放切换；没带 → **一次都不换**，绝不往法术模式塞冷兵器；
2. 换也**只换法术装备**（附属各 provider 认的法术书/法器）——mod 武器优先，按 附魔 > 耐久 择优；
   主手已经是法术装备则不换（尊重玩家搭配）；
3. 自主参战的法术任务改用同一道闸门（不再只看"有没有非原版物品"）：不会用法的女仆不会被切到
   法术任务上；
4. 其它模组战斗任务（拔刀剑/史诗战斗等）**口径完全不变**，照旧走任务自带的 isWeapon。

### 实现口径

新增软兼容层 `MaidSpellCompat`（与 `GunCompat` 同一套路）：**全反射 + 一次性缓存**，
附属没装 / 版本不符时所有判定恒 false、行为与旧版完全一致——本模组依旧只把 TLM 当硬前置，
不为任何第三方做编译期硬链接。"会用魔法"的判据用附属自己的公开 API：
`SpellBookManager.getLoadedMods/getProvider` + `ISpellBookProvider.isSpellBook` 认法术装备，
`MaidIronsSpellData/MaidArsNouveauSpellData.get(uuid).getSpellBooks()` 认附属数据里的法术书。

### 另外两处的兼容结论（本次未改动）

- **自主参战**：法术系任务已被识别为"远程"（按 namespace 判定），本次只补了上面那道闸门；
- **近战/远程空袭**：空袭的"武器位"仍是本模组自己的近战/远程武器判据，**不会**把法术装备算作武器——
  所以"只会法术"的女仆目前**不会**进入空袭模式（三件套永远差武器位）。要让空袭途中也能放法术，
  需要单独做一层"空袭法术"（把施法挂进飞行行为的节奏里），不属于本次收紧范围。

1.20.1 与 1.21.1 两树同步。

## 实测五百五十八【排班表和游戏时间差 6 小时：深夜在加班、白天在睡觉】

反馈：「排班表和游戏时间不一致，深夜在加班，白天在睡觉」。

### 根因：整张表的时钟比游戏挂钟早 6 小时

原版 Minecraft 的 `dayTime = 0` 是**日出 06:00**，不是午夜（`/time set midnight` = 18000 即证；
TLM 自己显示游戏时间用的也是 `dayTime/1000 + 6` 小时，反编译实证）。而排班表的换算
`分钟 = dayTime % 24000 × 3 ÷ 50` 直接把 dayTime 0 当成了 00:00 —— 于是**整张表比挂钟早 6 小时**：
你按挂钟填的「12:00~16:00 农场」实际跑在傍晚 18:00~22:00，「06:00~14:00 睡觉」实际睡到晚上——
观感就是"深夜在加班、白天在睡觉"。

### 修法

- **时钟补 6 小时偏移**（6000 tick）：现在 00:00 = 游戏午夜、06:00 = 日出、12:00 = 正午、
  18:00 = 日落——与 TLM 显示的游戏时间、F3 的时钟完全一致；
- **班次窗口跟着重锚**（旧窗口也是按"日出=0 点"排的）：早班 = **06:00~18:00**、
  晚班 = **18:00~06:00**（跨午夜，6 个槽的下半段落在 0:00~6:00）、全天 = 00:00~24:00；
  这样早班/晚班与 TLM 自家的 DAY（夜里睡觉）/ NIGHT（白天睡觉）作息正好重合；
- **服务端与客户端共用一份实现**：手册里的"当前时段高亮"、槽位标签、调度器决策全部走
  `ScheduleData`（旧版客户端自己抄了一份 dayTime 换算——两份实现不一致正是这类问题的温床）；
- 已存的日程**不做换算**：同一张表在新口径下就是"挂在挂钟上的那个时段"，正是修复的目的。

1.20.1 与 1.21.1 两树同步；时钟锚点、三个班次的槽窗口、跨午夜命中与"休息时段不排段"
都用公式镜像逐一验过。

## 实测五百五十七【建造默认速度改回 ×1；建完进度 HUD 不消失；进度到不了 100%】

三条都是实测反馈：「建造的默认速度应该改为 ×1，现在是极速」「建造完成之后，左上角的 GUI 不会
消失」（截图停在 80,445 / 80,446 = 99%）。

### 一、默认速度回到 ×1（不再一上来就是极速）

- `build.speedTier` 默认 **x1.5 → x1**；`build.turbo`（极速模式，放置冷却归零、吃满服务器上限）
  默认 **开 → 关**。旧默认下新装的档一开建就是上千块/秒，既看不出建造过程也白烧性能。
**旧档一次性迁移**：用一条**内部标记**（`build.speedMigrated`，跑一次就置位）来判定"只做一次"，
  而不是"值 == 旧默认"——实测玩家 toml 里的真实组合是 `speedTier = "x1" + turbo = true`
  （档位早就被切到 x1、只剩极速还开着），只凭值分不出"旧默认留下的 true"和"玩家自己打开的 true"。
  第一次进游戏后极速会被关掉（档位若是 x1.5 则一并归到 ×1），**之后你想开极速随时开、不会再被改回去**。

### 二、建造完成后左上角进度 HUD 不消失（根因：服务端不再推送，客户端就一直留着最后一帧）

- 进度 HUD 是**客户端拿服务端每秒推送的快照画的**；而服务端在"没有进行中计划"时旧版是**直接
  return、一个包都不发**——计划一完成/取消，客户端永远收不到"清空"，最后那一帧（实测卡在 99%）
  就挂在左上角直到退出游戏。
- 修法两道：① **计划清空时补发一次空快照**（客户端据此立刻清屏）；② 客户端加 **5 秒看门狗**
  ——超过 5 秒没收到任何快照（丢包 / 服务端广播异常 / 切世界重连）就本地清屏，HUD 不允许永久挂着。

### 三、进度卡在 99%（最后那一格）

- 蓝图盖在**已有地形**上时，"开工那一刻就已经是目标方块"的格子旧版**只前进游标、不计数**，
  于是她把该放的都放完了，进度仍差最后几格（截图里的 99% 就是这一格）。
- 现在这类格子**单独计数**（`prebuiltCount`）并计入 HUD 与手册进度条；为防一个格子算两遍，
  真放上去时把它从 prebuilt 里摘掉（两套计数互斥）。**完成判定（缺口扫描那套）口径不变**
  ——仍然只按"她真放的"算，所以"缺口补建"行为一个字节没动。

1.20.1 与 1.21.1 两树同步。

## 实测五百五十六【建造：预览/落点回退旧版（只留"区块内箱子取料"）+ 建造瞬移限频减半】

反馈：「建造功能回退旧版本，关于新版本的改造只保留一个可以从箱子里拿的功能，我发现在用新的
方式以后反而渲染不出方块了。而且 bug 也很多。」另外「将代码里建造女仆传送到要搭建的方块
旁边的间隔减半，移动区块方面的全部都按照旧版本」。

### 一、回退：投影与落点全部回到旧版

- 投影回到**彩色填充盒**（旧的填充盒 + 棱线：近处实心、远处线框那一套），服务端点云也回到
  「只发坐标」格式（去掉调色板段）——真方块渲染这条路整段撤销；
- 金色预览框回到**跟着玩家走**：点「建造此图纸」开预览 → 退出手册走位选点 → 再打开手册点
  第二下弹确认框（旧的两次点击流程），落点 = 你确认那一刻站的位置；「就地建」的快捷手感因此
  自动回归；
- 删掉「落点微调」界面（`BuildPlacementScreen`）、G 键位、方向键操控、`SelectBlueprintPacket`
  里的落点字段与服务端那套「离玩家 ≤ 32 格 / 区块已加载」校验，以及配套的三个配置项
  （投影真方块 / 投影每帧上限 / 落点微调范围）。

### 二、保留：从建造区块内的箱子取料

缺料 → 女仆去**建造区块内（外扩 4 格）**的箱子 / 木桶 / 潜影箱取那种材料：走过去 → 开箱
（盖子会动）→ 取一部分（背包塞不下的还回原槽）→ 关箱 → 回工地继续盖；末影箱不翻、区块外的
箱子不去。开建前算材料也把箱子里的算进去。配置项与手册里那一段都保留。

### 三、建造瞬移限频减半

「放置前把女仆瞬移到目标格旁」的限频 **4 tick → 2 tick**（`MaidBuildBehavior.TELEPORT_MIN_INTERVAL`）：
摆放连着出时瞬移跟得上每一次落块；瞬移前的判据（安全站立格、已在该格 2 格内不瞬移）与
「找不到安全站姿就隔空放置」的兜底都不变。

### 四、空袭：鞘翅取用优先级 + 飞坏自动补装（承五百五十五）

- 背包里**同时**有普通鞘翅与"鞘翅胸甲"这类装备时，**优先穿带护甲的那件**（既滑翔又当护甲）；
  没有护甲型才退回普通鞘翅；胸甲槽已经穿着其中任意一件时**不换**；
- 鞘翅飞坏/耗尽（原版每 20 tick 掉一点耐久，耗尽即消失）后**自动从背包再拿一件穿上**——
  「补齐三件套」那条链路在空袭任务下每 tick 都在跑，胸甲槽一空就补位；
  背包没有备用鞘翅时她**不会**凭空续航（原版滑翔闸门要求胸甲槽里有可滑翔物品），会滑翔落地；
- 每次换装写一行运行日志「空袭装备 · 胸甲槽换装：<新>（原 <旧>）」（40 tick 节流），
  想确认"到底换没换"看日志即可。

## 实测五百五十五【空袭：模组里"内置鞘翅的装备"（鞘翅胸甲这类）现在也认了】

反馈：「在我的整合包里女仆不会把内置了鞘翅的装备视为有鞘翅（如鞘翅胸甲）」。

**原因**：三件套的"鞘翅位"旧判据写死成 `stack.getItem() instanceof ElytraItem`——只认原版鞘翅
物品。而模组里这类装备基本都不是 `ElytraItem` 的子类，而是**护甲物品 + 重写滑翔钩子**，
于是被当成"没有鞘翅" → 三件套永远不齐 → 空袭模式不激活，还会一直播报"缺鞘翅"；
更糟的是"补齐三件套"那一步会去找背包里的原版鞘翅、把玩家给她穿的这件胸甲**换下来塞回背包**
（等于把护甲扒了）。

**改法**：判据换成 `ItemStack.canElytraFly(实体)`（Forge/NeoForge 扩展方法）。选它是因为
**口径完全同源**——原版滑翔的唯一闸门 `LivingEntity.updateFallFlying`（aiStep 内、travel 之前）
在 Forge/NeoForge 里读的就是这个方法（反编译实证：字节码里依次调用
`ItemStack.canElytraFly` 与 `ItemStack.elytraFlightTick`，**没有任何原版物品判定**；
原版 `ElytraItem` 自己实现的 `canElytraFly` 恰好就是 `ElytraItem.isFlyEnabled`，与旧判据等价）。
所以"她能不能滑翔"与"我们认不认这件装备"从此是同一个判据：
凡是**对玩家能滑翔**的物品，女仆一律认得，不会出现"我们认了但原版每 tick 把滑翔位清掉"。

- 受影响的三处判定全部走新口径：`hasElytra`（三件套检查 / 缺件播报）、`equip`（穿脱，
  **胸甲槽已有这类装备时原地不动**）、以及 `isModeActive`；
- **渲染层保持严格口径**（只认原版鞘翅）——鞘翅胸甲自带模型，再叠一层我们的翅膀只会穿模；
  原版自己的 `ElytraLayer` 同样只认 `Items.ELYTRA`；
- **新增诊断**：缺鞘翅时往运行日志写一行「空袭装备 · 缺鞘翅（鞘翅判定: 胸甲=<物品id>
  (原版鞘翅=false，canElytraFly=true/false) 主手=… 副手=…）」（与气泡同一节流，最多 15 秒一行）。
  看这一行就能分清是"我们没认"还是"那个物品压根没实现滑翔钩子"——后者对玩家同样滑不起来，
  该找的是那个模组（`canElytraFly` 与 `elytraFlightTick` 两个钩子都得有，Forge 给
  `elytraFlightTick` 的默认实现是 `false`，只重写一个滑不起来）；
- **取用优先级**（同一条的补充）：背包里同时有普通鞘翅和"鞘翅胸甲"这类装备时优先穿带护甲的
  那件；胸甲槽已穿着任意一件时不换。**鞘翅飞坏/耗尽后会自动从背包再拿一件穿上**（空袭任务下
  「补齐三件套」每 tick 都在跑），包里没有备用鞘翅才会落地；每次换装写一行运行日志
  「空袭装备 · 胸甲槽换装：<新>（原 <旧>）」（40 tick 节流）；
- 手册「空袭与飞行作战 → 三件套」的鞘翅那一条补了这句兼容说明。

1.20.1 与 1.21.1 两树同步。

## 实测五百五十四【落点微调改版：点「建造此图纸」直接弹界面，方向键搬区块】
> **本条已整体回退（实测五百五十六）**：落点微调界面、方向键操控、G 键位全部删除，落点回到
> 「点建造此图纸时以玩家为中心、跟着人走」的旧做法。下面这段仅作记录。


玩家反馈「微调界面按 G 键触发感觉不是很好」，要的是：点建造该建筑就跳出界面、区块就地产生，
然后直接按 ←↑→↓ 移动建筑——这四个键此时不接管移动，而是接管区块的移动。按这个改完了。

- **入口改了**：手册里点「建造此图纸」不再要点两次、也不再需要 G 键——一下就把「落点微调」
  界面顶出来，金色框同时定在你点那一下的脚下格（青色幽灵 / 真方块投影同帧就位）；
- **键盘驱动**：`←→` 走 X、`↑↓` 走 Z、`PgUp/PgDn` 调高度 Y，按住 `Shift` 一次 8 格；
  `Q`/`E` 转向（界面外照旧用 Z 键）、`R` 回到脚下、`回车` 确认建造、`Esc` 取消；
  按住可以连发（原版屏幕本来就收 GLFW 的重复按键事件）；
- **这四个键不会挪你**：原版 `KeyboardHandler` 在屏幕打开时**直接跳过**按键绑定更新
  （字节码实证：`screen != null` 时整段 `KeyMapping.set/click` 被跳过），所以 ←↑→↓ 只挪区块；
  顺带也挡住了误点方块 / 误攻击——这也是用界面而不是纯 HUD 的原因；
- **看得见才算能用**：面板只占**屏幕顶部一条**（98 px：图纸名 / 落点 / 占地 / 朝向 + 键位提示
  + 两排备用按钮），并**覆盖掉原版的全屏暗底**（`renderBackground` 置空）——挪的时候能一直看清
  世界里的金色框与投影跟着动。按钮仍在（鼠标党可用），键位都写在按钮上，不用背；
- **挪远了也知道在哪**：顶栏实时显示落点**离你多少格**，超过 32 格直接标红提醒（服务端会拒绝这种落点）。
- **确认仍是两步**：「确认建造」先弹一次原版确认框（落点 / 占地 / 朝向 / 离你多少格 + 会摧毁
  区块内障碍物），确认才发包；反悔点「再挪挪」直接回微调界面，落点与朝向不丢；
- **删掉 G 键位**：原来那条 `key.promaid.build_placement` 从原版按键设置里消失
  （`options.txt` 里可能残留一行失效配置，无害；`key.promaid.build_rotate` = Z 不变）。

服务端协议与校验（落点离玩家 ≤ `build.placementRange` 默认 32 格、区块已加载、重叠检查）
原样不动；实测五百五十三的真方块投影预览与「区块内容器取料」不受影响。

## 实测五百五十三【建造三件套：预览改真方块 / 落点固定 + 微调界面 / 从区块内的箱子取材料】
> **本条的第一、二部分已在实测五百五十六整体回退**（真方块投影渲染不出来、落点微调那一套 bug 多）：
> 投影回到彩色填充盒、金色框回到跟着玩家走、微调界面与 G 键位删除。**第三部分「区块内箱子取料」保留**。
> 下面第一、二节保留原样，作为当时的记录。


参照 [车万重工] TLM-Builder 的三处做法（只借思路，代码自己写）。三件事都做完并已部署。

### 一、投影预览改成"真方块"（不再是彩色填充盒）

以前幽灵投影是**轴对齐填充盒**（服务端只发坐标，客户端用 DebugRenderer 画盒子），
只能看出体量看不出材质。现在：

- 服务端点云格式改成 `<点>|<调色板>` 两段：点里带**调色板下标**（base36，1~2 字符），
  调色板按方块种类去重（`i~方块id~方块状态`）——同带宽只多 1~2 字符/点；
- 客户端按调色板还原 `BlockState`（走原版 `BlockStateParser`，**楼梯朝向/半砖/含水都还原**），
  用**原版烘焙模型**半透明绘制（alpha×0.6、RGB 略压暗，同参照的 GhostVertexConsumer 做法），
  已经建好的格子不再重复画；
- 渲染层挪到半透明层之后（AFTER_TRANSLUCENT_BLOCKS），每帧上限 `build.ghostBlockCap`（默认 1500）。
- 旧格式点云仍兼容（无调色板 → 回退填充盒）；开关 `build.realGhostBlocks`（默认开）。

### 二、落点固定 + G 键微调界面（不再"框跟着人走"）
> **本条的微调界面入口已在实测五百五十四改版**：不再按 G、也不再点两次——点「建造此图纸」
> 直接弹出界面，用 ←↑→↓（Shift=8 格）/ PgUp·PgDn / Q·E 搬动区块，回车确认、Esc 取消。
> 下面这段描述的 G 键入口与「X-1 / Y+1 按钮」操作方式已作废；落点固定与服务端校验不变。


以前金色框**每帧取玩家位置**，落点 = 你确认那一刻站的地方，想挪半格只能自己走位。现在：

- 打开预览时**定住落点**（= 点那一下的脚下格），之后不再跟着你动；
- 按 **G**（原版按键设置可改）打开**「落点微调」**界面：`X-1/X+1/Y-1/Y+1/Z-1/Z+1`
  （按住 Shift 步进 8 格）、`左转/右转`（每次 90°，Z 键照旧可用）、`回到脚下`、
  `确认建造`、`取消`——界面不暂停游戏，挪的时候能直接看世界里的金框与真方块投影；
- 点两次"建造此图纸"的**就地建快捷方式保留**（只是落点改为那一下的脚下格，不再漂移）；
- 协议带上落点：服务端校验「离玩家 ≤ `build.placementRange`（默认 32 格）」「区块已加载」
  后才建（重叠检查沿用原有逻辑）。

### 三、从**建造区块内**的箱子 / 潜影箱取材料（女仆会走过去并开箱）

以前材料只认"女仆背包 + 主人背包"，箱子里的东西不算也不去拿。现在：

- 新增取料行为（注册在建筑任务，优先级高于建造）：**计划里缺某种料**、且**建造区块
  （外扩 `build.chestSearchMargin`，默认 4 格）里的容器装着这种料**时，女仆会：
  解除"建造中静止" → 走到容器旁 → **开箱动画**（原版 `blockEvent`，箱子/桶/潜影箱的盖子都会动）
  → 取 `build.chestFetchPerTake`（默认 8）个（背包塞不下的**还回原槽**）→ 关箱 → 回工地继续盖；
- 容器识别用 `BlockEntity instanceof Container`：箱子、陷阱箱、桶、潜影箱、各类模组容器
  全覆盖；**末影箱天然不含 Container**，不会被翻；
- 找不到容器时行为不介入——缺料提示与延后重试仍是原逻辑（`build.fetchFromChests` 默认开，
  两趟取料之间有 `build.chestFetchCooldown`（默认 60 tick）冷却，防"箱子锁着/取不到"来回跑）；
- **开建预检也算上箱子**：材料全放在区块内的箱子里也能正常开建，不再误报"材料不足"。

### 验证

两树编译零错；jar 重建（1.20.1 = 9354824，1.21.1 = 9384700，新类已入包核对）；
Forge 1.20.1 与 NeoForge 1.21.1 专用服务器回归均 PASS；已部署两个客户端实例与 pack1201 服务器。
行为面（预览观感、微调手感、女仆走去开箱取料）待实测确认。

## 实测五百五十二【回血资源判定太窄：包里有食物（金胡萝卜/模组料理）却判成"没有"】

### 先纠错：五百五十一改错了地方

五百五十一 把搭高的门槛从"必须有回血资源"改成了"有方块就搭"——**这个改法是错的，已回退**。
没得回血爬上去确实没有意义，门槛就该是回血资源。也就是说：门槛是对的，错的是**回血资源的判定**。

### 决定性证据（五百五十一 新加的「自保诊断」日志）

```
血 16%  距威胁 1.7 格（贴身线 4.0 格） | 回血资源=无  可搭方块=有
血 65%  距威胁 3.7 格                 | 回血资源=无  可搭方块=有
血 65%  距威胁 4.8 格                 | 回血资源=无  可搭方块=有
```

威胁一直在贴身范围内（搭高分支进得去）、背包里有方块，但"回血资源=无"——而实际包里有食物。
原因：旧判定只认 **药水 / 金苹果 / 一小撮硬编码原版食物**（熟肉、面包、苹果…），
**金胡萝卜、各类模组料理一律不认**（而玩家喂给女仆的正是金胡萝卜）。

### 改法

- 回血资源判定改成与投喂系统**同一个口径**：`EmotionalActionExecutor.isFeedableFood`
  = TLM 自己的 `DefaultMaidHealSelfMeal.isHealMeal` 口径（**有食物属性且不在投喂黑名单里**）——
  任何"能吃的东西"都算回血资源；金苹果保留单独判定（它在投喂黑名单里刻意让给"低血即时增益"路径，
  但作为回血资源必须算）；
- **门槛、上限、塔顶处理全部回退**到五百五十一 之前（无回血资源不搭高；垫到 pillarMax；
  塔顶围困只清计数不主动下塔）；
- 「自保诊断」日志升级：写出**认到的那个物品名**（`回血资源=minecraft:golden_carrot`），
  以后判定再出问题一眼就能看出来。

### 验证

两树编译零错；重建 jar 并部署到两个客户端实例与 pack1201 服务器；双服务器回归均 PASS。

## 实测五百五十一【搭高的门槛压在"有没有带药"上——包里一堆方块也一块不搭】

> **本条的门槛改法已在实测五百五十二推翻并回退**：门槛（必须有回血资源）本身是对的，
> 真正的问题是"回血资源的判定太窄"（金胡萝卜/模组料理都认不出来），已改为通用食物判定。
> 下面这段保留原样，作为当时的排查记录。

### 现象

五百五十那道取料闸口修好、部署进游戏后，实测**仍然一块方块都不搭**，也不播报
"背包里没有搭方块的材料"。日志把范围缩小到一句话：整场自保只有一句
「情况不妙，我小心周旋！」，运行日志里**一行「自保搭高」都没有**——
说明搭高分支根本没进去，问题不在取料闸口，而在它前面的门槛。

### 根因：实测三百六十五那条"没回血资源就不搭"的口径

原口径是：包里没有药水/金苹果/治疗食物 → **搭高不再作为可选项**，被近身只做小幅走位。
理由（当时的反馈）是"塔的唯一意义是安全回血，没得回血垫塔纯属干耗"。

问题在于它把两件事绑在了一起：
- **能不能搭** 取决于包里有没有方块；
- **上去以后干什么** 才取决于有没有回血资源。

于是一个背着几组方块、只带武器（或只带金胡萝卜这类不在"治疗食物"名单里的食物）
的女仆，被怪按在地上打时，一块都不搭——正是"宁愿被打到死"。

顺带查出一条死链：塔顶无回血资源时，那条"围困满 10 秒"的处理在实测三百九十把
围困传送停用之后**只剩 `heallessSiegeTicks = 0` 这个空壳**——什么也不做。所以
"上去也是干耗"确实成立，只不过当初的解决办法是"干脆别上去"，这次改成"上去、
待不住就下来"。

### 改法

1. **搭高门槛改成材料**：`hasBuildBlock(maid)`（背包里有可搭方块）取代
   `hasHealResource(maid)` 作为搭高的前置——branch 2（连续被近身后的爆发搭高）
   与 branch 4（被近身垫到安全高度）都改。
2. **上限分流**：有回血资源 → 照旧垫到 `pillarMax`（塔顶守势安心回血）；
   没有回血资源 → 只垫到 `safePillarHeight`（默认 5 格，够不着就行），
   不再一路垫到 30 格站桩。
3. **塔顶不再干耗**：塔顶 + 威胁仍在 + 包里没有回血资源，围困满 10 秒 → 置位
   `towerDescendRequest`，branch 4 消费它清掉塔位状态下塔，回地面继续打；
   再被近身会重新垫上去（"打不过就上去躲、躲不成就下来打"）。
4. **决策快照日志**（本轮新增，为了不再靠猜）：每场自保第一次锁定威胁时写一行
   「自保诊断」——血 20% 距威胁 3.2 格（贴身线 4.0 格）| 回血资源=无 可搭方块=有。
   运行日志搜「自保诊断」。以后再出现"她怎么不搭方块"，一行就能看出卡在哪道门槛。

### 验证

两树编译零错；重建 jar 并部署到两个客户端实例与 pack1201 服务器；
Forge 1.20.1 与 NeoForge 1.21.1 专用服务器回归均 PASS。
行为面（她真的开始搭方块）待实测确认——若仍不搭，把「自保诊断」那一行发回即可定位。

## 实测五百五十【悬空禁搭把"她自己一跳"也判成了悬空——被围殴时不再搭方块自保】

### 现象

被怪围住时她不再往脚下垫方块自保，也不吭声，原地被磨死。连"背包里没有搭方块的材料"
那个气泡都没有——因为那个气泡在"悬空"时是**特意静默**的，于是整条搭高链路堵死时
一个字都留不下（实测四百四十三当时的设计本意是防误报，副作用就是这种静默失败）。

### 根因：四百九十八那次收紧的又一处误伤

四百九十八把"悬空禁搭"的口径从"坠落 ≥ 落地水触发高度"收紧成了"只要 `fallDistance > 0`"。
当时想拦的是滑翔与真正的高空悬空，但这一条把**她自己在战斗里的小跳**也算进去了：
战术跳劈、走位跨台阶、被击退，都会让她离地一格再落回来，落地前那几 tick
`fallDistance` 是正数 → 判定为悬空 → `takeBuildBlock` 返回 `null` → 搭高一块都放不下。
搭高自己的"放块 → 被顶起 → 落回"也是同一类 tick，等于她一边被围殴、
一边每次想搭方块都被自己的动作否决。

同一处收紧此前已经撞出一例（实测五百零九：岩浆里垫方块自救被禁搭误伤），
这次是它的第二类误伤——根都在"把下落当悬空"这个口径上。

### 改法

"悬空"回到它的字面意思：**脚下没有地**。判据从"是否正在下落"换成
"脚下 3 格内有没有可站立面"（用 `isFaceSturdy(level, pos, UP)`，台阶/楼梯/栅栏这类
"顶面能站人、但不是整块实心"的方块也算落点，免得站在台阶边跳一下就被误判）。
四百九十八要拦的一件没丢：

| 情形 | 判据 | 结果 |
| --- | --- | --- |
| 滑翔 / 飞行作战空中 | `MaidFlightKit.isFlightAirborne` | 禁（原样保留，这是四百九十八的主漏口） |
| 1.21.1 重锤跃起中 | `MaidMaceSmashBehavior.isAirborne` | 禁（原样保留） |
| 从高处掉下来 | `fallDistance ≥ 落地水触发高度` | 禁（恢复四百四十三的原始阈值） |
| 3~5 格浅坠、站在崖边往下跳 | 脚下 3 格内无地 | 禁（新判据天然覆盖） |
| 战斗里的小跳、被击退、搭高自身顶起 | 脚下 1 格就是地面 | **放行** |

三处照旧放行：水里、岩浆里（实测五百零九）、骑乘中。四个搭方块模块
（自保搭高 · 搭路 · 挖矿垫脚 · 伐木垫脚）共用这一道闸口，一起恢复正常。

### 附带的观测修复

自保搭高每次被拒都会往运行日志写一行原因（限频 5 秒/女仆，搜「自保搭高」）：
`悬空禁搭（脚下无地 / 滑翔飞行中）` / `目标格被主人碰撞箱占着` /
`放置格/头顶 2 格不是空气（防窒息拦下）` / `实际头顶被堵` / `背包里没有能搭的方块`。
以后再遇到"她怎么不搭方块"，一眼就能看出卡在哪一道闸口，不必再从行为链倒推。

### 验证

两树编译零错；重建 jar 并部署到两个客户端实例与测试服务器；
两个专用服务器回归均 PASS（Forge 1.20.1 / NeoForge 1.21.1，`Done` 后再盯 30 秒无延迟崩溃）。
行为面（她真的开始搭方块了）以待实测确认——本次另加了「自保搭高」被拒原因日志，
若仍有拦阻，运行日志里会直接写出是哪一道闸口。

## 实测五百四十九【1.21.1 上四个配方全都加载不了（数据包目录名与字段名漏改）；指标石在手册里找不到】

### 一、配方（只在 1.21.1 上坏）

**现象**：1.21.1 生存里合成不了指标石。顺带查出来蓝图书、药剂手册、排班表也都合不出来。

**根因**：1.21（数据包格式 48）把数据包目录名统一改成了单数——`recipes`→`recipe`、
`advancements`→`advancement`、`loot_tables`→`loot_table`、`tags/items`→`tags/item`。
移植 1.21.1 树时只改了后三者，`recipes/` 与 `tags/items/` 漏了，于是**整个目录在 1.21.1 上
根本不会被读取**：四个配方一个都不存在。另外三个老配方还留着 1.20.1 的字段名：

| 文件 | 旧写法（1.20.1） | 1.21 的正确写法 |
| --- | --- | --- |
| blueprint_book / brew_manual / schedule_book | `result.item` | `result.id` |
| schedule_book | `result.nbt` | `result.components` |

（`index_stone.json` 是后加的，字段当时就按新格式写了——但目录名一错，它同样加载不了，
所以玩家最先撞上的正是"指标石合不出来"。）

**改法**：neo 树 `data/maid_smart/recipes/` → `recipe/`、`tags/items/` → `tags/item/`；
三个老配方按 1.21 规范改字段（排班表那枚作标记用的附魔改成
`components` → `minecraft:enchantments` → `levels` → `minecraft:mending = 0`）。
**1.20.1 树不动**——复数目录 + `result.item` + `result.nbt` 才是 1.20.1 的正确写法。

**核验口径**：拿同版本的真实数据包当基准——1.21.1 侧 TLM 自己 49 个配方、灾变 210 个、
tacz 183 个、森罗物语 548 个，清一色 `recipe/` + `tags/item/` + `result.id`，而我们改前是
里面唯一的例外（`recipe`=0、`recipes`=4）；1.20.1 侧区块加载器 / 拔刀剑 / tacz / ProjectE
清一色 `recipes/` + `result.item`，与我们 1.20.1 的写法一致。

### 二、手册里找不到指标石

**现象**：手册目录和「功能总览」里都没有指标石，顺着手册读根本发现不了这个物品。

**根因**：用法文案写在「建造系统教程」章节的最末尾（那一章两页多），既没有独立标题、
也不在「功能总览」的跳转清单里——文案是有的，但等于藏起来了。

**改法**：
- 提为**独立章节**「指标石（即兴临时搭建）」，排在「建造系统教程」之后（全手册 31 → 32 章）：
  合成（9 块平滑石头 3×3 摆满）、三步用法（绿框 → 右键锁成红框 → 右击女仆绑定）、
  材料从哪来（先从她背包、不够从你背包，选数量最多且有碰撞的方块）、几条规矩
  （先锁再绑 / 同时只有一个锁定 + 一个绑定 / 换目标必须先解除 / 中断会报数收尾）、总开关位置；
- 「功能总览 · 一键跳转配置」的「生产与工作」一行加上**指标石**跳转链接，点一下直达它的配置行；
- 建造章节末尾只留一行指向该专章，不再重复正文。

## 实测五百四十八【装了「是，史蒂夫模型」(YSM) 时黑屏进不去游戏：鞘翅图层的泛型参数写死导致 ClassCastException】

### 现象

同时装本模组与「是，史蒂夫模型」(Yes Steve Model, YSM) 时，游戏能启动、能加载资源，
然后**卡在黑屏**进不去（日志里是 `Minecraft.abortResourcePackRecovery`）。

### 根因（一个 checkcast）

TLM 自带 YSM 联动：装了 YSM 时它把女仆交给 YSM 的渲染器
（`EntityMaidRenderer.initYsmModelRenderer`，字段 `ysmMaidRenderer` 的类型是
`IGeoEntityRenderer<Mob>`），并把已有 geo 图层逐个 `copy` 到那个渲染器上。

TLM 的基类是 `GeoLayerRenderer<T, R extends IGeoEntityRenderer<T>>`，`copy(R)` 的形参
就是 `R`。我们那两个图层（`LayerMaidElytraGecko` / `LayerMaidSpinAttackGecko`）把 `R`
写死成 `GeckoEntityMaidRenderer<Mob>`，泛型擦除后 javac 会生成桥接方法，里面
`checkcast GeckoEntityMaidRenderer`。而 YSM 的渲染器（`com.elfmcys.yesstevemodel.*`）
只实现了 `IGeoEntityRenderer`、继承的是 YSM 自己的类，**不是 `GeckoEntityMaidRenderer`
的子类**。于是：

```
ClassCastException: com.elfmcys.yesstevemodel.ooOoooo0o000Oo0OoOooo0oo
  cannot be cast to GeckoEntityMaidRenderer
  at com.maidsmart.client.LayerMaidElytraGecko.copy
  at TLM EntityMaidRenderer.initYsmModelRenderer
→ EntityRenderers.createEntityRenderers 整体中断（实体渲染器整批没建起来）
→ Minecraft.abortResourcePackRecovery → 黑屏
```

### 改法

1. 两个图层的 `R` 从 `GeckoEntityMaidRenderer<Mob>` **放宽为公共上界
   `IGeoEntityRenderer<Mob>`**——`copy` 的形参类型随之与擦除后的签名一致，桥接方法里的
   checkcast 直接消失；构造参数放宽为 `IGeoEntityRenderer<?>`，两个调用点（mixin）不用改。
2. `render()` 开头加 `getRenderer() instanceof GeckoEntityMaidRenderer` 守卫：走 YSM 时
   **不画**我们的鞘翅与旋转特效。本层的挂点逻辑依赖 Gecko 骨骼表
   （`AnimatedGeoModel.bones()` 里的 ElytraLocator/Elytra/UpperBody/Root），YSM 模型没有
   那套骨骼，硬画只会把翅膀堆在原点——那比不画更糟。女仆的背部物品/鞘翅在 YSM 那条链上
   由 TLM 自己的图层负责。
3. **两树同步**：1.20.1 的 TLM 1.5.3 同样带 `initYsmModelRenderer` + `ysmMaidRenderer`，
   装了 YSM 一样会崩，所以这不是 1.21.1 独有的问题。

### 验证

- 两树编译零错；重新打包后 javap 确认两个图层的类声明与 `copy` 形参都已是
  `IGeoEntityRenderer`，桥接 checkcast 消失；
- 没装 YSM 时 `getRenderer()` 必然是 Gecko 渲染器，守卫不生效，原有行为不变。

### 顺带说明（另一个"黑屏"传闻无关的坑）

把 jar 换进 `mods` 目录**必须在游戏完全退出后做**。运行中替换文件会让 ModLauncher
按旧偏移去读新文件，表现为"某个类明明在包里却 ClassNotFoundException"，同样会死在
加载阶段（实测五百四十六 那次的崩溃日志就是这种）。

## 实测五百四十七【空袭牵引绳：空袭期间离主人超出半径（默认 100 格）就立刻传送回主人身边】

### 一、"自己回不来"是怎么来的（三道路闸全对她关着）

她会飞高是机制决定的：两种空袭都是「放烟花给初速 → 爬升 → 收翅俯冲 / 空中盘旋」，一轮打完
人已经挂在几十上百格高空，而主人早就不在脚下。此前有三条"远距拉回"链路，它们**全都主动
放行空袭**：

| 链路 | 平时怎么触发 | 遇到空袭时 |
| --- | --- | --- |
| 同维度远距拉回 | 同维度、距离超过 48 格 | **不拉**（实测四百八十七 加的：怕打断扑击） |
| 跨维度跟随 | 主人换了维度 | **不传**（滑翔中不传，猛击落地后自然恢复） |
| TLM 原版 teleportToOwner | 离主人过远自动瞬移 | **被拦**（MaidTeleportPreserveMixin 在滑翔 / 飞行作战空中时直接返回 false） |

于是"飞太高、把目标打死后自己回不来"这条路径上谁都不拉她——她只能一路滑翔到落地，落点
可能在几百格外的山头上。这不是哪一处写错了，而是三条豁免叠起来的必然结果。

### 二、这一版加了什么

一句话：**空袭期间，以她为圆心、半径 N 格（默认 100）的球内找不到主人 → 立刻把她传送回
主人身边**，走的是排班表那次人工传送同一条链路（即实测五百四十六 的「强制 + 无视地块 +
可以空中传送」）。

- 新配置 **「空袭牵引绳（格）」**，默认 **100**，**填 0 = 关闭**。位置：配置面板 →
  「生存与复活 → 落地缓冲」小类（和「免疫鞘翅撞击 / 空袭免疫摔落 / 远程空袭近身弹开」放一起）。
- 距离按 **3D 距离**算（水平 + 竖直一起算）——所以"飞太高"本身就会触发：她爬到主人头顶
  100 格以上就出了球，那正是"飞太高回不来"的字面含义。
- 落点：先在主人身边 16 格内找可站立格；找不到就直接落在**主人当前所在格**（与排班表人工
  传送完全同一口径：主人飞在高空 / 悬在虚空边也照传）。
- 触发时主人会收到一条系统消息，并落一条 `[空袭牵引绳]` 运行日志（距主人多少格、半径多大）。

### 三、只在"她确实在空中"时生效

面板文案里那句"只在空中生效"不是偷懒，是刻意划的边界：

- **地面上的距离问题不归它管**——落回地面后交给"同维度拉回"那套更保守的规则（48 格，且
  守家 / 坐姿 / 骑乘 / 干活都有豁免）。否则"主人出门 100 格、空袭女仆守家站着不动"会被
  无条件拽走；
- 空中 = 空袭正在进行，这正是本条要管的窗口；
- 正因为已经有"空中"这道门，本入口**不查 home（在家）模式**：停放的语义已经被"她飞起来了"
  这件事本身打破；而我们的排班表**会自动给女仆锚定 home**，如果照抄 home 那道门，结果
  恰恰是"用了排班的那批女仆永远召不回"——正是报上来的场景；
- **主人跨维度时不抢**：那一路由跨维度跟随在本轮攻击结束后处理，在这里立刻抢会重演
  实测四百八十七「猛击被传送打断」的老问题。

### 四、实现（给日后排查用）

- 新增 `com.maidsmart.combat.MaidFlightRecall`：每 tick 的判定全在这里（是不是空袭任务 /
  她是否在空中 / 主人是否已加载且存活 / 是否同维度 / 3D 距离是否出球）。
- 新增 `MaidChunkLoadManager.recallFromFlight(女仆, 主人)`：复用现成的
  `teleportCore(女仆, 主人, force = true)`，与「一键集合 / 传送到我身边」同一条真传送链路
  （原版 teleportTo + 摔落距离归零 + 停导航 + 清速度 + 末影人音效）。
- 调用点挂在 `MaidToolAutoEquipBehavior.checkExtraStartConditions` 的飞行任务分支——
  core 行为，任何 activity、每 tick 一次。挂那里有个额外好处：本模组给所有有主活动女仆挂了
  2 级强制加载票（= 实体正常 ticking），所以哪怕她离主人几百格、区块早已出了模拟距离，
  她的 brain 照样在跑，这条牵引绳才真的"立即"。
- 反过来说：空袭行为本身要"有攻击目标"才 tick，而本条要管的恰恰是**目标已经死了之后**
  她还挂在几百格高空的那一段，所以这条判定不能写在空袭行为里。
- 失败保护：传送失败（主人实体不在已加载世界等）后 60 tick 内不重试；系统消息 10 秒最多
  一条（成功召回后她人已经在身边，不会连发）。
- 手册"飞行作战"章节与配置面板文案同步更新；那句"飞行期间禁止被传送（包括跟随拉回、一键
  集合）"现在补了一句"**唯一的例外就是空袭牵引绳**"——飞出 100 格早已超出任何扑击的有效
  范围，那一步是把她捞回来、不是打断她。


## 实测五百四十六【自动复活：去掉内置 CD、重生点不可用就强制落在主人身边；排班表传送同样强制 + 无视地块】

### 一、"卡住无法复活"是怎么来的

三样东西叠在一起：

1. 复活落点原本是「主人重生点（床/重生锚）→ **无则主世界出生点**」。出生点往往是另一个坐标
   （甚至另一个维度）的荒郊——女仆落在那儿可能立刻又死；
2. 她"复活后很快又死"会踩到 实测四百一十六 加的**死亡循环断路器**：把下次复活**推迟**
   60 秒，连续则 120 / 240 / 480 / 600 秒封顶；
3. 那个断路器的提示语写的是「已把下次复活推迟 60 秒」——而实际退避是**翻倍**的，
   玩家看到的就是"说要等 60 秒，然后一直不来"。

### 二、改法

| 位置 | 旧 | 新 |
| --- | --- | --- |
| 复活落点（重生点不可用） | 退回主世界出生点 | **强制落在主人当前所在位置**（不看地块，主人在高空/岩浆边/水底也照落） |
| 死亡循环断路器 | 硬编码退避 60→600 秒 + 那条"推迟 N 秒"的提示 | **整段删除**（不再有任何内置 CD） |
| 复活提示语 | 无论落在哪都说"正在主人重生点等你" | 按实际落点分流：落在重生点 → 原话；重生点不可用 → "重生点不可用（床被拆/重生锚没电/维度不允许），**已直接在你所在的位置复活**" |

理由：主人站的地方就是玩家自己站的地方，是这颗星球上最不会让她"一放出来又死一次"的落点；
这也是"复活到她该在的人身边"这个功能的本意。断路器删掉之后，"复活点不安全"不再被掩盖成
"等一会就好"，而是从根上不再发生。

**注意**：`复活延迟（秒）` / `复活时机（次日黎明）` 这两个**配置项保留不动**——去掉的是那个
硬编码的退避 CD，不是玩家自己设的复活延迟。（复活延迟正是"墓碑存在多久"的时长。）

### 三、排班表传送：强制 + 无视地块

`MaidChunkLoadManager.teleportCore` 原本在「主人身边 16 格内找不到可站立格」时直接**拒绝**，
于是主人飞在天上、悬在虚空/海面上时，点「传送到我身边」只会收到一句"落地后再点"。
现在给传送加了 `force` 口径：

- **人工传送走强制**（「传送到我身边」/「一键集合」以及未加载区块的自动补召回）：
  找不到可站立格就直接落在**主人当前所在格**——主人飞着她就落到空中，这正是
  "无视地块、可以空中传送"。（空袭女仆悬停/滑翔时也能被叫回来。）
- **自动路径保持非强制**（受困救援 / 同维度远距拉回）：这两条是系统自己发起的，
  仍然"宁可她走路归队，也不冒半空坠落的风险"，不受本次改动影响。
- 「**把自己传到女仆身边**」的跨维度分支原本也会因为"她脚下没找到可站立的位置"而**取消传送**
  （同维度分支早就有"退到她那一格"的兜底，跨维度漏了）——现补上同一个兜底。

顺带把几条不再成立的提示语改掉（"你身边 16 格内无可站立点（高空/虚空）——落地后再点"、
"N 名因身边无可站立点未动" → 改为状态异常的通用失败说明）。

### 验证

两树编译零错，jar 已重建并部署三个目标。本轮**没有跑行为测试**，留待进游戏实测。

## 实测五百四十五【投喂效果与"女仆自己吃"对齐：补上 TLM 那一记吃食物回血】

### 根因：TLM 的"吃食物回血"不在 eat() 里，而在**餐食系统**里

女仆自己吃食物时，回血发生在 TLM 的 `MaidHealSelfTask` → `DefaultMaidHealSelfMeal.onMaidEat`
（反编译两版同构）：

```java
// 1.20.1：total = 营养 + 营养 × 饱和度系数 × 2
// 1.21.1：total = 营养 + saturation()（那边的 saturation() 已含 ×2，两版等价）
float total = ...;
if (random.nextInt(5) < total) {          // MAX_PROBABILITY = 5
    maid.heal(Math.max(total / 5.0f, 1.0f));
}
```

**一根胡萝卜**：`nutrition=3`、`saturationModifier=0.6` → `total = 3 + 3×0.6×2 = 6.6`
→ `6.6/5 > 1` 即 **100% 触发** → 回 `6.6/5 = 1.32` 点血。

而本模组的投喂走的是**原版 `LivingEntity.eat`**（`feedSisterFood` / 蜂蜜分支都一样）——
那条路只做"食物自带效果 + 音效 + 饱食度"，**完全不经过 TLM 的餐食系统**。
也就是说：TLM 那一记回血在投喂时**整段缺失**，于是"喂胡萝卜只回了原版那点效果 /
喂蜂蜜只解了毒"，观感就是"喂食比她自己吃弱"。

### 修法：喂食后补调一次 TLM 的 HEAL_MEAL（不自己实现公式）

新增 `com.maidsmart.combat.MaidMealBridge`：走 TLM 自己的接口
（`MaidMealManager.getMaidMeals(HEAL_MEAL)` → `canMaidEat` → `onMaidEat`），于是

- 回血**公式、概率、上限、粒子、饰品（bauble）联动**全部与她自己吃**逐行同源**
  ——TLM 以后改公式我们也自动跟着变，不会再出现"两套回血口径"；
- **黑名单也自动一致**：`MaidHealMealsBlockList` / `HEAL_MEAL_REGEX` 里被禁的食物，
  喂食同样不给回血（与"她自己不吃这个"完全同一个判据）。

接入点三处：姐妹喂普通食物（`feedSisterFood`）、姐妹喂蜂蜜（蜂蜜也是食物、nutrition=6）、
即所有"喂给**女仆**"的入口。**主人链不动**——玩家走原版饥饿系统，
TLM 那记回血是女仆专属的餐食系统，套到玩家身上没有语义。

### 三个必须踩对的细节

1. **必须传快照**。反编译 `LivingEntity.eat`：末尾 `stack.shrink(1)`（`m_41774_`）。
   如果直接把 `toGive` 传给 Bridge，eat 之后它已经是**空栈** → Bridge 判空直接返回 →
   修复静默失效。所以先 `copy()` 出"喂进去的那一份"再调 eat。
2. **金苹果刻意不补**。TLM 自己吃金苹果也只走 `eat()`（`ApplyGoldenAppleEvent`，
   玩家喂她时触发），同样没有那记回血——所以投喂金苹果若补上 HEAL_MEAL 反而会
   **强于**她自己吃。现在的口径是"与她自己吃完全一致"，金苹果维持原样。
3. **失败要留痕**。若 TLM 未来改了 API（拿不到任何 HEAL_MEAL 实现），会退化成
   "只有原版 eat"的旧行为——这正是最难发现的那类静默缺失，所以加了**一次性**告警日志。

### 为什么还加了一条调试命令

"女仆喂女仆"发生在两个实体之间、不产生任何原版事件，而且互助链要求
"被喂的那只有主人、且主人在线"——**专用服务器上没有玩家，这条链结构上无法端到端触发**。
所以本次同时留了一个控制台可用的验证入口：

```
/maid_smart feedtest <itemId>        # OP；对最近一只未满血的女仆结算一次投喂并打印血量变化
```

它走的是与 `feedSisterFood` **完全相同的调用序列**（eat 快照 → eat → Bridge）；
**不消耗任何物品**（用新造的物品栈），所以它不是玩法入口，纯验证。

### 验证：新增行为级回归 `test_feed_heal.py`（两版 PASS）

```
verdict: PASS
note: Bridge 命中 HEAL_MEAL=True
note: 血量 12.00 → 13.32（+1.32）
note: 回血幅度 = 1.32（胡萝卜 total=6.6 → heal(1.32) 的 TLM 口径）
[promaid/投喂回血] FeedTestMaid 按 TLM 餐食口径结算 Carrot（DefaultMaidHealSelfMeal）：血量 12.00 → 13.32
```

`+1.32` 正是"她自己吃一根胡萝卜"的回血量——**数字对上了**，这才是"效果等同"的实证。
加上原有六项（`test_maid_load` / `test_spin_air` / `test_server` × 两版）共八项全绿。

**脚手架又踩到四个坑（都已修，留档）**：
①先造"满血女仆"再喂 → `heal()` 被原版上限吃掉 → `20.00 → 20.00` 假失败，必须先造成伤害；
②测试世界是持久化的，残留的**满血**女仆会被选成目标 → 命令改为"取存活中血量最低的那只"；
③按血量升序又选中了 **0 血的残留尸体** → 追加"只取存活"过滤；
④Brigadier 的字符串参数：`word()` 不接受冒号（`minecraft:carrot` 断在冒号）、
   `string()` 要求带引号 —— 只有 `greedyString()` 能吃下裸的物品 id。

## 实测五百四十四【激流：把空袭"收翅俯冲"那一记也换成旋转冲击（空中放行）】

### 改了什么：只开**一个**空中窗口

起手入口从两个变成三个，前两个的护栏**一条都没动**：

| 入口 | 位置 | 起手条件 |
| --- | --- | --- |
| TLM 原生近战 `MaidMeleeRiptideMixin` | 攻击模式 | 仍要求**站在地上** |
| 空袭地面近战 `groundMelee` | 空袭落地后 | 仍要求**站在地上** |
| 空袭**收翅俯冲** `smashHit` | 空袭扑击 | **允许空中**（本次新增） |

"收翅俯冲"本来就是她主动朝敌人砸下去的那一瞬，所以放行它不会碰 实测五百四十一 的护栏
（那道护栏防的是"在起跳/爬升途中横插一次突进"）。`airborne` 取的是 `!onGround()`，
所以她落地之后这一记自动退回地面规则。

### 空中与地面的三条差异（都有理由）

1. **不提前刹住**。地面那版一旦离目标 1.5 格内就把速度归零（免得"顶着怪推"）；空中照用会
   停在半空自由落体，而且 1.5 格根本够不到怪的碰撞箱 → 转一整套也不结算。空中改成只判
   "越过目标所在的那条线"，一路扫过它的碰撞箱——原版 `checkAutoSpinAttack` 的判定是
   **上一帧到本帧的包围盒并集**（扫掠），扫过去就必定结算一次，这正是玩家那记"撞上去"。
2. **重力照常**。反编译实证原版对旋转突进**没有任何**改重力/改速度的处理
   （`LivingEntity` 里 `f_20938_`/`autoSpinAttackTicks` 只出现在 `aiStep` 的递减与
   `checkAutoSpinAttack` 两处），所以空中旋转 = 一次朝敌人的冲量 + 正常抛物线。刻意不造悬停。
3. **坠落伤害由本链路兜掉**。既然重力照常、旋转又有 16 tick，她是边转边掉的；
   突进期间每 tick 把坠落距离清零（与收翅猛击同款口径），落地不自我摔伤。

### 空袭让位（这次是"收窄过"的让位）

突进那 16 tick 里，空袭**只让掉状态机**（`isDashing` 为真时提前 return，顺带收翅、压住原生近战）。
**必须让**：空袭同一 tick 会放烟花（推力沿视线）+ 开滑翔（`travel` 的滑翔分支把水平速度往视线
方向拽并限速），三套速度互相覆盖的结果是"刚起手就被冲掉，只闪一下"。
**这次不会重演 五百四十一 的"不会起飞"**：①起手时机 = 她的攻击时机（有攻击冷却 + 收招硬直，
不是按距离自主触发）；②让位有硬上限（一次旋转的时长 + 陈旧自愈）；③让位放在三件套维护与
"缺件提示"**之后**——装备照穿、缺件照报，被让掉的只有状态机本身。
配套把 `isEngaged`（"空中禁传送"的豁免）并入突进状态，免得主人一远、自动传送把她当场拽走，
白转一场。

### 顺带修的两件事

1. **1.21.1 树的常量漂移**：那份 `MaidTridentSpinBehavior` 的 `DASH_RANGE/SPIN_TICKS/RECOVER`
   还停在 实测五百三十八 那版（10/20/10），而 五百四十一 的定稿表格写的是 4/16/20——
   即 1.21.1 上仍是"从十格开外一路冲过去"的诡异口径。两树现已一致。
2. **突进状态的漏清**：`MaidTridentSpinBehavior.forget()` 定义了两版却从未接线。它现在会让
   空袭让位 + 豁免传送，残留的代价比之前大得多，已接进既有的两处统一清理点
   （登录清理 / 退图清理）——与 实测四百八十三 修滑翔位残留是同一类问题。

### 验证：新增一条**行为级**回归 `test_spin_air.py`

原有两条检查**结构上**覆盖不到本次改动（`test_maid_load.py` 只证明"女仆能被召唤出来、
mixin 没炸"；`test_server.py` 只等 `Done (`），而本次动的恰恰是 五百四十一 出过事的链路。
所以新增：`summon` 一个空袭女仆（NBT 指定任务 + 主手**激流**三叉戟 + 背包鞘翅/烟花）
+ 一只打不死的木桩，观察日志里是否**既起飞、又能空中旋转**。

```
verdict: PASS
note: 起飞=True 空中旋转=True
note: 武器上读到 riptide=True
[promaid/激流突进] SpinTestMaid 旋转冲击（空中起手，激流 3 级，力度 3.00，距离 2.9，目标 SpinTestTarget）
[promaid/飞行作战] SpinTestMaid 放烟花起飞
[promaid/激流突进] SpinTestMaid 旋转冲击（空中起手，激流 3 级，力度 1.8，目标 SpinTestTarget）
```

两版都是"起飞 ↔ 空中旋转"交替（不是卡在某一态），另加 `test_maid_load.py` /
`test_server.py` 两版 PASS，共六项全绿。

**脚手架上的三个坑（都已修，留档以免重踩）**：
①两版服务端日志编码不同（1.20.1 UTF-8 / neo GBK），且 neo 那份是 GBK 夹非法字节，
   必须 `errors='replace'` 解码——否则中文判据全部假失败，现象是"没起飞"而日志里明明有；
②两版附魔 NBT 完全不同：1.20.1 是 `tag.Enchantments`，1.21.1 是
   `components.minecraft:enchantments.levels`（`ItemEnchantments.CODEC` 优先认带 `levels` 层的形式）。
   写错时**不报错、只是没有附魔**，看起来就像"功能没生效"——已加 `data get` 回读自检；
③测试世界是持久化的，`@e[...,limit=1]` 会选中**上一轮**的女仆，选择器一律按名字限定。

### 一个待确认的点

空中旋转期间她**会掉**（这是原版语义：不抵消重力）。按现在的参数，一次 16 tick 的旋转从
起手高度掉约 6~13 格，随后空袭自己切回滑翔接住。若认为"掉得太快、看不全那几圈"，
两个可调量：`MaidTridentSpinBehavior.SPIN_TICKS`（缩短旋转）或 `MIN_UP/MAX_UP`（起手更朝上）。

## 实测五百四十三【那圈"激流特效"：扒出来是官方的一个独立图层，已照抄给女仆】

### 扒出来的结果：方向对了，但它既不是粒子、也不是玩家建模/动作

它是原版挂在 `PlayerRenderer` 上的一个**独立渲染图层** `SpinAttackEffectLayer`。两版源码同构：

```java
if (!entity.isAutoSpinAttack()) return;                       // 唯一门槛：LivingEntity 的方法
VertexConsumer vc = buffers.getBuffer(RenderType.entityCutoutNoCull(
        new ResourceLocation("textures/entity/trident_riptide.png")));
for (int i = 0; i < 3; ++i) {
    poseStack.pushPose();
    poseStack.mulPose(Axis.YP.rotationDegrees(ageInTicks * -(45 + i * 5))); // 45/50/55 度每 tick 三个反向自转
    float s = 0.75f * i;                                    // i=0 时 s=0（退化不可见），实际两层
    poseStack.scale(s, s, s);
    poseStack.translate(0.0f, -0.2f + 0.6f * i, 0.0f);      // 上下错开 0.6 格
    box.render(poseStack, vc, light, OverlayTexture.NO_OVERLAY);
    poseStack.popPose();
}
// box = CubeListBuilder.addBox(-8,-16,-8, 16,32,16)  ← 一个 16×32×16 的大方块
```

**所以那圈东西 ＝ 一个贴着"白色斜纹、透明底"贴图（`trident_riptide.png`）的大方块，
以三种速度反向自转。** 它不在玩家模型里、也不是玩家的动作，门槛只是
`LivingEntity.isAutoSpinAttack()` —— 这反而是好消息：**可以直接照抄给女仆**。
（贴图在原版资源包里就有，两版都确认存在，我们不需要打包任何素材。）

### 复刻做了什么

新增 `MaidSpinAttackEffect`（几何/贴图/三层方块的角度·缩放·位移**逐行照抄**原版），
再按两套模型体系各挂一个图层：

| 模型体系 | 图层 | 挂载点 | 坐标处理 |
| --- | --- | --- | --- |
| Bedrock（TLM 内置模型） | `LayerMaidSpinAttack` | `EntityMaidRenderer` 构造末尾（与鞘翅图层同一入口） | 与原版**同一个空间**，原样照抄 |
| Gecko（酒狐家族等） | `LayerMaidSpinAttackGecko` | `GeckoEntityMaidRenderer` 构造末尾 | 先 `scale(-1,-1,1)` 翻进原版模型空间（反编译实证：Gecko 图层跑在"格、Y 朝上、原点在脚下"的空间） |

判据用**原版那个已同步的旋转标志位**（`isAutoSpinAttack`）：它随实体数据同步到客户端，
而本模组那份"是否正在突进"的状态在服务端、客户端读不到；突进结束时我们会显式清标志位，
所以不会出现"没转却一直冒特效"。

### 与实测五百四十二 的关系

542 把旋转的**枢轴**抬高了 0.6 格（平躺后不再有一半埋在地面以下）；这圈特效是画在
"已经施加过旋转的坐标系"里的，所以两者叠加后观感才对：身体离地一段高度、斜纹方块绕着她转。

### 验证

两树编译零错；包内确认三个新类与 mixin 注册都在；`test_maid_load.py` / `test_server.py`
两版 PASS。**注意**：这是纯客户端渲染，服务端那几条检查覆盖不到，最终观感要在游戏里看。

## 实测五百四十二【激流旋转：把枢轴抬高，别像嵌进地里】

### 为什么偏低（几何上算得出来）

原版施加激流姿态是**绕实体原点（脚底）**转的——`LivingEntityRenderer.setupRotations` 里只有
两次 `mulPose`（`XP(-90 - 俯仰)` + `YP(-75 × tick)`），**没有任何平移**。女仆的模型是从脚下往上
建模的（身体占 y ∈ [0, ~1.8]），平躺之后这段"高度"变成了身体的"前后厚度"（约 ±0.3），于是整个
身体成了一块**以脚底高度为中心的薄片**：下半沉进地面、上半露在外面。平地勉强能看，**斜坡 / 台阶
上一转就像嵌进方块里**。玩家没有这个观感，因为玩家的激流只在水里 / 雨里、身体本来就不贴着地面。

### 修法（纯客户端渲染，机制零改动）

在旋转**之前**（矩阵还是"Y 朝上、原点在脚下"的时候）先把模型整体抬起 **0.6 格** —— 抬的是
**枢轴**，平躺的身体就以"离地 0.6 格"为中心了。新增 `MaidSpinLiftMixin`，注入**原版**
`LivingEntityRenderer.setupRotations` 的 HEAD：Gecko 与 Bedrock 两套模型最终都走这一层，
一处覆盖两边；判据与数值在 `com.maidsmart.client.SpinLift`（只对**正在旋转的女仆**生效，
其余实体 / 状态原样返回）。0.6 是唯一调节量：嫌"像在飘"调到 0.3 左右，嫌"还是压地"就调大。

### 顺带回答："空袭状态下可以触发激流吗？"

当前口径：**只有站在地上时才会**（攻击模式与空袭任务都一样）。空袭的空中链路（起跳滑翔 →
烟花助推 → 收翅俯冲）刻意完全不碰 —— 那正是上一版"不会起飞"的来源；空袭里她**落地之后的
近身战**才会用旋转冲击。想让她在空中（例如收翅俯冲那一下）也能转，说一声，我按那个窗口单独放行。

### 验证

两树编译零错；包内确认新 mixin 与注册都在；`test_maid_load.py` / `test_server.py` 两版 PASS。
**注意**：这条是**纯客户端渲染**，服务端那两条检查跑不到它（`LivingEntityRenderer` 是客户端类），
实际观感要在游戏里看。

## 实测五百四十一【激流改成"替代普通挥砍"；修"不会起飞"与"行动诡异"；去掉旋风特效】

### 一、为什么"不会起飞"（实测五百三十八 引入的回归，两条都是我这边的）

日志实证：13:44–13:45 那一串"旋转冲击（距离 1.5～9.9）"说明当时的版本**在空中也会发起突进**
（只排除了滑翔），加上触发距离 10 格、一轮只要 30 tick —— 她**大部分时间都在突进**；与此同时
`MaidFlightCombatBehavior.tick` 开头有一条"突进期间整段让位"的早退，于是"起跳滑翔 → 放烟花助推"
被反复打断，表现就是**不会起飞**。更糟的是：那个"正在突进"的状态**只要泄漏一次**（任务切换 /
大脑重建 / 被收回时行为没跑到收招），她就永久被判定成"正在突进"，**换什么武器都一直卡着** ——
这正是"换成别的武器也一样"。

### 二、修法（都是"把不该由突进插手的地方还回去"）

1. **突进只在站在地上时发起**（不在地面直接拒绝）→ 空中的空袭链路完全不受影响；
2. **删掉空袭里那条"让位"早退**，空袭状态机永远自己跑；
3. **突进状态自愈**：起手后超过一次旋转的时长还没结束（＝起手了但行为没能跑起来），
   下一次判据里直接丢弃它 —— 再也留不下"永久卡住"的状态。

### 三、触发判定改成"就是她的普通攻击"（回应"判定很奇怪"）

旧版是"目标进 N 格就自主起手"，所以会出现"什么时候触发说不清、还被跳劈等别的行为抢先"。
现在**起手只有两个入口，都是"本来要挥砍的那一记"被替换掉**：

| 入口 | 说明 |
| --- | --- |
| `MaidMeleeRiptideMixin` → TLM 原生近战那一记 `doHurtTarget` | 起手成功 = 这一记算打出去了（TLM 照常吃攻击冷却）；起不了手就**原样调回普通挥砍** |
| 空袭的地面近战 `groundMelee` | 同上，站在地上时才替换 |

共同前提：开关开 ＋ 攻击模式 / 空袭任务 ＋ 主手**激流**三叉戟 ＋ **站在地上** ＋ 目标不超
4 格 ＋ 不在突进中 ＋ 收招硬直已过。**起手时机 = 她的攻击时机**，其余时间这个行为什么都不做
（不再自己找目标）。同时把上一版放大的参数收回来：

| 参数 | 538 那版 | 现在 |
| --- | --- | --- |
| 触发距离 | 10 格 | **4 格** |
| 单次旋转 | 20 tick | **16 tick** |
| 收招硬直 | 10 tick | **20 tick** |

### 四、去掉旋风特效（实测五百三十九 回退）

参考图放大看过：主体是"**横躺自转的本体 + 被甩起来画圆的手持三叉戟 + 白色斜向拖尾**"，
主角其实是被甩起来的武器与它的拖尾 —— 靠粒子拼很难忠实还原。既然直接还原做不到，
就把 539 加的那圈粒子（`sweep_attack` / `electric_spark` / `small_gust`）**整段删掉**
（方法、常量、两处调用，两树都删干净）。现在旋转只剩"与玩家同款的本体自转 + 平躺"
（实测五百三十七 的渲染修复），不再有自造粒子。

### 验证

两树编译零错；`test_maid_load.py` 两版 PASS（女仆真的召唤出来、无 mixin 报错）；
`test_server.py` 两版回归 PASS。桌面那份 v1.2.0 更新文案草稿同步到最新。

## 实测五百四十【修：持激流三叉戟切攻击模式 → 女仆 AI 直接死、收回魂符再放下会消失】

### 根因（是我的锅，日志铁证）

实测五百三十八 新增的 `MaidMeleeRiptideMixin` 注入 TLM 近战任务时，**回调方法写成了非静态**，
而它的注入目标 `MaidMeleeAttack.lambda$create$0` 是 **static** 方法。Mixin 的规则是
"静态目标方法只接受静态回调"，于是在 `MaidMeleeAttack` **首次被加载**（＝女仆生成、
TLM 构建大脑的那一刻）直接 FATAL：

```
[Server thread/FATAL] [mixin/]: Mixin apply for mod promaid failed mixins.promaid.json:MaidMeleeRiptideMixin
    -> com.github.tartaricacid.touhoulittlemaid.entity.ai.brain.task.MaidMeleeAttack
org.spongepowered.asm.mixin.injection.throwables.InvalidInjectionException:
    non-static callback method ...MaidMeleeAttack::promaid$riptideReplacesMelee
    targets a static method which is not supported
```

`MaidMeleeAttack` 变形失败 → 女仆的大脑构建不出来 → 表现就是"AI 直接死、一动不动"；
从魂符再放下来会在同一处再炸一次，于是**女仆当场消失**。

注意：这里 `require = 0` 救不了 —— 它只容忍"没匹配到"，不容忍"回调签名非法"。

### 修法

两树的回调都改成 `private static`（静态回调仍然照常把 receiver 作为第一个参数收下）。
`javap` 已确认两个包里的方法都带 `static` 修饰。

### 为什么上一轮的服务端回归没抓到（重要教训）

`test_server.py` 只等 `Done (`，而 `MaidMeleeAttack` 这个脑任务类**只有在女仆实体生成、
TLM 构建大脑时才会被加载** —— 所以那次回归"全绿"却根本没碰到这条注入路径。

新增 `test_maid_load.py` 补上这个盲区：起服 → 等 Done → 控制台 `summon touhou_little_maid:maid`
→ 盯日志 → 判据 = **真的召唤成功** ＋ **没有任何 mixin 应用失败 / `MaidMeleeAttack` 报错**。
两版现在都 PASS（都有 `Summoned new ...` 且无 FATAL）。以后凡是要动 TLM 大脑行为类的注入，
都得先过这条检查。

### 顺带清掉的隐患

两个测试服的 mods 目录里还躺着旧包（`promaid-1.1.1` / `promaid-test` / `promaid-1.1.0`，
同 modId 双包会让"测试到底加载了哪一份"失去意义），已移到
`patched/backup_old/stale_server_jars/`（是移走不是删除）。

### 关于已经消失的那台女仆

那次消失是实体被移除导致的：如果魂符里还留着她的数据，用修好的包再放下来应当正常；
如果魂符已经空了，那台女仆的存档数据我这边找不回来 —— 这是我的 bug 造成的，很抱歉。

## 实测五百三十九【激流旋转补上"周身旋风"特效】

### 之前为什么没有

实测五百三十七 让**本体**的旋转回来了（TLM 把原版那对旋转反号抵消掉，已修），实测五百三十八 让这 20 tick
放得完、看得清。但玩家的观感其实由两部分组成：**① 横躺高速自转的本体**、**② 被甩起来绕体画圆的手持物
与粒子拖尾**。女仆这边 ① 有了；② 里的手持物由 TLM 的模型图层负责、也会随模型一起转，**但粒子拖尾我们
一颗都没生成** —— 所以看上去还是"只有音效"。

### 补了什么

旋转期间（起手 + 每 tick）在服务端就地把"绕着她转的旋风"画出来，附近玩家都看得到：

| 粒子 | 用途 |
| --- | --- |
| `sweep_attack`（白色弧刃） | 两条对置、每 5 tick 转一圈 —— 旋风的主体观感 |
| `electric_spark`（青白火花） | 反向、半径更小的第二环，呼应原版激流的青蓝色 |
| `small_gust`（小风旋，1.21.1）/ `cloud`（白雾，1.20.1） | 贴着她本体的那一圈"风"（1.20.1 没有 gust 系粒子，那是 1.20.5+ 才加的） |
| `crit` + `gust_emitter_small` | 起手与每 6 tick 一圈爆点，让"发动"这一下看得见 |

粒子只是观感，全部包在 `try/catch` 里：任何异常都不影响攻击结算。两树的粒子字段名都经过字节码核对
（1.20.1 是从 `ParticleTypes` 静态初始化的"注册名字符串 → 字段"配对里读出来的，1.21.1 按官方名并核对了
声明类型），不是猜的。

### 验证方式

两树 `javac` 编译零错；进游戏看到"旋转 + 绕体旋风"同时出现即算通过。

## 实测五百三十八【激流三叉戟：独立攻击链路（旋转冲击），并让旋转真的看得见】

### 先查清"为什么上一个版本还是看不清"

实测五百三十七 修的是**渲染被抵消**（TLM 的 `GeoReplacedEntityRenderer.setupRotations` 把原版那对
反号重复旋转消成了单位矩阵），那条修复本身是对的、也确实生效了 —— 客户端 `debug.log` 有实证：

```
[12:46:21] [Render thread/DEBUG] [mixin/]: Mixing MaidGeckoSpinDedupMixin from mixins.promaid.json
    into com.github.tartaricacid.touhoulittlemaid.geckolib3.geo.GeoReplacedEntityRenderer
```

但实测记录（`promaid.log`，12:47–12:49）每次都写着"激流 3 级，力度 3.00"，而**触发距离只有 5 格**：
3 格/tick → **约 2 tick 就撞上目标**；而"撞到实体"按原版 `checkAutoSpinAttack` 会**立刻收招**
（旋转计数清零 + 速度乘 −0.2）。20 tick 的旋转实际只放了 2 tick ≈ 0.1 秒，所以"动作也不明显"。

顺带取证：**原版玩家的旋转没有任何粒子特效**（`LivingEntity` 与 `ItemInHandRenderer` 里
`isAutoSpinAttack` 附近都没有 `addParticle`；后者那条只是第一人称手里三叉戟的翻转）。玩家看到的
"特效"就是**身体高速自转 + 平躺**本身 —— 所以这次要解决的是"让它放得完、看得清"。

### 改了什么（两树各一份）

**① 触发距离 5 → 10 格**（`DASH_RANGE`）。攻击既然就是"冲过去撞"，就得留出冲刺距离。

**② 突进期间每 tick 顶住标志位与速度**（`MaidTridentSpinBehavior.tick`）。原版"撞人即收招"是给
"玩家自己瞄着飞"设计的；女仆每次都朝怪撞，照搬就永远看不到旋转。现在：

- 每 tick 把剩余 tick 与标志位 4 重新写回 → **20 tick 的旋转画面完整放完**；
- 每 tick 按剩余比例重新给推力，**越过目标横线后停住**（不是一路滑出去几十格）→ 冲上去撞，
  然后贴着怪把这一圈转完；
- 撞到横向方块仍照原版提前收招。

**③ 同一目标每次突进只打一下**。标志位被顶住后，原版"撞击即收招"那条天然的一次性保护就没了，
所以 `onSpinTouch` 按本次突进记 hit 集合去重（最终效果与原版一致：一冲一伤）。

**④ 攻击环节被替换**（即"替换原本的攻击环节"）。持激流三叉戟且在攻击模式 / 空袭下时，
普通挥砍不再出手：

| 原本的攻击步骤 | 现在 |
| --- | --- |
| TLM 原生近战 `MaidMeleeAttack`（`swing` + `doHurtTarget`） | 新增 `MaidMeleeRiptideMixin`：把它的"近战可达"判据（1.20.1 `m_217066_` / 1.21.1 `isWithinMeleeAttackRange`，两版都是 **public**）判成 false → 整段不发生（不挥砍、不结算、不吃冷却） |
| 空袭的俯冲猛击 `smashHit` | 换成 `tryStartDash`：起手成功就按"这次出手了"收尾俯冲 |
| 空袭的地面近战 `groundMelee` | 同上（吃同样的攻击冷却） |

`MaidMeleeAttack` 这个任务**只负责"打成"**，走路 / 面向 / 索敌 / 冷却记忆都在别的任务里 —— 需求要的
"其他的如索敌等不变"因此天然成立。种田、伐木等其它任务里拿激流三叉戟**照旧正常挥砍**
（`replacesMelee` 带了任务口径，不会把女仆变成不会还手的沙包）。

**⑤ 空袭的移动权交接**。`MaidFlightCombatBehavior.tick` 在突进期间直接返回（这一秒归突进：不抢速度、
不点烟花、不改滑翔位）；另外**滑翔中不发起突进**（鞘翅物理会与突进互相抢速度，空袭那边本来就是
收翅俯冲时才出手）。

### 现在的表现

拿激流三叉戟 → 切成攻击模式或近战空袭 → 怪进 10 格：她朝怪冲过去，**平躺 + 高速自转 20 tick（1 秒）**，
撞到结算一次（攻击力 + 附魔，含击退 / 火焰附加 / 荆棘），撞完停在怪身边把这一圈转完，10 tick 硬直后
再来一次。走路、索敌、排班、其它任务一律不变。总开关仍是 `combat.riptideDash`
（面板「激流三叉戟旋转冲击」，默认开）。

### 验证方式

- 两树 `javac` 编译零错；
- 新增注入点经 `javap` 实证：TLM `MaidMeleeAttack.lambda$create$0` 内的
  `EntityMaid.m_217066_(LivingEntity)Z`（1.21.1 为 `isWithinMeleeAttackRange(LivingEntity)Z`），
  两版同名同签名、目标方法 public 可直接回指；
- "渲染被抵消"的完整推导见 实测五百三十七 那一段（CFR 反编译 + `javap` 字节码，非推测）。

## 实测五百三十七【激流旋转终于转起来了；弩用普通烟花默认改关】

### 一、Gecko 模型女仆的激流：只有音效和伤害，没有旋转与俯卧（bug 修复）

**症状**：只有音效和伤害，没有玩家那样的旋转特效与动作。

**根因：TLM 自己那对旋转把原版的那对正好抵消了。**

原版 `LivingEntityRenderer.setupRotations` 早就实现了激流姿态，而且**不是 Player 专属**
（1.20.1 字节码 197-199 行、1.21.1 202-204 行）：

```java
} else if (entity.isAutoSpinAttack()) {
    poseStack.mulPose(Axis.XP.rotationDegrees(-90.0f - entity.getXRot()));
    poseStack.mulPose(Axis.YP.rotationDegrees((entity.tickCount + partialTicks) * -75.0f));
}
```

TLM 1.5.3 的 `GeoReplacedEntityRenderer.setupRotations` 在 `super`（＝上面这段）之后
**又叠了同轴反号的一对**（javap 实证：`invokespecial LivingEntityRenderer.setupRotations`
→ `invokevirtual LivingEntity.isAutoSpinAttack` → 两次 `mulPose`，常量 `75.0f`）：

```java
if (deathTime <= 0 && entity.isAutoSpinAttack()) {
    poseStack.mulPose(Axis.YP.rotationDegrees((entity.tickCount + partialTicks) * 75.0f));
    poseStack.mulPose(Axis.XP.rotationDegrees(90.0f + entity.getXRot()));
}
```

`mulPose` 是**右乘**，于是合成矩阵为

```
M = M0 · XP(a) · YP(b) · YP(-b) · XP(-a) = M0      a = -90 - xRot，b = -75·(tick + partial)
```

中间那对同轴 YP 相邻、直接消成单位矩阵，剩下的一对同轴 XP 也消成单位矩阵 →
**净旋转恒等于什么都不做**。所以走这个渲染器的模型（Gecko 系：酒狐家族等）突进时永远不转、
也不俯卧。存档实体 NBT 核实：这边的女仆正是 Gecko 模型（`geckolib:winefox` 系列、
`geckolib:kluonoa`）；TLM 内置基岩模型（`touhou_little_maid:*`，走 `EntityMaidRenderer`）
不经这里，本来就有原版那一份，不受影响。

**修法（两树）**：新增客户端 mixin `MaidGeckoSpinDedupMixin`，把 TLM 那次
`isAutoSpinAttack()`（1.20.1 `m_21209_`）读取重定向为 `false` —— 只让它那对重复旋转整段
跳过、把**原版那一份留下**，于是效果与玩家**完全同款**（不自己写角度、不重复叠加）。该 `if`
块里除这两次 `mulPose` 没有别的语句，且标志位只在突进中为真，所以不突进的女仆一个字都不
受影响。取 `require = 0`：万一 TLM 以后自己删掉这对（＝它修好了），我们自动不再干预；写成
`require = 1` 会把"TLM 修好了"变成启动崩溃，反而有害。

覆盖范围如实说明：只覆盖走 `GeoReplacedEntityRenderer` 的 **Gecko 模型**；YSM 模型由 YSM
自己的 `IGeoEntityRenderer` 渲染、不经这个类（TLM 只给它 `is_riptide` molang 变量），那条路
的旋转取决于 YSM 本体，本 mixin 管不到也不该乱管。总开关仍是 `combat.riptideDash`（默认开）。

### 二、烟花火箭"一点伤害都没有"：不是 bug，是原版机制；开关默认改关

**结论：不是你哪里出错，也不是版本差异 —— 原版任何版本里，没放烟火之星的普通烟花火箭
伤害恒为 0。** 两版字节码完全一致：爆炸结算先看 `Fireworks.Explosions`，**为空则整段早退**。

```java
// 1.20.1 FireworkRocketEntity.m_37087_（就是爆炸伤害的结算）
float dmg = 0.0f;
ListTag ex = "Fireworks" -> "Explosions";
if (ex != null && !ex.isEmpty()) dmg = 5.0f + (float)(ex.size() * 2);
if (dmg > 0.0f) { /* 才结算伤害 */ }

// 1.21.1 同名逻辑 dealExplosionDamage()
float dmg = 0.0f;
List<FireworkExplosion> ex = this.getExplosions();
if (!ex.isEmpty()) dmg = 5.0f + (float)(ex.size() * 2);
if (dmg > 0.0f) { ... }
```

顺带：`m_37086_`／`hasExplosion()` 用的是同一个判据，所以普通烟花**连撞到方块的爆炸都不
触发**，只冒烟（客户端 `m_7822_`／`handleEntityEvent` 那条分支）。

- 想要伤害就**放烟火之星**（星越多伤害越高：`5 + 2×星数`）；`/give minecraft:firework_rocket`
  给的是无爆炸的那种，永远 0 伤害。
- **女仆这条链路本身没问题**：`shootCrossbowFirework` 把取到的弹药栈**原样**塞进
  `new FireworkRocketEntity(level, ammo, shooter, …, true)`（内部 `stack.copy()`，NBT／组件
  完整保留），带星的烟花在她手里照样打出 `5 + 2×星数`。

**配置默认值改关**：`combat.crossbowPlainFirework`（面板「弩用普通烟花当弹药」）
`true → false`。理由就是上面这条：普通烟花当弹药＝白烧一枚飞行燃料（0 伤害）。默认改成
"只有带烟火之星的**攻击性烟花**才当弩弹药，普通烟花全留给飞行推进"；想退回原版玩家口径
（任意烟花都能当弹药）随时可打开。配置面板说明与游戏内指南同步改写（指南里补上
"普通烟花打出去本来就是 0 伤害"的依据）。

> **已存在的配置文件不会被新默认值覆盖**（Forge 只补缺失的键、不覆盖已有值）：1.21.1 实例
> 的 `config/promaid-common.toml` 里这一项原本是 `true`，已一并改成 `false`；1.20.1 实例与
> 两个测试服务器的同名文件也核对过（1.20.1 客户端那份当时还没有这个键，下次启动写入新默认）。

### 验证方式

- 两树 `javac` 编译零错（1.20.1 239 源 / 1.21.1 241 源）；
- 注入点描述符经 `javap -p -c` 核对（1.20.1 `m_7523_` 内 `LivingEntity.m_21209_()Z`、
  1.21.1 `setupRotations` 内 `LivingEntity.isAutoSpinAttack()Z`），并确认编译用的 TLM jar
  与运行期 jar 逐字节相同（1.20.1 `maidmods\original_tlm.jar`、1.21.1 `libs_neo` 副本）；
- 本文所有"原版／TLM 行为"结论均来自 CFR 反编译 + `javap` 字节码，非推测。

## 实测五百三十六【不得把方块搭在主人身上（尤其是头部）】

### 判据

`new AABB(pos).intersects(owner.getBoundingBox())`。边界语义已用字节码核实：

```java
// AABB(BlockPos) 构造 = 该格整格体积
x..x+1, y..y+1, z..z+1
// intersects(m_82381_) 内部走 m_82314_，六个分量全是【严格不等式】
this.minX < o.maxX && this.maxX > o.minX && ...
```

两个后果都是我们要的：

- **主人站在方块上**时，脚下那格与他的碰撞箱只是**相切**（脚底 y 恰等于该格顶面
  y+1）→ 不算被占 → 该在她脚下/身下垫的方块照垫，不会把正常搭路打断；
- **碰撞箱真正探进去的格**（身体格、头部格）才算被占 → 正是需求要防的
  "塞进主人身体里 / 盖住头部"。

女仆**自己**所在格不受影响（`placeStep` 把块垫在自己脚下、蓝图以她脚下为原点先垫
自己那格，都是设计意图）——闸口只判主人。

### 闸口

新增 `MaidPlaceGuard.blockedAtOwner(maid, pos)`（与 实测四百四十三 的
`blocked(maid)`"悬空禁搭"并列，同一工具类、同一风格）。配置项
`misc.noPlaceOnOwner`（默认开，面板：**系统与杂项 → 交互与杂项 → 不得搭在主人身上**）。

### 覆盖的落点（两树合计 52 处）

| 模块 | 落点 |
| --- | --- |
| 自保搭高 | 岩浆垫高、搭高、搭桥、头封块、盖帽块 |
| 搭路 | 平桥、下行台阶、斜上台阶、垂直垫柱 |
| 挖矿垫脚 | 垫高、斜坡台阶、下行台阶、搭桥 |
| 伐木垫脚 | 同上四类 |
| 插火把 | 目标格 |
| AI 工具 `smart_place` | 目标格（计一次失败，不消耗） |
| 蓝图建造 | 主循环 + 延后轮询 + 主支撑 + 附着类支撑 + 下方支撑 + 门上半 |
| 碑石建造 | 目标格 |

**闸口一律放在取料之前**——被拦时不扣方块、不动世界（`MaidBuildBlockFilter.takeBuildBlock`
内部就会 `extractItem`，放在取料后等于白烧一块）。

### 蓝图类为什么是"延后"而不是"跳过"

蓝图/碑石建造的语义是"把图纸填满"，跳过会永久留洞。所以：

- 主循环 → `prog.deferred.putIfAbsent(i, 0)`（与"区块未加载""缺支撑"同款延后）；
- 延后轮询 → `reorder.add(idx)` 沉底轮换；
- 碑石 → `return`（与它自己处理"区块未加载"同款：本轮退避、不丢格）。

主人让开后，下一轮自动回来续建——**不会因为主人站了一下就在建筑上留个洞**。

### 门上半

`ensureDoorUpper` 原来是 `static`、拿不到女仆，为此**加了 `EntityMaid maid` 参数**
（调用点同步改），门的上半扇也过同一闸口——旧版"主人站在门口时上半扇直接扣在他头上"。

### 副作用

无。默认值 = 修复方向（原来就是"会搭在主人身上"）。关掉开关即恢复旧行为。

## 实测五百三十五【弩用普通烟花当弹药：加开关（默认开 = 原版行为）】

### 开关

`combat.crossbowPlainFirework`（配置面板：生存与复活 → 落地缓冲 → **弩用普通烟花当弹药**）

- **默认开** = 现在（实为原版）的行为：任意烟花火箭都能当弩的弹药。
  依据仍是原版 `CrossbowItem` 的弹药谓词——它只看 `is(FIREWORK_ROCKET)`，
  不看烟花有没有 `Explosions` 组件；玩家拿弩装普通烟花照样能射。
- **关** = 只有带烟火之星的【攻击性烟花】才当弹药，普通烟花一律留给飞行推进，
  绝不被弩烧掉。

### 两处判据读同一个开关（否则会出死角）

1. `MaidFlightKit.hasAmmoFirework(maid)`——**判"有没有弹药"**（决定要不要起飞/开火）；
2. `MaidFlightKit.takeBestCrossbowFirework(maid)`——**实际取用**。

两边都读 `COMBAT_CROSSBOW_PLAIN_FIREWORK`。这是本文件反复强调的红线：
只改一边会出现"判定有弹药、起飞后一发打不出来"（或反之：明明关了却仍会烧掉普通烟花）。

### 两种模式下都保留"优先威力大的"

无论开关开关，取用一律按 `fireworkPower`（= 爆炸条目数，见 实测五百三十三）
**降序**挑：有带烟火之星的先打带烟火之星的，没有才轮到普通烟花兜底。
开关只管"普通烟花算不算弹药"，不管"先打哪一个"。

### 本次事故说明（如实记录）

这条开关的第一次实现中途丢失：我为修复一个被脚本补丁写坏的
`PromaidConfigScreen.java`，对两树的该文件执行了 `git checkout --`，
而这两个文件带着**未提交**的改动——于是 10 条界面行与整套"投喂食物勾选"
（食物表）功能被一并回退掉。

- 受损范围仅限这两个文件；配置字段、`MaidFlightKit` 的两处逻辑、
  四个 `lang/*.json` 条目都没被回退（它们不在那次 checkout 的路径里）。
- 恢复方式：从事故前的构建产物（`patched/` 里的 jar）里反编译出旧 class，
  按成员表逐条比对后补回丢失代码；已核对 **1.20.1 448/448 方法、54/54 字段**，
  **1.21.1 454/454 方法、54/54 字段**，无缺失、无多余、无悬空配置引用。
- 本次补的正是那次丢失的部分：配置面板的这一行。

### 副作用

无。默认值 = 加入这条开关之前的行为，老存档/老配置升级后表现不变。

## 实测五百三十四【激流三叉戟：把玩家的"旋转突进"套到女仆身上】

### 关键结论：标志位是通用的，动画不用自己写

反编译两版后有三个决定性发现：

1. **旋转突进的整套机制都在 `LivingEntity` 上，不是 Player 专属**：
   `autoSpinAttackTicks`（1.20.1 为 `f_20938_`）与"标志位 4"都在 LivingEntity，
   `LivingEntity.tick` 里就会递减 tick、扫掠这次位移的包围盒、撞墙收招。
   置位之后女仆**自动获得原版整套行为**。
2. **动画 TLM 早就做好了**——它读的就是这个标志位：
   ```
   GeoReplacedEntityRenderer.setupRotations（两版都有，字节码实证）：
       if (deathTime <= 0 && isAutoSpinAttack()) {
           mulPose(Axis.YP.rotationDegrees((tickCount + partialTicks) * 75.0f));  // 高速自转
           mulPose(Axis.XP.rotationDegrees(90.0f + getXRot()));                    // 平躺突进
       }
   ```
   而且 `YSMBinding` 还把它暴露成 molang 变量 **`is_riptide`**
   （1.20.1 读 `m_21209_()`、1.21.1 读 `isAutoSpinAttack()`——同一个标志位）。
   所以"动画这一块"根本不用我写：**标志位一置，Gecko 模型女仆就自动转起来**，
   模型作者还能用 `is_riptide` 写专属动画。Bedrock 模型那条路 TLM 自己没做
   （`EntityMaidRenderer` 无此分支），与 TLM 原版一致，本模组不额外补。

### 唯一缺的一环：伤害

`checkAutoSpinAttack` 撞到实体时回调的 `doAutoAttackOnTouch`，在 **LivingEntity 上是空实现**
（1.20.1 字节码就一条 `return`），**只有 Player 覆写成 `attack()`**。所以女仆必须自己补。
新增 `MaidSpinAttackTouchMixin` 注入基类（不改 TLM 源码），回调到
`MaidTridentSpinBehavior.onSpinTouch` 结算：攻击力 + 附魔加成 → 击退 → 火焰附加 → 荆棘 →
命中音；主人/友方与非法目标一律跳过。

### 实现（两树）

- **`MaidTridentSpinBehavior`**：触发条件 = 攻击模式（`touhou_little_maid:attack`）或
  **近战空袭** + 主手是**激流**三叉戟 + 有存活敌对目标且在其 5 格内 + 冷却好；
  触发时照原版：`push(方向 × 3.0×(1+等级)/4)` → 置 tick 数 20 → 置标志位 4 →
  按等级放 `TRIDENT_RIPTIDE_1/2/3` → 耐久扣 1。行为优先级 235（低于落地水 240、高于战术 230）。
- **`LivingEntitySpinAccessor`**：新开的 mixin。**不能复用** `EntityFlagInvoker`——
  滑翔位走的是 `Entity.DATA_SHARED_FLAGS_ID`，而旋转突进读的是
  `LivingEntity.DATA_LIVING_ENTITY_FLAGS`，**是两个不同的 SynchedEntityData**（字节码实证）。
- 配置项 `combat.riptideDash`（默认开）+ 面板一行「激流三叉戟突进」+ 四份 lang。

### 与玩家版本的刻意差异（三处）

| 差异 | 理由 |
| --- | --- |
| **不要求在水里/雨中** | 原版那道闸门是为"游泳突进"设计的，女仆在陆战/空袭时根本不在水里；照搬等于永远触发不了。水外可用是本次需求的核心。 |
| **方向取"她→目标"而非视线** | 玩家自己瞄，女仆得自动瞄准；竖直分量夹在 `[-0.15, 0.35]`，避免目标在脚下时一头扎进地面。 |
| **伤害 = 攻击力 + 附魔** | 1.20.1 玩家激流走普通 `attack()`（随攻击冷却缩放）、1.21.1 是固定 8.0 + 附魔；女仆统一成一种口径（同 `hitOne`，不吃暴击倍率），两树一致。 |

## 实测五百三十三【弩认烟花火箭当弹药 + 优先用威力大的（烟火之星多的）】

### 为什么原来不认

旧版弩的弹药判据是**只认带 `Explosions`（爆炸组件）的烟花**——那是 实测四百九十九 定的，
理由是"不带爆炸的烟花打出去是 0 伤害，拿它当子弹等于白烧一份飞行燃料"。

这个理由**只说明它弱，不说明它不能用**：原版 `CrossbowItem` 的弹药谓词认的就是
**任意烟花火箭**，不看有没有爆炸组件；玩家拿弩装普通烟花照样能射。而本模组却因此
把玩家那叠普通烟花判成"不是弹药"→ 报"缺弹药"→ 一发不开。

### 改了什么（两树）

1. **弹药口径放宽**：`hasAmmoForRanged` 的弩分支改为直接复用 `hasFirework`
   （任意烟花火箭都算），不再要求带爆炸组件。原来的 `hasExplosiveFirework` 随之删除。
2. **按威力挑弹药**：新增 `MaidFlightKit.fireworkPower(stack)` = **爆炸条目数**
   （= 合成时用掉的烟火之星个数，普通烟花 = 0），取用改用
   `takeBestCrossbowFirework`——**威力降序**挑，同威力时按"副手 → 主手 → 背包"的稳定次序。
   于是：有带烟火之星的先打带烟火之星的，没有才用普通烟花兜底。
3. `isExplosiveFirework` 保留但改为 `fireworkPower(stack) > 0`（语义不变，去重复实现）。

### 为什么"烟火之星多"就等于"威力大"（不是拍脑袋）

原版伤害只有这一个数在决定——`FireworkRocketEntity.m_37087_` 字节码：

```
伤害 = 5.0f + 2 * Explosions.size()     // 每多一枚烟火之星 +2
不带爆炸 → 整段早退                       // 0 伤害
```

（同一方法里还可见：对 5 格内实体做 `(5 - 距离)/5` 的开方衰减、对发射者本人不结算。）
所以"烟火之星多"与"打出去更疼"是同一件事，排序依据直接取自原版公式。

### 副作用（如实说明）

普通烟花现在**可能被当弹药打出去**（没有更好的时候兜底）。这是原版玩家行为
（拿弩装普通烟花就能射），不再是需要规避的意外；代价是极少数情况下会烧掉一枚
本可当飞行燃料的普通烟花。弹药耗尽后仍回落到射箭。

## 实测五百三十二【激流三叉戟按"近战武器"归类（原版里它投不出去）】

### 提问与结论

问："TLM 是怎么处理三叉戟的激流附魔的？我觉得我们的空袭也需要注意一下这个，
理论上激流三叉戟应被判定为近战武器。"

答：**TLM 完全没处理**——两个版本的 jar 全文搜 `m_44932_` /
`getTridentSpinAttackStrength` / `startAutoSpinAttack` 全部 **0 命中**
（只剩 geckolib 一个客户端动画绑定读 `isAutoSpinAttack`）。TLM 的
`TaskTridentAttack` 只做了"剥忠诚"一件事，**投掷时不看激流**。

而原版 `TridentItem` 两版字节码都证明：**带激流的三叉戟根本投不出去**——
`use`/`releaseUsing` 里第一道闸门就是"激流 > 0 且不在水中/雨中 → 直接 return"，
而**造 `ThrownTrident` 那一支只在激流 = 0 时走**（1.20.1 是 `iload 7; ifne` 跳过），
激流 > 0 走的是 `startAutoSpinAttack` + `push` 的旋转突进分支。

所以"TLM 能投"这件事本身就是个不一致：玩家拿着它投不出去，女仆却能投。
这条判断是对的，本轮按该口径改。

### 改了什么（两树）

1. 新增 `MaidFlightKit.isRiptide(stack)`——1.20.1 走 `EnchantmentHelper.m_44932_`
   （方法体就读 `Enchantments.f_44957_` = RIPTIDE，字节码实证）；
   1.21.1 走 `stack.getEnchantments().keySet()` + `Holder.is(Enchantments.RIPTIDE)`
   （那个重载不需要 `RegistryAccess`，单参判据也能用）。
2. `isRangedWeapon` 里把激流三叉戟**排除** → 远程空袭的三件套不齐
   （会照常提示"缺远程武器"，不会静默不动）；近战空袭里它照旧算近战武器
   （`isMeleeWeapon` 不看激流，三叉戟带 8.0 攻击力属性，两版一致），
   由俯冲猛击那一记结算。
3. `MaidFlightRangedTask.throwTrident` 补上原版闸门：带激流就不投
   （正常路径不会走到——第 2 条已挡住；这条是防任务切换竞态的兜底）。

### 手册同步

远程空袭的"武器位"说明补了"带激流的三叉戟除外"；弹药那段补了一句说明
（想用投掷三叉戟请放在近战空袭/攻击任务里）。

### 没动的部分

TLM 式的"剥忠诚"保留（防副本飞回来被捡走刷三叉戟）；穿刺/引雷/火焰附加照旧由
`ThrownTrident` 自己结算；御币、弩、弓的通道一律不变。

## 实测五百三十一【飞行远战的手持武器附魔：弩（多重射击/穿透）+ 快速装填】

### 现象

"飞行没有办法触发手持武器的附魔。比如弓和弩，还有三叉戟。"

### 核查结论（逐个反编译实证，不是猜）

| 武器 | 结论 |
| --- | --- |
| **弓** | **本来就是通的**：1.20.1 侧的 `shootArrow` 已逐条照抄 `BowItem.releaseUsing` 的
Power/Punch/Flame/Infinity（SRG 名与常量逐字节一致）；1.21.1 侧箭矢带武器（`firedFromWeapon`），
命中时由 `EnchantmentHelper.modifyDamage` / `doPostAttackEffectsWithItemSource` 自行结算。 |
| **弩** | **真缺**：多重射击、穿透此前**一次都没有**触发过（旧版把弩当弓、自己造一支箭就完事）。 |
| **三叉戟** | 穿刺/引雷/火焰附加由 `ThrownTrident` 自己读物品结算（本来就通）；
**忠诚**是 TLM 两个版本都刻意剥掉的（防"副本飞回来被捡走"刷三叉戟），本模组照搬，故保持不动。 |

### 弩的修法（两树）

照原版 `CrossbowItem.createProjectile` + `performShooting` 的装配与发射：

- **多重射击**（`MULTISHOT`）：1 发 → **3 发**，偏角按原版字节码里的 **0° / -10° / +10°**；
  弹药仍只扣 **1** 份（原版第 2、3 发用的是副本，"1 支箭变 3 支"正是这个附魔的价值）；
- **穿透**（`PIERCING`）：1.20.1 侧显式 `setPierceLevel`（原版 `m_36767_`）；
  1.21.1 侧**不用手动设**——那边 `AbstractArrow` 带武器的构造器里就
  `EnchantmentHelper.getPiercingCount(...)` → `setPierceLevel`，且该方法是 **private**，
  原版就是要它自动生效；
- **命中音**改成 `CROSSBOW_HIT`（原版 `createProjectile` 的唯一额外动作）；
- 耐久仍是**每次射击扣 1**（原版 `onCrossbowShot` 口径），不是每支箭扣 1。

### 快速装填（`QUICK_CHARGE`）

原版弩的蓄力时长 = `25 - 5 × 等级`（1.20.1 `m_40939_` 字节码：`25; iconst_5; imul; isub`）。
飞行时没有"蓄力"这个动作，所以把**同一相对幅度**搬到开火间隔上：
`间隔 = 基础 20 tick × (25 - 5L) / 25`——III 级 20 → **8 tick**，与原版"25 → 10 tick"
同为 2.5 倍提速；下限 4 tick，避免叠出荒谬射速。弓没有射速类附魔，一律走基础间隔。

### 没动的部分

弓的附魔结算通道、三叉戟的忠诚剥离（TLM 同款）、御币弹幕的三个专属附魔、枪械通道，
全部照旧。

## 实测五百三十【垂直占位的高度容差 0 → 10：差 10 格以内一律走原链路】

### 改了什么

`ALTITUDE_TOLERANCE`（实测五百二十七 引入的"已占位"容差）**0.0 → 10.0**，两树同改：

```java
/** 判据：她比目标低 ALTITUDE_TOLERANCE 格以内即视为"已占位" */
private static boolean onTargetAltitude(EntityMaid maid, LivingEntity target) {
    return maid.getY() >= target.getY() - ALTITUDE_TOLERANCE;   // 10.0
}
```

- 目标高出她 **10 格以上** → 进垂直占位相位：朝目标爬升，并且把当前这枚烟花的推力吃完
  才交棒（实测五百二十八）；
- 差 **10 格以内**（含等高、含她更高）→ 原链路直接应付：近战俯冲猛击 / 远战盘旋开火。

### 连带影响（同一个判据）

`faceLaunchDirection` 用的就是这条判据，所以"10 格以内"连**起飞朝向**也回到老口径
"背离 + 向上"（那是为地面目标设计的：爬升时别站在对方近战/爆炸包线里）；只有
"高出 10 格以上"才会切成"朝目标 + 抬头"顺着他爬——正是"用原链路应付"的字面意思。

### 没动的部分

地面起飞门槛（水平 ≤ 20 格）、近战 62° / 远战 45° 的爬升仰角、烟花内置 CD、
以及实测五百二十九 的朝向修正，全部照旧。

## 实测五百二十九【1.21.1 起飞/爬升方向歪掉：两个朝向方法漏了"喂 LookControl"】

### 现象

"起飞方向有问题。不是在向着敌人的方向起飞。"

### 根因（反编译实证 · 1.21.1 客户端 jar）

朝向有两条通道，**两条都得走**，1.21.1 侧只走了一条：

1. `applyRotation(...)` 直接写 `yRot` / `xRot`（含 `yRotO` / `xRotO`，渲染插值要用）；
2. `getLookControl().setLookAt(x, y, z, 360, 360)` 把"期望角度"交给 `LookControl`。

为什么第 2 条不能省：`Mob.serverAiStep()` 里 `customServerAiStep()`（脑活动 → 本行为）在
**字节码偏移 189**，`LookControl.tick()` 在**偏移 236**——**后手执行**。而 `LookControl.tick()`
第一件事就是 `if (resetXRotOnTick()) mob.setXRot(0)`（对女仆恒真），**同一 tick 内就把我们
刚写进去的抬头角归零**；若它手上还留着"期望点"（`lookAtCooldown > 0`，主人 / 游荡 /
上一次瞄准留下的），还会拿**那一份残留**把俯仰和头朝向重新压过去。烟花推力是沿
`getLookAngle()` 给的，于是她既爬不起来、方向也被带偏。

实测四百八十一（1.20.1）当时就是靠"两手都上"解决的；问题出在 1.21.1 移植时只把那一套
搬给了 `faceAwayAndUp` / `faceTarget`，**`faceUpForward` 与 `faceOrbit` 一直漏着**——
这是两棵树唯一的口径差，也正是"不朝着敌人起飞"的根（`faceUpForward` 恰好是起飞、垂直
占位爬升、远战俯冲助推三处的朝向来源）。

### 修法

把 1.20.1 的写法原样补到 1.21.1 的这两个方法。期望点取"朝目标 1 格 + 抬高 tan(仰角)"，
`LookControl` 据此反解出的俯仰角与传入的 `pitch` 同口径；`faceOrbit` 额外按 1.20.1 取
`h = |o|` 再乘进 Y 分量：

```java
applyRotation(maid, yaw, pitch);
try {
    double tan = Math.tan(Math.toRadians(-pitch));
    maid.getLookControl().setLookAt(
            maid.getX() + dx / dh, maid.getEyeY() + tan, maid.getZ() + dz / dh,
            360.0f, 360.0f);
} catch (Throwable ignored) {
}
```

### 影响面

- 1.21.1：起飞方向、垂直占位爬升、远战"俯冲助推"朝向（同用 `faceUpForward`）、
  远战盘旋的高度保持（`faceOrbit`）四处一起纠正；
- 1.20.1：本来就是齐的（四个朝向方法都喂了 `LookControl`），**本轮未改动**。

## 实测五百二十八【空袭垂直占位：够高之后不立刻撤推力，等这枚火箭烧完再交棒】

### 上一版（实测五百二十七）漏掉了什么

"高于或等于就落回原链路"这个判据本身没错，错在**落回得太干脆**：挂载型烟花的推力方向
是**每 tick 跟着她当时的视线走**的，而"抬头朝目标爬"这个朝向只有垂直占位相位在给。
她一到目标高度就切走 → 朝向立刻被后一段"瞄准敌人"改成基本水平 → 同一枚烟花剩下的
20~30 tick 推力全被摊到水平方向。结果是：她刚爬到的那点高度又还回去，敌人只要还在升，
她就在目标高度上下来回蹭，**永远攒不出"在它头上"这个占位**。

### 现在的口径

进相位的判据从一条变成两条（或）：

```
进垂直占位相位 = 还在目标身下（onTargetAltitude 为假）
              或 本相位点着的这枚烟花推力还没烧完（CLIMB_BOOST 且 LAUNCH_LEFT > 0）
```

- `LAUNCH_LEFT` 在本相位里的含义改写为"**当前这枚烟花的推力还剩多少 tick**"
  （近战 30 / 远战 20）。烟花实际寿命是 20~31 tick、`FIREWORK_COOLDOWN = 30`，
  所以"推力刚烧完"基本就是"下一枚可以点了"，不引进新的计时器。
- **够高之后不再补烟花**：只有"还差高度"时才调 `tryLaunch`。否则冷却一好又点一枚、
  本相位被自己无限续命，她会一路烧到世界顶端。
- 够高 + 推力烧完 → 下一 tick 自然交棒给原链路（近战俯冲猛击 / 远战盘旋开火）。
- 新增 `CLIMB_BOOST`（本相位标记）：同时纳入 `isEngaged`，爬升途中也算"正在打这一轮"，
  不会被"主人太远自动传送"拽走（与实测四百八十七同款豁免）；`forget` / `pauseRound` /
  `clearAll` 三处同步清理，不会残留。

### 效果

对着比自己高的敌人：她一路爬、把每一枚烟花的推力吃满，**越过目标高度之后还会继续升一段**，
在它头上攒出高度差，然后才转俯冲（近战）/ 盘旋（远战）。敌人若一直在升，追得上就追；
追不上那是火箭抬升速率本身的极限，不再是"自己把推力扔掉"。

## 实测五百二十七【空袭垂直逻辑改为"垂直占位"：爬到目标头上再谈出手】

### 换掉了什么

上一轮（实测五百二十五）用的是"目标高过 **8 格**就朝它爬"的启发式。本轮按需求换成
**唯一的垂直判据**，8 格那个值连同 `AIR_TARGET_ABOVE` 常量一起删掉：

```java
/** 是否已"占位"——高度是否已经高于或等于目标 */
private static boolean onTargetAltitude(EntityMaid maid, LivingEntity target) {
    return maid.getY() >= target.getY() - ALTITUDE_TOLERANCE;   // 容差默认 0
}
```

新增 `ALTITUDE_TOLERANCE`（默认 **0**，严格按"高于或等于就走正常路径"）。留这个常量是为了
日后一句话就能给 1~2 格的台阶/土坡留余量——**默认值不改**，现在就是严格判据。

### 新的空中链路

`tick` 里在"猛击段"之后、阶段一之前插了一道**垂直占位**闸门：

```
目标没了 / 三件不齐  → 收尾（原样）
起跳等待（JUMP_LEFT） → 原样（3 tick）
猛击段（SMASH）       → 原样（已经收翅砸下去了，让它砸完）
── 垂直占位 ──        ← 新增
   空中 且 高度 < 目标高度 → 只做一件事：爬（tickClimbToAltitude）
   否则（高于或等于）      → 落回原链路：近战俯冲猛击 / 远战盘旋开火
阶段一（LAUNCH_LEFT） → 原样
地面支点             → 原样（水平 ≤ 20 格起跳 / 太远先走过去）
阶段二               → 原样
```

**爬升相位做四件事**（`tickClimbToAltitude`）：

1. **朝向**：朝目标水平方向 + 抬头——角度与起飞同口径（近战 62° / 远战 45°，抽成 `climbPitch()`）；
2. **烟花**：走 `canLaunch` 那套**内建 CD**（`FIREWORK_READY` 30 tick + 有烟花），
   与地面起飞、远战补烟花同一道闸门，**没有新开计时器**；
3. **保持滑翔位**：挂载型烟花只推"正在滑翔"的实体；
4. **远战额外开火**：近战爬升途中没有可打的（要贴到 3.5 格才收翅猛击），远战则不然——
   所以爬升途中照常调 `fireRanged`，不白白浪费输出窗口。

**为什么这一支要"吃掉"阶段一与等待再放**：本相位自管烟花与朝向，所以进相位就把
`LAUNCH_LEFT`（阶段一倒计时）与 `WAIT_LAUNCH`（等猛击后再放）清掉。否则她爬到目标高度
之后会先走完 30 tick 的"背离 + 向上"（那是给地面目标设计的躲闪爬升），白等半秒多才俯冲。

**起飞朝向**（`faceLaunchDirection`）也统一成同一条判据：还在目标身下 → 朝它爬；
已在目标头上（含等高）→ 老口径"背离 + 向上"。四个起飞调用点（起跳 / 阶段一 /
`jumpForLaunch` / `tryLaunch`）一个都没增删。

### 出手看方块阻隔：补上两处漏网的视线门

需求里那句"是否出手肯定还是要看有没有受到方块阻隔的"——查了一遍，**有五条出手链路，
其中三条本来就有视线门**（新锁定 `scan`、地面近战 `groundMelee`、猛击的范围补刀），
另两条没有，本轮补齐：

| 链路 | 之前 | 现在 |
|---|---|---|
| 猛击**主目标**那一记（`smashHit` → `hitOne`） | 无条件结算——隔着一层方块也能凭空打中 | 加 `hasSight`；**放在冷却检查之后、消费冷却之前**：看不见 = 这次没出手、不吃攻击冷却，`tickSmash` 会继续俯冲压着打，视线一通就打出去 |
| 远战弓弩开火（`fireRanged` 的箭矢那一路） | 不看视线（箭自己撞墙是另一回事：先扣冷却、白放一箭） | 加 `hasSight`；枪械那一路本来就有 `canSeeGunTarget`，现在两条枪口口径一致 |

这与 TLM 原生近战"出手前必过 `hasLineOfSight`"、以及本模组实测五百零五给自保链路补
视线的口径完全一致。

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap` 逐项核对（两树各自）：
  - `AIR_TARGET_ABOVE` **已无残留**；`ALTITUDE_TOLERANCE` 常量 = `0.0d`；
  - `tickClimbToAltitude` 已定义且被 `tick` **调用 1 次**，相位内 `faceUpForward` /
    `canLaunch` / `tryLaunch` / `fireRanged` 四件齐全；
  - `onTargetAltitude` 已定义、被调用 **2 次**（`tick` 的闸门 + `faceLaunchDirection`）；
  - `faceLaunchDirection` 仍被调用 **4 次**（四个起飞点，没增删）；
  - `SelfPreservationBehavior.hasSight` 调用点由 **2 → 4**（`smashHit` 主目标 + `fireRanged`
    各一处，原有 2 处未动）；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `deploy_all.py` 三端 `match=True`；
- `test_server.py 1201` 与 `test_server.py neoforge1211` 均 PASS。

### 边界（如实交代）

- **地面那一段一行没动**：还是"水平 ≤ 20 格起跳 / 太远先走过去"。垂直判据只拦**空中**那一段——
  否则她站在地上、目标在半空时会跟着往天上跳（她还没起飞，没有滑翔位，烟花推力吃不到）。
  她一离地就进爬升相位，所以观感仍是"起飞即朝目标爬"。
- **地面等高的敌人行为完全不变**（`getY()` 相等 → 判据为真 → 走原链路：起跳 → 阶段一
  "背离 + 向上" → 阶段二俯冲）；1 格台阶上的目标现在会让她起飞（差值 > 0 = 未占位）——
  这是需求的严格口径，想要余量就调 `ALTITUDE_TOLERANCE`。
- **近战爬升途中不出手**（要贴到 3.5 格才收翅猛击），只有远战会边爬边打；
- 索敌口径（50 格发现 / 128 格维持 / 新锁定要视线）**未动**——垂直占位只管"往哪飞"，
  不管"锁谁"；
- 摔落免疫仍是上一轮改成的**默认开**；落地水"接住即收"、重锤落地帧的内置 CD 也都保持；
- 未改任何配置键名、网络协议号、用户可见文案。

### 已部署待实测

1. **高目标（飞在半空/崖上）**：她应持续放烟花往上爬，高度一够就转俯冲猛击，不再原地仰头看；
2. **等高目标（平地怪）**：行为应与以前一致（起跳 → 背离爬升 → 俯冲）；
3. **1 格台阶上的怪**：按严格口径她会起飞打（差值 > 0）；若认为"为一格台阶就烧烟花"
   不值当，我可以把 `ALTITUDE_TOLERANCE` 调到 1.0~2.0；
4. **隔墙**：目标躲在方块后时不应打中（猛击主目标 / 箭矢都不会结算），
   视线一开立刻打——日志里 `[promaid/飞行撞击免疫]`、`[promaid/空袭免摔]` 不受影响。


## 实测五百二十六【空袭免疫摔落伤害：默认关 → 默认开】

### 改法

默认值一个字的位置改动，但配套的**五处文案**必须跟着改，否则面板写着"默认关"、实际默认开：

| 位置 | 改动 |
|---|---|
| `MaidSmartConfig`（两树） | `COMBAT_FLIGHT_NO_FALL_DAMAGE` 的 `define("flightNoFallDamage", false)` → `true`，config comment `（默认关）` → `（默认开）` |
| `PromaidConfigScreen`（两树） | 该行 tooltip 的 `（默认关）` → `（默认开）` |
| `MaidFlightWallGuard`（两树） | 代码注释里的"（默认关）"同步 |
| 游戏内手册「空袭与飞行作战」章（两树） | 「空袭免疫摔落伤害（§c默认关§r）」→「（§a默认开§r）」，并把"开启即彻底免摔"改写成"默认彻底免摔；关掉才按落地水/雪保护" |
| 桌面发布文案草稿 | 那句"另有默认关的…开关"→ 改成默认开并说明原因 |

### 为什么"默认开"更合理（写进注释里了）

空袭整条链路本身就是"**高空盘旋 + 收翅俯冲**"，落地缓冲（水/雪）只是**兜底**；而兜底失败的
情形都在这条链路的常态里——背包没桶、落点被占、被打断，以及 `实测五百二十五` 修掉的那类
"水还在、滑翔分支被顶掉"。她只有 20 血，这些都属于"机制没接住"，不该由玩家承担。
想按原版吃摔伤，随时可在「生存与复活 → 落地缓冲」里关掉（关掉后行为与以前完全一致）。

### 一个必须交代的坑：改默认值**不会**改动已有存档的配置文件

Forge/NeoForge 的 COMMON 配置一旦生成就会把值写进 `config/promaid-common.toml`，
之后**代码默认值不再生效**（读的是文件里的值）。所以本次顺带核对了你本机的三个配置：

```
D:\.minecraft\versions\1.20.1-Forge_47.4.21\config\promaid-common.toml      flightNoFallDamage = true
D:\.minecraft\versions\1.21.1-NeoForge_21.1.250\config\promaid-common.toml  flightNoFallDamage = true
```

**两边都已经是 true**（此前已手动打开过），所以本机不需要改配置文件、行为立刻一致；
对**新装/新档**的玩家，这次改的默认值才会体现出来。这一点写在这里免得日后被当成"改了没生效"。

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap -c` 反汇编 `MaidSmartConfig` 的 `<clinit>`，`flightNoFallDamage` 后面紧跟的
  是 **`iconst_1`**（= true）——两树都确认；对照组 `flightNoWallDamage` 同为 `iconst_1`，
  `ominous_bottle` 仍在 `List.of` 的实参里（实测五百二十五 那条没被碰掉）；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `deploy_all.py` 三端 `match=True`；
- `test_server.py 1201` 与 `test_server.py neoforge1211` 均 PASS。

### 边界

- **只改默认值与文案**：`MaidFlightWallGuard` 的拦截逻辑（`FALL` 伤害取消 + 日志
  「空袭免摔」）一行未动，`实测四百九十四` 引入的行为完全保留，只是默认值反过来。
- 免疫仍**只作用于两个空袭任务**（`MaidFlightKit.isFlightTask` 门控）——普通近战/生产任务的
  女仆照常吃摔落伤害。
- 免疫的是**摔落伤害**（`fall`），不是"免疫所有伤害"：被怪打照样掉血、照样可能被击落，
  与 `实测四百九十四` 的口径一致。
- 未改任何配置项键名、网络协议号；`config.promaid.combat.flightNoFallDamage` 的翻译键与
  中英文名都没动（面板行的标题仍是「空袭免疫摔落伤害」）。


## 实测五百二十五【排班表分类说明补齐 + 空袭"敌人更高"方案 + 落地水不再挡飞行 + 不祥之瓶默认不喂】

## 一、排班表挪了分类，两处"文字说明"漏改（两树）

`实测五百一十八` 把排班表从「移动与行为」挪到「生产与工作」，但那次**只改了 `Section`
枚举的归属**，两处写给玩家看的说明漏改：

| 位置 | 旧文（错的） | 现在 |
|---|---|---|
| 配置面板大类页一句话（`groupHint`）· 生产与工作 | `建造 / 挖矿 / 伐木 / 烧制酿造 / 农场宰杀` | 末尾补 ` / 排班表` |
| 同上 · 移动与行为 | `跟随 / 空闲流畅 / 排班表 / 搭路` | 排班表已不在这个大类 → 去掉 |
| 手册「功能总览 · 一键跳转配置」① 生产与工作 | 只有建造/挖矿/伐木/烹饪与酿造/农场与宰杀五个链接 | 补 `[[@WORK:SCHEDULE|排班表]]` 链接 |
| 同上 ⑤ 移动与行为 | 还挂着 `[[@WORK:SCHEDULE|排班表]]`（跳得到，但列错了大类） | 去掉（已在 ① 列出） |

判据就是 `Section` 枚举里的实际归属（`SCHEDULE("排班表", Group.WORK)`），不再靠人工记忆。
手册那一章是「一键跳转」的性质，现在 ①②③④⑤⑥⑦ 七行与配置面板首页**逐项对齐**。

## 二、空袭面对"比自己高的敌人"没有方案（两树）

两个原因叠在一起，都在 `MaidFlightCombatBehavior`：

**① 起飞门槛用的是 3D 距离——她根本进不了起飞分支。**

```java
double dist = maid.distanceTo(target);           // 旧：3D 距离
if (maid.onGround()) {
    if (dist <= LAUNCH_RANGE && canLaunch(...)) { jumpForLaunch(...); }   // LAUNCH_RANGE = 20
    ...
}
```

敌人比她高 50 格时，**光是垂直分量就 ≥50 > 20**：她就算站在敌人正下方也永远进不了这个
分支——不跳、不放烟花，只会原地仰头看（反馈原文："只会对着比自己高 50 格的敌人看着"）。

现在改判**水平距离**：

```java
double dxh = maid.getX() - target.getX();
double dzh = maid.getZ() - target.getZ();
double distH = Math.sqrt(dxh * dxh + dzh * dzh);
if (distH <= LAUNCH_RANGE && canLaunch(...)) { jumpForLaunch(...); }
```

`LAUNCH_RANGE` 那句话的原意（常量注释）本就是"太远先跑过去，避免越炸越远"——这是个
**水平**概念；空袭是立体作战，垂直方向不该设限。爬升量由起飞仰角（近战 62°/远战 45°）承担。

**② 起飞朝向是给地面敌人设计的——目标是空中目标时会越飞越远。**

原口径是"背离敌人 + 抬头 62°"，目的是爬升时不站在对方的近战/爆炸包线里。可目标在头顶
50 格时她本来就不在对方包线内，"背离"只会让她**越飞越远再掉头飞回来**，全程烧烟花。

新增 `faceLaunchDirection(maid, target)`（起飞阶段四处调用点统一走它）：

```java
if (target.getY() - maid.getY() > AIR_TARGET_ABOVE) {   // 8 格
    faceUpForward(maid, target, -62°);   // 空中目标：朝它爬（复用远战补烟花那套）
} else {
    faceAwayAndUp(maid, target);         // 地面目标：保持原口径，一行没动
}
```

门槛取 8 格的理由：对方在 8 格以上高度时她的近战包线本来就够不着，"躲开"没有收益；
而地面怪站在 1~7 格台阶/土坡上时仍走旧口径，避免刚起飞就贴脸。

## 三、落地水会阻拦女仆飞行（两树；1.21.1 重锤落地帧另补 CD）

### 真根因不是"缺 CD"

水是**流体**。`LivingEntity.travel` 的第一个分支就是
`isInWater() → travelInWater(...)`——只要她还在水里，**滑翔分支整个被顶掉**：

- 烟花推力被水阻尼几乎吃光（`FireworkRocketEntity` 只是往速度里加，真正结算的是 `travel`）；
- 而且在水里 `onGround()` 恒为 false → 空袭"落脚再起飞"的支点判定也进不去。

所以她站在自己刚放的那格水里"看着敌人不动"。**细雪不是流体、没有这一层**，所以过去
只有落地水挡飞行——这正是"落地雪没事、落地水有事"的来源。

`实测五百一十二` 已经给空袭分支加过 2 秒内置 CD（`FLIGHT_CLUTCH_CD = 40`，这次核对
**两树都在、值没变**）。但 CD 拦的是"不要连着再放一次"，拦不住"这一格水还在那儿"。

### 改法：这类缓冲只负责"接住这一下"

新增 `flightPlaced` 标记（`placeFluid` 增加一个带来源的重载），空袭/重锤落地帧置位；
主循环里对这类**水**加一条"接住了就收"：

```java
if (this.flightPlaced && !this.placedSnow
        && maid.getDeltaMovement().y >= 0.0) {   // 不再下坠 = 已落进水里被阻尼停住 / 已踩到地
    this.recoverFluid(level, maid);
    return;
}
```

- 只用**竖直速度 ≥ 0** 做判据，不用 `onGround()`——因为在水里 `onGround()` 恰恰是 false，
  用它就永远收不掉（这正是上面那条连锁的机关）；
- **细雪不走这一支**（它不是流体，不挡滑翔分支）；
- **通用（高塔坠落）那条路不置位**：它的保持时长仍由配置 `waterHold` 决定，
  "收水后再放一次接住极高塔"的既有语义完全没动。

### 按要求：1.21.1 重锤落地帧也吃同一个 2 秒闸门

`maceLanding` 分支原先没有任何冷却（只靠 `FORCED_CLUTCH` 一次性消费）。重锤 3 秒一发、
落地水只保持 5 tick 时，"跃起 → 落地 → 立刻再跃起"的连击会让水连着放、每次都要从水里
重新起飞。现在这一帧同样过 `gameTime >= flightClutchCd` 并写回 `+FLIGHT_CLUTCH_CD`，
与空袭分支同口径（1.20.1 树没有重锤落地帧，无此改动）。

## 四、不祥之瓶默认不能喂（两树）

1.21 的**不祥之瓶**带食物组件、能"喝"，于是被投喂的通用判定（"有食物属性且不在黑名单"
= 能喂）当成普通食物。喂下去等于顺手给女仆挂**不祥之兆**（袭击/试炼的触发条件）——
玩家喂她是为了回饱食度，不该捎带这个。已加入 `aidFoodBlacklist` 默认值：

```java
"minecraft:enchanted_golden_apple",
"minecraft:ominous_bottle"), o -> o instanceof String s && !s.isEmpty());
```

1.20.1 注册表里没有这个物品，那张表在那边是空操作——**两端默认值保持一致**（配置面板的
勾选页本来就只列注册表里真实存在的食物，多一个未知 id 不会显示、也不影响计数）。
手册「贴身辅助」章那句"默认黑名单…"同步写明不祥之瓶与原因。

## 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap` 逐项核对：
  - `AIR_TARGET_ABOVE` = `double 8.0d`、`LAUNCH_RANGE` = `double 20.0d`（两树）；
  - **`faceLaunchDirection` 的调用点恰好 4 处**（起跳分支 / 阶段一 / `jumpForLaunch` /
    `tryLaunch`）、**`faceAwayAndUp` 只剩 2 处**（`faceLaunchDirection` 内部 + 远程近身弹开）
    ——即起飞阶段已全部改走新朝向；
  - `tick` 里出现新的 `Math.sqrt`（水平距离）且两个 `jumpForLaunch` 都在它之后；
  - `WaterClutchBehavior`：`FLIGHT_CLUTCH_CD` = `long 40l`、
    `placeFluid(...,boolean,boolean)` 重载存在、`flightPlaced` 读写齐全、竖直速度判据在位；
  - `MaidSmartConfig`：`ominous_bottle` 两端都在；
  - **自查（又一次踩同一个坑）**：第一版校验脚本把 `LAUNCH_RANGE` 判成"0 次比较"、
    重载方法体切不出来——原因是 `static final double` 常量被 javac **内联**（字段名在方法体里
    消失，这正是 实测五百二十一 记录过的坑），以及我的方法体切片正则失手。改用
    `javap -c` 原样 grep 调用点后结论如上表，**是脚本错、不是代码错**；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `deploy_all.py` 三端 `match=True`；
- `test_server.py 1201` PASS（Done 2.614s）、`test_server.py neoforge1211` PASS（Done 0.852s）。

## 边界（如实交代）

- 飞行链路的**数值**一个没动：起飞仰角（近战 62° / 远战 45°）、阶段一时长（30 / 20 tick）、
  收翅距离 3.5、盘旋半径 10、盘旋高度 3.5、弹开 3 格 / 1.5 秒、重锤下落加成算法全部原样；
  本轮只改了"进不进得了起飞分支"（水平距离）与"往哪个方向爬"（有高空目标时朝它爬）。
- `实测五百一十二` 的 2 秒 CD **没有被删除或改值**——落地水的"挡飞行"不是它没生效，
  是它管不到"水还在"；两者是互补的（CD 防连放、接住即收防挡飞）。
- 通用落地水的"收水后再放一次"语义、细雪的全部语义（含追补雪垫）**一行未动**。
- **没有改**投喂判定的通用口径（"有食物属性且不在黑名单 = 能喂"），只调默认黑名单一项；
  玩家仍可在「投喂食物勾选」页里把不祥之瓶重新勾成"能喂"。
- 未改任何网络协议号、配置项键名、用户可见文案的既有措辞（新增的说明文字除外）。

## 已部署待实测

1. 配置面板首页进「生产与工作」，上方的说明应含"排班表"；进「移动与行为」应不含；
   手册「功能总览」① 里的"排班表"链接能跳到排班表小类；
2. **空中敌人**：找一个高处的敌人（或凋灵/幻翼），她应能起飞并**朝它爬**，而不是原地仰头看；
   地面敌人（1~7 格高差）行为与以前一致；
3. **落地水**：空袭/重锤落地后她应能立刻重新起飞——水在她落地停稳的那一刻就被收回，
   不再有"泡在自己放的水里不动"；
4. 往女仆背包塞不祥之瓶：饿到阈值以下她**不应**喂它（顶多去拿别的食物）。


## 实测五百二十四【空袭"飞太高就丢目标、飘走"根治：索敌三处修正（16 格上限 / 起飞丢锁 / 停止清瞄准）】

### 先用实测日志定位，不靠猜

日志就在本机，直接读现场（1.21.1 客户端 `logs/promaid.log`，2026-09-16 22:19~22:20，
打的是监守者）：

```
22:19:32 [空袭索敌] task=flight_combat 候选=1 → 存活=0 → 可攻击=0 → … → 选中=-
22:20:25 [空袭索敌] cands=4 → 存活=3 → 可攻击=1 → 活动范围内=1 → 有视线=1 → 选中=minecraft:warden@7.1
22:20:25 [飞行作战] 放烟花起飞          ← 锁上了，起飞
22:20:27 [空袭索敌] 候选=4 → 存活=3 → 可攻击=1 → 活动范围内=1 → 有视线=0 → 选中=-   ← 2 秒后目标没了
22:20:25~22:20:39 [重锤门] 主手=minecraft:mace 目标=无 地面=false                          ← 悬在空中、没有目标
```

三条硬结论：**候选都在（有敌人）、可攻击也在，却锁不上**；**起飞 2 秒后就丢锁**；
**长时间"在空中 + 无目标"**——没有目标 = 行为停止 = 没有任何朝向指令，于是顺惯性飘走。
这就是"飞得特别高就失去方向"。

### 三个真根因（逐个字节码实证）

**① 视线门误用了 `Sensor.isEntityAttackable`——它内含 16 格上限，我们的"50 格索敌"根本没生效。**

这是本轮最关键的一条。旧版 `FlightTargeting.scan` 里那一关写的是
`Sensor.isEntityAttackable(maid, e)`，注释还写着"与 TLM 原版逐环一致"。反编译实证：

1.20.1（CFR，`Sensor.m_26803_`）：

```java
f_26793_ = TargetingConditions.m_148353_().m_26883_(16.0);                       // forNonCombat().range(16.0)
f_26794_ = TargetingConditions.m_148353_().m_26883_(16.0).m_26893_();            // … + ignoreLineOfSight
m_26803_(a, b) { return brain 已把 b 记为 ATTACK_TARGET ? f_26794_.test(a,b) : f_26793_.test(a,b); }
```

1.21.1（`javap` 客户端 jar，`Sensor.isEntityAttackable`）：

```
static { TARGET_CONDITIONS = TargetingConditions.forCombat().range(16.0);
         ATTACK_TARGET_CONDITIONS_IGNORE_INVISIBILITY_TESTING =
                 forCombat().range(16.0).ignoreInvisibilityTesting(); }
isEntityAttackable(a, b) { return brain.isMemoryValue(ATTACK_TARGET, b)
                                    ? …IGNORE_INVISIBILITY_TESTING.test(a, b)
                                    : TARGET_CONDITIONS.test(a, b); }
```

而 `TargetingConditions.test` 里就是那句
`d = a.distanceToSqr(b); if (d > (range * visibility)²) return false;`。

**两个变体都带 `range(16.0)`。** 也就是说这一环真正卡的是"**16 格以内**"，视线只是顺带。
后果：我们扫的是 50 格球（日志 `盒尺寸=100.6/101.5/100.6`，候选确实捞进来了），却**在 16 格这一关
被全数否决**（日志 `有视线=0 → 选中=-`）。她一旦被起飞弧线推到 16 格外，**再也拿不回目标**。

`实测五百二十一` 之所以"反而更严重"，根因就在这里：旧版（521 之前）根本没有"按距离丢锁"这回事
（它直接读 brain，TLM 什么时候擦才什么时候丢），所以锁能一直挂到打完；521 引入了"距离超限就丢锁"，
而她一旦丢锁，**重新索敌又被这个 16 格上限掐死**——于是从"偶尔丢"变成"丢了就再也回不来"。

**② 维持锁定与新锁定共用 50 格上限——起飞那口气就把她推出 50 格。**

起飞动作是**主动背离敌人 + 抬头 62°** 去吃烟花推力（`faceAwayAndUp` + `LAUNCH_CLIMB_TAN_MELEE=1.88`
+ `LAUNCH_TICKS_MELEE=30`），实测日志正好是这条线的证据：**锁定时距离 7.1 格，放烟花起飞 2 秒后
目标就没了**。旧口径下每一轮起飞都在半路自我解除瞄准，行为随即停止、状态被整清，她在高空
保持滑翔却没有任何朝向指令。

**③ 行为一停止就用 `forget()` 整清，把"瞄准"和"烟花冷却"一起清掉了。**

空袭一轮里行为本来就会短暂停（三件不齐那两拍、`canStillUse` 与 `tick` 的边界），而这些地方
旧版都走 `forget()`。它连带清掉：

- `FlightTargeting` 的锁定 → 她得重新过"限频扫描 + 视线门"，而此刻她多半在高空（① 的 16 格上限
  在这里再次致命）→ **有敌人却拿不到目标**，正是日志里那片 `选中=-` 的状态；
- `FIREWORK_READY` 烟花冷却 → 停一下就能重开一次，实测日志里出现了**同一秒两次"放烟花起飞"**。

### 改法（两树同步）

| 位置 | 改动 |
|---|---|
| `FlightTargeting.scan` | 视线门改用本模组自有的 **`SelfPreservationBehavior.hasSight`**（纯 raycast：`ClipContext.Block.COLLIDER`、中心点上抬 1.2 格、**没有距离上限**）。"隔墙不出手"照旧成立，但"能不能发现"重新交回用户指定的 50 格球。与实测五百零五给自保近战补视线用的是同一个方法，口径统一 |
| `FlightTargeting` 常数 | 拆成两个：`RANGE = 50.0`（**只管发现**，新锁定）+ 新增 `HOLD_RANGE = 128.0`（**维持锁定**）。起飞弧线最坏约 60 格，128 有约两倍余量，同时仍能防住"追着跑出战场" |
| `FlightTargeting.resolve` | 维持/接管锁定这两条分支顺手把扫描冷却推到"下个 tick 即可扫"——限频只为"没敌人时别空扫"，不该在交战中拖延重新锁定 |
| 新增 `FlightTargeting.pause(uuid)` | **轻量清理：保留锁定**，只清每 tick 的缓存。给"行为中途停止"用 |
| 新增 `MaidFlightCombatBehavior.pauseRound(uuid)` | 停止时只清"本轮动作"（`LAUNCH/JUMP/SMASH/WAIT/RANGED_*/ATTACK_READY/MAX_Y`），**刻意保留 `FIREWORK_READY` 与索敌锁定** |
| `endFlightSafely` | `forget(id)` → **`pauseRound(id)`**；硬清仍走 `forget`（女仆移除、清空换任务） |

净效果：**打起来之后，瞄准不会再因为"飞远了 / 视线被挡 / 行为停一拍"而断**——她会一直朝那个方向
飞回去；空中重启也直接进阶段二扑击，不会又补一轮背离爬升。

### 顺带修掉诊断日志的误导标签

`FlightTargetProbe`（实测五百一十五 加的一次性排查工具，本轮正是靠它锁定根因）原来把
`Sensor.isEntityAttackable` 的通过数标成 **"有视线"**——可那个方法含 16 格上限，
"有视线=0"常常根本不是视线问题、而是目标在 16 格外，排查时被它带偏过。现在：

- 逐环计数**改照我方索敌链**：可攻击 → 活动范围内 → **50 格内** → **我方有视线**，
  末端的 `我方选中` 就等于 `scan` 真正会锁的那个；
- 原版那一关单独列出并**标明含 16 格上限**。

新格式：

```
[空袭索敌] maid=… task=… activity=work 盒尺寸=100.6/101.5/100.6 候选=4 → 存活=3 → 可攻击=1
  → 活动范围内=1 → 50格内=1 → 我方有视线=1 → 我方选中=minecraft:warden@7.1（原版可判(含16格上限)=1）
```

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
  中途我自己出过一次错：给 neo 的 `pauseRound` 抄了 1.20.1 才有的 `GROUND_READY` 字段，
  javac 报"找不到符号"，删掉后重编通过——如实记录；
- `javap` 逐项核对（两树各自）：`RANGE` 常量 = `50.0d`、`HOLD_RANGE` = `128.0d`、
  **`keepable` 体内是 `16384.0`（=128²）而不再是 `2500.0`（=50²）**、
  `scan` 调 `hasSight` 且**不再出现 `isEntityAttackable`/`m_26803_`**、
  `pause` 存在且**不碰 `LOCKED`**、`endFlightSafely` 调 `pauseRound` 而非 `forget`、
  `pauseRound` 内**不出现 `FIREWORK_READY`**；
- 1.21.1 侧那条"16 格上限"是用**客户端 jar 的 `javap` 字节码**确认的，不是从 1.20.1 推断；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `deploy_all.py` 三端 `match=True`；
- `test_server.py 1201` PASS（Done 2.658s）、`test_server.py neoforge1211` PASS（Done 0.863s）。

### 边界（如实交代）

- **只改了索敌与收尾口径**，没动飞行链路本身：起飞仰角/时长（近战 62°×30t）、收翅距离 3.5 格、
  盘旋半径 10 格、盘旋高度 3.5 格、弹开 3 格/1.5 秒、重锤下落加成的算法**全部原样**。
  她起飞还是会爬很高——那是"吃满烟花推力"的设计，不是 bug；本轮修的是"爬高之后丢掉瞄准"。
- `HOLD_RANGE=128` 是**有界**的：目标跑出 128 格、或死了/打不着/越出活动范围，仍然会正常收尾。
- 未改任何用户可见文案、配置项与网络协议号。

### 已部署待实测

1. 近战空袭（1.20.1 暴击 / 1.21.1 重锤）能否**完整走完一轮**：起飞 → 朝敌飞 → 收翅猛击 → 再起飞；
2. 飞得再高也不丢目标，不再"飘出战场"；落地后若目标还在，是否立刻重新起飞；
3. 隔墙的怪**仍然不会被新锁定**（这条要专门验一下，是本轮唯一"放宽"的地方的对立面）；
4. 日志里 `我方有视线` 与 `原版可判(含16格上限)` 两列的差别，能直观看出旧口径卡在哪一环。


## 实测五百二十三【版本号统一为 1.2.0（两端一致）】

### 改法

把**未发布批次**的版本号统一为 **1.2.0**，1.20.1 Forge 与 1.21.1 NeoForge **两端一致**。

落地范围（共 **657 处**）：

| 位置 | 改动 |
|---|---|
| `promaid_src/META-INF/mods.toml` | `1.1.1` → `1.2.0` |
| `promaid_src_neo/META-INF/neoforge.mods.toml` | `1.1.0` → `1.2.0`（实际被打包的那个） |
| `promaid_src_neo/META-INF/mods.toml` | 同步（该文件不参与打包，仅避免日后混淆） |
| 构建脚本 | jar 名 → `promaid-1.2.0.jar` / `promaid-1.2.0-neoforge-1.21.1.jar` |
| 部署 / 测试 / 校验脚本 | 源与目标路径改名 |
| `deploy_all.py` | 新增：部署时**自动删除目标目录里旧的 `promaid-1.1*.jar`**——jar 改名后旧包若残留，同 modId 共存会导致启动失败 |
| 游戏内手册 | 玩家可见版本标注（"v1.2.0 起新增功能速览"等） |
| 内置变更日志 | 两树 `changelog.txt` |
| README | 两平台版本标注 |

### 顺带归一的历史口径

这批未发布改动原先在源码注释里**混用了 `v1.1.3` 与 `v1.1.4`**（同一批功能前后写下的两个号），
另有一处 `v1.1.2`——现在**全部归一为 `v1.2.0`**。

**安全性依据**：远端 tag 当时只到 `v1.1.1`（`git ls-remote` 确认），所以 `v1.1.2/3/4` 从未发布，
归一不影响任何已发布记录；已发布标号（`v1.0.x` / `v1.1.0` / `v1.1.1`）**原样保留、未触碰**。

### 我自己踩的坑（如实记录）

批量替换是用脚本做的，中途把两树 `changelog.txt` 写成了**双 BOM**——读用 `utf-8-sig`
（吃掉 BOM）、写又用 `utf-8-sig`（再补一个），文件头变成 `EF BB BF EF BB BF`，
游戏里首行会显示成一个方框。已修正为单 BOM 并复核（首行仍是【版本号说明】、第 2 行仍是最新条目）。

### 验证

两树编译零错；jar 内 `version=1.2.0`（两平台）、手册含 `v1.2.0` 且无 `v1.1.2/3/4` 残留、
内置日志单 BOM 且首行正常、`pack_format` 分别 15 / 34；
`deploy_all.py` 三端 `match=True`，且两个客户端目录里的旧 `promaid-1.1.x` 包已确认清除
（只剩 1.2.0）；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

### 升级提示

两个 jar 的 `modId` 相同，**安装前请删除 mods 目录里的旧 `promaid-1.1.x` 包**，
否则同时加载会启动失败。

## 实测五百二十二【手册「详细介绍」全面重写：补上空袭专章 + 补齐缺失功能 + 修失效跳转】

### 先做体检：手册到底漏了什么

没有凭印象改，先把两边的**实际内容**都盘出来对账：

- 手册侧：`GuideContent` 共 30 章、1108 行（两树内容基本同源），逐章读完；
- 模组侧：`MaidSmartConfig` 共 **326 个配置项 / 12 个配置组**，配置面板另有独立的
  **7 大类 / 28 小类**层级——这才是"模组实际有什么"的权威清单。

然后拿功能关键词去手册里检索，得到硬结论：**手册完全没提的功能有一整片**，
其中最大的一块是——

### 最大的缺口：整个「空袭 / 飞行作战」系统手册只字未提

全篇检索「飞行作战 / 空袭 / 鞘翅 / 烟花火箭」= **0 次**。

而这是模组里体量最大的一套系统：两树合计 4 个任务类 + 1 个行为类（1300+ 行）
+ 装备/弹药/姿势 3 个工具类 + 若干 mixin；配置项还**散在三个小类里**
（总开关在「主动参战」/ 1.21.1 在「单兵战术」，落地保护三项在「落地缓冲」）。
玩家切到这两个任务后，完全不知道三件套是什么、为什么不起飞、为什么她贴地不飞。

**新增整章「空袭与飞行作战（会飞的女仆）」**（约 5 页，与最长的既有章节持平），
七节写全：两个任务的区别 / 三件套（鞘翅+武器+烟花，远程空袭另需弹药）/
保姆式首飞举例 / 近战空袭完整链路 / 远程空袭完整链路 / 索敌机制 / 开关位置与常见问题。

口径全部照当前字节码与配置 comment，不凭印象：
- 激活判据 = `MaidFlightKit.isModeActive/hasElytra/hasWeapon/hasFirework`（远程另需 `hasAmmoForRanged`）；
- 链路常量实测值：收翅距离 3.5 格、范围补刀 2.5 格、盘旋半径 10 格、
  盘旋高度目标上方 3.5 格、开火距离 24 格、近身弹开 3 格 / 1.5 秒、地面重新起飞限 20 格；
- 两树差异**分开写**：1.20.1 无重锤 → 收翅下落按原版暴击 ×1.5；
  1.21.1 手持重锤 → 吃**下落加成 + 风爆**，且按「本轮最高点」结算（`forcedFallDistance` 后门）。

特别把 521 那次索敌重写的**用户可感知结论**写进手册：索敌是以自身为圆心、
半径 50 格的球，且**一旦锁定就持续瞄准**（不再飞高丢目标、飘出战场）。

### 顺手修掉一个真实的失效链接

体检时发现：`MOVE:SCHEDULE` 这个跳转目标**不存在**——排班表在实测五百一十八
已从「移动与行为」挪到「生产与工作」，但手册里两处引用漏改：
① 功能总览章的 `[[@MOVE:SCHEDULE|排班表]]`；② `CFG_LINK_RULES` 里的同名规则。
点了跳不过去。现在两处都改为 `WORK:SCHEDULE`，并给新的空袭章补了两条跳转规则。

### 其余补齐（手册原来一个字都没有的功能）

- **投喂食物勾选**（实测五百一十九/五百二十的新功能）：写清"现在认模组食物
  （带食物属性就算能喂）、勾选面板在哪、点图标切换能不能吃、默认黑名单有哪些"，
  附"让女仆喂三明治"的保姆式举例；
- **AI 记忆**补「人格种子」（人设与聊天记忆分离）与「双 agent 提取」（摘要/事实分两次调用）；
- **农场与杂项**补：蛋糕可食用、悬空禁搭方块、手持光源发实光、TNT 点火保护期、红石机器专属搭建；
- **伐木**补「树冠清理」、**挖矿**补「穿透预算」；
- 「模组详细配置面板导航」补空袭相关的分类指引（说明它为什么横跨两个小类）；
- 「这是什么？」补 v1.2.0 功能速览（空袭 / 投喂认模组食物 / 三叉戟按任务分类 / 排班表分类调整）。

### 还修了一处会误导读者的编号引用

章内多处写「（见第 3 章）」指"配置 LLM"，但按实际章节顺序它是**第 4 章**
（序言与功能总览各占一位）——读者按图索骥会找错。统一改成按章节名指路
（「准备篇：配置 LLM」），不再依赖会漂移的编号。

### 验证

两树编译零错；内容校验全绿——**章节总数 31**（原 30 + 新增空袭章，
`javap` 确认字节码里 `chapters()` 调用数也是 31）、
**全部 30 个跳转链接与 CFG_LINK_RULES 目标都是配置面板里的合法目标**（不再有 `MOVE:SCHEDULE`）、
本次新增/补齐的关键词全部出现在正文；
排版估算：新章约 5 页（与最长既有章持平）、全手册约 70 页，无异常巨章；
打包 → `deploy_all.py` 三端 `match=True`；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

### 边界

只改了 `GuideContent`（手册正文）与其跳转规则。**没动**任何功能代码、
**没动**配置项本身、**没动**用户可见的语义文本（AI 记忆角色标签、对话翻译提示等保持原样）。
两树内容保持同步，差异只在预期处（版本号措辞、重锤 vs 暴击、空袭总开关所在小类、
`DataComponents.FOOD` vs `FoodProperties`）。

## 实测五百二十一【空袭索敌改为"以自身为圆心、半径 50 格的球"，每 tick 持续瞄准】

### 根因（反编译实证，不是推测）

TLM 的索敌是"传感器扫盒子 → 写可见列表 → 任务从列表里选"三段，
而**扫描盒的大小是按排班活动分流的**：

```
EntityMaid.searchDimension() {
    if (getScheduleDetail() == Activity.WORK) return getTask().searchDimension(this);
    return TaskManager.getIdleTask().searchDimension(this);   // ← 非 WORK 一律走这里
}
```

我们两个空袭任务覆写的 50 格盒子**只在活动恰为 WORK 时参与**；其余时段走 idle 任务的默认口径
（`IMaidTask.searchDimension` 默认 = `inflate(searchRadius, 4.0, searchRadius)`）——
**垂直半径只有 4 格**。空袭是立体作战，她一爬高十几格，敌人就直接掉出那个 4 格高的盒子。

目标掉出可见列表之后：`StartAttacking` 再也选不到它（它的目标来源就是那个列表）；
而空袭行为的 canStillUse 又要求"当前有目标"，目标一丢行为立刻停止、状态被 `forget()` 整清——
她在高空保持滑翔却**没有任何朝向指令**，于是顺着惯性飘出战场。这就是"飞太高 → 丢目标 → 往别处飞"。

### 改法：空袭索敌从 TLM 链路上整个摘出来

新增 `com.maidsmart.combat.FlightTargeting`（两树同源），每 tick 自己维持三件事：

1. **自己扫**以自身碰撞箱为中心、半径 **50 格的球**——AABB 膨胀 50 格做粗筛、距离平方
   （`2500.0`）精筛成球。**完全不看排班活动**，所以她爬多高都算。
2. 目标选定后**每 tick 回写** `ATTACK_TARGET` + `LOOK_TARGET`。TLM 那边什么时候擦、
   按什么判据擦都不再影响空袭：下一 tick 就补回来。

   **还有第二条丢目标的通路，一并被这一条堵住**：TLM 注册在空袭任务 brain 里的
   `StopAttackingIfTargetInvalid` 除了"目标死了/换了维度/不可攻击"之外，还有一条
   **200 tick「寻路到不了就放弃」**的擦除判据——反编译实证它读的是
   `CANT_REACH_WALK_TARGET_SINCE`（1.20.1 `f_26326_`，官方 mappings 名 =
   `cant_reach_walk_target_since`），而那个记忆由 `MoveToTargetSink` 在"路径算不出来"
   时写入。她在高空盘旋时寻路本来就经常算不到地面目标，于是这条判据会把目标判成
   "追不到"而擦掉。现在因为每 tick 都回写，擦掉与否对空袭都不再有影响。
3. 已锁定的目标只要"还活着 + 同维度 + 能攻击（`canAttack`）+ 在活动范围内
   （`isWithinRestriction`）+ 仍在 50 格内"就**继续用它，不再要求视线**。
   这一条正是"持续瞄准"的实质——她飞高时与地面敌人之间的视线经常被地形/树叶/怪物身体挡住，
   旧口径下这会让她把目标判成"不可见"而放弃；现在视线只作为**新锁定**的门槛。

**与 TLM "隔墙不出手"没有任何冲突**：新锁定仍然要过 `Sensor.isEntityAttackable`
（内含 `TargetingConditions` → `Sensing.hasLineOfSight`，与 TLM 原版逐环一致），
墙后的怪不会被新锁定；改变的只是"已经打起来的目标不会因为她飞高、视线被挡一下就被丢掉"。

### 实现细节（两处必须交代）

- **同 tick 去重**：空袭行为同一 tick 会调三次索敌（`checkExtraStartConditions` /
  `canStillUse` / `tick`）。加了 `RESOLVED_TICK` 表按 gameTime 去重，保证三次给出**同一个答案**——
  否则会出现"启动判定看到一个目标、运行中维持的是另一个"的分裂，同时省掉两遍重复扫描与 brain 写入。
- **扫描限频**：行为未运行时 Brain 每 tick 仍会评估 `checkExtraStartConditions`。
  若不加限制，没有敌人的空袭女仆会每 tick 扫一次 50 格 AABB 盒。加了 `SCAN_INTERVAL = 10`
  （0.5 秒），只在"没有锁定"时生效；一旦锁定成功，维持分支短路，扫描根本不参与。

状态表用 `WeakReference` 存目标、并由 `MaidFlightCombatBehavior.forget/clearAll` 在
行为收尾、女仆移除、服务器停止时清空（与既有的状态表同一套回收路径）。

### 验证

两树编译零错；字节码核对（javap）：`FlightTargeting` 成员/字段齐全、
`RANGE` 常量值 = `50.0`、`RANGE_SQR` = `2500.0`、
**视线门只在 `scan`（新锁定）里、不在 `keepable`（维持锁定）里**、
`keepable` 含活动范围与 50 格距离判据、空袭行为已调 `FlightTargeting.resolve/forget/clearAll`
且**不再直读** `ATTACK_TARGET`/`f_26372_`（调用数 0）；
打包 → `deploy_all.py` 三端 `match=True`；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

### 一点自查

我这轮校验脚本自己写错了三次判据（double 的字节序、1.20.1 视线门方法名多打了个下划线、
编译期常量被内联后不该再找字段名），一度报出 4 条假 FAIL。三处都核对过字节码原样后才改判据，
最终 `ALL PASS`——**是脚本错，不是代码错**，记在这里免得下次再踩。

﻿## 实测五百二十【投喂食物改为"勾选面板"：像矿表那样点图标切换能不能吃】

### 改法：照矿表/替代品表那套子页做

原来那个"逗号分隔输入框"（实测五百一十九 留的）换成与**矿物名单/替代品名单完全同款**的勾选子页。
入口在「战斗与自保 → 贴身辅助 → **投喂食物勾选**」（按钮显示"可喂 X / 不能吃 Y"）。

进去之后：

- **网格里列出全部带食物属性的物品**——包括所有模组食物（三明治等）。数据源是新建的
  `ensureFoodCache()`：遍历全部注册物品，取 `getFoodProperties() != null`（1.21.1 是
  `DataComponents.FOOD`）的，**不套黑名单**（黑名单就是本页勾选的结果，不能用来过滤候选）。
- **点图标切换能不能吃**：绿框 + ✔ = 能喂；红框 + ✖ = 不能吃。悬停时右侧显示中文名/英文 id +
  "当前：能吃（点击改成不能吃）"。
- 搜索框（中英文皆可）、翻页、下方"不能吃"名单（每行带「允许吃」按钮可一键恢复）、
  输入框（直接输入注册名列为不能吃，回车生效）——全部与矿表子页同交互。
- 底部提示实时显示"共 X 种食物，当前可喂 Y 种"。

**语义**：打勾 = 女仆可以喂（默认全勾），取消勾选 = 列为「不能吃」= 写进 `aidFoodBlacklist`。
与反馈的直觉一致——"打勾的就能吃"。

### 一个必须交代的坑（我自己踩了）

移植到 1.21.1 时我直接复制了 1.20.1 的列表类，结果 neo 侧编译报了一串"需要';'"。
根因是 **`ObjectSelectionList` 的构造签名两树不同**：1.20.1 是 6 参（`top, bottom, itemHeight`），
1.21.1 是 5 参（`top` 与 height 合并）+ 没有 `setRenderBackground`/`setRenderTopAndBottom`、
滚动条覆盖方法是 `renderItem` 不是 `renderScrollbar`。另外注册表取值 1.21.1 是
`BuiltInRegistries.ITEM.get(...)`（无 `getValue`）。
全部照 neo 既有的 `AltList` 写法改齐后编译通过——**这是我引入的错误，已修**。

### 验证

两树编译零错；字节码核对：`foodTable` 字段、`ensureFoodCache`、`isFoodChecked`、
`toggleFoodChecked`、`countFeedableFoods`、`AID_FOOD_BLACKLIST` 引用、食物属性读取、
`$FoodList` 内部类——两树**全部 YES 且一致**；打包 → `deploy_all.py` 三端 `match=True`；
`test_server.py 1201` 与 `neoforge1211` 均 PASS。

（配置项本身仍是同一个 `aidFoodBlacklist`，只是编辑界面从文本框换成了勾选面板；
此前设的值照旧生效，不需要重新配置。）

---

## 实测五百一十九【投喂判定：21 项原版白名单 → 通用判定 + 可配置黑名单】

### 根因：投喂链用的是 21 项硬编码原版白名单

`EmotionalActionExecutor` 里有个 `Set<ItemStack> FOODS`，内容是**手工列出的 21 个原版物品**
（熟牛排/猪排/羊肉/鲑鱼/鸡肉/兔肉/鳕鱼、兔肉煲/甜菜汤/蘑菇煲/南瓜派、金胡萝卜/烤马铃薯/面包/
胡萝卜/苹果/西瓜片/甜浆果/发光浆果/曲奇/海带干 + 蜂蜜瓶）。所有投喂判定都是
"遍历这个集合并按物品身份比对"——**任何模组食物（三明治等）都不在内**，
所以背包塞满也判"没有食物"→ 疯狂播报"我背包里没有吃的了"。

对比之下，TLM 自己"女仆饿了吃"那条链（`DefaultMaidHealSelfMeal.isHealMeal`，javap 实证）判据是
**"有 FoodProperties && 不在黑名单"**，从不看是不是原版——一个模组里两套食物哲学，
这正是本问题的根源。

### 改法（用户选定：通用判定 + 黑名单）

新增 `EmotionalActionExecutor.isFeedableFood(ItemStack)`，与 TLM 完全同口径：

```
有食物属性（1.20.1 Item.getFoodProperties() / 1.21.1 DataComponents.FOOD）
  && 不在 AID_FOOD_BLACKLIST 里
```

- **`FOODS` 白名单整体删除**（不是留壳）：两树字节码核对确认 `FOODS` 字段引用已彻底消失。
  任何注册了食物属性的物品——模组三明治、模组蛋糕、其他模组的任何食物——现在都能喂。
- **新增配置项 `aidFoodBlacklist`**（面板「战斗与自保 → 贴身辅助 → 投喂食物黑名单」，
  TextRow 逗号分隔编辑，与「危险方块表」同款交互）。留空 = 不排除任何食物。
- **默认黑名单**：腐肉/蜘蛛眼/毒马铃薯/河豚/紫颂果/可疑炖菜 + 全部生食
  （beef/porkchop/chicken/mutton/rabbit/cod/salmon/tropical_fish）+ **金苹果/附魔金苹果**。

### 一个必须说明的取舍：金苹果

旧白名单的注释里明确写着"金苹果/附魔金苹果【不在内】——走即时增益道具路径
（`MaidAidOwnerBehavior.useGoldenApple`）"。改成通用判定后，金苹果天然会满足"有食物属性"，
**如果不排除，低血时会被当普通食物喂掉**（浪费——金苹果的价值在吸收+再生，不是那 4 点饱食度）。
所以我把这两种也放进了默认黑名单，保住原有行为。这一点是**新口径引入的回归风险，已堵**。

### 改动的 6 处调用点（两树）

`MaidAidOwnerBehavior` 里六处"遍历 FOODS"逐处换成 `isFeedableFood(...)`：
主人链的背包/手持选优、`feedHealingFood` 的背包/手持选优、姐妹互助链的背包/手持选优、
`countFoods` 诊断、`isAidItem` 预检。判定逻辑一字未改，只换了"什么算食物"。

### 验证

两树编译零错；字节码核对：`FOODS` 字段引用两树两文件**均为 False**（白名单确实删除）、
`isFeedableFood` 调用数（本类 3 + 行为类 6，正是改动的 6 处）、
判定链正确（`Item.m_41473_` / `DataComponents.FOOD` → 取注册名 → 遍历黑名单比对）、
`MaidSmartConfig` 与 `PromaidConfigScreen` 均含 `AID_FOOD_BLACKLIST`；
四个 lang 文件（两树 × zh_cn/en_us）插入翻译键后 JSON 仍合法、BOM 保留；
打包 → `deploy_all.py` 三端 `match=True`；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

---

## 实测五百一十八【配置面板：排班表从「移动与行为」挪到「生产与工作」】

### 改法

`PromaidConfigScreen.Section` 里 `SCHEDULE` 的所属大类：

```java
// 改前
SCHEDULE("排班表", Group.MOVE),
// 改后
SCHEDULE("排班表", Group.WORK),
```

只改**所属大类**这一个参数——排班表小节的参数行（总开关、战斗还原宽限、可用性检测、
反向切换窗口/阈值/冷却、最短持有期、切段后大脑自省、排班活动半径）一行未动，
`MaidSmartConfig` 里的配置项更没动（改分组不影响存档/配置键）。

理由：排班表的本质是"给女仆安排各时段干什么活"（种地/挖矿/伐木/烹饪…），
属于**生产调度**而不是移动行为，放生产与工作下更符合直觉。

### 顺带同步了手册里的旧路径文案

`GuideContent` 里有 6 处硬编码了旧的面板路径，一并改掉（两树）：

- `「移动与行为 → 排班表」的「排班活动半径」` → `「生产与工作 → 排班表」…`
- `总开关：「模组详细配置 → 移动与行为 → 排班表」` → `… → 生产与工作 → 排班表」`
- 七个大类的清单：`①生产与工作：…/ 农场与宰杀；` → 末尾加 `/ 排班表；`
- 同一清单：`⑤移动与行为：移动与跟随 / 空闲与流畅 / 排班表 / 搭路；` → 去掉「排班表 /」
- 「怎么找某个开关」：`· 干活（…/宰杀）→ 生产与工作；` → 末尾加 `/排班`
- 同一段：`· 跟随/拉回/散步/排班/搭路 → 移动与行为；` → 去掉「排班/」

全库扫描确认：除上述改动说明注释外，**再没有**"排班表+移动与行为"的残留路径文案。

### 验证

两树编译零错；字节码逐常量核对所属大类：

| Section | 改后 Group |
|---|---|
| `SCHEDULE` | **WORK** ✅（原 MOVE） |
| `FOLLOW` / `IDLE` / `BRIDGE` | MOVE（未受影响，确认没误伤） |
| `BUILD` / `FARM` | WORK（同组其它成员照旧） |

两树结果一致；打包 → `deploy_all.py` 三端 `match=True`；`test_server.py 1201` 与
`neoforge1211` 均 PASS。

---

## 实测五百一十七【三叉戟在攻击模式下走近战，不再原地转圈】

### 根因：不是 TLM 不认三叉戟，是**我们自己的走位分类**按物品一刀切

先把三叉戟的事实用 `javap` 钉死（两树）：

- `TridentItem` **不是** `ProjectileWeaponItem`——1.20.1 是 `extends Item implements Vanishable`，
  1.21.1 是 `extends Item implements ProjectileItem`。
- 它的近战 `ATTACK_DAMAGE` = **8.0**（构造器里 `AttributeModifier(..., 8.0d, ADDITION)` 实证）。
- TLM 侧完全认它：`TaskAttack.isWeapon` = "主手装备槽带攻击力属性"（三叉戟满足），
  而 `MaidMeleeAttack` 只在 `isHoldingUsableProjectileWeapon`（**只认 `ProjectileWeaponItem`**）
  时才不出手 → **近战任务是愿意挥三叉戟的**。

所以"攻击模式不认三叉戟"不是 TLM 的限制。真正的问题在我们自己的
`MaidCombatTacticsBehavior`：它把 `main instanceof TridentItem` 写进了
**"物品即远程"**的判定里，于是拿三叉戟走攻击模式时——

1. `engageRange` 被撑到任务搜索半径（十几格就接管走位）；
2. `tick` 的主分支落进 `rangedTick` → 保持理想射程 ≥7 格 + **横移绕圈**。

这正是反馈里的"像个傻子一样在那边转圈"：她按远程风筝逻辑跑，而三叉戟在她手里
既投不出去（投掷要走 `trident_attack` 任务）、又不肯走近战。

顺带还有第二处：`findBestMeleeWeapon` 把三叉戟排除在"近战武器"之外，
导致远程任务被贴身时的近战反击**手里握着 8 点攻击力的三叉戟却按 1 点结算**。

### 改法：分类看【当前任务】，不看物品类型

三叉戟是**双用武器**，该按她当前在做什么来分：

```java
// MaidCombatTacticsBehavior 主分支（两树同款口径）
if (main.getItem() instanceof ProjectileWeaponItem
        || GunCompat.isGun(main)
        || isRangedTaskActive(maid)) {   // ← 取代原来的 instanceof TridentItem
    rangedTick(maid, target);            // 弓/弩/枪械/远程分类任务 → 风筝
} else {
    meleeTick(maid, target);             // 含 attack 近战任务持三叉戟 → 走近战
}
```

- `attack`（近战任务）持三叉戟 → **`meleeTick`**：蛇形逼近、跳劈暴击、贴脸绕圈——
  8 点攻击力的近战形态这才用得上。
- `trident_attack`（投掷任务）→ `isRangedTaskActive` 为真 → **`rangedTick` 风筝照旧**，
  远程机制一点没动。
- 判定口径与 `AutoCombatSwitch.isRangedTask`（同样按任务 UID 分类）对齐，两层不再打架。

`engageRange` 同步去掉 `TridentItem`（否则近战任务持三叉戟仍会被撑到搜索半径、
从十几格就被接管走位）。

`findBestMeleeWeapon` 也去掉 `TridentItem` 排除项——它现在是合格近战武器参与评分；
仍然排除的是**纯投射武器**（弓/弩 `ProjectileWeaponItem`、枪械），它们没有可用的近战形态。

**保留未动**：`isRangedAttacker`（判**敌人**手里拿什么）里的 `TridentItem` 照旧——
敌人拿三叉戟确实会投掷，那里的判定是对的。全类现在只剩这一处 `TridentItem` 引用
（字节码实证）。

### 验证

两树编译零错；字节码核对：全类仅剩 1 处 `TridentItem`（在 `isRangedAttacker`）、
主分支与 `engageRange`/`findBestMeleeWeapon` 均已改走 `isRangedTaskActive`，
两树一致；打包 → `deploy_all.py` 三端 `match=True`；`test_server.py 1201` 与
`neoforge1211` 均 PASS。

---

## 实测五百一十六【严格门控：中立威胁驱动的"写目标/挥砍"也归"主动参战"管】

### 根因：三件事里只有第一件被总开关管住

`NeutralThreatDriver`（实测三百四十四为"发狂的狼女仆没反应"加的兜底驱动）做三件事，
但只有 ① 在 `AutoCombatSwitch` 内部查了 `COMBAT_AUTO_SWITCH`：

| | 做的事 | 旧版门控 |
|---|---|---|
| ① | 触发参战（切战斗任务） | ✅ `COMBAT_AUTO_SWITCH` |
| ② | 往 brain 写 `ATTACK_TARGET` / `LOOK_TARGET` | ❌ **无** |
| ③ | 直接 `doHurtTarget` 挥砍 + 导航追人 | ❌ **无** |

我扫了两树全部配置引用，这个类**一个配置项都没读**（`引用的配置项: (无)`），且它在
`ProMaidExtension` 里是无条件注册的。于是关掉总开关后：任务不切（① 被挡），但 ②③ 照跑
——**任务 UID 不变、人却自己走上去挥刀**，正是反馈里的"工作模式没变却在打怪"。

### 为什么看着像"空手攻击"

`isArmed` 只查主手有没有 `ATTACK_DAMAGE` 属性。我用字节码确认 `DiggerItem` 构造器会给主手挂
`Attributes.f_22281_`（`"Tool modifier"`）——**镐/斧/锹/锄这些干活工具都算"有武器"**。
工作模式的女仆手里正拿着工具，所以判定通过、直接挥砍，伤害却只有工具那点加成，
观感就是"空手在打"。

### 为什么创造模式测不出

本驱动的威胁判据是行为化的 `mob.getTarget() == 主人/女仆`，而原版 `TargetingConditions.test`
会经 `canBeSeenAsEnemy()` 过滤目标，创造模式玩家的 `Abilities.f_35934_`（invulnerable）为 true
→ 该判定 false → **怪物根本不会把创造模式的玩家当成敌人**，`getTarget` 恒为 null → 本驱动
静默。切回生存（invulnerable=false）→ 怪物立刻锁定主人 → `getTarget` 就位 → 当轮即出手。

（同一环在 1.21.1 是 `Player.canBeSeenAsEnemy()` 读 `Abilities.invulnerable`，两树都实证过。）

### 改法（用户选定：严格口径）

在 `drive()` 里 ① 之后、② 之前插入总开关门控：

```java
if (!MaidSmartConfig.COMBAT_AUTO_SWITCH.get()) {
    boolean mine = ASSIGNED_TARGETS.remove(maid.getUUID()) != null;
    ATTACK_CDS.remove(maid.getUUID());
    if (mine) {
        maid.getBrain().eraseMemory(MemoryModuleType.ATTACK_TARGET);
        maid.getBrain().eraseMemory(MemoryModuleType.LOOK_TARGET);
    }
    return; // 关了主动参战 → ②写目标 / ③挥砍 一律不做（只挨打不还手）
}
```

三点设计说明：

- **放在 ① 之后**：威胁扫描与"威胁解除清理"（`threat == null` 时那两个 `remove`）照常运行，
  保持 Map 不积压；只是不再主动出手。
- **顺手收回自己写过的目标**：关掉开关时若她脑里还留着**我们写的** `ATTACK_TARGET`，
  TLM 的攻击行为仍会照着打。所以用 `ASSIGNED_TARGETS`（只登记"我们写过的"）精确判断，
  命中才清，**不碰 TLM 自己写的目标**（战斗任务的女仆由 TLM 传感器写目标，本表无记录）。
- **两树同步**（1.20.1 SRG / 1.21.1 Mojmap）。

### 行为后果（如实交代，这是取舍不是缺陷）

按严格口径，**关掉"主动参战"后女仆被怪咬只会挨打、不还手**——除非另外开「单兵战术」
（`COMBAT_TACTICS`，默认开，它有自己的开关与战术行为，不受本门控影响）。
这正是用户选定的语义："关了主动参战，女仆就只挨打不还手。"

另：重锤猛击（1.21.1 专属）有独立的 `COMBAT_MACE_SMASH` 开关、且只在战斗任务下激活，
不属于本次漏网范围，未改动。

### 验证

两树编译零错；字节码核对：`getstatic COMBAT_AUTO_SWITCH` 紧跟 `tryEngagePublic`（①）之后，
而 `eraseMemory`/`doHurtTarget`（②③）均在其后——门控位置正确；`ASSIGNED_TARGETS` 只被
本类与 `assignedTargetOf`（无人调用）引用，改用精确判断无外部影响；打包 → `deploy_all.py`
三端 `match=True`；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

---

## 实测五百一十五【空袭索敌：只加诊断日志，数值不动——并交代一个已证实的结构性堵点】

### 排查结论：不是"范围太小"，是**我们的半径在大多数时段根本不参与**

我先把 `EntityMaid.searchDimension()` 用 `javap` 反编译看了实现（两树同款，1.20.1 走
`m_37980_`，1.21.1 走 `Activity.WORK`）：

```
if (getScheduleDetail() == Activity.WORK) return getTask().searchDimension(this);
return TaskManager.getIdleTask().searchDimension(this);   // ← 非 WORK 一律走这里
```

`getScheduleDetail()` 是按**当前时间**返回排班活动的（`getDayTime() % 24000` 查
`MAID_DAY_SHIFT_SCHEDULES` / `MAID_NIGHT_SHIFT_SCHEDULES`）。所以：

- **只有排班活动恰为 WORK 时**，`MaidNearestLivingEntitySensor` 拿到的才是我们覆写的那
  个 `inflate(50,50,50)` 盒子；
- **其余时段**（夜间 REST、以及一切非 WORK 段）盒子来自**空闲任务**的默认实现
  `IMaidTask.searchDimension`：水平 = `searchRadius()`（= 限制半径 `m_21535_`），
  垂直**硬编码 4.0**。

也就是说 实测五百零八/五百一十三 那两次调半径，在非 WORK 时段**等于没改**。这本身就足以
解释"范围仍然很奇怪"，而不是"50 还不够大"。既然如此，把 50 加到 200 也只会**在 WORK 时段
生效**，非 WORK 照样是空闲口径——所以按用户指示**保持原样，先取证**。

（另：`Level.getEntitiesOfClass` 一链没有任何 AABB 体积上限——`EntityGetter` 的默认实现只
判 `getSize() < 1.0E-7` 才早退，所以 200 格扫描本身可行，不存在"盒子太大静默失效"的问题。
这一点也顺手排除了。）

### 改法：新增只读诊断探针 `FlightTargetProbe`（两树同步）

按 TLM 原版那条链**逐环计数**，让日志直接指出候选是在哪一环被滤掉的：

| 计数名 | 判据（全部照 `javap`，不新造） |
|---|---|
| `候选` | 我们自己按 `maid.searchDimension()` 复扫 AABB（`Level.getEntitiesOfClass`）拿到的活体总数 |
| `存活` | `isAlive() && e != maid`（TLM 传感器 lambda 的唯一过滤） |
| `可攻击` | `EntityMaid.canAttack(e)` → `IAttackTask.canAttack` → `DefaultMonsterType.canAttack`（动物/村民/玩家/盔甲架在这一环出局） |
| `活动范围内` | `EntityMaid.isWithinRestriction(blockPos)`（有 home/工作点限制时越界出局） |
| `有视线` | `Sensor.isEntityAttackable(maid, e)`——**含视线判定**，"隔墙不出手"就在这里 |
| `选中` | 五环全过里最近的那个（= TLM 会选中的目标） |

日志同时打印 **`activity=`（当前排班活动）** 与 **`盒尺寸=x/y/z`**——这两个字段是本次的关键：
一眼就能看出"此刻盒子到底多大、是不是走了空闲口径"。格式：

```
[promaid/空袭索敌] maid=<名字> task=<任务 uid> activity=work 盒尺寸=100.0/100.0/100.0 候选=3
  → 存活=3 → 可攻击=1 → 活动范围内=1 → 有视线=0 → 选中=-
```

（上例就是一例"看得见却打不着"：可攻击、范围内都过了，卡在视线那一环。）

**挂载点**：`MaidToolAutoEquipBehavior.checkExtraStartConditions`（core 行为、任何
activity 都跑、每 tick 一次）。挂这里是为了拿到**真实的排班活动字段**——搜索盒子正是按它
分流的。

**频率**：每女仆每 40 tick（2 秒）最多一条，且**只在空袭任务 + 当前没有攻击目标**时打印
（正是"锁不到"的现场）。有目标或非空袭任务一律静默，零刷屏。走 `PromaidLog`，受「运行日志」
总开关控制，同时写 `logs/promaid.log` 与 `latest.log`；搜关键词 **`空袭索敌`**。

**纯只读**：不写任何 brain 记忆、不改任何状态、不参与任何战斗决策——一次扫描 + 计数 + 落盘。

### 明确的边界（如实交代）

- **数值一个字没动**：`FLIGHT_SEARCH_RADIUS` 两树仍是 50.0，没有改成 200——按用户指示办。
- **本次没有"修复"任何东西**：只加了观测手段。上面那条 WORK 门控是**已证实的事实**，但
  "用户实际遇到的是不是正是它"要等日志说话——探针的 `activity=` 字段就是用来验这一点的。
- 探针是**一次性排查工具**，验证完可直接删（类注释里已写明）。

### 验证

两树编译零错；字节码逐项核对：五环判据（`canAttack`/`isWithinRestriction`/
`isEntityAttackable`/`Sensor.m_26803_`）、盒尺寸三访问器、`activity` 名、实体类型名、
core 行为里的挂载点，两树各自落位正确；打包 → `deploy_all.py` 三端 `match=True`；
`test_server.py 1201` 与 `neoforge1211` 均 PASS。

---

## 实测五百一十四【副手展示改用"实际消耗的那枚火箭"——修正五百一十一的错误判断】

### 我上一轮错在哪

实测五百一十一 我让副手展示一枚**朴素火箭**（`new ItemStack(Items.FIREWORK_ROCKET)`，
不写 `Explosions`），理由写的是"避免被当弩弹药吃掉"。这次去核实，发现**这个理由方向反了**：

- 我先验证了烟花火箭的**物品模型是静态的**——`models/item/firework_rocket.json` 全文只有
  `{"parent":"item/generated","textures":{"layer0":"minecraft:item/firework_rocket"}}`，
  **没有任何 overrides、不读 NBT/组件**（1.20.1 与 1.21.1 的 extra jar 都实证过）。
  所以"带不带爆炸组件"在手部看上去**完全一样**——我那句"观感一致"是对的，
  但"必须换掉真实那枚"就没有依据了。
- 而所谓"被当弹药吃掉"的风险**根本不存在**：展示窗口内若正好开弩，
  `takeExplosiveFirework`（副手优先）读到的是**真实的爆炸烟花**，它本来就是合格弹药；
  反倒是朴素火箭万一被当弹药打出去才是 **0 伤害**（`FireworkRocketEntity.dealExplosionDamage`
  开头 `if (!this.hasExplosion()) return;`）。也就是说我为了"避险"引入了一个更糟的假想风险。

用户一句"应该用使用的那个火箭的那个贴图吧"点破了这层：既然模型一样，
就该用**她真正消耗掉的那一枚**显示——语义也对（"掏出的就是刚才那枚"）。

### 改法

`FlightFireworkPose.show(EntityMaid)` → **`show(EntityMaid, ItemStack consumedFw)`**：

```java
ItemStack display = (consumedFw != null && !consumedFw.isEmpty())
        ? consumedFw.copy()                     // 用实际那枚（模型与它完全一致）
        : new ItemStack(Items.FIREWORK_ROCKET); // 兜底（理论上不会走到）
display.setCount(1);
maid.setItemInHand(OFFHAND, display);
```

四个调用点（两树 × 近战起飞 / 远程补推）都改成把 `takeFirework` 取出的 `fw` 传进去。
其余部分（10 tick 保持、原物快照与还原、core 行为里的归还入口、`clearAll`）**完全不动**。

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap -p -c` 反汇编构建产物：`FlightFireworkPose.show` 签名已是
  `(EntityMaid, ItemStack)` 两参，方法体里能看到
  **"参数非空且非空栈 → `consumedFw.copy()`，否则 `new ItemStack(Items.FIREWORK_ROCKET)`"**
  的分支（字节码 `ifnull` / `isEmpty` / 两条 `copy()` 路径清晰可见）；
  两树调用点的 `show` 调用均为 2 参形式；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.833s）、`test_server.py neoforge1211` PASS（Done 0.994s）。

已部署待实测：空袭放烟花那一瞬，副手显示的就是**你包里被消耗掉的那枚**烟花（外观与它一致），
约 0.5 秒后自动换回原副手物品（盾牌/食物）；其余行为与五百一十一 相同。


## 实测五百一十三【空袭索敌：三轴统一 50 格（以自身为圆心），隔墙不出手由原版保证】

### 演进
实测五百零八 先把**垂直**半径从 TLM 默认的 4 格提到 50 格，水平仍沿用 `maid.searchRadius()`
（默认 16）。当时刻意"只动垂直"是保守取舍，但实测下来**水平 16 格仍然太小**——空袭是立体作战，
敌人在斜上方 20~40 格时水平距离早就出界了。本条按需求把三轴统一。

### 改法

```java
private static final double FLIGHT_SEARCH_RADIUS = 50.0;

@Override
public AABB searchDimension(EntityMaid maid) {
    if (maid.m_21536_()) {   // 有 home/工作点限制 → 以限制中心为基准（与 TLM 同款）
        return new AABB(maid.m_21534_()).m_82377_(50, 50, 50);
    }
    return maid.m_20191_().m_82377_(50, 50, 50);
}
```

`AABB.inflate(50, 50, 50)` 是三轴**独立**膨胀，等价于"以自身碰撞箱为中心、半径 50 的立方盒"
（边长 100）。**水平/垂直一视同仁**，且**已彻底不再引用 `searchRadius`**（字节码可见
`m_21535_`/`searchRadius` 在这两个方法里归零）——水平不再受 TLM 默认 16 格限制。

### 隔墙不出手：由 TLM 原版机制保证，我们没动这条链路

这是本条最需要说清的一点——**"放大扫描范围"与"能不能打"是两件事，分属两层**：

| 层 | 谁负责 | 作用 |
|---|---|---|
| 扫描范围 | **本方法** `searchDimension` | 决定 `MaidNearestLivingEntitySensor` 用多大的 AABB 去扫，结果写进 `NEAREST_VISIBLE_LIVING_ENTITIES` |
| 能否出手 | **TLM 原版** `IAttackTask.findFirstValidAttackTarget` | 从该记忆里 `findClosest(predicate)` 选目标 |

而 `NearestVisibleLivingEntities` **自带视线过滤**：它的构造器把 `Sensor.isEntityAttackable`
作为可见性谓词（字节码实证），后者一路走到 `TargetingConditions.test`，其 `ignoreLineOfSight`
字段默认 false → 最终查 `Sensing.hasLineOfSight`。

所以：**墙后的怪能进候选列表，但会在可见性那一关被滤掉**——放大范围不会让女仆隔墙出手。
这正是"和 TLM 原版一样，有隔墙阻挡不出手"的要求，且我们**没有修改这条链路的任何一环**。

（另外，实测五百零五 给自造的那几条近战链路补过 `hasSight`——那是另一层保险，
针对的是我们绕过 TLM 索敌直调 `doHurtTarget` 的路径，与原版这条链路互不冲突。）

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap -p -c` 反汇编构建产物，四处逐一确认（按方法边界切出 `searchDimension` 全文）：
  - 每个 `inflate`/`m_82377_` 调用的三个参数都是 **`ldc2_w 50.0d` × 3**（常量序列
    `['50.0','50.0','50.0','50.0','50.0','50.0']`——两个分支各三个）；
  - `searchRadius`（`m_21535_` / `searchRadius`）在方法体内**出现 0 次**（已不再依赖）；
  - 两分支（有/无活动范围限制）都保留。
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.816s）、`test_server.py neoforge1211` PASS（Done 0.888s）
  ——本条是服务端行为（索敌传感器跑在服务端），回归有参考价值。

已部署待实测：空袭时 50 格内（含斜上方/正上方）的敌人都能被锁定；隔墙的目标仍不出手
（与原版一致）；守家/工作点受限的女仆索敌盒以限制中心为基准。


## 实测五百一十二【空袭落地雪/水无限重放 → 女仆无法起飞；内置 2 秒冷却】

### 根因：空袭分支复用了 `clutchedThisFall`，而它在回收时被清零

`WaterClutchBehavior.recoverFluid()` 结尾有一行：

```java
// v1.5.25e：收水后允许再放一次——极高塔坠落时 1 秒水可能提前收，
// 落地前最后一段若速度仍达标则再放一次接住
this.clutchedThisFall = false;
```

这行对**通用落地水**是合理的（极高塔坠落确实需要补一次）。但空袭分支
（`isFlightAirborne`）复用了**同一个标志**，于是变成无限循环：

1. 空袭落地缓冲触发 → `clutchedThisFall = true` → 放出雪垫；
2. 保持时长（雪默认 100 tick / 水按配置）到期 → `recoverFluid()` → **标志被清零**；
3. 下一个 tick：条件再次满足 → 又放一次 → 再清零……**无限循环**。

**为什么"落地了还在触发"**：细雪**不是实心方块**——实体是沉进雪里的，`onGround()`
一直不成立，所以那行"落地即重置"（`if (maid.onGround()) clutchedThisFall = false;`）
在雪里**永远不执行**；而 `recoverFluid` 又会清零，两条路径叠加就让她在雪里/雪外
反复重放。表现即"落地后一直释放、雪花不停铺、行为槽一直被占 → **无法起飞**"。

### 改法（内置 2 秒 CD）

新增独立字段 `flightClutchCd`（下一允许触发的 tick）与常量 `FLIGHT_CLUTCH_CD = 40L`
（2 秒），**只作用于空袭分支**：

```java
&& gameTime >= this.flightClutchCd     // 条件里加这一条
...
this.flightClutchCd = gameTime + FLIGHT_CLUTCH_CD;   // 触发时记冷却
```

**刻意与通用分支解耦**：`recoverFluid()` 里那行"收水后再放一次"原样保留、不动它的语义
（那是为通用落地水的极高塔场景服务的）。CD 是独立字段，`recoverFluid` 不碰它，
所以空袭分支的循环被彻底切断，而普通坠落的落地水行为完全不受影响。

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- 改动过程中我自己出过一次错：第一次编辑误删了 `boolean flightWater = ...` 那一行，
  靠括号平衡核对（`{`/`}` 各 61/69、diff=0）与"`boolean flightWater =` 出现次数 = 1"
  发现并补回——这类结构性错误编译不一定第一时间报，所以每次改完都会核对；
- `javap -p -c` 反汇编构建产物，两树 `tick` 方法体内确认：
  `getfield flightClutchCd` + `lcmp`（比较 `gameTime >= cd`）、
  `ldc2_w 40l` + `putfield flightClutchCd`（触发时写入 2 秒冷却）——两树完全一致；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.748s）、`test_server.py neoforge1211` PASS（Done 1.059s）
  ——本条是服务端行为（放块/回收都在服务端 tick 里），回归有参考价值。

已部署待实测：空袭落地放一次雪/水后，2 秒内不再重复释放；落地后能正常起飞；
普通（非空袭）坠落的落地水"收水后再补一次"语义不变。


## 实测五百一十一【放烟花的瞬间副手"亮一下"烟花模型（纯表现）】

### 背景
实测五百一十 已把副手**永久让出**给盾牌/食物（烟花改为从背包按需取用，发射路径自己造弹体）。
功能上正确，但"她在放烟花"这件事完全看不到了——本条只补**动作表现**：发射那一刻把烟花
放进副手 0.5 秒，然后**把原副手物品原样还回去**。

### 实现：新增 `FlightFireworkPose`（两树各一份，约 130 行）

- **不碰任何运行逻辑**：不改弹药判定、推力、冷却、三件套就绪判定。只是在 `show()` 里
  `setItemInSlot(OFFHAND, 烟花)`，`tick()` 到点还原。烟花在手上**不参与任何计算**。
- **原物必还**：放置前先 `getOffhandItem().copy()` 快照整栈（含 NBT/组件/数量），
  到点整体写回——盾牌/食物绝不会丢。
- **有界**：每女仆至多一条倒计时记录（`HOLD_TICKS = 10`，0.5 秒），到点自清。
- **同步**：`setItemSlot` / `setItemInHand` 走 `LivingEntity` 的装备槽同步
  （`EntityMaid` 覆写了 `m_8061_`），服务端一改客户端立刻看得到手里的烟花——**不需要新网络包**。
- **固定展示一枚朴素火箭**（`new ItemStack(Items.FIREWORK_ROCKET)`，不写 `Explosions`）：
  烟花火箭在**手部的物品模型与有没有爆炸组件无关**（颜色只在飞行轨迹里体现），观感一致；
  而这样能避开一个交互风险——展示窗口内若正好开弩，TLM 的 `getProjectile`（副手是烟花就交出来）
  与我们的 `takeExplosiveFirework`（副手优先）都会先读副手，展示"带爆炸的那枚"就可能被当弹药吃掉。

### 归还点选在 core 行为（这是本条最关键的设计）

第一版我把归还在 `forget()` 里做，但 `forget(UUID)` **拿不到实体引用**，清表会把"原副手物品"
快照一起丢掉 → 若行为在 10 tick 窗口内被强杀（切任务/目标丢失/死亡），盾牌/食物会**永久**留在
烟花状态。这不可接受，所以改成：

**归还挂在 `MaidToolAutoEquipBehavior.canUse`** —— 那个 core 行为任何 activity 都跑、每 tick 一次
（正是本项目自己注释里写的"任何 activity 都跑"），且空袭分支里本来就调了 `MaidFlightKit.equip`。
`FlightFireworkPose.tick(maid)` 放那里 = **无论飞行行为是否在跑，最迟 10 tick 后一定还原**。
`forget()` 因此刻意**不清表**（注释里写明了原因），由 `clearAll()` 在服务器停机时收尾。

### 接线（两树各 4 处）

| 位置 | 调用 |
|---|---|
| `MaidFlightCombatBehavior` 近战空袭起飞（`tryLaunch`） | `FlightFireworkPose.show(maid)` |
| `MaidFlightCombatBehavior` 远程空袭掉高补推（`tickRangedAir`） | `FlightFireworkPose.show(maid)` |
| `MaidToolAutoEquipBehavior`（core，每 tick） | `FlightFireworkPose.tick(maid)` ← **归还入口** |
| `MaidFlightCombatBehavior.clearAll()` | `FlightFireworkPose.clearAll()` |

### 验证

- 两树编译零错（新文件已重跑 `gen_compile.py` / `gen_compile_neo.py` 进 argfile）；
- `javap -p -c` 反汇编构建产物：
  - `FlightFireworkPose`（两树）六个方法齐全（`show`/`tick`/`restoreNow`/`forget`/`clearAll`/`restore`），
    且含副手写入（`m_21008_` / `setItemInHand`）、副手快照读取（`m_21206_` / `getOffhandItem`）、
    快照用 `copy()`（`m_41777_` / `copy`）、以及 `Items` 常量（朴素火箭）；
  - **调用点逐一计数**：`MaidFlightCombatBehavior` 里 `show` = **2**、`clearAll` = 1；
    `MaidToolAutoEquipBehavior` 里 `tick` = **1**——两树完全一致，与上面接线表相符；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.532s）、`test_server.py neoforge1211` PASS（Done 0.986s）
  ——本条虽是表现层，但槽位操作与还原都在**服务端**执行，回归能覆盖"新增类不崩服"。

已部署待实测：空袭放烟花（起飞/掉高补推）那一瞬，副手会短暂显示一枚烟花火箭（约 0.5 秒），
随后**自动换回你放在副手的盾牌/食物**；期间盾牌/食物只是被暂时盖住、不会丢失；
飞行机制、弹药消耗、三件套判定全部不变。


## 实测五百一十【空袭锁死副手 → 无法进食回血；副手让出，烟花改为按需取用】

### 根因：`equip()` 把烟花常驻副手，且写进了"就绪"判定

`MaidFlightKit.equip` 旧版做三件事：胸甲穿鞘翅、主手换武器、**副手放烟花**——
最后一步是长驻占用，而且就绪判定也要求副手必须是烟花：

```java
// 副手：烟花
if (!isFirework(hands.getStackInSlot(1))) {
    ItemStack fw = takeFromBackpack(maid, MaidFlightKit::isFirework);
    if (!fw.isEmpty()) { hands.setStackInSlot(1, fw); ... }
}
return ok && ... && isFirework(hands.getStackInSlot(1));   // ← 副手必须是烟花
```

后果：**空袭期间副手被烟花锁死**——盾牌装不进去，食物也装不进去，于是 TLM 的
回血吃（`MaidHealSelfTask`，扫"主手/副手/背包"、换手时要把旧物腾出来）拿不到东西，
"空战无法进食回血"。用户的判断是对的。

### 关键事实：烟花根本不需要拿在手上

发射走的是 `MaidFlightCombatBehavior.launchFirework`，它自己造弹体：

```java
ItemStack rocketStack = new ItemStack(Items.FIREWORK_ROCKET);   // 全新一枚
fireworks.m_128405_("Flight", 1);                               // 只写 Flight、不写 Explosions
rocketStack.m_41784_().m_128365_("Fireworks", fireworks);
new FireworkRocketEntity(level, rocketStack, maid);             // 直接入世界
```

**全程不读女仆手里的物品**——方法签名里那个 `ItemStack consumedIgnored` 参数就是证据
（收了却不用）。所以"常驻副手"纯属多余占用。

### 改法

1. **副手完全让出**：`equip()` 删掉副手放烟花那一段，**不再写副手任何槽位**——
   盾牌/食物可以正常装在副手。
2. **起飞门槛改为"烟花在任意位置"**：就绪判定由 `isFirework(副手槽)` 改为 `hasFirework(maid)`。
   而 `hasFirework` 本来就是"主手 / 副手 / 背包"三处覆盖（实测五百零二已对齐），
   所以烟花放背包、放主手、放副手都认。
3. 同步修正了 5 处已不适用的注释/文案（代码本就正确、描述已过时）：
   - `MaidFlightKit.equip` 与类头："副手拿烟花" → "不再占用副手"；
   - `MaidToolAutoEquipBehavior`（两树）的幂等性说明：从"副手已有烟花"改为"烟花在身上任意位置"；
   - `MaidFlightRangedTask`（两树）"两种身份抢同一个副手槽" → 说明已不抢槽，但**"只认带爆炸的烟花"
     这条规则必须保留**（0 伤害烟花当子弹打等于白烧燃料）；
   - neo 的用户可见配置文案（`MaidSmartConfig` + `PromaidConfigScreen`）：
     "主手换重锤、副手拿烟花" → "主手换重锤（烟花不必拿在手上，副手留给玩家放盾牌/食物）"。

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap -p -c` 反汇编构建产物，精确切出 `equip` 方法体（按方法声明边界，不靠字符串搜索）：

| 树 | `equip` 行数 | `setStackInSlot` 次数 | 槽位常量 | 就绪判定 |
|---|---|---|---|---|
| 1.20.1 | 117 | **1** | `iconst_0`（主手） | `hasFirework(maid)` |
| 1.21.1 | 129 | **1** | `iconst_0`（主手） | `hasFirework(maid)` |

  即**副手槽（`iconst_1`）零写入**，且就绪链是
  `isUsableElytra(胸甲) → isWeaponForTask(主手) → hasFirework(整个女仆)`；
- 全库复查"副手 + 烟花"的所有提及，剩下都已是**正确**表述（描述 TLM 原版弩口径、
  或其它功能的副手用途），无残留误导；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.777s）、`test_server.py neoforge1211` PASS（Done 0.835s）
  ——本条是服务端行为（装备槽操作在服务端 tick 里跑），回归有参考价值。

已部署待实测：空袭时把盾牌/食物放副手不再被顶掉；缺血 ≥2 点时能正常进食回血；
飞行动力照常（烟花从背包按需取，发射推力不变）；三件套判定不受影响。


## 实测五百零九【岩浆垫方块自救被禁搭误伤——四百九十八收紧引入的回归】

### 根因：1.20.1 的放行名单从来没有岩浆这一条（SRG 名注反了）

`MaidPlaceGuard.blocked` 的放行名单本意是"落地 / 在水里 / **在岩浆里** / 骑乘 → 不拦"，
但 1.20.1 侧的 SRG 注释写的是：

```java
// SRG：m_20096_=onGround m_20071_=isInWater m_20069_=isInLava   ← 最后这个注反了
if (maid.m_20096_() || maid.m_20071_() || maid.m_20069_() || maid.m_20159_()) return false;
```

javap 复核（`Entity` 字节码）：

| 方法 | 实际实现 | 真身 |
|---|---|---|
| `m_20069_()` | `return f_19798_`（触水标志位） | **isInWater** |
| `m_20071_()` | `m_20069_() \|\| m_20285_()（下雨）\|\| m_20305_()（气泡柱）` | isInWaterRainOrBubble |
| `m_20077_()` | `!f_19803_ && f_19799_.getDouble(FluidTags.f_13132_=LAVA) > 0` | **isInLava** |

三个全是"水"，**真 isInLava 的 `m_20077_` 一次都没出现**——所以岩浆从来就没被放行过。

### 为什么以前没暴露、四百九十八之后才炸

岩浆不会像水那样把 `fallDistance` 清零（水有两条清零路径，都只认水 tag），
岩浆只有 `Entity.baseTick` 里一句 `f_19789_ *= 0.5f`（每 tick 减半）。
所以泡岩浆下沉时 `fallDistance` 会稳定停在一个**小正数**（稳态 ≈ 每 tick 下沉量）。

- **四百四十三那版**（阈值"≥ 落地水高度 6 格"）：岩浆下沉的小正数远小于 6 → 放行，垫方块自救正常；
- **四百九十八收紧成"只要在下落就禁（> 0）"**：岩浆下沉必然满足 → **禁搭** →
  `lavaStepUp` 里 `takeBuildBlock` 返回 null → **岩浆垫方块自救整条失效**。

所以这是四百九十八那次收紧**顺带引入的回归**，不是老问题。失效后的表现还很隐蔽：
她背包**只有方块**（没水桶/没末影珍珠/没抗火药）时会一路走到
"自救手段都用尽了，我没招了！"；而且 `announceNoMaterial` 自己也调 `blocked()` 来抑制误报，
连"背包里没有搭方块的材料"这个气泡都不会出——属于**静默失败**。

**1.21.1 侧一直是对的**（用 Mojmap 的 `isInLava()`，不存在注反问题），所以这是两树行为不一致。

### 改法

1.20.1 放行名单补上真 `m_20077_()`（isInLava），并把那行错误的 SRG 注释改对：

```java
if (maid.m_20096_() || maid.m_20071_() || maid.m_20069_() || maid.m_20077_()
        || maid.m_20159_()) return false;
```

保留 `m_20069_`/`m_20071_`（水与雨/气泡柱放行本身是对的），只加岩浆这一条。
1.21.1 代码无需改动，仅补注说明这条为何必须留——**两树现在同款**。

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap -p -c` 反汇编构建产物：1.20.1 `MaidPlaceGuard.blocked` 的放行段现在是
  `m_20096_ → m_20071_ → m_20069_ → **m_20077_** → m_20159_` 一路 `ifne` 到同一个"返回 false"，
  与 1.21.1 的 `onGround → isInWater → isInLava → isPassenger` 结构完全对齐；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 3.007s）、`test_server.py neoforge1211` PASS（Done 0.848s）。
  本条是**服务端行为判定**（放行名单在服务端 tick 里求值），回归有参考价值。

已部署待实测：泡岩浆时女仆照常垫方块脱离液面（无抗火/无火焰保护饰品也一样）；
水/雨/气泡柱里的放行不变；正常悬空/滑翔/坠落仍按四百九十八的口径禁搭（不受影响）。


## 实测五百零八【空袭的垂直索敌提到 50 格——修"凋灵飞高了就锁不上"】

### 根因：TLM 的索敌盒把垂直方向硬编码成了 4 格

女仆的索敌范围由 `IMaidTask.searchDimension(maid)` 决定，`MaidNearestLivingEntitySensor`
每 tick 用它的返回值做 AABB 扫描，扫到的结果写进 `NEAREST_VISIBLE_LIVING_ENTITIES`——
而 `IAttackTask.findFirstValidAttackTarget`（我们两个空袭任务注册的索敌行为）
正是从这个记忆里挑目标（反编译实证：`findFirstValidAttackTarget` 读 `f_148205_`）。

TLM 的默认实现（`IMaidTask`，两树同构）：

```java
default AABB searchDimension(EntityMaid maid) {
    float r = searchRadius(maid);
    if (maid.hasRestriction()) return new AABB(maid.getRestrictCenter()).inflate(r, 4.0, r);
    return maid.getBoundingBox().inflate(r, 4.0, r);
}
```

**注意中间那个 `4.0`——垂直方向被写死了，水平才是 `searchRadius`（默认 16）。**
所以空袭模式下女仆的垂直索敌只有 **4 格**：凋灵稍微飞高一点就出了这个盒，
传感器扫不到它 → 记忆里没有 → `findFirstValidAttackTarget` 选不出目标 → 失去锁定。
空袭本来就是打空中目标的模式，4 格垂直盲区完全不合理，这跟实测现象完全吻合。

**我们两个空袭任务都没覆写 `searchDimension`**（已核对：`MaidFlightCombatTask` /
`MaidFlightRangedTask` 原文里都没有这个方法），所以一直是吃 TLM 的默认值。

### 改法

在**两个空袭任务**（近战空袭 `MaidFlightCombatTask` + 远程空袭 `MaidFlightRangedTask`）
各覆写 `searchDimension`：**只把垂直半径提到 50 格**（按需求"提高到 50 格左右"），
水平仍沿用 `maid.searchRadius()`——保持与其它任务一致的接战半径，不改索敌广度，
避免"顺带把水平拉大"引入你没要求的行为变化。

两分支写法与 TLM 保持一致：有活动范围限制（home/工作点）时以限制中心为基准，
否则以自身碰撞箱为基准——这样守家中的空袭女仆不会因为索敌盒变大而跨出活动范围。

### 验证

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`）；
- `javap -p -c` 反汇编构建产物，**四处逐一确认**：
  - 都声明了 `searchDimension(EntityMaid)`（覆写生效）；
  - 方法体里能看到 `searchRadius()` 取值 + `AABB.m_82377_(dx, dy, dz)` / `inflate(...)` 调用，
    且中间那个参数是 **`ldc2_w 50.0d`**（垂直位），两侧是同一个水平值（`dload_3`）——
    即 `inflate(horizontal, 50.0, horizontal)`，正是预期形态；
  - 两个分支（`m_21536_()`/`hasRestriction()` 为真/假）都在。
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.603s）、`test_server.py neoforge1211` PASS（Done 0.959s）。
  **这一条的回归比前面几条有意义**：索敌跑在服务端（传感器 + brain 记忆），
  不是纯客户端渲染，所以服务端能正常加载并启动对本次改动是有参考价值的。

已部署待实测：空袭时凋灵飞高（50 格内）仍能被锁定并追击；地面目标行为不变；
守家/工作点受限的女仆索敌盒以限制中心为准，不会因此跑出活动范围。


## 实测五百零七【鞘翅光泽改回"只有真附魔才亮"——撤掉五百零四的常亮】

### 根因：这是五百零四我自己改出来的

实测五百零四那次把光泽参数固定成了 `true`（常亮）：

```java
var vc = ItemRenderer.m_115184_(buffer, RenderType.m_110431_(WINGS), false, true);
```

当时的理由是"本模组手册/排班表/协议源石都是常亮光泽的口径，顺手统一"——但**鞘翅跟那几件物品
不是一回事**：手册是我们自己的物品（常亮是我们的设计选择），而鞘翅是**玩家自己附魔的装备**，
光泽应当如实反映它有没有附魔。注释里我甚至留了"想改回只有附魔才亮就把 true 换回去"的话，
但默认值本身选错了，等于把"有没有附魔"这条信息抹掉了。

### 改法

四处（两树 × Bedrock/Gecko）全部改回与原版 `ElytraLayer` 完全同款的判定：

| 树 | 传参 |
|---|---|
| 1.20.1 | `chest.m_41790_()`（= `ItemStack.hasFoil`） |
| 1.21.1 | `chest.hasFoil()` |

**顺带确认了判定不会被本模组污染**：`MaidFlightKit.equip()` 只是把鞘翅从背包**原样搬进**
CHEST 槽（`m_8061_` / `setItemSlot`），全程不改写 NBT、不加附魔、不动 `Enchantments`——
所以 `hasFoil` 读到的就是玩家自己那件鞘翅的真实附魔状态。

**1.20.1 Gecko 层原本就是对的**（五百零四之前一直传 `chest.m_41790_()`），本次四处的差异
在于：1.20.1 两处 + 1.21.1 两处，其中 **1.21.1 那两处改前是硬编码 `false`（从来不亮）**，
被五百零四一并改成了 `true`，现在一并改为按附魔判定。

### 验证

- 两树编译零错。过程中修掉一个自己引入的错误：1.21.1 的 `LayerMaidElytra` 里原本没有
  `chest` 局部变量（那是内联调用 `maid.getItemBySlot(...)` 的写法），我直接写了 `chest.hasFoil()`
  → "找不到符号: 变量 chest"；已补上局部变量与 `ItemStack` 导入（该类原本没引这个 import）。
- `javap -p -c` 反汇编构建产物，四处逐一确认光泽位已从常量变为真实调用：
  - 1.20.1：`invokevirtual ItemStack.m_41790_:()Z` 出现在 foil 调用点前的常量序列末位
    （序列 `iconst_1, iconst_0, iconst_0, m_41790_`——前两参是 `noEntity`/`useTranslucent`，
    末位才是光泽位）；
  - 1.21.1：`invokevirtual ItemStack.hasFoil:()Z` 同位置（序列 `iconst_1, iconst_0, hasFoil`）；
  - 四处都含取胸甲槽的调用，且 `hasFoil`/`m_41790_` 判定存在。
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.770s）、`test_server.py neoforge1211` PASS（Done 0.905s）。
  注：本条同样是**纯客户端渲染**改动，服务端回归只证明"新 jar 没让服务器启动失败"，
  不对渲染效果构成证据——实际观感仍以客户端目视为准。

**待部署**：与上一条相同，`deploy_all.py` 检测到游戏实例在运行时会拒绝写入。
退出游戏后执行：

```
python deploy_all.py
```

已部署待实测：普通鞘翅（未附魔）不再有流光；给鞘翅附上任意附魔（如耐久/经验修补）后正常出现
附魔光泽；其余外观（尺寸/上移/展翅/前倾）不受影响。


## 实测五百零六【飞行作战"跟随俯角"前倾——让滑翔姿态向玩家鞘翅靠拢】

### 一、"游泳动作"的真身：确实是我们自己让它游泳的

本项目自己的 `MaidSwimGlideMixin` 让女仆滑翔时 `isVisuallySwimming()` 返回 true；而 TLM 的
Gecko 动画选择表 `AnimationRegister`（两树反编译实证）里，游泳动画的触发谓词正是它：

```java
AnimationRegister.register("swim", 0, (maid, event) -> maid.asEntity().m_6067_());   // 1.20.1
AnimationRegister.register("swim", 0, (maid, event) -> maid.asEntity().isVisuallySwimming());  // 1.21.1
```

酒狐（winefox）模型里确实带着 `swim` / `swim_stand` 两段动画（`winefox.animation.json`
实测有条目），于是滑翔期间播放的是**游泳动画**——身体水平俯卧、中轴直指目标，正是反馈里
"全身体正对敌人"的观感来源。

### 二、玩家鞘翅的权威配方（`PlayerRenderer.setupRotations`，字节码实证）

```java
float ramp = Mth.clamp(t * t / 100.0F, 0.0F, 1.0F);   // t = 连续滑翔 tick
poseStack.mulPose(Axis.XP.rotationDegrees(ramp * (-90.0F - player.getXRot())));
```

即"基础 -90° 之外**再减掉俯仰角**"。女仆这边的基础 -90° 由模型动画承担，所以本次补的
正是 **`- getXRot()`** 这一项——俯冲时（低头为正角）身体继续向前翻；平飞/盘旋（俯角≈0）
几乎无变化，不需要再分阶段判定。

这也是"大约 35 度"的准确含义：**35° 是俯冲角本身，不是一个要固定加上的角度**。所以本条
实现的是"跟随俯角"，而非硬编码 35°。

### 三、只改 Gecko 这条链

| 模型类型 | 渲染路径 | 本次处理 |
|---|---|---|
| Gecko（酒狐家族等） | `EntityMaidRenderer.render` 里 `isGeckoModel()` 为真 → 转交 `GeckoEntityMaidRenderer`（继承 `GeoReplacedEntityRenderer`） | **本次已改** |
| Bedrock（原版风格） | `EntityMaidRenderer` → `BedrockModel.setupAnim` | 未动（本次只做 Gecko） |

**为什么走 mixin 而不是 TLM 官方扩展点**：TLM 的 `ICustomAnimation` 确实同时提供
`setupRotations`（Bedrock）与 `setupGeckoRotations`（Gecko）两个回调，但 TLM 自己的
`SwimAnimation` **只覆写了 `setupRotations`、没有覆写 `setupGeckoRotations`**——说明 Gecko
路径的姿态根本不经过扩展点分发（它的俯卧来自模型自带动画）。扩展点够不到，必须注入
`GeoReplacedEntityRenderer` 的 `setupRotations` 本身。

注入位置选在 **RETURN**：该方法先调 `LivingEntityRenderer.setupRotations` 摆好朝向，此刻矩阵
仍在"Y 朝上、原点在脚下"的世界式空间（`scale(-1,-1,1)` 与 `translate(0,-1.501,0)` 尚未施加，
即模型渲染之前），原版所有飞行/俯卧姿态都在这一层用 `Axis.XP` 施加——轴向与枢轴因此与游戏
一致，叠加而非覆盖模型动画。

### 四、触发口径

`MaidFlightKit.isFlightTask(maid) && !onGround()`——与鞘翅图层的展翅口径完全一致。
**刻意不认 `isFallFlying()`**：收翅猛击那段 `tickSpin` 会主动 `setGliding(false)`（收翅才吃得到
猛击判定），只认滑翔位的话"飞向敌人"最需要前倾的那一段恰好不生效。

### 五、可调与验证

唯一调节量 `FlightDiveTilt.TILT_FACTOR`（默认 1.0 = 与玩家鞘翅同款）：嫌翻太多调小、嫌不够
调大；方向若相反，把 `apply` 里的取负去掉即可（一行）。纯客户端渲染，**不动任何飞行机制**、
不加网络包、服务端无感。

- 两树编译零错（1.20.1 `EXITCODE=0`；1.21.1 `exit=0`，仅既有警告）；新文件已重跑
  `gen_compile.py` / `gen_compile_neo.py` 进 argfile；
- `javap` 反汇编构建产物逐项确认：
  - `FlightDiveTilt` 含 `MaidFlightKit.isFlightTask` + `onGround`（两树）+ `getXRot`/`m_146909_`
    + `Axis.XP`/`f_252529_` + `rotationDegrees`/`m_252977_` + `mulPose`/`m_252781_`；
  - `MaidFlightDiveTiltMixin` 调 `FlightDiveTilt.apply`，注解值经 `javap -v` 提取为
    **`method="m_7523_"`（1.20.1）/ `method="setupRotations"`（1.21.1）**，`at=RETURN`；
- **注入点离线核对**：TLM 目标类 `GeoReplacedEntityRenderer` 里对应方法确实存在，且描述符
  与 mixin 签名逐一匹配——1.20.1 是 `protected void m_7523_(T, PoseStack, float, float, float)`
  （**5 参**，与原版 `LivingEntityRenderer` 一致），1.21.1 是
  `protected void setupRotations(T, PoseStack, float, float, float, float)`（**6 参**）。
  两树签名不同，故 mixin 各写一份。
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `test_server.py 1201` PASS（Done 2.723s）、`test_server.py neoforge1211` PASS（Done 0.855s / 0.815s，
  首次跑出现一次 TIMEOUT，重跑两次均 PASS——与上一轮同样的启动偶发超时，机器同时跑着一个 2.8 GB 的
  Minecraft 客户端时 1.21.1 服务器争不到 CPU，如实记录）。

**⚠️ 关于服务端回归的证据边界（必须说清）**：本条的 mixin 目标是 `GeoReplacedEntityRenderer`，
它是**纯客户端**渲染类，且 mixin 注册在 `client` 列表里——**专用服务器根本不加载这个类、也不加载这段
client mixin 配置**。所以上面两条 `test_server` 结果对本次改动**不构成任何有效性证据**，它们只证明
"新 jar 没让服务器启动失败"。本条的真正依据是前面那三项：编译零错、字节码断言、以及**注入点离线核对**
（目标类里方法存在且描述符与 mixin 签名逐一匹配）。实际渲染效果**只能由你在客户端目视确认**。

**待部署**：部署时检测到你正在游玩（Minecraft NeoForge 1.21.1，窗口运行中），
`deploy_all.py` 按设计拒绝写入（防覆盖运行中的 jar），故本轮**尚未部署到游戏实例**。
退出游戏后执行：

```
python deploy_all.py
```

已部署待实测：近战空袭俯冲飞向敌人时，酒狐身体随俯角继续前翻（更接近玩家鞘翅），抬头爬升时
自然回正；平飞盘旋几乎无变化；落地即恢复站立；飞行机制（索敌/伤害/烟花推进/收翅猛击）完全不变。


## 实测五百零五【隔墙疯狂挥空——四条自造攻击链路都绕过了"视线"这道门】

### 根因：TLM 原生近战有视线门控，我们自己造的链路全绕过了它

TLM 的 `MaidMeleeAttack`（两树反编译实证）出手条件是三条：

```java
!isHoldingUsableProjectileWeapon(maid)
&& maid.isWithinMeleeAttackRange(target)
&& nearestVisibleLivingEntities.contains(target)   // ← 视线门控
```

最后那条 `contains` 走 `Sensor.isEntityAttackable` → `TargetingConditions.test`
（`ignoreLineOfSight` 字段默认 false）→ `Sensing.hasLineOfSight`。
**也就是说原版女仆隔墙本来打不到怪**，这道门一直都在。

问题出在本模组自己实现的攻击链路上——它们为了补 TLM 的结构性盲区（中立/魔改生物索敌、
近身反击、空袭命中率）而直接调 `m_7327_` / `doHurtTarget`，**全部没有把这道门带上**：

| 位置 | 旧行为 | 后果 |
|---|---|---|
| `NeutralThreatDriver` 第③步兜底挥砍 | 只判"距离 ≤ 3.1 + `getTarget()` 锁定我方" | **主犯**：墙后怪持续锁定主人/女仆 → 每 12 tick 对着墙挥一刀 |
| `MaidCombatTacticsBehavior.meleeCounterAttack` | 按目标周围 2 格 AABB 横扫，只查友伤 | 隔墙能把墙后怪**一起真打到**（比空挥更糟） |
| `SelfPreservationBehavior.counterAttack` | 上游 `findThreat` 查了视线，但"着火兜底"走的是 `findNearestThreatNearby`（只有类型+距离，**没有视线**） | 墙后怪也能让她隔墙挥刀/放箭/射弹幕 |
| `MaidFlightCombatBehavior` 的 `smashHit` 范围命中 + `groundMelee` | 按 AABB 取"其他合法敌对目标"直接结算 | 空袭/落地近战隔墙也能打到 |

### 改法：统一往项目现有的那条视线工具里收，不新造轮子

项目里本来就有 `SelfPreservationBehavior.hasSight(from, other)`（public static，两树同名，
raycast 到眼睛高度 +1.2 的 `ClipContext.Block.COLLIDER`），`findThreat` 与举盾判定都在用它——
缺的只是没有贯穿到"直接出手"的路径上。六处出手点现在统一补上这道判定（两树同改）。

**【隔墙不罚站】** 这是本次最关键的取舍：`NeutralThreatDriver` 只在"挥砍"这一步拦，
导航分支照常执行——她仍会朝怪走过去、绕到能看见的位置再打，**追击欲望不受影响**。
因此原第③步末尾那段内联导航被提成了 `navigateTo(maid, threat)`，供"看不见所以不出手"
的路径复用（否则她会在墙这边原地罚站）。空袭的 `groundMelee` 同理：看不见就
`moveTo(target)` 继续走近，而不是干等。

### 验证

- 两树编译零错（1.20.1 `compile_promaid.bat` EXITCODE=0；1.21.1 `run_javac_neo.py` exit=0，
  仅 16 条既有警告）。过程中修掉一个自己引入的语法错误：`NeutralThreatDriver` 首次编辑
  多留了一个右大括号（`drive` 的收尾括号被替换带了过来），表现为
  "`-source 21` 中不支持隐式声明的类"——报错指向的是结果不是原因，靠括号平衡核对定位；
- `javap -p -c` 反汇编构建产物，**按方法边界逐一统计** `hasSight` 出现次数（避免把常量池
  引用误判成方法体命中）：

| 类 | 方法 | hasSight 命中 | 期望 |
|---|---|---|---|
| `NeutralThreatDriver`（两树） | `drive` | 1 | 1（出口前那一处） |
| `MaidCombatTacticsBehavior`（两树） | `meleeCounterAttack` | 2 | 2（主目标 + 逐受害者各一次） |
| `SelfPreservationBehavior`（两树） | `counterAttack` | 1 | 1（收口处，覆盖三个分支） |
| `MaidFlightCombatBehavior`（两树） | `groundMelee` / `smashHit` | 各 1 | 各 1 |

- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK` + lang json OK；
- `deploy_all.py` 三处 `match=True`；
- `test_server.py 1201` PASS（Done 3.005s）、`test_server.py neoforge1211` PASS（Done 0.882s）。

已部署待实测：墙后站怪时女仆不再对墙挥刀，而是绕过去（走位照旧）；绕到有视线的那一刻
立刻开打；近身横扫/自保反击/空袭范围命中同样不再穿墙；开阔地带的正常战斗观感不变。


## 实测五百零四【鞘翅渲染附魔光泽 + 绑定 TLM「显示背部物品」】

### 一、附魔光泽

原版 `ElytraLayer` 本来就带光泽通道，只是它传的是"有没有真的附魔"：

```java
// 1.20.1
ItemRenderer.m_115184_(buffer, RenderType.m_110431_(WINGS), false, stack.m_41790_());
// 1.21.1
ItemRenderer.getArmorFoilBuffer(buffer, RenderType.armorCutoutNoCull(WINGS), stack.hasFoil());
```

`m_115184_` / `getArmorFoilBuffer` 的第 4 / 第 3 个参数就是 `hasFoil`——**字节码实证**：为 true 时
额外复合一层 glint 渲染类型（1.20.1 `armor_entity_glint`，1.21.1 `RenderType.armorEntityGlint`），
为 false 时直接返回原 buffer。

改前的实际状态两树还不一致：

| 树 | 改前 | 表现 |
|---|---|---|
| 1.20.1（两处图层） | 传 `chest.m_41790_()` | 只有**真附魔**过的鞘翅才亮；普通鞘翅全无光泽 |
| 1.21.1（两处图层） | 硬编码 `false` | **永远不亮** |

现在四个图层统一固定传 `true`（**常亮**）——与本模组手册 / 排班表 / 协议源石的"常亮光泽"口径一致。
想改回"只有附魔鞘翅才亮"：把 `true` 换回 `chest.m_41790_()`（1.20.1）/ `chest.hasFoil()`（1.21.1）即可，
代码里已就地留了注释说明。

### 二、绑定 TLM「显示背部物品」

TLM 女仆配置页那一项就是 `gui.touhou_little_maid.maid_config.show_back_item` = **显示背部物品**
（两树 lang 键同名），落点是：

```
EntityMaid.getConfigManager().isShowBackItem()
```

**字节码实证**：它读同步实体数据 `EntityMaid.BACK_ITEM_SHOW`，在 `defineSynchedData` 里
默认值 = `true`（`iconst_1`），所以绑定后**默认行为与旧版完全一致**，只有玩家主动关掉那一项才会消失。
两树 API 同名同签名（`javap` 实证 `MaidConfigManager.isShowBackItem():boolean` 均为 public）。

鞘翅画在女仆背上，本就属于"背部物品"——TLM 自己的 `LayerMaidBackItem` /
`GeckoLayerMaidBackItem` 正是在 `isShowBackItem()` 这个条件下绘制的，我们跟着走才是同一个口径。
四个图层（两树 × Bedrock/Gecko）都在"任务为飞行作战 + 胸甲槽是可用鞘翅"之后追加了这道判定。

### 三、验证

- 两树编译零错（1.20.1 `compile_promaid.bat` EXITCODE=0；1.21.1 `run_javac_neo.py` exit=0，仅 16 条既有警告）；
- `javap -p -c` 反汇编构建产物，四个 class **逐一**确认：
  - 含 `getConfigManager` + `isShowBackItem` 调用；
  - 含 `m_115184_` / `getArmorFoilBuffer` 调用，且其**光泽参数位为 `iconst_1`**；
  - `m_41790_` / `hasFoil` 残留为 **False**（证明确实把旧参数换掉了，不是只加不改）；
- `build_promaid.py` / `build_promaid_neo.py` 均 `verify_jar_classes: OK`，lang json OK；
- `deploy_all.py` 三处 `match=True`；
- `test_server.py 1201` PASS（Done 2.815s）、`test_server.py neoforge1211` PASS（Done 1.030s）。

已部署待实测：穿鞘翅 / 空袭激活后，翅膀带附魔流光；女仆配置页关掉「显示背部物品」→ 翅膀消失，
重新打开 → 恢复；其余外观（尺寸 / 上移 0.7 格 / 展翅）不受影响。


## 实测五百零三【远程空袭新增"近身弹开"——3 格内有怪就给她一个远离怪物的速度矢量】

### 为什么要加：盘旋逻辑本身没有"别贴脸"这个概念

远程空袭的盘旋（`faceOrbit`）只做两件事——**绕一个半径 10 格的圈**（切向 + 径向修正）和
**按高度偏差调俯仰**。它的切向分量里天然带着"向敌"的横向速度，一旦怪物主动贴过来，或者她自己
被地形/烟花推力挤到怪物身边，盘旋会**让她贴着怪物转**。而她的血量只有 20，被贴脸打两下就没了。
起飞瞬间那次"背离敌人 + 向上"的烟花推力只在起飞有效，进入盘旋后就没有这层保护了。

### 关键实现细节：为什么必须"同时改速度 + 改朝向"

这是本次最容易做错的地方。滑翔的物理在 `LivingEntity.travel` 的 `isFallFlying()` 分支里
（反编译实证，1.20.1 在 :1946 起的块内）：

```java
if (this.m_21255_()) {                       // isFallFlying
    ...
    if ($$16 > 0.0) {
        $$13 = $$13.m_82520_(
            ($$14.f_82479_ / $$16 * $$17 - $$13.f_82479_) * 0.1, 0.0,
            ($$14.f_82481_ / $$16 * $$17 - $$13.f_82481_) * 0.1);   // ← 关键
    }
    this.m_20256_($$13.m_82542_(0.99f, 0.98f, 0.99f));
```

它每 tick 都做一次 `速度 += (视线水平单位向量 × 当前速率 - 速度水平分量) × 0.1` ——
也就是**持续把水平速度往"视线方向"拽**。后果是：

| 只做一件 | 结果 |
|---|---|
| 只压速度、不改朝向 | 视线仍指着敌人 → 那份 10%/tick 的转向力一两 tick 就把速度拉回"朝敌人"，**等于没弹** |
| 只改朝向、不压速度 | 起步太慢（10%/tick），贴脸时来不及 |

所以两者必须一起给：**速度反向（立刻离开）+ 朝向背离（让转向力与推力同向）**。
也正因为存在这份转向力，朝向与推力**必须引用同一个威胁**，否则会自相拉扯（朝 A 飞、被 B 推）——
实现上让 `pushAwayFromThreat` 把"该背离的威胁"作为返回值交给调用方去摆朝向。

### 具体参数与取舍

| 项 | 取值 | 理由 |
|---|---|---|
| 触发半径 | **3 格**（需求原话） | `distanceToSqr <= 3²` |
| 水平速度 | **0.55 格/tick**（覆盖式，非叠加） | 鞘翅水平巡航大致 0.4~0.8，0.55 足以在 1~2 tick 内把"向敌"压反向，但仍是连续弧线而非瞬移；叠加式会让反复触发时速度越滚越大、几 tick 后变成"被弹飞" |
| 向上分量 | **+0.25 格/tick** | 顺手抬离怪物的近战竖直包线；竖直保留原值再抬，不把她往地面压 |
| 持续 | **30 tick（1.5 秒）** | 盘旋的向敌分量每 tick 都在拉她，只弹一 tick 立刻被拉回来；1.5 秒与放烟花冷却同量级，够飘出一次攻击间隔 |
| 只在滑翔中生效 | `isFallFlying()` | 落地/骑乘时不叠加速度，免得出现贴地滑行 |

### 只弹女仆、不弹怪物（需求明确否定后者）

"弹开敌人"等于让远程角色获得一个**持续的、无需操作的群体击退**，太超模。弹开自己则是
"用机动换安全"——她离开怪物的同时也就**脱离了输出位**，而且在**狭小空间（墙角/洞穴）里跑不掉**，
所以敌人仍然有机会命中，这正是需求里要的平衡。

### 威胁判据（与既有口径对齐，不自己发明）

只把**真正会威胁她的**算作威胁，避免"中立动物路过"也被弹：
`Enemy` 接口 → 已记仇的中立生物（`NeutralMob.m_21660_()` / neo `isAngry()`，走
`AutoCombatSwitch.neutralAngry` 同款防御封装：模组 getter 抛异常不能炸 tick）→
**正在锁定她或她主人的任意 Mob**（魔改生物不实现前两个接口，靠这条兜住）。
另外用 `FriendlyFireGuard.isFriendly` 先行排除**主人 / 同主女仆 / 友军**——绝不弹开自己人。

### 开关与文档

- 新增配置 `combat.flightRangedPush`（默认开），配置面板加一行"远程空袭近身弹开"；
- 四个 `lang/*.json` 补 `config.promaid.combat.flightRangedPush`（中/英，两树）；
- 弹开是**高频事件**，日志按 5 秒/女仆限频（`PromaidLog` 只落盘状态迁移，不能被它刷屏）。

### 验证

1. 两树编译零错误（过程中修掉两处真实问题：`NeutralMob` 的记仇 getter 我一开始误用
   `m_21672_()`，实为 `m_21660_()`；滑翔判定一开始误用 `m_20161_()`，反编译证实它是
   `m_6144_()`、与滑翔无关，正确名是 `m_21255_()`——本项目 `MaidFlightKit` /
   `EntityFlagInvoker` / `MaidSwimGlideMixin` 全用这个名字）。
2. **字节码校验**（`javap -p -c` 反汇编构建产物，只认真正的声明行）：
   - `tickRangedAir` 同时含 `pushAwayFromThreat` / `faceAwayAndUp` / `faceOrbit` / `faceUpForward`
     （即：弹开分支在、原盘旋分支也还在，是"二选一"而非替换）；
   - `pushAwayFromThreat` 含 `COMBAT_FLIGHT_RANGED_PUSH`（读开关）、
     `m_21255_`（1.20.1）/ `isFallFlying`（1.21.1）、`findNearestThreat`、`applyAwayVelocity`、`logPushThrottled`；
   - `applyAwayVelocity` 含 `m_20256_` + `m_20184_`（1.20.1）/ `setDeltaMovement` + `getDeltaMovement`（1.21.1）；
   - `isThreat` 含 `Enemy` / `NeutralMob` / 记仇方法 / 目标锁定方法。两树全部命中。
3. `deploy_all.py` 三处 `match=True`；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

已部署待实测（1.20.1 + 1.21.1）：远程空袭盘旋时，怪物进到 3 格内女仆会立刻往反方向飘 1.5 秒
（日志 `[远程空袭] xxx 近身弹开（2.4 格：zombie）`，5 秒限频）；脱离后自动恢复盘旋。
把配置项关掉即完全回到旧行为。


## 实测五百零二【烟花检测的副手覆盖核查——副手本来就查了；顺带补上"攻击性烟花"漏查的主手】

### 逐条核查结果：副手**确实查了**，四处烟花函数都没有漏

我把"烟花在不在身上"的全部实现逐行拉出来核对，并用字节码确认了关键前提：

| 前提 | 实证 |
|---|---|
| `m_21206_()` 就是副手 | 反编译 `LivingEntity.m_21206_()` = `return this.m_6844_(EquipmentSlot.OFFHAND)` |
| `getHandsInvWrapper()` 的 slot 1 就是副手 | `EquipmentSlot.values()` 的 `<clinit>` 常量创建顺序 = `MAINHAND, OFFHAND, FEET, LEGS, CHEST, HEAD`；`EntityHandsInvWrapper` 只保留 `Type.HAND` 的槽并按该顺序装进 `slots` 列表 → slot 0 = 主手、slot 1 = 副手 |

四处函数的手部覆盖面（改动前）：

| 函数 | 主手 | 副手 | 背包 |
|---|---|---|---|
| `hasFirework`（三件套判定用） | ✓ `m_21205_()` | ✓ `m_21206_()` | ✓ |
| `takeFirework`（放烟花消耗用） | ✓ slot 0 | ✓ slot 1 | ✓ |
| `hasExplosiveFirework`（弩弹药判定） | **✗ 缺** | ✓ | ✓ |
| `takeExplosiveFirework`（弩弹药取用） | **✗ 缺** | ✓ | ✓ |

也就是说：**"副手没被检测"这一点在代码里不成立**——反倒是上面那两个"攻击性烟花"
（四百九十九为弩射烟花新加的一对）**漏了主手**，方向与预期相反。
另外确认过消耗路径的两次调用（起跳/远战补烟花各一处）走的都是 `takeFirework`，
副手在 slot 1 是**第一优先**，所以"副手放烟花却不生效"也不是这条链路的问题。

### 顺手修掉的真实不一致：攻击性烟花漏查主手

`hasExplosiveFirework` / `takeExplosiveFirework` 只查"副手 + 背包"，而
`hasFirework` / `takeFirework` 是"主手 + 副手 + 背包"三处都认。同一条"烟花在不在身上"
的口径出现两套覆盖面，是隐患——具体到这个场景会真实出错：

> 主手拿着一枚**带爆炸的**烟花、弩放在背包里时，`resolveRangedWeapon` 会返回**背包那把弩**
> （它的规则是"主手已是本任务合法武器才用主手"，而烟花不是武器）。
> 那一刻弹药就在**主手**上，但旧代码只扫副手 → 判成"缺弹药" → 远程空袭不激活、
> 气泡报"缺弹药"。

修法：把这对函数补齐为与 `hasFirework` / `takeFirework` **完全一致的三处覆盖面**，
顺序也照抄（取用时副手 → 主手 → 背包；"副手优先"这条原版口径保留不变）。

### 验证

1. 两树编译零错误。
2. **字节码覆盖面校正**（`javap -p -c` 反汇编构建产物）：现在四个函数在主手/副手/背包三处
   全部为 True。`takeExplosiveFirework` 的字节码里能直接看到 `iconst_1` → `getStackInSlot(1)`（副手）
   紧跟 `iconst_0` → `getStackInSlot(0)`（主手），最后才是 `takeOneFromBackpack`。
3. **产物一致性**：`out/` 里的 `MaidFlightKit.class` 与 jar 内该类的 SHA-1 逐一比对相同
   （防止"改了源码但打进旧字节码"）。
4. `deploy_all.py` 三处 `match=True`；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

## 实测五百零一【御币被误判"缺弹药"——它是 ProjectileWeaponItem 的子类，被"数箭"那一路吃掉了】

### 根因：判据顺序错了——"不需要弹药"的那条兜底永远走不到

`MaidFlightKit.hasAmmoForRanged` 的注释本来就把御币归在"**不消耗弹药**"那一类里
（弹幕用武器本体），最后也写了一条 `// 御币 / 三叉戟 / 其它：不消耗弹药 → 放行`。
问题在于**它排在 `ProjectileWeaponItem` 分支之后**，而御币恰恰是这个类的子类：

```
ItemHakureiGohei extends ProjectileWeaponItem      ← 两版 TLM 字节码实证
```

所以御币根本走不到那条兜底，而是落到"数箭"分支（`hasArrowFor`）：
副手不是箭 → 背包按 `supportedProjectiles` 找箭 → 再兜底找任意箭 → **都没有就返回 false**
→ 判定"缺弹药" → 远程空袭不激活（`missingParts` 于是报出"弹药"那一项）。

三条旁证，把这条路径钉死：

| 证据 | 内容 |
|---|---|
| 御币自己的弹药谓词 | `m_6437_()` / `getAllSupportedProjectiles()` 是 `Predicates.alwaysTrue()`（反编译逐字）——它压根没有"专用弹药"这个概念 |
| 御币自己的发射通道 | 1.20.1 **没有**覆写 `shootProjectile`/`m_6394_`（空实现）；说明它不走原版箭矢通道，硬塞箭进去也没有意义 |
| TLM 自己的弹幕任务 | `TaskDanmakuAttack` 全部成员里**没有任何** `hasArrow` 之类的弹药判据（javap 实证），只有 `hasGohei`（查的是"有没有御币"） |

也就是说：**原版/TLM 都没有"御币要箭"这个前提**，是我们自己那条门禁的判定顺序把它卷进去了。

### 改法：把"无需弹药"的几类提到 `ProjectileWeaponItem` 之前

判据顺序改为（两树同步）：

```
枪械 → 御币 → 三叉戟 → 弩 → 其它弹射武器 → 放行
```

- **御币**：新增判定，用 TLM 自带的 `ItemHakureiGohei.isGohei(stack)`——与开火路径
  `MaidFlightRangedTask#isGohei` **同一口径**（门禁与开火必须一致，否则会出现
  "判定有弹药却没按弹幕打"或"能打弹幕却不起飞"）；
- **三叉戟**：它本来就不是 `ProjectileWeaponItem`，走不到那条分支，但**显式写出来**，
  让门禁与开火路径三条"无需弹药"通道逐条对齐、以后改动不漏；
- `MaidFlightKit` 新增 public 助手 `isGohei(ItemStack)`，供门禁与后续复用。

**只动判定顺序，不动伤害/弹幕/激活条件**：御币该走 `shootDanmaku` 照样走（那条分流本来
就在 `shootArrow` 之前，顺序是对的），这次修的是**门禁**那一侧。

### 验证

1. 两树编译零错误。
2. **字节码顺序校正**（`javap -p -c` 反汇编构建产物，直接看常量池里的判定调用序列）：

```
21: GunCompat.isGun          33: isGohei           45: instanceof TridentItem
57: instanceof CrossbowItem  82: instanceof ProjectileWeaponItem(→hasArrowFor)
```

   两树都实测到 `isGohei` 与 `TridentItem` **都排在** `ProjectileWeaponItem` **之前**
   （三项顺序断言全部 True），`isGohei` 方法已就位。
3. `deploy_all.py` 三处 `match=True`。
4. `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS。

已部署待实测：远程空袭持御币时不再报"缺弹药"，能正常起飞并按弹幕开火（与她自己用弹幕任务
时的表现一致）；弓/弩仍然照旧要求箭（弩还认攻击性烟花）；不带御币的配置不受影响。


## 实测五百【空袭不适配拔刀剑——TLM 的拔刀斩门控在她的"地面攻击任务"上，空袭任务永远进不去】

### 根因之一：TLM 的拔刀剑支持被硬编码门控在它自己的攻击任务上

TLM 对拔刀剑的全部支持只有一处：`EntityMaid.swing()`（1.20.1 `m_6674_`）开头调用
`SlashBladeCompat.swingSlashBlade(maid, 主手)`。而那个方法体（反编译实证）第一步就是：

```java
if (isSlashBladeItem(stack)
        && maid.getTask().getUid().equals(TaskAttack.UID)) {   // ← 只认 touhou_little_maid:attack
    ... AttackManager.doSlash(...)
}
```

`TaskAttack.UID` 的静态初始化字节码是 `new ResourceLocation("touhou_little_maid", "attack")`。
我们的飞行任务 UID 是 `maid_smart:flight_combat` / `maid_smart:flight_ranged`，**不是**它 →
这个 `equals` 恒 false → **女仆在空袭里挥刀，拔刀斩一次都不会触发**。
不是"触发得少"，是根本不进那段代码。同一个女仆换成地面攻击任务却能触发，
这正是反馈看到的"空袭不适配拔刀剑"。

### 根因之二：我们自己的命中结算只吃原版 `ATTACK_DAMAGE`，没有拔刀剑的伤害倍率

`MaidFlightCombatBehavior#hitOne` 的伤害是 `ATTACK_DAMAGE × 暴击 1.5 + 附魔`；
而拔刀剑自己的口径（`AttackHelper.calculateTotalDamage` 反编译）是：

```
伤害 = (ATTACK_DAMAGE + 横扫加成 + 段位加成 + 附魔加成)
       × comboRatio × getSlashBladeDamageScale(攻击者) × SLASHBLADE_DAMAGE_MULTIPLIER
```

`getSlashBladeDamageScale` 读的就是拔刀剑注册的 `SLASHBLADE_DAMAGE` 属性（默认 1.0，
随练度/段位成长）。我们那条通道整段没有这个乘数，同一把刀在空袭里打出的数字明显低于
她自己地面攻击时打出的。

### 改法：只补一件事——把 TLM 给"地面攻击"补的那一下，补到空袭的挥刀点上

TLM 那处门控改不了（那是它的类，且 `slashblade` 不在我们的编译 classpath 里），
于是在**我们自己**的挥刀点复刻 TLM 同一段调用（逐点一致，不自己发挥）：

```java
int roll = random.nextInt(60) - 30;
AttackManager.doSlash(maid, roll, Vec3.ZERO, /*mute*/ false, /*critical*/ false,
                      /*comboRatio*/ 1.0, KnockBacks.smash);
// 再记一次 BLADESTATE.setLastActionTime(level.getGameTime())（TLM 同款）
```

`doSlash` 生成的 `EntitySlashEffect` **自己按拔刀剑的完整公式结算伤害**
（`EntitySlashEffect.tick` → `AttackManager.areaAttack` → `AttackHelper.attack`）——
所以**根因二那条缺口是被同一个调用一并补上的**。这里刻意**不**往 `hitOne` 里再乘一次
倍率：那会变成"我们的原版伤害 + 拔刀斩伤害"叠乘，比玩家和 TLM 的女仆都强。
加完之后，空袭持刀的表现 = 她自己地面攻击持刀的表现（TLM 地面也是"原版命中一记 +
拔刀斩一记"两件都做，`MaidMeleeAttack` 字节码实证 `m_6674_` 与 `m_7327_` 相邻两条）。

新增 `com.maidsmart.combat.SlashBladeCompat`（两树同步），挂在两个挥刀点：

| 挂载点 | 说明 |
|---|---|
| `MaidFlightCombatBehavior#smashHit` | 收翅俯冲那一记（"空袭"的主体） |
| `MaidFlightCombatBehavior#groundMelee` | 落地后的贴身近战退路（只补俯冲会留下"落地就变哑巴"的半截体验） |

**全反射**：`compile_promaid.txt` / `compile_neo.txt` 的 classpath 里都没有 slashblade
（已核对），模组也不保证装 —— 直接 import 会编译不过、装了就崩。所有符号按需
`Class.forName` 解析并在 `ModList.isLoaded("slashblade")` 之后一次性缓存；缺模组时
一次布尔判断即返回，零开销。

`doSlash` 在 `AttackManager` 里有 **7 个重载**，按参数名盲选会静默失败
（`IllegalArgumentException`），所以按**精确签名**定位：
`(LivingEntity, float, Vec3, boolean, boolean, double, KnockBacks)`。

### 1.21.1 侧的额外实情（必须如实告知）

- **1.21.1 的 TLM 1.5.3 完全没有拔刀剑兼容代码**：jar 内常量池扫 `slashblade` 零命中，
  1.20.1 那份 `SlashBladeCompat` / `SlashBladeRender` 在 neo 版里不存在（已核对）。
  也就是说 1.21.1 上"女仆持拔刀剑"本来就没有任何官方联动，**连地面攻击任务都不会触发拔刀斩**。
  本兼容层挂在我们自己的挥刀点上（不依赖 TLM 的门控），所以在 1.21.1 上它**同时补上了
  地面与空袭**——不是回归，是净新增。
- **1.21.1 的拔刀剑 2.0.7 换了状态存储机制**：`CapabilitySlashBlade` 这个类在 2.0.7 里
  **不存在**（capability 包已改成 `BladeStateAccess` / `SlashBladeDataComponents`），
  所以状态入口走 `BladeStateAccess.of(ItemStack)` 这个静态方法。
- 1.21.1 侧**客户端实例**里拔刀剑本体是 `.disabled` 状态（`SlashBladeResharped-2.0.7-1.21.1.jar.disabled`、
  `mrqxs_Slashblade_Core-1.21.1-1.4.4.jar.disabled`），所以**玩法层面**本次无法在客户端实测；
  但 **NeoForge 测试服是启用状态**（`mc_server_test/neoforge1211/mods/` 下两个拔刀剑 jar 均无 `.disabled`），
  本模组的 `SlashBladeCompat` 在该服上随模组加载、启动与回归全程无异常——即"装了拔刀剑的 1.21.1 服务端"
  这条路径是实测过的；反射符号另外按 2.0.7 的真实 jar 逐个校验（见验证第 2 条）。

### 主人免伤

`doSlash` 的伤害来源是 `damageSources().mobAttack(女仆)`（`AttackHelper.attack` 的非玩家分支，
反编译实证 `attacker.m_269291_().m_269333_(attacker)`，`m_269333_` = `mobAttack`）→
加害者就是女仆 → 已有的 `FriendlyFireGuard` 会拦掉主人/友方，**无需新增免伤代码**。
另有两层天然保险：拔刀剑自身默认 `pvp_enable=false`、`friendly_enable=false`，
它的目标筛选器（`TargetSelector$AttackablePredicate`）本来就只打 `Enemy`。

### 验证

1. 两树编译零错误（新增类需重跑 `gen_compile.py` / `gen_compile_neo.py` 才会进编译清单，
   首次编译报"找不到符号"即此原因，已处理）。
2. **反射符号逐个实证**（对真实 jar 起 JVM 校验，`initialize=false` 不触发 mod 静态初始化）：

| 符号 | 1.20.1（1.9.65） | 1.21.1（2.0.7） |
|---|---|---|
| `ItemSlashBlade` 可加载 / 父类 = `SwordItem` | OK | OK |
| `KnockBacks.smash`（static final） | OK | OK |
| `AttackManager.doSlash` 重载数 | **7**，精确命中 7 参那条 | **7**，精确命中 7 参那条 |
| 拔刀剑状态入口 | `CapabilitySlashBlade.BLADESTATE` OK | `BladeStateAccess.of(ItemStack)` OK（2.0.7 已无 `CapabilitySlashBlade`） |
| `ISlashBladeState.setLastActionTime(long)` | OK | OK |
| `Vec3` 零向量常量 | OK（`f_82478_`） | OK（`ZERO`） |
| ModList 判定 | `net.minecraftforge.fml.ModList` OK | `net.neoforged.fml.ModList` OK |

   1.20.1 合计 11/11、1.21.1 合计 5/5 全通过。
3. **字节码校验**（`javap -p -c` 反汇编构建产物）：`SlashBladeCompat` 三个公开方法就位；
   `MaidFlightCombatBehavior` 的 `smashHit` 与 `groundMelee` **各含一次** `invokestatic swingSlash`
   （两树都是 2 处），位置都在挥刀之后、原版命中结算之前。
4. 1.20.1 测试服**装着拔刀剑**（启动日志实证 `slashblade 1.9.65` + `mrqxs_Slashblade_Core-1.4.1`），
   回归 PASS 且启动无异常。
5. `deploy_all.py` 三处 `match=True`；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

已部署待实测（1.20.1）：空袭持拔刀剑时，俯冲那一记与落地近战都会带出拔刀斩（有伤害、有刀光）；
持其它武器完全不受影响；没装拔刀剑的实例零开销、行为与之前一致。


## 实测四百九十九【弩无法发射攻击性烟花火箭——飞行远战任务的武器分流里压根没有"弩"这一路】

### 根因：`MaidFlightRangedTask.performRangedAttack` 把弩当成了普通弓

分流原样是这样的（1.20.1 / 1.21.1 同款）：

```java
if (GunCompat.isGun(weapon)) return;              // 枪械
if (isTrident(weapon)) { ... return; }            // 三叉戟
if (!(weapon.getItem() instanceof ProjectileWeaponItem pwi)) return;
if (isGohei(weapon)) { ... return; }              // 御币
shootArrow(shooter, target, weapon, pwi);         // ← 弩必然落到这里
```

弩是 `ProjectileWeaponItem`，于是**必然落到 `shootArrow`**，而 `shootArrow` 只认 `ArrowItem`：

| 版本 | 手上有弩 + 攻击性烟花、背包没箭时的实际表现 |
|---|---|
| 1.21.1 | `findAmmo` 第一站是 `maid.getProjectile(主手)`，TLM 的 `EntityMaid.getProjectile` 在副手是烟花时**就把副手那叠烟花交出来**（TLM 给女仆做弩的官方口径，字节码实证）；拿回来却被 `instanceof ArrowItem` 判掉 → 退化成一支普通箭，最后 `arrowStack.shrink(1)` 还把这枚烟花**消耗掉了**。表现 = 烟花少一枚、飞出去一支箭 |
| 1.20.1 | `findArrow` 第一站是副手，同样只认 `ArrowItem`；副手烟花判不过 → 去背包找箭 → 没有就返回空 → `shootArrow` 直接 `return`，**一枪都放不出来** |

原版和 TLM 本身都支持这件事——原版 `CrossbowItem.createProjectile` 有专门的烟花分支；
TLM `TaskCrossBowAttack.hasAmmunition` 把副手烟花算作弹药，1.21.1 还有 `MixinCrossbowItem`
专门给女仆做弩的瞄准（`shootCrossbowProjectile`）。**唯独我们自己那条飞行远战通道漏了。**

### 一个必须处理的撞车：副手烟花既是"弹药"也是"飞行燃料"

本模组的飞行任务把烟花放**副手**喂鞘翅（`MaidFlightKit.equip` 副手槽，
`MaidFlightCombatBehavior#launchFirework` 优先消耗副手）；而原版口径把**副手烟花当弩弹药**——
两者抢的是同一个槽位。

关键事实：**不带爆炸（`Explosions`）的烟花打出去是 0 伤害**——
`FireworkRocketEntity.dealExplosionDamage` 开头就是 `if (!this.hasExplosion()) return;`
（等价于 `!getExplosions().isEmpty()`，字节码实证）。拿它开火 = 白烧一份飞行燃料 + 空中掉高风险。

所以本模组的口径是：**弩的弹药只认"攻击性"烟花**（带爆炸组件）；没有就回落到射箭，而不是烧燃料。

### 改法

**(a) `MaidFlightKit` 新增三个"攻击性烟花"接口**（两树同步）：

| 方法 | 作用 |
|---|---|
| `isExplosiveFirework(stack)` | 是烟花 **且** 爆炸列表非空（1.20.1 读 NBT `Fireworks.Explosions`，与 `FireworkRocketEntity` 自己的读法一致；1.21.1 读组件 `DataComponents.FIREWORKS.explosions()`） |
| `hasExplosiveFirework(maid)` | 只查不取：副手 → 背包（给"弹药门禁"用） |
| `takeExplosiveFirework(maid)` | 取 1 枚当弹药：副手优先（原版口径），背包路径**只抽 1 枚**，绝不整叠拿走 |

弹药门禁 `hasAmmoForRanged` 的弩分支同步改为"有攻击性烟花 **或** 有箭"——
门禁与开火路径必须严格同口径，否则会出现"判定有弹药、起飞后一发打不出来"的死角
（这正是该文件注释里反复强调的坑）。

**(b) `MaidFlightRangedTask.performRangedAttack` 新增弩分支**，排在 `shootArrow` 之前：

```java
if (weapon.getItem() instanceof CrossbowItem && shootCrossbowFirework(shooter, target, weapon)) {
    return;
}
shootArrow(shooter, target, weapon, pwi);
```

`shootCrossbowFirework` 逐点对齐原版 + TLM，不自己发挥：

| 项 | 取值 | 依据 |
|---|---|---|
| 弹体 | `new FireworkRocketEntity(level, 弹药, 女仆, x, 眼高-0.15, z, true)` | 原版 `CrossbowItem.createProjectile` 的烟花分支逐字一致；末位 `true` = `shotAtAngle` → 命中即爆、**不给射手推力**（不是给滑翔加速的那种挂载烟花） |
| 瞄准 | 取双方眼高差 + `setNoGravity(true)` 直线飞 | TLM 女仆弩口径（1.21.1 `MixinCrossbowItem#shootCrossbowProjectile` 字节码实证；1.20.1 走原版 `shootProjectile`） |
| 速度 / 散布 | `clamp(距离/10, 1.6, 3.2)` / `1 - clamp(距离/100, 0, 0.9)` | 同上 |
| 耐久 | 扣 **3**（射箭是 1） | 原版 `getDurabilityUse` 的烟花分支 |
| 音效 | `CROSSBOW_SHOOT`，音高带 ±0.4 随机 | 同上 |
| 顺序 | 先取弹药 → 立即建弹体入世界 → 再扣耐久 | 取弹与入世界之间只有算术，没有任何可能失败的分支（不查方块/目标），所以不存在"烟花扣了却打不出去"的窗口 |

### 主人免伤

烟花是**爆炸**伤害，`DamageSources.fireworks(烟花, 女仆)` 的**造成者就是女仆**，
会被已有的 `FriendlyFireGuard`（1.20.1 走 `LivingAttackEvent` + `LivingHurtEvent`，
1.21.1 走 `LivingIncomingDamageEvent`）拦下——**女仆绝不会用烟花炸到主人**，
本功能无需新增任何免伤代码。

### 验证

1. 两树编译零错误。
2. **字节码校验**（`javap -p -c` 反汇编构建产物）：
   - `MaidFlightRangedTask` 出现 `shootCrossbowFirework`，且 `performRangedAttack` 里有
     `instanceof CrossbowItem` + `invokestatic shootCrossbowFirework`，位置在 `shootArrow` 调用之前；
   - `MaidFlightKit` 三个新接口就位，且 `isExplosiveFirework` 被 `hasExplosiveFirework`、
     `takeExplosiveFirework`、`hasAmmoForRanged` 三处引用（3/3）；
   - 烟花判定路径分别引用到位：1.20.1 = `Explosions`，1.21.1 = `FIREWORKS`。
3. `deploy_all.py` 三处 `match=True`。
4. `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS。

已部署待实测：飞行远战任务下，弩 + 副手/背包里的**攻击性**烟花 → 女仆打真烟花（命中即爆、有伤害）；
弩 + 只有箭 → 照旧射箭；弩 + 只有普通烟花（无爆炸）且没箭 → 判为缺弹药不开火，**不会烧掉飞行燃料**。


## 实测四百九十八【悬空/滑翔仍会搭方块——四百四十三的两处漏口】

### 根因：四百四十三的判定口径本身漏了两处

`MaidPlaceGuard.blocked` 旧口径是"未落地 **且**（重锤跃起 **或** 坠落距离 ≥ 落地水触发高度）"，
问题就出在这两处：

**(a) 滑翔被显式放行。** 旧代码把 `isFallFlying()` 和"站在地面/水里/骑乘"摆在同一个放行名单里：

```java
if (maid.onGround() || maid.isInWater() || maid.isInLava()
        || maid.isPassenger() || maid.isFallFlying()) {
    return false;          // ← 滑翔在这里被整条放行
}
```

空袭期间她本来就在滑翔（我们自己置的第 7 位），所以"滑翔中搭方块"从来没被拦过。

**(b) 普通悬空只拦"深坠落"。** 剩下那条阈值是 `fallDistance >= combat.waterFallDistance`（默认 6 格）——
3 格、5 格的悬空下落照样放行。**而且这条阈值对滑翔根本不可能触发**：滑翔中
`LivingEntity.travel` 每 tick 调 `Entity.m_245125_()` 把 `fallDistance` 钳在最多 1.0
（这条在本模组 `MaidFlightCombatBehavior` 的注释里早有实证记录）。所以哪怕只把滑翔从
放行名单里删掉、保留阈值不动，滑翔期依然永远不会被拦——必须**单独判滑翔位**。

### 为什么空袭期真的会搭

搭路行为 `BridgeUpBehavior` 注册在 **core 245**（任何 activity 都跑一遍）。自保
（`SelfPreservationBehavior`，core 250）在飞行任务下被硬闸整个关掉了，**搭路没有这道闸**——
空袭期间只要脑内暂时没有攻击目标，搭路就会启动，朝主人方向或前方缺口铺块。这正是玩家
看到的"悬空搭方块"（而不是渲染/日志上的错觉）。

### 改法

`MaidPlaceGuard.blocked` 重写为：未落地、且不在水/岩浆/骑乘时，满足任一条即禁搭：

| 条件 | 说明 |
|---|---|
| `MaidFlightKit.isFlightAirborne(maid)` | **新增主漏口**：滑翔 / 收翅猛击 / 被击落盘旋等飞行期一律不搭 |
| `MaidMaceSmashBehavior.isAirborne(maid)`（1.21.1） | 重锤跃起整段空中禁（上升段也算），保留四百四十三的原意 |
| `fallDistance > 0`（1.20.1 / 1.21.1） | **阈值收紧**：由"坠够 6 格"改为"只要在降就禁" |

`fallDistance == 0` 的**上升段不受影响**，所以"站在地面或浅跳时往前方脚下垫一块"这类
正常垫脚/搭路链路照常工作——这也是四百四十三当初选阈值时的原始前提
（"本模组的垫脚动作都是站在地面或浅跳时……fallDistance ≈ 0"）。

### 覆盖面（逐条核对）

四个搭方块模块的 `takeBuildBlock` 全部先问闸口，被禁即返回 `null` →
各调用点已有的 `if (item == null) return false` 自然放弃本次放置（**不消耗方块、不改世界**）；
"背包里没有搭方块的材料"气泡按同一判定静默，避免悬空误报。

| 模块 | takeBuildBlock 实现体内含闸口 |
|---|---|
| `SelfPreservationBehavior`（自保搭高/搭路） | YES |
| `BridgeUpBehavior`（搭路） | YES |
| `MaidMineBehavior`（挖矿垫脚） | YES |
| `MaidWoodBehavior`（伐木垫脚） | YES |

**不拦**：落地水/细雪（`WaterClutchBehavior` 直接放水，是本模组防摔死的保命机制，正是它该救场）、
照明光块（`MaidHeldLight`）、农耕播种、插火把（要求目标格脚下有实心支撑面且贴着主人）、
AI 工具 `smart_place`（玩家的显式指令）——这些要么是保命机制，要么与"她自己在空中乱搭"无关。

### 验证

1. 两树编译零错误。
2. **字节码校验**（`javap -c` 反汇编构建产物）：两树 `MaidPlaceGuard` 里依次是
   `isFlightAirborne` → （1.21.1 加 `MaidMaceSmashBehavior.isAirborne`）→ `fallDistance > 0`；
   且 `isFallFlying` / `m_20161_` **已彻底消失**（旧的滑翔放行口确认关闭）。
3. 覆盖核对：两树各 4/4 个 `takeBuildBlock` 实现体内都含 `MaidPlaceGuard.blocked`。
4. `deploy_all.py` 三处 `match=True`。
5. `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS。

已部署待实测：空袭（近战/远战）飞行期间不再搭方块；正常挖矿/伐木垫脚、搭路、浅跳垫高不受影响。

## 实测四百九十七【1.21.1 女仆"很多模型被隐藏"——同 domain 的模型包被 TLM 排序逻辑整包丢弃】

### 根因（TLM 字节码 + 磁盘双实证）

**模型包的 id 就是 `assets/` 下的域名目录名**：`CustomPackLoader.loadMaidModelPack(rootPath, domain)` 里
`pack.decorate(domain)`，所以"两个模型包都把模型放在 `assets/geckolib/`"= 两个包**同 id**。

而 `MaidModels.sortPackList()`（两树 1.5.3 逐字节相同）是这么写的：

```java
for (String id : DefaultPackConstant.MAID_SORT) {          // [touhou_little_maid, _old, _seihou,
    packList.stream()                                      //  geckolib, authors_and_credits, minecraft_15th]
        .filter(p -> p.getId().equals(id))
        .findFirst().ifPresent(defaultPackList::add);      // ← 每个 id 只收【一个】包
}
packList.stream()
    .filter(p -> !MAID_SORT.contains(p.getId()))           // ← id 在名单里的包【全部】被排除
    .forEach(sortPackList::add);
packList.clear();
packList.addAll(defaultPackList);
packList.addAll(sortPackList);                             // ← 没进任何一个 list 的包，就此消失
```

第一个 `findFirst()` 只收同 id 的第一个包，第二个 filter 又把"id 在名单内"的包全部排除 ⇒
**同 id 的第二个及以后的包既不在前段也不在后段，直接从 `packList` 里消失**。
GUI 是**按包**取模型的（`MaidModelGui` → `AbstractModelGui.getDisplayModelList()` →
`modelPackList.get(packIndex).getModelList()`；搜索模式的全量表 `allModelsList` 也是按包拼的）
⇒ 整包模型一起"被隐藏"，一个都点不到。

**为什么只有 1.21.1 犯病（磁盘 + 日志实测）**

| | tlm_custom_pack 里的女仆包 | domain=geckolib 的包 | 结果 |
|---|---|---|---|
| 1.21.1 | 7 个 | **2 个**：`maidspell_geckolib_models-1.0.0`(3 个模型) + `touhou_little_maid-1.0.0`(27 个) | maidspell 在 `listFiles()` 顺序里靠前（客户端 debug.log 实测它先加载）→ **内置 geckolib 包被丢** |
| 1.20.1 | 6 个 | **1 个**：只有内置 `touhou_little_maid-1.0.0`(27 个) | 无撞名，不丢包 → 界面正常 |

被丢掉的是**声明 27 个模型（含 `extra_textures` 展开后共注册 41 个 id）的内置 geckolib 包**——
winefox 全家族（winefox / _blue / _nine_tailed / _hanfu / _new_year / _jk / _astronaut / _kongfu /
_magical / _saint / _elf / _salesperson / _little / _matured / _momo / _wedding / _tactics / _mini /
_survivor）+ zhiban 系列 + hailuo_new_year + kluonoa + foxmaid + rice_cake_fox，全都没了——
正是玩家说的"有很多模型被隐藏了"。

（排查中另有一处易混点：日志里 `geckolib:qingluka`、`next_update_model:*` 这些是**椅子/装饰**模型，
走 `ChairModels` 另一个列表、另一套 `CHAIR_SORT`，与女仆模型界面无关，本次不动。）

### 改法

新增客户端 mixin `com.maidsmart.mixin.MaidModelPackSortMixin`（两树同源；登记在
`mixins.promaid.json` 的 **client** 段——`MaidModels` 是客户端类，绝不能进通用段）：

```java
@Mixin(value = MaidModels.class, remap = false)
public abstract class MaidModelPackSortMixin {
    @Inject(method = "sortPackList", at = @At("HEAD"), cancellable = true)
    private void promaid$sortPackListKeepAllPacks(CallbackInfo ci) { ... ci.cancel(); }
}
```

替换实现只做**排序**、不再丢包：MAID_SORT 名单里的 id 仍按名单顺序优先（同 id 的多个包按加载顺序
依次排在一起），其余包按 id 排序跟在后段。**唯一的语义变化是"不再丢弃"**——正常无撞名的场合
（如 1.20.1）新旧顺序完全一致，故不影响任何现有布局。

### 验证

1. **对真实 TLM 类跑算法对照**（不是读代码推断）：`SortDemo` 用 TLM jar 里真实的
   `DefaultPackConstant.MAID_SORT` 与 `CustomModelPack.getId()`，输入取两个实例磁盘实测的女仆包清单
   与客户端日志实测的加载顺序：
   - 1.21.1（7 包）：原版算法 **6/7 存活**，`geckolib`(内置 27 个模型) 消失；本模组算法 **7/7，丢包 0**；
   - 1.20.1（6 包）：原版与本模组算法**都是 6/6**（对照组解释得通）。
2. 两树编译零错误（1.20.1 229 个源文件 / 1.21.1 231 个源文件）。
3. jar 重建并逐项核对：新旧 class 已入包、`mixins.promaid.json` 的 `client` 段已含
   `MaidModelPackSortMixin`、`javap -c` 可见 `MaidModels.getPackList` / `DefaultPackConstant.MAID_SORT` /
   `CustomModelPack.getId` / `CallbackInfo.cancel` 全部就位。
4. `deploy_all.py` 三个部署点全部 `match=True`（1.21.1 客户端、1.20.1 客户端、pack1201 服务器）。
5. `test_server.py 1201` PASS（Done 3.199s）、`test_server.py neoforge1211` PASS（Done 0.937s）；把本条 changelog 打进 jar（重新打包）后复测仍 PASS（Done 2.847s / 0.976s）。
6. 已部署待实测：打开女仆模型选择界面 → geckolib 页应能看到 27 个内置模型（winefox 全家族等）；
   1.20.1 侧顺序与内容应完全不变。


## 实测四百九十六【鞘翅要"起飞一次"才渲染 → 一穿上就渲染】

### 根因（代码路径实证）
渲染层的显示条件本身**没有问题**——两树 `LayerMaidElytra` / `LayerMaidElytraGecko` 都只看三件事：

```
isInvisible==false  &&  MaidFlightKit.isFlightTask(maid)  &&  isUsableElytra(胸甲槽)
```

**没有**"必须离地/必须滑翔"之类的额外条件，所以"穿上即渲染"在渲染侧本来就成立。

问题出在**有没有被穿上**。全局搜索确认：**胸甲槽的唯一写入者是 `MaidFlightKit.equip()`
（两树各一处：`m_8061_(EquipmentSlot.CHEST, ely)` / `setItemSlot(EquipmentSlot.CHEST, ely)`）**，
而它此前只在飞行作战行为里被调用：

```java
// MaidFlightCombatBehavior.tick(...)
LivingEntity target = currentTarget(maid);
if (target == null) { endFlightSafely(maid, id); return; }   // ← 没敌人，直接返回
...
MaidFlightKit.equip(maid);                                   // ← 永远走不到
```

于是因果链是：**切到空袭任务 → 还没遇到敌人 → `tick` 在 `currentTarget == null` 处就返回
→ `equip()` 从未执行 → 胸甲槽没有鞘翅 → 渲染层看不到 → 直到第一次接敌起飞（= `equip` 终于
跑了一次）才出现**。这正是玩家描述的"必须起飞一次才渲染"。

`MaidToolAutoEquipBehavior`（core 行为，任何 activity 每 tick 都跑）此前虽然会在飞行任务下
被访问到，但那个分支**只发缺装备提示、把换装完全交给行为里的 `equip`**（源码注释原文：
"本分支只提示、不擅自换装（换装仍由 MaidFlightKit.equip 在行为里做）"）——所以没有兜住。

### 改法
在两树的 `MaidToolAutoEquipBehavior` 飞行任务分支里，先调用一次
`MaidFlightKit.equip(maid)` 再发提示：

```java
if (MaidFlightKit.isFlightTask(maid)) {
    MaidFlightKit.equip(maid);                       // ← 新增：切到任务即穿戴整齐
    MaidFlightCombatBehavior.notifyNotReady(maid, gameTime);
    return false;
}
```

安全性论证（都已核对源码）：
- **幂等**：`equip()` 三段都先判"是否已就位"（胸甲已是可用鞘翅 / 主手已是本任务合法武器 /
  副手已是烟花），就位则整段跳过、零开销——所以每 tick 调用不会反复换装。
- **不丢物品**：换下来的旧装备经 `giveBack` 塞回背包空格，无空格才落地（`m_5552_`）。
- **与行为里那次调用不冲突**：同一份幂等逻辑，重复调用无副作用；战斗行为保留原调用
  （它还要用返回值判断三件是否齐备）。

### 验证
1. 两树编译零错误。
2. **字节码校验**（javap -c 反汇编构建产物）：两树 `MaidToolAutoEquipBehavior` 的
   `checkExtraStartConditions` 里依次出现 `MaidFlightKit.isFlightTask` →
   **`MaidFlightKit.equip`** → `notifyNotReady`，与改法一致。
3. `deploy_all.py` 三个部署点全部 `match=True`。
4. `test_server.py 1201` PASS（Done 2.734s）、`test_server.py neoforge1211` PASS（Done 0.930s）。

### 待实测
- **把女仆切成空袭任务（附近没有敌人）**：鞘翅应立刻出现在背上，不再需要先起飞一次；
- 再切回其它任务：鞘翅应随任务切换而脱下（`equip` 只在飞行任务分支里调）；
- 接敌起飞后滑翔/收翅的展翅表现不受影响（渲染层的展翅逻辑本次未改）。

## 实测四百九十五【远程空袭激活时检测弹药——没弹药就不起飞】

### 现状
激活判定 `MaidFlightKit.isModeActive` 原来只有三件：鞘翅 + 武器 + 烟花火箭。
远程空袭因此会在**背包一支箭/一颗子弹都没有**的情况下照常起飞、升空盘旋，然后
一发都打不出来——白烧烟花、白暴露在敌人的远程火力下。

### 关键：不能一刀切"没弹药就不激活"（反编译 TLM 实证）
远程武器里**只有一部分消耗弹药**。逐个反编译 TLM 自己的远程任务，得到权威口径：

| 武器 | TLM 的弹药门禁 | 说明 |
|---|---|---|
| 弓 `TaskBowAttack` | **有** `hasArrow` | `ItemsUtil.findStackSlot(inv, bow.m_6437_())` |
| 弩 `TaskCrossBowAttack` | **有** `hasAmmunition` | 箭 **或** 烟花火箭（`FireworkRocketItem`） |
| 御币 `TaskDanmakuAttack` | **无** | 弹幕不消耗弹药（用武器本体发射） |
| 三叉戟 `TaskTridentAttack` | **无** | 投出去的是武器自身的副本 |

枪械另有既有判据：`GunCompat.hasGunAndAmmo` 已经正确处理了"卓越前线能量武器
内部充能、不吃常规弹药"（`isEnergyGun` 直接豁免）。

所以本模组的门禁**只对"真的吃弹药"的武器生效**：弓/弩认箭、枪认子弹；
**御币/三叉戟/能量枪一律放行**。若对它们也强加门禁，会造出一个比原问题更糟的
新 bug——"持御币/三叉戟的女仆永远判定缺弹药、永远不起飞"。

### 改法
1. `MaidFlightKit.hasAmmoForRanged(maid)`（两树新增）：按上表分类判定，箭的判据
   **与开火路径 `MaidFlightRangedTask#findArrow/findAmmo` 的三层一一对齐**
   （副手箭 → 武器 `supportedProjectiles` 认可的弹药 → 背包任意箭）——激活检测与
   实际能不能打出去必须是同一口径，否则会出现"判定有弹药却打不出去"或反之。
2. `isModeActive`：近战空袭不变；**远程空袭**在三件之外追加弹药要求。
3. `missingParts`：远程空袭缺弹药时额外报出「弹药」——否则玩家只看到"三件齐了却
   没起飞"，完全不知道为什么（这正是本次要修的可观测性问题）。

### 实现时发现并规避的一个陷阱
`isModeActive` 是在 `equip()` **之前**调用的，那一刻**主手可能还是空的**（弓和箭
都还在背包里）——若直接拿主手判弹药，会把这种完全可用的配置误判成"缺弹药"→
永远不激活。所以新增 `resolveRangedWeapon(maid)`，**逐字照抄 `equip()` 的选武器
规则**（主手已是本任务合法武器就不换，否则从背包取第一把合法的）再判弹药。

### 验证
1. 本条新用的符号逐个用 javap 对着**编译期真实 jar** 核对存在
   （`m_6437_` = `ProjectileWeaponItem.supportedProjectiles`、
   `getAllSupportedProjectiles`、`CrossbowItem`、`FireworkRocketItem`、`ArrowItem`）。
2. 两树编译零错误。
3. **字节码校验**（javap 反汇编构建产物）：两树 `MaidFlightKit` 均含
   `hasAmmoForRanged` / `resolveRangedWeapon` / `hasArrowFor`，且 `.class` 常量池里
   有 `ProjectileWeaponItem` / `CrossbowItem` / `FireworkRocketItem` /
   `GunCompat.hasGunAndAmmo` 引用与缺件文案「弹药」。
4. `deploy_all.py` 三个部署点全部 `match=True`。
5. `test_server.py 1201` PASS（Done 2.735s）、`test_server.py neoforge1211` PASS（Done 0.783s）。

### 待实测
- 远程空袭 + **空手或只有弓、背包无箭** → 不起飞，气泡/系统消息应报「缺弹药」；
- 放上箭后 → 正常起飞盘旋开火；
- **御币 / 三叉戟 / 枪械（含卓越前线能量武器）** 即使背包没有箭也**照常激活**（不应被门禁误伤）；
- **弩 + 副手烟花火箭** → 算有弹药、正常激活（照 TLM 口径）；
- 近战空袭不受影响（不查弹药）。

## 实测四百九十四【空袭落地水/雪改成重锤同款特殊落地缓冲 + 新增空袭免摔开关】

### 根因（代码实证）
先讲清"重锤那套"是什么：重锤跃起落地时，行为置位 `FORCED_CLUTCH`，由
`WaterClutchBehavior`（core 优先级 240，在重锤行为 235 之后 tick）在同一 tick
**在落点强制放一格水/细雪**——不看 `fallDistance` 阈值、不看抑制条件
（因为猛击加成此刻已结算完，放水只负责让她落地不摔）。

空袭侧的问题是**结构性的**，两树表现不同：

1. **1.21.1（Neo）**：`MaidMaceSmashBehavior.isAirborne` = `isLeaping || MaidFlightKit.isFlightAirborne`
   —— 把飞行也并进了"空中"，于是 `WaterClutchBehavior` 把它当成"重锤跃起中"直接
   `return`（让位，保住 fallDistance 给猛击）。但飞行侧**从不**置位 `FORCED_CLUTCH`
   （只有重锤落地帧置位）→ **空袭落地完全没有落地水/雪**，纯靠硬摔。
2. **1.20.1**：这棵树没有重锤类，通用落地水其实还在跑；但通用逻辑按
   `fallDistance >= 阈值` 触发，而滑翔中 `LivingEntity.travel` 每 tick 把
   `fallDistance` 钳在 ~1.0（本模组多处注释已实证），远低于默认阈值 3
   → 空袭里几乎不触发，等于没有。

### 改法
**一、空袭落地缓冲（两树，与重锤同款语义）**
把"重锤跃起让位"与"空袭落地缓冲"**拆开判**，不再共用一个 `isAirborne`：

- 跃起中（`isLeaping`）→ 仍然完全让位（保住 fallDistance 给猛击加成，原语义不变）；
- 重锤落地帧（`consumeForcedClutch`）→ 先判（落地那一 tick 两个标志会同时为真，
  顺序不能反），在落点强放一格水/雪，原语义不变；
- **空袭（新增）**→ 落点帧改由落地水自己判：`未落地 + 正在下坠 + 脚下 1~2 格内有
  地面`，即重锤 `isNearGround` 的同款判据（口径照抄，两处同名 private 方法）。
  **不看 fallDistance 阈值**——滑翔把它钳在 ~1.0，按阈值判永远不触发（这也正是
  1.20.1 旧版接不住的原因）。有落点且背包有水桶（非下界）/细雪桶 → 强制放一格，
  与重锤落地帧完全一致。

这样"一直防止女仆摔死"和"不干扰收翅猛击加成"两个目标同时成立：猛击段（跃起中）
不放水保住加成，落地帧（下坠贴地）才放水接人。

**二、新增配置开关 `combat.flightNoFallDamage`（默认关，用户明确要求默认关）**
- 复用 `MaidFlightWallGuard`（它已负责"飞行免疫鞘翅撞击伤害"），同一套
  `message_id` 判定手法新增一条 `fall` 分支；
- `data/minecraft/damage_type/fall.json` 的 `message_id` 两版都是 `"fall"`
  （1.20.1 与 1.21.1 版本目录 jar 逐个开包核对过 68/79 个 damage_type json，
  与既有的 `flyIntoWall` 同样跨版本稳定）；
- 只作用于飞行任务（`MaidFlightKit.isFlightTask`），不影响普通坠落；
- 触发时有日志（latest.log 搜「空袭免摔」）便于实测确认是本闸拦下的；
- 默认关 = 保持"靠落地水/雪保护"的原始体验；开启 = 彻底免摔。

配置面板在"生存与复活"分区、紧跟"免疫鞘翅撞击伤害"一行；四个语言文件同步
（`config.promaid.combat.flightNoFallDamage`：空袭免疫摔落伤害 /
Air Raid: Immune to Fall Damage）。

### 验证
1. 两树编译零错误。
2. **字节码校验**（javap -c 反汇编构建产物）：两树 `MaidFlightWallGuard` 都含
   `String "fall"` 比较 + `COMBAT_FLIGHT_NO_FALL_DAMAGE` 字段读取 + `setCanceled`，
   与 `flyIntoWall` 分支并列；两树 `WaterClutchBehavior` 均含空袭分支与
   `flight clutch trigger` 诊断日志。
3. 四个语言文件 JSON 合法且新键值正确。
4. `deploy_all.py` 三个部署点全部 `match=True`。
5. `test_server.py 1201` PASS（Done 2.990s）。
6. **NeoForge 首跑 TIMEOUT，已定位为新增配置项触发的一次性配置迁移**（日志尾部是
   `promaid-common.toml is not correct. Correcting`，目录里出现
   `promaid-common.new.tmp.toml`）；核对迁移后的文件确认
   `flightNoFallDamage = false` 已写入、旧键保留；**重跑即 PASS（Done 0.996s）**。
   残留的 `.new.tmp.toml` 已清理（`.toml.bak` 是 NeoForge 正常备份，保留）。

### 待实测
- 两种空袭（近战空袭/远程空袭）从高空落地：背包有水桶/细雪桶时应自动放一格
  接住、不摔伤（latest.log 搜 `flight clutch trigger` 可确认触发）；
- 收翅猛击那一下的伤害加成不应因放水而丢失（放水发生在猛击结算之后）；
- 开关打开后（默认关），空袭状态从高空摔下应完全不掉血（latest.log 搜「空袭免摔」）；
- 普通坠落（非空袭任务）不受本开关影响。

## 实测四百九十三【1.21.1 的 pack_format 写成了 1.20.1 的值（15 → 34）】

### 背景
这是我在此前汇报里主动标出、但当时未获授权修改的一处瑕疵。本次要求修复。

### 现状与实证
四处 `pack.mcmeta`（两树各一 + 对应 `staging_*` 构建产物）原本**全部写着 15**。
构建脚本 `build_promaid*.py` 的真源是 `promaid_src/pack.mcmeta` /
`promaid_src_neo/pack.mcmeta`（第 71/76 行 `shutil.copy2(SRC/pack.mcmeta, STAGING/...)`），
`staging_*` 只是中间产物、每次构建会被覆盖。

**1.21.1 的正确值是 34**，三条独立证据一致：

1. **vanilla 客户端常量（最权威）**——`javap -v` 读 `net.minecraft.SharedConstants` 的 ConstantValue：
   ```
   public static final int RESOURCE_PACK_FORMAT;
       ConstantValue: int 34
   public static final int DATA_PACK_FORMAT;
       ConstantValue: int 48
   ```
   （1.20.1 侧这两个字段名被混淆、读不到符号名，但同一常量表里资源/数据包格式均为 `15`，
   与下面两条一致。）
2. **TLM 官方**——`touhou_little_maid` 自己的 `pack.mcmeta`：
   1.20.1 版 = **15**，1.21.1 版 = **34**。
3. **同目录其它 1.21.1 mod**——TACZ（`tacz-neoforge-1.21.1`）= **48**（它的数据包格式）；
   而写着 15 的那几个（Neat 标 `1.21`、IMBlocker 标 `1.20.6-1.21.8`）都是**跨多版本打包**的，
   用旧值兼容更宽的区间，不能作为 1.21.1 单一版本的目标值。

### 修法
只改一处：`promaid_src_neo/pack.mcmeta` 的 `pack_format: 15 → 34`。
1.20.1 那棵树**本来就是对的（15）**，不动。

### 影响
`pack_format` 只参与资源包的版本匹配校验（不匹配时游戏给"该资源包为旧/新版本制作"的警告，
并影响是否默认启用）。它**不参与任何代码逻辑**，改它不会影响玩法、存档或本次其它修复。

### 验证
1. 构建后逐个开包核对，**构建产物、三处部署点、两处 staging 中间产物、两处源码**共 8 个副本
   的 `pack_format` 全部符合预期：1.20.1 系 = 15、1.21.1 系 = 34。
   （首次核验时发现"已部署的 1.21.1 仍是 15"——因为我改源码并重建后还没重新部署；
   部署后复验通过。这条如实记录。）
2. `deploy_all.py` 三个部署点全部 `match=True`。
3. `test_server.py 1201` PASS（Done 2.767s）、`test_server.py neoforge1211` PASS（Done 1.133s）。

### 待实测
1.21.1 客户端加载时不应对本模组的资源包报"为其它版本制作"的警告；TLM 配置界面的
Gecko 皮肤列表应与修复前一致（该改动不涉及皮肤资源）。

## 实测四百九十二【飞行攻击的附魔效果核查与补齐 + 两个模式改名】

### 一、附魔核查结论（逐条由反编译/字节码实证，不靠记忆）

**近战（两树都基本齐全）**
`MaidFlightCombatBehavior.hitOne` 已用 `EnchantmentHelper.m_44833_`（锋利/亡灵杀手/节肢杀手
的伤害加成）、`m_44914_`（火焰附加）、`m_44823_`（荆棘/looting 等 doPostAttack）。
1.21.1 更是已经走现代 API（`modifyDamage` / `modifyKnockback` / `doPostAttackEffects`）。
**唯一缺口**：1.20.1 的击退量被写死 `m_147240_(1.0, ...)`，没读击退附魔
（`EnchantmentHelper.m_44894_`）也没读 `ATTACK_KNOCKBACK` 属性 → "击退"附魔不生效。
已按原版 `Mob.m_7327_` 算式补齐：`(属性 + m_44894_) * 0.5`。

**远战·弓弩箭矢（1.20.1 全缺，1.21.1 本就生效）**
这是本次最主要的缺口。
- **1.20.1 根因（反编译实证）**：`ArrowItem.m_6394_(level, stack, shooter)` **只有三个参数、
  不收武器**（`new Arrow(level, shooter)` 后只 `m_36878_(stack)` 设拾取物）。所以"给箭上附魔"
  在原版里是 **`BowItem.releaseUsing` 手工写上去的**：Power → `m_36781_(m_36789_() + lv*0.5 + 0.5)`、
  Punch → `m_36735_(lv)`、Flame → `m_20254_(100)`。我们旧版造完箭直接射出，**这三条从未被调用**。
- **1.21.1 为什么不用补**：那边 `createArrow(level, stack, shooter, weapon)` **多一个武器参数**，
  存进箭矢的 `firedFromWeapon`，命中时由 `EnchantmentHelper.modifyDamage` / `modifyKnockback`
  读取结算（`onHitEntity` 第 374 行、`doKnockback` 第 457 行），且这两个是**数据驱动、
  无 `instanceof Player` 门控**（实测确认）——我们早已把 weapon 传进 createArrow，所以一直生效。
- **修法**：1.20.1 的 `shootArrow` 照抄原版 BowItem 那三行（算式逐字一致）；
  另补**无限附魔**（`f_44952_`）：有则不吃背包箭（两树都补——我们自己扣箭，绕过了原版
  `processAmmoUse`）。

**远战·御币弹幕（两树都缺三个专属附魔）**
TLM 的阻碍/神速/末影杀手是注册在 GOHEI 类别上的附魔
（反编译 `InitEnchantments`：`EnchantmentCategory.create("gohei", item -> item instanceof ItemHakureiGohei)`），
**自身没有行为**，作用全靠 `TaskDanmakuAttack` 读等级后传给弹幕 API：
`speed *= (speedyLevel+1)`、`setImpedingLevel(impedingLevel)`、`setHurtEnderman(endersEnderLevel>0)`。
旧版整段跳过（注释写"保持与旧版一致：不吃这三个"），本次按用户要求补齐，三条算式与 TLM 一致。
1.20.1 用 `EnchantmentHelper.m_44843_(InitEnchantments.X.get(), stack)`；
1.21.1 用 `EnchantmentKeys.getEnchantmentLevel(registryAccess, EnchantmentKeys.X, stack)`。

### 二、两个模式改名（显示名；UID 不动）
| 位置 | 旧 | 新 |
|---|---|---|
| `flight_combat` | 飞行近战 / Flight Melee（1.21.1 英文原为 Flight Combat） | **近战空袭 / Melee Air Raid** |
| `flight_ranged` | 飞行远战 / Flight Ranged | **远程空袭 / Ranged Air Raid** |

- 改了：四个 `lang/*.json` 的名称值 + `.desc` 首句称呼、两个任务的
  `getMaidActionSummary()`（TLM 任务列表 UI 显示）、运行时日志标签 `远程空袭`。
- **刻意不改任务 UID**（`maid_smart:flight_combat` / `flight_ranged`）：那是写进存档的
  内部标识，改名会让已存档女仆的任务失效。
- 配置里的"飞行作战"是**伞形总开关**（覆盖两个模式），不是模式名，保持原样。
- 历史变更日志（实测四百xx 里按旧名记录的条目）保持原样，不改写历史。

### 验证
1. 本次新用的每个符号都先用 javap 对着**编译期真实 jar** 逐个核对存在
   （`f_44988_/f_44989_/f_44990_/f_44952_`、`m_36781_/m_36789_/m_36735_`、`m_44894_`、`f_22282_`、
   `InitEnchantments.IMPEDING/SPEEDY/ENDERS_ENDER`、`EnchantmentKeys.*`、`setImpedingLevel`）。
2. 两树编译零错误。
3. **字节级校验**（直接读 jar 内 .class 常量池/常量引用）：17 项全 OK——1.20.1 远战含
   Power/Punch/Flame/Infinity 与三个自定义附魔、含 `setBaseDamage`/`setKnockback`/`setImpedingLevel`；
   1.20.1 近战含 `m_44894_` 与 `f_22282_`；1.21.1 远战含三个 EnchantmentKeys 与 INFINITY。
   四个语言文件均为新名、无旧名残留。
4. `deploy_all.py` 三个部署点全部 `match=True`。
5. `test_server.py 1201` PASS（Done 2.803s）、`test_server.py neoforge1211` PASS（Done 0.889s）。

### 待实测
- 1.20.1 飞行远战拿**附魔弓**（力量/冲击/火矢/无限）射击：箭伤应提高、命中应击退、应点燃、
  有无限时不掉箭；拿**附魔御币**（阻碍/神速/末影杀手）：弹幕应更快、有减速、能打末影人。
- 1.20.1 飞行近战拿**附魔剑**（击退）：命中应把怪打退（旧版退距固定）。
- 两个模式名在 TLM 任务列表里应显示为 **近战空袭 / 远程空袭**。

## 实测四百九十一【鞘翅渲染：尺寸改为与玩家等大 + 顶端下移 0.3 格对齐肩线】

### 落点
四个文件（两树各两个图层）：
`promaid_src/.../client/LayerMaidElytra.java`（Bedrock 模型女仆）、
`promaid_src/.../client/LayerMaidElytraGecko.java`（GeckoLib 模型女仆），
以及 `promaid_src_neo/` 下同名两个。两树常量原本完全一致。

### 改了两个常量
```java
SIZE_FACTOR = 0.85f  ->  1.0f    // 尺寸：玩家的 0.85 倍 -> 与玩家等大
LIFT_BLOCKS = 1.0f   ->  0.7f    // 上移：1.0 格 -> 0.7 格（净降 0.3 格）
```

### 为什么"降 0.3 格"是减在上移量上（而不是另加一个位移）
变更日志实测四百八十记着：整体**上移 1 格**（用户当时的要求）。用户现在说
"再降低 0.3 格"，方向无歧义 = 把那次上移从 1.0 减到 0.7。

### 为什么"顶端对齐肩膀"由 LIFT_BLOCKS 单独决定（实测四百九十一·数据实证）
扫全部 18 个 winefox 族模型 JSON（`maidmods/work/.../geckolib/models/entity/winefox*.json`）：

```
winefox.json:  ElytraLocator pivot=[0, 30.9644, 3]  parent=Elytra
               Elytra        pivot=[0, 30.9644, 3]  parent=UpperBody
               LeftArm       pivot=[3.25, 29.1, 0]  cubeBOX 顶 y=30.43603
               UpperBody     pivot=[0, 23.9644, 0]  cubeBOX 顶 y=31.18343
```

`ElytraLocator` 的枢轴（y=30.9644px = 1.935 格）与肩线（`LeftArm` 枢轴 29.1 /
手臂方块顶 30.436px ≈ 1.902 格）换算成格后落在**同一高度**——即**挂点本身就在
肩线上**。所以翅膀顶端高度完全由 `LIFT_BLOCKS` 一个量决定，不需要额外的锚点偏移。

### 为什么改尺寸不会挪动顶端（实证）
反编译原版 `ElytraModel.m_170539_()`：左右翅的 `PartPose` 枢轴是 `(±5, 0, 0)`，
翅面沿 +Y 向下延伸（cube `-10,0,0, 10,20,2`）。**枢轴就在 y=0**，所以
`scale(k)` 只改变翅膀大小、不改变枢轴位置 → 顶端位置与尺寸解耦，
两个常量互不干扰，可以各自独立调。

### 变换链坐实（本次新证的依据，供后续调参查阅）
- `RenderUtils.prepMatrixForLocator` 逐骨骼 `translateMatrixToBone` / `translateToPivotPoint`
  全部按 `/16.0f` 换算 → **锚点之后单位是"格"、Y 朝上**，`LIFT_BLOCKS` 的"格"语义成立。
- `IGeoRenderer.renderEarly` 直接 `poseStack.scale(width, height, width)`，而
  `GeckoEntityMaidRenderer` 覆写 `getWidthScale/getHeightScale` 返回 `renderEntityScale`
  → 实体缩放确实乘在 poseStack 上；且 `renderEarly`（render 第 82 行）与后面的图层
  调用（第 246 行）之间**没有 push/pop 包住那段 scale**，所以图层矩阵里带着该缩放
  → 注释里"除以 modelScale 才是实打实的世界格"成立。
- winefox 的 `render_entity_scale` = 0.65（`geckolib/maid_model.json`），
  不除缩放的话 0.3 格会被放大成约 0.46 格。

### 验证
1. 两树编译零错误（1.20.1 `compile_promaid.bat`；Neo `run_javac_neo.py`，仅既有 unchecked 警告）。
2. 两个 jar 构建成功：`promaid-1.1.1.jar` 9153153 字节、`promaid-1.1.0-neoforge-1.21.1.jar` 9183421 字节。
3. **字节级校验**：直接读 jar 内 `.class` 常量池，四个类的 IEEE-754 常量均为
   `1.0f` + `0.7f` + `0.9375f`，旧的 **`0.85f` 已完全消失**（不只看编译日志）。
4. `deploy_all.py` 三个部署点全部 `match=True`。
5. `test_server.py 1201` PASS（Done 2.929s）、`test_server.py neoforge1211` PASS（Done 0.889s）。

### 待实测
飞行作战穿鞘翅，看两点：① 翅膀大小是否与玩家背上的一致；② 翅膀顶端是否落在
winefox 肩线上。若仍偏高/偏低，只需调 `LIFT_BLOCKS`（两树四个文件同名常量），
每 0.1 格就是 ±0.1。

## 实测四百九十【御币的扇形范围攻击，在飞行远战期间丢失】

### 根因（CFR 反编译实证）
`DanmakuShoot` 有两条发射路径，**它们是不同的形态**：

```java
public void aimedShot()      // 只造 1 发，直线瞄向目标
public void fanShapedShot()  // 按 setFanNum(n) + setYawTotal(弧度) 张开扇形
```

`fanShapedShot` 的内部实现（反编译）：以目标方向为轴，`yaw` 从 `-yawTotal/2` 起、
每发递进 `yawTotal/(fanNum-1)`，逐发 `Vec3.yRot(yaw)` —— 这就是"扇形"。

而我们的飞行远战里，御币的分支是**无条件单发**：

```java
DanmakuShoot.create()....setVelocity(1.6f).setInaccuracy(1.0f).aimedShot();
```

整段扇形规则从未被调用，所以御币在飞行远战时退化成"一发一发单点"。

### TLM 的原始规则（**不是"永远扇形"**）
TLM `TaskDanmakuAttack.performRangedAttack` 按【周围同种敌人数量】分档：

| 周围同种敌人数 | 发射形态 |
|---|---|
| ≤1 且 **无**多重射击 | `aimedShot()` 单发瞄准 |
| ≤1 且 **有**多重射击 | `fanShapedShot()` 3 发 / 0.2618 rad（15°） |
| ≤5 | `fanShapedShot()` 8 发 / 1.0472 rad（60°） |
| >5 | `fanShapedShot()` 32 发 / 2.0944 rad（120°） |

所以"单挑时是单发、群战才明显撒开"是有意设计。旧实现把整张表丢了。
（"同种敌人"判据 = 可攻击 + 同 EntityType + 有视线，与 TLM `enemyEntityTest` 同款。）

### 修法（两树同步）
`shootDanmaku` 按上表重写，并把 TLM 原式的伤害/速度/散布一并对齐：

```
speed      = 0.3*(distanceFactor+1)*(急速+1) + clamp(dist/40 - 0.4, 0, 2.4)
inaccuracy = 1 - clamp(dist/100, 0, 0.8)   （扇形路径再 /5）
damage     = 攻击力 × (df + 1.0 / 1.2 / 1.5)（按档位）
```

**本模组只有两处有意偏离 TLM 原版**（都已在代码注释里写明）：
1. 速度基线 `0.3f → 1.6f`：飞行远战是空中交战、目标更远，沿用原速会导致弹幕追不上
   （这也是旧版把 `setVelocity` 写死 1.6 的原因，此处保留该取向）；
2. 不吃 TLM 独有的「阻碍 / 神速 / 末影杀手」三个自定义附魔（与旧版一致），
   只保留**多重射击**这一条影响形态的。

数敌人所用记忆按各自版本取 TLM 原文口径：1.20.1 读 `NEAREST_VISIBLE_LIVING_ENTITIES`
（`f_148204_`，`stream` 计数），1.21.1 读 `NEAREST_LIVING_ENTITIES`（`List`，`stream` 计数）
+ `hasLineOfSight` —— 与 TLM 两版实现分别一致。

另：开火节流在本模组由 `RANGED_SHOT_COOLDOWN` 控制，调 `performRangedAttack` 时
`distanceFactor` 恒传 `1.0f`（= 满蓄力，与 TLM 蓄力完成才开火的效果一致）。

### 验证
- 本次新用到的每个 SRG/Mojmap 名都用 javap 逐个核对存在（不凭记忆）：
  `m_142582_`(canSee) / `m_6779_`(canAttack) / `m_6095_`(getType) / `f_148204_` /
  `m_44843_`(getEnchantmentLevel) / `f_44959_`(MULTISHOT) / `m_14036_`(clamp) 等；
- 两树编译零错；`deploy_all.py` 三处 `match=True`；
- `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS；
- 已部署待实测：飞行远战手持御币，**单只怪** → 单发；**3~5 只同种怪聚在附近**
  → 一次 8 发扇形；**超过 5 只** → 32 发大扇形。

## 实测四百八十九【床铺互通：核对两端均已落地；并修掉 Neo 编译 classpath 顺序隐患】

### 一、床铺互通核对结论：计划的两端**都已实现并记录在案**
用户提示"mixin 和 MaidBedInteropBehavior 两树都在，但 config 开关/面板条目/语言键全都没"——
实际逐项核对后，这些**都已在后续提交里补齐**（配置 2026-09-15 08:22 落地），
所以本次没有新增功能代码，改为**逐项验证其确实可用**：

| 检查项 | 1.20.1 | 1.21.1 |
|---|---|---|
| `MaidBedInteropBehavior`（女仆睡原版床） | 在 | 在 |
| `MaidBedBlockInteropMixin`（玩家睡女仆床） | 在 | 在 |
| mixin 配置登记 | 在 | 在 |
| `MISC_BED_INTEROP` 配置开关（默认开） | 在 | 在 |
| 配置面板「杂项 → 床铺互通」一行 | 在 | 在 |
| `lang` 中/英条目 | 在 | 在 |
| 手册章节 `bedInteropGuide()` | 在 | 在 |
| `getRestBehaviors()` 注册（优先级 6） | 在 | 在 |
| changelog / CHANGELOG 记录 | 在 | 在 |

**注入签名逐字对照 TLM 真实字节码**（本项目历史上出过"签名不匹配静默失败"）：

- 1.20.1：`isBed(..., Entity)` ✓、`m_6227_` 返回 `InteractionResult` ✓
- 1.21.1：`isBed(..., LivingEntity)` ✓、`useItemOn` 返回 `ItemInteractionResult` ✓

计划里标注"需实测确认"的两个技术点也已验证：
- `ServerPlayer$RespawnPosAngle.of(Vec3, BlockPos)` 确为 public static，Neo 侧可直接调用；
- `NeoForge IBlockExtension.getRespawnPosition` 默认返回空 → 我们显式实现它是**必要**的。

### 二、修掉一个真实的编译期隐患：Neo 编译 classpath 顺序
`gen_compile_neo.py` 里 `cp = [neo, mc, neou, tlm]` —— 未修补的 vanilla srg jar 排在
NeoForge 修补 jar **之前**，与文件里自己的注释要求相反。

javap 实测三个 jar 里 `ServerPlayer$RespawnPosAngle` 的访问标志：

| jar | 声明 |
|---|---|
| `client-1.21.1-…-srg.jar`（vanilla） | `final class`（**包私有**） |
| `neoforge-21.1.250-client.jar` | `public final class`（NeoForge 修补版） |
| `neoforge-21.1.250-universal.jar` | 无此类 |

而 `MaidBedBlockInteropMixin` 要实现 `getRespawnPosition`，其返回类型正是这个类。
用**包私有**类型做 public 方法的返回类型，按 JLS 就是编译错误。旧顺序能过只是因为
classpath 上先命中了 `neo(client)` 里的 public 版本 —— 属于"靠偶然命中"，一旦换成
universal-only 的 classpath 或调整 jar 顺序就会突然编译不过。

**修法**：`cp = [neo, neou, mc, tlm]` —— 与运行期一致，修补版在前；并把这层依据写进注释。

### 验证
- 对照实验：同一 classpath 下 `mc` 在前解析出 `final class`（包私有），`neo` 在前
  解析出 `public final class` —— 证实顺序确实决定可见性；
- 两树编译零错；`deploy_all.py` 三处 `match=True`；
- `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS；
- 已部署待实测：女仆睡原版 16 色床；玩家睡女仆床并能把它设为重生点；
  关掉「床铺互通」开关后两者都恢复 TLM 原版行为。

## 实测四百八十八【空战装备不齐的提示与语音，只在遇到敌人时才触发；应进入模式即报】

### 排查（语音包本身没问题，问题在触发时机）
逐项验证过，**语音资源链路全部正常**：

| 检查项 | 结果 |
|---|---|
| `flight_not_ready.ogg` 是否存在且有效 | 是（30981 字节，`OggS` 头） |
| 两树 jar（构建版 + 已部署版）是否打进该 ogg | 是 |
| manifest 条目 | `{"text": "空战装备不齐", "file": "flight_not_ready.ogg", "mode": "contains"}` |
| 按真实 manifest 模拟 `matchKey` | 命中 `flight_not_ready.ogg`（且排在 122 条最前） |
| `TTS_JAR_PACK_ENABLED` 默认值 | true |
| `speakable()` 是否放行 | 放行（含汉字） |

所以"没触发"是**根本没走到那句气泡**——语音是挂在气泡上的。

### 根因
`notifyNotReady` 只被 `MaidFlightCombatBehavior.tick()` 里那两处"三件不齐"分支调用。
而 `tick()` 前面还有一道 `canUse`：

```java
protected boolean checkExtraStartConditions(ServerLevel level, EntityMaid maid) {
    ...
    LivingEntity target = currentTarget(maid);
    if (target == null) {
        return false;          // ← 没有敌人 → 行为根本不启动
    }
```

于是**没敌人时缺装备永远走不到提示分支**；只有真遇到怪、行为开始跑，才顺带报一句。
这正好对上反馈的"触发条件很奇怪，只在遇到敌人才发文本"。

### 修法（两树同步）
1. `notifyNotReady` 改为 **public**，并挪到 `MaidToolAutoEquipBehavior.checkExtraStartConditions`
   里调用——那是 **core 行为**（优先级 200、任何 activity 都评估、每 tick 一次），
   与"有没有敌人"无关。切到空战任务立刻检查、缺件立刻报。
2. **齐备时清冷却**：`missingParts == null`（三件齐全）时 `NOTIFY_READY.remove(maid)`，
   这样"补齐 → 又缺"能立刻再报，不会被上一次的 15 秒冷却吃掉。
3. 该分支只提示、**不擅自换装**（补齐装备仍由 `MaidFlightKit.equip` 在行为里做，
   保持"玩家指定什么就是什么"的既有口径）。

### 验证
- 两树编译零错；`deploy_all.py` 三处 `match=True`；
- `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS；
- 已部署待实测：给女仆切到飞行近战 / 飞行远战但故意缺一件（如不放烟花）→
  **当场**冒气泡 + 系统消息 + 语音；补齐后再拿走 → 重新报。

## 实测四百八十七【飞行近战：飞向敌人贴近地面时，主人太远触发自动传送，把这一轮攻击卡掉】

### 根因
负责"空中禁传送"的 `MaidFlightKit.isFlightAirborne` **只认滑翔标志位**：

```java
public static boolean isFlightAirborne(EntityMaid maid) { return isGliding(maid); }
```

而收翅猛击那一段**主动清掉滑翔位**（`tickSmash` 里 `MaidFlightKit.setGliding(maid, false)`
——原版猛击判定要求 `!isFallFlying()`，不收翅就打不出猛击）：

```java
// ── 即将接触 → 取消滑翔，转入猛击段 ──
if (dist <= SMASH_RANGE && !WAIT_LAUNCH.contains(id)) {
    MaidFlightKit.setGliding(maid, false);
    SMASH.add(id);
```

于是"飞向敌人、贴近地面"那一刻恰好**不满足** `isGliding` → 禁传送豁免在此失效。
此时主人一超过阈值，任意一条自动传送链路就把她拽走，这一轮扑击直接作废。

另外查出两处漏接线：

| 链路 | 1.20.1 | 1.21.1 |
|---|---|---|
| TLM 原生 `teleportToOwner` 拦截 | 有飞行豁免 | **完全没有**（这棵树定义了 `isFlightAirborne` 却从未调用） |
| 同维度远距拉回 `trySameDimPull` | **没有飞行豁免** | **没有飞行豁免** |
| 跨维度跟随 `followIfCrossDimension` | 没有飞行豁免 | 没有飞行豁免 |

### 修法（两树同步）
1. **暴露"本轮攻击进行中"状态**：`MaidFlightCombatBehavior.isEngaged(maid/UUID)`——
   起跳（`JUMP_LEFT`）/ 烟花爬升（`LAUNCH_LEFT`）/ 收翅猛击（`SMASH`）/
   等待再放烟花（`WAIT_LAUNCH`）/ 远程俯冲助推（`RANGED_BOOST_LEFT`）任一为真即算。
2. **并入空中判定**：`isFlightAirborne` = 滑翔中 **或** `isEngaged`。豁免因此覆盖
   "收翅扑向敌人"这段滑翔位为空的窗口。
3. **接到全部自动传送入口**：同维度远距拉回、跨维度跟随（两树）；
   TLM 原生 `teleportToOwner` 拦截（补上 1.21.1 缺失的那道）。
4. **豁免有界**：状态表由行为收尾 `forget()` 整清——一轮打完 / 目标消失 / 行为停止
   立刻恢复正常传送，不会永久禁传（这也是没做成"全局永久开关"的原因）。

### 验证
- 两树编译零错；`deploy_all.py` 三处 `match=True`；
- `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS；
- 已部署待实测：飞行近战扑向敌人（含贴地猛击）期间主人拉开距离 → 女仆不被传送、
  这一轮打完；打完站着不动、主人再拉开 → 恢复自动传送。

## 实测四百八十六【指标石：女仆搭建中被收回后，建造不停；再放出来建模卡住】

### 根因（CFR 反编译实证，1.20.1 与 1.21.1 同源）
TLM 的 `ItemSmartSlab.storeMaidData` / `AbstractStoreMaidItem.storeMaidData` 是
**先给女仆拍 NBT 快照、再 post `ToItem` 事件**：

```
CompoundTag data = stack.getCompound("MaidInfo");
maid.save(data);                      // ← 快照此刻已写好（任务=index_build、建造标记=true）
MinecraftForge.EVENT_BUS.post(new ToItem(maid, stack, data));   // ← 我们在这里
```

也就是说我们的 `ToItem` 钩子执行时，魂符里**已经躺着**一份"临时建造中"的快照。
旧实现只清女仆活体 `persistentData`，改不到那份快照。放出来时 TLM `maid.load(快照)`：

1. 读 `MaidTask` → `setTask(maid_smart:index_build)`：隐藏的临时建造任务**当场复活**——
   而会话早在收回时就结算了 → 她又在"没有会话"的状态下进入临时建造（**建造不停**）；
2. 读回 `ForgeData` 里的 `maid_smart_index_build_active=true` → 行为 `canUse` 通过 →
   只能靠 `selfHealStale` 自愈，而**自愈路径是在行为 tick 内部直接调 `setTask`** →
   TLM `refreshBrain` 重建整个 Brain = 行为列表被换掉（未定义行为；项目在
   `IndexStoneBuildBehavior.finish` 的注释里已明确记录过这个风险）→ **女仆僵住（建模卡住）**。

### 修法（两树同步）
1. **清洗魂符快照**：`IndexStoneService.onMaidRecalled` 新增 `snapshot` 参数，
   收符时把 TLM 即将存进物品的那份 `event.getData()`（同一对象引用）一起修：
   ①`MaidTask` 写回【原任务】（从快照自己的 persistentData 读 `prev_task`）；
   ②建造/站桩标记整段删掉。**自守护**：只有快照里的任务确实是我们那个隐藏任务时才动
   一个字段——相机/胶卷等其他"存女仆物品"路径只洗快照、不结束会话（玩家拍照不该
   把正在进行的搭建判死刑）。
2. **自愈延后一 tick**：`selfHealStale` 的调用改为与 `finish` 同款
   `server.execute`（1.20.1 `m_18707_`）延后执行 + `healing` 锁防重复排程，
   杜绝"行为 tick 内重建 Brain"。
3. 站桩标记 `maid_smart_work_still` 一并从快照清除（它会让女仆保持站桩/影响播报）。

### 验证
- 两树编译零错；`deploy_all.py` 三处 `match=True`；
- `test_server.py 1201` PASS、`test_server.py neoforge1211` PASS；
- 已部署待实测：搭建中收回 → 行为立刻结束、播报"女仆被收回"；再放出 → 女仆恢复正常任务、不僵住。

## 实测四百八十五【指标石：锁定期间完全不能改选其它方块】

### 修法（两树同步）
锁定期间只认**两种结果**，没有第三种：

| 右击目标 | 行为 |
|---|---|
| **同一个锁定方块** | 解除锁定（唯一解除途径） |
| 别的方块 / 空气 / 超出距离 | **一律无效**：保持原锁定不变，吞掉这次右键并提示怎么解锁 |

三处改动：
1. **客户端钩子** `IndexStonePreviewClient.onInteraction`：已锁定时先 `pickAnyBlock` 取瞄准的方块，
   只有命中格 == 锁定格才发解锁请求；其余情况**不再发任何锁定请求**（旧版会发"换锁定点"），
   改为 `hintLocked()` 提示 + 取消事件。
2. **服务端兜底** `IndexStoneService.toggleLock`：把"已锁定 + 不同格"从"换锁定点"改为
   **拒绝并保持原锁定**，提示"锁定中——要先右击那个锁定方块解除锁定，才能选别的方块"。
   客户端已拦截，这层防伪造包/其它入口绕过。
3. **近处方块路径的误报修复** `IndexStoneItem.useOn`：锁定期间右击别的近处方块会被
   `toggleLock` 拒绝并保持原锁定，旧文案却把**被拒的那一格**播报成"已锁定 X,Y,Z"。
   现在按"操作前是否已锁定在别处"分流，只在**真正锁上新格**时报告坐标。

新增 `IndexStoneItem.lockedHint()`（锁定期间专属提示），与 `usageHint()` 一样是纯字符串、
无客户端类型依赖；`hintLocked` 与 `hintUsage` 共用 `hintCooldown` 限频，同一 tick 不会刷两条。
顺带把"（右击别的方块=换锁定点）"这类过期文案全部改掉。

编译零错（两树）、字节码校验入包、两个 server 回归 PASS、部署三目标。
﻿## 实测四百八十四【指标石：解除锁定必须右击【那个锁定方块】】

### 根因
服务端 `IndexStoneService.toggleLock` **本来就是对的**——它只在"右键的位置与已锁格相同"时
解锁，右键别的方块是**换锁定点**：

```
if (s.lockedBlock.equals(pos)) { s.lockedBlock = null; ... return false; }   // 同一格 → 解锁
// 落到下面 → 换锁定点
```

问题在**客户端输入钩子** `IndexStonePreviewClient.onInteraction`：已锁定时它**无条件**发
`LockRequestPacket(unlock=true, 0,0,0)`，而服务端收到 `unlock=true` 就走 `toggleLock(player, null)`
（`pos == null` 语义 = 直接解锁）。于是**只要右击任何地方**（别的方块、甚至空气）都会解锁。

### 修法（两树同步）
已锁定时先做长射线取"瞄准的方块"，再按三种情况分流：

| 右击目标 | 行为 |
|---|---|
| **同一个锁定方块** | 发解锁请求 → 解除锁定 |
| 别的方块 | 发锁定请求 → 服务端按"换锁定点"处理（不是解锁） |
| 空气 / 超出距离 | **什么都不做**，锁定保持不变，只提示用法 |

新增 `pickAnyBlock(mc)`：与原有 `farPick` 的区别是**不做距离过滤**。已锁定的方块常常就在
眼前（触及距离内），而 `farPick` 会把 ≤ 触及距离的命中过滤成 null，用它判断会漏掉近处的
锁定格 —— 那会导致"对着锁定方块右键却解不掉"。

顺带更新了会误导的文案与注释（原"再右键 → 取消锁定"/"再右击取消"→ 明确为
"右击**同一个**锁定方块才解除；右击别的方块=换锁定点"）。

编译零错（两树）、字节码校验入包、两个 server 回归 PASS、部署三目标。
﻿## 实测四百八十三【两版本代码交叉审查：修掉 1 处真实逻辑 bug + 4 处移植不一致 + 2 处死代码】

### 审查方式
对 `promaid_src`（1.20.1/SRG）与 `promaid_src_neo`（1.21.1/Mojmap）做了跨全树对照：
规范化 SRG↔Mojmap 命名后比较文件集、mixin 清单、网络包、配置项、方法集、常量表、
数值字面量、try/catch 覆盖与资源文件，可疑点全部 Read 原文件核实（不凭正则下结论）。

### 修掉的真实 bug（两树都有，与移植无关）
**`getTagType` 冒充 `getByte` 读布尔值 —— 三个开关"关不掉"**
代码注释写"getBoolean 等价实现 = getByte != 0"，但实际调用的是 `getTagType`。
已用官方 mappings + 字节码双重实证：`getTagType` 返回 `Tag.getId()`，而
`ByteTag.getId()` 是 `iconst_1/ireturn`（**常量 1**），所以 `!= 0` **恒为真**——
等于上一版声称修好的"关了又自己打开"原样复发。`getByte`（1.20.1 `m_128445_`）
才返回真实值。三处已改：
- `AiMemoryManager`（per-maid 记忆开关）
- `LlmEnableManager`（per-maid LLM 开关）
- `BuildArchive`（legacy 计划的 `paused` 状态）

### 修掉的移植不一致
1. **1.20.1 缺飞行作战 static 表的清理接线（唯一有"卡死"后果的）**：
   `MaidFlightCombatBehavior.forget/clearAll` 在 1.20.1 早已写好却**零调用**。
   滑翔/俯冲状态是 static 表，崩溃/强杀/卸载时行为没走到 `stop()` 就会残留，
   该女仆**永久"俯冲中"**——再也进不了飞行作战，并卡死"空中禁传送/落地缓冲"链路。
   已按 1.21.1 同步两处：`ProMaidExtension.onServerStarted`（逐女仆 forget +
   清滑翔位 + 全清兜底）、`AiMemoryManager.onEntityLeaveLevel`（卸载路径 forget + 清滑翔位）。
2. **鞘翅"上移 1 格"的方向与缩放（我自己上一轮引入的回归）**：
   1.20.1 的 Gecko 图层写成 `+LIFT_BLOCKS`（翻转后该空间 Y 朝下，向上应为 `-Y`，
   方向相反），且两处图层都漏除 `modelScale`（图层矩阵已被实体缩放乘过，
   不除则位移量随模型缩放漂移、不是实打实的 1 格）。两处已与 1.21.1 对齐为
   `-LIFT_BLOCKS / modelScale`。
3. **1.21.1 Bedrock 鞘翅图层缺 try/finally**：`pushPose` 与 `popPose` 之间没有
   try/finally，中途抛异常会漏 `popPose`、污染后续渲染矩阵（同功能的 Gecko 图层
   两树都有 try/finally，属单点退化）。已补。
4. **1.20.1 `sweepWithin` 缺"上一 tick 位置未初始化"守卫**：`xo/yo/zo` 全为 0
   （刚生成/刚传送）时，线段会从世界原点拉到当前位置 → 扫掠判定被放大成超大命中
   范围（等于放宽猛击命中）。已按 1.21.1 加守卫。

### 死代码清理
- 1.21.1 `MaidFlightCombatBehavior`：删 `SMASH_SWEEP_SQR`（扫掠半径已改用
  `SMASH_HIT_SQR`）与 `MELEE_REACH`（地面近战已改用 `isWithinMeleeAttackRange`）。

### 撤回一处未确证的改动
审查发现 1.20.1 `jumpForLaunch` 没有 1.21.1 的 `hurtMarked = true`（位移同步标志）。
但我**无法在合理成本内确证该字段的 SRG 名**（官方 mappings 给出的 `f_19864_`
在真实 jar 里不存在，映射表与 jar 版本不一致），所以**撤回了这次改动**，
不留凭记忆写的字段名。此项作为已知差异记录于此，不影响功能正确性。

### 未发现问题的覆盖面（明确说明，便于判断审查完整度）
文件集/mixin 清单/网络包/配置项/坐标与朝向计算/防摔死/防误伤/防刷物品 的
两树口径**逐条对齐**；`try/catch` 覆盖差异仅来自删掉的诊断代码、1.20.1 独有的
暴击粒子辅助方法与上面第 3 条；数组越界与空指针扫描 36 处下标访问两树对称且都有守卫。
已知合理例外：重锤（1.21.1 独有物品）、药水组件适配、数据包目录单复数。

编译零错（两树）、字节码校验修复已入包、两个 server 回归 PASS、部署三目标。
﻿## 实测四百八十二【生存搭建：搭一个扣一个（补支撑/种树苗不再白送方块）】

### 排查结论（先把事实摆清楚）
把 build + task 两个包的全部"扣料点 vs 放置点"扫了一遍：

- **蓝图建造主流程本来就是"搭一个扣一个"**：`MaidBuildBehavior` 在 `doPlace` 之前先调
  `BlueprintLib.consumeBlock`（内部 `extractItem(...,1)`），顺序正确；
- **不存在"搭完统一结算"的机制**：`placedCount`/`placedSet` 只用于进度显示与缺口检查
  （`BuildHudTracker`、`BuildPlan.getProgress`），不参与扣料；完工路径
  （激活红石/水、TNT 结算、清理掉落物）都不扣方块；
- 索引石搭路 `IndexStoneBuildBehavior.place` 的目标格材料由调用方 `resolveMaterial`
  先取（`takeBuildBlock` 先扣 1），下方支撑也走 `takeOneFor`（先 `extractItem`）**再**
  `setBlock`——顺序同样正确。**（我先前把它判成"先放后扣"是扫描器的启发式误报，此处更正。）**

### 真正"放了不扣"的口子（本次修掉）
1. **蓝图建造的"自动补支撑"白送方块**（3 处）：
   `doPlace` 里当附着方块（火把/按钮/拉杆/甘蔗/雪层等）的支撑面缺失时，会
   `level.setBlock(supPos, support, 3)` **补一块支撑**，但**完全不扣料** ——
   每补一次就凭空多一块，生存可刷。现在三处补支撑（主支撑 / 掉落验证后的附着类补支撑 /
   漏网类型补下方格）统一改为**先扣后放**：新增 `consumeExtraBlock(maid, support)`，
   走与主放置同一套 `BlueprintLib.consumeBlock` + `tryTakeFromOwner` 补料链；
   扣不到就**不补支撑、延后**（等有料再建），绝不出现"放了但没扣"。
2. **种树苗先种后扣、且不检查扣除结果**：`MaidPlanting` 原本先 `setBlock` 种下、
   再 `extractItem` 扣树苗，并且**完全没看扣除返回值**——扣除失败（槽位被并发改动）
   时树苗已经种下去了，等于白得一棵苗。现在改为**先扣后种**：新增
   `takeOneSapling`（手部栏优先、其次背包，槽位失效时退化为按物品搜），
   返回非空才算扣成功；扣不到直接放弃本轮、不放置。

### 顺带修正
- `tryTakeFromOwner` 改为 `static`（不依赖实例字段，供 static 的 `doPlace` 复用）。

编译零错（两树）、字节码校验新方法已入包、两个 server 回归 PASS、部署三目标。
﻿## 实测四百八十一【1.20.1 飞行作战与 1.21.1 未同步：只水平飞 + 漏了若干优化】

### 「只水平飞」的根因（字节码双重确证）
`LookControl` 的每 tick 逻辑在 `resetXRotOnTick()` 为真时会**把俯仰角清零**：
```
1.21.1  LookControl.tick():       0: invokevirtual resetXRotOnTick:()Z
                                  7: getfield mob  11: fconst_0  12: setXRot(F)   ← 清零
1.20.1  LookControl.m_8128_(tick):0: invokevirtual m_8106_:()Z
                                  7: getfield f_24937_  11: fconst_0  12: m_146926_(F) ← 清零
        m_8106_():                0: iconst_1  1: ireturn                          ← 恒真！
```
而 `lookAt`（`m_7618_`）写进去的抬头角就在被清零的字段上 → **下一 tick 就被抹掉**，
烟花推力只剩水平分量，于是永远平飞。

1.21.1 侧靠 `applyRotation()` **每 tick 重新写入角度** + `getLookControl().setLookAt(...)`
把"期望角度"交给 LookControl 来对抗；**这套机制当时没同步到 1.20.1**——1.20.1 的
`faceAwayAndUp` / `faceTarget` / `faceUpForward` / `faceOrbit` 全都只有 `m_7618_`。

### 修法
1. 1.20.1 新增 `applyRotation(maid, yaw, pitch)`，与 1.21.1 对齐：
   `m_146922_(yaw)` + `m_146926_(pitch)` + `f_19859_/f_19860_`（yRotO/xRotO，**渲染插值要用**）
   + `m_5616_/m_5618_`（yHeadRot/yBodyRot）；
2. 四个朝向函数改为"直接算 yaw/pitch → `applyRotation` 立即写入"，并保留 LookControl 的
   期望角度（`m_24950_(x,y,z,360,360)`），让每 tick 的归零被重新覆盖。

### 顺带补齐的另一处漏同步
`suppressVanillaMelee`（飞行期间给 Brain 写 40t 的 `ATTACK_COOLING_DOWN`，避免 TLM 自带近战
在她滑翔时插手挥砍、与"视线当操纵杆"打架）**只有 1.21.1 有**，1.20.1 完全缺失——
3 个调用点（阶段一爬升 / 阶段二盘旋 / 猛击段）一并补上。
SRG 名以官方 mappings 实证：`ATTACK_COOLING_DOWN = f_26373_`（同表 `ATTACK_TARGET = f_26372_`，
与项目既有用法交叉验证一致）、`Brain.setMemoryWithExpiry = m_21882_`。

### 逐项核对结果（两树飞行机制对照）
| 机制 | 1.20.1 | 1.21.1 |
|---|---|---|
| endFlightSafely 安全收翅 | 有 | 有 |
| notifyNotReady 未激活提示 | 有 | 有 |
| sweepWithin 位移线段命中 | 有 | 有 |
| applyRotation 立即转向 | **本次补上** | 有 |
| suppressVanillaMelee 抑制原版近战 | **本次补上** | 有 |
| isCriticalHit / isNearGround | 有 | 有 |
| tickGunFire / stopGunAim | 有 | 有 |
| takeFirework 只抽 1 枚 | 有 | 有 |
| LAUNCH_TICKS / CLIMB_TAN 分模式 | 有 | 有 |
| RANGED_BOOST 补烟花 | 有 | 有 |
| throwTrident / 御币弹幕 | 有 | 有 |
| 重锤后门（MAX_Y/forcedFall） | 不适用 | 有 |

重锤后门仅 1.21.1：重锤是 1.21.1 独有物品，1.20.1 没有 `Items.MACE`，因此**不是漏同步**。

编译零错（两树）、字节码校验新逻辑已入包、两个 server 回归 PASS、部署三目标。
﻿## 实测四百八十【鞘翅渲染：尺寸 0.85×、上移 1 格、修「只有 winefox 能渲染」】

### 反馈与根因
1. 仍然太小 → 改成玩家的 **0.85 倍**；
2. 位置偏下 → 整体**上移 1 格**；
3. **除 winefox 外都渲染不出鞘翅**。

第 3 条的根因查清了，而且和"小/偏下"不是同一件事。扫描全部 28 个 Gecko 女仆模型的骨骼 JSON：

```
Elytra            17/28   ← 全部是 winefox 家族
ElytraLocator     17/28   ← 同上
UpperBody         26/28
Root              28/28
```

旧代码只找 `ElytraLocator` / `Elytra`。**没有这两根骨骼的 7 个模型**（foxmaid、hailuo_new_year、
sta、winefox_hanfu、winefox_mini、zhiban_hanfu、zhiban_new_year）全部掉进"硬编码兜底"，
而那个兜底是从**原版** ElytraLayer 抄来的 `scale(-1,-1,1) + translate(0,-1.501,0)`——
那是**原版模型空间**的补偿量。可 Gecko 图层栈根本不在原版模型空间（TLM 的 Gecko 渲染器
自己 pushPose，单位=格、Y 朝上、原点在脚下），于是兜底把翅膀顶到脚下/身体内部 → 看不见。
这就是"只有 winefox 显示"的确切原因：**只有 winefox 家族恰好带那根骨骼，绕过了坏兜底。**

### 修法：永不掉进兜底
按优先级找骨骼，找不到专用挂点就锚到**一定有**的躯干/根骨骼，并补一段**从真实模型统计出来**
的平均偏移（17 个样本，取 `Elytra.pivot − bone.pivot` 的均值再 /16）：

| 锚点 | 优先级 | 补偿偏移（格） | 覆盖率 |
|---|---|---|---|
| `ElytraLocator` | 1 | (0, 0, 0) | 17/28 |
| `Elytra` | 2 | (0, 0, 0) | 17/28 |
| `UpperBody` | 3 | (0, +0.4310, +0.2011) | 26/28 |
| `Root` | 4 | (0, +1.8596, +0.2022) | 28/28 |

偏移在骨骼空间（Y 朝上）施加。**28/28 模型现在都有锚点**，兜底路径实际到不了；
万一还有都没有的模型，会打一条「鞘翅挂点」日志（只报一次）便于定位，而不是静默失败。

### 尺寸与位置
- 尺寸：`k = 0.85 × 0.9375 / modelScale`（0.9375 = 原版 `PlayerRenderer.scale`，
  即原来的"与玩家等大"；再乘 0.85）；
- 上移 1 格：Gecko 侧锚定后先 `scale(-1,-1,1)`（转为 ElytraModel 的 Y 朝下空间），
  故向上 = `-Y`，写作 `translate(0, -1.0/modelScale, 0)`；Bedrock 侧同样为 `-Y`。
  两者都放在 `scale(k)` **之前** → 不受鞘翅自身尺寸缩放影响；除以 `modelScale`
  是因为图层矩阵已被 `renderEarly` 的 `scale(widthScale, heightScale, widthScale)`
  乘过（字节码实证），不除会变成 `1/modelScale` 格。

改动文件（两树各 2 个）：`LayerMaidElytraGecko.java`、`LayerMaidElytra.java`。
编译零错（两树）、字节码校验新逻辑已入包、两个 server 回归 PASS、部署三目标。
﻿## 实测四百七十九【重锤后门·取消 40 格封顶，改回无限增伤】
### v1.2.0 上限是我不该加的，已去掉

**反馈**：不要上限，要的就是无限增伤。

**旧版为什么会有上限**：实测四百七十七里我自作主张加了 `MAX_FORCED_FALL = 40`，理由写的是「一轮烟花爬升通常 10~25 格，40 格基本不会触顶，只是防连放多枚烟花爬上天算出离谱数字」。这条既不是原版行为，也不是需求——原版三段式末段 `22 + (fd-8)` 本就无上限。

**修改**：`forcedFallDistance` 去掉了 `Math.min(MAX_FORCED_FALL, drop)`，字段 `MAX_FORCED_FALL` 删除。**只保留下限** `SMASH_MIN_FALL + 0.01`——那是为满足原版严格的 `fallDistance > 1.5`（`canSmashAttack` 字节码是 `ifle`，恰好 1.5 时加成仍为 0），不是封顶。

**数值**：落差 8 格 = +22、10 = +24、15 = +29、30 = +44、50 = +64、100 = +114，无上界。

仅 Neo 树（重锤是 1.21.1 独有物品；1.20.1 无 `Items.MACE`，其暴击只需 `fallDistance > 0`）。
编译零错（两树）、两个 server 回归 PASS、部署三目标。
﻿## 实测四百七十八【飞行远战·三叉戟单独一路（ThrownTrident 通道）】
### v1.2.0 三叉戟不是 ProjectileWeaponItem，旧版一枪都放不出来

**背景**：上一轮修「手持武器不参与开火」时，我曾提示「三叉戟也是 ProjectileWeaponItem，现在会落到造箭那支」。**这个说法是错的，此处更正**——反编译实证：
- 1.20.1：`TridentItem extends Item implements Vanishable`
- 1.21.1：`TridentItem extends Item implements ProjectileItem`（`ProjectileItem` 与 `ProjectileWeaponItem` 是两个不同接口）

也就是说三叉戟**两版都不是** `ProjectileWeaponItem`。旧版 `isRangedWeapon` 与 `performRangedAttack` 都以它为前提 → 女仆拿三叉戟时：
① 三件套里的「武器位」判否 → **飞行远战根本激活不了**；
② 自动装备也认不出它。

**为什么不能走原版那条路**：`TridentItem.releaseUsing`(`m_5551_`) 第一件事就是 `if (!(entityLiving instanceof Player)) return;`（字节码 `instanceof Player; ifne`）——女仆不是 Player，调用它等于空转。

**修复**（照搬 TLM `TaskTridentAttack.performRangedAttack`，字节码实证，不自己发挥）：
1. `MaidFlightKit.isRangedWeapon` 增加 `instanceof TridentItem`（与 TLM `TaskTridentAttack.isWeapon` 同口径）→ 远战激活 + 自动装备都能认出三叉戟；
2. `MaidFlightRangedTask` 新增 `isTrident` / `throwTrident`：自建 `ThrownTrident`，投**副本**（主手只掉 1 点耐久）；
3. **剥掉忠诚附魔**（1.20.1 `Enchantments.f_44955_`，已用官方 mappings 第 78057 行 `LOYALTY -> E` 交叉验证即忠诚；1.21.1 `Enchantments.LOYALTY`）——留着它 `isAcceptibleReturnOwner()` 对活着的女仆为 true，副本会飞回来还可能被捡走 = 凭空刷取；
4. `setNoGravity(true)` 直线飞行（配 TLM 的 `ThrownTridentMixin`：命中实体后才恢复重力）；
5. `pickup = CREATIVE_ONLY`，副本不落地可捡、到期由 `tickDespawn` 自行回收；
6. 速度/散布照 TLM：`clamp(距离/10, 1.6, 3.2)` 与 `1 - clamp(距离/100, 0, 0.9)`；音效 `TRIDENT_THROW`。

编译零错（两树）、两个 server 回归 PASS、部署三目标。
## 实测四百六十六【飞行作战·模式内两条强制限制：不自动换武器 + 自主战斗不介入】
### v1.2.0 重锤增伤按「最大上升高度」强制结算（后门，实测四百七十七）

**需求**：能不能走后门强制吃加成？增伤强制从最大上升高度算？

**结论：可以，且比改飞行 AI 更对路。** 实证：`getAttackDamageBonus` 只读 `fallDistance` 一个字段，
`canSmashAttack` = `fallDistance > 1.5 && !isFallFlying()`，风爆谓词同为 `fall_distance.min=1.5`。
`fallDistance` 是普通字段，且这两处结算都由本模组自己发起 —— 调用前后临时改写即可，
不必真的爬那么高（滑翔物理每 tick 把 fallDistance 钳回 1.0，靠飞行动作永远攒不出高值）。

**实现**：
1. `MAX_Y` 每 tick 记录本轮最高 Y，放烟花（新一轮爬升）时重置；
2. `forcedFallDistance` = clamp(最高点 − 当前高度, 1.51, 40)；
3. `hitOne` 持重锤时写入 → 结算 → **finally 必还原**（不留假值给原版摔落判定）；
4. 撤掉「必须等攒过 1.5 才出手」的门，恢复「够近就打」，顺带缓解 DPS 问题；
5. 附诊断日志（搜「重锤后门」）打印「落差X → 写入Y（真实Z）」。

**数值**：落差 8 格 = +22，10 格 = +24，15 格 = +29，30 格 = +44，40 格封顶 = +54。
仅 Neo 树（重锤为 1.21.1 独有物品）。

编译零错（两树）、两个 server 回归 PASS、部署三目标。
### v1.2.0 飞行远战「手持武器不参与开火」修根（实测四百七十六）

**反馈**：远战正常，但只会发射箭；换了武器（如御币）也没用，依旧发射弓箭。

**根因**：`performRangedAttack` 只判了武器是不是 `ProjectileWeaponItem`，
之后**无条件自己造箭**——武器本身该怎么发射，从未被调用。

**实证**：反编译 TLM 御币，`ItemHakureiGohei` 虽 extends `ProjectileWeaponItem`，
但 `shootProjectile` 是**空实现**、`getAllSupportedProjectiles` 返回恒真谓词——
它压根不走原版箭矢通道；TLM 自己用 `DanmakuShoot` 弹幕 API 开火。

**修复**：按武器分流——
枪械走 TLM 枪械通道；**御币走 `DanmakuShoot` 弹幕通道**；
其它弹射武器造箭，且弹药优先按武器自己的 `supportedProjectiles` 判据找。

编译零错（两树）、两个 server 回归 PASS、部署三目标。
### v1.2.0 支援语音包核查 + 语音触发间隔 8→5 秒（实测四百七十五）

**反馈**：女仆支援语音似乎没有语音包。

**实证结论：支援语音包一直都在。** 逐条转写核对（whisper）：
`aid_sister_help`「しまいっ頑張って今助けるからね」、
`aid_food`「ご主人様お腹空いたでしょ 食べ物持ってきたよ」、
`aid_potion_given`「ポーション、カバンに入れたよ」等 **10 条 `aid_*` 全部是正常日语人声**，
响度也已按全包统一归一（max ≈ -1.5dB，与其它 112 条同档）。
两树 + 三个已部署实例的 jar 里 10 条齐全、manifest 命中正确。

**真正原因**：被播报间隔吞掉了。内置语音最小间隔默认 8 秒，而支援/互助台词与
系统消息**共用同一道门**——战斗中系统消息频繁，支援语音轮到播放时大概率仍在冷却窗口内。

**修复**：`jarPackMinIntervalS` 默认 **8 → 5 秒**（面板「语音与 TTS → 内置语音最小间隔」可调，0~60，0 = 不限制）；
三个已部署实例的现有配置文件一并改为 5（旧配置不跟随新默认值，改前已备份）。
另更正面板说明里的条数 115 → 122。

编译零错（两树）、两个 server 回归 PASS、部署三目标。
### v1.2.0 空战未激活提示（气泡 + 系统消息）+ 新增语音（实测四百七十四）

**需求**：两种空战未进入激活状态时，给一条气泡对话 + 系统消息。

**实现**：
- `MaidFlightKit.missingParts(maid)`：按 `isModeActive` 同一口径列出缺哪几件
  （鞘翅 / 近战或远程武器（按任务分流）/ 烟花火箭），缺多件一起报。
- 行为里两处未激活分支调用 `notifyNotReady` 发气泡：
  「空战装备不齐，缺鞘翅、烟花火箭，先按普通战斗来」。
- 一个入口满足两个需求：`addTextChatBubble` 经 `ChatBubbleLimitMixin` 会同时
  同步成主人系统消息并汇入 TTS 朗读，所以气泡 / 系统消息 / 语音共用同一句文本。
- 限频：气泡 5 秒限频由 mixin 兜底，另加 15 秒冷却（`WeakHashMap` 防泄漏），
  因为「三件不齐」是持续状态，不拦会每 tick 刷屏。

**语音**：新增 `flight_not_ready.ogg`（日语，小酒狐 v2Pro 音色）。
manifest 用 `contains` 探针「空战装备不齐」，且**必须排在 `fallback_missing` 的「缺」之前**
——manifest 顺序匹配，旧的通用「缺」条会把这句吞掉。

编译零错（两树）、两个 server 回归 PASS、部署三目标。
### v1.2.0 飞行近战命中率 + 重锤下落加成/风爆 修根（实测四百七十三）

**问题**：飞行近战命中率差；重锤打不出下落增伤、附魔「风爆」几乎不触发，空中时间长导致 DPS 低。

**根因（反编译 + 附魔数据实证）**：

原版 `MaceItem.canSmashAttack` = `fallDistance > 1.5 && !isFallFlying()`；
`getAttackDamageBonus`（下落三段加成）与「风爆」附魔**都以它为门**：
`wind_burst.json` 的 requirements 明写 `is_flying=false` + `fall_distance.min=1.5`，
其中 `is_flying` 就是 `LivingEntity.isFallFlying()`（滑翔位）。
旧版在 `t>=2` 就出手，那一刻 `fallDistance` 还被滑翔钳在 ~1.0，
于是重锤 = 白板、风爆 = 永不触发。

**修复**：
1. 持重锤时等 `fallDistance` 真正攒过 1.5 再出手（收翅自由落体两三 tick 达标）；
   贴地/落地不再等、照打；门槛用严格大于 1.5 与 `ifle` 对齐。
2. 新增 `sweepWithin` 位移线段扫掠判定——俯冲 1~2 格/tick 时，
   只比当帧中心距会整段掠过目标（命中率差的主因）。
3. `smashHit` 返回 boolean：冷却未好时不收尾、下一 tick 继续压着打，
   不再"整个俯冲白打一次"。
4. 收尾分支移到命中分支之外，保证未命中时也会落地清零（防带着满坠落距离撞地）。

编译零错（两树）、两个 server 回归 PASS、部署三目标。
### v1.2.0 飞行近战回退 + 摔死根治（实测四百七十二）

**问题**：飞行近战比上一版「更容易摔死、撞死」，且鞘翅撞击免疫「没生效」。

**根因（日志实证）**：上一版为飞行远战调参（`LAUNCH_TICKS 30→20`、
`LAUNCH_CLIMB_TAN 1.88(62°)→1.0(45°)`），而这两个常量**近战远战共用**——
近战每次重新起飞都要先拉高度，仰角被压到 45° 后一放烟花就往目标压头，
几 tick 内贴地、滑翔位被原版 `updateFallFlying` 清掉 → 自由落体摔死。
整合包实机日志中飞行期死亡**全部是 `类型=fall 伤害=21~23`**（她 20 血必死），
全程零 `flyIntoWall`，与撞墙无关。

**修复**：
1. 近战/远战起飞参数拆分——近战回退 62°/30t，远战保留 45°/20t。
2. 新增 `endFlightSafely`：目标丢失/三件不齐/行为结束三处不再无条件清滑翔位，
   空中保留滑翔自然下降、落地再收翅（旧版在高空清位即自由落体）。
3. `MaidFlightWallGuard` 拦下时补日志「[promaid/飞行撞击免疫]」，使该功能可核验；
   经核实注解自动注册生效（FriendlyFireGuard 同机制且日志持续），不额外显式注册以免重复。

编译零错（两树）、两个 server 回归 PASS、部署三目标。
## 实测四百六十九【全盘修复五条：任务名全英文 / 飞太高 / 冲击伤害 / 鞘翅渲染 / 落地水雪】
## 实测四百七十【指标石：临时蓝图制作器（锁定方块 → 绑定女仆 → 即兴搭建）】
## 实测四百七十一【指标石细节：中途失败/中断一律视作结束和初始化（收回/死亡/缺料 → 烂尾收尾）】
## 实测四百七十二【修 bug：指标石建造完成后不自动清绑定与幽灵方块】

**现象**（反馈）：临时搭建**全部完成之后**，橙色幽灵方块和"绑定女仆"没有自动清掉。

**根因**（是我上一轮引入的分支顺序错误，不是漏写）：行为 tick 顶部先判
`s == null || s.cells.isEmpty()` → 走 `selfHealStale`。而 `selfHealStale` 是为"魂符把女仆
NBT 存进去、放回来时残留标记"设计的，出于安全考虑它**绝不触碰玩家会话**（因为那个会话
可能已属于另一只女仆）。于是**最后一格放完之后 cells 变空**这一刻，正好落进这条分支：
会话里的绑定/锁定框没被清、`TAG_INDEX_ACTIVE` 也没被释放 → 她永远卡在"临时搭建"任务上，
客户端也就一直显示红框与橙影。

**修法**：把**完成判定提到最前**——
"她的会话还在（`s.maidId` 就是她）**且** cells 已空" = **成功完成**，走 `finish`
（`server.execute` 延后一 tick 调 `onMaidFinished`：清 cells 幽灵格 + 解绑 + **取消锁定框** +
释放强制加载票 + 还原原任务/作息 + 播报 + 立即 `syncTo` 推空状态，客户端红框橙影当场消失）。
另外两处加固：
- 加 `finishing` 闸：延后一 tick 期间本 tick 会被重复进入，防重复排程/重复播报；
- `onMaidFinished` 改为**无论会话是否还在，都解除她的临时建造任务**（防会话先一步消失时，
  她永久卡在临时任务上——这正好是上面那个 bug 的次生后果）。

编译零错（两树）、两个 server 回归 PASS、已部署三目标。


**需求**：搭建期间女仆被收回/死亡 → 清除幽灵方块 + 解绑，视作结束和初始化；
搭建中途发现缺材料也视作失败，系统提示后清除幽灵方块 + 解绑。表现就是"搭了一半，报告，烂尾，结束"。

**做法**（两树）：
- 新增统一的失败收尾 `IndexStoneService.failSession(maid, reason, releaseMaidTask)`——
  **幂等**（多处触发只结算一次，靠 `TAG_INDEX_ACTIVE` 判重）：清橙色幽灵格（清 cells）+ 解除绑定 +
  **取消锁定框**（"初始化"语义）+ 释放该会话的 FORCED 强制加载票 + 还原女仆原任务/作息 + 系统提示
  （如实报"已搭 X / Y 块"）+ 立即 `syncTo` 推空状态，客户端红框与橙影当场消失。
- **缺材料**：行为里连续 `NO_MATERIAL_GRACE = 60`（3 秒）拿不到任何材料 → 按失败收尾
  （留宽限防"刚消费完一叠、主人背包正在补"的瞬时缺料被误判）；期间每 20 tick 冒一次气泡。
- **长时无进展**（20 秒）同样按烂尾收尾，防彻底卡死时女仆永久站桩。
- **中断检测三个入口**（都走同一幂等 failSession）：
  1. `EntityLeaveLevelEvent`——项目既有的"女仆离场"统一清理点。**用 RemovalReason 精确区分**：
     `KILLED`（死亡）/ `DISCARDED`（收回/解雇）→ 结束；`UNLOADED_TO_CHUNK` / `UNLOADED_WITH_PLAYER`
     （区块/视野卸载）与 `CHANGED_DIMENSION`（换维度）→ **不结束**（她会回来，结束=白搭一半）。
     不能用 `isRemoved()`：区块卸载时它也为真，会误判。
  2. `LivingDeathEvent`——死亡兜底（部分模组"死亡→复活"先移除再重建实体，上个事件时机不保证）。
  3. `MaidAndItemTransformEvent.ToItem`（魂符收回）——TLM 把女仆收进魂符的**唯一汇聚点**
     （手动空魂符 / 自动回魂符都走 `ItemSmartSlab.storeMaidData`）。字节码实证 `storeMaidData` 是
     "**先 `m_20240_` 存 NBT 再 post ToItem**"，所以在这个钩子里清标记已经晚了一步（标记已被存进魂符），
     此处只用来结束会话。
- **放回来时的残留标记**：魂符把女仆 NBT 一起存了进去，放回来后 `TAG_INDEX_ACTIVE` 还在而会话早已结算。
  行为顶部 `selfHealStale` 兜底：只还原原任务、**不播报"搭好了"**（那不是完成，是中断）。
- **会话归属校验**（关键防护）：`failSession` 与 `selfHealStale` 都先确认"这个会话确实属于这只女仆"
  （`s.maidId.equals(maid.uuid)`）才动会话/才播报。否则会出现"魂符放回来一个旧女仆，把主人此刻
  正在给**另一只**女仆干活的新会话清掉"。
- **完成路径**同步收紧：`onMaidFinished` 也取消锁定框 + 释放票 + 只在自己是会话所属者时才动会话
  ——完成即可立刻开下一个（契约：任务完成了才可以搞下一个）。

编译零错（两树）、`lang json: OK`、两个 server 回归 PASS（Neo 有一次 90 秒内未到 `Done`，
放宽到 240 秒后 `Done (0.871s)` PASS——是该重模组服务器启动慢，非崩溃也非本次改动的死循环）、已部署三目标。


**贴图**：物品图标换成玩家提供的「带指针的平滑石头」成品图（64×64，指针红/白、石头面带明暗过渡），
替换两树 `assets/maid_smart/textures/item/index_stone.png`；模型 `item/generated` 引用同一路径，
无需改动，物品栏与手持渲染自动生效（`item/generated` 把整张图铺在物品面上，分辨率提高只增加细节、不改变尺寸）。

## 实测四百七十一【飞行作战（近战/远战）下不触发自保】

**需求**：2 种飞行作战状态下不触发自保。

**做法**（两树 `SelfPreservationBehavior`）：在 `tick` 最顶端加一道飞行硬闸——
`MaidFlightKit.isFlightTask(maid)` 为真时，**既不进自保会话，也把已经在会话里的立刻安静中止**
（`sessionActive=false` + 清 `PRESERVE_TAG` + 清垫高/塔顶状态 + 从 `MOVING_SURVIVE` 摘除），
然后只保留与保命无关的常驻轻量逻辑（蜂蜜/牛奶解负面），直接 return。

**两个容易踩的点（都已在实现里避开）**：
1. **不能把豁免放在 `canUse`（`checkExtraStartConditions`）里**——那会让行为在飞行任务下
   根本不启动（`setTask` → `refreshBrain` 后新实例永不 tick），而"清残留 `PRESERVE_TAG`"
   的代码就藏在 tick 里，结果是从飞行切回来时标记残留、排班/参战/搭方块对这只女仆永久让位。
   豁免统一放 tick（行为常驻、每 tick 都能清理）。
2. **清标记必须无条件**（判 `sessionActive || PRESERVE_TAG`，而不是只看 `sessionActive`）——
   区块卸载/崩溃/魂符收放会把 `PRESERVE_TAG=true` 写进存档而内存态归零；若只看内存态，
   那种残留正好落在飞行任务上时，下方"会话外自愈"分支被本闸提前 return 挡掉，标记再也清不掉。

**顺带**：闸放在岩浆感知扫描之前——空中不需要岩浆避让，省掉每 10 tick 一次的全量方块扫描。

**口径说明**：只拦"自保接管"，**不放宽真实伤害**——飞行中照样掉血、照样可能被击落，
只是不再由自保系统把她从空中拽下来（自保的传送/垫高/放水/逃跑会打断滑翔与烟花推进）。
与既有约定一致：`AutoCombatSwitch` 里飞行任务本就"自主战斗不介入"。

编译零错（两树）、两个 server 回归 PASS、已部署三目标。


**玩法**（需求逐条落地）：
- **物品**：`maid_smart:index_stone`，9 块平滑石头 3×3 合成，创造栏「建筑方块」+「工具与实用品」两页都有。
- **锁定**：手持时视线指向的方块渲染成**绿色**（跟随指针实时移动）；右键 → **红色**锁定不再移动；
  再右键 → 取消。**可锁非常远的方块**（客户端 512 格长射线），**不可锁空气**。
- **绑定**：手持指标石右击**自己的女仆** → 绑定（系统消息提示"绑定成功"）；再右击同一只 = 解绑；
  右击另一只 = 先解绑旧的再绑新的。**必须先锁方块再绑女仆**，先绑女仆会提示"要先用它锁定一个方块"。
- **搭建**：两件事齐备 → 从**女仆当时所在方块**到**锁定方块**之间所有空气方块渲染成**橙色幽灵方块**
  （与建造投影同款：线框 + 半透明填充），女仆立刻进入一次性临时建造，**从自己所在格逐步向锁定格填充**。
- **材料无限制**：先从女仆背包取（含主副手），不够从**主人背包**拉一组；选材复用搭路规则
  （`MaidBuildBlockFilter.takeBuildBlock`：数量最多者优先、必须有碰撞、排除下落方块/TNT/危险方块）。
  创造模式主人的材料视为无限。
- **表现一致**：站桩 + 瞬移到工地旁 + 挥臂 + 放置音效，与建造任务完全相同（复用
  `MaidBuildBehavior.teleportToWorkSite` / `PlacedBlockTracker.placeSound`）。
- **单次约束**：一个玩家同时只能有一个"锁定 + 绑定"，任务完成前不能开下一个（未完成时换锁定点/取消会被拒绝）。
- **收尾**：完成 → 渲染框/幽灵消失、女仆**还原原任务与作息**、系统消息播报格数；取消/解绑/玩家下线同样还原。

**实现**（两树同构，新增 7 个文件 + 改动注册/配置/面板/混合注入/资源）：
- `IndexStonePlan`：3D 体素连线（Amanatides-Woo）取"线接触到的所有空气格"，服务端开建与客户端幽灵
  **共用同一几何**（保证"看到什么就填什么"逐格一致），按离起点距离排序 → 天然"由近及远"填充。
- `IndexStoneService`：玩家会话（内存态 `ConcurrentHashMap` + 女仆 `persistentData` 双写，与 `BuildPlan` 同约定）；
  隐藏任务 `maid_smart:index_build` 的强制指派与还原（照搬主动战斗的 `ScheduleSwitchGuard.runInternal` +
  快照/还原原任务/原 home/原作息）；**会话级 FORCED 票据**给起点→锁定点之间区块强制加载（超上限则不挂，
  完成/取消/下线/停服全部释放，防票残留锁区块）。
- `IndexStoneItem` + `IndexStoneInteractHandler`（右键女仆走 `PlayerInteractEvent.EntityInteract`，命名空间
  比对判定物品，同 `BrewManualInteractHandler` 模式）；`IndexStoneNetworking`（C2S 锁定请求 + S2C 会话状态）；
  `IndexStonePreviewClient`（渲染 + `InteractionKeyMappingTriggered` 长射线锁定）。
- 配置 `BUILD_INDEX_STONE`（默认开，面板「建造」区行名"指标石"）；lang 四份补物品名/配置名/隐藏任务名。

**审查中拦下的四个坑（都已修）**：
1. **右键女仆会被吞**——客户端在"已锁定"时取消了 use 键事件，而原版 `handleKeybinds` 被 cancel 后整段
   跳过 `startUseItem` → 绑定永远收不到包。改为：视线命中实体（`mc.hitResult` 为 `EntityHitResult`）时
   一律不拦截（与 `startUseItem` 判 ENTITY 分支用同一个 hitResult）。
2. **在行为 tick 里 setTask = 未定义行为**——`EntityMaid.setTask` 会 `refreshBrain` 重建整个 Brain；
   收尾改为 `server.execute(...)` 延到本 tick 之后执行（项目既有约定）。
3. **未加载区块的格被永久丢弃**——原写法直接 remove，蓝图留洞；改为退避等强制加载就绪。
4. **强制加载票泄漏**——解绑/取消/下线/停服路径原本没释放，补 `releaseTickets` 全路径覆盖。

编译零错（两树）、`lang json: OK`、新类与新资源均已入包、两个 server 回归 PASS、已部署三目标。


**① 任务栏 mod 任务全部变英文（根因是 JSON 语法错误，不是翻译缺失）**
`promaid_src_neo/assets/maid_smart/lang/{zh_cn,en_us}.json` 末尾多了一个**尾逗号**，导致整份语言文件
被 `ClientLanguage` 跳过（日志实证：`Skipped language file: maid_smart:lang/zh_cn.json`）。TLM 的任务名
走 `task.<命名空间>.<路径>` 翻译键（`IMaidTask.getName()` 默认实现），键查不到就原样显示键名/英文。
删掉那个逗号即恢复；已用 `json.load` 复验四份 lang 全合法。**1.20.1 树本来就合法，所以只有 1.21.1 中招。**

**② 飞行远战飞太高（够不到地面敌人）**
- 起飞仰角 `LAUNCH_CLIMB_TAN` 1.88（≈62°）→ **1.0（45°）**；
- 爬升窗口 `LAUNCH_TICKS` 30 → **20**（提前转入朝敌飞行/盘旋）；
- 补烟花抬头角 55° → **45°**、抬头窗口 20 → **10 tick**（只借前半段升一点就回盘旋）；
- 期望盘旋高度 `RANGED_HOLD_HEIGHT` 6 → **3.5 格**（在目标斜上方盘旋，保持锁敌与开火视线）。
- **远战图标由弓改为鞘翅**，与飞行近战统一。

**③ 免疫鞘翅撞击伤害（后门，可配置）**
新增 `MaidFlightWallGuard`（两树）+ 配置 `COMBAT_FLIGHT_NO_WALL_DAMAGE`（默认开），
挂在**生存与复活 → 落地缓冲**分区，行名"免疫鞘翅撞击伤害"。判定用
`DamageSource.getMsgId().equals("flyIntoWall")`——该字符串跨版本稳定（两版
`fly_into_wall.json` 的 `message_id` 都是 `flyIntoWall`），比绑定 SRG 常量名更耐版本变化。
只作用于飞行任务；NeoForge 用 `LivingIncomingDamageEvent`，1.20.1 Forge 用 `LivingAttackEvent`。

**④ 鞘翅渲染（用的就是玩家的模型，问题在"挂到哪、缩多少"）**
之前几版失败同源：**硬编 `translate(0,-1.501,0)` 且没有抵消模型的 `render_entity_scale`**。
实测数据（`winefox.json`，0.65，模型高 44.4px ≈ 1.80 格 ≈ 玩家 1.875）：
- 鞘翅被 0.65 缩 → 只有玩家的 **69%**（"太小"）；
- 硬编挂点落在腰/下背，而**长发几何正好在那儿**（LongHair z 0.34~0.64 格、LongHair2 z 0.38~0.45），
  鞘翅外表面 z≈0.25 格直接埋在头发里 → 深度测试判失败（"被头发覆盖"）；
- 只在 `isGliding` 时强制展翅，标志位同步间隙会露出折叠态（"不张开"）。
修法：①锚定**模型自带的 `Elytra` 骨骼**（pivot [0,30.9644,3] → 世界 y≈1.258、z≈+0.122 格，
恰好在上背且在头发之前；16/27 个酒狐模型都有），用 `RenderUtils.prepMatrixForLocator` 沿祖先链
锚定；②`scale(-1,-1,1)` 转朝向；③`scale(0.9375/0.65)` 抵消模型缩放并对齐玩家 → **与玩家等大**；
④展翅条件放宽为"飞行任务且离地"。骨骼缺失的模型退回旧的 1.501 路径，至少不会不显示。
Bedrock 分支（`LayerMaidElytra`）同样补了 `0.9375/renderEntityScale` 的缩放抵消。

**⑤ 两个模式下落地水/雪恢复照常**
`WaterClutchBehavior` 撤掉"飞行任务直接 return false"的屏蔽（当年为"水雪浇在女仆与怪物之间
干扰作战"而加）。现在收翅猛击命中时已把 `fallDistance` 清零，落地水/雪只在真坠落时触发，
正常作战不会误放。

**验证**：两树编译零错（1.20.1 首次因 Forge 无 `LivingIncomingDamageEvent` 报错，改用
`LivingAttackEvent` 后通过）；四份 lang `json.load` 校验合法；两个 jar 重建部署三目标；
`test_server.py 1201` 与 `neoforge1211` 回归 PASS。

## 实测四百六十八【飞行远战·兼容枪械（TACZ / 卓越前线）】

**需求（用户指定）**：把此前"不兼容 TACZ 之类枪械"的结论落地——让枪械能进飞行模式。
按说好的，接在"武器位判据 + 开火路径"两处。

**判据（武器位）** —— `MaidFlightKit.isRangedWeapon` 从 `ProjectileWeaponItem` 放开为
`|| GunCompat.isGun(stack)`。因为 `isWeaponForTask` / `hasWeapon` / `isModeActive` /
`MaidFlightRangedTask.isWeapon` 全都复用这一个判据，所以一处放开即全线生效：
**飞行远战**认枪械（鞘翅 + 枪 + 烟花三件齐备即激活），**飞行近战**仍只认近战武器
（枪械挥砍没有意义）。`equip` 的远战分支也会从背包把枪掏到主手（主手已有远程武器时不换）。

**开火路径** —— `MaidFlightCombatBehavior` 新增 `fireRanged` / `tickGunFire`：
- 主手是枪 → 走 TLM 自己的枪械通道，照搬 `GunShootTargetTask.tick` 的开火段（字节码实证）：
  `GunCommonUtil.tick` 每 tick 换弹/上膛 → 冷却结束且 `GunCommonUtil.canSee` 为真时
  `performGunAttack` 开火（返回值即下一发冷却）→ 再按女仆的 `MAID_GUN_ATTACK_SPEED` 缩放。
  射程改用 `GunCompat.gunMaxRange()`（TLM 枪械中距离配置），不是弓弩那 24 格。
- 主手是弓弩 → 维持原来的 `performRangedAttack` 箭矢通道，射程 24 格。
- `MaidFlightRangedTask.performRangedAttack` 对枪械直接返回，两条通道不会互相抢。

**一个容易踩的坑（已处理）**：`TacInnerCompat.performGunAttack` 会**自己按 dx/dy/dz 算
yaw/pitch、再把身体拧向目标**（不是按女仆当前视线开枪）。而滑翔的前进方向就是视线方向——
所以**开火必须放在"朝向"之前**，开完火立刻把盘旋朝向盖回去，否则滑翔会顺着那次瞄准
把女仆直接拉向目标，"盘旋"就散了。

**退出模式收枪**：`stop()` 里对持枪女仆调 `GunCommonUtil.onStop`（TLM 的
`GunShootTargetTask.stop` 做的就是这件事），复位 TACZ 的 ADS 瞄准 / 卓越前线的手雷冷却——
否则离开模式后枪会一直保持瞄准姿势。本模式没有那个 task，用一个一次性
`GunShootTargetTask` 实例顶上（已确认它**没有静态初始化块**，未装枪械 mod 时引用也不会炸）。

**兼容范围**：TACZ（`tacz:modern_kinetic_gun`）与卓越前线（superbwarfare 各枪物品）——
判据复用本模组既有的 `GunCompat.isGun`（TLM 兼容层 + 注册名兜底），未装枪械 mod 时安全返回 false。
弹药 / 换弹由 TLM 负责（它会自动从女仆背包找同型弹药）。

**验证**：两树编译零错；两个 jar 重建部署三目标；`test_server.py 1201` 与 `neoforge1211` 回归 PASS。

## 实测四百六十七【飞行远战·盘旋高度保持（不再慢慢掉高度 + 不再无条件烧烟花）】

**问题（用户反馈）**：飞行远战在空中盘旋时高度会慢慢降低（就像玩家滑翔那样）；
而且每 5 秒无条件补一枚烟花，既费烟花又顶不住高度。

**原因**：盘旋走的是原版鞘翅滑翔。滑翔**转弯时也会持续掉高度**（不只是平飞下沉），
原来一个写死的 `ORBIT_PITCH = -6°` 抬头量顶不住——这正好解释了"每 5 秒补一枚还是往下掉"。

**改动（两树同步，`MaidFlightCombatBehavior`）**
1. **盘旋俯仰改成"按高度偏差的反馈控制"（这就是用户要的"再加一个速度"）**：
   期望盘旋高度 = 目标上方 6 格；高度偏差按 5°/格 换算成俯仰，并留 1 格向上偏置
   （抵消滑翔的固定下沉，平衡点 ≈ 原来的 -6°）。低于期望高度就抬头——把前进速度换成升力爬回去；
   高于期望高度就低头——把高度换成速度。限幅 +45°/−35°，避免抬头过头失速、俯冲过猛。
2. **烟花从"定时"改成"按需"**：只有真的掉出高度带（低于期望高度 3 格）才补一口，
   5 秒间隔仍是最小间隔下限。高度稳的时候不再白白烧烟花，续航更长。
   补的时候仍是"抬头 55° 放"，把推力全用在爬升上，并写一条"掉高补烟花"日志。
3. 其余不变：开火仍走 TLM `performRangedAttack` 通道（24 格内、每 2 秒一发）；
   烟花耗尽 / 鞘翅损坏 → 三件不齐 → `isModeActive`（含"有烟花"）为假 → 模式自然未激活 → 退回地面。

**关于"能不能像幻翼一样盘旋"**：能绕圈——朝向是"目标周围圆的切向 + 半径修正"，
所以轨迹是绕目标的一个圆；但飞行模型是鞘翅滑翔而非幻翼的悬停 AI，所以半径随速度变化、
没有真正的原地悬停。加上高度保持后，表现是"稳定在一个高度带上绕圈"。

**可调参数**（都在 `MaidFlightCombatBehavior` 的飞行远战段）：
`RANGED_HOLD_HEIGHT=6.0`（期望高度，格）、`RANGED_HOLD_GAIN=5.0`（度/格，越大纠偏越猛）、
`RANGED_HOLD_BIAS=1.0`（抵消下沉的向上偏置）、`RANGED_BOOST_DROP=3.0`（掉多少格才补烟花）、
`ORBIT_RADIUS=10.0`（盘旋半径）、`RANGED_BOOST_INTERVAL=100`（补烟花间隔下限，tick）。

**验证**：两树编译零错；两个 jar 重建部署三目标；`test_server.py 1201` 与 `neoforge1211` 回归 PASS。


**需求（用户指定）**：飞行作战（近战 / 远战）模式下强制两条限制——
①女仆**不会自主切换武器**；②**自主战斗不会在这个模式下被触发**。

**改动（两树同步）**
1. **不自动换武器**：`MaidToolAutoEquipBehavior` 的 `checkExtraStartConditions` 对飞行任务直接
   返回 false —— 于是 `ensureForTask`（主手按任务择优换装）与 `ensureShieldForCombat`
   （副手换盾）都不再执行。模式内装备只由 `MaidFlightKit.equip` 负责"补齐三件套"
   （鞘翅 / 武器 / 烟花），且**主手已有合格武器时绝不替换**——玩家指定什么就是什么。
2. **自主战斗不介入**：`AutoCombatSwitch.tryEngageMaid` 对飞行任务直接返回 2
   （= "已是战斗任务，跳过"）。叠加此前已有的三处 UID 排除（入战选任务不进池 /
   战中不换战术 / 还原链视为玩家接管），自主战斗从"参战评估 → 选任务 → 换战术 → 还原"
   全链路都不会碰飞行作战的女仆。
   - 注：该限制按**任务**生效——即使这一轮因为没烟花/鞘翅坏了而"模式未激活"（退回普通攻击表现），
     只要任务还是飞行作战，就不会自动换武器、也不会被她人/威胁事件切走。这是"这个模式下"的字面实现。
3. **顺带回答"兼容 mod 武器吗"**（判据见 `MaidToolAutoEquip.isMeleeWeapon` / `MaidFlightKit.isRangedWeapon`）：
   - **飞行近战**：与 TLM"攻击模式"同判据——主手槽的默认属性修饰符里含 `ATTACK_DAMAGE` 即算武器
     （镐 / 弓 / 弩 / 御币排除）。所以绝大多数模组近战武器（剑 / 斧 / 长枪 / 锤 / 镰…只要走属性系统）
     **天然兼容**，且能被"补齐三件套"从背包自动装上；
   - **飞行远战**：与 TLM 弓 / 弩任务同判据——`ProjectileWeaponItem`（原版弓弩、御币、多数模组弓弩）
     **天然兼容**；
   - **不兼容**：TACZ 等**枪械**不属于 `ProjectileWeaponItem`，也不带 `ATTACK_DAMAGE` 修饰符，
     所以进不了这两个模式（本模组已有 `GunCompat.isGun/hasGunAndAmmo`，要接的话另开一轮）。

**验证**：两树编译零错；两个 jar 重建部署三目标；`test_server.py 1201` 与 `neoforge1211` 均 PASS。

---

## 实测四百六十五【回退"落地贴身就地近战" + 强制受击绑定攻击频率】

**反馈**：担心"落地贴身就地近战"会让女仆只顾地面近战、不再起飞；既然已经有"后门强制触发攻击事件"，
这个改动就不需要了，回退。同时强调：**强制受击的间隔要取决于女仆的攻击频率，不是无条件触发**。

**改动（两树同步）**
1. **回退**：删掉 `GROUND_FIGHT_RANGE` 常量与"落地时目标仍在 5 格内 → 就地近战收尾"的分支，
   恢复原逻辑——落地只要在 `LAUNCH_RANGE(20)` 格内且能放烟花就立刻起跳重新进入飞行链路，
   够不到才退回地面近战。飞行近战的循环因此保持"一直在飞"。
2. **强制受击绑定攻击频率**：`smashHit`（含主目标与范围补命中）新增攻击冷却闸 `ATTACK_READY`——
   冷却没好就不出手；出手后按**女仆自己的攻击间隔** `20 / ATTACK_SPEED`（与 TLM `MaidMeleeAttack`
   同口径）记冷却。所以那一记范围强制命中是"每次能攻击时最多一次"，不是无条件每 tick 触发。
   （它本来一轮猛击只调用一次，这道闸是显式保证 + 防未来重复触发。）

**保留**：命中率的另两项仍然生效——攻击判定距离 4.0 格 / 收翅后 2 tick 即可出手；以及被无敌帧
吃掉时清零 `invulnerableTime` 再打一次（真正的"后门"）。飞行远战不受影响。

**验证**：两树编译零错；两个 jar 重建部署三目标；两个 server 回归 PASS。

---

## 实测四百六十四【飞行作战拆分为「飞行近战 / 飞行远战」】

**需求**：手持远程武器时近战链路不适用——起飞不变，其后改为持续在天上盘旋（类似幻翼）、
每 5 秒补一次烟花，攻击改为用手持远程武器在锁敌范围内开火；击败敌人后同样降落。
于是把原任务改名"飞行近战"，另加一个"飞行远战"。

**改动（两树同步）**
1. **改名**：原 `maid_smart:flight_combat` 显示名 → **飞行近战**（**UID 保持不变**，老存档的任务
   指定不受影响）；`getMaidActionSummary` 同步。
2. **新增** `maid_smart:flight_ranged` = **飞行远战**（图标 = 弓，不响应自主切换）。
   - 三件套 = 鞘翅 + **任意远程武器**（`ProjectileWeaponItem`：弓/弩/御币/模组弹射武器）+ 烟花火箭；
   - 起飞方式与近战完全相同（起跳滑翔 + 放烟花给"背离敌人 + 向上"的初速）；
   - 起飞后**不再扑击**，改为**持续盘旋**：绕目标约 10 格画圈（切向视线 + 半径自动拉回），
     每 **5 秒（100 tick）补一枚烟花**——补的那一下先开 20 tick 的"抬头 55° 窗口"，
     让挂载烟花的推力全部用在爬升上、抵消滑翔下沉；
   - **开火**：目标在 24 格内 → 走 TLM 自己的通道 `EntityMaid.performRangedAttack`（1.20.1 `m_6504_`）；
     本任务实现 `IRangedAttackTask`，在 `performRangedAttack` 里自己找箭（副手优先、其次女仆背包——
     原版 `getProjectile` 找不到女仆背包）、造箭、朝目标射出、耗 1 箭 + 耗武器耐久 + 射击音效；
     **刻意不注册 TLM 的 `MaidShootTargetTask`**：它每 tick 会把视线拽向目标，与盘旋转向打架；
   - 击败敌人后行为自然结束 → 清滑翔落地（与近战一致）；落地后若还有目标就重新起飞恢复盘旋。
3. `MaidFlightKit.isFlightTask` 现在覆盖**两个** UID（`isFlightUid`），所以已有的接入点
   （鞘翅渲染 / 滑翔禁传送 / 飞行作战期间不收落地水雪 / 自主切换三处排除）**自动对两个模式同时生效**；
   武器位判据按任务分流：近战认近战武器、远战认远程武器（`isWeaponForTask`）。

**验证**：两树编译零错（1.21.1 / 1.20.1 各 218 源文件）；两个 jar 重建部署三目标；
`test_server.py 1201` 与 `test_server.py neoforge1211` 均 PASS。

---

## 实测四百六十三【飞行作战·命中率与展翅修复（落地打不到怪 / 落地水雪干扰 / 空中翅膀不张开）】

**反馈**：链路能正常触发了，但①命中率堪忧，女仆落地以后基本上打不到怪；②落地水/雪可能干扰作战；
③鞘翅在空中没有张开、常态被头发遮住（建模 Wine fox）。建议"走点后门"在攻击时强制命中。

**改动（两树同步）**
1. **命中率——三道"后门"**（用户要求）：
   - 攻击判定距离 2.5 → **4.0 格**，且收翅后 **t≥2 tick 即可出手**（收翅那一刻 `fallDistance`
     已被滑翔钳在 ~1.0，暴击条件 `fallDistance>0` 已成立，久等只会让她擦身而过）；
   - **范围强制命中**：以女仆为中心 2.5 格内的**其他合法敌对目标**也结算一次同样的攻击。
     只认 `IAttackTask.canAttack` 判据 → 不会误伤主人/宠物/中立动物；
     **女仆一律跳过 = 女仆免疫这一记重锤友伤**（用户要求）；
   - **无敌帧绕过**：这一记若被目标的无敌帧吃掉（刚被其他来源打过），清零 `invulnerableTime`
     再打一次——保证"触发了链路攻击就一定打到"。
2. **落地后贴身就地近战**：落地时目标还在 5 格内 → 直接地面近战收尾，**不再立刻起飞**。
   旧逻辑落地只要在 20 格内就起飞，她永远在飞、从不在地面输出，这正是"落地以后打不到怪"的主因。
3. **飞行作战期间不收落地水/雪**：`WaterClutchBehavior` 对该任务直接跳过——收翅下落是作战动作
   （命中/落地我们自己清 `fallDistance`），放水放雪会浇在她与怪物之间、干扰作战（用户反馈）。
4. **滑翔中强制完全展翅**：原版 `ElytraModel.setupAnim` 是**按下落速度决定展开程度**的
   （bytecode 实证：`f = 1 - pow(-vy_normalized, 1.5)`，在折叠态 xRot 0.2618 / zRot -0.2618 与
   完全展开态 xRot 0.34906584 / zRot -1.5707964 之间插值）；女仆扑向目标时一直在下降 → 看着"没张开"。
   新增 `ElytraModelAccessor`（@Accessor 读左右翼）+ `ElytraSpread`：**滑翔中把角度钉到 f=1 的
   完全展开值**（与玩家平飞展翅一致），收翅/落地不干预（俯冲收翅膀保持原版）。
   两树字段名不同：1.21.1 `leftWing/rightWing`，1.20.1 SRG `f_102533_/f_102532_`、
   ModelPart `f_104203_/f_104204_/f_104205_`（xRot/yRot/zRot）。

**验证**：两树编译零错（1.21.1 / 1.20.1 各 217 源文件）；两个 jar 重建部署三目标；
`test_server.py 1201` 与 `test_server.py neoforge1211` 均 PASS。

---

## 实测四百六十二【1.20.1 同步移植飞行作战（武器无关 + 暴击版）】

**需求**：1.20.1 也做这个功能；因为 1.20.1 原版没有重锤，武器位定为"任意近战武器"。

**实现（与 1.21.1 同一套机制，SRG 化；猛击段改为原版暴击）**
- 新增 8 个类：`MaidFlightKit`、`MaidFlightCombatTask`、`MaidFlightCombatBehavior`、
  `LayerMaidElytra`（Bedrock）、`LayerMaidElytraGecko`（GeckoLib）、`EntityFlagInvoker`、
  `MaidSwimGlideMixin`、`LivingEntityRendererAddLayerInvoker`、`MaidElytraLayerMixin`、
  `MaidElytraLayerGeckoMixin`；接入 `mixins.promaid.json`、`addMaidTask`、
  `MaidSmartConfig.COMBAT_FLIGHT_MODE`、配置面板、lang、`AutoCombatSwitch`（入池/换战术/还原三处排除）、
  `MaidTeleportPreserveMixin`（滑翔途中禁传送）。
- 三件套 = 鞘翅 + **任意近战武器** + 烟花火箭（判据复用 `MaidToolAutoEquip.isMeleeWeapon`，
  已放宽为 public；镐/弓/弩/御币排除，与自动装备口径一致）。
- 猛击段改为**原版暴击**：伤害 = 攻击力 × 1.5 + 附魔加成（复刻本模组
  `MaidCombatTacticsBehavior.swingCritHit` 同款算法）+ 击退/火焰附加/荆棘/暴击粒子音效。
  依据：滑翔中 `LivingEntity.travel` 每 tick 调 `Entity.m_245125_()` 把 `fallDistance` 钳在 ≤1.0，
  收翅后再自由落体 2~3 tick 即满足暴击的 `fallDistance > 0`（比重锤的 1.5 格门槛还低）。
- 1.20.1 特有的技术点（javap/反编译实证，都踩过一遍）：
  · `RenderLayer` 的抽象方法在 SRG 里是 `m_6494_(…, Mob, …)`（参数是 T 实例化后的 Mob，不是 Entity）；
  · `Behavior` 的四个钩子是 `m_6114_(checkExtraStartConditions)` / `m_6737_(canStillUse)` /
    `m_6725_(tick)` / `m_6732_(stop)`；
  · 烟花是 NBT 结构：只写 `Fireworks.Flight`、**不写 `Explosions`** → 挂载烟花到期不炸自己（同 1.21.1 的安全处理）；
  · `EntityAnchorArgument` 在 `net.minecraft.commands.arguments` 包（不是 world.entity）；
  · `ItemStack.isEmpty()/EMPTY` = `m_41619_()/f_41583_`；
  · 图层注册走客户端 mixin（不用 `ILittleMaid.addAdditionMaidLayer`，避免专用服务器 RuntimeDistCleaner 崩溃）。
- 鞘翅图层同样做**双路径 + 空间换算**（Gecko 图层需补 `scale(-1,-1,1)` + `translate(0,-1.501,0)`），
  与 1.21.1 修复后的做法一致；`isVisuallySwimming` 用 mixin 强制按"是否滑翔"返回，套用游泳/展翅动画。

**验证**：编译零错（215 源文件）；`test_server.py 1201` PASS、`test_server.py neoforge1211` PASS；
jar 重建部署 neo 客户端 / 1.20.1 客户端 / pack1201（`match=True`）。

---

## 实测四百六十一【1.21.1 飞行作战·武器位放开为"任意近战武器" + 非重锤结算暴击】

**需求**：飞行作战除了重锤，对别的武器有没有意义；1.21.1 也放开只认重锤的限制。

**实证**：原版暴击（`Player.attack`）的条件是 `fallDistance > 0 && !onGround && !onClimbable && !isInWater
&& !hasEffect(BLINDNESS) && !isPassenger && !isSprinting` → 伤害 ×1.5。它只看"在下落、脚不着地"，
**比重锤的 1.5 格门槛还低**——所以"取消滑翔再命中"这一步对所有近战武器都成立。

**改动**
- `MaidFlightKit.hasMace` → `hasWeapon`（任意近战武器），判据新增 `isMeleeWeapon` 委托
  `MaidToolAutoEquip.isMeleeWeapon`（该类同名方法放宽为 public）；`equip` 主手位若已有近战武器则**不换**，
  空手时从背包取（重锤优先）。
- `MaidFlightCombatTask.isWeapon` 改为接受任意近战武器（仍保证 AutoCombatSwitch 不会把她切走）。
- `smashHit` 分流：手持**重锤** → 原重锤路径（下落加成 + 猛击粒子/音效）；其他**近战武器** →
  附魔 + **暴击 ×1.5** + 暴击粒子/音效。两者都带击退、武器耐久，收尾 `resetFallDistance`。
- 配置面板/lang 文案同步标明"鞘翅 + 任意近战武器 + 烟花"。

**仍不适用的武器**：远程（弓/弩）不吃这一套——它需要的是"滑翔中射击"的另一套逻辑，本轮不做。

**验证**：编译零错；两个 server 回归 PASS；jar 重建部署三目标。

---

## 实测四百六十【飞行作战·链路补全：起跳滑翔 + 取消滑翔→重锤猛击→切回滑翔】

**需求**：在链路中间插入"取消滑翔"，让重锤吃到下落加成；攻击完之后再切回滑翔。
另外因为烟花推力要求正在滑翔，链路开头必须先"起跳滑翔"。

**实现（补齐后的完整链路）**
遇到敌人 →【起跳滑翔】→ 放烟花（初速 = 敌人反方向 + 向上 62°）→ 1.5 秒后转入飞行、朝敌人飞
→ 进入 3.5 格 →【取消滑翔】收翅自由落体 → 触底前一次**重锤猛击**（含下落加成）→【切回滑翔】
→ 再放烟花 → 如此反复，直至怪物死亡或模式退出。

- **起跳滑翔（新增第 0 步）**：`LivingEntity.updateFallFlying`（aiStep 内、travel 之前）每 tick 用
  "已置位 && !onGround && 胸甲可用鞘翅"重算第 7 位——站在地上置位必被清掉。所以先垫一个 0.42
  上跳离地，下一 tick 再放烟花，推力从第一 tick 起就生效。
- **取消滑翔能拿到猛击加成的依据（实证）**：`MaceItem.getAttackDamageBonus` / `canSmashAttack`
  都要求 `!isFallFlying()`；而滑翔中 `LivingEntity.travel` 每 tick 调 `Entity.m_245125_()`，
  把 `fallDistance` **钳在最多 1.0**。收翅后再自由落体 2~3 tick 即过 1.5 门槛
  （f≤3 时每格 4 点加成），所以"3.5 格取消滑翔"完全来得及。
- **猛击段**：保留大部分滑翔动量（`dm*0.95 + 目标方向*0.02` 轻导向，不做强刹车，避免像上一版那样
  "收翅后砸空"）；距目标 ≤2.5 格且（下落 ≥1.5 格 / 已拖 6 tick / 快贴地）→ `smashHit`
  （附魔 + 下落加成 + 击退 + 猛击粒子/音效 + 武器耐久），命中即 `resetFallDistance()`；
  打空落地或超时也 `resetFallDistance()` 兜底，不会自我摔伤。
- **收尾**：猛击后清状态 → 若在空中则**切回滑翔** → 下一 tick 再次放烟花（`WAIT_LAUNCH`）形成循环；
  若已落地则回到"起跳滑翔"再放烟花。

**验证**：编译零错；`test_server.py neoforge1211` PASS、`test_server.py 1201` PASS；
jar 重建并部署 neo 客户端 / 1.20.1 客户端 / pack1201（`match=True`）。仅 1.21.1。

---

## 实测四百五十九【飞行作战·两阶段循环重构（放烟花给初速 → 1.5s 后朝敌飞 → 接触攻击 → 再放烟花）】

**需求（用户指定链路）**：遇到敌人 → 放烟花给予初速度（方向 = **敌人反方向 + 向上，更偏向往上飞**）
→ 放烟花后 1.5 秒进入飞行、**保留获得的速度**朝敌人飞 → 即将接触时触发**一次**重锤攻击
→ **再次放烟花** → 如此反复，直到怪物死亡或模式退出。烟花用完 / 鞘翅损坏 → 模式自然变为未激活。
使用烟花要有**最小间隔**。

**实现（两阶段状态机）**
- **阶段一（LAUNCH，30 tick = 1.5s）**：放烟花后维持"背离敌人的水平朝向 + 向上 62°"的视线，**并保持滑翔**。
  关键前提（反编译实证）：挂载型烟花**只对 `isFallFlying()` 的实体给推力**——`FireworkRocketEntity.tick`
  里只有 `if (attachedToEntity.isFallFlying())` 才按 `getLookAngle()` 加速，否则烟花只是跟着她飘、零推力。
  所以"先给初速"必须与滑翔同时成立；滑翔物理本身也按 `getLookAngle()` 加速，两个推力自然同向（背离 + 向上）。
- **阶段二（APPROACH）**：把视线转回目标，靠滑翔 + 已有速度扑向敌人；进入 3.5 格触发一次重锤攻击
  （每轮一次，`swing` + `doHurtTarget`，与普通攻击模式同款、附魔照常结算），随后**立即再次放烟花**回到阶段一，
  形成"打一下 → 被抛出去 → 再扑上来"的循环。
- **地面是新一轮的起点**：距离 ≤ 20 格才起飞（避免"越炸越远"）；起飞时垫一个 0.42 的上跳离地——
  `LivingEntity.updateFallFlying`（aiStep 内、travel 之前）每 tick 用"已置位 && !onGround && 胸甲可用鞘翅"
  重算第 7 位，**站在地上时一定会被清掉**；原版玩家与参考实现（`JerotesPlayerEntity.aiStep`
  在主手是烟花且落地时调 `playerJump`）同样先跳再放烟花。
- **烟花最小间隔 30 tick**（= 阶段一长度）。不再需要人工的助推上限/滞空超时：烟花数量与鞘翅耐久
  （滑翔每 20 tick 掉 1 点，`updateFallFlying` 内实证）本身就是终止条件，耗尽即
  `isModeActive=false` → 三件不齐 → 退回普通攻击模式（地面近战）。
- **移除**上一版自创的"收翅俯冲猛击"分支及其静态状态（`DIVING`/`DIVE_TICKS`/强制落地缓冲调用）。
  代价如实说明：本循环在滑翔中接触攻击，因此吃不到重锤的下落加成——
  `MaceItem.getAttackDamageBonus` 与 `canSmashAttack` **都要求 `!isFallFlying()`**，滑翔中恒为 0；
  附魔（锋利/火焰附加/密度…）照常结算。若日后要重锤猛击加成，需另加"接触前瞬时收翅"的一小段。

**验证**：编译零错；`test_server.py neoforge1211` PASS、`test_server.py 1201` PASS；
jar 重建并部署 neo 客户端 / 1.20.1 客户端 / pack1201（`match=True`）。仅 1.21.1。

---

## 实测四百五十八【飞行作战·鞘翅渲染修复（实机反馈：鞘翅倒挂 / 挂在脑袋上 / 很小 / 飞行时不张开）】

**反馈现象**：鞘翅这次有渲染了，但①倒挂；②贴在女仆脑袋上；③很小；④滑翔时也不张开。

**根因（空间换算没做 + 挂点选错）**
1. **Gecko 图层空间 ≠ 原版模型空间**。反编译实证两条渲染链：
   - 原版 `LivingEntityRenderer.m_7392_`：`setupRotations` → `scale(-1,-1,1)` → `scale(entity)`
     → `translate(0,-1.501,0)` → 之后才跑图层；
   - TLM `GeoReplacedEntityRenderer.render`：`setupRotations` → `translate(0,0.01,0)` → `renderEarly` 的
     `scale(w,h,w)` → 图层；而 `IGeoRenderer.renderRecursively` / `RenderUtils.translateToPivotPoint`
     是 `+pivotY/16`，**全程不倒 Y**。
   即 Gecko 图层空间 = "脚下原点、Y 朝上、X 不镜像"，原版模型空间额外多了
   `scale(-1,-1,1)` + `translate(0,-1.501,0)`。上一版没做这步换算 → 原版 `ElytraModel` 的 Y 没翻 = **倒挂**。
2. **挂点用错**。上一版复用 `GeckoLayerMaidBackpack` 的
   `prepMatrixForLocator(backpackBones)` + `translate(0,1,0.25)`：位置确实在背上，但 `ElytraModel`
   自带的原点在"脚上 1.501 格"处，再抬 1 格 → 原点落到头部高度 = **挂在脑袋上**；
   且 `prepMatrixForLocator` 会乘上模型根骨骼的缩放 → **很小**。

**修复**：Gecko 图层不再锚任何骨骼，直接在实体原点补齐空间差——
`scale(-1,-1,1)` → `translate(0,-1.501,0)` → 原版 `ElytraLayer` 的 `translate(0,0,0.125)`
→ `setupAnim` → `renderToBuffer`。位置/朝向与玩家穿戴鞘翅一致。
翅膀张合仍交给原版 `ElytraModel.setupAnim`（依据 `isFallFlying()` = 共享标志位 7）：滑翔=张开、落地/俯冲=收拢。
Bedrock 模型女仆走的 `LayerMaidElytra` 本就在原版图层空间（`EntityMaidRenderer` 走 `super.m_7392_`），保持不变。

**附带修复（全盘审查发现）**：俯冲 `MAX_DIVE_TICKS(200)` 超时且人还悬在空中时，旧逻辑走 `finishDive`
收翅 → 无滑翔自由落体直坠会吃摔伤；现改为清俯冲计数并恢复滑翔自然下降（滑翔分支不吃摔伤）。

**验证**：编译零错；`test_server.py neoforge1211` PASS；jar 重建（605 条目）并部署
neo 客户端 / 1.20.1 客户端 / pack1201（`match=True`）。仅 1.21.1。

---

## 实测四百五十七【飞行作战修复（实机反馈：鞘翅没显示 / 起飞反向飞很远 / 卡住狂烧烟花还受伤 / 打不到怪）】

**反馈现象**：①鞘翅完全没有渲染；②遇到怪物先打一下，然后用烟花起飞，但起飞方向总是朝反方向、
飞出去很远；③飞出去后卡住会一直用烟花，浪费烟花且自己受伤；④根本打不到怪。

**根因（逐条查实）**
1. **没有操纵方向（核心）**：参考实现 `JerotesPlayerEntity.aiStep` 的操纵方式是每 tick
   `lookAt(target, 360, 360)` + `getLookControl().setLookAt(target, 360, 360)` **立即把身体对准目标**，
   再由滑翔物理（`LivingEntity.travel` 滑翔分支按 `getLookAngle()` 加速）+ 挂载烟花推力推向目标。
   上一版却用 `setDeltaMovement` 自行"拐弯"，滑翔物理随后按旧朝向覆盖回去 → 她朝背后飞、距离永不缩小，
   于是每 20 tick 继续补烟花直到超时。**修复：忠实移植——每 tick 立即对准目标（含俯仰限幅），不再手工转向。**
2. **鞘翅在 Gecko 模型上根本不渲染**：实测女仆用 `geckolib:winefox_*` 模型，
   `GeckoEntityMaidRenderer` 把渲染整个交给 Gecko 渲染器并 return，Bedrock 的 RenderLayer 链不执行；
   且上一版错用 `body` 骨骼做锚点（`BedrockPart.translateAndRotate` 在 Y 翻转空间里把翅膀挪到身体下方）。
   **修复**：Bedrock 层逐字节照抄原版 `ElytraLayer`（`pushPose + translate(0,0,0.125) + setupAnim + renderToBuffer`）；
   **新增 GeckoLib 图层**，挂点照抄 TLM 自己的 `GeckoLayerMaidBackpack`
   （`RenderUtils.prepMatrixForLocator` + `locationModel.backpackBones()`，无背包骨骼时退回躯干骨），
   经客户端 mixin 注入 `GeckoEntityMaidRenderer` 构造注册。显示条件改为"任务为飞行作战且胸甲穿着可用鞘翅"
   （激活即显示，不再随滑翔标志位闪烁）。
3. **起飞前先打一下 + 狂烧烟花**：上一版同 tick 既起飞又调地面近战（"先打一记"），且地面起飞无冷却
   （卡住时每 tick 烧一枚）。**修复**：空中/地面互斥；起飞走 20 tick 冷却；单次起飞最多 6 枚；
   滞空超时后不再推进、靠滑翔自然下降（滑翔中不吃摔伤）。
4. **自己受伤**：参考实现直接用玩家手上的烟花，带爆炸星的挂载烟花到期 `explode()` 会对骑乘者造成
   `5+2n` 伤害（字节码实证），女仆只有 20 血。**修复**：助推弹改用"无爆炸星"烟花
   （`flightDuration=1`、`explosions` 空），仍消耗玩家 1 枚真实烟花，外观/推力与玩家铺翼一致但不炸自己；
   俯冲收尾再补 `resetFallDistance` + 落地缓冲。

**验证**：编译零错；`test_server.py neoforge1211` PASS；jar 重建（605 条目）并部署
neo 客户端 / 1.20.1 客户端 / pack1201（`match=True`）。仅 1.21.1。

---

## 实测四百五十六【飞行作战——新模式（鞘翅 + 重锤 + 烟花，套用 JerotesWarehouse 类玩家单位鞘翅长矛机制）】

**需求**：新增一个作战模式（图标=鞘翅），套用参考模组里"类玩家单位**穿鞘翅时**用长矛"
那一套，武器换成重锤；不响应自主切换；滑翔途中禁止传送。

**参考实现（反编译实证）**：`com.jerotes.jerotes.entity.Mob.JerotesPlayerEntity.aiStep`
——滑翔置位条件 = `自身坠落 / 正在游泳 / 攻击目标在空中 / 已在滑翔` 且胸甲是鞘翅；
主手或副手是烟花火箭且滑翔中且 `useFireworkRocketCooldown<=0` 时
`new FireworkRocketEntity(level, 烟花, 自己)` 助推，冷却置 20 tick。
（`JerotesSpearUseGoal` 是地面长矛拉扯；本条要的是鞘翅版的类玩家单位。）

**实现**
- `MaidFlightCombatTask implements IAttackTask`：图标 `Items.ELYTRA`，注册优先级 4；
  `isWeapon` = 重锤（必须覆写——`IAttackTask.isWeapon` 默认恒 false，不覆写会被
  自主切换判成"无可用武器"从而**把玩家指定的飞行作战切走**）。
- `MaidFlightCombatBehavior`：滑翔接近 → 烟花脉冲推进（20 tick/枚）→ 爬到目标上方
  收翅 → 自由落体俯冲 → 重锤猛砸（复刻 `Player.attack`：基础攻击力 → 附魔修正 →
  下落加成 → 附魔后效 → 击退 → 耐久），落地仍有烟花则继续起飞。
  未激活（三件缺一）时在同行为内退回普通地面近战（自己走位 + 按攻速挥砍）。
- `MaidFlightKit`：装备检测（背包/主副手/护甲位）与穿戴（胸甲鞘翅、主手重锤、
  副手烟花），以及滑翔标志位的读写。
- `EntityFlagInvoker`（新 mixin）：暴露 `Entity.setSharedFlag` —— 原版滑翔物理在
  `LivingEntity.travel`，唯一闸门 `isFallFlying()`（标志位 7），无 `instanceof Player`；
  标志位经 `SynchedEntityData` 自动同步，无需自定义包。
- `MaidSwimGlideMixin`：滑翔时让 `EntityMaid.isVisuallySwimming()` 返回 true →
  自动套用女仆的游泳动画（Gecko `swim` 与 Bedrock `SwimAnimation` 都由该判定选中）。
- `LayerMaidElytra` + `LivingEntityRendererAddLayerInvoker` + `MaidElytraLayerMixin`：
  用原版 `ElytraModel` 在女仆背后画鞘翅（TLM 本体零鞘翅支持）。
- 空中禁传送：滑翔/俯冲并入既有 `isAirborne` 抑制链（跟随瞬移 / 同维拉回 / 跨维跟随 /
  自保归位传送全部让位）。
- 自主切换排除：`AutoCombatSwitch` 的 `buildPools`（挡切进来）、`retuneCombatTactics`
  与还原链（挡切出去）三处按 UID 显式排除。
- 配置 `combat.flightMode` + 中英 lang + 配置面板行。

**自查修掉的三个真 bug**
1. **专用服务器启动崩溃**（回归实测抓到）：把 `addAdditionMaidLayer` 覆写写进了
   `ProMaidExtension`，方法描述符含客户端类型 `EntityMaidRenderer` → FML 注册事件
   总线反射时 `RuntimeDistCleaner` 抛 "invalid dist DEDICATED_SERVER"，整个 mod
   加载失败。改为客户端 mixin（`MaidElytraLayerMixin` 注入渲染器构造）。
2. **攻击权竞争**：Brain 同 activity 行为并发 `tryStart`，TLM 普通近战会在落点先
   出手让目标吃上无敌帧 → 重锤下落加成被吸收。改为本任务只注册飞行行为（独占攻击权）。
3. **静态状态残留**：`DIVING` 等 static 表在女仆死亡/卸载时行为未必走到 `stop()`，
   残留会让该 UUID 永久"俯冲中"（再进不了该模式 + 空中禁传送/落地缓冲卡死）。
   接入既有 `EntityLeaveLevelEvent` 清理链，并在服务端启动时全清。

**验证**：编译零错；两树 server 回归 PASS；部署 neo 客户端 + 1.20.1 客户端 + pack1201
（`match=True`）。仅 1.21.1——重锤是 1.21+ 内容，1.20.1 树未改动。

---

## 实测四百五十五【移除鞘翅突袭测试功能（烟花鞘翅重锤连招不生效 / 与风弹冲突）】

**问题**：实机测试确认——穿鞘翅、带烟花火箭、持重锤的女仆不会打出「烟花加速 → 收翅俯冲
重锤」那套连招，技能没有发生；而且该机制要求同时持有风弹，与重锤猛击的前置条件相互干扰。
按反馈整体删除。

**删除内容（仅 1.21.1，1.20.1 树本就没有该功能）**
- `MaidElytraAssaultBehavior`（GLIDE→DIVE 状态机、脉冲式烟花推进、`smashHit` 复刻）；
- `EntitySharedFlagInvoker` mixin（`@Invoker` 暴露 `Entity.setSharedFlag` / `getSharedFlag`，
  即"非玩家实体强行置滑翔标志位"那条路径），并从 `mixins.promaid.json` 注销；
- 重锤猛击里的全部鞘翅耦合：`isAirborne` 不再并入鞘翅空中状态、`checkExtraStartConditions`
  不再为突袭让位、`requestForcedClutch` 外部入口一并移除（落地帧强制缓冲只保留跃起自身这条）；
- 行为注册优先级 **236**（远距离突袭）；重锤猛击回到 235、战术 230；
- 5 项配置（鞘翅突袭开关 / 消耗烟花 / 冷却 / 触发距离 / 俯冲高度）+ 配置面板 5 行 + 中英 lang 5 键；
- 核心行为注册日志不再打印 `ElytraAssault=236`。

**结果**：1.21.1 与 1.20.1 的差异回到「重锤猛击（1.21.1 专属）」这一项。编译零错、
jar 重建部署、server 回归 PASS。

---

## 实测四百五十四【落地雪补齐独立配置 + 水/雪默认触发高度改 4 格 + 修复迁移钩子失效】

**需求**：落地雪没有像落地水那样的可填面板，配置只能调落地水；同时落地水与落地雪的默认
释放高度都改为 4 格。

**一、落地雪补齐独立配置（两树）**
- 此前落地雪只**借用**落地水的三个数值，面板里只有"落地水"的三项，雪的调不了。
- 新增三项：`snowFallDistance`（触发高度，默认 4）、`snowHold`（保持时长，默认 5 tick）、
  `snowLandingScan`（下探格数，默认 2）。
- `WaterClutchBehavior` 的水/雪两条路径改为各读各的值（含"落地雪维持期补垫"与
  重锤落地帧强放缓冲两条支路）；两者都有桶时仍优先用水。
- 配置面板补齐四行（开关 + 三项数值），中英 lang 同步。
- 默认值：落地水触发高度 **6 → 4 格**，落地雪 4 格。

**二、顺带挖出并修复一个沉默很久的 bug（实测才暴露）**
- 需求本身很简单，但实测「旧默认自动迁移」没生效，追下去发现两层问题：
  1. **Forge 树把 `ModConfigEvent` 注册在了 `MinecraftForge.EVENT_BUS`（主总线）**，
     而它是 **MOD 总线**事件（javap 实证 `implements IModBusEvent`）→ 该回调
     **从未被调用过**。这意味着此前累积的所有迁移（穿透预算 22→6、看门狗 30/45→8、
     搭路 8→5、排班可用性 true→false）**一直是静默失效的**。
     修复：注册改到 `getModEventBus()`，并在 `ServerStartedEvent` 兜底再跑一次。
  2. **Neo 树迁移在内存里生效但不回写磁盘**（探针实证：config 事件里读到旧值 6.0，
     服务端启动时读到已迁移的 4.0，但配置文件仍是 6.0）→ 配置面板会显示旧值。
     修复：在配置事件里保存官方 `ModConfig`，迁移后显式 `ILoadedConfig.save()`。
- 迁移逻辑抽成静态 `runConfigMigration()`（幂等：只在"值 == 旧默认"时才改），
  配置事件与服务端启动双入口调用。

**三、验证（关键：实测而非只看编译）**
- **实测迁移**：把配置文件里的 `waterFallDistance` 故意写回旧默认 6.0（并 `breakBudget=22`），
  启动服务器 → 优雅退出（stdin `stop`，确保 Forge 落盘）→ 重读配置文件：
  - Forge：`waterFallDistance 6.0 → 4.0`、`breakBudget 22 → 6`
  - Neo：`waterFallDistance 6.0 → 4.0`、`breakBudget 22 → 6`
  （修复前两树都保持旧值不动；用探针进一步确认了 Neo 的"内存已改、磁盘未写"。）
- 两树编译零错；jar 重建部署 6 处；`test_server.py 1201` 与 `neoforge1211` 回归均 **PASS**。

---

## 实测四百五十三【开局赠礼被重复给予：改用原版进度系统（对齐《记忆中的幻想乡》）】

**现象**：玩家每次登录都会被发一次《Promaid 手册》与《排班表》，背包里不断多出。

**一、日志实证（整合包存档）**
- 同一存档、同一玩家，两次登录各发一次：
  ```
  09-13 10:55:05 登录 → 10:55:14 赠予（第 1 次）
  09-13 10:59:00 登录 → 10:59:10 赠予（第 2 次）
  ```
- 而玩家 NBT 里 `maid_smart_blueprints_given = 1` **确实是写着的**（TAG_Byte）。
  结论：旧版手搓的 persistentData 标记拦不住跨会话重发。

**二、正解：照搬 TLM《记忆中的幻想乡》的做法（原版进度系统）**
- 反编译实证 TLM 的一次性手册用的是进度系统：
  - `GivePatchouliBookConfigTrigger extends SimpleCriterionTrigger`
  - `data/touhou_little_maid/advancement(s)/grant_book_on_first_join.json` → 触发器 `give_patchouli_book_config`
  - 奖励走 `AdvancementRewards` 的 loot 表
  - `EnterServerEvent` 在 `PlayerLoggedInEvent` 里 `trigger(player)`
- 「只发一次」由**原版进度数据**保证；且发奖励只在**从未完成 → 完成**的跃迁触发
  （`PlayerAdvancements.award` 字节码：`!wasDone && isDone()` 才调
  `AdvancementRewards.grant`）——重复 award 已完成的进度是空操作。

**三、实现（两树）**
- 新增进度 `data/maid_smart/advancement(s)/first_join_gift.json`：用原版
  **`minecraft:impossible`** 触发器（永不自触发，只由本模组显式授予），正好当
  "已领取"的持久记录，**无需自定义触发器类、无需在注册表里注册**。
- 新增奖励掉落表 `.../loot_table(s)/gift/first_join.json`（两本书各 1）。
- 新增 `FirstJoinGift.onLogin(player)`：登录时
  ① 进度已完成 → 直接返回（不重发）;
  ② 老存档（旧标记已写）→ `grantProgress` **静默记账、不触发奖励**（不多给一份）;
  ③ 首次 → `award()` → 记录进度 + 原版按掉落表发书。
  ④ 异常兜底：数据包缺失/进度系统报错 → 退回旧标记 + 直接塞背包（保证功能不消失）。
- `ProMaidExtension.onPlayerLoggedIn` 改为调用 `FirstJoinGift.onLogin`，删除原手搓逻辑。
- 版本差异：1.20.1 目录是**复数**（`advancements` / `loot_tables`）+ `Advancement`；
  1.21.1 是**单数**（`advancement` / `loot_table`）+ `AdvancementHolder`。两树分别适配。

**四、验证（关键：证明进度真的加载了）**
- 两树编译零错（Forge rc=0 / Neo exit 0）。
- **差分测试**：同一测试服分别装旧 jar / 新 jar，比对启动日志的
  `Loaded N advancements` —— 旧 **2081** → 新 **2082**，**正好 +1**，且无任何
  `maid_smart` 解析错误 → 证明我们的进度被解析并注册（排除了"JSON 写错静默失效"
  这个最危险的失败模式）。
- 两树数据包文件已确认打进 jar 且路径正确、JSON 合法。
- jar 重建部署 6 处；`test_server.py 1201` 与 `neoforge1211` 回归均 **PASS**。

**五、未做**
- 未做真人客户端登录验证（原始协议探测会被 FML 握手拒绝）。
  「只发一次」的实际观感需你进游戏登录两次确认。

---

## 实测四百五十二【复活反复摔死：实机日志实证 + 死亡循环断路器】

**来源**：整合包「龙之冒险：新征程 v2.4a」实机反馈——女仆摔死后自动复活，复活瞬间又摔死，
「摔死 → 复活 → 立刻摔死 → 收魂符 → 放出 → 又摔死」反复循环。

**一、日志实证（`latest.log`，2026-09-13）**
- 受击类型明确是 **fall（摔落）**，不是战斗伤害：
  ```
  [maidhurt] 类型=fall 伤害=91.4 位置=(28.2, 294.0, 101.9)
  [maidhurt] 类型=fall 伤害=88.7 位置=(-70.5, 114.0, 55.5)
  [maidhurt] 类型=fall 伤害=87.9 位置=(29.5, 293.0, 102.5)
  [maidhurt] 类型=fall 伤害=113.1 位置=(-81.5, 112.0, 51.5)
  ... 共 9 次
  ```
- 落点高度 **y=282~294**（空岛平台）与 **y=112~115**（地面），与「从 y≈386 天空落下
  约 90~110 格」完全吻合；女仆只有 20 血，摔伤 86~113 → **必死**。
- 循环节奏与配置吻合：该整合包 `autoResurrectDelaySeconds = 1`（默认 60），
  所以变成「复活 → 4 秒摔死 → 1 秒复活 → 又摔死」的 4 秒高频循环
  （日志中 11:24:20/24/28/32 连续 4 次）。

**二、根因＝实测四百五十已修项**
- `findSafeLanding` 把 `getHeight`（384，维度总高度）当成最低建筑高度、
  `getMinBuildHeight`（-64）当成上限 → 两个扫描循环全空 → 兜底落点 `startY+1` = **y≈386 天空**。
- 该修复此前已完成：上下界归位（`getMinBuildHeight+1` / `getMaxBuildHeight-3`），
  并且复活落点改走**原版 `findStandUpPosition`**（玩家复活站的那一格）。

**三、新增：死亡循环断路器（治因之外的兜底）**
- 只修落点是治因，但只要**任何**因素让复活点不安全（地形被改、床被拆、跨维传送、
  维度规则），「复活即死」仍会复发，且延迟越小转得越快。
- 实现：记录每只女仆**上次复活的世界时间**。若她又在**30 秒窗口内**死亡 → 判定复活点
  不安全，把下次复活**退避推迟**（1 分钟起、翻倍、上限 10 分钟），并给主人一条明确
  告警（提示检查重生点 / 先关掉自动复活）。正常间隔的死亡不受影响（计数重置）。
  关掉自动复活时断路器状态一并清空（玩家已介入，不留残余退避）。
- 退避数学单测：60→120→240→480→600（封顶）秒；间隔 >30 秒的死亡不误触发。

**四、顺带纠正一个说法（"对齐原版复活"）**
- 字节码实证：TLM 原版右键墓碑**只做「归还物品 + 删除墓碑」**——`EntityTombstone.use()`
  全程没有 `EntityMaid`、没有 `load`、没有 `addFreshEntity`；墓碑 NBT 只存
  `ownerId / items / maidName`，**根本没有女仆存档**。
- 也就是说「自动复活」是本模组**新增的能力**，不存在可照抄的"原版复活实现"。
  能做到的"对齐原版"就是复用玩家复活的落点算法 `findStandUpPosition`——已完成。

**五、验证**
- 两树编译零错（Forge rc=0 / Neo exit 0）；断路器退避数学单测通过；
  jar 重建并部署 6 处（**整合包 promail-1.1.1.jar 已更新**）；
  `test_server.py 1201` 与 `neoforge1211` 回归均 **PASS**。
- 实机待验：需你进整合包确认复活落点是否正常、循环是否停止。

---

## 实测四百五十一【排班表血量百分比：公式取反修正】

**问题**：排班表列表页女仆名字后面那个「100%」含义不明——它是血量百分比，但公式取反了。

**根因（字节码实证）**
- `m_21233_` = **getMaxHealth**（读 `MAX_HEALTH` 属性）、`m_21223_` = **getHealth**
  （读同步器当前血量）——两处独立核实：映射表 + `LivingEntity` 方法体字节码。
- 列表行却写成 `getMaxHealth / getHealth * 100`，方向反了：

| 血量 | 修复前显示 | 修复后显示 |
|---|---|---|
| 20/20 | 100% | 100% |
| 15/20 | **133%** | 75% |
| 10/20 | **200%** | 50% |
| 5/20 | **400%** | 25% |
| 1/20 | **2000%** | 5% |

- 满血时恰好 100%，所以这个 bug 一直没被发现；一旦掉血数字就往上涨。
- 本仓库其余 8 处同类计算（`SelfPreservationBehavior`、`MaidAidOwnerBehavior`、
  `PerceptionManager`、`ProactiveDialogueManager`、`FollowPreserveMixin`、
  `MaidTeleportPreserveMixin` 等）写的都是正确的 `当前/最大`——**只有排班表这一行是例外**。

**修复**：两树改为 `当前/最大`，与其它处口径统一。全仓库检索确认已无反向写法残留。
按用户要求**保留**该显示（不删行），只把数值改对。

**验证**：两树编译零错；算术对照表确认 5 档血量显示均正确；
全仓库反向写法残留数 = 0；jar 重建并部署 6 处；`test_server.py 1201` 与
`neoforge1211` 回归均 **PASS**。

---

## 实测四百五十【复活后原地反复摔死：根因修复 + 复活保护 + 清理失效配置】

**现象**：女仆摔死后自动复活，复活瞬间又在原地摔死；随后「摔死 → 复活 → 立刻摔死 →
自动收魂符 → 放出 → 又摔死」无限循环。

**一、根因：安全落点函数把两个高度取值写反了（字节码实证）**
- `m_141928_` = **getHeight**（维度总高度：主世界 384），`m_141937_` = **getMinBuildHeight**
  （主世界 -64），`m_151558_` = **getMaxBuildHeight**（= min + height = 320）。
  三重核实：MCPConfig 官方 `joined.tsrg`、`LevelHeightAccessor.isOutsideBuildHeight`
  的字节码（`y < m_141937_()` 才是"在建筑高度之外"）、以及本仓库其余调用点。
- 而 `findSafeLanding` 写的是：
  `minY = m_141928_() + 1`（= 385）、`limitUp = m_141937_() - 3`（= -67）
  → 向上/向下两个扫描循环**都是空循环** → `airY` 恒为 -1 → 走"密闭空间兜底"
  → 返回 `startY + 1.0` = **y≈386 的天空**。三个维度全部如此。
- 女仆因此被放在天上自由落体；摔死又触发自动收符，放出再摔 → 死循环。
  1.21.1 树同名函数同样写反（`getHeight()` 当成最低建筑高度）。
- **修复**：`minY = getMinBuildHeight + 1`、`limitUp = getMaxBuildHeight - 3`，
  上下界各自归位。单测对照：修复前落点 y=386（天空）→ 修复后贴地。

**二、落点改为"原版等效"（用户要求：效果应对齐原版复活）**
- 新增 `respawnLanding()`：床走 `BedBlock.findStandUpPosition`、重生锚走
  `RespawnAnchorBlock.findStandUpPosition`——**就是玩家复活时站的那一格**；
  非床/非锚才退回柱状扫描。
- 为什么必要：床是两格宽方块，自己柱状扫描会把"站在床上方"当成可站立，
  再一路向下贴地 → 落到床边地面，与原版复活点差出半张床。

**三、复活状态清洗（用户建议的兜底，作为第二道保险）**
- 新增 `sanitizeDeathState(nbt)`：死亡快照 load **之前**清掉 `FallDistance` /
  `Motion` / `Fire` / `DeathTime` / `HurtTime`——否则她带着"还在下坠"的状态出现，
  落地首 tick 再吃一次摔落伤害。
- 新增 `cleanseAfterRevive(maid)`：清 fallDistance / deathTime / hurtTime /
  hurtDuration、**invulnerableTime=20**（落地首 tick 免伤）、清着火、清全部
  **负面效果**（按 `MobEffectCategory.HARMFUL`），并给 **抗性提升 V + 抗火各 2 秒**。
- 修正一处字段误用：旧代码注释写 `f_20920_` 是 deathTime，实际 `f_20920_` =
  `oAttackAnim`；真正的 `deathTime` 是 `f_20919_`（NBT `"DeathTime"` 读写的正是它）。
- 魂符释放路径同样清洗：反编译确认 TLM 是 **post(ToMaid(data)) 之后 `maid.load(data)`**
  （1.20.1 与 1.21.1 同构，同一个 CompoundTag 对象），所以直接清洗 `data` 即在
  load 生效之前完成——"放出即摔死"的死循环一并堵掉。

**四、清理失效配置项**
- 「释放血量比（`soulSpellReleaseRatio`，默认 0.35）」**从写入之日起就没有任何读取方**：
  只在收符时写进魂符 NBT，TLM 释放读的是女仆自己的存档血量，从未读过它。
- 已整体移除：配置声明 + 定义 + 配置面板行 + 魂符 NBT 写入 + 常量 + 手册引用。
  全仓库检索残留为 0（无 lang 条目）。当前版本已是"受致命伤害收回魂符"，
  与该项的语义早已不符。

**五、验证**
- 两树编译零错（Forge rc=0 / Neo exit 0）；落点算法单测：主世界/下界、
  平地/地下基地三种场景，修复前均返回天空兜底、修复后均贴地 **PASS**；
  jar 重建并部署 6 处（含旧 jar 清理）；`test_server.py 1201` 与 `neoforge1211`
  回归均 **PASS**。
- 未做实机验证：落点/保护窗口的实际观感需你进游戏确认（杀死一只女仆等复活）。

---

## 实测四百四十九【排班表：显示女仆当前坐标 + 「去她身边」传送】

**需求**：排班表快捷设置里加一个显示女仆当前坐标的功能，以及传送到女仆身边的按键。

**一、坐标显示（第 1 页·快捷设置）**
- 顶部新增一行「位置：维度 X Y Z」。女仆一直在走动，**打开排班表那一刻的快照不作数**，
  所以详情页开着期间**每秒向服务端问一次**当前位置；退出详情页/切到排班页即停，不产生常驻流量。
- 服务端找不到她（已收进魂符 / 所在区块未加载）时显示「她现在不在场」并停止刷新，
  避免对着一个不存在的目标无意义地持续发问。
- 新增 C2S/S2C 一对包 `MaidCoordRequestPacket` / `MaidCoordPacket`（Forge id 15/16，
  NeoForge 以 `CustomPacketPayload` 同样注册）。

**二、「去她身边」传送（与原有「传送到我身边」成对）**
- 原来详情页只有一个「传送到我身边」（把女仆召过来），本次拆成一对方向键：
  **「⚑ 召她过来」**（她过来）与 **「⚑ 去她身边」**（我过去）。两键同宽并排，
  **纵向不多占一行**——240 高最小窗口下再加一行会压住底部提示。
- 「去她身边」：同维度落在她脚边的安全格（复用 `MaidChunkLoadManager.findStandNear`
  的站立判定，与她自己的召回同口径）；**跨维度走原版完整换维度流程**
  （1.20.1 `ServerPlayer.m_264318_` → `m_8999_`，字节码核实内含 Forge
  `onTravelToDimension` + `ClientboundRespawnPacket` 全套），传送后聊天栏报落点。
  她脚下找不到落脚点则取消并说明，不做半空投放。
- 新增 C2S 包 `MaidTeleportToPacket`（Forge id 17）。

**三、顺带修掉一个老问题：排班表界面会暂停单人世界**
- `Screen.isPauseScreen()` 默认返回 **true**，而 Minecraft 的暂停判定是
  「`hasSingleplayerServer()` && 当前 Screen `isPauseScreen()` && 未开局域网」→ **暂停整个世界**
  （字节码实证：`Minecraft.tick` 690~746 计算该标志，`IntegratedServer.tickServer`
  在暂停时走 `tickPaused`、**跳过 `super.tickServer()`**，而 tick 连接正是在那里面）。
- 后果：单人游戏里打开排班表时世界冻结，本界面发出的包（保存日程 / 改名 / 召唤）
  **全部堆在通道里、要等关闭界面才被服务端处理**；本次新增的坐标轮询更会永远停在
  「位置查询中…」。
- 修复：两树 `ScheduleBookScreen` 覆写 `m_7043_()` / `isPauseScreen()` 返回 `false`
  ——与原版 `AbstractContainerScreen`、`ChatScreen` 同口径（那正是背包/聊天界面
  不暂停世界的原因）。已 javap 核实两个 jar 里都带上了该覆写。

**四、验证**
- 两树编译零错（Forge rc=0 / Neo exit 0）；`verify_jar_classes` 通过，
  三个新包类均已入包（Forge 589 项 / Neo 590 项）；`isPauseScreen` 覆写已入 jar；
  包编解码用真实 `FriendlyByteBuf` 往返自测 **ALL OK**（无读写错位）；
  jar 重建并部署 6 处（含旧 jar 清理）；`test_server.py 1201` 与 `neoforge1211` 回归均 **PASS**。

**五、未改动**
- 没有改列表页那个「100%」数字——它是血量百分比，但公式方向可疑，待确认后单独处理。

---

## 实测四百四十八【蛋糕可食用：新增配置开关（应对第三方投喂模组的交互冲突）】

**现象**：往野生（无主）女仆喂蛋糕，蛋糕被消耗、却不触发 TLM 驯服（没有爱心 / 成就）。

**一、定位（字节码核实）**
- Forge 1.20.1 的 `Player.interactOn` 先调 `ForgeHooks.onInteractEntity`：**事件被取消就直接
  返回，不再调用 `Entity.mobInteract`** → TLM 的 `tameMaid`（消耗驯服物 + 认主）不会执行。
- 本模组的 `MaidCakeEdibleMixin` 把 `minecraft:cake` 设为可食用物品（"女仆爱吃蛋糕"的设定）。
- 部分第三方模组（如 solmaiddream）会**按"食物判定"识别投喂目标**，因此把蛋糕也纳入投喂物，
  并在**不检查 `isOwnedBy`** 的情况下抢走这次右键交互 —— 二者叠加才出现该现象。

**二、实测结论：未装该模组时一切正常**
在不加入该第三方模组的环境实测：喂蛋糕驯服野生女仆**完全正常、成就照常触发**。
也就是说这是**模组间的交互冲突**，并非本模组的功能缺陷，因此**没有去改驯服逻辑、
也没有加任何事件拦截**。

**三、改动（仅一处）**
新增配置项 `misc.cakeEdible`（默认开，配置面板「交互与杂项」→「蛋糕可食用」）：
- **开**（默认）：女仆把蛋糕当食物 —— 吃整块蛋糕回复 14 点生命并 +10 好感，
  玩家用蛋糕右击自己的女仆会触发投喂；
- **关**：`MaidCakeEdibleMixin` 与 `MaidCakeEatHandler` 同口径整体停用，蛋糕恢复原版行为
  （只能放置、不能被女仆当食物），"女仆吃蛋糕 / 喂蛋糕加好感"相关功能全部下线。

遇到上述第三方模组冲突时，**把这一项关掉即可**；不冲突则无需改动。

**四、验证**
- 两树编译零错；jar 重建并部署 6 处；`test_server.py 1201` 与 `neoforge1211` 回归均 PASS。

---

## v1.1.1 发布【1.20.1 Forge 正式版】

**版本号**：1.20.1 Forge 版 `1.1.0` → **`1.1.1`**；附件更名为 `promaid-1.1.1.jar`。
1.21.1 NeoForge 版仍为 beta.2，jar 文件名不变（`promaid-1.1.0-neoforge-1.21.1.jar`），
本次同步替换其附件内容。

**升级提示**：安装新 jar 前请删除 mods 目录里的旧 `promaid-1.1.0.jar`——
两个 jar 的 `modId` 相同，同时加载会导致启动失败。

**本版合并发布了 v1.1.0 之后的所有改进**（逐条见上方各实测条目），主要面：

| 方向 | 内容 |
|---|---|
| 语音 | 内置日语语音包（121 条）随 jar 分发；情绪分五档（战斗/关心/俏皮/干活/请求）；排班闲聊 49 条；感知/敌袭 6 条；拥抱摸头 6 条；规则气泡补漏 6 条；响度归一化；睡觉静默 |
| HUD | 冷却可视化（女仆复活倒计时 / 回魂符冷却）；1.20.1 渲染根因修复（窗口高度取错 SRG 字段，冷却+建造两个 HUD 同修） |
| 战斗自保 | 重锤猛击与重锤专属落地水、空中禁传送（1.21.1）；悬空禁搭方块；收符期间冷却条保留；右键墓碑不再当场复活；回魂符冷却持久化修复 |
| 移动与休息 | 床铺互通（女仆睡原版床 / 玩家睡女仆床）；搭路默认开启并迁到「移动与行为」；主人踩踏刷新垫脚方块 |
| 界面 | 配置面板重组（两级菜单 7 大类 / 28 小类，开关集中化）；手册详细介绍重写 + 链接一键跳转配置；手册内自动复活设置页；排班表快捷调整整页任务选择 |
| 其他 | 女仆自动复活（墓碑到期自动消失、主人重生点复活、背包全恢复）；女仆绝不伤害主人三层防护；彻底移除第三方软联动（纯 TLM 独立版） |

---

## 实测四百四十七【规则气泡语音补漏 6 条：「附近没有值得挖的矿石」还是人机语音】


**一、根因（不是没触发，是没登记）**
- 这句来自 `dialogue/WorkStatusReporter.java` 的"卡住原因"播报（挖矿任务空闲时每 N 秒
  弹一次气泡解释卡在哪）。
- 它和其他气泡不同：**文本是以变量传进气泡的**（`addTextChatBubble(reason)`、`reasonOf`
  返回字符串），而不是字面量，所以历次补语音时被漏掉了。
- 完整链路：`ChatBubbleLimitMixin` → `SystemTTSManager.speak` → `JarVoicePack.matchKey(text)`。
  manifest 里没有这条文本 → 命中失败 → 落到最后的 `④ TLM TTS 合成` = 人机语音。

**二、全盘扫描（把同类漏洞一次找干净）**
用 site-aware 方式提取两棵树**所有** `addTextChatBubble(...)` 调用（含字符串拼接与三元
表达式），并追到**变量间接来源**（WorkStatusReporter / PerceptionManager /
EmotionNetworking / ScheduleBubbleBehavior / SmartDesignTool / MaidAidOwnerBehavior），
再逐条与 manifest 比对。命中情况：

| 来源 | 文本 | 补漏前 |
|---|---|---|
| WorkStatusReporter | 附近没有值得挖的矿石 | **缺** |
| WorkStatusReporter | 我没有镐，没法挖矿——给我一把镐吧 | **缺** |
| WorkStatusReporter | 附近没有需要打理的农田了 | **缺** |
| WorkStatusReporter | 被骑乘着，动不了 | **缺** |
| MaidAidOwnerBehavior | 主人，金苹果给你！ | **缺** |
| MaidAidOwnerBehavior | 主人，附魔金苹果给你！ | **缺** |

其余规则气泡（感知 7 条、排班 49 条、拥抱/摸头 6 条、建造/挖矿/伐木/酿造/自保/投喂等）
**全部已有条目**，未发现新的漏网之鱼。

**三、修复（数据层，不动逻辑）**
1. 新增 6 条日语台词并合成 ogg（沿用五档情绪：挖矿/农田=work，没镐/被骑=plead，
   金苹果=care）；
2. manifest 115 → **121 条**（两棵树同步）。**附魔金苹果条目排在普通金苹果之前**——
   二者都是 `contains`，不这样排会被 `金苹果给你` 先截胡；
3. 插在 `fallback_*` 兜底条目之前，避免被兜底条目遮蔽；
4. **零遮蔽校验**：把 121 条每一条用自身 `text` 反查 `matchKey`，必须仍命中自己，
   结果 `SHADOWED 0`；
5. ASR 客观校验：新合成音频用 whisper 转写，「近くに掘る価値のある鉱石が無いの」
   「乗られてて動けないの」等整句正确（个别片假名如 ツルハシ 被 whisper 拆错，
   属 ASR 对孤立词的已知误写，非发音问题）。

**四、交付**
- 语音随 jar 分发（`assets/promaid/voice/`），重建两个 jar 并部署 6 处目标；
- `gen_voice_pack.py` 的台词表同步补上这 6 条，今后整体重训不会遗漏。

---

## 实测四百四十六【冷却 HUD 在 1.20.1 不渲染的真正根因：窗口高度取错了 SRG 字段】


**一、证据链（从已轮转的玩家会话日志里挖出来）**
- `logs/debug-1.log.gz`（玩家 01:23~01:25 那一轮）里：
  - 服务端每秒在下发：`[promaid/冷却HUD] KThragg 下发 2 条 | revive:汉服酒狐:58/60, …`
  - 客户端有 `cooldown hud: first snapshot received (2 entries)`
  - 客户端也有 `cooldown hud: first render event`（444 改的注册/事件是好的）
  - **但没有 `cooldown hud: first draw`** → 数据到了、回调也进来了，是在渲染函数里被
    某个守卫提前 `return` 掉了。

**二、根因（对照 `srg1201_to_moj1211.json` 才看清）**
- `com.mojang.blaze3d.platform.Window` 的 SRG 名是：
  - `m_85445_` = `getGuiScaledWidth`
  - `m_85446_` = **`getGuiScaledHeight`**
  - `m_85447_` = **`getX()`（窗口位置！）**
- 433/444 两版都误把 `m_85447_` 当"高度"用：`int h = event.getWindow().m_85447_();`
  → h 实际是**窗口的 X 坐标**。窗口贴左时（最大化/全屏，x=0）`h - LINE_H - 2 = -12`，
  而 `y = 4`，于是 `y > -12` 恒成立 → **每帧提前 return，HUD 永远不画**。
  只有窗口恰好停在 x≥16 的位置时才偶然画得出来 —— 这正是"我自己把窗口拉出来测试就正常、
  你那边一直没有"的原因（我上次验证成功，纯粹因为测试窗口恰好在 x>16）。

**三、修复**
1. 冷却 HUD：`h` 改用 `m_85446_`（`getGuiScaledHeight`）。
2. **建造 HUD 同款错误一并修**：旧版 `w = m_85446_`、`h = m_85447_` 两个都错，
   正确是 `w = m_85445_`、`h = m_85446_`；折行宽度与"超出屏幕不画"判定一起恢复正常。
3. 加一道保险：`h <= 32` 视为取值异常，不再用它拦绘制（宁可画出来，也不要因为取值错误
   整块消失）。
4. 诊断：新增 `cooldown hud: SKIP[原因] …`（按原因各记一次、最多 5 行），
   以后若再看不到，日志会直接指出是 `screen / f3 / stale / noRoom / fontNull` 哪一个。

**四、实测验证**
- 把存档**复制**成一次性世界、用玩家本人 UUID 启动 1.20.1 客户端，并把游戏窗口
  **贴到屏幕左上角**（复现 x=0 的失败条件），杀掉一只女仆触发待复活：
  `latest.log` 依次出现 `first snapshot received (3 entries)` → **`first draw at y=44`**，
  HUD 确实画出来了。
- 测试世界、临时脚本、假账号缓存条目均已清理；原存档 mtime 未变（已核对）。
- 1.21.1 侧用的是 Mojmap 的 `getGuiScaledHeight()`，本来就没这个问题（与你"1.21.1 已经好了"
  的观察一致）。

---

## 实测四百四十五【语音包情绪化重制（按情境分五档）+ 落地水"没声音/染红"两个 bug】


**一、情绪化重制（115 条重写 + 五档情绪）**
- 关键约束：`manifest.json` 只存中文 key，**上一版的日语台词没有落盘**，所以本次按中文
  语义**重新撰写**了 115 条日语台词（顺手把台词也写得更口语、更有情绪）。
- 情绪的实现方式：GPT-SoVITS 的语气由**参考音频**决定。磁盘上只有两条【小酒狐本人】
  的参考音频：
  - A《この命ご主人様のために捧げますね》= 郑重/沉稳；
  - B《どうしました私があまり可愛いからびっくりしちゃったんですか》= 俏皮/活泼。
  两条同一说话人 → **不会变声**，只是换语气底色。分档：
  | 情绪 | 条数 | 参考 | 语速 | 用在哪 |
  |---|---|---|---|---|
  | 战斗·紧张 | 20 | A | 1.08 | 敌袭/主人受伤/低血/自保/图腾/盾牌/落地水雪 |
  | 关心·温柔 | 11 | A | 0.95 | 投喂/药水/复活归来/主人注视 |
  | 俏皮·日常 | 60 | B | 1.05 | 49 条排班闲聊 + 钓鱼 + 天气 + 拥抱/摸头 |
  | 干活·汇报 | 17 | A | 1.03 | 建造/搭路/挖矿/伐木/酿造 |
  | 请求·为难 | 7 | B | 1.02 | 缺料/没食物/够不着 |
- **采样参数是 ASR 对照试出来的**：`temperature 1.15 / top_k 20 / top_p 0.95`
  这组会把短句合成糊掉——「敵が来たよ、気をつけて！」被 whisper 转写成
  「てちがちだよ 塩つけて」；而 `speed 1.08 + 默认采样（temp 1.0 / k15 / p1.0）`
  反而转写完全正确。所以**情绪靠参考音频 + 语速，采样一律用默认保守值**。
  最终五档各抽一条 ASR 复核（enemy_near / aid_food / hug_02 / build_done /
  no_food_left）全部清晰可辨。

**二、"落地水没语音、还是人机语音"**
- 根因：`SystemTTSManager.speak` 把「内置包最小间隔」（`jarPackMinIntervalS`，默认 **8 秒**）
  的判断放在了**命中判定之前**——一旦命中但被间隔挡下，代码会继续往下走，最后落到
  第 ④ 段 TLM TTS 合成，于是听到"人机语音"。
- 修复：**先命中，再谈间隔**。命中即由内置包负责：被间隔挡下就**静默**，绝不回退 TTS；
  并让 `clutch_water` / `clutch_snow` / `enemy_near` 三条短促关键台词**豁免**该间隔
  （否则落地水常被吞掉，看起来像语音包失效）。

**三、"[警示]落地水"染红**
- 落地水/落地雪是女仆自己的**动作播报**，不是威胁提示；旧版只要周围 12 格有敌对生物
  就统一按敌袭染红（`§c[警示]` 前缀 + 红色气泡）。现在这两句豁免，聊天框前缀与世界
  气泡颜色都回到常规青色。

**四、验证**
- 两树编译零错；jar 重建（含新语音包）并部署到 6 个实例；`test_server.py 1201` 与
  `neoforge1211` 回归均 PASS；新语音用 whisper 抽样复核（见上）。

**五、已知限制（说明）**
- 磁盘上只有两条小酒狐参考音频，做不到"每种情绪一套专属语气模型"。如果你能提供更多
  **小酒狐本人**不同情绪（生气/慌乱/大笑…）的片段，我可以直接拿它们当参考再合成，
  情绪会更到位；否则就只能在"同参考 + 语速 + 台词"这个范围内优化。

---

## 实测四百四十四【冷却 HUD 在 1.20.1 恒不渲染：渲染回调根本没被调用】


**一、定位（日志说话）**
- 1.20.1 客户端 `latest.log` 里有
  `cooldown hud: first snapshot received (1 entries)`，
  但**没有** `cooldown hud: first draw` —— 说明服务端数据正常下发、客户端包也收到了，
  是**渲染回调从未进入**（不是数据问题、不是绘制逻辑问题）。
- 1.21.1 侧同款逻辑（NeoForge 的 `RenderGuiLayerEvent.Post` + 注解注册）是好的 →
  问题只在 1.20.1 的那套挂载方式。

**二、根因**
- 1.20.1 侧 433 版本改成了
  `@Mod.EventBusSubscriber(modid="promaid", bus=FORGE, value=CLIENT)` +
  `RenderGuiOverlayEvent.Post` 只认 HOTBAR 层。
- 该**注解自动注册在 1.20.1 对这个客户端 FORGE 总线类目没有生效**（本模组里此前只有
  MOD 总线的注解订阅者、以及懒注册的先例），于是 overlay 事件根本到不了这个类，
  HUD 永远不画。

**三、修复（1.20.1 侧）**
1. 换事件：改用 `net.minecraftforge.client.event.RenderGuiEvent.Post`
   （反编译 `ForgeGui.render` 实证：方法末尾**每帧必然 post 一次**），
   不再依赖"逐层 overlay 各发一次事件 + 比对 `minecraft:hotbar` id"，少一个失败点。
2. 换注册：改为**显式注册** —— 客户端 Mod 构造期
   `PromaidClientSetup.registerHudHooks()` → `CooldownHudRenderer.ensureRegistered()`
   （`MinecraftForge.EVENT_BUS.register(类)`），`onSnapshot` 里再兜一次；
   `registered` 闸保证只注册一次。不再依赖注解扫描。
3. 诊断三点链：`first snapshot received` / `first render event` / `first draw`，
   以后再出问题一眼能区分是没数据、没注册/没事件、还是没绘制。

**四、验证（客户端烟测）**
- 把一份存档**复制**成一次性世界 `_hudtest`（不动原档），用测试账号离线启动 1.20.1
  客户端，`latest.log` 依次出现：
  `first snapshot received (2 entries)` → `first render event` → `first draw at y=4`，
  即 HUD 确实被画出来了。
- 测试世界、测试账号在 `usercache.json` 的条目、以及临时脚本/截图均已清理；
  原存档 mtime 未变（已核对）。

---

## 实测四百四十三【悬空禁搭方块（自保搭高 / 搭路 / 挖矿垫脚 / 伐木垫脚）】


**一、做法：一个统一闸口**
- 新增 `com.maidsmart.tool.MaidPlaceGuard.blocked(maid)`；四个搭方块模块
  （自保搭高 / 搭路 / 挖矿垫脚 / 伐木垫脚）都在各自 `takeBuildBlock` 取料前问一次
  —— 被禁就返回 `null`，各调用点现有的 `if (item == null) return false` 会自然
  放弃本次放置：**不消耗方块、不改动世界**。
- 取料失败时会播的"背包里没有搭方块的材料"气泡按下同一判定**静默**，
  避免悬空时误报（自保 `announceNoMaterial`、搭路 `notifyNoBlock`、
  挖矿/伐木 `notifyNoBuildBlock` 四处）。

**二、判定口径**
- 开关 `misc.noPlaceInAir`（默认开）。
- **一律放行**：站在地面、在水里/岩浆里、骑乘、鞘翅滑翔 —— 这些都不是"悬空危急"。
- **坠落距离 ≥「落地水触发高度」**（`combat.waterFallDistance`，默认 6 格）→ 禁搭。
  理由：到这个高度落地水本来就会接管；此时搭方块既救不了她，还会把落点的水/地面
  结构改掉、把落地水挡住 —— 正是实测的摔死根因。
- **1.21.1 另加**：重锤跃起（`MaidMaceSmashBehavior.isAirborne`）**整段空中都禁**
  （上升段也算，避免起跳瞬间乱搭）。

**三、不破坏正常垫高**
本模组的垫脚动作都是"站在地面或浅跳时往前方脚下垫一块"（`fallDistance ≈ 0`），
所以照常工作；只有真正的坠落才被拦。

**四、验证**
- 两树编译零错（新增源文件已重新生成 argfile）；jar 重建并部署到 6 个实例；
  `test_server.py 1201` 与 `neoforge1211` 回归均 PASS。

---

## 实测四百四十二【重锤专属落地水 + 重锤空中禁传送（1.21.1 专属）】


**一、重锤专属落地水**
- 旧版是"半吊子抑制"：`suppressesFallClutch` 只在【目标仍在命中范围内】时让开
  落地水——冲刺段目标一旦跑出范围，水就可能提前放、把 `fallDistance` 清零
  （重锤下落加成全丢）；或者该放的时候来不及放，落地照样摔。
- 现在改成：
  - **跃起全过程**（`isAirborne`）通用落地水/雪**完全让位**；
  - **落地帧**：`MaidMaceSmashBehavior` 在**结算完猛击之后**置位
    `FORCED_CLUTCH` 请求，`WaterClutchBehavior`（优先级 240，在本行为 235 之后
    tick）在同一 tick 于落点**强制**放一格水（有水桶；下界不放水）或细雪
    （只有细雪桶），不看 `fallDistance` 阈值。
- **顺序是关键**：先算猛击（读此刻仍非零的 `fallDistance`）→ 再放水。加成不丢、
  人也摔不着。放水位置复用原落地水的 `findLandingPos`（落点 + 脚格双格兜底）。

**二、重锤空中禁传送**
- 新增 `MaidMaceSmashBehavior.isAirborne`；跃起期间以下**自动传送**全部让位：
  - TLM 跟随过远瞬移（`MaidTeleportPreserveMixin.teleportToOwner`）；
  - 同维度远距拉回，含 Y 轴"搭太高"拉回分支（`MaidChunkLoadManager.trySameDimPull`）；
  - 跨维度跟随（`MaidChunkLoadManager.followIfCrossDimension`）；
  - 自保归位传送（`SelfPreservationBehavior` 两处）。
- **不受影响**：玩家手动「一键集合/救援」（人工意图）与主人死亡传送（紧急意图）。
- 跃起最长 `MAX_AIR_TICKS = 100 tick`（5 秒）自动收尾，不会长期禁传。

**三、验证**
- neo 树编译零错；jar 重建并部署到 6 个实例；`test_server.py 1201` 与
  `neoforge1211` 回归均 PASS。

---

## 实测四百四十一【收符期间冷却条消失：手动收符的魂符不带标记 + 魂符落位写错槽】


**一、根因**
- HUD 的"女仆在符里"分支（`CooldownHudTracker` 扫主人背包）只认【自动标记】的魂符：
  `charmCooldownUntil` 要求 `AUTO_SAVED_TAG` + 冷却戳同时存在。
- 玩家**手动**用空魂符收女仆时，魂符是 TLM 自己 `new` 的、不带任何标记 →
  HUD 扫不到 → 冷却条消失；而冷却其实还记在女仆 persistentData 里继续计时
  （所以"实际上还在计时"）。
- 另外发现魂符落位写错槽：主手恒写 `items[0]`（玩家选中槽 ≠ 0 时会把 0 号槽
  的物品直接顶掉、而手里的空符还是空的）、副手误写 `armor[0]`（顶掉靴子）。

**二、修复**
- 新增 `MaidSoulSpellGuard.onSoulSpellToItem`，挂在 TLM 的
  `MaidAndItemTransformEvent.ToItem`（反编译实证：`storeMaidData` 里发出，
  1.21.1 在 `AbstractStoreMaidItem`、1.20.1 在 `ItemSmartSlab`）——**自动/手动两条
  收符路径都会经过它**：女仆仍在冷却时，把冷却戳 + 女仆名盖到魂符物品上。
  女仆没在冷却则不标记（手动收符不该凭空冒出冷却条）。
- `charmCooldownUntil` 放宽为**只认冷却戳**；`AUTO_SAVED_TAG` 仍只用于
  "释放时是否重算冷却"的语义，职责分开。
- 自动收符路径改为**先写女仆 persistentData、再 `storeMaidData`**，确保
  `ToItem` 钩子（以及魂符里的女仆 NBT）都能拿到本次冷却。
- 修魂符落位：主手 → `items[selected]`，副手 → `offhand[0]`，不再顶掉别的物品。
- `CooldownHudTracker` 的节流日志补上条目详情（`kind:名字:剩余/总`），
  下次再出现"条不见了"能一眼区分是没数据还是名字丢了。

**三、验证**
- 两树编译零错；jar 重建并部署到 6 个实例；`test_server.py 1201` 与
  `neoforge1211` 回归均 PASS。
- 预期：女仆被收进魂符（无论自动/手动）期间，只要她的回魂符冷却还在跑，
  HUD 的「回魂符 · 名字 M:SS」就会一直显示到冷却结束。

---

## 实测四百四十【敌袭/感知系统消息接上内置日语语音包（6 条）】


**一、根因**
- 所有系统气泡都会经 `ChatBubbleLimitMixin.addTextChatBubble` 汇入
  `SystemTTSManager.speak`，先查内置包（`assets/promaid/voice/manifest.json`）。
- 但 `PerceptionManager` 的感知类气泡**从来没进过 manifest**：
  敌袭出现「有敌人靠近了！离我 X 格」、敌袭靠近「有敌人靠近…离我 X 格了」、
  「附近的敌人都清掉了，安全了！」、「主人受伤了！生命值降到 X%」、
  「主人血量很低了，我好担心…」、「主人一直在看着我…」、「天气变成…了」。
- 未命中 → 落到第 ④ 段 TLM TTS 站点合成，就是你听到的「人机语音」。
- 附带难点：这些文案带**动态距离/血量数字**，manifest 的 `exact` 模式永远匹配不上。

**二、修复**
- 新增 6 条 **`contains`（前缀包含）** 映射，配 6 条日语语音
  （GPT-SoVITS 小酒狐 v2Pro 合成、峰值归一化 -1.5 dB、libvorbis q4）：
  | 气泡文本（动态） | manifest key（contains） | 语音文件 |
  |---|---|---|
  | 有敌人靠近了！离我 X 格 / 有敌人靠近…离我 X 格了 | `有敌人靠近` | `enemy_near.ogg` |
  | 附近的敌人都清掉了，安全了！ | `附近的敌人都清掉了` | `enemy_clear.ogg` |
  | 主人受伤了！生命值降到 X% | `主人受伤了` | `owner_hurt.ogg` |
  | 主人血量很低了，我好担心… | `主人血量很低了` | `owner_low_hp.ogg` |
  | 主人一直在看着我… | `主人一直在看着我` | `owner_looking.ogg` |
  | 天气变成下雨/雷雨/放晴了 | `天气变成` | `weather_change.ogg` |
- 「有敌人靠近」一条同时覆盖"出现"和"靠近"两种敌袭提示；「天气变成」一条覆盖三种天气。
- 语音说的是固定日语台词（不念数字）；**气泡照旧显示精确距离/血量**，两者互不影响。
- 内置包条数 **109 → 115**（60 条系统消息 + 49 条排班气泡 + 6 条拥抱/摸头），
  配置面板说明、手册「语音系统」章、设置页提示同步。
- 追加位置在 manifest 末尾；脚本回归校验 12 条样例（含 3 条既有条目）全部命中，
  未被既有 `contains` 条目截胡。

**三、验证**
- 两树 `gen_compile`/`run_javac6`/`run_javac_neo` 编译零错；
- jar 重建（含 6 个新 ogg + manifest 115 条）并部署到 6 个实例；
- `test_server.py 1201` 与 `neoforge1211` 回归均 PASS。

---

## 实测四百三十九【回魂符冷却"放出女仆后就没了"根因：1.21.1 的持久化键是 NeoForgeData】


**一、根因（javap 实证）**
- 1.20.1 Forge 的实体持久化数据子标签叫 `ForgeData`；**1.21.1 NeoForge 改成了 `NeoForgeData`**
  （证据：补丁版 `net.minecraft.world.entity.Entity` 的 `getPersistentData()` 读写的就是
  `NeoForgeData`，常量池里只有这一个键，没有 ForgeData 的兼容读取）。
- neo 树把「释放女仆时重新起算的冷却」写进的是 `ForgeData` 子标签 → 女仆加载后
  `getPersistentData()`（读 `NeoForgeData`）根本看不到这个值 →
  **冷却失效（可被立刻再次收符）+ HUD 不显示**。
- 这正好解释你看到的现象：女仆在符里时，HUD 靠【魂符物品自身】的冷却戳还能显示；
  一旦放出来、物品标记被清掉，实体上又没有冷却 → CD 与冷却条一起消失。

**二、修复**
- neo 树 `FORGE_DATA_TAG` 由 `"ForgeData"` 改为 **`"NeoForgeData"`**（写进 `MaidInfo` 子 NBT 的
  那条路径也一并随之修正——TLM 释放时读的就是它）。
- 两树都加一步【双保险】：在 `ToMaid` 事件里**再直接写一次实体 `getPersistentData()`**，
  防止 TLM 在事件之后才把 `data` 应用到实体导致写入被覆盖。
- 冷却口径不变：仍按实测四百零四「从**释放时刻**重新起算」。

**三、效果**
- 放出女仆后：冷却正常生效（冷却期内不会再被自动收符，防收放循环恢复），
  左上角 HUD 的「回魂符 · 名字 · 剩余/总秒」也会一直显示到冷却结束。

**四、验证**
- 两树编译零错；重建 jar 并部署；`test_server.py 1201` / `neoforge1211` 回归 PASS。

## 实测四百三十八【右键墓碑不再当场复活：改为原版行为 + 取消本次自动复活】


**一、改动**
- **删掉"右键墓碑 = 立即复活"**（实测四百二十六引入的 `reviveFromTombstone` + 对应注入），
  mixin 重写为 `MaidTombstoneClickMixin`：
  - 只在【服务端 + 主手 + 自动复活开启 + 墓碑属于本人待复活登记】时，把这块墓碑对应的
    **待复活登记移除**；
  - **不取消事件** → 右键继续走 TLM 原版（归还墓碑里的物品等），观感与行为完全一致。
  - 只有墓碑所属主人本人的右键会取消（防他人误触）；未登记 / 非主人 = 完全不介入，纯原版。
- 新增 `MaidAutoResurrect.cancelPendingOnTombstoneClick(...)`；移除已废弃的
  `reviveFromTombstone` / `pendingOwnerOfTombstone`（无引用）。mixin 注册在
  `mixins.promaid.json` 里同步改名。

**二、效果**
- 冷却恢复意义：不再有"绕过 CD 的免费复活"。玩家若手动右键墓碑取回物品，就等于放弃
  本次自动复活——她不会在到期时又自己冒出来。
- HUD 同步：取消登记后，左上角的「复活 · 名字」倒计时随之消失（与服务端同一份数据源）。

**三、文档**
- 手册「女仆自动复活」章：把「右键墓碑 = 立即复活」改写为「右键墓碑 = 原版行为 + 取消本次
  自动复活」；测试示例里"懒得等就右键"一句同步改为"想缩短等待只能调小复活延迟"。

**四、验证**
- 两树编译零错（mixin 改名后已重跑 `gen_compile*.py` 重建源清单）；重建 jar 并部署；
  `test_server.py 1201` / `neoforge1211` 回归 PASS。

## 实测四百三十七【重锤"打着打着不再触发"：门禁加固 + 诊断日志】


**一、先把最可能的两个原因说清楚**
- **风弹耗尽**：现在规则是「重锤 + 风弹缺一不可」，**每次猛击消耗 1 枚风弹**——给的那几枚用完之后自然就不再触发（与战斗模式无关）。
- **主手被换走**：本行为只认「主手 = 重锤」。若她切了别的任务/武器（自动装备把她手上的重锤换掉），也会停。
- 这两个都能被下面的诊断日志直接证实。

**二、目标读取加固（对应玩家的猜测）**
- 新增 `currentTarget(maid)`：优先读脑内 `ATTACK_TARGET`，读不到退回 `maid.getTarget()`（Mob 原生目标）——
  玩家怀疑"自主战斗写脑内记忆、TLM 原生攻击写别处"，两条都认就不会漏触发。
  （旁注：TLM 的 `EntityMaid` 本身就持有 `ATTACK_TARGET`，理论上两种模式都会写；兜底无害。）

**三、风弹读取加固**
- 旧版 `hasWindCharge` / `consumeWindCharge` **只扫 `getMaidInv()`（背包）**——风弹放**副手**时
  会出现「检查通过但消耗失败」或「压根不触发」。现在**背包 + 副手都认、都能消耗**。

**四、门禁诊断（下次复现就能定位）**
- 只要"身上有重锤却没触发"，每只女仆 5 秒最多写一条到 `logs/promaid.log`（分类 `重锤门`），
  内容：原因 + 主手物品 + 风弹数量 + 目标距离 + 是否着地 + 冷却剩余。
- 例如 `[重锤门] 小玉 不触发: 没有风弹（重锤+风弹缺一不可） | 主手=mace 风弹=0 目标=2格 地面=true 冷却剩=0t`
  ——一眼就能区分是弹药、武器、目标还是距离的问题。

**五、验证**
- neo 树编译零错；重建 jar 并部署；`test_server.py neoforge1211` 回归 PASS。

## 实测四百三十六【重锤：风爆（Wind Burst）机制核实 + 补上原版猛砸冲击表现】


**一、结论先说：没有附魔就没有风暴，这是原版行为，不是 bug**
- 反编译/数据双向核实：`data/minecraft/enchantment/wind_burst.json` 里风爆是
  `minecraft:post_attack` → `minecraft:explode`（`affected/enchanted: attacker`，
  半径 3.5、`gust_emitter_large/small` 粒子、`entity.wind_charge.wind_burst` 音效、
  `knockback_multiplier`），触发条件 = direct_attacker **下落 ≥1.5 格且非飞行**。
- 也就是说：**只有重锤附了「风爆」才会炸出风暴**（吹飞周围、并把女仆自己顶起来）；
  没附魔的重锤猛砸本来就不产生风暴、砸完直接落地——原版玩家也一样。

**二、风爆对女仆【确实生效】（javap 实证调用链）**
- `EnchantmentHelper.doPostAttackEffects(level, target, source)` 内部：
  `attacker = source.getEntity()` → 若为 LivingEntity 则取 `attacker.getWeaponItem()`，
  再用 `runIterationOnItem(itemSource, MAINHAND, attacker, visitor)` 遍历**攻击者主手**的
  附魔 → 因此女仆（非玩家）的 `mobAttack` 伤害源同样会跑到风爆（`enchanted: attacker`、
  mainhand）这条组件上。
- 我们的 `smashHit` 是「先 `hurt` → `doPostAttackEffects` → …… → 最后才 `resetFallDistance`」，
  风爆判定时 `fallDistance` 仍然很大（≥1.5）且未飞行 → **条件满足**。
- 所以：给女仆一把附「风爆」的重锤，猛砸就会出风爆；没附魔就只剩落地，符合原版。

**三、真正缺的是「原版猛砸冲击表现」——已补**
- 原版 `MaceItem`：命中时 `levelEvent(PARTICLES_SMASH_ATTACK = 2013, 目标脚下, 750)`
  （砸击烟尘环），音效按原版三选一——贴地且落距 >5 用 `MACE_SMASH_GROUND_HEAVY`、
  贴地用 `MACE_SMASH_GROUND`、空中用 `MACE_SMASH_AIR`。
- 我们之前只放了 `MACE_SMASH_GROUND` 一个音效、没有烟尘 → 观感就是「砸下去没动静」。
  本次按原版补齐（**无附魔也有这一下烟尘**），位置取 `target.getOnPos()`，数值照原版 750。
- 手册「单兵战术」章：重锤段补上「砸击烟尘」与「风爆」说明。

**四、验证**
- neo 树编译零错；重建 jar 并部署；`test_server.py neoforge1211` 回归 PASS。

## 实测四百三十五【拥抱 / 摸头亲昵语音（6 条，只训练女仆说的话）】


**一、台词来源与范围（严格只取女仆台词）**
- `EmotionNetworking` 的 `say(maid, ...)`（G 摸摸头 / H 抱抱触发）：
  - 拥抱 3 条：抱抱！好温暖～ / 主人的怀抱最安心了～ / 嘿嘿，被抱着都不想动啦～
  - 摸头 3 条：嘿嘿，好舒服～ / 摸摸头就又有精神了！ / 最喜欢主人摸头了～
- **旁白（第三人称括号叙述，如"（你张开双臂，把 XX 轻轻拥入怀中……）"）一律不训练**——按要求排除。
  这两句只走 `player.sendSystemMessage`，不进气泡、不触发语音。

**二、制作（与四百二十 / 四百二十八同款流程）**
- 本机 GPT-SoVITS（小酒狐 `小酒狐-e15.ckpt` + `小酒狐_e8_s184.pth`，日语参考音）逐条合成日语；
- 各自峰值归一化到 −1.5 dB（`volume` + `alimiter`，libvorbis q4）；
- 产物 `hug_01~03.ogg` / `pat_01~03.ogg` 打进 jar（`assets/promaid/voice/`），两树各一份；
- manifest 追加 6 条 `{中文原句, 对应 ogg, mode: exact}`（追加在 103 条之后，exact 防串扰）→ 共 **109 条**；
  已用脚本复刻 `JarVoicePack.matchKey` 验证 6/6 命中自己的音频。

**三、无需改代码**
- `say()` 走的是 `addTextChatBubble` → `ChatBubbleLimitMixin` → `SystemTTSManager.speak` →
  内置语音包命中即播放；本次只补了音频与 manifest，接线本来就在。

**四、同步**
- 面板「启用内置语音包」说明、手册语音章、GuideScreen 音量提示的条数 103 → 109（54+49+6）。

**五、验证**
- 两树编译零错；重建 jar 并部署；`test_server.py 1201` / `neoforge1211` 回归 PASS。

## 实测四百三十四【重锤改为「重锤 + 风弹」缺一不可 + 附魔生效性核查】


**一、改为缺一不可（不再占用风弹当"加成"）**
- `MaidMaceSmashBehavior.checkExtraStartConditions` 增加前置：`combat.maceWindCharge` 开启时
  **必须背包里有风弹**，否则本行为不启动 → 交还原版（TLM 正常持锤平砍），**不会起飞**。
- `start` 里只有消耗掉 1 枚风弹才继续起跳（`wind==false` 直接 return 兜底）；
  起跳垂直初速固定用风弹档 1.7。
- 配置语义改写：`combat.maceWindCharge` = 「重锤·必须消耗风弹（默认开）」；关掉 = 恢复旧版
  「不消耗风弹自由起跳」（标注不推荐）。总开关 `combat.maceSmash` 不变。

**二、附魔生效性核查（javap 实证原版调用链）**
- **伤害加成** ：`EnchantmentHelper.modifyDamage`（锋利 / 亡灵杀手 / 节肢杀手）；
- **下落加成** ：`Item.getAttackDamageBonus` 内部会调 `modifyFallBasedDamage` → **密度** 附魔算数；
- **攻击后效** ：`EnchantmentHelper.doPostAttackEffects` → **火焰附加** 等 POST_ATTACK 组件；
- **护甲穿透** ：**Breach** 走原版 `hurt` 的护甲结算（`modifyArmorEffectiveness` 从伤害来源的武器解析）；
- **耐久** ：`hurtAndBreak(int, LivingEntity, EquipmentSlot)` 含**耐久**附魔；
- **击退** →：原版这一步在 `Mob.doHurtTarget` 里用 `EnchantmentHelper.modifyKnockback` 施加，
  而我们为了带出重锤下落加成是**直接 `hurt`**、绕过了它 → 本次补上：命中后按 `modifyKnockback`
  施加击退，并按原版把自身水平速度乘 0.6。

**三、同步**
- 面板行改名「重锤·必须消耗风弹」+ 帮助文本；手册「单兵战术」章重锤段改写（明确需风弹、
  并列出生效的附魔）；中英 lang 同步。

**四、验证**
- neo 树编译零错；重建 jar 并部署；`test_server.py neoforge1211` 回归 PASS（1.20.1 树不含此功能）。

## 实测四百三十三【冷却 HUD 照搬 HeartPact 显示机制 + 收符窗口数据缺口修复】


**一、证据链（先定位是"没数据"还是"没渲染"）**
- 1.20.1 实例 `pro Maid.log`（promaid.log）显示 22:30:22–22:31:49 服务端**确实**在每秒下发
  `[冷却HUD] KThragg 下发 1 条`（随后 22:31:51 `[自动复活] 汉服酒狐 已在主人重生点复活`）——
  也就是说**服务端数据一直在发，屏幕上却没有 HUD** → 问题在客户端渲染通路，不在数据。
- 另外「只看到身上的效果、没有 HUD」还有第二个缺口：**女仆被收进魂符后实体已移除**，
  旧的回魂符 CD 扫描只查"存活女仆的 persistentData"，这段窗口自然没有任何条目可显示。

**二、显示机制：完全照搬 HeartPact 的分娩倒计时（玩家点名要求）**
- `CooldownHudRenderer` 改为 **`@EventBusSubscriber` 注解常驻注册**（1.20.1
  `Mod.EventBusSubscriber(Bus.FORGE, Dist.CLIENT)`；1.21.1 `EventBusSubscriber(modid, Dist.CLIENT)`）——
  彻底去掉"收到第一个快照才 `EVENT_BUS.register(Class)`"的懒注册（这是本次 HUD 不显示的
  最大嫌疑：懒注册未触发则整个监听器不存在）。
- 绘制事件改为**只认 HOTBAR 那一层**（1.20.1 `RenderGuiOverlayEvent.Post` +
  `VanillaGuiOverlay.HOTBAR`；1.21.1 `RenderGuiLayerEvent.Post` + `VanillaGuiLayers.HOTBAR`）——
  与 HeartPact 同款：每帧在原版热键栏画完时正好画一次。
- 带阴影绘制、按剩余秒升序、时间 `M:SS`（沿用四百三十）。

**三、补上收符窗口的数据缺口**
- `MaidSoulSpellGuard` 新增 `AUTO_SAVED_NAME_TAG`（收符时把女仆名写进魂符本身），并新增
  `charmCooldownUntil(ItemStack)` / `charmMaidName(ItemStack)`（1.20.1 读 NBT；1.21.1 读
  `DataComponents.CUSTOM_DATA`）。
- `CooldownHudTracker` 每个玩家额外扫描其背包里的自动魂符：冷却中即产出
  【回魂符 · <名字> M:SS】条目——**女仆在符里时也能显示**。

**四、可验证性（关键）**
- 客户端新增两条诊断日志（各一次）：`cooldown hud: first snapshot received (N entries)` 与
  `cooldown hud: first draw at y=..`，与服务端「冷却HUD」日志配对——下次若仍不显示，一眼就能
  区分"包没到客户端"还是"到了没画"。

**五、验证**
- 两树编译零错；重建 jar 并部署；`test_server.py 1201` / `neoforge1211` 回归 PASS。

## 实测四百三十二【重锤猛击（1.21.1 专属，参考僵尸用重锤）】

需求：「1.21.1 版本专属更新，女仆可以使用重锤。使用机制就参考 vanilla_mob_remake 里面的僵尸使用重锤。唯一的一个疑点就是要跟落地水平衡防止干扰。同时在拥有风弹的时候可以自己起跳一次。」

**一、机制（反编译 vanilla_mob_remake 的 ZombieMaceAttackGoal 比对实现）**
- 新增 `com.maidsmart.combat.MaidMaceSmashBehavior`（core 行为优先级 235，低于落地水 240、高于单兵战术 230）：女仆**主手持有重锤** + 有存活敌对目标 + 目标在起跳距离（默认 3 格）内 + 冷却（默认 60 tick）就绪 → 朝目标起跳（水平 0.15 / 垂直 1.2，与原 mod 同数值），喷 GUST / GUST_EMITTER_SMALL 粒子 + WIND_CHARGE_BURST 音效；下落中贴地（提前一 tick）或已落地时，若目标仍在 4 格内 → 猛砸。

**二、为什么必须自己算重锤加成（javap 实证）**
- 1.21.1 原版 `Item.getAttackDamageBonus`（重锤下落加成）**全 jar 只有 `Player.attack` 调用**（另有 Mob.doHurtTarget 只走 ATTACK_DAMAGE 属性）——所以**生物拿重锤挥砍是没有下落加成的**（参考 mod 的僵尸实际上也只有"起跳+挥砍"动作，没有真加成）。
- 本实现显式复刻 Player.attack 的那一步：ATTACK_DAMAGE 属性 → `EnchantmentHelper.modifyDamage` → `Item.getAttackDamageBonus`（4f / 12+2(f−3) / 22+(f−8) 三段）→ `hurt` → 附魔后效 → `hurtAndBreak` + `resetFallDistance`。
- 于是女仆的猛砸是**真实加成伤害**，下落越高越疼（最高 +22 以上）。

**三、与落地水的平衡（玩家点名的疑点）**
- 猛击跃起会坠落 8 格以上；若落地水/雪在落点放桶会**清零 fallDistance → 猛砸加成全丢**（两功能互相干扰）。
- 处理：`WaterClutchBehavior` 在猛击跃起期间让开（`suppressesFallClutch`），但**只在目标仍在命中范围内时**抑制——目标跑出 4 格立刻放开，落地水照常保命；命中后 `resetFallDistance`（同原版 postHurtEnemy）也不会摔伤。两头都不牺牲。
- 另外 `MaidCombatTacticsBehavior` 在跃起期间交出移动控制（起跳水平速度只有 0.15，被战术导航覆盖就跳不向目标）。

**四、风弹自我起跳**
- 背包有风弹（可关）时消耗 1 枚，起跳垂直初速 1.2 → 1.7（跳得更高 = 更重的一砸）；没有风弹仍按原 mod 做普通起跳，不会因为没风弹就完全不会猛击。

**五、配置（仅 1.21.1 树，位于「战斗与自保 → 单兵战术」小节）**
- `combat.maceSmash`（默认开）/ `maceWindCharge`（默认开，消耗风弹自我起跳）/ `maceCooldown`（60 tick）/ `maceTriggerRange`（3 格）；手册「单兵战术」章补写重锤段；lang 补 4 条翻译。

**六、验证**
- neo 树编译零错；重建 jar 并部署；`test_server.py neoforge1211` 回归 PASS；1.20.1（1201）树不含此功能、不受影响。

## 实测四百三十一【搭路默认开启 + 配置从「生存与复活」迁到「移动与行为」】


**一、默认开启**
- `bridge.enabled` 默认 `false` → `true`（两树 `MaidSmartConfig`），配置项注释「搭路（默认关）」→「搭路（默认开）」，配置面板 `bridgeRows` 说明末尾「默认关闭」→「默认开启」。
- 关键：已有实例的 `promaid-common.toml` 里早已写入 `enabled = false`，**光改 Java 默认值不会覆盖旧 toml**——所以 6 个本地实例（2 客户端 + 整合包 + 3 服务器）的 `[bridge] enabled` 也一并改成 `true`，否则实测看到的仍是关。

**二、大类迁移**
- `Section.BRIDGE` 的归属大类 `Group.SURVIVAL` → `Group.MOVE`（两树 `PromaidConfigScreen`）；`groupHint` 同步——「移动与行为」加「搭路」，「生存与复活」去掉「搭路」。
- 搭路在功能上确实是移动/接近手段（跟随、拉回、散步、排班同属一类），原先落在生存与复活是分类失误。

**三、手册同步**
- 总览可点链接 `[[@SURVIVAL:BRIDGE|搭路]]` → `[[@MOVE:BRIDGE|搭路]]`（否则点击会跳到错误的大类页）；
- 「七个大类与各自的小类」清单与「怎么找某个开关」把搭路从生存与复活挪到移动与行为；
- 被动技能章「都在〔生存与复活〕大类下…搭路在搭路小类」改写为两处分别标注大类的写法；正文「〔生存与复活 → 搭路〕小节」→「〔移动与行为 → 搭路〕小节」；
- 搭路说明「（v1.1.0，§c默认关§r）」→「（v1.1.0，§a默认开§r）」；`BridgeUpBehavior` 类注释「默认关闭」→「默认开启」。

**四、验证**
- 两树编译零错；重建 jar 并部署全部本地目标；`test_server.py 1201` / `neoforge1211` 回归 PASS。

## 实测四百三十【冷却 HUD 恒不渲染根因修复（F3 门禁用错字段）+ 显示方式对齐 HeartPact】


**一、根因（javap + 玩家 options.txt 双向实证）**
- 四个 HUD 渲染器（两树 × 冷却/建造）的「打开界面 / F3 调试屏不显示」门禁都写成了
  `mc.options.useNativeTransport`——该字段根本不是 F3 标志，且玩家 `options.txt` 里
  `useNativeTransport:true`（默认即 true）→ 条件恒真 → `onGui` 第一行就 `return`，
  **HUD 永远不会被画出来**。
- 正确字段：1.20.1 是 `Options.renderDebug`（SRG `f_92063_`，由 `Gui` 调
  `DebugScreenOverlay.m_94056_` 前的 `getfield Options.f_92063_` 门禁字节码实证）；
  1.21.1 是 `Minecraft.getDebugOverlay().showDebugScreen()`（javap 实证）。

**二、顺带修掉被门禁掩盖的连带缺陷**
- 1.21.1 `BuildHudRenderer` 的 `w/h` 颠倒（`getGuiScaledHeight` 当宽、`getGuiScaledWidth` 当高）——折行/防出屏全按错误尺寸算。
- 1.21.1 `BuildHudRenderer` 折行绘制误用 `drawCenteredString(font, seg, x=4, ...)`——以 x=4 为中心会画到屏幕左外；改为左对齐 `drawString`。

**三、显示方式对齐 HeartPact 分娩倒计时**
- 冷却 HUD 改带阴影绘制（`drawString(..., true)`）；
- 条目按剩余秒升序排列（最紧急在最上，参考其 `lines.sort(...)`）；
- 时间格式改为 `M:SS`（参考其 `formatSeconds`）；
- 1.20.1 从 per-overlay 的 `RenderGuiOverlayEvent.Post` 换成 `RenderGuiEvent.Post`——整帧只画一次，且在所有 overlay 之后触发，读到的建造 HUD 底边一定是本帧最新值（不再依赖注册先后）。

**四、可验证性**
- `CooldownHudTracker` 增加限频核对日志（每 5 秒一条，`promaid.log` 搜「冷却HUD」）：
  下一次测试若仍不显示，可直接用日志区分「服务端没数据」还是「客户端没渲染」。

**五、验证**
- 两树编译零错；重建 jar；已部署全部本地目标；`test_server.py 1201` / `neoforge1211` 回归 PASS。

## 实测四百二十九【睡觉中静默系统气泡与语音】


**一、判定口径**
- 睡觉判定统一用 `LivingEntity.isSleeping()`（1.20.1 SRG `m_5803_`；javap 字节码实证其实现 = `getSleepingPos().isPresent()`，与 TLM `MaidBedTask` 调用的 `startSleeping` 同源，两树同款公共方法）。

**二、改动点（两树各一份）**
- `ChatBubbleLimitMixin.addTextChatBubble`（HEAD）：睡着直接 `-1` 返回——一次性拦住【所有女仆系统气泡】+【同步到主人聊天框】+【由此汇入的 TTS/内置语音包朗读】。覆盖感知/工作/自保/投喂/建造等全部规则气泡。
- `SystemTTSManager.speak` 入口兜底：睡着直接 return——拦住不经 mixin 的直接调用者（排班贴身气泡）与其它触发路径，双保险。
- `ScheduleBubbleBehavior.checkExtraStartConditions`：把原先"简化不判睡"的注释换成显式 `isSleeping` 判定（睡着不聊）。
- `MaidCakeEatHandler`：唯一绕过 mixin 的直发 `addChatBubble` 路径（玩家蛋糕右击投喂）——睡着不再弹蓝色气泡/聊天框消息；回血与好感照常结算，不阻止投喂。

**三、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS（同时验证 mixin 注入点有效）；已部署两个客户端实例（含整合包实例）、三台本地服务器。

## 实测四百二十八【排班气泡日语语音包（49 条，GPT-SoVITS 小酒狐）】

排班里的 50+ 条对话没有语音包。

**一、范围**
- `ScheduleBubbleBehavior` 的排班贴身气泡文本池共 **49 条**（工作吐槽/撒娇/打工人毒鸡汤），此前只出气泡文字、没有语音。

**二、制作（与四百二十同款流程）**
- 用本机 GPT-SoVITS（小酒狐 `小酒狐-e15.ckpt` + `小酒狐_e8_s184.pth`，日语参考音 `この命ご主人様のために捧げますね.wav`）为 49 条逐条合成日语语音；
- 每条合成后立即做峰值归一化（`volume` + `alimiter`，峰值 −1.5 dB，libvorbis q4）——与四百二十七口径一致，保证响度；
- 产物 `sched_01.ogg` ~ `sched_49.ogg` 打进 jar（`assets/promaid/voice/`），两树各一份。
- manifest 追加 49 条 `{中文原文, sched_XX.ogg, mode: exact}`（**追加在原有 54 条之后**，且用 exact 避免短模式串扰）；已脚本校验 49 条全部命中自己的音频、无被既有 contains 规则截胡。

**三、接线**
- `ScheduleBubbleBehavior.start` 冒完气泡后调用 `SystemTTSManager.speak(maid, text)`：命中内置包即播放（含压制 TLM 原生语音包），未命中则沿用原有 磁盘包 → 缓存 → TTS 合成 链路。
- 内置包总条数 54 → **103**（54 系统消息 + 49 排班气泡）；配置面板说明与手册「语音系统」章同步更新。

**四、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS；已部署两个客户端实例（含整合包实例）与 pack1201。

## 实测四百二十七【内置语音包响度修复（素材归一化 + 音量上限提升到 20）】


**一、根因：语音素材本身太轻（不是播放音量不够）**
- 用 ffmpeg volumedetect 实测内置 ogg：**平均 −32 dB、峰值仅 −12 dB**（正常游戏音效峰值应接近 0 dB）。GPT-SoVITS 直出的音频没有做响度处理，所以音量倍率再乘也事倍功半。
- 播放链路本身没问题：`PromaidVoiceSoundInstance` 覆盖 `f_119573_` 为「TTS 倍率 × 内置包倍率」，`MaidTtsVolumeMixin` 的放大被我们的覆盖值取代，不存在双重/被压。位置音衰减（EntityBoundSoundInstance）属正常设计。

**二、修复**
- **素材峰值归一化**：两树各 54 条 ogg 全部重编码，把峰值抬到 −1.5 dB（`volume=<gain>dB,alimiter=limit=0.95`，libvorbis q4）——平均响度 −32 → **−21 dB**，整体约 **+11 dB**。文件体积 2,052,945 → 2,138,974 字节。
- **音量上限 5.0 → 20.0**（`jarPackVolume`，默认仍 1.0）；配置面板「内置语音包音量」说明与手册「语音系统」章同步更新（并修正台词条数 53 → 54）。

**三、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS；已部署两个客户端实例（含整合包实例）与 pack1201。
- 提示：若此前把「内置语音包音量」存成了 5.0，素材归一化后再乘 5.0 会非常响——建议先调回 1.0~2.0 再按需微调。

## 实测四百二十六【女仆复活机制修复：计时口径 + 墓碑右键立即复活 + 时机可配置】


**一、根因（两处硬 bug，均在 旧版就存在）**
1. **到期永远不触发**：登记到期用 `level.getGameTime()`（世界游戏时间，随存档累计、数值很大），每秒检查却用 `server.getTickCount()`（服务器运行刻数，每次启动从 0 开始）。老存档里前者远大于后者，`now >= due` 永不成立 → 自动复活从不触发。
2. **墓碑被清空**：开启自动复活时把墓碑容器清空（旧版为防物品复制），导致右键墓碑拿不到任何东西、墓碑直接消失，看起来就是「点击墓碑复活的方式没了」。另外经 CFA 反编译确认：TLM 原版右键墓碑本来只是**归还物品并删除墓碑**，并不复活女仆。

**二、修复与重做**
- **统一计时口径**：到期与检查都用世界游戏时间 `getGameTime()`；HUD 倒计时同步改口径。
- **不再清空墓碑**：墓碑物品原样保留；开启时右键墓碑 = **立即复活**（新增 mixin `MaidTombstoneReviveMixin` 注入 TLM `EntityTombstone.m_6096_`/`interact`：墓碑属于本功能登记的待复活女仆且点击者是主人 → 销毁墓碑并在主人重生点复活，取消原版取物品）；非本人墓碑仍走原版提示。
- **关闭 = 完全原版**：开关关闭时本功能不介入（不清墓碑、不登记），并在检测到关闭时丢弃待复活表，避免「关掉又打开」对已被取回的墓碑重复复活。
- **复活时机可配置**（照驯养革新宠物床，新增配置项 `autoResurrectTiming`）：
  - `0 = 延迟 N 秒`（默认，用「复活延迟（秒）」，默认 60）；
  - `1 = 次日黎明`（游戏时间 `dayTime % 24000 == 1` 才复活，即 DI 宠物床的次日清晨）。
  两种方式下右键墓碑都能立即复活，不受时机项影响。
- **复活地点**：维持主人重生点（床/重生锚，无则主世界出生点）。
- 配置面板「生存与复活 → 死亡与复活」新增「复活时机」循环按钮；中英翻译补齐；手册「女仆自动复活」章重写说明（右键立即复活、时机两项、关掉=原版、墓碑物品保持原样）。

**三、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS；已部署两个客户端实例与 pack1201。

## 实测四百二十五【主人踩踏刷新扩展到挖矿/伐木/自保垫脚方块】

需求（承接四百二十二）：「这个也应包含，防止玩家跟着女仆挖矿/伐木时掉下去。」——即：不仅搭路方块，挖矿/伐木/自保（搭高）她垫的方块也应在主人踩上去时刷新回收倒计时。

**一、改动**
- 四套搭方块追踪器全部开启 `refreshOnOwnerStand`：
  - `BridgeUpBehavior.PLACED_TRACKER`（四百二十二已开，本次不变）；
  - `MaidMineBehavior.PLACED_TRACKER`：`new PlacedBlockTracker(supplier, 4.0, 6.0, true)`；
  - `MaidWoodBehavior.PLACED_TRACKER`：`new PlacedBlockTracker(supplier, 4.0, 6.0, true)`；
  - `SelfPreservationBehavior.COMBAT_TRACKER`：`new PlacedBlockTracker(supplier, 0.5, 12.0, true)`。
- `PlacedBlockTracker` 字段注释更新为「四套实例全部开启」；`masterOnBlock(...)` 判定逻辑不变（绑定女仆的主人、同维度、踩在上面/脚下一格）。

**二、效果**
- 玩家跟在女仆后面挖矿/伐木/爬战斗塔时，踩到她垫的方块即把该块寿命补满——不会出现「她走开、方块到期消失、玩家正踩着就踩空坠落」。
- 她真正走远（挖矿/伐木/搭路：水平 >4 格或高度差 >6；自保：>0.5 格或 >12）且主人也不在方块上时，才按剩余寿命回收。

**三、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS；已部署两个客户端实例与 pack1201。

## 实测四百二十四【手册详细介绍重写 + 一键跳转配置】

需求：「现在我们对手册详细介绍这一块进行一个重写。同时在聊到某个功能的时候，可以附加一个链接，让玩家跳转到模组详细配置的对应功能处。」

**一、可点击的配置链接（核心新能力）**
- 手册正文新增链接语法 `[[@大类:小类:行标签|显示文字]]`：`GuideScreen` 的换行把整块链接当**原子 token**（按标签宽度计量、不拆行），渲染时画成§青色带下划线§r，并记录命中区；点击 → `PromaidConfigScreen.openAt(...)` 直接打开「模组详细配置」的对应页。
- 两级定位：只给大类 → 进大类页；给到大类:小类 → 进小类参数页；再给行标签 → **翻到该行所在页并金色高亮目标参数行**。
- 跳转用的配置面板以手册为父界面：在配置面板点「保存并返回」会回到手册正文。

**二、手册内容重写**
- 新增首章「功能总览 · 一键跳转配置」：按 7 大类清单列出全部 28 个小类的可点击链接，并给出手动导航路径。
- 每章正文末尾**自动**附加一行「配置入口：点此打开对应配置」链接——`GuideContent.configLinkFor(标题)` 按章节关键词映射到目标小类（建造/挖矿/伐木/烧制/农场/排班/战斗/被动技能/自动复活/床铺互通/贴身辅助/单兵战术/武器切换/AI记忆/对话/感知情绪/AI工具/语音 等），`GuideScreen.buildLines` 统一追加。
- 「手册使用指南」章补充「点链接直达配置」的用法说明；手册内不再有任何开关（承接四百二十三）。

**三、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS；已部署两个客户端实例与 pack1201（rename-swap）。

## 实测四百二十三【模组详细配置面板重组（两级菜单）+ 开关集中化】

需求：「手册里的各功能配置开关和配置项目实在是太多太乱了。我这个开发者都找不到哪个开关在哪里，我觉得需要进行一个改革重写，明确划分，重新分配和分类，细化分配。不再只有原来的那几大块。重新组一个。先声明，所有开关统一扔进模组详细配置界面里，不得出现在其它地方。……配置面板的方式跟原来是一样的，只是我需要对小项进行一个更明确的划分和重组。」

**一、两级菜单（大类 → 小类 → 参数页）**
- `PromaidConfigScreen` 新增 `Group` 枚举（7 大类），重写 `Section` 枚举（28 小类，每个小类记住所属大类）；首页只列大类，大类页列小类，小类页才是参数行（沿用原分页/保存机制）。
- 7 大类 28 小类：
  1. 生产与工作：建造 / 挖矿 / 伐木 / 烹饪与酿造 / 农场与宰杀
  2. AI 与对话：AI 记忆 / 对话与自主 / 感知 / 情绪 / AI 工具
  3. 战斗与自保：自保与保命 / 自保战术 / 单兵战术 / 主动参战 / 贴身辅助 / 玩家伤害策略
  4. 生存与复活：落地缓冲 / 搭路 / 死亡与复活 / 传送与逃生 / 女仆安全与区块
  5. 移动与行为：移动与跟随 / 空闲与流畅 / 排班表
  6. 语音与显示：语音与 TTS / 显示与提示
  7. 系统与杂项：交互与杂项 / 运行日志
- 原来混在「战斗自保 / 被动技能 / 杂项」三大页里的约 230 条参数按配置键重新分配；`passiveRows`/`combatRows`/`miscRows` 拆成 19 个细化小类方法。原 12 板块 → 7 大类 28 小类。
- 归类示例：「挖矿中禁止拾取」→ 挖矿；「任务垂直范围」→ 烹饪与酿造；「收符冷却」→ 死亡与复活；「树苗/作物骨粉催熟」→ 农场与宰杀；「冷却可视化 HUD / 气泡限频」→ 显示与提示。
- 两级菜单共用 `menuGrid`（两列竖排、行距按数量/窗口高度自适应，永不与底部按钮重叠）；小类页「← 返回分类」、大类页「← 返回大类」。
- 完整性脚本校验（对重组前备份）：BoolRow 112 / NumRow 175 / BtnRow 6 / CycleRow 3 / TextRow 5 / InfoRow 1 完全一致；配置键无丢失、无重复。

**二、开关集中化（手册内不再有开关）**
- 移除手册目录页的「自动复活设置」「日语语音包设置」两个设置页入口——所有开关/参数只在「模组详细配置」面板里调（要求）。两个设置页相关代码标记 @Deprecated 保留（不可达，仅备回滚）。
- 手册文本全面更新到新路径：导航章重写为 7 大类 28 小类清单 + 「怎么找某个开关」指引；各章交叉引用同步改为新路径（原来大量「杂项 → X」「战斗自保 → Y」的旧写法全部改掉）。

**三、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS；已部署两个客户端实例与 pack1201（rename-swap）。

## 实测四百二十二【搭路方块 CD·主人踩踏也重置】

需求：「现在搭路方块只有在被女仆踩了以后才会被重置CD，更改为主人踩到了也会重置。」

**一、改动**
- `PlacedBlockTracker`（两树）新增第 4 个构造参数 `refreshOnOwnerStand`（默认 false，挖矿/伐木/自保方块不受影响）；
- `expirePlaced` 的刷新判定在原有「绑定女仆踩着 / 任意女仆踩着 / 绑定女仆在附近」之外，新增 `masterOnBlock(...)`：当实例开启该开关时，绑定女仆的【主人】（玩家）正踩在该方块上（脚位同格或脚下一格）也把该块寿命补满；同维度才判，女仆离线时自然不成立（那时方块本就暂停倒计时）；
- `BridgeUpBehavior`（两树）的搭路追踪器改用 `new PlacedBlockTracker(supplier, 4.0, 6.0, true)` —— 只有搭路开这个开关。

**二、效果**
- 主人踩着女仆搭的桥走，每踩到一块就把那一块的回收倒计时补满：谁（女仆或主人）在桥上走，脚下那块就不会断；两人都走远才按剩余时间回收。

**三、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；重建 jar 后 `test_server.py 1201` / `neoforge1211` 回归 PASS；已部署两个客户端实例与 pack1201（rename-swap）。

## 实测四百二十一【冷却可视化 HUD（女仆复活倒计时 / 回魂符冷却）】

需求：「我希望女仆复活的CD及自己回魂符的CD在玩家屏幕上可视化。」

**一、服务端倒计时采集**
- `MaidAutoResurrect`：`Pending` 增加 `maidName`（墓碑生成时用 `PromaidLog.nameOf` 记录，随 SavedData 持久化；旧存档缺字段回退「女仆」）；新增 `hudReviveEntries(server, ownerId)` → {名字, 剩余秒, 总秒}，倒计时刻与到期判定同源（`getTickCount()`），并夹紧到配置的复活延迟上限；
- `MaidSoulSpellGuard`：新增 `hudCooldownSeconds(maid, nowTick)`，读女仆 persistentData 的 `maid_hp_low_protect_cooldown_until` → {剩余秒, 总秒}；未在冷却返回 null（收在魂符里时本就安全，不显示）；
- 新增服务端 `CooldownHudTracker.broadcast(server)`：每秒（ProMaidExtension 与建造 HUD 同节拍）全图扫一次存活女仆按主人分桶，叠加待复活表，逐玩家打包下发；只发本人女仆、无条目不发（客户端超时自清）。

**二、网络与客户端 HUD**
- 新增 S2C `CooldownHudPacket`（1.20.1 手册频道 id 26 / Neo `playToClient`），每项 {kind, 名字, 剩余秒, 总秒}，kind = `revive` / `soul`；
- 新增客户端 `CooldownHudRenderer`（两树）：左上角「冷却」列表，`复活 · 名字 42秒` / `回魂符 · 名字 18秒 / 60秒`；行高 10px 逐行步进、按 `BuildHudRenderer.bottomY()` 排在建造进度下方不重叠、打开界面/F3 不画、超出可用高度不画、超 2.5 秒没收到新快照自动清空（不残留旧倒计时）；`BuildHudRenderer` 两树新增 `bottomY()` 记录本帧底边。

**三、配置与文档**
- 杂项新增 `冷却可视化 HUD`（`misc.cooldownHud`，默认开，两树）+ 中英翻译 + 配置面板行；手册「女仆自动复活」章补充冷却可视化说明。

**四、验证**
- 两树 `run_javac6.py` / `run_javac_neo.py` 编译零错；
- 重建 jar：`patched/promaid-1.1.0.jar` 6,936,317 字节、`patched/promaid-1.1.0-neoforge-1.21.1.jar` 6,946,795 字节；两个 jar 各含 `CooldownHudTracker.class`、`CooldownHudRenderer.class`、`BlueprintBookNetworking$CooldownHudPacket.class`；
- `test_server.py 1201` 与 `test_server.py neoforge1211` 回归均 **PASS**；已部署到两个客户端实例与 pack1201 服务器（服务器用 rename-swap，无需停服）。

## 实测四百二十【内置日语语音包（GPT-SoVITS 训练，打进 jar）】

需求：「帮我训练一个语音包，日语，就用桌面上这个 API.bat 来进行训练。训练的内容就是目前游戏里的那些系统消息语音，打包放进 .jar 里。效果就是当触发了这些系统消息之后，会自动播放这个语音。可以在手册里调整开关和音量大小以及最小间隔，播放此语音时会暂时卡掉 tlm 原来的语音包，播放完成后解除，有比原生语音包更高的优先级。」

**一、语音素材（GPT-SoVITS 合成 → OGG）**
- 用本机 GPT-SoVITS（`API.bat`，权重 `小酒狐-e15.ckpt` + `小酒狐_e8_s184.pth`、日语参考音 `この命ご主人様のために捧げますね.wav`）合成 **53 条日语台词**；
- 覆盖范围 = 全部 49 个系统消息点（建造完成/缺料/替代/跳过 ×4 类、投喂/治疗/蜂蜜/牛奶/背包满、复活/死亡传送/盾牌共享、自保 8 条、落地水/雪、钓鱼 4 条、不死图腾 3 条、排班锁定、搭路 2 条、酿造缺料、挖矿伐木 6 条、攻击应答、发现矿/前方有）+ 4 条兜底（"缺…"/"有个…"等动态前缀）；
- 匹配语义：manifest **自上而下、contains 优先**（更具体的模式排前面，`totem_peer` 必须先于 `totem_self` 这类包含关系）；动态消息（带坐标/方块名）在运行时是全串拼接，用静态片段（如"取代一下"/"被基岩之类的方块挡住了，我跳过它啦"）匹配；
- ffmpeg 转 vorbis ogg（q4、约 32kHz 单声道），连同 `manifest.json` 打进 jar：`assets/promaid/voice/`（**两树各一份，53 ogg + manifest**）。

**二、播放链路（服务端匹配 → 只发文件名 → 客户端本地取字节）**
- 新增 `com.maidsmart.voice.JarVoicePack`：从 jar 内 classloader 读 manifest + ogg（惰性 + 内存缓存），提供 `matchKey(text)` → 文件名；
- `SystemTTSManager.speak` 提升为**四段优先级**：**① 内置日语语音包（最高）** → ② 磁盘系统语音包（`config/maid_smart/system_voice/`）→ ③ TTS 语音缓存 → ④ TLM TTS 站点合成；
- 内置包放在**站点/总开关检查之前**：只认自己的开关 + 自带最小间隔，**不要求配置 TTS 站点**（本地有音频，开箱即用）；
- 新增 S2C 包 `PlayJarVoicePacket`（1.20.1 走手册 SimpleChannel id 25；1.21.1 走 `playToClient`）——**只下发女仆实体 id + 文件名**，客户端从自己的 jar 取同名 ogg 播放（省流量、两端一致）。

**三、音量与「播放时压制原生语音包」**
- 新增 `PromaidVoiceSoundInstance`（客户端，继承 TLM `MaidAISoundInstance` 复用其 OGG/Opus/MP3 解码），构造后覆盖音量字段 = `TTS 音量倍率 × 内置包音量倍率`（1.20.1 写 `f_119573_`；1.21.1 写 `volume`）；
- 新增 `ClientVoicePlayback`：播放前按 ogg 字节估算时长（≈7000 字节/秒 + 0.5s 余量）开启**压制窗口**；窗口内监听 `PlaySoundEvent`，把 TLM 原生语音包实例 `setSound(null)`——1.20.1/1.21.1 的 `MaidSoundInstance`（女仆语音/音效）、1.21.1 另有的 `MaidSoundInstanceAtPos`（定点音效，按类名判定以跨版本安全）；**窗口到期自动解除**；`MaidAISoundInstance`（TTS/内置语音本体）不压；`isTestSound()` 试听音放行；客户端 tick 兜底在未进世界时清窗口；
- 连续两条语音时压制窗口顺延不缩短。

**四、配置与手册**
- 新增 4 个配置项（两树）：`TTS_JAR_PACK_ENABLED`（默认开）、`TTS_JAR_PACK_VOLUME`（1.0，0.1~5.0）、`TTS_JAR_PACK_MIN_INTERVAL_S`（8，0~60）、`TTS_JAR_PACK_MUTE_NATIVE`（默认开）；
- 配置面板「语音 → 内置日语语音包」4 行；**手册目录页新增「日语语音包设置」整页**（开关 + 音量 + 最小间隔，与「自动复活设置」并排；输入延迟提交、越界钳制，关界面/返回时统一写盘）；四份 `lang/*.json` 补条目；手册「语音系统（TTS）」章节补写内置日语语音包段落。

**验证**：两树全量编译零错误；jar 重建（1.20.1 = 6,927,410 字节 / 含 54 个 ogg 共 2,052,945 字节；1.21.1 = 6,937,705 字节）；1.20.1 与 1.21.1 服务器回归均 PASS；匹配自检（按 manifest 自身顺序 + contains 语义跑全部 49 条运行时消息样本）全部命中（仅 2 条"变量在中间"的样本是自检脚本无法拼全，运行时含映射片段必命中）。已部署到两个客户端实例与 pack1201 服务器。**未提交 GitHub，等玩家测试。**

## 实测四百一十九【排班表快捷调整：点任务名进整页选择】

需求：「排班表的快捷调整也引入和排班一样的机制，不再仅用 ◀ ▶ 翻页，而是点任务名进整页选择。」

**改动**
- 背景：第 2 页「排班」的任务槽此前已支持「点任务名 → 整页任务选择」（实测四百零二/四百零六），但第 1 页「快捷设置」的任务行仍只有 ◀ ▶ 循环 + 点中间换下一个——任务多时要循环点十几下。本次把整页选择推广到快捷页。
- 两页共用同一套整页选择实现（`pickPageButtons`），靠新增字段 `pickQuick` 区分来源与去向：
  - **快捷页进入**（点任务名）：选中任务 → 走既有 `QuickApplyPacket` **立即生效**（服务端仍校验排班锁定）→ 返回快捷设置页；「空闲」按钮 = 切到 TLM 空闲任务 `touhou_little_maid:idle`；左上返回显示「← 返回快捷设置」；标题提示「点击任务立即切换她的当前任务」。
  - **排班页进入**（点任务槽）：选中任务 → 填入该时段槽 + 置未保存脏标记 → 返回排班页；「空闲」按钮 = 清空槽；左上返回显示「← 返回排班」；标题提示「点击任务填入第 N 时段」——**原行为完全不变**。
- 两侧 ◀ ▶ 箭头全部保留（旧习惯不变）；当前任务/当前槽的「●」标记按来源取对应当前值。
- 渲染层选择页标题提示也随来源切换，避免快捷页误显示"填入第 N 时段"。

**两树同步**（1.20.1 SRG / 1.21.1 Mojmap）。**验证**：两树全量编译零错误；jar 重建（1.20.1 = 4,901,239 字节，1.21.1 = 4,911,622 字节）；1.20.1 与 1.21.1 服务器回归均 PASS。已部署到两个客户端实例与 pack1201 服务器。**未提交 GitHub，等玩家测试。**

## 实测四百一十八【床铺互通：女仆床 ↔ 玩家床】

需求：「让女仆床和玩家床的代码互通。女仆和玩家可以互相使用对方的床。」

**堵点（javap/CFR 实证）**
- **女仆睡不了原版床**：TLM 的 `MaidBedTask`（REST 活动里唯一的睡觉行为）用 POI 只查 `InitPoi.MAID_BED`，且 `start` 里硬判 `state.is(InitBlocks.MAID_BED.get())` —— 原版 16 色床既不进这个 POI 类型，也不满足这个 `is()`。
- **玩家睡不了女仆床**：`BlockMaidBed.isBed(...)` 只在 `entity instanceof EntityMaid` 时返回 true，玩家（及重生点解析传的 null）走 `super`（= `instanceof BedBlock`）恒 false；且它的 `use`/`useItemOn` 只处理染料，非染料直接 PASS，从不调 `startSleepInBed`。
- **可扩展点现成**：Forge/NeoForge 都已把 `LivingEntity.startSleeping` 与玩家重生点解析改为 `BlockState.isBed` / `setBedOccupied` / `getRespawnPosition` 钩子；`BlockMaidBed` 的 `OCCUPIED`/`PART` 与 `BedBlock` 是**同一 Property 实例**，`FACING` 来自 `HorizontalDirectionalBlock` —— 门一开，原版睡眠/占床/重生逻辑直接跑通。

**一、方向一：女仆能睡原版床（两树）**
- 新增 `com.maidsmart.task.MaidBedInteropBehavior`（模板照 `MaidStrollBehavior`），经 TLM 扩展点 `IExtraMaidBrain.getRestBehaviors()` 注册（两树此前都未覆写该钩子），优先级 6 —— 排在 TLM 自带 `MaidBedTask`(5) 之后、随机散步(20) 之前，**女仆床优先**；
- 用原版 `PoiTypes.HOME` 找最近的原版床（原版把 16 色床 HEAD 都注册进 HOME），校验 `is(BlockTags.BEDS)` + `PART==HEAD` + `!OCCUPIED`（防别的模组往 HOME 塞非床方块），有 home 限制时要求床在活动范围内；
- 够远用 `WalkTarget` 走过去，到 2 格内调 `startSleeping(headPos)` + `setPos`，与 TLM 自己的 `MaidBedTask.start` 同款 —— 占床、回血、好感度全部走 TLM 既有逻辑。

**二、方向二：玩家能睡女仆床（两树）**
- 新增 `MaidBedBlockInteropMixin`（`@Mixin(BlockMaidBed.class)`，登记进 `mixins.promaid.json`）：
  1. `isBed` HEAD 注入：`entity == null || entity instanceof Player` 时返回 true（null 必须放行，否则重生点解析失败；女仆仍走 TLM 原 `EntityMaid` 分支，不放行其他生物）；
  2. `use`(1.20.1 `m_6227_`) / `useItemOn`(1.21.1) HEAD 注入：手持可染色染料 → 放行走 TLM 原染色逻辑（染色优先）；潜行右键 → 不躺下；否则服务端调 `startSleepInBed(head)`，占用时发原版「床已被占用」提示；
  3. **1.21.1 专用**：显式实现 `getRespawnPosition`（NeoForge `IBlockExtension` 默认恒 `Optional.empty()`，而 1.21.1 重生点解析只在非强制分支问这个钩子——不实现则睡下后死亡会报「重生方块缺失」回世界出生点），委托 `BedBlock.canSetSpawn` + `findStandUpPosition`，与睡原版床算法一致。1.20.1 Forge 的接口默认实现本身已走 `isBed`，无需额外方法。
- `setBedOccupied`/`getBedDirection` 用平台默认实现（属性实例共享 + FACING 齐全），不改。

**三、构建与开关**
- 两方向共用配置 `misc.bedInterop`（默认开），面板「杂项 → 床铺互通」一行，关掉完全恢复 TLM 原版；四个 `lang/*.json` 补条目；手册新增「床铺互通（女仆床 ↔ 玩家床）」章节；
- **neo 编译 classpath 顺序修正**：把 NeoForge 修补 jar 排到 vanilla 之前 —— `ServerPlayer$RespawnPosAngle` 在未修补 vanilla jar 里是**包私有**、在 NeoForge 修补版是 public，顺位反了「给女仆床实现 getRespawnPosition」编译不过（javac 实证）。

**验证**：两树全量编译零错误；jar 重建（1.20.1 = 4,900,330 字节，1.21.1 = 4,910,712 字节）；1.20.1 与 1.21.1 服务器回归均 PASS（日志无任何 promaid 相关 mixin 报错）。已部署到两个客户端实例与 pack1201 测试服务器（服务器 jar 用 rename-swap 替换，重启后加载）。**未提交 GitHub，等玩家测试。**

## 实测四百一十七【手册内自动复活设置页】

需求：「自动复活功能应该在手册里面也能够调整 CD 和开关。」

**改动**
- 《详细介绍》章节目录页右下角新增 **「自动复活设置」** 按钮，进去是一页 **总开关 + 复活延迟（秒）+ 复活血量比** 三项控件，改完「保存并返回目录」即时生效，不必再跳「模组详细配置」面板；
- 输入交互完全照配置面板的既有管线：点击输入框显式聚焦并自跟踪 `activeBox`，键盘输入经 `charTyped`/`keyPressed` 直接转发（1.20.1 走 `m_5534_`/`m_7933_`）——避开 1.20.1 焦点链差异导致的「点得进打不了字」；数字文本延迟提交（输入时只校验+红/白字，离开本页或关闭界面时统一写入 + `SPEC.save()`），不在输入路径上写配置，杜绝旧版「每按键 `set()` 卡输入」；
- 越界值钳制到配置声明范围（延迟 1~86400 秒、血量比 0.05~1.0），非法/空文本跳过并保留原值；
- 「详细介绍」新增 **「女仆自动复活（v1.1.0）」** 章节：运作链路（死亡抓快照 → 墓碑标记 → 到期消失 → 重生点复活）、话语提示、血量比、保姆式测试步骤，以及 **在哪里调开关与 CD**；顺手把手册里一处过期的「详见第 20 章」改为按章节标题引用（插入新章节不再导致编号漂移）；
- 设置页布局用「控件与渲染共用同一套坐标」的 `settingsLayout()`（行高按窗口高自适应、说明文字按可用高度取舍），小窗口不重叠/不压按钮。

**两树同步**（1.20.1 SRG / 1.21.1 Mojmap）。**验证**：两树全量编译零错误；jar 重建（1.20.1 = 4,892,596 字节，1.21.1 = 4,902,422 字节）；1.20.1 与 1.21.1 服务器回归均 PASS。已部署到 1.20.1 / 1.21.1 两个客户端实例与 pack1201 测试服务器（服务器 jar 用 rename-swap 替换成功，其重启后加载新版本）。

## 实测四百一十六【女仆自动复活（新功能）】

需求：女仆死亡后 60 秒，墓碑自动消失，她随即在主人出生点复活（也是 60 秒 CD）。

**一、死亡→墓碑→复活链路**
- 监听 TLM 的 `MaidDeathEvent` 在死亡瞬间抓取女仆**完整存档 NBT**（`saveWithoutId`，此时背包/饰品尚未被搬走）；再监听 `MaidTombstoneEvent` 拿到墓碑引用，把「快照 + 主人 UUID + 到期刻 + 墓碑 UUID」写入新增的 `MaidAutoResurrect` 持久化表（SavedData 存主世界，跨维度共享，重启不丢）；
- 延迟到期（默认 60 秒）→ 自动让**墓碑消失**（`discard`，走 TLM 的 `remove` 钩子同步清理 `MaidWorldData` 墓碑记录）→ 在**主人重生点**复活女仆（床/重生锚；无重生点则主世界出生点，复用 `MasterDeathTeleportHandler` 的校验与安全落点）；
- 复活时**女仆用话语提示玩家**（气泡「主人，我回来啦！让你担心了～」+ 主人的系统消息「你的女仆 XX 已复活」+ `promaid.log` 记录）；复活实现与 TLM 的「魂符释放」同构：`new EntityMaid(level)` → `load(nbt)` → 复用**原 UUID**（记忆/灵魂目录按 UUID 索引）→ 设置落点与血量 → `addFreshEntity`。背包/饰品/任务/模型全部原样恢复。

**二、防物品复制的关键设计**
- 抓取快照后立即**清空墓碑容器**（墓碑转为纯标记）——物品只存在于死亡快照中，复活时随 NBT 归还。否则 60 秒窗口内玩家可拾取墓碑内容，复活又归还快照 → 物品翻倍。

**三、安全与边界**
- 复活血量默认满血（`autoResurrectHealthRatio` 可调，0.35 = 三成半）；`deathTime`/摔落距离/速度全部清零，避免"复活即濒死/摔伤"；
- **主人不在线**：墓碑保留、等她上线再复活（快照不丢）；复活过程异常时推迟 5 秒重试，不丢女仆；
- 无主女仆不自动复活（没有可去的重生点，保持原版死亡流程）；
- 开关 `combat.autoResurrectEnable`（默认开）+ 延迟 `autoResurrectDelaySeconds`（默认 60 秒）+ 血量比 `autoResurrectHealthRatio` 三项，配置面板「战斗自保 → 女仆自动复活」小节可调；关掉恢复 TLM 原版流程。

**验证**：两树全量编译零错误；jar 重建（1.20.1 = 4,884,363 字节，1.21.1 = 4,894,075 字节）；1.20.1 与 1.21.1 服务器回归均 PASS。

## 实测四百一十五【彻底移除第三方软联动：爱憎分明 / 心契誓约 / 超越维度 / 精妙储存】

判定口径：保留「按攻击力属性」的通用武器联动不动（tacz / 卓越前线 / 拔刀剑 / 万法皆通等模组武器照常参与判定与自动装备），只把**对特定第三方模组的专属联动**彻底删干净——爱憎分明、心契誓约、超越维度、精妙储存相关的一切代码与资源全下线。

**一、精妙储存 / 超越维度（存储联动全删）**
- 删除 `com.maidsmart.storage` 整包：`BoundStorageInteractHandler`、`StorageBindingStore`（以及此前已删的 Beyond/Unified 系列）；
- 删除两个绑定卡物品 `storage_bind_card` / `storage_unbind_card` 及其注册项、创造标签页入口、主类事件注册；
- 删掉全部调用点：建造材料统计（`BlueprintLib.countMaterial`）、建造取物回退（`BlueprintLib.consumeBlock`）、酿药取物回退（`MaidBrewBehavior.extractItemFromMaid`）；
- 删除配套资源：4 个模型/贴图 + 2 个配方 JSON + 4 个语言键。

**二、爱憎分明（Love Loathe）**
- 删除配置项 `MISC_LOVELOATHE_DISABLE_HUNGER` / `_MASTER` / `_EXTREME_HUNGER` / `_EMOTION` 四项及其界面行、语言键；
- 删除配置面板「爱憎分明模组调试」整页（含 `loveloatheVersion()` 反射取版本、`probeClass()` 接口探测）；
- 删除自保的极端饥饿反射保命逻辑（原反射读 `HungerData`）+ 相关注释，自保回血链改为纯治疗食物/药水/金苹果阶梯；
- `AiMemoryExtractor` 的"纯模组注入状态消息跳过"规则保留（属通用健壮性），但改为不含特定模组名的通用描述。

**三、心契誓约（maidmarriage / Heartfelt-connection）**
- 删除 `MaidConfigMemoryMixin` 中 `RelationshipExemption.relationLabel` 反射句柄与关系标签渲染；
- 删除情绪层的纪念日脉冲 `AffectManager.onAnniversaryDay/onAnniversaryApproaching`，以及 `BlueprintBookNetworking` 的纪念日调试行、手册 Heartfelt 纪念日联动配置项；
- 删除建造 `MaidBuildBehavior.tickBuildSit` 的心契拥抱交互锁说明与相关判定；
- 清理 `LlmEnableManager` / `MaidChatLlmGateMixin` / `PromaidPromptMixin` 等类注释里的跨模组表述。

**四、其它同批清理**
- 删除 `callresponse` 相关语言键（`bubble.callresponse.*`，共 4×2 语言）与配置面板注释；
- 配置项 `MEMORY_RELATIONSHIP_ADAPTER` / `MEMORY_RELATION_SCAN` / `MEMORY_TRUST_DELTA` / `MEMORY_HEARTFELT_ANNIVERSARY` 四项及其界面行全部删除；
- 手册「详细介绍/术语表/配置面板导航」中所有相关条目重写为纯独立版描述；
- 两树 `mods.toml` / `neoforge.mods.toml` 描述去掉第三方联动声明。

**保留（未动）**：TLM 本体（唯一前置）、按攻击力属性识别的通用武器判定与自动装备、本模组自有全部功能（建造/排班/AI 记忆/战斗自保/任务/交互等）。

**验证**：两树全量编译零错误；两 jar 经审计确认无任何 storage/爱憎/心契类与资源残留、语言键无悬挂；1.20.1（Forge 47.4.x）、1.21.1（NeoForge 21.1.250）、95 模组整合包服务器三处回归全部 PASS。

## 实测四百一十四【主人免伤三层加固 + 移除第三方软联动（纯 TLM 独立版）】

需求：再加保险——清除仇恨 + 取消女仆攻击主人的受击事件；同时删除对其他 mod 的软联动；覆盖两个正式版并更新主页面介绍。

**一、主人免伤第三层（在四百一十三的"事件总闸 + 注入点过滤"之上）**
- **定期清除仇恨**（`FriendlyFireGuard` 新增 ServerTick 扫描，每 2 秒）：凡女仆的 `ATTACK_TARGET` / 实体目标（`setTarget`）/ 复仇目标（`getLastHurtByMob`）是主人或同主友方 → 直接清掉（日志搜 `friendly-fire hate-clear`）。TLM 打雪仗等娱乐行为写入的目标不再残留成"敌对状态"；
- **攻击事件取消**：1.20.1 增加 `LivingAttackEvent`（与 `LivingHurtEvent`/`LivingDamageEvent` 共三层）；1.21.1 为 `LivingIncomingDamageEvent`。女仆造成的主人/友方伤害在受伤链最上游即被取消；
- 配合四百一十三的注入点过滤（战术 isActive/跳劈/横扫、中立威胁、自保反击含弹幕射箭、LLM 攻击工具）——**女仆对主人零伤害**。

**二、删除第三方软联动（按判定口径：只删第三方 mixin + 存储/关系联动；保留按攻击力属性的通用武器逻辑）**
- 删除第三方 mixin：`LoveLoatheHungerGateMixin`（爱憎分明饥饿门控）、`MixinInteractionSittingAllow`（心契誓约拥抱/子女交互）、`MaidDebugPanelMixin`（心契誓约调试面板）、废弃的 `CallResponseChatSpamMixin`；
- 删除 Beyond Dimensions（超越维度）存储联动：`BeyondBindingInteractHandler`/`BeyondBindingStore`/`UnifiedStorageItemHandler` 三个类、`beyond_bind_card`/`beyond_unbind_card` 两个物品（含模型/贴图/配方/语言）及其全部调用点（BlueprintLib 材料统计与取物、女仆酿造取物兜底、创造页签、主类注册）；
- 删除关系联动：`RelationshipMemoryAdapter`（心契/爱憎关系记忆反射适配）及其调用点（AI 上下文关系标签、武器守卫儿童判断改回 `isBaby`）、建造心契交互锁反射、自保极端饥饿反射（爱憎分明 `HungerData`）、手册 Heartfelt 纪念日联动、配置面板「爱憎分明模组调试 / heartfelt 联动」两个板块；
- 保留：TLM 本体（唯一前置）、按攻击力属性的通用武器判定/自动装备（tacz/卓越前线/拔刀剑/万法皆通等模组武器天然参与，不依赖专属代码）、精妙储存终端绑定（本模组自有功能）。

**部署**：两树同步；1.20.1 jar 4,889,089 字节、1.21.1 jar 4,898,745 字节；三客户端实例 + 三测试服务器；1201 与 neoforge1211 服务器回归 PASS。

> 注：移除第三方软联动后，本模组为**纯 TLM 独立版**（只要求车万女仆前置）。此前装的软联动模组（心契誓约/爱憎分明/Heartfelt/超越维度等）与本模组不再有代码交互；对应联动功能（爱憎饥饿保命、心契交互锁、超越维度终端绑定卡、关系记忆标签等）随之下线。

## 实测四百一十三【女仆绝不伤害主人/友方——雪地打雪仗后对主人跳劈修复】


**根因**：TLM 原版空闲模式在雪地有打雪仗行为（`MaidStartSnowballAttacking`），该行为会把【主人】写进女仆 brain 的 `ATTACK_TARGET`（雪球目标）。promaid 的单兵战术行为（`MaidCombatTacticsBehavior`，v1.5.202 起不再限定战斗任务）读到 `ATTACK_TARGET` 就接管走位 + 跳劈，用 `target.hurt(mobAttack, 攻击力×1.5+附魔)` 对主人打出**真实近战暴击伤害**（主手武器攻击力越高伤害越高 = "拿刀更疼"）。旧版只拦了"雪球 0 伤害触发参战"这一层（`AutoCombatSwitch`），拦不住 TLM 自己写攻击目标这条路径。

**修复（双层防护，1.20.1 与 1.21.1 两树同步）**：
- **最终保险（新增 `FriendlyFireGuard`）**：监听伤害事件（1.20.1 `LivingHurtEvent`+`LivingDamageEvent`；1.21.1 `LivingIncomingDamageEvent`），**女仆造成的伤害只要受害者是主人/同主女仆/友军 → 直接取消**（覆盖所有路径，含 TLM 自身与未来新增）；
- **注入点过滤（12 处）**：战术行为 `isActive`（主人/友方直接不激活）、`swingCritHit`（跳劈暴击）、`meleeCounterAttack`（近战横扫）、中立威胁反击 `doHurtTarget`、自保近战反击 `doHurtTarget`、LLM 攻击工具 `SmartAttackTool`（拒绝把主人/友方写进 ATTACK_TARGET）——目标为主人/友方时一律不执行，连动画都不出。
- 友方判定口径：自身 / 主人（UUID 比对）/ 同主人的其他女仆 / `isAlliedTo` 同队友军。

**部署**：（1.20.1 jar 4,926,514 字节；1.21.1 jar 4,936,516 字节）→ 三实例客户端 + 三测试服务器；1201 与 neoforge1211 服务器回归 PASS（Done 正常，无注入/加载错误）。

验收：雪地让女仆打雪仗 → 她仍会对你扔雪球（TLM 娱乐行为保留），但**绝不会**冲你跳劈/挥刀，主人不掉血；主手换任何武器都不再影响主人受到的伤害；同主女仆之间互不误伤。

## 1.21.1 NeoForge 版（beta）【首发实测修复 ×4】

1.21.1 NeoForge 移植版首次实机测试（进服放女仆→手册/排班表→建造进度条全流程）后修复的 4 个问题：

**1. 放女仆即崩——MoveToTargetSinkDurationMixin 注入失败**
- 根因：原版 MoveToTargetSink 的 150/250 常量在**无参构造器**里（(II)V 构造器只是参数透传）；此前批量加描述符时被误改为 `<init>(II)V` → 注入点扫描 0 目标 → MixinApplyError。放女仆触发 MaidBrain.registerCoreGoals 加载该类即崩（服务器测试不生成女仆，懒加载问题只在客户端暴露）；
- 修复：精确指向无参构造器 `<init>()V`。同时用 javap 逐一验证其余 13 个被改过描述符的 mixin 目标方法在 TLM 1.21.1 中真实存在。

**2. 首次进服不发手册/排班表**
- 根因：SRG `m_36054_`（Inventory.add，放入背包）被误映射为 `contains`（只检查不放入）——标记写了、提示发了，书从未真正进背包；
- 修复：改回 `add`，并改为按背包实际缺什么补什么（坏版本进过服的存档自动补发缺失的书），全部放入成功才写"只送一次"标记。

**3. 手册/配置等 6 个 Screen UI 图层错乱（文字发暗、渐变带被盖）**
- 根因（字节码实证）：1.21.1 的 `Screen.render()` 开头自动调用 `renderBackground`（游戏内=全屏模糊+菜单底纹），1.20.1 不会——render() 先画好的自定义背景与文字被 super.render() 内部的背景再盖一层；
- 修复：6 个 Screen（酿造手册/蓝图书/配置页/文件选择/内置手册/排班表）重写 `renderBackground` 为空 + render() 开头显式画 1.20.1 语义背景（游戏内半透明黑渐变）。

**4. 蓝图书建造进度条洋红污染**
- 根因：1.21 删除合并纹理 `textures/gui/bars.png`（拆为独立精灵 `hud/experience_bar_background/progress.png`）——blit 引用缺失纹理 = 拾取洋红棋盘格；
- 修复：改用 `blitSprite` 精灵 API 画暗底+亮绿进度（与原版经验条同款观感）。

## v1.1.0 正式版【专用服务器兼容修复 ×2】


**修复一：主类内联配置界面 lambda 触发 RuntimeDistCleaner 拦截（issue #3）**
- 根因：`ProMaidMod` 构造函数里内联注册配置界面扩展点，lambda 编译成主类合成方法，方法描述符带 `net.minecraft.client.gui.screens.Screen`；Forge 专用服务器加载主类时被 RuntimeDistCleaner 拦截 → `Attempted to load class net/minecraft/client/gui/screens/Screen for invalid dist DEDICATED_SERVER` → mod 加载失败、服务器启动中止；
- 修复：带客户端类型签名的 lambda 全部移入客户端专类 `com.maidsmart.client.PromaidClientSetup`，主类只在 `dist.isClient()` 分支静态调用——服务端不执行该分支，客户端类永不加载；
- 验证：旧 jar 复现崩溃 → 新 jar 服务器 `Done` 正常启动（Forge 47.4.23 与 47.4.21 双版本回归 PASS）。

**修复二：主动对话调用 TLM 客户端专用方法 ChatClientInfo.fromMaid（issue #4）**
- 根因：`ChatInfoUtil.fromMaid` 直接调用 TLM 的 `ChatClientInfo.fromMaid(EntityMaid)`——该方法标注 `@OnlyIn(Dist.CLIENT)`（内部用 `Minecraft.getInstance()` 读客户端语言、`CustomPackLoader.MAID_MODELS` 读模型描述），专用服务器加载 TLM 时被 RuntimeDistCleaner 剥离 → 服务端触发主动对话即 `NoSuchMethodError` → 服务器 tick 崩溃；
- 修复：不再调用 TLM 客户端工厂，改为服务端安全地自行构造 `ChatClientInfo`——语言取配置（默认 zh_cn），名字取女仆实体名，描述留空；
- 验证：javap 实证 TLM 1.5.3 jar 中该方法带 `@OnlyIn(CLIENT)` 注解；修复后 62 mod 主整合包服务器启动 PASS、tick 正常运行。

> 说明：开发者平时只玩单人模式，mod 一直按单人场景开发，未对专用服务器做过适配；感谢反馈这两个服务端问题，现已修复。若服务器上还有其他问题，欢迎继续反馈。

## 实测四百一十二【回魂符提示语带上 CD 值】

需求：防止玩家不知道收回魂符有冷却机制（常见误解："第二次死了怎么不收"），在系统提示原句"你的女仆生命值过低，已回到魂符中。"后面加"（存在CD，CD为 ?? 秒）"，数值取玩家配置的当前值。

**实现（MaidSoulSpellGuard.tryReturnToSoulSpell）**：
- 提示语改为运行时拼接：CD > 0 时 → "你的女仆生命值过低，已回到魂符中。（存在CD，CD为 N 秒）"（N = SOUL_SPELL_COOLDOWN_SECONDS 当前配置值，默认 60）；CD = 0（无冷却）时保持原句——"存在CD"为 0 会变成反话；
- 原静态常量 SUCCESS_MESSAGE 保留给无冷却分支。

部署：promaid-1.1.0.jar（4,924,302 字节）→ D:\ 验证："存在CD，CD为 " 文案与原句字节级在包内。rc=0。

验收：默认配置下收符 → 聊天栏显示"……（存在CD，CD为 60 秒）"；面板把冷却调 0 → 收符显示原句不带 CD 字样。

## 实测四百一十一【贴身气泡文本池扩到 50 条——打工人毒鸡汤】

需求：加点打工人毒鸡汤语录（"看到女仆傻乎乎的被半骗着说这些话也挺有意思的"），文本池扩到 50 条；同时强调文案要贴合"按日程干活"的实际情境，防止出戏。

**实现（ScheduleBubbleBehavior）**：
- 文本池 30 → 50：新增 20 条毒鸡汤（上班第几个小时/下辈子换大扫帚/老板画的饼我饭量好/摸鱼是不可能摸鱼的/干满一百天给加餐等）；
- 防出戏口径：毒鸡汤全部落在女仆身份与排班情境里——「老板」就是主人、「工资」是摸摸头、「加餐」是干满一百天、「同事」是别的女仆——她被排班表支配但心甘情愿，不会说出"打工人/公司/加班费"这类现代职场词；
- 触发机制/CD/豁免不变（30 秒/只，战斗/自保/坐骑豁免）。

部署：promaid-1.1.0.jar（4,924,040 字节）→ D:\ 验证：毒鸡汤代表句（老板画的饼/摸鱼是不可能摸鱼的/加餐）字节级在包内。rc=0。

验收：同前（实测四百一十），50 条随机——毒鸡汤与日常碎碎念混出，重复感进一步稀释。

## 实测四百一十【排班贴身气泡——情绪价值彩蛋】

需求：玩家靠近正处于排班状态的女仆时，女仆冒出气泡框对话，30 种可能文本，触发 CD 30 秒。

**实现**：
- 新增 ScheduleBubbleBehavior（core 优先级 48，任何任务都运行）——canUse 门禁：MISC_SCHEDULE_BUBBLE_ENABLED（默认开）+ ScheduleData.isOn（排班中）+ 非坐姿/骑乘 + 非自保 + 主人同维度且水平距离 < MISC_SCHEDULE_BUBBLE_RADIUS（默认 3.5 格）+ 每只女仆 30 秒冷却（NEXT_ALLOWED 按 UUID 记 gameTime，懒清理防膨胀）；
- 30 条贴身文本池（排班情境：干活被主人看到的碎碎念/撒娇/报备风格），start 时随机一条 addTextChatBubble（TLM 气泡 + ChatBubbleLimitMixin 5 秒全局限频双保险——本功能 30 秒 CD 远大于限频，观感上不会被吞）；
- 睡觉豁免简化：排班睡觉时段 WALK_TARGET/攻击目标/自保多半不满足，气泡本就难触发，不再单独判睡（避免猜测未映射的 SLEEPING MemoryModuleType SRG 常量）；
- 注册 Pair.of(48, ...)（低于散步 50，保证散步优先级不被挤）。

排错记录：javap 实证 Brain 无 hasMemoryValue 直名（SRG = m_21874_/m_21876_），SLEEPING 常量字段无法可靠定位——睡觉豁免改为依赖既有豁免链（战斗/自保/坐骑）自然覆盖；新源文件需先跑 gen_compile.py 重新生成 compile_promaid.txt（源数 201→202）。

部署：promaid-1.1.0.jar（4,923,241 字节）→ D:\ 验证：ScheduleBubbleBehavior.class 在包内、30 条文本入常量池、注册引用在 ProMaidExtension$1。rc=0（202 源）。

验收：给女仆排班并开启 → 走到她 3.5 格内 → 30 秒内第一条随机贴身气泡（同步到聊天框）；离开再回来 30 秒内不重复；战斗/自保/坐骑中不触发；配置面板「杂项」可关。

## 实测四百零九【搭路被动技能化——"实质接战"替代"任务名占用"】


**机制盘点（先确认启动机制再动手）**：BridgeUpBehavior（core 245）唯一状态闸门 = isTaskOccupied。旧判定中【战斗任务（IAttackTask instanceof）无条件占用】——弓兵/宰杀站着发呆（周围无敌人/无牲畜）也被拦，正是"任务名挂空"的假性占用。挖矿/伐木早已是目标制（MINING/WOODING 找到目标才登记、无目标自动摘除）；烹饪/酿造/建造是站桩/蓝图任务需任务级硬闸（防搭路劫持离岗，实测一百七十四）。maid_survival 对照：它是 LivingTick 事件驱动的卡位检测架构，promaid 已有更完整的行为链，不照搬、只拆假性占用。

**改动（BridgeUpBehavior.isTaskOccupied）**：
- 删除 IAttackTask instanceof 无条件占用；
- 战斗占用收窄为【脑内有存活 ATTACK_TARGET】（正在接战）——战斗模式的空闲照常搭路；
- 新增宰杀任务（maid_smart:slaughter）占用判定：仅在追杀在途（WALK_TARGET 存在）时占用，无牲畜闲逛 = 空闲；
- 烹饪/酿造/建造/挖矿/伐木/WORK_STILL 判定原样保留。

**安全边界**：战斗中（有目标）依然绝不搭路——ATTACK_TARGET 检查独立覆盖接战状态，一百六十七"战斗中边打边搭路"不会回归；威胁半径门禁（threatDist）独立存在，敌对生物靠近即中止。

部署：promaid-1.1.0.jar（4,917,692 字节）→ D:\ 验证：isTaskOccupied 字节码无 IAttackTask 引用、含 slaughter uid / ATTACK_TARGET / WALK_TARGET / cook 判定。rc=0（201 源）。

验收：弓兵模式无敌人时主人拉远/垫高 → 她会主动搭路靠近；一旦接战（出现攻击目标）立即停搭让位战斗；宰杀无牲畜同理；烹饪/酿造/建造照旧不劫持。

## 实测四百零八【排班页高亮当前时段行——一眼看到她在干什么】

需求：女仆处于排班状态时，打开排班表要能一眼看出她现在在哪一段——例：早班、游戏时间 7 点，则"6:00~8:00"标签和"清雪"按钮变绿，整行被绿色框框住。

**实现（ScheduleBookScreen）**：
- 当前槽计算：排班开关开（loadedOn）且客户端世界存在时，取 level dayTime（f_91073_.m_46468_），内联与调度器 ScheduleData.currentMinute 相同公式（%24000×3/50 → 0~1439 分钟），落在班次窗口内 → 槽号 = (minute-win0)/len；排班关闭或不在工作窗口（休息时段）不高亮；
- 渲染端（m_88315_ 排班页）：该行时段标签变绿并加"▶"前缀；该行画 2px 绿色外框（m_280046_ fill 四条 1px 矩形 ×2 圈 = 0xFF3CE03C），罩住 ◀/任务按钮/▶ 整行；
- 按钮端（schedPage）：该行任务按钮文字加 §a 变绿；
- 两处用同一公式各自计算（渲染帧每帧重算，时间走动自动换行高亮；按钮文字在界面重建时取值）。

排错记录：f_91074_ 是 LocalPlayer（玩家），f_91073_ 才是 ClientLevel（javap Minecraft 字段确认）；m_280046_ 是 6 参 fill(x0,y0,x1,y1,z,color)（javap 字节码确认）。

部署：promaid-1.1.0.jar（4,917,607 字节）→ D:\ 验证：字节码含绿色常量 -12787652、f_91073_ 引用 4 处（schedPage+m_88315_）、m_280046_ 调用 4 处。rc=0（201 源）。

验收：她有排班且开着 → 排班页当前时段行：绿色▶+绿色时间标签+绿色任务按钮+整行绿框；关掉排班开关或休息时段全部恢复灰色。

## 实测四百零七【整页任务选择器残留重叠修复】

截图（18:22）问题：四百零六的整页选择界面仍然混乱——标题两行文字叠在一起、"空闲（清空）"按钮上叠着灰色提示、◀▶ 分页钮骑在"三叉戟攻击/枪械"按钮上、"农场"按钮被"← 返回排班"切腰。

**根因（三处，全在 406 实现里）**：
- 标题双绘：渲染主分支先在 TOP_TITLE_Y 画"XX 的排班"，选择页又在同一 y 画"选择任务"→重影；
- 提示压按钮：提示语画在 CONTENT_TOP+8，而"空闲（清空）"按钮占 CONTENT_TOP..+18；
- 列表超高压底行：每页固定 8 行从 CONTENT_TOP+26 铺下，默认缩放窗口逻辑高仅 ~287px → 列表铺到 h-13，压住 h-46 分页与 h-24 返回钮。

**修复（ScheduleBookScreen）**：
- 渲染端 pickSlot>=0 分支整体前置：选择页只画自己的标题+提示（提示上移到 TOP_TITLE_Y+12，标题副行位），排班页文字一概不画；
- pickPageButtons 每页行数随窗口高度自适应：rowsPerPage = max(3, (h-52-(CONTENT_TOP+26)+4)/22)，列表下沿止于 h-52，给分页行和底行让位；240 高最小窗口 ≥5 行；
- 顺修重构时的一处大括号错位（编译期发现）。

部署：promaid-1.1.0.jar（4,917,152 字节）→ D:\ 验证：javap 显示 pickPageButtons + 5 个 lambda、旧 pickButtons 引用计数 0、四条界面文案（返回排班/选择任务/点击任务填入第/空闲（清空））字节级在包内。rc=0（201 源）。

验收：排班页点某时段任务名 → 整页只有"选择任务（第 N 页）"标题+提示、"空闲（清空）"、N 个任务按钮、◀▶ 与页内返回钮，任何文字与按钮零重叠；缩小窗口行数自动减少而非压到按钮上。

# 更新日志

## 实测四百零六【排班表任务选择改整页跳转——修 GUI 覆盖】


**修复（ScheduleBookScreen）**：
- 任务选择从【浮层面板】改为【整页跳转】（手册阅读页同款）：点任务槽中间按钮 → 整页独占——标题行"选择任务（第 N 页）" + 左上"← 返回排班" + 内容区任务列表（每页 8 个 + "空闲（清空）"档）+ 分页 ◀▶；
- 选择页【不渲染】班次按钮/时段标签/女仆列表返回/页签/排班开关——与手册目录→内容页跳转同构，零浮层、零重叠；
- 选择页副提示"点击任务填入第 N 时段；左上返回排班"；
- 旧 pickButtons 浮层方法整体删除。

部署：promaid-1.1.0.jar（4,917,149 字节）→ D:\\ 验证：ScheduleBookScreen 含 pickPageButtons、无旧 pickButtons、"返回排班"文案在包内。

验收：排班页点某时段任务名 → 整页跳到任务选择（清屏只剩标题+列表+返回+分页）→ 点击任务填回该时段并自动返回排班页（保留班次与其它时段编辑状态）。
## 实测四百零五【回魂符冷却并入战斗自保配置面板】

要求："此 CD 在战斗自保中要有可配置面板。"

改动（PromaidConfigScreen）：
- 「收符冷却（秒）」行从「致死伤害自动回魂符」子分区移到「自保参数」子分区——与传送冷却/珍珠逃生冷却/治疗冷却等自保冷却参数并列，统一在"逃生与自保（被动保命）"面板页尾调参；
- 文案补充：0 = 无冷却（彻底关闭防抖振）；其余回魂符参数（开关/致死保护/释放血量比/主人半径）仍在"致死伤害自动回魂符"子分区。

部署：promaid-1.1.0.jar（4,917,190 字节）→ D:\\ 验证：PromaidConfigScreen 引用 SOUL_SPELL_COOLDOWN_SECONDS。

验收：手册配置 → 逃生与自保（被动保命）→ 自保参数 → 「收符冷却（秒）」可调；0 = 每次致死都收（无防抖振）。
## 实测四百零四【回魂符二次作战不收符——冷却起算点修正】

实测："女仆收回魂符有 CD 吗？第一次都能触发，放下来作战第二次就正常死亡，不回魂符了。"

**根因（TLM 释放链路反编译实证）**：收符瞬间把 `cooldown_until = 收符时刻 + 冷却` 写进女仆数据，数据随 ItemSmartSlab.storeMaidData 存进魂符；释放时 TLM 的 EntityMaid.m_20258_（readAdditionalSaveData 恢复路径）把魂符里的数据【原样读回女仆】——即女仆带着"收符时刻+180s"（剩余约 175 秒）的标记复生。第二次作战在释放后的冷却窗口内 → 致死也不收 → 正常死亡。完全符合日志口径。

**修复（MaidSoulSpellGuard.onSoulSpellToMaid）**：
- 冷却改为【从释放时刻重新起算】：写回女仆的 until = 释放时刻 + 冷却（不再读魂符里残留的收符时刻）；
- 默认冷却 180 秒 → 60 秒（放下来作战 1 分钟后再死可再收；0 = 无冷却彻底关闭防抖振）；
- 保留清魂符自动标记（防"这个符再收一次"的循环）。

**效果**：第一次致死 → 收符；放下作战 → 60 秒冷却窗口（防"放出即死→又收又放"抖振）→ 之后正常作战再死 → 再次收符。低血量仍不触发（实测四百零三口径保留，自保正常发挥）。

部署：promaid-1.1.0.jar（4,917,179 字节）→ D:\\ 字节码验证：onSoulSpellToMaid 现为 gameTime + COOLDOWN_SECONDS×20 写入女仆。

验收：收起 → 放下 → 立即再被打死 = 正常死亡（冷却窗口）；等 1 分钟（或把冷却调 0）再死 = 再次收符。
## 实测四百零三【回魂符触发口径收紧——仅致死伤害且无保命物品】


**改动（MaidSoulSpellGuard）**：
- 删除低血量检查（onMaidTick 每秒轮询 + 收符血量阈值配置）——低血量交给自保（喝药/搭高/珍珠），不再抢收；
- 致死伤害保护加【保命物品门禁】：伤害 ≥ 当前血量 且 无保命物品（绀珠之药/不死图腾，SelfPreservationBehavior.hasDeathSaveItem 同口径——饰品栏/双手/背包）才尝试收魂符——有保命物品时让保命物品生效（死亡复活），不抢收；
- 配置：删除 SOUL_SPELL_HEALTH_RATIO（收符血量阈值）；SOUL_SPELL_ENABLE / LETHAL_GUARD 文案更新为"致死伤害自动回魂符"；释放血量比/主人半径/冷却保留。

**最终行为**：女仆被打出必死一击且没有绀珠之药/不死图腾 → 收进主人背包空魂符（免神龛复活）；低血量 → 自保正常发挥（喝药/搭高/珍珠/走位），不触发收符。

部署：promaid-1.1.0.jar（4,917,054 字节）→ D:\\ 验证：MaidSoulSpellGuard 无 onMaidTick、含 hasDeathSaveItem 门禁；MaidSmartConfig 无 SOUL_SPELL_HEALTH_RATIO。

验收：①女仆低血量（如 20%）不消失、自保照常；②无保命物品时被打出致死伤害 → 收符；③带绀珠之药/不死图腾时致死 → 保命物品生效、不收符。
## 实测四百零二【低血量自动回魂符 + 排班表任务选择面板】

玩家两个优化建议：①排班表任务切换要跳转选择框（攻击和挖矿隔十几个任务，循环点太累）；②低血量自动回魂符（免去神龛复活），参考 maid_survival-1.9.5.jar 机制。

**功能一：低血量自动回魂符（MaidSoulSpellGuard，参考 maid_survival 的 MaidSoulSpellGuard）**
- 机制：女仆血量 ≤ 阈值（默认 25%，每秒检查一次）或受到【致死伤害】（伤害 ≥ 当前血量，成功则取消伤害）→ 尝试收进主人背包里的空魂符（TLM 原版 SMART_SLAB_EMPTY）；
- 触发条件：女仆存活/未移除/未骑乘 + 冷却未到 + 主人是同维度 ServerPlayer 且在半径内（默认 24 格）+ 主人背包（主手/副手/物品栏）有空魂符；
- 动作：新建 SMART_SLAB_HAS_MAID 魂符 → ItemSmartSlab.storeMaidData 存女仆数据 → 魂符打自动标记（冷却时间戳 + 释放血量比）→ 放入主人背包空槽 → 主人收到提示 + 再生药水效果（时长=冷却）+ 升级音效 → 女仆实体从世界移除；
- 冷却：成功收符后女仆 persistentData 写冷却时间戳（默认 180 秒），期间不再触发；
- 防收放循环：魂符右键释放（MaidAndItemTransformEvent.ToMaid）时把冷却写回女仆 ForgeData 并清自动标记——释放后冷却期内不会立刻又被收回去；
- 配置（战斗段）：开关 / 致死保护 / 收符阈值（0.25）/ 释放血量比（0.35）/ 主人半径（24 格）/ 冷却（180 秒）；
- 与自保的关系：自保是"活着保命"（喝药/搭高/珍珠），本功能是"保不住就收符"兜底；玩家手动用魂符收/放女仆不受影响（只认自动标记的魂符）。

**功能二：排班表任务选择面板（ScheduleBookScreen）**
- 排班页 6 个任务槽的中间任务名按钮：点击不再"换下一个"，改为弹出【任务选择面板】——分页（每页 8 个）列出全部任务 + "空闲（清空）"档，点选即填入该槽（◀▶ 上一个/下一个保留）；
- 面板带暗红背景/标题/分页 ◀▶/关闭；当前槽已选任务标 ●。

**顺带清理**：实测四百零一移除高地狙击时漏删的 COMBAT_SNIPE / COMBAT_SNIPE_HEIGHT 配置项（MaidSmartConfig + PromaidConfigScreen 配置行）一并删除。

部署：promaid-1.1.0.jar（4,917,483 字节）→ D:\\ 验证：MaidSoulSpellGuard 类/注册/6 个 SOUL_SPELL_* 配置字段在包内、无 HighGroundSnipe/COMBAT_SNIPE 残留、ScheduleBookScreen 含 pickSlot/pickButtons。

验收：①女仆血量掉到 25% 以下（或被打出致死伤害）且主人背包有空魂符 → 女仆消失、魂符出现在主人背包（带"自动保存"标记）、主人收到提示；右键释放 → 女仆回到身边（血量按释放比恢复），冷却期内不再自动收；②排班表排班页点任务名 → 弹出任务列表直接点选，不再循环点十几下。
## 实测四百零一【移除垫高狙击——恢复旧版远程战斗】

定夺："把远程武器攻击期间搭方块逻辑删掉，恢复旧版逻辑。" 垫高狙击（实测三百六十六起）经 387~400 多轮修复（会话静态化/单例/生命周期 SRG 修正/首 tick 续命）仍无法稳定——行为生命周期与 TLM Brain 的交互过于脆弱，玩家放弃该功能。

**本次改动（全量摘除）**：
- ProMaidExtension：删除 235 挂载（HighGroundSnipeBehavior.singleton()）；
- MaidMoveSuppressMixin：删除 SNIPING 分支（垫高静止拦截 + 诊断日志）；
- AutoCombatSwitch：删除"狙击中不还原"让位；
- MaidCombatTacticsBehavior：删除 isSniping 让位（保留自保 isMovingToSurvive 让位与战斗方块塔顶禁走位——自保垫高塔仍受保护）；
- SelfPreservationBehavior：删除 isSniping 让位、两处 isTeleportBlocked 禁传检查（teleportHomeOnExit / teleportHome）、envDangerSoft 恢复 headInSolid 原判定（399 的 SNIPING 豁免一并移除）；
- 删除 HighGroundSnipeBehavior.java（含 SnipeSession/OptionalTarget/Holder 内部类）。

**恢复后的行为**：远程女仆 = TLM 原版远程战斗（弓/弩 strafe 走位 + 射击）+ 战术行为 rangedTick 风筝（拉开/逼近/绕圈，理想射程 ≥7 格）；自保 = 原版全链路（含传送兜底，仅自保会话内禁传保留——实测三百九十的玩家设定）。不再有任何"远程攻击期间搭方块"逻辑。

部署：promaid-1.1.0.jar（4,910,702 字节）→ D:\\ 验证：jar 内无 HighGroundSnipeBehavior 类/引用/[snipe] 日志串、ProMaidExtension$1 无 235 挂载、构建脚本自动 purge 4 个残留类。

## 实测四百【首 tick 续命死锁——行为每 tick 停→启循环的根治】

玩家 399 实测："修了个啥？一点用都没有"。日志铁证（399 部署后窗口）：`onStop 114 条`与`"进入垫高状态" 115 条`一一对应、间隔恒 1 tick、无任何解除原因日志（无"外力传送/远离塔列/自保解除/垫块失败"）——行为每 tick stop→restart 循环，塔永远垫不到顶（enter=115 / build=25 / lockline=0 / topReached=0）。

**根因（Brain 生命周期时序）**：Brain 启动链 = tryStart（m_22554_，RUNNING + 调 onStart）→ 同周期 tick（m_22558_ 先查 canContinue）→ doTick。399 把 onStart 改成零操作后：canContinue 首 tick 查到空会话（SESSIONS 尚未建、SNIPING 空、base=-1、findTarget 恰为空）→ 立即 stop（onStop）→ 下一 tick 又 tryStart → 每 tick 停→启。397 版"onStart 清会话"恰好掩盖了这条死锁（行为启动时清、死锁时也清，循环照样空转），399 修对了清理方向却把首 tick 续命凭据也删了。

**修复（HighGroundSnipeBehavior.m_6735_ onStart）**：启动即 `st(maid)` 建会话——canContinue 从第一 tick 起恒 true（SESSIONS.containsKey），行为变常驻（与自保行为完全同构：常驻 + tick 内部闭环进场/解除/垫块），条件波动不再重启。onStop 仍是唯一清理出口。

配套（实测三百九十九，已在包内）：onStop 真清理 + 永不超时 + canContinue session 优先 + 自保 SNIPING 期间 headInSolid 误报豁免 + m_6732_ 泄漏清理。

部署：promaid-1.1.0.jar（4,925,129 字节）→ D:\\ 验证：onStart 字节码即 `invokestatic st + pop + return`（纯建会话零清理）、m_6732_/MAX_VALUE 构造均在）。

验收点（latest.log 搜 [snipe]）：①"行为停止（onStop）"一行都不再出现（循环消失）；②一场战斗"进入垫高状态"1~2 条 + 垫块 #1→#5 连续递增 + "已达塔顶" + "钉柱段执行"每秒一条；③她站在塔上狙击不再乱走。
## 实测三百九十九【垫高摔落根因修复——Behavior 生命周期 SRG 语义反转 + 自保窒息误报打断】

玩家三测反馈（弓打尸壳）：②弓模式垫高每次只垫 2 块就掉；③自主战斗同样；新问题④持弓却向尸壳贴脸。398 日志层一击命中三个根因：

**根因①【Behavior 生命周期 SRG 语义搞反】**（原版字节码铁证：Behavior.m_22554_ tryStart 成功后调 m_6735_=onStart；停止走 m_22562_→m_6732_=onStop）：旧版把全部清理写进了 m_6735_（实际是 onStart）=【每次启动都清空会话】，真 onStop（m_6732_）漏覆写。且默认构造 super(emptyMap)=60 tick 运行时长——3 秒超时自动停。行为 stop→restart 循环时每次重启会话归零（日志"进入垫高状态"每 1~5 tick 一条、base=-1、built 恒 1-2、全程 0 条"钉柱段执行"）。

**根因②【自保窒息误报打断狙击】**：垫块瞬间头部与新方块短暂重叠 → SelfPreservationBehavior.envDangerSoft 的 headInSolid 误报 → hp=100% 也 sessionEnter（日志实证每条"self preserve start"紧跟"垫块#1"）→ sessionEnter 清 ATTACK_TARGET → 狙击 canContinue 假 → 行为 stop → 下一 tick sensor 回写目标 → 重启 → onStart 又清会话 → 永远垫不上去。

**根因③【持弓贴脸】**：自保稳定激活的静默段里，狙击行为 canUse 恒 false 不启动、isSniping 让位战术/狙击全部歇菜 → TLM 原版 MaidAttackStrafingTask 全权移动（其 strafingBackwards 判定"太近才后退、否则前压"）→ 贴脸 + 走出塔顶摔落（10:37:51 塔顶 y=-58 → 52.06 三格外 y=-59 坠落、无任何解除日志 = 行为已死的静默段）。

**修复**：
- HighGroundSnipeBehavior：①构造 super(emptyMap, Integer.MAX_VALUE, Integer.MAX_VALUE)（永不超时，与战术行为同款）；②m_6735_（onStart）零清理（会话由 m_6725_ 内部管理，与自保同架构）；③新增 m_6732_（真 onStop）承接全部清理（SNIPING remove + SESSIONS remove + 解冻 + 清禁传闸 + "行为停止（onStop）解除"日志——旧版泄漏标记导致行为死后 Mixin 还在拦移动却没人在钉柱）；④canContinue 改 session 存活优先（自保清 ATTACK_TARGET 的瞬间行为不再死）。
- SelfPreservationBehavior.envDangerSoft：SNIPING 期间 headInSolid 不触发窒息会话（狙击自带 antiSuffocate 兜底；真危险——岩浆/着火/溺水——照常进自保，狙击 m_6725_ 的"进入自保解除"分支正常交还）。

部署：promaid-1.1.0.jar（4,925,130 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证：构造 ldc 2147483647 双参、m_6732_ 存在且含 SNIPING/SESSIONS remove + unblockTeleport + applySpeedFreeze、m_6735_ 零清理、envDangerSoft 含 isSniping 门禁）。

验收点（日志直接可验）：①"进入垫高状态"一场战斗最多 1~2 条（开局+被打下重定基各一条）；②垫块#1→#5 连续递增、"已达塔顶"出现、"钉柱段执行"每秒一条；③"self preserve start"不再紧跟垫块；④塔上站桩狙击、不再走下塔；⑤持弓风筝距离恢复（战术 ranged kite 日志 dist 稳定在 ideal±2）。

## 实测三百九十八【轨迹 + 伤害双层日志——定位"怎么摔的"】

要求：加一层日志检测女仆运动轨迹与行为，确定摔下来是怎么摔的、以及所受伤害类型。latest.log 搜 `[snipe]`（垫高+轨迹）与 `[maidhurt]`（伤害）两类过滤。

**轨迹日志**（HighGroundSnipeBehavior.m_6725_，垫高状态或坠落中每 10 tick 一条，字段已验证 SRG）：
`轨迹 x=… y=… z=… vert=… hspd=… fall=… 着地=true/false`
- fall = 原版坠距（逐 tick 减，>0 即在坠落）；vert = 垂直速度（负=加速下坠）；hspd = 水平速度（>0 = 有横向位移，定位"带着速度摔下"）；着地 = 实时脚下实心判定——配合"站上目标格/超时"日志可还原完整摔落路径（从哪格开始掉、掉多快、在哪着地）。

**伤害类型日志**（SelfPreservationBehavior.onMaidHurt，每次受击即时一条，SRG 已 javap 验证）：
`[maidhurt] 名: 类型=fall [弹射物] 来源=… 伤害=… 位置=(…，…) y=…`
- 类型 = DamageSource.getMsgId（m_19385_）：fall=摔落、inFire/onFire/lava=燃烧、arrow=箭、trident=三叉戟、explosion=爆炸、mob/player=攻击、magic=魔法、outOfWorld=虚空；
- 弹射物标 [弹射物]（m_7986_）；来源实体名/类名（m_7640_）；伤害量（原值，未计护甲减伤）；受击位置与 y 坐标。
- 两条日志对照：若摔落伤害（类型=fall）前出现"轨迹 hspd>0"→ 横向速度致摔；若轨迹显示"站上超时 fall>0"→ 垫块窗口未站稳；若类型不是 fall（如 projectile/explosion）→ 不是自己摔、是被打下来的。

部署：promaid-1.1.0.jar（4,925,051 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证 lastFallLogTick 字段、m_19385_/m_7986_ 调用、[maidhurt] 串均在包内）。

排查指引：把摔落瞬间的 `[snipe] 轨迹` 前 3~5 条 + 同刻 `[maidhurt]` 一条发来即可一秒定位根因。

## 实测三百九十七【静态单例·根治多实例并行互踩】

三百九十六把会话迁静态 per-maid map 后，实测日志仍异常，但拿到决定性新证据：同一 tick 内（4~6ms 间隔）连续 5+ 条"进入垫高状态"——**同一女仆有多个行为实例并行运行**。getCoreBehaviors() 每被查询就 new 一批全部行为实例（List.of 在方法体内），Brain 构建/活动重建多次 → 多批实例累积并存，每个实例都走 !building 入口分支、互相清零 builtCount（垫块恒 #1、钉柱段 0 次——三百九十六的静态 map 被多实例互踩，同实例内状态本来没问题）。

修复（根治）：HighGroundSnipeBehavior 改为**静态单例**（Holder 持有，`HighGroundSnipeBehavior.singleton()`），挂载点（ProMaidExtension getCoreBehaviors）改调用单例——实例被查询多少次都只剩一个，多实例并行从源头消失。本类所有会话状态已在静态 per-maid map（实例零状态），全服女仆共享同一实例完全安全（状态按 maid UUID 隔离）。

部署：promaid-1.1.0.jar（4,924,319 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证 singleton() 方法 + Holder + ProMaidExtension$1 内 invokestatic singleton）。

验收点（日志直接可验）：①同一 tick 不再有多条"进入垫高状态"；②"进入"频率降到每 tick 一条以下；③垫块#N 递增到 #5、已达塔顶出现；④钉柱段执行每 1 秒一条（锁定持续）。

## 实测三百九十六【会话状态迁静态 map——根治"搭完就摔"】

三百九十五日志铁证定案：`进入垫高状态` 每 0.05 秒（每 tick）一条且带 `built=0`、`垫块#N` 恒为 #1、`已达塔顶` 0 次——**行为实例的会话字段（baseY/lock/walkOn）每 tick 失忆**，钉柱/静止段从未持续多 tick 运行，塔垫完就自由移动摔下（"每次 4 格柱就摔"“搭好还会乱走“的最终根因，与三百九十四的垫块锚定是**两回事**：锚定只让塔能垫直，会话失忆让垫完就散）。

根因：IExtraMaidBrain.getCoreBehaviors() 方法体里的 `List.of(new HighGroundSnipeBehavior(), …)` 每次调用都重建全部行为实例（extra-core 列表每次查询都 new），实例字段随重建全部归零。自保每次触发是独立会话、失忆无感所以"自保正常"——狙击是唯一需要跨 tick 会话状态的核心行为，全项目只有它崩。

修复（HighGroundSnipeBehavior 重构）：
- 全部会话字段（baseY/buildCd/stateScanCd/lockX/lockZ/lockValid/walkOnTicks/walkOnY/lastTick/lastTickValid/diagCd/builtCount/topReached/everStarted）迁入**静态 per-maid SnipeSession map**（ConcurrentHashMap<UUID,SnipeSession>）——行为实例怎么重建、getCoreBehaviors 被调多少次，会话状态跨实例存活。
- 入口/钉柱/垫块/解除/下塔全部改走 session；行为停止（stop）与解除（clearSnipeFrom）清 session 防泄漏（女仆死亡/离队后 map 条目不再挂留）。
- 新增"钉柱段执行"限频日志（1 秒一条）——确认锁定段每 tick 在跑；解除扫描轮询加了"刚启动不轮询"（everStarted + walkOn 守卫），首块站上前不误触发半径 8 解除。

部署：promaid-1.1.0.jar（4,923,709 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\。

验收点（关键变化）：①日志里 `进入垫高状态` 不再每 tick 刷屏（会话跨 tick），`垫块#N` 从 #1 逐层递增到 #5，`已达塔顶` 出现；②塔顶 `钉柱段执行` 每 1 秒一条 = 静止持续生效，不再乱走摔下；③半径8无敌人 → `已传回塔脚` 一次收摊。

## 实测三百九十五【全链路诊断日志】

要求：把垫高狙击整条链路打上日志，方便后续检查。latest.log 搜 `[snipe]` 过滤。

**事件日志（每次一行）**（HighGroundSnipeBehavior）：
- 进入垫高状态（塔基 y / 目标高 h / 敌人距离）
- 垫块#N @(x,y,z) 塔列(x,z)——确认 394 锚定是否生效、N 是否卡在 4 就停
- 站上目标格 / 站上超时（y / 目标 / fall=落距）——确认 4 格柱窗口问题
- 达塔顶 / 跌破塔顶判定进入补垫
- 掉塔落地重定基（旧基 y->新 y）——"被打下去再建一根"的触发点
- 各解除点：外力传送（dx/dz）、环境致命、远离塔列、进入自保、半径 8 无敌人（成功传回塔脚 / 塔脚无落点保留状态）、垫块失败收摊（含 built 计数）
- 行为停止清理完成（Brain 收回/换任务/排班）

**状态轮询**（1 秒一行）：状态 y / base / h / built / lock=(x,z) / walkOn / fall —— 链路健康度快照，可对照事件看时序。

**Mixin 命中确认**（MaidMoveSuppressMixin，限频 1 秒一条）："MoveToTargetSink 已拦截（垫高静止生效）y=…" ——确认 393 的静止拦截在跑。若日志中大量出现而女仆仍动，说明移动通道在 MoveToTargetSink 之外（如 strafe 直连 MoveControl m_24988_——394 锚定已缓解垫块窗口漂移，但塔顶侧向漂移若再现，日志能分离定位）。

部署：promaid-1.1.0.jar（4,922,480 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证 Logger/[snipe] 字符串/builtCount/diagCd/Mixin 限频 tag 均在）。

排查指引（把对应片段发来即可）：①垫块#N 系列 → 看 N 是否卡在 4、坐标是否都在同一塔列（x,z 恒定）；②站上超时 → 看 fall 值（>0 = 已起跳/坠）；③"移动拦截"是否持续出现；④摔落地瞬间的前 2 秒轮询行 + 事件行。

## 实测三百九十四【修复：垫块锚定塔列——"4 格柱就摔、第 5 块从未垫成"】

玩家关键观察："每次都是 4 格柱子就摔下去，搭建 5 个这个操作从来没完成过，每次都是达到一定数量之后因为拥有速度从塔上摔下来。"

javap 实证找到移动通道盲区：TLM 远程 strafe（MaidAttackStrafingTask）**直连 MoveControl（m_21566_→m_24988_(FF) 施加横向速度），完全绕开实测三百九十三拦截的 MoveToTargetSink/WALK_TARGET 链路**（393 只掐断了 MaidRangedWalkToTarget 那一路）。即速度归零（瞬态乘 0）也管不住它直接写速度。

推论链（与玩家观察完全吻合）：
- 垫块瞬间（beginWalkOn 12 tick 窗口）strafe 每 tick 拉她横向位移；
- snipeBuildUp 的 place 用实时 blockPosition（m_20183_()）取格 → 每块垫偏一格、垫歪的块不在柱心正上方；
- lockColumn 把她钉回柱心时柱心正上方是空气 = 悬空 → 带速度摔离塔列；
- 偏差在逐块垫高过程中累积，到第 4~5 层触发 →"每次 4 格柱就摔"。

修复（snipeBuildUp）：place 的 X/Z 锚定塔基列（Math.floor(snipeLockX/Z) 所在格），Y 逻辑不变（脚下悬空垫脚下、实心垫身体格顶起）。塔绝对竖直：钉柱永远有落脚、walkOn 目标格恒与钉柱一致、不会再被"钉回柱心时悬空"。snipeLockX/Z 只在首次进入（锁定原点）时写入，此后不随漂移更新。

部署：promaid-1.1.0.jar（4,920,104 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证：塔列锚定 Math.floor×2 + d2i×5、snipeLockX/Z 字段读取、放块 m_7731_ 均在方法体内）。

验收点：①弓模式 5 格塔完整垫成，过程中零横向漂移；②垫块/塔顶不再出现"钉回柱心悬空"坠落；③塔绝对竖直（塔列锚定），补垫/重建也走同一列。

## 实测三百九十三【垫高状态套用建造同款静止——塔顶乱走根治】

玩家（userselect 引用）："没有怪阻拦，搭得好好的，女仆自己从塔上乱走，然后摔下去。"玩家指明方向："搭完却仍在移动，需要套用一个静止的逻辑，建造状态是个不错的方向，[垫高状态]赋予一个类似于建造的状态。"

排查确认根源：TLM 远程攻击的 strafe 走位行为（MaidRangedWalkToTarget / SetWalkTargetFromAttackTargetIfTargetOutOfReach）每 tick 都写 WALK_TARGET 让女仆横移走位。钉柱（setPos 回柱心）与这些移动输入是同 tick 先后手——位置校准永远慢半拍；移速×0 挡横向推力但挡不住"每次都被 AI 挪一步"。建造状态所以稳（反馈实测），是因为它不止事后清理，而是从源头掐断移动意图。

实现（MaidMoveSuppressMixin）：新增 SNIPING（垫高状态）分支——与工作站桩【彻底静止】同档：清 WALK_TARGET + 停导航 + 取消 MoveToTargetSink 本 tick。TLM 远程 strafe 写的 WALK_TARGET 从源头不执行 → 塔顶零移动意图，比钉柱/冻结更根本（不是"推不动"，是"根本没被要求动"）。仅影响移动：弓的瞄准/射击不走此 sink，攻击不受影响（正是"此状态不影响攻击，只影响移动"的规格）；状态解除（clearSnipe/stop 移除 SNIPING 标记）即恢复移动。

部署：promaid-1.1.0.jar（4,920,051 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证 mixin 含 SNIPING 分支 + 彻底静止档调用 m_26569_）。

验收点：①弓模式塔顶：TLM strafe 完全失效——塔上纹丝不动、继续射击；②垫高过程中（每垫一块站上时）同样静止，不再边走边垫；③解除（半径 8 无敌人/进自保/外力传送）后移动立即恢复；④建造/烹饪/自保/战术的既有抑制分支不受影响。

## 实测三百九十二【修复：三叉戟被误判近战武器】

玩家询问自动切换武器逻辑里三叉戟算近战还是远程。查证结果：任务层三叉戟 = 远程（isRangedTask 白名单五件套 + 关键词 trident）；但 **HighGroundSnipeBehavior.hasMeleeWeapon 用攻击力属性口径，原版唯一带攻击力的远程武器（三叉戟，9 点）被误判为"近战武器"**。

后果：自动指派三叉戟任务 + 包里只有三叉戟（或手持三叉戟）+ 敌人近身 → isTaskAutoAssigned && hasMeleeWeapon = true → 不垫高。但实际没有剑可切、自动切换也不会切去近战任务（三叉戟任务本身就是远程）——她既不垫高也不切近战，近身白挨打。

修复：hasMeleeWeapon 排除 TridentItem（s.m_41720_() instanceof TridentItem → 跳过）——三叉戟按判定口径算远程，自动指派时近身走垫高流程。其余判定（剑/斧/锄/拔刀剑/史诗战斗武器）不变。

部署：promaid-1.1.0.jar（4,919,993 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证 hasMeleeWeapon 方法体内含 TridentItem 判定）。

验收点：三叉戟任务 + 只有三叉戟 + 近身 → 不再卡死（有方块则垫高拉开，无方块行为照常）。

## 实测三百九十一【修复：解除后放生塔顶乱走摔下】


根因：safeDismount 用 findStandNear 从【塔基格】起扫落点——塔基格被她自己垫的第一块方块占着，扫描向下全是实心地面、向上第一个可站格是塔基+1（= 塔顶），传送等于原地踏步；状态却已解除（半径 8 无敌人/方块耗尽路径），下一 tick 行为停止、TLM 游走 AI 接管，塔顶没有钉柱没有冻结 → 乱走两步摔下。"没有怪阻拦"正是触发解除路径的场景。

修复（HighGroundSnipeBehavior）：
- **safeDismount 重写**：改为塔基旁 4 方向（±1 格，跳过柱心）找"站立格空气 + 脚下实心"的实地落点——塔柱 1×1，旁 1 格必是起塔前的地面；解除条件①保证塔脚无怪。返回 boolean。
- **解除调用点失败不解除**：半径 8 无敌人路径与方块耗尽路径都改为"safeDismount 成功才清状态"——失败（塔脚 4 向都无落点，罕见）则保留状态继续钉着等落点，绝不"解除后放生塔顶"。
- 自保让位路径（②）不做安全下塔，维持原样（移动/逃生交还自保）。

部署：promaid-1.1.0.jar（4,919,970 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证：safeDismount 新签名返回 boolean、4 向循环、m_264318_ 传送、m_60795_/m_60796_ 落点判定均在方法体内，findStandNear 引用已移除）。

验收点：①半径 8 无敌人 → 她出现在塔脚实地（不再原地踏步）；②解除后移动能力立即恢复、不再乱走摔下；③方块耗尽 → 先下塔再解除；④塔脚被围（解除条件①不触发）→ 继续钉塔输出。

## 实测三百九十【自保模式禁止传送（玩家日程表手动传送除外）】

规则：女仆进入自保模式后禁止传送；玩家手动用日程表传送除外，依旧可行。

排查确认外部传送面早已封闭：跨维度跟随/同维度拉回/一键集合外的自动传送都读 PRESERVE_TAG（实测一百九十六），TLM 原生传送有 FollowPreserveMixin/MaidTeleportPreserveMixin 拦截。唯一还在自保期间传她的是**自保自己的逃生传送**，本次全部停用：

- **围困传送停用**（SelfPreservationBehavior）：围困 10 秒（塔上无回血资源）→ 原来清搭高状态+传送回主人身边；现在只清 heallessSiegeTicks 计数防无限累积，不传送、不清搭高状态（没传送就不该放弃塔位），本地逃生（珍珠/药水/走位/搭高/反击）接管。
- **兜底传送停用**：teleportHome 顶部加会话闸（isSelfPreserving → return）——岩浆环境危急/血量 <15%/弹尽粮绝（无方块无珍珠）三种兜底传送全部不再触发。【后果提示】弹尽粮绝死局（被围殴、无方块、无珍珠、走位拉不开）原先靠传送保命，现在会战斗到死——本地逃生接管，玩家已确认此取舍。
- **不受影响的通道**：①玩家日程表召唤（ScheduleNetworking.summonAll/summonOne 一键集合，玩家主动意图优先）；②TLM 排班换岗传送（TLM 自身通道）；③解除自保的归位传送（teleportHomeOnExit 在会话结束、标记清除之后才发，三七十八"解除即传"功能保留）；④魂符收回（实体移除，非位置传送）。
- 配套：teleportHome 方法与全部安全护栏（主人身边安全判定/落点闸/冷却）原样保留，只是会话期间被闸拦——随时可逆。

部署：promaid-1.1.0.jar（4,919,922 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证 teleportHome 方法体内含 isSelfPreserving 会话闸）。

验收点：①自保中（低血/围困/弹尽粮绝）女仆不再消失传回家，全程本地战斗；②被围困在塔上 10 秒不再被传走、塔位不丢；③玩家用日程表/一键集合召唤她 → 照常传送；④解除自保 → 立刻传送回主人身边（三七十八）照常；⑤自保期间主人切维度 → 不再被自动跨维拽走（一百九十六原有行为不变）。

## 实测三百八十九【垫高状态·玩家重写全套逻辑】

玩家重写了高地狙击的完整规格，HighGroundSnipeBehavior 按新逻辑整文件重写：

- **进入条件重述**：自主战斗/自动切换武器切到远程 + 敌人近身 → 先查近战武器（自动指派时），有近战武器走切换近战流程不垫高（原已实现）；没近战武器但有方块 → 进入【垫高状态】。手动指派的远程任务照旧可进（canUse 与入口评估双挡，含"行为存活中自保才激活"的时序兜底）。
- **垫高高度 8→5 格**：COMBAT_SNIPE_HEIGHT 默认 5（范围 2~12 不变），配置注释/配置界面/zh_cn 文案同步更新。
- **状态行为**：原地定住（钉柱 + 水平移速×0，只影响移动不影响攻击，射击交给 TLM 远程 AI）+ 立刻逐渐垫高；禁传闸与状态绑定——状态存续每 tick 刷新，状态解除即清闸（unblockTeleport 用 putLong 0 复位，免新增 SRG），不再有 30 秒尾闸。
- **高度维持（替代旧收摊口径的核心新机制）**：每 tick 比对"当前高度-塔基"与目标高度，低于即续垫——塔被炸低/踩塌 → 补垫；被轰飞落地 → 重定基后重垫一根；等效且快于规格中的 10 tick 轮询。
- **解除条件两条（替换旧的目标丢失 20tick/丢视野 3 秒/无威胁 3 秒三套口径）**：①以自己为球心半径 8 内没有任何敌人（10 tick 轮询，统一威胁口径 + 当前攻击目标 8 格内也算防中立误判）→ 解除并安全下塔；②进入自保（PRESERVE_TAG，每 tick 查）→ 立即解除、不做安全下塔（移动/逃生交还自保）。canUse 同时拒绝自保期间重新进入，杜绝两边抢移动权。
- **保留的硬性安全阀**：泡水/岩浆、方块耗尽、传送级位移（一 tick 水平跳 >3 格且速度≈0 = 排班表等外力传送，放行；>10 格 = 远离塔列）。
- 与自保优先级反转：自保会话激活 → 垫高让位；自保未激活时垫高优先（自保移动类动作查 isSniping 让位，贴脸反击/喝药并行）。SelfPreservationBehavior 新增 public static isSelfPreserving(maid)。
- 删除的旧机制：目标丢失/丢视野/无威胁收摊口径（noTargetTicks/losLostTicks/noThreatTicks/lostScanCd/hasNearbyThreat 全部移除）、"无目标地面冻结 2 秒封顶"（新口径下状态与攻击目标完全解耦，不存在该场景）。

部署：promaid-1.1.0.jar（4,919,960 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\（javap 验证 hasEnemyInRadius8/isSelfPreserving/stateScanCd/unblockTeleport 均在包内，旧 hasNearbyThreat 已删）。

验收点：①敌人近身 → 原地定住垫高 5 格，全程零滑动；②期间自保/围困传送全部被拦；③塔被 TNT 炸低/踩塌 → 立刻补垫回 5 格；被轰飞 → 落地原地再垫一根；④半径 8 内敌人清空 → 传回塔脚收摊、移动恢复；⑤把她打到进自保（残血）→ 垫高立即解除、自保接管（珍珠/围困传送可用）；⑥射击全程不受影响。

## 实测三百八十八【全盘审查·塔柱循环防 bug 修复两处】

要求对塔柱循环（三八七）做全盘审查。跨文件联动复核了 PlacedBlockTracker/自保传送闸/AutoCombatSwitch 自动指派判定/部署 jar 新鲜度（javap 验证 markTeleportBlocked=3 处、safeDismount=5 处引用），发现并修复两个真 bug：

- **战斗方块刷新垂直带 vs 塔高（塔底溶解 bug）**（PlacedBlockTracker + SelfPreservationBehavior）：靠近刷新的垂直带是共享硬编码 ±6 格，而狙击塔默认 8 格——她站上塔顶后，塔底两格方块（垂直差 6.5~7.5）够不到刷新带，30 秒寿命到期后塔从底下开始逐格溶解（顶部方块因 ownerOn/±6 带持续刷新，人不会掉，但塔在狙击中烂掉）。修复：垂直带参数化（构造器第三参），桥/搭路类保持 6.0 不变，战斗方块表（COMBAT_TRACKER）提到 12.0，覆盖满配 12 格塔。0.5 格水平半径不变——只有她自己柱子上的方块会续命，离开塔 30 秒必回收的语义不受影响。
- **无目标+有威胁时地面/半柱冻结无上限**（HighGroundSnipeBehavior）：目标记忆丢失分支的"钉住等再锁定"只该发生在塔顶（塔上安全）。被打落地/攀爬中目标没了时，ot==null 提前 return 导致建塔分支不跑、冻结却不撤——她以 0 速僵在地面或半柱上挨打且永不恢复（威胁扫描判"仍有威胁"就无限等）。修复：非塔顶（用实时 Y 与塔基差判定）无目标等待封顶 40 tick（2 秒），到点 clearSnipe + safeDismount 传回塔脚交还正常 AI（重新索敌/走位/反击；再满足触发条件会开新会话重起塔）。塔顶"钉住等再锁定"行为保留。

部署：promaid-1.1.0.jar（4,920,175 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\。

审查确认无问题的项：禁传闸每 tick putLong 开销可忽略；传送识别基线在会话外不生效（无误判）；击退（伴随高速度）与传送（速度≈0）的区分阈值成立；m_6735_ 清标记后无残留路径；解除自保的 teleportHomeOnExit 为一次性调用，狙击期间被闸吞掉属预期（她在塔上输出优先）；isTaskAutoAssigned / 模组任务武器门禁逻辑自洽。

## 实测三百八十七【塔柱完整循环：不滑出 + 全程禁传（排班放行）+ 被打下去再建一根】

定夺的完整效果链：垫高后不再滑出自己垫的柱子；期间 30 秒禁传送（但玩家可以通过排班表主动触发传送）；在柱子上安心狙击敌人；被敌人打下去之后想办法再建一根柱子，重复循环。本次补齐三处缺口，"再建一根"的机制（落地重定基 + 完全承诺建塔，实测三百六十八）本身已就位：

- **禁传闸会话全程刷新**（HighGroundSnipeBehavior）：此前只在每块垫成瞬间刷新 30 秒闸，塔上狙击一超过 30 秒闸就过期，自保的围困传送会把狙击中的她拽走、打断"安心狙击"。现在建塔/塔顶期间每 tick 刷新——"此期间"全程禁传，收摊后仍保留 30 秒尾闸。
- **传送识别·排班表放行**：会话激活期间水平位置一 tick 跳 >3 格且水平速度≈0 = 被外力传送（排班表换岗传送等）→ 立即收摊放行（清标记/钉柱/冻结），残留的钉柱不会把她从传送落点再拽回旧塔。大击退的位移同样可能 >3 格，但伴随高速度（hvsq≥0.25），与传送区分 → 保留会话，落地重定基重起塔，正是"被打下去就再建一根"。
- **m_6735_ 补清 SNIPING 标记**：行为被 Brain 强停（玩家换任务/换排班导致 Brain 收回行为）时同步清标记——此前只在 tick 内各收摊出口清，强停路径会残留标记，卡死 AutoCombatSwitch/战术让位逻辑。

部署：promaid-1.1.0.jar（4,920,079 字节）→ D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\。

验收点：①垫高+塔顶全程零滑动（实测三百八十六速度归零冻结）②狙击期间自保传送全程被拦（不再只保 30 秒）③换排班表 → 她被正常传走、不回拽、标记/冻结无残留 ④塔被打掉/被大击退轰飞 → 落地原地再建一根柱子继续狙击，循环往复 ⑤收摊后 30 秒尾闸内解除归位传送改走路（预期行为）。

## 实测三百八十六

- 【为什么钉柱压不住】setPos 钉柱与 AI 的移动输入是同一 tick 内的"先后手"：我们的 tick 先钉回去，弓 AI 的 strafe/游走输入在后半段的 travel 里照样把她推出去——位置校准永远慢半拍，残留滑动位移。
- 【决定性手段：移动速度归零】狙击会话期间给女仆挂瞬态属性修饰符（MOVEMENT_SPEED × 0，MULTIPLY_TOTAL）——**AI 的移动输入乘上 0 速度 = 物理上推不动她**，strafe/游走/寻路全部失效。攀爬推送/击退/重力走 deltaMovement 通道不受影响（垫高照常、被打会退、会下落）。
- 【SRG 全部 javap 常量池验证】MOVEMENT_SPEED=f_22279_（"generic.movement_speed" ldc→putstatic 实证）、addTransientModifier=m_22118_（区别于 addPermanentModifier=m_22125_：后者会写永久集合 f_22091_）、getModifier=m_22111_、removeModifier(UUID)=m_22120_、Operation.MULTIPLY_TOTAL（枚举名未被 SRG，直接可用）。
- 【安全】瞬态修饰符不入 NBT（下线/重登自动清）；会话任何出口都解除冻结：clearSnipe（所有收摊点）/行为停止 m_6735_/会话未激活分支——三重兜底，不存在"永久 0 速"路径。
- 【保留】无条件钉柱/edgeGuard/防窒息/水平速度清零/禁传 30 秒/收摊传回塔脚，全部不变（钉柱现在与速度冻结互为双保险）。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,919,796 字节）。
- 待实测：①垫高+塔顶全程零滑动（这是速度归零的直接验收点）②收摊后/狙击结束 → 移动能力立即恢复（跟得上人、走得了路）③下线重登 → 无残留 0 速 ④垫高爬升不受影响。
## 实测三百八十五

- 【根因】潜行边界钳制只能让她"贴边滑动"：塔顶虽然不走路了，但弓 AI 的横向 strafe 每 tick 都在推她，edgeGuard 拦在方块边上但拦不住"贴着边来回滑"（= 站在柱子上仍有移动行为）。要站桩不动只有钉回柱心一条路。
- 【修复】lockColumn 改回**全程无条件钉柱**（自保同款，攀爬期与塔顶统一）：每 tick 无条件 setPos 钉回柱心（≤2.5 格），被轰飞 >2.5 格仍然解锁掉落、落地重定基重起塔（三百八十二"去掉任意距离拉回"的取舍保留）。edgeGuard 保留作第二层边界兜底。
- 【定稿口径】狙击会话全程 = 零移动钉柱（垫高+塔顶一致），射击/索敌/收摊判定照常并行；收摊下塔 = 传回塔脚（三百八十三）。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,919,225 字节）。
- 待实测：①垫高全程钉在柱心不动 ②塔顶站桩输出全程钉在柱心不动（只有转身高视目标）③被强击退/爆炸炸飞 >2.5 格 → 掉落落地重定基重起塔 ④怪全死 → 传回塔脚。
## 实测三百八十四

- 【根因】实测三百八十一给攀爬期钉柱加的"偏出 0.3 格才拉"软阈值是错误设计：TLM 移动 AI 每 tick 都在推她，软阈值 = 允许她在柱内被推来推去（垫高期仍有可见移动行为），被一步步挤出塔沿掉下去。自保的钉柱是**每 tick 无条件钉回柱心**——这正是"自保能稳定成柱、狙击会掉"的残余差距。
- 【修复】攀爬期（!onTower）钉柱改回自保同款：**每 tick 无条件 setPos 钉回柱心**（≤2.5 格；>2.5 格解锁落地重定基）——垫高期零移动，贴着柱子长上去。
- 【分工定稿】垫高攀爬期 = 无条件钉柱（零移动）；塔顶站桩 = 潜行机制兜底（edgeGuard 边界钳制，无位置拉扯，三百八十二拍板不变）。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,919,252 字节）。
- 待实测：①垫高全程女仆钉在柱心不动，一路垫到 8 格顶 ②塔顶站桩输出 → 潜行钳制兜住不出格 ③被打下塔 → 正常落地重定基重起塔。
## 实测三百八十三

- 【悬空状态根因】实测三百八十的"速度降为 0"三维全清把**重力也清了**——她离塔下落时每 tick 垂直速度被清零，只剩重力逐 tick 累积 ≈ 1.5 格/秒的慢速漂浮下落（诡异悬空）。改回**只清水平、垂直归还物理**（水平清零继续压放风筝，垂直交给重力/击退/下落）。
- 【掉下去根因】收摊放生后的乱走：塔上收摊（目标死/无威胁）后潜行钳制随会话一起停，TLM 游走 AI 把她从塔边带走摔下 8 格。改为**收摊下塔 = 直接传回塔脚**（沿用实测三百七十三"直接传送比走下去实用"的拍板）：三个塔上收摊点（目标丢失/丢视野远离/无威胁）统一走 safeDismount——findStandNear 安全落点判定（主人身边传送同款）传回自己塔基旁实地，摔伤归零；找不到落点留在塔上不冒险。
- 【不受影响】潜行式边界钳制/攀爬期钉柱/清寻路停导航/水平速度清零/walkOn 推送/防窒息/禁传 30 秒闸全部保留；顶格 >10 格远程离塔收摊点不需要 dismount（人已不在塔上）。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,919,266 字节）。
- 待实测：①战斗中塔顶站桩 → 不再漂浮不再掉落 ②怪全死 → 塔上直接传回塔脚，无摔伤 ③被强击退打下塔 → 正常落地（不再慢速漂浮），按重定基重新起塔或继续战斗。
## 实测三百八十二（玩家 userselect："被击飞则从任意距离拉回塔顶（塔顶无松手）——这个机制去掉，现在只需要负责垫高就可以了，其他的都由潜行机制来兜底"）

- 【按拍板精简】去掉三百八十一的"塔顶无松手、任意距离拉回柱顶"——**钉柱（lockColumn 的位置拉扯）只负责垫高攀爬期**：偏出 0.3 格拉回柱心（0.3 内微动不管防抖）、被击飞 ≤2.5 格强拉回、>2.5 格解锁落地重定基。
- 【塔顶全交潜行机制兜底】塔顶（onTower）不做任何位置拉扯：柱内微动不管、到边走不动（edgeGuard 潜行同款边界钳制——脚下有支撑就把位置钳在支撑方块中心 ±(0.5-半宽) 出不了格）；被击飞落地后由重定基/重起塔自然兜底。
- 【不受影响】清寻路/停导航/速度清零/walkOn 攀爬推送/防窒息/边缘钳制/人离塔列 >10 格收摊/威胁扫描垂直半径 10/丢视野水平口径，全部保留。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,918,968 字节）。
- 待实测：①塔顶站桩期间小幅击退 → 边界钳制兜住不出格 ②强击退/爆炸炸飞 → 会掉下塔、落地后按重定基逻辑重新起塔（可接受的取舍）③攀爬期垫块 → 依旧稳定成柱。
## 实测三百八十一

- 【真凶一：塔顶收摊误判（威胁扫描框够不到地面）】hasNearbyThreat 扫描框垂直 ±4 格——人站 8 格塔顶时框只覆盖塔身中部，**塔脚下的怪根本扫不到** → "3 秒无威胁"在交火中误判 → 她被放生 → 乱动掉下去。垂直半径 4→10。
- 【真凶二：丢视野收摊的"远处"口径被塔高击穿】怪贴塔基时 3D 距离 = √(塔高²+水平²) ≈ 8.5 格恒 >4——三百七十六"目标在远处丢视野才收摊"的口径在 8 格塔上永远成立：视线被塔角挡 3 秒就收摊放生。改**水平距离**口径：目标水平 ≤4 格（就贴在塔脚下，摸不到塔顶的她）→ 钉住等它绕开；真正水平远离（>4）丢视野 3 秒才收摊。
- 【真凶三：钉柱"偏出 1 格即松手"】被击推出 1.1 格钉柱就解锁，AI 再推一把人就出塔——松手后 edgeGuard 又因脚下悬空（半空）失去支撑判定失效。这是"击退+走位一步步把人挤出塔"的元凶。
- 【玩家潜行式钉柱】三段式：**①柱内微动不管**（不做每 tick 中心吸附——吸附与 TLM 移动 AI 互相拉扯就是"乱动"抖动源，偏出 0.3 格内交给边界钳制）；**②到边界走不动**（edgeGuard 潜行同款：脚下有支撑就把位置钳在支撑方块中心 ±(0.5-半宽)，出不了格）；**③被击飞拉得回来**（塔顶无距离上限——1 格宽的塔顶不存在"塔外的合法位置"，被击退/炸飞多远都拉回柱顶；攀爬期 ≤2.5 格，>2.5 落地重定基重起塔）。
- 【配套】人已远离塔列 >10 格（解除归位传送后）→ 不钉柱直接收摊（防残留塔基把人从主人身边再拽回旧塔）。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,918,956 字节）。
- 待实测：①塔顶站桩输出期间骷髅远射/近战怪贴塔 → 全程钉在塔顶不乱动不掉落 ②被击退/爆炸炸飞 → 拉回塔顶继续输出 ③怪贴塔基绕圈挡视线 → 钉住不收摊 ④怪全死 3 秒 → 正常收摊跳下 ⑤解除自保传送归位 → 不会再被残留塔基拽回去。
## 实测三百八十

- 【"垫一块就游走"根因】实测三百七十七的建塔期复查门禁（敌人>贴身+2/姿态失效→弃塔）与 TLM 弓手 AI 的后撤放风筝互相打架：垫一块后 AI 一拉开距离就触发弃塔→人回风筝循环→敌人再贴身再垫一块——永远成不了柱。
- 【机制更替·完全承诺】触发条件只在首块前全量复查（敌人贴身+远程姿态+有方块+自动任务时无近战武器可切）；**首块一垫=完全承诺：建塔期不再有任何放弃判定**，钉柱+清寻路/停导航+**速度三维清零**（攀爬推送时保留垂直）一路垫到塔顶；垫块节奏 4→3 tick。
- 【塔高 3→8】COMBAT_SNIPE_HEIGHT 默认 8（范围 2~12）——几乎无怪能够到；下塔为跳下（约 3 颗心摔伤，落地水/自保兜底）。配置界面/语言同步。
- 【30 秒禁传闸】每垫成一块刷新 `maid_smart_snipe_notp` 时间戳（persistentData putLong，javap 验证 m_128356_/m_128454_）——自保的保命传送（teleportHome）、围困传送、解除归位传送（teleportHomeOnExit）在闸期内全部拦住：塔就是她的保命位，谁也不许把她拽走。
- 【手动远程任务可触发】新增 AutoCombatSwitch.isTaskAutoAssigned（ASSIGNED_TAG 与当前任务比对，参战时即写入）——"没有近战武器"门槛只对自动指派的远程任务生效；玩家手动指派的远程任务 = 战斗方式已被锁定，包里有剑也不会自动切 → 近身照常搭塔。
- 【自保与塔并行】狙击不再被自保（PRESERVE_TAG）压死（旧口径=手动远程女仆被围殴只反击永不搭塔）：自保查 isSniping 让位全部移动类动作（珍珠逃生/击退爆发/走位周旋/自保搭高），贴脸反击与喝药照常并行；安全阀=泡水/岩浆时狙击立即让位给环境逃生（塔不能建成溺水陷阱）；解除归位传送在禁传闸过后照常生效（三百七十八语义保留）。
- 【配套】目标丢失且人已远离塔列（>4 格，如被传送归位后）→ 立即收摊不等 20 tick（防传送后钉柱段原地定住 1 秒）。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,918,584 字节）。
- 待实测：①自主战斗弓女仆被近身 → 原地定住一路垫满 8 格站桩输出，全程不再游走 ②手动远程任务女仆被近身 → 同样起塔（包里有剑也搭）③塔上期间自保触发 → 不传送、不被拽走，反击照常 ④怪全死 3 秒 → 收摊跳下，方块 30 秒回收 ⑤泡水/岩浆 → 立即放弃塔位正常逃生。
## 实测三百七十九

- 【日志实证】latest.log：`combat pools: maid=汉服酒狐 melee=[touhou_little_maid:attack,] ranged=[maidspell:spell_combat_melee,maidspell:spell_combat_far,]` + `retune: touhou_little_maid:attack -> maidspell:spell_combat_far (dist=14.9)`——女仆没有弓（原版 ranged_attack 根本没进池），两个万法皆通魔法任务却进了远程池，远距离被切去魔法。
- 【根因（javap 反汇编实证）】万法皆通 SpellCombatMeleeTask.isWeapon 字节码为 `iconst_1; ireturn`——**恒返回 true**（hasSpellBook 同样恒 true）：背包里任何物品（原版剑/食物都行）都被认作"魔法武器"，两个魔法任务凭空进池；池内有模组任务时原版任务整体让位（实测一百八十一规则）→ 原版任务被挤掉。
- 【修复：模组物品背书】buildPools 新增门禁——**模组任务必须匹配到至少一件非原版（命名空间 ≠ minecraft）物品才进池**：只给原版武器 → 万法皆通任务不再进池，剑娘远距离继续追击用剑；女仆持有万法皆通物品（捡到/给予）→ 魔法任务照常参与（实测一百八十一"模组武器→模组任务"语义保留）；拔刀剑/史诗战斗武器本就是模组物品，不受影响。
- 【新配置】`模组任务参与自主切换`（COMBAT_AUTO_SWITCH_ALLOW_MOD_TASKS，默认开）：关 = 模组任务完全不参与自主切换，只用原版任务（近战/弓/弩/三叉戟/弹幕/枪械）；配置界面战斗页 + zh_cn lang 同步。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,917,857 字节）。
- 待实测：①只给剑 → 自主战斗全程近战追击，不再切魔法（日志 combat pools 的 ranged=[] 为空）②只给弓 → 正常弓手放风筝 ③给她一本万法皆通物品 → 魔法任务恢复参与 ④手动指派魔法任务芯片不受影响（自主切换不碰手动任务）。
## 实测三百七十八

- 【解除即传】自保退出（血 ≥70% 安全 1 秒 / 完全安全 20 秒，两条退出线 + 20 tick 稳定窗口任一满足）→ **立刻传送回主人身边一次**，不再要求"威胁已散"（旧口径交火中退出只交还战术继续打）——恢复即支援，交火中也撤出战场归位。
- 【绕过冷却闸】teleportHomeOnExit 增加 ignoreCooldown：解除归位是每场一次的收尾动作，绕过"5 秒尝试间隔 + 30 秒成功冷却"双闸（否则上一场传送的长冷却会把解除归位静默吞掉）；会话已结束不存在连传。安全护栏全保留：主人死亡不传（实测八十三）、看家模式不传、已在主人 5 格内不传、主人身边无合法落点不传（实测二百零五防摔）、跨维度安全落点判定不变。
- 【配套：狙击残留状态重置】自保接管时狙击彻底重置（snipeBaseY 清 -1）——解除必传后"解除后复用旧塔基"路径不复存在；否则传送归位后残留塔基会让狙击钉柱段在主人身边空跑（原地定住约 1 秒才被目标丢失收摊兜住）。塔块仍登记、30 秒自回收。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,916,976 字节）。
- 待实测：①残血自保 → 回到 70% 立即传回主人身边（哪怕怪还在旁边）②完全安全 20 秒退出 → 同样立刻归位 ③主人悬空/死亡/看家模式 → 不传不摔 ④自保期间搭的塔在她离开后 30 秒回收。
## 实测三百七十七

- 【不分场合搭方块·根因一】三百七十五的"搭高惯性"放得太开：起塔条件（敌人贴身/没近战武器/远程姿态）只在第一块之前查，之后永不放弃——敌人短暂贴身一次就 commit 整座塔；自保退出后旧塔基续建路径同样不重查条件。现改为**建塔期每 tick 复查 + 滞回**：敌人 > 贴身距离+2 格（自家箭击退 4~6 格的抖动不弃塔，真跑了就收摊）/捡到近战武器/远程姿态失效 → 弃塔回归正常战斗；攀爬推送中（walkOnTicks>0）暂不弃——半途松锁是"垫两块摔下来"的元凶。起塔门禁保持 ≤4 格不变。
- 【不分场合搭方块·根因二】hasRangedPosture 末尾兜底 return true 让"远程任务+主手为空"（切枪中/武器被没收）也算远程姿态。现主手空 → 直接 false。
- 【方块永不消失·根因】expireCombatPlaced 每 tick 都在跑，但 PlacedBlockTracker.maidNearBlock 硬编码"绑定女仆水平 ≤4 格恒刷新"（实测二百零七为搭路桥防摔设计的口径）——女仆在自己塔边打架就永远给塔续命。现半径参数化：**战斗方块实例 0.5 格（仅同柱刷新）**——站塔上整柱恒刷新不塌，人一离开塔 30 秒后必回收进背包；搭路/桥实例保持 4.0 默认（二百零七防摔口径不变）。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,916,913 字节）。
- 待实测：①近战女仆/有剑的远程女仆 → 任何情况都不再搭方块 ②弓箭女仆敌人贴身 → 起塔，敌人跑出 6 格 → 弃塔回归放风筝 ③塔上站桩结束离开后 → 塔块 30 秒回收消失 ④搭路模式不受影响（桥块靠近 4 格内仍恒刷新）。
## 实测三百七十六

- 【无威胁收摊误判修复】hasNearbyThreat 只扫水平 8 格——骷髅/远程怪在 8 格外放箭时被误判"威胁解除"，她在交火中自己走下塔。现在**自家攻击目标活着且带敌意（PerceptionManager.isThreat）也算威胁**，继续站桩输出；目标死了/跑了且周围也扫不到威胁 → 3 秒后照常收摊。
- 【贴塔怪不收摊】怪贴着塔基时视线擦过塔角 → 丢视野 3 秒收摊 → 走下塔正走进它怀里。视线守卫改口径：丢视野 3 秒只在**目标在远处（> 贴身距离）**时收摊（被墙挡住 → 让 TLM 寻路重占位置）；目标贴塔（≤4 格）时它摸不到塔顶的她——钉住等它绕塔让开，视线恢复继续狙击。
- 【目标丢失收摊硬门禁】目标记忆丢失 ≠ 战斗结束——TLM 感知换目标的间隙/目标短暂出 16 格护栏时，周围仍有真威胁就**继续钉在塔上等再锁定**（收摊 = 松钉柱 = 走下塔挨打，是最差结局）；周围也扫不到威胁了才收摊（0.5 秒扫一次，防每 tick 全场扫描，新增 lostScanCd 节流）。
- 【战术塔顶禁走位】MaidCombatTacticsBehavior 让位检查新增：脚下 -1/-2 格是自己登记的战斗方块（狙击塔/自保垫高塔，PlacedBlockTracker 口径）→ 本 tick 让位。狙击收摊后/自保接管期间 SNIPING 标记已清，此前战术走位会把人从塔上拽下去（"搭高完成后自己走下去"的又一条主路径）；塔顶交还 TLM 攻击 AI 站桩输出。
- 【登记寿命无需另修】expirePlaced 已有"绑定女仆站上恒刷新 + 靠近（水平 ≤4/垂直 ≤6）恒刷新"——塔顶站桩期间整座塔不会中途到期塌陷。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,916,797 字节）。
- 待实测：①塔顶站桩期间远处骷髅放箭 → 不再自己走下塔，站桩对射 ②近战怪贴塔基游走 → 钉在塔顶不下来，视线恢复即狙击 ③怪全死 3 秒后 → 主动下塔 ④自保接管又恢复后（30%~70% 区间）→ 不再被战术走位拽下塔。
## 实测三百七十五

- 【结构性根因】自保柱子稳的真正原因不是 walkOn 本身，而是**钉柱+清水平速度+walkOn 推送在 m_6725_ 最顶上、任何 early return 之前每 tick 无条件执行**，且后面紧跟 edgeGuard+antiSuffocate。狙击此前把 lockColumn 放在目标校验/放弃判定之后：攻击目标记忆抖动一 tick、或自家弓箭把敌人击退出 4 格一瞬（触发 dist>4 放弃判定），锁就漏空，TLM 移动指令/击退趁虚而入把人拽偏——"垫两块就摔"在狙击上复现的根因。
- 【钉柱段顶置】狙击 m_6725_ 重构为自保同款结构：snipeBaseY>=0（塔在建/建成）期间每 tick 顶部先执行 SNIPING.add + lockColumn（清寻路目标/停导航/钉柱/清水平速度/walkOn 推送，逐行=自保 pillaring 段）+ edgeGuard + antiSuffocate，之后才做目标校验/收摊判定——目标记忆抖动不再漏锁。
- 【edgeGuard/antiSuffocate 逐行搬运】自保 v1.5.140/241 边缘钳制（脚下悬空查 4 斜下对角支撑）与 v1.5.25 防窒息（bodyInSolid 身体中心口径，半身卡块立即顶到方块顶面）完整移植进狙击，取代旧 lockColumn 内的轻量头顶检查（旧口径只查脚上 0.9 格，横向被挤半身入块查不到）。
- 【搭高惯性】塔开建后不再因敌人拉开而中断（自家弓箭把敌人击退出 4 格 ≠ 放弃塔）——放弃条件只剩：捡到近战武器（站稳当前块后收，攀爬推送中不收）/目标丢失 1 秒/塔顶丢视野 3 秒/无威胁 3 秒。起塔前的降级门禁（敌人不贴身不起塔）不变。
- 【攀爬保护】目标记忆丢失不累计收摊计时（walkOnTicks>0 时跳过）——先站稳这一块再谈收摊；clearSnipe 收摊时必清 walkOnTicks，不带惯性进下一 tick。
- 编译零错误；已部署 D:\.minecraft\versions\1.20.1-Forge_47.4.21\mods\promaid-1.1.0.jar（4,916,593 字节）。
- 待实测：①弓箭女仆被近战怪贴脸且包里没剑斧 → 起塔应能一次成柱到 3 格顶站桩输出 ②搭塔途中箭把敌人击退出去 → 不再中途弃塔，继续垫到顶 ③包里有剑 → 正常切近战不搭塔 ④敌人全死/走光 3 秒 → 从塔顶下来回归战斗。
## 实测三百七十四

- 【触发重定义】高地狙击从"远程交战的常规战术"降级为**反近身兜底**，四条件全满足才起塔：①远程攻击状态（远程任务+手上真是远程武器+弓有箭）；②**包内没有近战武器**可切（攻击伤害属性口径，含手部栏——有剑/斧就正常走自主切换切近战，绝不搭塔）；③背包有搭方块；④敌人近身（攻击目标进入贴身距离 COMBAT_CLOSE_DISTANCE，默认 4 格）。
- 【语义】= 自主切换远近武器逻辑里"没有武器可切"的分支改为尝试搭方块：远程女仆被近战怪贴脸、又没有近战武器能换 → 垫塔拉开高度继续射；平时远程交战/敌人还远 → 照常放风筝，不再抢着搭塔卡掉自主战斗。
- 【中途放弃】搭塔期间敌人拉开或捡到近战武器 → 放弃建塔回归正常战斗（已垫方块 30 秒自回收）；塔已成型则继续守到退出条件（目标死/丢视野/无威胁 3 秒）。
- 【配置提示】"高地狙击"说明文字同步新语义。
- 已部署 4,915,963
- 待实测：①弓箭女仆远程放风筝时不再搭塔；②被近战怪贴脸（4 格内）且包里没剑斧 → 起塔反制；③包里有剑 → 正常切近战不搭塔；④敌人拉开半途 → 放弃搭塔回归战斗。

## 实测三百七十三

- 【下塔机制删除】MaidSafeDescend 整类删除（自保/狙击/自动切换所有引用清空）：塔顶状态在退出/围困时直接清空——威胁已散 → 传送归位（"直接传送下来"）；交战中 → 任务/战术 AI 自然下塔（3~5 格坠落有落地水兜底）。自保"无回血资源被围困 10 秒" → 清塔 + 直接传送到主人身边（播报"包里没有回血的东西了，我先撤到你身边！"）。顺带修复：围困计时器被重复块计了两次（实际 5 秒就触发）→ 恢复 10 秒。
- 【狙击塔完整套用搭路 walkOn】上一版只借了"起跳"，没借关键的"持续推送直到站上"——现在完整照搬搭路 beginWalkOn/walkOn：垫块后登记目标格（12 tick 超时），每 tick 重给垂直速度 0.42 推送，直到 feetY 达到方块顶才停止并允许垫下一块；被击退打断也会继续推（不再一次脉冲后靠天意）。自保垫高同款。钉柱/清水平速度/清寻路全程保留。
- 已部署 4,915,224
- 待实测：①弓箭女仆狙击塔一格格跳着成型、站定开射；②自保垫高同样成柱；③塔顶收工/围困后走传送，不再出现"下塔"环节。

## 实测三百七十二

- 【采纳搭路逻辑】自保 buildUp 与狙击 snipeBuildUp 全面改用搭路模式（实测八十四/一百八十）验证过的"垫块 → 原地起跳（垂直速度 0.42 ≈ 跳 1.1 格）→ 落在刚垫的方块顶上"——替代挤压顶起（横向弹射，三百七十一根因）和 setPos 传送（观感生硬），物理自然、视觉像玩家搭路。
- 【站稳才垫下一块】搭路"踏入目标格才停"节流同款：上一块垫的方块 Y 有记录，人没站上去（feetY < 方块Y+0.95，起跳中）→ 本 tick 等待不垫下一块——固定 4 tick 冷却会在半空中垫下一块的老毛病根治；等待算"进行中"不算失败（不触发无材料播报/放弃建塔）。
- 【搭路同款细节】垫块摆臂动画（m_6674_）+ 放置音效（placeSound）补齐；狙击补防窒息兜底（起跳失败身体嵌进方块 → 推到顶面，桥模式精简版）。
- 【补丁·锁定先于下落守卫】：狙击侧"下落中不垫块"守卫原在钉柱之前——起跳落向新方块顶的几 tick 有微小 fallDistance 恰好绕过锁，TLM 移动指令趁窗口拽人。修正：下落中也钉柱+清水平速度，守卫只阻止"下落中垫块"本身。自保侧确认无此窗口（钉柱全程生效）。
- 已部署 4,918,667
- 待实测：①自保/狙击垫高一格一跳自然成柱；②半空中不再抢垫下一块；③被击退打断起跳 → 钉柱拉回 + 下一块继续，柱不歪。

## 实测三百七十一

- 【物理根因】此前垫高依赖"方块放进她所在格 → 碰撞解算把人挤上去"。但实体在柱内被放块时，逃逸方向按【最小穿透轴】选择：她宽 0.6、柱内水平净空仅 0.2，向上顶起需要 1.0——**横向逃逸永远比向上更容易**，稍有偏移就被横向弹出半柱。这就是"垫两块就摔、反复循环"的物理根因，锁速度/钉位置都只是治标（防的是走偏，防不住弹射）。
- 【根治】buildUp/snipeBuildUp 放置成功后【直接把她传送到刚垫方块的顶面】（setPos 确定性操作，不经过碰撞解算）——每一块都精确 +1 格，柱心由钉柱机制保持，头颈空间由放置前的空气检查保证。附带效果：下落中垫块会直接把人接到方块顶上（变相接住坠落）。
- 已部署 4,918,177
- 待实测：①自保垫高一口气到 5 格成柱；②狙击塔一口气 3 格；③塔顶站桩不漂移；④下落中垫块把人接住不摔。

## 实测三百七十

- 【正常近战也垫塔·根因】狙击行为的"远程姿态"判定（远程任务+手上真是远程武器）**定义了但从未接线**——canUse 只查开关，任何有攻击目标的女仆（含纯近战满血）都进了建塔逻辑。修复：canUse = 开关 + 远程姿态门禁（远程任务 + 弓弩有箭/枪械/御币），纯近战任务永远不搭塔。
- 【摔柱根治·钉柱】velocity 锁挡不住两个物理过程：①TLM 攻击寻路每 tick 重新下导航指令（清了下一 tick 又走）；②脚下放块顶起瞬间，实体若稍有横向偏移会被碰撞解算**横向弹出**柱体。修复 = 位置钉柱：垫高开始时记录柱心坐标，之后每 tick 清寻路/停导航 + **把人直接钉回柱心**（强拉范围内）；被击飞离柱（>1 格）则解锁交回重评估。自保（forcedPillar/pillarBaseY 全程）与狙击（建塔+塔顶）同机制。
- 已部署 4,918,134
- 待实测：①近战女仆正常打架不再垫脚；②弓箭女仆狙击塔/自保塔一口气成柱不再反复摔；③被击飞离柱后正常重评估（重垫/走位），不会瞬移拉扯。

## 实测三百六十九

- 【根因】垫方块靠"脚下放块把人顶起"，只要她还在走（战术走位/任务移动/导航残留），顶起瞬间就偏离柱心、从 1~2 格高的半柱上摔下来——自保垫高与狙击建塔都中招。
- 【垫高全程锁移动】自保侧：`forcedPillar`/`pillarBaseY` 任一激活（开垫到塔顶站桩全程）→ 每 tick 清寻路目标 + 停导航 + 清水平速度（保留垂直——顶起/下落靠垂直位移，坐姿锁 v1.5.348 同款教训）；战术经 moving 标记同步让位。围困下塔触发时塔基同步清掉，锁交给下塔机制。
- 【狙击建塔同样锁】SNIPING 标记提前到**起塔瞬间**（自动切换/战术走位从第一块起就让位，之前只挡塔顶站桩、建塔期照样走位正是摔柱主因之一）；建塔期同款三重锁；没材料不锁不置标记、建塔放弃（头顶被堵/方块耗尽）清标记交还战术。
- 已部署 4,917,632
- 待实测：①自保垫高一口气到 5 格不再半路摔；②狙击塔一次成柱 3 格；③建塔放弃后战术走位正常恢复。

## 实测三百六十八

- 【击退抑制】塔顶每 tick 清一次水平速度——箭矢/远程弹的击退在下一 tick 就被抵消，人基本不会被推下塔（爆炸掀掉方块等真掉落场景走下述兜底）。
- 【掉落伤害】狙击塔 3 格，掉落越过原版 3 格免伤线之前落地 → 零摔伤；更高的塔有落地水（水桶）兜底。
- 【塔基重置（bug 修复）】被击落到地面后重新垫塔时，塔基 Y 还记着旧塔基准 → 按"离旧塔基 3 格"续垫会垫出 6 格怪塔。修复：落到低于旧塔基的实地 → 塔基重置到当前位置，重新垫 3 格爬回去继续狙击。
- 【下落禁垫】下落中（fallDistance>0）不垫块——防"半空中垫出悬浮块"的边角（落地站稳再垫）。
- 【链条确认】被射到血 <30% → 自保无缝接管：远程怪在 4~12 格 = 走位周旋 + 喝药回血，<15% 危急传送，弹尽粮绝传送兜底；狙击模块清标记让位，不抢移动。
- 已部署 4,917,558
- 待实测：①被骷髅连射 → 塔顶不被箭的击退推下去；②被炸下塔 → 落地零摔伤、重新垫 3 格（不是 6 格）爬回塔顶；③边掉血边被射 → 残血自保接管走位喝药。

## 实测三百六十七

- 【威胁解除 → 主动下塔】狙击塔顶每 0.5 秒扫一圈周围（水平 8 格、垂直 ±4，复用感知系统统一威胁口径）：**持续 3 秒不存在任何威胁 → 主动安全下塔**，不再一直站上面；扫到威胁立即重新计时。与"记忆目标消失 1 秒下塔"互补——目标记忆残留时也能靠真实威胁扫描收摊。
- 【塔顶防走位漏洞（链路捋查发现）】自保接管狙击塔后若无回血资源会走"走位周旋"——但人在 3 格塔顶，走位导航会把自己从塔边拽下去摔伤。修复：脚下（及下落容错第二格）是登记战斗方块 = 站在自家塔顶 → 禁止走位，原地站桩（贴脸反击/战术并行），下塔交给下塔机制。近身爆发分支同款守卫。
- 【下塔=下挖确认】下塔机制本质就是"逐格挖掉自己垫的方块往下走"（MaidSafeDescend，八道保险见三百六十六），本次两处修复均不触碰其核心逻辑。
- 已部署 4,917,438
- 待实测：①狙击打死最后一只怪 → 约 3 秒后自动拆塔下地；②怪走远（8 格外无威胁）→ 同样下塔；③残血无资源的自保女仆在塔顶不再自己走下来，等围困 10 秒下塔机制接手。

## 实测三百六十六（定夺："老版本远程女仆搭高狙击的骚操作有实战价值，战术可以有条件实现"；要求：战斗方块 30 秒、下塔逻辑多加保险）——高地狙击 + 安全下塔公共化

- 【高地狙击·新战术】新增 HighGroundSnipeBehavior（core 235，默认开，配置页"战斗与自保"区有开关和塔高）：远程女仆（弓/弩/枪械/御币弹幕等远程任务）面对敌人时垫 3 格方块（可配 2~6）站上制高点**站桩输出**——近战怪摸不到，老版本的"骚操作"回来了。
- 【只管位置不管射击】本行为只负责搭塔+站定+锁水平位移，瞄准开火完全交给 TLM 自己的远程攻击 AI——不重复造射击轮子，零冲突面。
- 【与自动切换武器的冲突解法】狙击激活期间置 SNIPING 标记：AutoCombatSwitch 的还原/换近远程扫描查标记直接跳过（她已经在好位置上拿着对的武器）；MaidCombatTacticsBehavior 的走位查标记让位（站桩，不绕圈把人拽下塔）；狙击结束由模块自己清标记。
- 【触发门槛】远程姿态（主手投射武器/枪械/远程任务）+ 弓弩有箭 + 背包有垫脚方块 + 目标 16 格内；进自保会话则让位并清标记（塔保留给自保守势/下塔复用——同一座塔无缝换挡）。
- 【退出条件】目标死亡/丢失 1 秒、塔上丢视野 3 秒（目标绕墙进屋）、弹药耗尽、方块耗尽搭不上去、骑乘/坐姿——退出时人在塔上走 MaidSafeDescend 安全下塔。
- 【战斗方块寿命 60→30 秒】要求"利落"：自保/狙击搭的方块 30 秒回收（女仆站上面的方块照旧刷新计时，走开才开始倒数，不会塌塔摔人）。
- 【安全下塔公共化 MaidSafeDescend】自保与狙击共用唯一实现，按要求加足保险，共八道：①blockId 比对——玩家替换过的方块绝不拆；②脚下非登记块只在落到实地才收工、空中一律等待，绝不拆自然地形/玩家建筑；③落地判据为实心方块（含台阶/耕地等碰撞形状口径）；④骑乘/坐姿立即收工；⑤10 秒总时长强制收工（任何异常不锁死）；⑥下塔每 tick 清寻路+停导航（防任务/战术拽人）；⑦拆块节流 4 tick（重力落位，不产生多格坠落）；⑧死亡/维度异常立即收工。PlacedBlockTracker 补 trackedBlockId（登记方块名查询）与 untrackAny（主动拆除移除登记，不留空转条目）。
- 已部署 4,916,659
- 待实测：①弓箭女仆遇怪 → 垫 3 格塔站桩放箭、近战怪在塔下干瞪眼；②狙击期间武器不切、走位不绕圈；③目标死亡/进屋 → 自动拆塔下地、方块回背包；④狙击中被射残血 → 自保接管同一座塔回血；⑤自保下塔回归实测三百六十五场景。

## 实测三百六十五

- 【垫高门控（根治）】包里没有任何回血资源（药水/金苹果/治疗食物，hasHealResource 无副作用探测）→ 搭高【不再是可选项目】：被近身 = 击退爆发改为走位周旋、近身搭高分支整体跳过——塔的唯一意义是安全回血，没得回血垫塔纯属干耗，从根上断绝"无意义的塔"。
- 【"回家"取消】塔顶无回血资源被围困 10 秒 → 不再传送回家，改为**主动下塔接着打**（播报"包里没有回血的东西了，我下塔接着打！"）；传送只保留血 <15% 危急 / 弹尽粮绝 / 泡岩浆三个保命兜底 + 战斗结束后的归位支援。
- 【安全下塔机制】新增 tickDescend：逐格拆掉脚下【自己登记过的战斗方块】降下（4 tick 一格，重力自然下落零摔伤，方块回收进背包；自然地形/玩家建筑绝不碰）；下塔期间清寻路/停导航、战术让位（MOVING_SURVIVE），10 秒保险防卡死，跨会话执行完（退出自保后塔也要下完）。
- 【退出下塔】解除自保时人还在塔上且威胁还在（不能传送）→ 先启动安全下塔再归位/继续战斗；威胁已散则直接传送归位，不用下。
- 【被打下来】塔基 Y 有记录，掉回基准线以下自动清空搭高状态重新评估：材料够且有回血资源 → 击退怪接着搭；无回血资源 → 走位周旋；血危急 → 珍珠/传送；下落瞬间有落地水兜底。
- 已部署 4,908,026
- 待实测：①空包（没吃的没药）残血被围 → 不再垫塔，原地走位周旋+反击；②塔顶吃完最后一份食物被围 10 秒 → 播报并逐格拆塔下来继续打；③血回 70% 解除自保时在塔上且威胁还在 → 拆塔下来而非传走；④被打下塔 → 立刻重新评估接着搭/周旋。

## 实测三百六十四

- 【解除线回调】安全回归血量（自保解除线）默认 0.45 → **0.70**：残血自保要真回血才归位，45% 太贴地；滞回带回扩为 30~70（重新进场要跌回 30% 以下，不会抖动）。"完全安全 20 秒无论血量解除"的第二退出线保留——没回血资源的女仆不会被永久卡在自保里。
- 【塔顶无回血资源兜底】新增 hasHealResource 无副作用探测（包里有无药水/金苹果/治疗食物）：垫高到顶后威胁持续扎营、包里又没有任何回血资源 → 在塔上干耗没有意义，累计 10 秒触发传送回家养伤（主人可以喂她）；有回血资源的不受影响，安心塔顶守势回血到 70% 自然归位。
- 【配置文案】"安全回归血量"行改名"解除血量"（默认 0.7）；语言条目同步。
- 已部署 4,906,988
- 待实测：①残血搭高后包里没吃的没药、怪在塔下扎营 → 10 秒后被接回家；②包里有熟食/药水 → 塔顶安心回血到 70% 才归位；③完全安全 20 秒后无论血量多少都解除自保。

## 实测三百六十三

- 【垫高何时停】垫到安全高度（pillarSafeHeight，默认 5 格）即停，头顶被堵/没材料也停——塔顶进入守势：安心回血（药水→金苹果→食物阶梯）+ 战术 230 并行对塔下反击，塔是安全点不是终点；**血回 45%（安全回归线）即下塔恢复战斗/工作**，不再干等威胁散伙/回满 70%；塔顶全程不传送。
- 【措施切换时机】承诺窗口制：移动类策略（搭高/走位）选定后执行到位（搭到顶/走完这几步）才重新评估；瞬时动作（喝药/进食/贴脸反击/珍珠）随时插队不打断当前策略；环境危险（岩浆/着火）永远最高优先抢占。
- 【进出判定】进入不变：血 <30% + 威胁（或环境危险）上升沿。退出二线（都过 1 秒防抖窗）：①血 ≥45% 即解除——**威胁还在也退**（恢复即支援，战斗交还战术、工作交还任务）；②完全安全持续 20 秒（threatGoneExit）解除【无论血量】——危机结束就该回归，带伤回家干活，再被打回 30% 以下重新进入（回血资源耗尽也不会永久滞留）。
- 【传送两时机】A 保命兜底：泡岩浆 / 血 <15% / 弹尽粮绝（有威胁+血 <30%+无方块无珍珠——走位拉不开死局）；搭高进行中与走位承诺期不传（危急/岩浆例外）。B 恢复后归位：解除自保时威胁已散且离主人远 → 传回主人身边支援。**恢复中（30%~45%）不提前接走**——原地回完血再走。
- 【逃跑删除】flee（反方向蛇形撤离）与 fleeEscaping（8 格突围扫描）删除，替换为 strafeDisengage **小幅走位**：朝威胁侧后方挪 2~3 格（8 向打分：怪少+偏离威胁+避岩浆），12 tick 承诺窗口，攻击目标不清、贴脸反击照旧——战斗姿态下的拉开身位，不再临阵脱逃。八方皆险（围死）原地站桩硬撑交反击/搭高/传送。
- 【联动清理】"情况不妙，我先撤了"→"情况不妙，我小心周旋"；"保命物品下保留逃跑"开关改名"保命物品下允许惊慌"（现只管 TLM 原生惊慌+播报，自保走位/搭高不受影响）；"逃跑速度"改名"走位速度"；hasLocalEscape 不再把"能逃跑"算逃生资源（走位零成本）。
- 已部署 4,906,626
- 待实测：①残血贴身+有圆石 → 搭到 5 格塔顶守势回血，45% 下塔继续打/干活；②威胁还在血回 45% 也会解除自保（不再被 tag 卡住干耗）；③没方块没珍珠被围殴+血 <30% → 才会传回主人身边；④被 3~4 只怪追时小幅度侧移绕着打，不再一口气跑出 8 格。

## 实测三百六十二

- 【根因一：传送优先级错位】自保会话每 tick 第一步就无条件尝试传送回主人——主人身边安全（最常见场景）时，搭高/珍珠/逃跑这些本地逃生永远轮不到执行：残血刚进场、方块刚垫两块就被拽走，塔白搭；传回→回血→跑回危险区→再传还形成连传循环（成功传送冷却实际仅 5 秒）。
- 【传送降级为兜底】新 tryTeleportFallback：只在五种【本地手段失效】情况传送——①泡岩浆（传送比任何自救都快）②血量危急 <15% ③威胁已消失且血未回安全回归线（安静接回家养伤）④威胁仍在但本地逃生无计可施（无搭方块材料+无珍珠+不允许逃跑，hasLocalEscape 无副作用探测）⑤塔上被围困（已垫到安全高度但威胁持续 10 秒不散）。
- 【策略承诺窗口】搭高进行中（forcedPillar/pillarBaseY 未到安全高度）或逃跑方向承诺期（fleeDirCooldown>0）不传送——选定策略执行完再谈换招；危急/岩浆例外（保命优先）。
- 【成功传送长冷却】teleportCooldown 语义重定义为"成功传送后的冷却"（默认 200→600 tick=30 秒，一场遭遇战最多被接走一次）；传送失败（主人身边有怪/无落点）重试间隔固定 5 秒保持实时性。自保传送与退出传送共用此闸。
- 【根因二：退出灰区】旧版唯一退出条件"血 ≥70% 且无环境危险"——女仆明明已脱险却因 PRESERVE_TAG 不清被排班/参战/搭方块等系统持续让位，站在原地干耗到 70%。
- 【退出链重做】三条解除线（都需 20 tick 稳定窗口）：①威胁消失+血 ≥ 安全回归线（safeReturnRatio 默认 0.45，闲置配置正式启用）——危机解除即归位，滞回带从 30~70 收窄到 30~45；②血 ≥70% 绝对线+无环境危险——占上风直接回正常态（威胁还在也退，战术并行继续打）；③威胁消失+已在主人 5 格内持续 threatGoneExit（20 秒）——回血无望限时收场防永久滞留。威胁扫描提前到退出判定前（原判定在扫描之前，用的是上一轮的旧威胁状态）。
- 【配置面板】战斗页："解除血量"更名"绝对解除血量"、"传送回家冷却"更名"传送成功冷却"，safeReturnRatio/threatGoneExit 提示语更新；zh_cn 语言条目同步。
- 已部署 4,907,067
- 待实测：①残血女仆 3×3 内有怪、背包有圆石 → 就地搭高躲伤害回血，不再被立刻传走；②打完脱险血回 45% 即恢复工作（不再干耗到 70%）；③同一场遭遇战最多被传送一次（30 秒冷却）；④背包空无一物被围殴 → 才会传回主人身边。

## 实测三百六十一

- 【伐木误报】走到树前检查主手非斧 → 播报"我没有斧头"——施肥换手中主手是骨粉（斧头正躺在副手）也会命中。修正：主手是骨粉且换手暂存中 → 先还原主手（斧头回来直接开工）再判定，不再对着骨粉喊没斧。
- 【挖矿误报】状态查询"我没有镐，没法挖矿"同款——主手骨粉 + 换手暂存中 → 不播报。
- 【新入口】MaidPlanting.isHandHolding（查询换手暂存状态）。
- 已部署 4,905,941
- 待实测：伐木女仆施肥后走到树前 → 不再说"我没有斧头"（斧头从副手还原直接砍）；挖矿女仆同款不再误报没镐。

## 实测三百六十

- 【道具复制根因】换手状态机用"原物品 insert 进背包槽、还原时 setStackInSlot(0, original)"搬运——【同一个 ItemStack 实例同时被背包槽和主手槽引用】（背包槽旧引用未清），每次换手循环多留一个幽灵引用，存档时同一实例重复序列化 → 读档后背包刷出大量斧头/锄头副本。
- 【状态机重写】原主手物品改放【副手】暂存（随实体持久化——施肥中途关服也不丢），不再过背包；全链路 extract-then-place（每个槽位写入前先把旧实例完整取走），任何实例任一时刻只存在于一个槽位，复制路径彻底封死。HAND_HOLD 只记还原时刻。
- 【优先级】伐木/耕地 > 骨粉：①农场——锄地有活（找到可锄目标/正在锄/正在走过去）本轮不施肥，锄地无活才催熟作物；②伐木——正在砍树/找树（isWooding 占用）不施肥，空闲间隙才催熟树苗。工作独占主手，不再和骨粉来回抢（鬼畜消除）。
- 已部署 4,905,703
- 待实测：①农场女仆在有踩坏耕地的农田里：锄地优先、不鬼畜，锄完空闲才施肥；②伐木女仆砍树时不施肥、砍完空闲才催树苗；③反复施肥几十轮后存档重进——背包不再刷出多余工具；④施肥中途关服重进——工具不丢（在副手）。

## 实测三百五十九

- 【根因】equipBoneMeal 换手用 setStackInSlot 直接覆盖主手——换手状态存活期间（持续施肥时 1 秒宽限不断刷新，等于条目一直在），锄头/斧头自动装备等第三方行为可能把别的工具换进主手（顶掉我们的骨粉）；下一轮施肥时 original 记账还停留在第一代，setStackInSlot(0, 骨粉) 会把第三方换上的那个工具【原地覆盖消失】——这就是"工具被卡掉"。
- 【修复】覆盖前先把当前主手物品安顿（收进背包，满则脚下掉落防丢）——任何情况下不再有无物品覆盖主手；original 记账保持第一代原物品，还原链路（restoreNow：主手空才放回、否则收背包）本就安全。
- 【同查】ensureHoeForFarm 是交换式（旧主手进背包槽）、restoreNow 只在主手空时放回——均无丢失点。
- 已构建 4,905,692（部署待游戏退出）
- 待实测：伐木/农场女仆背包放骨粉 → 施肥若干轮 → 斧头/锄头完好（施肥时主手举骨粉，停止后还原），不再消失。

## 实测三百五十八

- 【排查结论】锄地核心逻辑自实测三百四十九以来零改动（git diff 确认）——判定链有 6 个静默失败点（无锄头/无"曾经是耕地"标记/上方非空气/canDestroyBlock 拒绝/范围内无目标/距离远只导航），外部完全不可见，无法定位。
- 【全链路诊断】锄地每 30 秒/女仆落一条诊断日志（logs/promaid.log 搜 "till diag"）：hoe=有无锄头、farmland=范围内耕地数（标记自愈来源）、dirt/marked/airAbove=候选漏斗各环计数、tillable=最终合格数、target=本次目标；无锄头单独落 "hoe=false"；锄地成功落 "till ok: pos="（每次落）。跑一轮即可从漏斗计数看出卡在哪一环。
- 已部署 4,905,656
- 待实测：农场女仆在有踩坏耕地的农田待 30 秒 → 日志出现 "till diag"（漏斗计数）→ 把该行反馈即可精确定位；若正常工作则直接出现 "till ok"。

## 实测三百五十七

- 【根因】上一版"施了肥就跳过本轮锄地"在【有骨粉 + 有未成熟作物】时每轮必施肥（0.5 秒一轮全命中）→ 锄地被永久挤掉 → 踩坏的耕地再也不修。
- 【共存方案】锄地与施肥同轮共存、锄地优先：每轮先锄地（锄地前【强制还原】施肥换手——剩余骨粉收回背包、原物品放回主手，锄头干净换上），再施肥（换持骨粉催一株）。两者互不挤占：有踩坏的地就修，有未成熟的作物就催。
- 【新入口】MaidPlanting.forceRestoreHand——立即还原主手（不等 1 秒宽限），供锄地/其他需要主手的行为调用。
- 已部署 4,904,806
- 待实测：给农场女仆放骨粉 + 踩坏一块耕地 → 踩坏的地被锄回耕地（不再被施肥挤掉），未成熟作物照常催熟，两者同轮各干各的。

## 实测三百五十六

- 【根因】作物筛选用反了判定：isBonemealSuccess（m_214167_）对 CropBlock 【恒返回 true】（javap 实证 iconst_1），我误当"已成熟"跳过——**所有作物全被跳过**，一株都选不到（骨粉换到手上一株都不催）。成熟过滤实际在 isValidBonemealTarget（m_7370_）里：CropBlock 的该方法 = age < maxAge（未成熟 true / 成熟 false，javap 实证调用 isMaxAge）。修正为只用 m_7370_ 过滤成熟。
- 【非作物排除】原版实现骨粉接口但不是农作物的方块加排除表：树苗（归伐木）/ 草方块与草丛蕨 / 藤蔓 / 发光地衣 / 蘑菇与菌类 / 菌岩 / 海带海草 / 竹子 / 紫颂 / 垂滴叶 / 洞穴藤 / 缠怨垂泣藤 / 杜鹃 / 苔藓块。保留：小麦/胡萝卜/土豆/甜菜/火把花/猪笼草（CropBlock 系）、西瓜南瓜茎、可可豆、甜浆果丛。
- 【施肥轮跳过锄地】施肥成功后主手举着骨粉，若同轮继续锄地，锄头自动装备会把骨粉从主手抢走（换手/还原状态机被外部打断）——施了肥的轮次本轮跳过锄地。
- 【顺手防抖】农场施肥改为先找目标、找到才换手（旧版先换手再找目标，没目标时骨粉换上又换下来来回抖）；树苗催熟同步调整。
- 已部署 4,904,669
- 待实测：给农场女仆包里放骨粉 → 未成熟小麦/胡萝卜等被逐株催熟（粒子特效、主手举骨粉）；成熟作物和草方块不浪费骨粉；催熟期间不锄地。

## 实测三百五十五

- 【农场作物催熟】农场模式的女仆背包里有骨粉时，对身边（半径 16 格、垂直 ±4）最近的一株未成熟作物使用骨粉催熟——每 0.5 秒一株（随锄地驱动节拍），带粒子特效。只催【当前世界有骨粉配方】的作物（isValidBonemealTarget 判定，原版/模组作物自动兼容）；成熟作物不催（isBonemealSuccess 会成功 = 催了也白费）；树苗不在这里催（归伐木催熟管）。
- 【同款换手逻辑】找骨粉 主手 → 副手 → 背包；施肥时主手临时换持骨粉（equipBoneMeal 从 MaidPlanting 抽成 public 复用），停止 1 秒后由 tryRestoreHand 自动还原（剩余骨粉收回背包、原物品放回主手）。
- 【配置开关】模组详细配置 → 杂项页 →「农场作物骨粉催熟」，默认开（MISC_MAID_BONEMEAL_FARM）；手册「农场与杂项功能」章同步介绍。
- 已部署 4,904,228
- 待实测：给农场女仆包里放骨粉 → 未成熟小麦/胡萝卜等被逐株催熟（0.5 秒一株、粒子特效、主手举骨粉）；成熟作物不浪费骨粉；杂项页开关可关。

## 实测三百五十四

- 【检测主副手】找骨粉顺序改为 主手 → 副手 → 背包——旧版只扫背包（与实测二百三十"手拿树苗判没苗"同款坑：手部栏是独立槽位）。
- 【换手持骨粉】施肥时主手临时变为骨粉（从副手/背包整组拉到主手，真持有——growCrop 直接消耗主手栈），原主手物品暂存；施肥停止 1 秒后自动还原：手上剩余骨粉收回背包（满则脚下掉落防丢），原物品放回主手（被自动装备等行为抢先占了手位则收进背包不强抢）。她本来就手持骨粉时直接用，不换手也不还原。
- 【生命周期】驱动每 0.5 秒对所有女仆做还原检查（含刚切走伐木任务的）；女仆卸载清理（forget/purge）覆盖换手状态表。
- 已部署 4,902,914
- 待实测：骨粉放副手/背包 → 施肥时主手举起骨粉（带粒子），停止施肥 1 秒后主手变回斧头、剩余骨粉在背包；骨粉放主手 → 直接施用不换手。

## 实测三百五十三

- 【节拍 0.5 秒】催熟从挂在 5 秒种树冷却上改为独立节拍：种树驱动加密到 10 tick（0.5 秒）一轮，每轮尝试催熟一株；种树本身仍 2 秒一轮。原版 isBonemealSuccess 是 45% 概率判定（javap 实证 0.45D），配合 0.5 秒节拍一棵苗几秒内就能催成。
- 【补粒子特效】根因：BoneMealItem.growCrop 字节码里根本没有 levelEvent 1505——原版骨粉粒子在玩家 useOn 路径上放，dispenser 式调用天生无粒子。现在每次施肥手动 levelEvent(1505)——每次都有粒子反馈。
- 【修双扣骨粉】推翻上轮结论：growCrop 尾部自带 shrink(1)（javap 实证 offset 83，45% 判定成败都扣），旧版又手动 extractItem = 每次催熟实际扣 2 个骨粉。删掉手动扣减。
- 【文案同步】配置注释/配置面板/手册介绍全部改为"每 0.5 秒施肥一次（带粒子特效）"。
- 已部署 4,901,138
- 待实测：给伐木女仆包里放骨粉 → 0.5 秒一次施肥节奏、每次有绿色粒子、一棵苗几秒催成；骨粉消耗速度恢复正常（一次 1 个）。

## 实测三百五十二

- 【骨粉催熟】伐木模式的女仆背包里有骨粉时，对身边（半径 6 格、垂直 ±2）最近的一株合格树苗使用骨粉催熟——每轮（随"补种树苗冷却"节流，默认 5 秒）最多催熟一株，优先把已种下的树苗催大而不是种新的；即使背包没树苗也照常催熟。成功后播放骨粉粒子+挥臂动画，手动扣 1 个骨粉（growCrop 不 shrink 栈，javap 实证）。
- 【选择条件】SaplingBlock 实例 + 非深色橡树（单株永不生长，不浪费骨粉）+ 干列净空（树干上方被实心方块挡死的长不成，也不浪费）。【不查光照】——骨粉走 performBonemeal，不经过原版 randomTick 的光照门槛，这正是地下/室内种树能长成的唯一途径。
- 【配置开关】模组详细配置 → 杂项页 →「树苗骨粉催熟」，默认开（MISC_MAID_BONEMEAL_SAPLING）。
- 【手册介绍】伐木系统教程章新增「随手种树」三段介绍：补种树苗玩法、种点筛选三条件（3×3 碰撞/7 格干列净空/光照达标 + 深色橡树苗排除）、骨粉催熟与开关位置。
- 已部署 4,901,071
- 待实测：给伐木女仆包里放骨粉 → 已种的树苗被逐株催大（含光照不足的洞里/室内树苗）；深色橡树苗和树干顶死的树苗不被浪费骨粉；杂项页开关可关。

## 实测三百五十一

三项优化全部按原版树苗生长机制（javap 反编译实证）实现：

- 【干列净空检查】TreeFeature.isFree 实证：树干只能长在空气/可替换方块/树叶上（树叶可被树干穿过）——树干列被实心方块挡住 = 整棵树 100% 长不出来（树冠反而只是"尽力而为"，放不下就剪枝）。可种地块现在要求种点上方 1~7 格（橡树干 4~6 + 桦树最高 7）没有实心方块；树叶、火把、花草照常放行。天花板/桥面下的格子不再浪费树苗。
- 【光照检查】原版 SaplingBlock.randomTick 实证：自然生长条件 = getMaxLocalRawBrightness(树苗上方格) ≥ 9（含昼夜折减）。种点光照 <9（地下/封闭室内）永不自然生长——直接跳过不种。
- 【深色橡树苗排除】DarkOakTreeGrower 实证：单株特征 getConfiguredFeature 返回 null——深色橡树苗【单株永不生长】（原版必须 2×2 四株）。女仆只会单种，种了白种还白占冷却——isSaplingItem 直接排除（按注册名判定），捡取/种植都不认。
- 已部署 4,899,039
- 待实测：①伐木女仆不再把树苗种进 3 格高的洞里/屋顶下（宁可不种）；②地下洞穴/封闭房间内不补种；③背包里的深色橡树苗不再被种下；④林间（头顶是树叶）照常补种。

## 实测三百五十

- 【3×3 碰撞体积检查】可种地块判定（findPlantSpotCount）在"空气 + 脚下 #minecraft:dirt/草方块"之外，再要求以候选格为中心的 3×3（同层）没有【带碰撞体积】的方块——树苗有生长空间，不会被墙/箱子等闷住。判定用原版碰撞箱 getCollisionShape(...).isEmpty()：火把/花草/地毯等无碰撞方块 isEmpty=true 照常放行（与自保安全落点 standableCell 同款口径）。
- 【清理死代码】旧版 findPlantSpot（无调用方，tick 只用统计版 findPlantSpotCount）删除，避免两套种点判定逻辑漂移。
- 已部署 4,898,686
- 待实测：伐木女仆在火把旁/花草间照常补种树苗；贴墙、贴箱子的格子不再被选为种点。

## 实测三百四十九（玩家四连报："排班表内改名不显示新名字；烧制吞附魔物品；排班女仆跨区块加载没生效；排班女仆不应触发死亡传送"）

- 【排班表改名不显示】：服务端改名成功但【没有回包】——客户端 `maids` 列表是打开排班表那一刻收集的快照，永远显示旧名字。新增 S2C `MaidRenameSyncPacket`（id=14）：改名成功 → 推新名字 → `ScheduleBookScreen.syncMaidName` 同步列表行（m[1]）与详情页标题（selName），列表/标题/搜索过滤立即生效。
- 【烧制吞附魔物品】：`extractAnySmeltable` 的"装备类永不熔"只拦 TieredItem/ArmorItem 等类型——附魔的方块/模组物品照样被喂进炉子。新增统一守卫 `isSafeToFeed`：**带附魔（isEnchanted，哪怕 1 条）或耐久未满（isDamaged）的物品一律不进炉子**，接入全部四条路径——通用可烧制物回退（extractAnySmeltable）/ 矿物回退（extractOreFromMaid）/ 烟熏炉·高炉（extractByRecipe）/ 燃料侧（extractBestFuel，附魔物品绝不当柴火烧）。
- 【排班女仆跨区块加载没生效】：旧票是【会话级】addRegionTicket——关服 `releaseAll` 清光；重进游戏后调度只扫【已加载】女仆，远处未加载区块里的排班女仆永远扫不到、永远没票 → 她的区块永远不加载（每次重启都丢票，观感"没生效"）。排班女仆改走 **Forge 持久化强制区块**（`ForgeChunkManager.forceChunk`，modid=maid_smart，ticking=true）：票写进维度 chunks.dat，**跨重启自动恢复**（Forge 启动时 reinstatePersistentChunks 重挂）；换区块自动换票、关排班自动撤票。普通跟随女仆维持会话票不变。
- 【排班女仆死亡传送】：实测一百零二曾把"排班 Home 模式"从豁免中拿掉（当时口径：排班女仆也要赶重生点）。按玩家最新口径反转——排班中的女仆（含排班自动 home 守家女仆）**不参与死亡传送**：她们驻守排班锚点，全拽到重生点 = 守家空窗。想召回走排班表「传送到我身边」（先关排班，与一键集合同口径）。
- 已部署 4,898,582
- 待实测：①排班表内改名 → 列表与详情页标题立即变新名字；②背包放附魔镐/附魔书/用旧的工具 → 烧制女仆不烧不熔；③给远处女仆排班 → 重启游戏 → 她的区块保持加载（排班照常切换、熔炉照常烧）；④主人死亡 → 排班女仆原地驻守不传送，非排班女仆照常传送到重生点。

## 实测三百四十八

- 【根因】主人链 feedFoodDirect 的系统字幕在 `owner.m_5584_`（eat，会 shrink 栈内数量）【之后】才读 `food.m_41786_().getString()`——extractItem 返回的是活引用，eat 后栈变空 → 空栈的 getHoverName 返回原版 "Air"（中文环境显示"空气"）→ 系统字幕"女仆喂你吃了 空气"。姐妹链 feedSisterFood 早就修过同款坑（"名字在进食前读"），主人链漏了。
- 【修复】食物名改为【进食前】读快照（与姐妹链同款），eat 后只拼字幕字符串，不再读栈。
- 已部署 4,895,742
- 待实测：女仆喂主人食物时系统字幕应显示真实食物名（熟牛排/面包等），不再出现"空气"。

## 实测三百四十七

- 【真兜底让位】兜底不再只看"本 tick 冷却记忆是否在"——TLM 打完后 ATTACK_COOLING_DOWN 只持续十几 tick，攻击循环间隙里兜底插进来就是"原有的攻击没了、只剩空手连拍"。改为记录 TLM 最近动作时刻：冷却记忆在（TLM 正在打）或近 40 tick（2 秒）内打过 → 完全让位不打不导航；只有 TLM 长时间没动作（链路真断了）才接管补刀。
- 【空手不打】主手没有带攻击力属性的物品（空手/纯工具）→ 只导航跟随保持威胁登记，不亲自挥刀（实测空手反击又快又痛失衡）。参战切换本身会自动从背包装备武器（CombatTaskCompat.prepareSwitch），装上后自然恢复挥砍。
- 【按攻击速度属性算冷却】冷却从固定 12 tick 改为原版公式 20/attack_speed（剑 1.6 → 12.5 tick、斧 1.0 → 20 tick）——与真实攻速一致，附魔/急速加成自然生效。
- 【补挥臂动画】doHurtTarget 直调不出摆臂动画——swing 主手让玩家看到"挥了一下"，与 TLM 攻击观感一致。
- 已部署 4,895,738
- 待实测：①惹怒狼 → 女仆参战、TLM 正常攻击带动画（兜底让位不抢）；②空手女仆被咬 → 不再空手连拍，参战装武器后正常挥砍；③拿斧的女仆兜底攻击频率明显降下来（20 tick 一刀）。

## 实测三百四十六

- 【统一口径】全项目威胁判定点审计后统一并入【行为化判定】：任意 Mob 只要 getTarget 锁定主人/女仆就是敌对威胁——不依赖 Enemy/NeutralMob/Tamable 原版类型接口（模组魔改的"被动变中立"生物不实现任何一个是常态），getTarget 是 Mob 基类字段、所有生物 AI 攻击前必写它。女仆之间永远不算威胁。
- 【PerceptionManager.isThreat】（自保威胁扫描/敌袭判定/感知播报的统一入口）：getTarget 判定从"NeutralMob 专属通道"提升为普适通道——任意 Mob 锁定主人/女仆即威胁；补女仆排除（女仆锁定主人时——跟随/护主场景——绝不能被姐妹当威胁围殴）。
- 【AutoCombatSwitch 五处判定点】①主人开火判定（onOwnerAttack/onOwnerAttackDamage）：被打生物即使 isAngry 未就位（模组自有仇恨系统不写 isAngry），只要锁定主人/主人任意女仆也算交战对象；②女仆被打判定（maidVictimOfMonster）：锁定受害女仆或其主人的任意生物算攻击来源；③战斗接触登记（onMaidStrikeEnemy）：女仆打到锁定我方的生物也记接触（僵局逃逸阀计时——旧版只认 Enemy，打狼时收不到接触信号误判死局）；④动态威胁圈登记（touchContactFromSource）：锁定我方的攻击者也登记进威胁圈（还原扫描跟踪它，防还原后被咬回）；⑤威胁圈来源（ringSource）：同口径。
- 【SelfPreservationBehavior.anyVisibleMonsterAround】传送安全闸：center 是主人时旧版只认 Monster——发狂的狼站在主人身边时安全闸恒放行（传过去又被咬）。并入行为化：锁定主人/主人任意女仆的生物算可见威胁。
- 【MaidAidOwnerBehavior.ownerInDanger】增益助战判定：主人被狼/魔改生物追时女仆照常扔力量/迅捷药水助战（旧版只认 Monster，主人被狼追时增益链不触发）。
- 已部署 4,894,969
- 待实测：①惹怒任意生物（狼/北极熊/魔改被动生物）→ 女仆参战+实际攻击+战术走位；②威胁锁定主人身边女仆时主人的其他女仆也响应；③战斗接触/还原计时对这类生物正常工作（打完还原，不卡战斗态）；④主人被追时女仆扔增益药水助战。

## 实测三百四十五

- 【现象】实测三百四十四生效一半：参战触发成功（女仆确实切到攻击模式），但攻击行为没执行——写目标只解决"脑内有目标"，TLM MaidMeleeAttack 是 WORK 组行为，canUse 还有一串条件（切任务后 brain 重建时序 / 活动段 / NEAREST_VISIBLE_LIVING_ENTITIES 可见列表 / 攻击距离），任一环断了女仆就"进了攻击模式站着不动"。
- 【修复·攻击执行兜底】NeutralThreatDriver 的 ③ 分支不再限定"非战斗任务才直接攻击"——无论什么任务，只要威胁还在就自己打：走近（直连导航，战术行为激活时让位给它管走位）+ 近身挥砍（doHurtTarget，反编译实证 canAttack 只是距离判定不滤类型）。
- 【与 TLM 攻击节奏同步】通过 ATTACK_COOLING_DOWN 记忆判定：TLM 正常攻击成功会写入冷却记忆 → 驱动读到就记 12 tick 冷却并跳过（不抢不叠，TLM 链路健康时完全由 TLM 打）；TLM 链路断了冷却不走 → 冷却归零后由驱动补刀。两边不会同 tick 双击。我方补刀成功也记一次战斗接触（还原扫描的僵局逃逸阀计时用）。
- 已部署 4,894,412
- 待实测：惹怒狼 → 女仆参战后应实际挥刀砍狼（TLM 链路断了也有兜底补刀），砍死还原。

## 实测三百四十四

- 【根因·反编译实证】女仆对发狂的狼零反应是 TLM 索敌链的【结构性盲区】：① MaidHostilesSensor 的 ACCEPTABLE_DISTANCE_FROM_HOSTILES 白名单【只有苦力怕一个条目】——isHostile 只查白名单，发狂的狼永远不进 NEAREST_HOSTILE；② TaskAttack.createBrainTasks 用原版 StartAttacking，目标选择只认 Monster 或 NeutralMob.isAngryAt(女仆)——狼记仇的是【主人】不是女仆 → isAngryAt(女仆) 恒 false → 切了战斗任务也【选不到目标】→ 女仆站着不动；③ 实测三百一十八只补了【驯服】狼（isAngryTamedAt 要求 isTame），野狼（未驯服）全部漏判；主人打野狼时 LivingHurtEvent 在狼记仇状态设置【之前】触发，isAngry 判定也恒 false。
- 【判定加强·行为化】（玩家补充：模组会魔改被动生物成狼那样的中立生物）威胁判定全面改为【看行为不看类型】：Mob.getTarget() 是 Mob 基类字段，所有敌对/中立/魔改生物攻击前必写它——狼发狂咬人的瞬间 target==主人，模组改的"攻击性生物"冲锋时同样写 target。不再依赖 Enemy/NeutralMob/TamableAnimal 原版类型接口（魔改生物不实现任何一个是常态）。
- 【新驱动 NeutralThreatDriver】每 0.5 秒扫描：16 格内 getTarget 锁定主人/女仆的任意 Mob → ① 补触发 AutoCombatSwitch 参战（主人打狼时事件早于记仇就位，这里补上迟到的触发）；② 索敌写目标——把威胁直接写进女仆 brain 的 ATTACK_TARGET/LOOK_TARGET（MaidMeleeAttack 的 canAttack 反编译实证只是距离判定、不滤类型 → 写了目标攻击行为就能执行，战斗任务/战术走位/跳劈全链路自动接管）；③ 无战斗任务也能打——非攻击任务的女仆没有攻击行为（WORK 组由任务 createBrainTasks 注册），写了目标没人挥刀 → 直接调 doHurtTarget（含横扫/饰品事件，自保近身反击同款通道）+ 直连导航走向目标，农场/挖矿/跟随的女仆不再干挨咬。排班/自保/坐下/幼年女仆让位。
- 【AutoCombatSwitch 行为化兜底】maidVictimOfMonster（女仆被打判定）/hasThreatNearby（还原威胁判定）/nearestThreatDist（换战术距离）/threatDetail（诊断）全部并入 isTargetingOurSide（getTarget 锁定主人/女仆即威胁）——野狼咬女仆时 getTarget 可能仍指向主人（记仇主人但顺手咬近身的女仆），两侧都查；还原扫描不再漏掉锁定中的魔改生物（否则还原后立刻又被咬回战斗态，反复横跳）。
- 已部署 4,894,311
- 待实测：① 惹怒野狼 → 女仆应立即参战拔刀围殴（不再站着）；② 狼咬女仆 → 被咬女仆还击，姐妹响应；③ 装魔改生物的 mod（被动生物变中立）→ 攻击女仆/主人时同样触发参战；④ 打完狼威胁解除 → 女仆照常还原原任务。

## 实测三百四十三

- 【列表页·全员在家】批量行下方新增「全员在家：开/关（x/y）」按钮——点击循环开/关，与全员模式同款交互，统一调控全部女仆的在家模式；排班中的女仆跳过（home 由排班管理，服务端同样兜底拦截）。翻页按钮右移到 cx+40/cx+90 给按钮让出空间。
- 【网络层】新增 BatchHomePacket（C2S，跨维度扫描 + 主人校验 + 排班跳过 + 结果播报）。
- 已部署 4,890,483
- 待实测：排班表列表页「全员在家」按钮——一键开/关全部女仆在家模式；详情页第 1 页「在家模式」开关——单一女仆调控。

## 实测三百四十二

- 【新功能】排班表内单独调整女仆在家模式：①女仆列表行显示「在家」状态（紫色标记）；②详情页第 1 页新增「在家模式：开/关」按钮——不依赖排班开关，不开排班也能让女仆守家（home 模式）；排班开着时 home 由排班管理（开排班自动 home、关排班解除），按钮锁定提示先关排班（服务端同样兜底拦截）。
- 【网络层】女仆列表包 8 字段 → 9 字段（追加在家模式 m[8]）；新增 HomeTogglePacket（C2S，跨维度查找女仆 + 主人/OP 校验）。
- 已部署 4,887,742
- 待实测：排班表 → 点女仆 → 第 1 页「在家模式」开关——不开排班也能让女仆守家；列表行显示「在家」标记。

## 实测三百四十一

- 【详细介绍更新】手册「详细介绍」补上 v1.1.0 新功能：①功能速览加宰杀任务/耕地标记制/精妙超越绑定卡；②农场章节补耕地标记制（只锄「曾经是耕地」的地块、自然生成耕地自动补标、草径也认）与锄地索敌（16 格主动走向可锄地块）；③排班章节补任务 ◀▶ 左右箭头（第 1 页任务行 + 第 2 页 6 槽）；④酿造章节补成品药水当基底下料（3 分钟夜视自动推进 8 分钟）；⑤杂项章节补宰杀任务机制（分组计数/阈值/每 3 秒宰一只/无目标闲逛/不误杀宠物与亡灵马）与精妙/超越绑定卡用法。
- 已部署 4,885,003
- 待实测：手册 → 详细介绍 → 翻到对应章节——新功能描述应已更新。

## 实测三百四十

- 【无目标闲逛】宰杀行为 SEEK 分支无超阈值组时不再站桩（旧版 setStill(true) + 清 WALK_TARGET + 停导航）——改为四处闲逛：每 2 秒在周围 8 格内随机选一个点设 WALK_TARGET（BlockPosTracker 固定点，MoveToTargetSink 消费驱动寻路），走位期间不站桩；扫到超阈值组立即打断闲逛去追（下轮 tick 的 SEEK 分支优先）。
- 已部署 4,883,659
- 待实测：宰杀任务 + 无超阈值牲畜（或空畜栏）——女仆应持续走动闲逛，不再原地站桩；畜栏牛群超阈值时照常锁定追踪宰杀。

## 实测三百三十九

- 【快捷设置页·任务行】任务按钮拆成三键——◀ 上一个 / 中间任务名（点击仍循环，保留旧习惯）/ ▶ 下一个，与工作模式行同款布局（实测三百一十二的三键模式）。
- 【排班页·6 任务槽】每个槽同样拆成三键——◀ 上一个 / 中间任务名（点击仍循环）/ ▶ 下一个，含"空闲"档回绕（循环不依赖 uid 匹配，与点击循环同口径）。
- 已部署 4,883,409
- 待实测：排班表第 1 页任务行、第 2 页 6 个任务槽——左右箭头应能直接切上一个/下一个任务。

## 实测三百三十五

- 【判定检查】逐项核验可锄地块判定链：①方块 SRG 映射——javap 实证 f_50493_=泥土（普通 Block 构造）、f_50440_=草方块（GrassBlock 构造）、f_50093_=耕地（FarmBlock 构造）、**f_50092_=CropBlock（马铃薯）不是注释声称的 dirt_path**——DirtPathBlock 构造 → putstatic f_152481_（dirt_path 正确字段）；②canDestroyBlock 无距离限制（TLM 字节码实证：canEntityDestroy + Forge 事件，不查距离）——16 格索敌不受影响。
- 【修复】onToolModification 草径判定字段 f_50092_ → f_152481_——旧版草径被锄成耕地时不打标（判定失败），草径踩坏后女仆不认（"可锄地块判定有问题"的直接原因）。
- 已部署 4,883,072
- 待实测：草径（dirt_path）被锄成耕地后踩坏 → 女仆应认标记并锄回耕地。

## 实测三百三十四

- 【链路审查】锄地路径：FarmTillDriver（每 0.5 秒全图扫女仆）→ tillNearby（扫女仆脚下 5×5）→ isTillable（泥土/草方块 + 上方空气 + 有"曾经是耕地"标记 + 有锄头）→ 锄成耕地 + 打标。积极性低的根因：①扫描范围只有脚下 5×5——home 模式下女仆在锚点附近，农田稍远就永远锄不到，只能等随机巡逻撞上；②无索敌——扫到可锄地块也不走过去，原地等。
- 【锄地索敌重写】①扫描半径 5×5 → 16 格（与酿造/熔炉/宰杀同口径，misc.brewRadius 同值）；②冷却 1 秒 → 0.5 秒（与扫描节流一致）；③近身（≤3 格）直接锄，远的目标【直连导航走过去】（m_26519_，不走 MoveToTargetSink——站桩标记/移动抑制拦不住，自保逃跑验证过的通道）；④标记自愈保留（范围内当前是耕地的地块实时打标）。
- 已部署 4,883,065
- 待实测：农场任务 + 农田（有标记的踩坏地块）——女仆应主动走向 16 格内的可锄地块逐块锄掉，不再等巡逻撞上。

## 实测三百三十三

- 【宰杀确认】v332 检索判定重写生效——日志实证"扫描到牲畜组: cow=5（阈值 5）"（此前 alive=0 恒空）。但 5>5 不超阈值 → 无目标 → 行为 setStill + 清 WALK_TARGET + 停导航 → 原地站桩（"不会动"的直接原因）。
- 【全盘推翻·Home 工作移动独立驱动】新建 HomeWorkMovementDriver：为【home 模式 + 农场/宰杀任务】的女仆提供独立运动，不依赖 TLM 大脑活动/行为站桩标记。每 0.25 秒全图扫描（Entity.class 全量 + instanceof，有限 AABB——桶 bug 与 ±∞ 溢出均已绕开）：有 WALK_TARGET（工作目标）→ 交给 MoveToTargetSink 正常寻路不干扰；无目标 → home 锚点附近随机点【直连导航】（PathNavigation.moveTo，m_26519_——不走 MoveToTargetSink，站桩标记/移动抑制拦不住，自保逃跑验证过的通道）→ 女仆在 home 范围内持续巡逻走动，农场/宰杀任务照常扫描执行。
- 已部署 4,882,643（460 条目，新增 HomeWorkMovementDriver.class）
- 待实测：home 模式 + 农场/宰杀——女仆应持续走动（无目标时巡逻，有目标时走向目标干活）。

## 实测三百三十二

- 【宰杀·根因实锤】统计诊断 total=45 animal=17 alive=0 kept=0——17 只 Animal 全被过滤误伤。javap 实证 LivingEntity.m_6336_()（getMobType）默认实现【恒返回 MobType.f_21640_（UNDEAD）】——原版牲畜（牛/羊/猪/鸡/兔/山羊等）都不覆写该方法 → 全部命中"排除亡灵马"条件 → alive=0 → 永远无目标 → 站桩不动。此 bug 自 v314 加"排除亡灵马"起埋下，历次修复（v325 索敌重写/v329 Entity.class/v331 统计）都没触及这条过滤。
- 【宰杀·检索判定重写】按要求重写：① Entity.class 全量扫描（绕开 ClassInstanceMultiMap 桶 bug）② instanceof Animal ③ 存活（isAlive && !isRemoved）④ 排除驯服宠物（TamableAnimal）⑤ 排除亡灵马——限定 AbstractHorse 且 getMobType==UNDEAD（普通马 getMobType 返回 UNDEFINED 不排除，马/驴/骡算牲畜）⑥ 按 EntityType 分组计数，超阈值组随机选一只。
- 【home 巡逻·AABB 溢出修复】v330 改 HomePatrolHandler 时保留了 ±∞ AABB——±∞ 经 SectionPos.blockToSection 换算溢出收敛到同一值（floor 溢出回绕 + >>4），section 循环只执行一次且落在世界外 → 扫描恒空 → home 巡逻失效（"home 模式又不会动了"）。改为有限值（±131072/±4096）。同 bug 的其余 3 处（DangerEscapeHandler/MaidDangerMalusHandler/ProMaidExtension 两处）一并修复。
- 已部署 4,880,081
- 待实测：①宰杀——畜栏牛群应被扫到并逐只宰杀（日志出现"锁定目标"→"宰杀"循环）；②home 模式——女仆应恢复巡逻走动。

## 实测三百三十一

- 【酿造确认】定向 3→8 分钟夜视已生效（快照实证：槽位 night_vision → long_night_vision 循环推进）——v329 条件写反修复坐实。
- 【宰杀·矛盾诊断】部署 jar 字节码核验 scanAndPick 已用 Entity.class + instanceof Animal（v329 生效），但日志仍"范围内无牲畜"而"扫描框内实体"（同 box 同 tick 的 Entity.class 扫描）扫到 cow@-4,-60,4 距9格 等——两个 Entity.class 扫描结果矛盾。加实体统计诊断（total=扫到实体数 / animal=instanceof Animal 数 / alive=存活数 / kept=入组数）——下次实测直接看出过滤在哪一步丢的（如 total 大但 animal=0 = instanceof 失效；animal 大但 alive=0 = 存活判定问题）。
- 【耕地频率】FarmTillDriver 扫描节流 20 tick（1 秒）→ 10 tick（0.5 秒）——玩家指定。
- 已部署 4,880,116
- 待实测：①宰杀——看"统计 total=… animal=… alive=… kept=…"行定位过滤断点；②耕地——0.5 秒一轮锄地。

## 实测三百三十

- 【根因】FarmTillDriver 用 m_45976_(EntityMaid.class, 全图AABB) 遍历女仆——与宰杀 Animal.class 完全同构的 ClassInstanceMultiMap 桶 bug：m_13533_(Class) 按请求 Class 精确建桶，未预建的 key 返回空桶 → EntitySection.m_188348_ 直接跳过整个 section。跟随模式女仆所在的 section 被 TLM 感知系统预建了 EntityMaid 桶 → 能扫到 → 会锄地；home 女仆单独站的 section 没预建 → 空桶 → 永远找不到 → 不锄地。HomePatrolHandler（home 巡逻驱动）同 bug。
- 【系统性修复】全仓审计 m_45976_ 调用，9 处全图 EntityMaid.class 扫描全部改为 Entity.class 全量 + instanceof 过滤：FarmTillDriver（锄地）、HomePatrolHandler（home 巡逻）、ScheduleManager（排班切段）、AutoCombatSwitch（战斗还原扫描——注释自证"还原扫描从未扫到过任何战斗女仆"）、DangerEscapeHandler（险境脱离）、MaidDangerMalusHandler（危险寻路惩罚）、PetImmunityGuard（宠物仇恨清除）、BlueprintLib（蓝图材料统计）、ProMaidExtension（启动 BRIDGING 清理 + 钓鱼坐垫走位）。局部小范围扫描（玩家/女仆周围几格）不受影响（section 内桶大概率已预建，且影响面小）。
- 已部署 4,880,032
- 待实测：home 模式 + 农场任务——女仆应锄地（5×5 标记地块）+ 收割/播种持续运转；顺带验证排班切段/战斗还原/危险避让等此前"时灵时不灵"的系统。

## 实测三百二十九（实测——宰杀"扫描框内实体"铁证 + 酿造背包药水清单：两个根因同时坐实）

- 【宰杀·扫描 bug 实锤】同一 box 同一 tick：m_45976_(Entity.class, box) 扫到 cow@15,-60,0 水平距2格、cow@16,-60,0 距2格、cow@18,-60,1 距3格 等大量牛，而 m_45976_(Animal.class, box) 返回空 → "明明贴得很近却报无牲畜"。反编译 ClassInstanceMultiMap 实证：m_13533_(Class) 按请求 Class 精确建桶（computeIfAbsent），桶内实体靠 add 时对【已存在的 key】做 isInstance 填充——Animal.class 这个 key 在 section 里从未预建 → 空桶 → EntitySection.m_188348_ 直接 return CONTINUE 跳过整个 section → Animal 扫描恒空。Entity.class 是全量桶（日志实证工作正常）。修复：宰杀扫描改用 Entity.class 全量 + instanceof Animal 过滤（语义等价，绕开桶机制）。
- 【酿造·条件写反实锤】背包药水清单：night_visionx4（3 分钟夜视 4 瓶）+ strengthx40 都在女仆身上，但槽 0-3 恒空、无缺料报告。processTargeted 空槽分支条件写反：`if (!bottle.m_41619_())` 在【无水瓶】（bottle 空栈，m_41619_=isEmpty=true）时为 false → 走 else 把空栈放进槽 0（等于没放）→ extractBrewableBase 永远不执行 → 3 分钟夜视永远不进酿造台。修复：改为 bottle 空时走回退取基底（与 processBatch 同款正确条件）。
- 已部署 4,879,801
- 待实测：①宰杀——畜栏牛群应被扫描到并逐只宰杀（日志出现"锁定目标"→"宰杀"循环）；②酿造——3 分钟夜视应自动进酿造台 → 下红石 → 酿成 8 分钟收走。

## 实测三百二十八

- 【根因】javap 实证 IFarmTask.createBrainTasks 默认注册 [MaidFarmMoveTask(优先级5), MaidFarmPlantTask(优先级6)]。v319 只把 move 加进 NativeTaskSmoothMixin 的 INFRA 豁免名单，**漏了 plant**：MaidFarmPlantTask 构造是单参 Map（maxDuration=60），start 处理 TARGET_POS 目标格后清目标、无 tick 覆盖——它和 move 一样靠 60 tick 周期 doStop 重启来响应 move 重搜出的新目标。被改无限时长后：处理完第一个目标 → 行为永远空转 → 新 TARGET_POS 出现也不重启 → 农场只处理一个目标就停摆。"一按 home 就停"的时序：切 home 瞬间活动切换打断行为，重启后 move 找目标、plant 处理一个目标，之后永不再动。
- 【修复】INFRA 名单补入 MaidFarmPlantTask（isAssignableFrom 父类兼容自动覆盖子类）——plant 恢复 60 tick 周期重启，move 每 3 秒重搜新目标 → plant 重启处理 → 农场持续运转。
- 已部署 4,879,787
- 待实测：home 模式 + 农场任务（普通作物 + 甘蔗/西瓜）——女仆应持续走动收割/播种，不再"一按 home 就停"。

## 实测三百二十七

- 【全实体坐标诊断】宰杀扫描报"范围内无牲畜"时，额外把扫描框内【全部实体】的类型+坐标+与女仆的水平距离打出来（节流 5 秒，框内无实体也如实记录）——直接区分两种误判：①牛在框外（位置误判：女仆站太远/牛在 16 格外）②牛在框内但扫描漏了（扫描 bug：m_45976_ 过滤链/排除逻辑误伤）。日志格式：`扫描框内实体: cow@x,y,z 水平距N格, player@...`。
- 已部署 4,879,770
- 待实测：畜栏放牛 → 切宰杀 → 看"扫描框内实体"行——牛在框内（有 cow@ 且水平距<16）却仍报无牲畜 = 扫描 bug 坐实，直接修过滤链；无 cow@ = 牛确实不在框内，把女仆/畜栏拉近即可。

## 实测三百二十六（实测运行 1 分钟——酿造/宰杀诊断快照首轮解读）

- 【酿造快照解读】反推表正常（链=3步、表大小=40——反射构建成功，二百七十九的 Holder 解包在实机工作正常）；燃料已自动补（烈焰粉×1）；但槽 0-3 全空且无"真缺料"日志——extractBrewableBase 从背包取 3 分钟夜视这步持续返回空。最大嫌疑：药水不在【执行酿造的这只女仆】背包里（日志同名"大正女仆酒狐"同时刷宰杀+酿造，可能是两只女仆错位）。本轮补诊断：快照附带背包药水清单（双手+背包全部药水 id/药水 id×数量）——下次实测直接看出药水到底在不在她身上。
- 【宰杀快照解读】每秒扫描节奏正常（新版生效），如实报"范围内无牲畜（半径 16）"——女仆 16 格内确实没牲畜；重写后的索敌追踪逻辑只有扫到目标才走动，扫描圈空则原地等待（行为正确）。本轮补诊断：扫描日志附带女仆坐标——对照畜栏坐标即可判断是距离问题（女仆站太远）还是真没牲畜。
- 已部署 4,879,392

## 实测三百二十五

- 【扫描核验】m_45976_(Class, AABB) 反编译核验签名合法（EntityGetter default 方法，走 m_6443_ 过滤链）；日志反复"范围内无牲畜（半径 16）"更可能是女仆站的位置周围真无牲畜（旧版从不移动）。排除扫描调用本身的问题。
- 【根因·纯站桩够不着】旧版宰杀行为每 tick setStill(true) + 清 WALK_TARGET + 停导航——设计成原地检测原地杀，牲畜在 3 格外就永远够不着；日志反复"范围内无牲畜"却从不挪一步，畜栏里的牛永远等不到宰杀。
- 【重写·攻击索敌式状态机】SEEK（无目标：每 1 秒扫描，超阈值组随机选一只锁定 + 落盘"锁定目标"日志）/ CHASE（有目标：每 tick 设 WALK_TARGET = EntityTracker(目标)——追踪目标移动，速度 1.0 与攻击同款；目标死/被移除/追出扫描圈 1.2 倍容差 → 回 SEEK）/ STRIKE（距目标 ≤ 3 格：kill + 挥臂 + 3 秒冷却 → 杀下一只）。走位期间不站桩（MoveToTargetSink 必须消费 WALK_TARGET 驱动寻路），只在近身宰杀瞬间与无目标等待时站桩。行为停止丢弃目标（防残留锁定）。
- 【扫描间隔】无目标时 1 秒一轮（旧版 3 秒——索敌响应更快）；宰杀节奏仍 3 秒一只。
- 已部署 4,879,095
- 待实测：畜栏放 >5 只牛 → 切宰杀任务 → 女仆应走向牛群逐只宰杀（promaid.log 应出现"锁定目标"→"宰杀"循环）

## 实测三百二十四

- 【日志审查结论】promaid.log/latest.log/debug.log 全查：酿造链【零运行时日志】——MaidBrewBehavior 全程不打日志，notifyMissing 只发气泡/聊天不落盘；测试会话（16:07-16:09）里唯一女仆任务是宰杀（maid_smart:slaughter），无任何酿造痕迹。无法从日志定位卡点。
- 【字节码核验】forge-1.20.1-47.4.21-client.jar 里 PotionBrewing$Mix 的 from/to（f_43532_/f_43534_）确为 Holder$Reference——与 BrewRecipeResolver.unwrapHolder 解包逻辑吻合，反射链理论上可用；f_43494_ 药水配方表字段名一致。定向链代码推演（3分钟夜视=long_night_vision 链上 progress=2 → 槽3 下红石 → 20 秒 → isTargetFinal 收走）未发现死路。
- 【配置核验】promaid-common.toml：brewAuto=true、brewRadius=16、processCooldown=40——开关全开。最近一次手册配置（9/4 21:44）为 定向/强化=萤石(2)/形态=饮用/目标=long_night_vision（注意强化=萤石但夜视只有红石延长版——v318 已做双材料回退，理论上应下红石）。
- 【修复·酿造诊断日志】旧版整条链零日志，"一直做动作但练不出药水"无从验查。新增：①processStand 周期快照（5 秒节流/女仆）——配置（模式/强化/形态/目标）+ 反推表状态（链步数/表大小/反射错误）+ 酿造台槽 0-3 内容（物品id/药水id×数量）+ 燃料槽——槽位快照能直接对出卡点（燃料空=酿造台不烧；表空=定向整体退化为不下料；链不可达=不下料；槽3 有料不消耗=材料不在配方上）；②notifyMissing 真缺料落盘（旧版只有气泡/聊天）；③BrewRecipeResolver.tableSize() 诊断接口。全走 PromaidLog（promaid.log），MISC_LOG_ENABLED 总开关控制。
- 已部署 4,878,542
- 待实测：定向 3→8 分钟夜视场景跑 1 分钟 → 查 promaid.log [酿造] 快照——若表空/反射错误 = 反射构建失败坐实；若燃料空 = 没给烈焰粉；若槽3 有红石但 20 秒不消耗 = 下料逻辑/槽位问题；快照会直接给出答案。

## 实测三百二十三

- 【部署核验】此前修复链路已全部落地并核实：①v319 —— NativeTaskSmoothMixin 的 INFRA 排除名单加入 MaidFarmMoveTask（javap 实证 searchForDestination 只在 start 调用一次，改无限时长后永不重新搜索目标 → 农场停摆）；②v322 —— INFRA 判定从 Set.contains 精确匹配改为 isAssignableFrom 父类兼容（甘蔗/西瓜用 MaidFarmSurroundingMoveTask extends MaidFarmMoveTask，旧精确匹配漏掉子类 → 照样改无限时长 → 同样停摆）。本次反编译核验：MaidFarmMoveTask.start 字节码确认搜索仅在 start（周期重启驱动），MaidCheckRateTask 无限时长行为 start 只跑一次（checkExtraStartConditions 走过就不再挡，但永不 doStop）；isHomeModeEnable 时 checkOwnerPos 直通（TLM 原版即支持 home 农场，home 模式本身不拦农场寻路）
- 【部署一致性核验】deployed jar（D:\.minecraft\...\mods\promaid-1.1.0.jar，4,877,413）与 patched 最新构建逐条目比对：459 条目零差异；部署 jar 内 NativeTaskSmoothMixin.class 字节码同时包含 v319 排除 + v322 isAssignableFrom 逻辑（verify_deployed_nsm 实证）；HomePatrolHandler.class 同字节；latest.log 确认 Mixing NativeTaskSmoothMixin into MaidBrain 正常应用，无 Mixin apply failed / Error applying；promaid-common.toml 实证 nativeTaskSmooth=true、workUninterrupted=true
- 【链路确认】home 模式下移动驱动全景：干活中（farm 等）由任务行为驱动（v322 后周期重启恢复 → 持续搜索目标/走位）；非干活的 home 女仆由 HomePatrolHandler 每 4 秒巡逻（实测一百一十三）；干活不被打断由 SchedulePosTickMixin 兜底（isNonCombatWork 跳过拉回，farm 命中）
- 待游戏内实测：home 模式 + 农场任务（含甘蔗/西瓜特殊农场）——女仆应走动收割/播种，不再原地站死

## 实测三百一十九（Issue #2：maidmarriage 2.2.0/2.3.0 打开心契同眠调试面板即崩）

- 【Mixin 崩溃修复】MaidDebugPanelMixin 注入 maidmarriage 的 MaidDebugPanelScreen.init 加 require=0——maidmarriage 更新后 init 方法被移除/重命名，@Inject 找不到目标 → Mixin apply failed → 打开界面即崩（100% 复现，crash-report 实证）。require=0 后找不到目标静默跳过（AI 记忆按钮不显示，功能降级但不崩），与 MixinInteractionSittingAllow 的 require=0 同款策略
- 已部署 4,877,413

## 实测三百一十八

- 【强化材料双测修复】批量模式配置萤石强化（强化=2）但背包只有红石粉时，旧版只测配置材料：isBatchFinal 测"夜视+萤石"无配方 → 误判"无强化变体"放行 → 3 分钟夜视被当成品收走（挥臂动作）；extractBrewableBase 判定"已最终状态"不取 → 永远放不进酿造台；processForm 只找萤石 → 报缺萤石卡死。修复：强化判定双材料——配置材料无配方时自动测另一种（配置萤石但夜视只有红石延长版 → 下红石；反之亦然），两种都无配方（如 healing 无任何强化变体）才放行
- 【全形态强化修复】喷溅/滞留瓶也能强化（喷溅夜视+红石=喷溅长夜视，原版配方表支持）——旧版只强化饮用形态，喷溅/滞留瓶永远普通时长
- 【定向模式形态推进修复】extractBrewableBase 定向分支：药水效果已到目标但形态未到（如目标滞留、背包是饮用 8 分钟夜视）→ 可取来由 processForm 下火药/龙息推进形态。旧版直接跳过 → 饮用成品永远放不进酿造台
- 已部署 4,877,199

## 实测三百一十五

- 【同维度拉回补坐垫/骑乘豁免】trySameDimPull（同维度远距拉回）旧版只有 home/干活/搭路豁免，漏了坐垫/骑乘——反馈"坐垫+跟随模式至少会在大世界传到我身边"正是这条路径：坐垫女仆在基岩层（同维度）距主人远 → 被拉回主人身边。修复：坐垫/骑乘 = 玩家明确停放，不拉（与救援/一键集合同口径）
- 【TLM 原版传送补蹲下/坐垫/骑乘豁免】MaidTeleportPreserveMixin 只拦自保/建造/搭路——TLM 原版 teleportToOwner（离主人过远自动传送）仍会把坐垫/骑乘/蹲下的女仆拉走（反馈"蹲下、坐垫全都固定会这样"的元凶：我们 mod 的救援/拉回已豁免，TLM 原版没拦）。修复：坐垫/骑乘/蹲下（m_6040_ = isShiftKeyDown，javap 实证）一律拦截 TLM 原版传送
- 已部署 4,872,515

## 实测三百一十四

- 【宰杀误杀修复】宰杀任务排除【驯服宠物】——狼/猫/鹦鹉（TamableAnimal）都是 Animal 子类，会被当牲畜误杀；只杀野生牲畜。另排除【亡灵马】（骷髅马/僵尸马，AbstractHorse 也是 Animal 子类但 MobType=UNDEAD 不是牲畜）
- 【跨维度绑定落错表修复】精妙/超越绑定卡右击终端完成绑定时，绑定写入【女仆所在维度】的表——旧版写玩家当前维度，女仆在其他维度时绑定落错表，取物（按女仆维度查表）永远找不到 → 跨维度绑定失效
- 【缺料报告永不触发修复】缺料双通道确认的 10 秒超时漏洞——pending 超时后旧版重新记 pending 不报告，而酿造台烧制一轮 20 秒（>10 秒），女仆两次缺料检查间隔必然超时 → 永远走"记 pending"分支 → 真缺料永远不报。修复：pending 超时 = 视为真缺直接报告
- 已部署 4,872,346

## 实测三百一十三

- 【Promaid 手册 GUI 美化】背景从原版纯黑改为**蓝金品牌渐变**——多层半透明色带叠加（深海军蓝→宝蓝→亮蓝渐层）+ 靛蓝顶部饰条 + 金色细线（m_280509_ ARGB 叠加实现渐变），全页面（目录/建造/女仆管理/记忆）统一生效
- 【女仆酿造手册 GUI 美化】同样改为**紫金品牌渐变**——深紫蓝→蓝→紫渐层 + 紫金顶部饰条 + 金线，与药水主题呼应；标题加斜体层次
- 已部署 4,872,173

## 实测三百一十二

- 【模式前后切换键】排班表→女仆详情→第 1 页快捷设置：工作模式由"点一下循环切换"改为**三键布局——◀ 上一个 / 中间模式名（点击仍循环，保留旧习惯）/ ▶ 下一个**（早班↔晚班↔全天任意方向直达，不用再点两三次绕圈）；排班中的女仆仍锁定（三键都不发包）；立即生效同旧版
- 已部署 4,871,908

## 实测三百一十一

- 【宰杀任务】新工作模式「宰杀」（maid_smart:slaughter，TLM 任务列表可切）——女仆检测周围 5×5（水平）内牲畜（Animal 子类：牛/猪/羊/鸡/兔/山羊等）按**实体类型分组**计数，某组数量超过面板阈值（杂项设置→「宰杀数量阈值」，misc.slaughterCount，默认 5，范围 2~64）→ **每 3 秒随机 kill 一只**该组牲畜（m_6074_ = Entity.kill，正常死亡流程掉落物照常；挥臂动画）；≤ 阈值不动；无超阈值组时站桩等待不漫游；看门狗/排班兼容已接入
- 已部署 4,871,737

## 实测三百一十（玩家担忧："女仆拥有共用的背包。女仆在使用建造和酿药的时候要从中取物品，如果同时选中了同一个物品，会不会出现错误呢？对于检测建造材料的判定会不会出现误判和错误呢？"——选定方案 B）

- 【共享存储防抢（方案 B）】分析：服务端单线程逐 tick——`extractItem(i,1,false)` 原子，A 取走后 B 提取瞬间为空 → 返回空 → 该步跳过下轮重试，不会丢物品/放错/崩溃；真实风险只有两处【假性判定】：①建造缺料统计（countMaterial 含共享终端数）可能算"够"但被别的女仆抢走 → 慢一拍自动恢复；②酿药缺料报告在 A 抢走瞬间误报"缺材料"（终端明明有）。修复：改**双通道确认**——缺料第一次取不到只记 pending（不报），下一轮处理周期重试；连续两轮（10 秒内）都取不到（背包/主副手/精妙终端/网络接口全空）才真报缺料气泡+系统消息；补料成功清 pending。建造侧缺料判定（含终端统计）与消耗同源，取不到静默跳过不误报，无需改
- 已部署 4,867,195

## 实测三百零九

- 【维度终端绑定卡】新物品「维度终端绑定卡」（maid_smart:beyond_bind_card，**9 末影珍珠合成**，创造栏工具页）——手持右击自己的女仆选定目标，再右击【超越维度网络接口】（beyonddimensions:net_interface，注册名判定零硬依赖）完成绑定；卡不消耗可反复用
- 【维度终端解绑卡】新物品「维度终端解绑卡」（**9 黏液球合成**）——右击女仆清空她的网络接口绑定
- 【女仆从网络接口取物】NetInterfaceBlockEntity 实现 Forge capability 代理（getCapability(ITEM_HANDLER, null) 透传网络对面容器的物品能力，javap 实证）——被绑定接口视作女仆的另一个背包：建造（consumeBlock/countMaterial）与酿药（extractItemFromMaid）在背包/精妙终端之后继续回退到网络接口（距离 ≤32 格；capability 空/没装超越维度自然回退）
- 【绑定独立存储】与精妙储存分开（BeyondBindingStore，maid_smart_beyond_binding）——两套绑定可同时生效（先精妙后超越），各自的解绑卡只解各自的表
- 已部署 4,867,095

## 实测三百零八

- 【精妙终端绑定卡】新物品「精妙终端绑定卡」（maid_smart:storage_bind_card，9 皮革合成，创造栏工具页）——手持右击自己的女仆选定目标，再右击【精妙储存控制器】完成绑定（sophisticatedstorage:controller，注册名判定零硬依赖）；卡不消耗可反复用，系统消息确认
- 【精妙终端解绑卡】新物品「精妙终端解绑卡」（9 甘蔗合成）——右击女仆清空她的终端绑定
- 【女仆从终端取物】控制器方块实体（ControllerBlockEntity）实现 Forge IItemHandler——被绑定终端视作女仆的另一个背包：建造（consumeBlock/countMaterial）与酿药（extractItemFromMaid）在背包/主副手之后自动回退到终端提取（距离 ≤32 格防跨图搬运，没装精妙储存/没绑定自然回退原逻辑）
- 【多女仆一终端】绑定表按维度存 SavedData（StringTag 列表）——终端坐标 → 列表，天然支持多只女仆绑同一终端；一只女仆只绑 1 个终端（新绑定替换旧的）
- 已部署 4,856,580

## 实测三百零七

- 【救援循环修复】根因：受困救援（MaidChunkLoadManager.tick）只有 needsRescue（**下界 y≥126 一刀切**）+ 距离 ≥8 格判定，**没有 home/坐垫/骑乘豁免**（summon 系列都有，唯独救援漏了）。猪人塔女仆故意放在基岩层（y≥126）→ 拉开距离后被每 tick 循环拽回主人身边（传送声），home 模式又把她送回基岩顶 → 下个 tick 再传 → 无限循环；传回主世界 → 跨维度再拽 → 又送回去。修复：坐垫/骑乘/在家模式 = 玩家明确停放，不救援（与 summonOne/summonMaidTo 同口径；建造女仆仍可救援）
- 已部署 4,846,071

## 实测三百零六

- 【成品药水当基底下料】批量模式下料槽 0 空且没有水瓶时，从背包取**按当前配置还能继续推进的成品药水**当基底自动放入酿造台（如 3 分钟夜视 + 配置红石增强 → 自动放入，红石延长成 8 分钟）——旧版槽 0 只认水瓶，成品药水永远只能玩家手动放。判定：主副手优先 + isBatchFinal==false（还需要强化/形态）+ 非水瓶/粗药；已最终状态的药水不取（放进去会被当成品收走死循环）
- 已部署 4,846,035

## 实测三百零五

- 【锄地事件打标修复】根因：getFinalState 恒为 null——原版锄地（HoeItem.m_6225_）走 fallback 逻辑直接 setBlock，不 setFinalState，旧版 finalState==null 直接 return → 玩家锄地永远打不上标记（标记自愈只覆盖女仆扫描范围内的耕地，玩家锄完走开/踩掉后女仆扫不到 → 不恢复）。修复：改用 getState()（锄地前的原方块）判定——锄地动作 + 原方块是锄头可锄目标（泥土/草方块/草径，HoeItem.f_41332_ 表）即打标
- 已部署 4,845,793

## 实测三百零四

- 【标记自愈】根因：标记制死锁——女仆锄地需要标记、标记又只能靠锄地事件产生（第一块地永远锄不了）；区块加载扫描只在区块加载瞬间跑一次（玩家站农田旁时早已加载）→ 标记永远为空 → 女仆完全不耕地。修复：FarmTillDriver 扫描时**当前是耕地的地块直接打标**（耕地是"曾经是耕地"的活证据）——农田的标记实时补上，踩坏的地块立刻有标记可锄；从未耕过的泥土依然无标记 → 不连锁扩散
- 已部署 4,845,766

## 实测三百零三

- 【自然生成耕地打标】区块加载扫描兜底——结构生成（村庄农田等）的耕地是直接放置方块，不触发锄地事件，标记会漏。ChunkEvent.Load 时遍历区块各 section 的耕地方块自动打标记（跳过空 section，hasOnlyAir 快速跳过；FarmlandMarkStore.markAll 批量写入只 setDirty 一次）
- 已部署 4,845,691

## 实测三百零二

- 【耕地标记制】锄地判定从"3×3 内有耕地"启发式改为**标记制**——新增 FarmlandMarkStore（SavedData 持久化，每维度一份 data/farmland_marks.dat，long[] 存压缩坐标）：玩家/女仆用锄头把泥土/草方块锄成耕地时自动打"曾经是耕地"标记（锄地事件监听，覆盖玩家手锄/女仆手锄/女仆原版农场任务锄地）；女仆只锄**有标记且当前不是耕地**的地块（泥土/草方块 + 上方空气 + 有标记 + 有锄头）。从未耕过的泥土/草方块没有标记 → 不锄——超平坦地形不再连锁扩散全变耕地（旧版锄一块后周围 3×3 就有耕地 → 5×5 全被锄成耕地）
- 【女仆锄地保持标记】FarmTillDriver 锄成耕地后立即补标记——标记是"曾经是耕地"的凭证，锄完不能丢，否则下次踩坏后女仆不认
- 已部署 4,844,703

## 实测三百

- 【按物品类型选炉子】findFurnace 重写——先扫物品栏定炉型偏好：有食物 → 烟熏炉优先（其次熔炉）；无食物但有高炉配方可烧物（矿物/粗金属）→ 高炉优先（其次熔炉）；只有仅熔炉可烧物（圆石/沙子等）→ 只选熔炉。烟熏炉只给食物、高炉只给矿物、熔炉通用
- 【绑定后无料取消】已绑定的炉子与背包不匹配（如高炉烧完矿后背包只剩食物）→ 取消绑定释放占用，重新找匹配的炉子
- 【木材黑名单】木材类（原木/木板/树苗/竹等，BlockItem 方块为原木/树叶/树苗）默认进黑名单不烧——女仆不会拿木材当原料烧（避免"用木头烧木头"）；模组详细设置新增「烧木材」开关（misc.cookBurnWood，默认关），勾选后木材类照常可烧（仍受「烧任何可烧制物」开关约束）
- 【燃料按槽位顺序】燃料选择简化为**按物品栏摆放顺序取第一个可燃烧物品**（原版 isFuel 判定）——不再按燃烧时长/数量评分；木材黑名单只拦"当原料烧"，当燃料不受影响（原木/木板照常可烧炉子）
- 【燃料改回评分制】实测后玩家改回——燃料选择恢复**燃烧时长/数量评分**（纯燃料优先：煤炭/木炭/烈焰棒等不可烧制的可燃烧物先选，评分 = 燃烧时长 ×100000 + 数量；没有纯燃料才退而选可烧制燃料兜底）
- 已部署 4,842,467

## 实测二百九十九

- 【炉子背包门控】findFurnace 加背包门控——背包里既没有食材/可烧制物、也没有燃料时，不绑定任何炉子（站桩等待，不白跑）；有任一可烧制物或燃料才去找炉子
- 【可烧制物优先不可燃】extractAnySmeltable（通用可烧制物回退）改为**不可燃烧的可烧制物优先**（圆石/沙子/矿石等——烧它们不抢燃料），可烧制燃料（原木/木板/树苗——既是原料又是燃料）最后兜底——旧版按槽位顺序取，原木槽位在前就喂原木 → 原木进原料槽、原木又进燃料槽 = "用木头烧木头"
- 已部署 4,840,666

## 实测二百九十八

- 【锄地降级为顺带逻辑】农场模式运作完全回原版——FarmMoveTillMixin 的锄地目标注入作废（不再把"需要锄的泥土"列为移动目标，TARGET_POS 只由收割/种植驱动）；FarmSweepMixin 的 tillAround 调用与方法删除（不再挂在 start TAIL）
- 【5×5 顺带锄地】FarmTillDriver 扫描范围 3×3 → **5×5**（玩家指定）——每 1 秒扫描女仆周围 5×5 内"曾经是耕地"的地块（isTillable：泥土/草方块 + 上方空气 + 3×3 内有耕地），执行换工具逻辑（ensureHoeForFarm）+ 锄地动画（音效/挥臂）+ 锄成耕地 + 消耗耐久
- 已部署 4,840,329

## 实测二百九十七

- 【酿造主副手识别】所有酿造提取（水瓶/烈焰粉/下界疣/红石/萤石/火药/龙息/粗药材料）改为**主副手优先**——先扫双手（getHandsInvWrapper，TLM 公开 API），再扫背包
- 【锄地独立驱动】根因：锄地只挂在 MaidFarmPlantTask.start 的 TAIL——start 只在 TARGET_POS 存在时触发（MaidFarmMoveTask.searchForDestination 设置，只认可收割/可种植/可锄目标）。锄完一块泥土变成耕地后，周围没有成熟作物/空耕地时扫描空转 → TARGET_POS 不设置 → start 不触发 → 锄地再也不跑。修复：新增 FarmTillDriver——ServerTick 每 1 秒主动扫描农场任务女仆周围 3×3 的可锄泥土（isTillable 判定）并锄掉（与 tillAround 同款动作：装备锄头/锄成耕地/音效/挥臂/消耗耐久），冷却表复用 FarmSweepCache.TILL_CD
- 已部署 4,841,142

## 实测二百九十六

- 【撤销实测二百九十五】枪械优先于法术任务的改动已回滚（isSpellTask 删除、buildPools 的枪械排除分支删除）——反馈实测：三叉戟被识别为近战疯狂绕圈、远程武器被识别为法杖，疑似与超时空救援等模组冲突，非本 mod 问题。恢复原逻辑（法术任务与枪械任务同池加权随机），等反馈确认冲突来源后再处理
- 已部署 4,837,785（回滚后与二百九十四同字节）

## 实测二百九十五

- 【枪械优先于法术】根因：法术任务（万法皆通 maidspell 等）的 isWeapon 认带攻击力属性的物品（法术书/法杖/枪械都带）——枪械同时被法术任务认作武器 → 与 gun_attack 同池加权随机 → 随机落到法术任务 = "自动切换变成法术模式"。修复：女仆持有枪械（GunCompat.hasGunAndAmmo）时，法术类任务（isSpellTask：命名空间 maidspell/spellbook 或 UID 含 spell）不进候选池——枪械就该用枪械模式（TLM gun_attack 有换弹/走位/弹药全套机制）
- 已部署 4,837,881

## 实测二百九十四

- 【锄地范围 3×3 → 1×1】只锄目标格（base）——不再扫周围 3×3
- 【自由漫步恢复】根因：3×3 锄地把周围泥土/草方块全锄成耕地后，FarmMoveTillMixin 又把新耕地旁的目标列为可锄目标 → TARGET_POS 常驻 → 散步门禁（已有移动目标不打扰）永远拦着 → 农场女仆失去自由漫步。1×1 后目标自然清空，散步恢复
- 【看门狗覆盖农场】SMART_TASKS 加 touhou_little_maid:farm（TaskNormalFarm UID javap 实证）——农场女仆空闲发呆时每 10 秒气泡解释"附近没有需要打理的农田了"
- 已部署 4,837,785

## 实测二百九十三

- 【GUI 时长按形态折算】药剂手册 tooltip 效果行旧版直接显示 Potion 注册表的饮用型满时长——喷溅/滞留型药水的效果描述与实际不符。修复：按原版规则折算——喷溅 = 饮用 ×3/4、滞留 = 饮用 ×1/4（如夜视 3:00 → 喷溅 2:15、滞留 0:45），等级不变
- 已部署 4,837,765

## 实测二百九十二

- 【批量收成品检查配置】根因：批量模式收成品（isDonePotion 为真就收）不检查配置——3 分钟夜视一出现就被收走，processForm 的强化步骤永远轮不到 → 配置"红石延长"却产出普通时长药水。修复：新增 isBatchFinal——未到目标强化/形态的成品【不收】，留在酿造台继续推进；平凡/浓稠（无效果死路）照常收走腾位；该药水无对应强化变体（如 healing 无延长版，原版配方表 m_43529_ 实测判定）放行不卡死
- 【缺强化材料不转形态】根因：processForm 缺红石/萤石时静默跳过强化直接转形态 → 产出未强化的滞留/喷溅药水。修复：缺强化材料 → notifyMissing 报告 + return 不转形态——饮用瓶留在酿造台等材料，补上后自动强化，强化完才转形态
- 【工程】ResourceLocation.getPath 在 SRG 下是 m_135827_（MaidCookBehavior.hasOreTag 同款实证），getPath 编译不过
- 已部署 4,837,717

## 实测二百九十一

- 【酿造台占用】两个女仆抢同一酿造台 → 熔炉同款占用表（BREW_USERS，维度|坐标 → 占用女仆 UUID）：启动即登记、findBrewingStand 跳过他人占用、台子丢失/行为停止释放、占用者死亡懒清理——多女仆自动分散到不同酿造台
- 【空瓶卡死修复】女仆往酿造台放空瓶导致酿造完全无法进行——根因：isDonePotion 只认 PotionItem，空瓶（glass_bottle）既不是药水（不收走）又占着槽（不补水瓶）→ 卡死。修复：非药水物品（空瓶/杂物）一律收走腾位；extractWaterBottle 严格只认【饮用型】水瓶（排除喷溅/滞留水瓶——旧版只认 PotionItem 不区分形态，喷溅水瓶也会被当水瓶放进槽 0 卡死）
- 【锄地草方块判定】泥土变草方块后女仆彻底失去判定——根因：isTillable 只认 dirt（f_50493_），草方块蔓延后不再锄。修复：判定扩展认 grass_block（f_50440_，javap 实证 GrassBlock 构造 → f_50440_）——原版锄头（HoeItem.f_41332_ 表）本来就能锄 dirt/grass_block/dirt_path → farmland，判定与锄地动作对齐
- 【锄地频率】冷却 40→20 tick（2 秒→1 秒）——"锄地检测的频率不行，导致没有办法及时锄地"
- 【批量酿造材料轮换】批量模式材料选择从"按背包槽位顺序取第一个"改为"优先选与上次不同的材料"（每女仆记录 LAST_INGREDIENT）——给多种材料时交替酿多种药水（A→B→C→A…），不再死磕第一种；只给单一材料时自然只酿那种（符合批量本意）
- 已部署 4,837,317

## 实测二百九十

- 【触发半径】4 格 → **2 格**——摸头/抱抱是贴身动作，站 4 格外隔空摸头太假；2 格内存在女仆按 G/H 即触发（取最近）
- 【旁白文案】系统消息改为第三人称叙述（括号包裹）：摸头 = "（你伸出手，轻轻揉了揉 XX 的头。她眯起眼睛，像只被顺毛的猫一样蹭着你的手心，头顶仿佛冒出了小星星）（好感 +1…）"；抱抱 = "（你张开双臂，把 XX 轻轻拥入怀中。她先是愣了一下，随即红着脸回抱住你，把脸埋在你胸口蹭了蹭，小声嘟囔着「主人的怀抱最安心了～」）（好感 +3…）"
- 【判定简化回顾】实测二百八十九最终方案：不再做视线/角度/射线判定（cos≥0.9 贴脸时垂直差拉低 cos 永远失败；射线-AABB 也失败）——改为空手右击同款逻辑：范围内存在女仆即触发；查询范围 16 格（m_82363_ 正参数只向正方向膨胀，6 格查询实测找不到 dist=2.3 的女仆，16 格查询经日志实证可靠）
- 已部署 4,844,062

## 实测二百八十九

- 【根因链】按键链路全程正常（客户端捕获→发包→服务端收包，日志实证），断点在服务端判定：旧版 cos≥0.9 角度阈值 + 距离下限 0.5 + 参考点 +0.8——TLM 女仆矮（约 1.4 格），玩家眼睛 1.62，贴脸时垂直差占比大，cos 被拉低到 0.9 以下，越贴脸越失败；距离下限 0.5 把贴脸女仆直接跳过
- 【修复】判定简化为"范围内最近女仆"（空手右击同款逻辑），零视线判定；查询范围 16 格（6 格查询实测找不到 dist=2.3 的女仆——m_82363_ 正参数只向正方向膨胀，范围语义存疑，16 格经日志实证可靠）
- 【诊断】emotion key / emotion srv 日志链路（客户端捕获、服务端收包、判定结果），验证后保留精简版
- 已部署 4,843,883

## 实测二百八十八

- 【默认键位回退】摸摸头 = **G**、抱抱 = **H**——撤销二百八十七的 Shift+G/Shift+H 方案（Screen.m_96638_ 的 SRG 归属未做字节码实证，存在映射错误的可能，导致组合键触发不了）；恢复为直接 G/H 触发，tick 轮询 consumeClick 不再过滤 Shift
- 【键位描述】lang 回退为"摸摸头（对准女仆）/ 抱抱（对准女仆）"
- 已部署 4,843,598

## 实测二百八十七

- 【默认键位】摸摸头 = **Shift+G**、抱抱 = **Shift+H**——MC 的 KeyMapping 原生不支持修饰键组合注册，实现为绑定 G/H 后在 tick 轮询处过滤 Shift 状态（Screen.m_96638_ = hasShiftDown）；单独按 G/H 不触发，且 Shift 未按下时主动排空按键计数（防松开 Shift 后残余计数延迟误触发）
- 【键位描述】lang 更新为"摸摸头（Shift+G 对准女仆）/ 抱抱（Shift+H 对准女仆）"；原版按键设置里显示的主键仍是 G/H（可改键，改键后 Shift 修饰依然生效）
- 已部署 4,843,700

## 实测二百八十六

- 【HeartPact 源码研究】（github.com/Asumi-Nishiki/TouhouLittleMaid-HeartPact，MIT 开源）：其拥抱/摸头动作 = **mixin 到 TLM `HardcodedAnimationManger.playMaidAnimation/playGeckoMaidAnimation` 的 RETURN，在最终骨骼覆盖层手写角度**（Bedrock 模型走 ModelRendererWrapper、Gecko 模型走 AnimatedGeoBone，骨骼名多套兼容模糊查找），服务端状态 + S2C 同步 + 客户端按进度淡入淡出插值——不是动画文件
- 【动作内建】把该机制移植进 ProMaid：EmotionPoseState（服务端姿势状态，游戏 tick 自动过期）+ EmotionPosePacket（S2C 同步，TRACKING_ENTITY_AND_SELF 广播）+ EmotionPoseClient（客户端墙钟进度：摸头 2s/抱抱 4s，300ms 淡入 500ms 淡出）+ EmotionPoseMixin（Bedrock/Gecko 双路径骨骼覆盖，姿势参数取 HeartPact 已调优数值：抱抱=肩/大臂/前臂三段弧线环抱+身体前倾，摸头=头部 sin 轻摆+微前倾；抱抱中摸头自动降级为头部轻摆叠加）
- 【不依赖 HeartPact】玩家 mods 里 HeartPact 处于 disabled 状态——动作直接内建进 promaid，无需启用第三方 mod；G/H 键触发链路不变，现在按下后女仆会**播放对应姿势动画**
- 【工程】Gecko 头部 yaw 轴与 Bedrock 反向（HeartPact 实证）已按其调整；Math.sin 显式 (float) 强转；mixin 注册于 client 段（与其它 client mixin 同包避免 build 校验子包路径问题）；女仆卸载时 EmotionPoseState.forget 挂入 AiMemoryManager 遗忘链路
- 已部署 4,843,599

## 实测二百八十五

- 【TLM 调查结论】1.5.3 无内置摸头/抱抱动作（animationId 仅模型内置 idle/walk/attack 等；joy 是坐垫方块）；可用的官方能力 = 好感度 API（EntityMaid.getFavorabilityManager().add(int)/getLevel()，javap 实证）+ 心形粒子（SpawnParticleMessage.HEART）+ 女仆语音（tryPlayMaidPickupSound）+ 气泡
- 【触发】新增键位 **G【摸摸头】/ H【抱抱】**（默认键，注册进原版按键设置"Promaid 建造"分类可改键；G/H 原版零占用，避开 V 语音/B 背包/JEI R）。对准 4 格内的女仆按键触发；客户端只发动作类型，服务端全量验证
- 【服务端验证】视线夹角（cos≥0.9，坐标手算不依赖不确定的 Vec3 SRG）+ 距离 ≤4 格 + **仅主人**（m_21830_，他人提示"她只让主人摸哦～"）+ 冷却（摸头 8 秒/抱抱 30 秒，按女仆）
- 【效果】摸头 = 好感 +1 + 心形粒子 + 挥臂 + 女仆语音 + 随机开心气泡 + 系统消息（含好感等级）；抱抱 = 好感 +3 + 双向回 1 心 + 粒子加倍 + 气泡 + 系统消息；女仆看向主人（复用 lookAtOwner）
- 【实现】EmotionNetworking（自建 channel maid_smart/emotion，C2S 包 varint 动作类型——m_130130_/m_130242_）+ EmotionKeysClient（键位注册 + tick 轮询 consumeClick，Screen 打开时原版机制天然不喂键）
- 已部署 4,835,545

## 实测二百八十四

- 【根因·getter 反了】effectLine 把 duration/amplifier 的 SRG 搞反——截图实证：跳跃药水（duration=3600、amplifier=0）显示"跳跃提升 3601"，即 duration(3600) 被当成等级喂进罗马数字函数（roman(3601) 超表回落数字），时长分支反而不触发。字节码字段序号实证：f_19503_（第 1 个 int 字段，vanilla 顺序 effect/duration/amplifier）= duration → **m_19557_**；f_19504_ = amplifier → **m_19564_**
- 【修复后】效果行"跳跃提升 I (3:00)"：等级始终显示（I/II/III 罗马，I 也不再隐藏），时长 duration/20 分:秒格式
- 已部署 4,827,566

## 实测二百八十三

- 【遗留播报删除】WorkStatusReporter 的"炉子/酿造台明明就在附近（N个）但我没能开始工作"诊断气泡删除——烹饪/酿造早已改为站桩模式，女仆贴着方块等待（烧制中/缺料/收成品）是正常工作状态，这句 v1.5.7 的诊断输出就是纯误报；COOK/BREW 空闲诊断分支整体移除（countBlocks 随删）
- 【页码重叠】翻页箭头间距拉开（◀ cx-70 / ▶ cx+50）——旧版箭头（cx±34/-14）与居中页码"第 1/2 页"重叠
- 【tooltip 药水效果】悬停 tooltip 在药水名下加效果行（"跳跃 II (3:00)"：效果本地化名 + 罗马等级 + 时长 mm:ss；m_19544_/m_19557_/m_19564_，appendHoverText 字节码实证）——旧版只有名字看不到效果
- 【mod 配方通用适配·拓展点】BrewRecipeResolver 追加读取 **Forge BrewingRecipeRegistry**（mod 通过 Forge API 注册的酿造配方在 Forge 自己的表里，不进 vanilla PotionBrewing 表）——getRecipes/getInput/getIngredient/getOutput 全公开 API 运行时遍历，输出为药水的配方自动并入反推链（input 提取基底、Ingredient 取代表材料）；任何 mod 的新药水配方**自动进入定向酿造**，无需逐个适配（自定义 IBrewingRecipe 实现无法静态反推的除外）
- 已部署 4,827,578

## 实测二百八十二

- 【根因·形态选中态】绿框判定只比对药水 id、不管形态——targetPotion 在切形态时不变，绿框恒亮。玩家视角：勾选"跳跃药水"后切喷溅/滞留，"对应的喷溅/滞留药水"也显示被勾选；重开 GUI 依次点三个形态按钮，网格绿框每次都亮——**三种形态看起来同时被选中**，完全无法分辨当前目标形态，也解释了"手册内部对喷溅/滞留的判定有问题"的观感
- 【修复】切换形态按钮即清除已选目标（cfg.targetPotion=""）——绿框语义变为「**当前形态下**已选定的目标」：切形态后必须重新点选（网格图标已是新形态的瓶），点选才亮；重开后绿框只亮在保存的形态上。点同一形态按钮不清空（防误触丢选中）
- 【说明文案】模式说明与 提示同步更新交互语义（"先选形态，再点选目标；切形态会清除已选；缺料会立即报告"）
- 注：服务端 form 本就正确保存/生效（保存消息有形态摘要）；女仆酿不出喷溅/滞留的主因是此前缺料无提示（二百八十一已修）——喷溅需火药、滞留需火药+龙息，女仆背包缺料时会立即气泡+系统消息报告
- 已部署 4,827,299

## 实测二百八十一

- 【材料清单改悬停 tooltip】链 + "每批 3 瓶材料"（物品名 ×数量逐行）改为**跟随鼠标的标准 tooltip**（m_280677_，仿 EMC 面板悬停样式）——旧版画在网格右侧固定区，链换行多时把材料区推出面板/被矮窗口保护线裁掉，玩家根本看不见；信息区右侧恢复只留药水名
- 【根因·"喷溅/滞留没法酿"】形态推进逻辑本身完整（progress 到顶 → processForm 下火药/龙息），但**缺料时静默等待零提示**——玩家不知道还要往女仆背包放火药（喷溅）/火药+龙息（滞留），女仆卡在酿造台旁不动，观感即"没法选中/没法酿"。修复：缺料立即报告——女仆气泡（"主人，酿造缺材料：火药 ×1，放进我背包我就继续～"，服务端中文映射）+ 主人系统消息（translatable 组件，物品名客户端本地化）；30 秒冷却 + 同材料不重复刷屏，补料后重置
- 【女仆动作】补燃料/补水瓶/下料/形态材料/收成品时挥臂（m_6674_），酿造过程有动作可视化
- 【搜索框中文】potionCache 加本地化名字段（构建时 stack hoverName），搜索匹配 id + path + **中文名**——"跳跃药水"现在能直接搜到
- 【保存后系统提示】保存成功即发系统消息：模式/目标/形态完整摘要（"批量酿造 / 强化=红石延长 / 形态=喷溅"）——玩家能确认形态真的存进去了，旧版保存无任何反馈
- 已部署 4,827,236

## 实测二百八十

- 【形态图标联动】网格里的药水图标随形态按钮联动：饮用=水瓶/喷溅=喷溅型水瓶（splash_potion）/滞留=滞留型水瓶（lingering_potion），容器物品 + 药水 NBT 渲染，颜色随药水不变
- 【材料清单】悬停药水图标时，信息区显示"每批 3 瓶材料"：水瓶 ×3 + 链上每步材料 ×1（酿造台 1 个材料可同时酿 3 瓶）+ 形态材料（喷溅=火药 ×1 / 滞留=火药+龙息各 ×1）——图标 + 右下角数量小字（仿物品栏），随当前形态实时变化
- 【配方链尾部补形态步】链文本现在含形态转换材料（"… → 兔子脚 → 火药"），与材料清单一致
- 【根因·定向模式死循环（隐藏炸弹，顺带修复）】resolveChain 链基底此前回退到 water **或 awkward**——awkward 基底的链不含 water→awkward 步，而 processTargeted 空槽补水瓶 → progressOf(水瓶)=-1（不在链上）→ 水瓶被当无关药水收走 → 再补再收**死循环**（原版绝大多数药水从 awkward 起步，仅 weakness 等少数 water 直达）→ 定向模式实际无法工作。修复：isBase 只认 water，链始终完整回退到水瓶起步（GUI 链/材料清单同步完整）
- 【id 行移除】悬停信息去掉注册名行（截图实证超宽溢出面板右缘）
- 已部署 4,824,775

## 实测二百七十九

- 【根因·点击全选】选中/点击判定此前用 `ITEMS.getKey(stack.getItem())`——所有药水瓶物品的注册名都是 `minecraft:potion`（不是药水 id）→ 点任一格后 targetPotion 恒为 "minecraft:potion" → 渲染时所有格子都匹配 → 全部显示绿框，保存的目标也是错的。改为按**药水注册名**（如 minecraft:healing）判定（GridEntry 记录药水 id，图标 stack 只负责渲染）
- 【根因·全部"无法酿造"】Forge 1.20.1 对 PotionBrewing 的 **binpatch**（Forge GitHub patch 文件实证）把 `Mix.from/to` 字段类型从 Potion/Item 本体改成 **Holder.Reference**——javap 未 patch 的原版 jar 看不出来，实机反射拿到的是 Holder → `instanceof Potion` 全部失败 → 反推表为空 → isTableUsable false（连水瓶都列出来）+ 悬停全显示"无法酿造"。修复：取值后经 `unwrapHolder` 解包（m_203334_ = Holder.value()，javap SRG 实证），并新增 lastError 诊断字段；服务端定向酿造与 GUI 同源修复
- 【悬停名】悬停此前显示注册名 "minecraft:potion"，改用药水**本地化名**（stack hoverName 走客户端语言文件，如"治疗药水"/"喷溅型夜视药水"）；配方链预览的基底/材料名同样改本地化名（"水瓶 → 下界疣 → 粗制药水 → 金西瓜"），超宽自动换行
- 【布局重排】模式行（y=26）→ 说明行独立一行（y=50，不再压按钮）→ 形态行两种模式同一位置（y=90）；**强化行仅批量模式显示**（定向的强化走 long_/strong_ 变体药水，不使用强化参数——此前定向模式下强化行照常显示是认知混乱源头）；批量模式补充已选强化说明；矮窗口 3/2/1 行兜底；页码移到翻页箭头间
- 【列表净化】基底/无效果药水（water/mundane/thick/empty）不再作为可选目标
- 已部署 4,824,144

## 实测二百七十八

- 【根因】锄地逻辑挂在 `MaidFarmPlantTask.start` 的 TAIL，而 start 只在 canUse 返回 true 时触发；canUse 要求 TARGET_POS 内存存在，TARGET_POS 由 `MaidFarmMoveTask.shouldMoveTo` 扫描设置——它只认**可收割（成熟作物）/可种植（空耕地+背包有种子）**两种目标。玩家验证场景（踩坏的泥土 + 空耕地 + 只有锄头没种子）两种目标都不存在 → 找不到目标 → TARGET_POS 永不设置 → start 永不触发 → 锄地永不运行（javap 字节码实证完整链路）
- 【修复·目标判定注入】新增 `FarmMoveTillMixin`：`MaidFarmMoveTask.shouldMoveTo` TAIL 注入第三个目标判定——"需要锄的泥土"（dirt + 上方空气 + 3×3 内有耕地 + 背包/主手有锄头，判定抽到 `FarmSweepCache.isTillable`）也算目标。女仆会走过去，start 触发后锄地
- 【中心格锄地】目标格本身也可能是泥土（新链路下目标格就是泥土），`tillAround` 不再跳过中心格，3×3 全扫
- 【判定统一】`isTillable`/`nearFarmland` 抽到 FarmSweepCache（普通类，mixin 与业务代码共用），FarmSweepMixin 与 FarmMoveTillMixin 同源判定
- 已部署 4,822,720

## 实测二百七十七

- 【新物品·女仆药剂手册】`maid_smart:brew_manual`：水瓶+书本无序合成，创造栏「工具与实用品」页；手持右击自己的女仆打开酿造配置 GUI（附魔光效同排班表）
- 【两种模式】批量酿造（默认）：背包里有什么正向材料就酿什么，按配置的强化路线（无/红石延长/萤石强化）与成品形态（饮用/喷溅/滞留）统一处理；定向酿造：只按目标药水的配方链精确下料，缺料停止下料等待（半成品留在酿造台，补料后自动继续，不换材料凑合）
- 【配方链反推】BrewRecipeResolver 反射读取原版 PotionBrewing 运行时配方表（f_43494_ Mix 表，javap 实证 1.20.1 SRG），从目标药水递归回退至 water/awkward 基底得整条链；酿造台判定（m_155294_）完全走原版 PotionBrewing，反推链与运行时行为一致
- 【GUI】仿挖矿矿物面板格式：模式切换 + 强化/形态选择行 + 药水选择面板（搜索框 + 创造网格 + 已选列表，药水图标带颜色渲染，悬停显示配方链预览）；只列"有链可达"的药水
- 【配置持久化】每女仆 NBT（maid_smart_brew_config），服务端校验归属/药水存在；MISC_BREW_AUTO=false 仍是总闸（完全禁用自动下料，向后兼容）
- 【网络层】BrewManualNetworking（SimpleChannel maid_smart/brew_manual）：S2C 打开包 + C2S 保存包（方向校验同排班表）


## 实测二百七十六

- 【实现·耕地恢复】FarmSweepMixin 新增 `tillAround`（挂在农场连收连种增强里，2 秒冷却）：以目标格为中心 3×3 水平扫描——该格是**泥土**（耕地退化/踩踏来的）且上方是空气、且水平 3×3 内存在**耕地**（农田区域信号，自然泥土/草方块旁无耕地不锄）→ 锄成耕地
- 【锄头装备·与矿镐同优先级】`MaidToolAutoEquip.ensureHoeForFarm`：主手已是锄头且未即将用坏 → 零开销不换（玩家亲手放的锄头不触发切换）；空手/非锄头/快坏 → 从背包装备评分最高的一把（附魔词条数 > 剩余耐久，锄头对耕地无挖掘等级差异）；快坏锄头黑名单保护跳过；背包无锄头 → 跳过不空手硬锄
- 【耐久消耗】锄地后 `m_41622_(1, maid)` 消耗 1 点耐久（HoeItem.m_6225_ 字节码实证同款消耗路径）+ 锄地音效（SoundEvents.f_11955_）+ 挥臂动画
- 已部署 4,792,226

## 实测二百七十五

- 【根因】建造行为进入时强制 `setHomeModeEnable(true)`（防 TLM 跟随拉走），而召回链路（一键集合/单独传送/未加载区块召回）全部豁免 home 模式 → 建造女仆恒被挡，必须先解除建造才能召回
- 【实现·建造女仆召回豁免】新增 `isBuildingMaid` 判定（建造任务中），5 处召回拦截点全部放行建造女仆：①一键集合在场扫描；②单独传送（详情页「传送到我身边」）；③summonMaidTo 咽喉点；④未加载区块待召回队列（强载出来发现是建造女仆 → 直接召回而非保持原位）；⑤LAST_SEEN 快照（建造女仆不记 stayPut，未加载区块也能被强载召回）
- 【召回后自动回工地】建造女仆被召回后，`teleportToWorkSite`（每 4 tick 限频）自动把她瞬移回目标格旁继续建——落点不稳时先召回再让她自己回去，无需解除建造
- 已部署 4,791,056

## 实测二百七十四

- 【实现·气泡统一静默】所有女仆系统气泡都走同一入口 `ChatBubbleManager.addTextChatBubble`（79 处调用，含自保喊话/喂食/互助/钓鱼/图腾/挖矿/伐木/闲聊），在既有 ChatBubbleLimitMixin 拦截点加规则：建造任务中的女仆、且调用栈**非建造系统来源**（`com.maidsmart.build` 包）→ 气泡丢弃（返回 -1）。建造系统的气泡（缺料/跳过/完成/设计师回复等）照常显示
- 【实现·字幕点加闸】9 处"女仆自发"直接发聊天栏的系统消息逐点加 `shouldMute` 闸：没食物播报、喂食系统提示、互助支援、药水塞包、金苹果、牛奶、不死图腾、挖矿/伐木挡路字幕、主人死亡传送气泡、主动对话日上限——建造中全部静默
- 【保留】玩家 GUI/命令操作的响应消息（排班书操作/集合/蛋糕互动/LLM 开关/配置面板/手册赠送）不受影响——那是玩家操作的反馈，不是女仆自发消息
- 已部署 4,790,852

## 实测二百七十三

- 【实现·BuildShieldGuard 新类】建造任务进行中的女仆三层保护（全部任务级判定，切出建造立刻失效）：
  - **无限时长抗性 V**：首次进入建造施加（`MobEffectInstance.f_267388_` = -1 无限时长，字节码实证 isInfiniteDuration 判 f_19503_==-1；`MobEffects.f_19606_` = resistance，static 块 putstatic 顺序实证），无粒子无 HUD 图标，切出建造移除效果并清标记（只移除我们给的，玩家自己叠的抗性不动）
  - **受击取消**：LivingHurtEvent cancel——覆盖近战/箭矢/弹幕/爆炸/魔法等一切伤害来源，比抗性减伤更彻底（虚空/窒息等不吃抗性的伤害也免疫）
  - **仇恨拦截**：LivingChangeTargetEvent.setNewTarget(null)——Forge 在 Mob.setTarget 发此事件，怪物脑内传感器/行为每次重评估目标都经过它 → 任何生物永远不把建造女仆锁定为目标，已存在的旧仇恨下次重评估即清
- 【接入】效果维护挂在 tickBuildSit 每 tick（全 activity 覆盖，任务级标记防残留）
- 已部署 4,790,080

## 实测二百七十二

- 【方案选择】方案 B（先瞬移到要搭方块的位置，再放置）——女仆"先到位 → 挥臂 → 方块在眼前出现"，视觉因果链完整，配合既有挥臂动画即"亲手摆放"观感；方案 A（放置后再瞬移）是"方块先凭空出现、她后闪现过去"，因果倒置，违和感未消除
- 【实现·先瞬移再放置】新增 `teleportToWorkSite`：两处 doPlace（主循环 + 延后补建）放置前把女仆瞬移到目标格旁的安全站立格——水平相邻同层、格本身空气 + 头顶空气 + 脚下有承托（非空气非流体）、区块必须已加载（防未加载区块伪空气传进虚空）；找不到安全站姿则不瞬移（隔空放置兜底）；已在 2 格内不瞬移；限频 4 tick（极速模式连续放置不连闪）
- 【坐姿改造】`tickBuildSit` 从"强制坐下"改为"确保站立"：建造中若仍坐姿（玩家手动 shift+右键/旧版遗留）→ 下一 tick 站起；**锁移动全部保留**（清 WALK_TARGET/停导航/清水平速度 + BUILD_SIT_TAG 标记，MaidMoveSuppressMixin/MaidStationaryMixin 的移动源头锁继续生效）——她瞬移到位置后站定放置，不会被 TLM 跟随/行为拉走
- 已部署 4,788,629

## 实测二百七十一（女仆直接把女仆床拆掉）

- 【根因】持光系统撤放光块的 placeBlock 用 `level.m_7731_` **直接替换**目标格方块——女仆睡觉时身体与床方块重叠，光块位置=床格，床被光块顶掉；女仆离开后光块又被清成空气，床彻底消失
- 【修复】placeBlock 三个写入路径统一加固：目标格是**空气或本光块**才可写入，被床等任何方块占据 → 跳过（绝不覆盖）；放置/补回/光强更新全部走同一入口。睡觉、坐垫上、水中等正常场景不再误伤方块
- 已部署 4,787,917

## 实测二百七十（点击应用空闲女仆仍显示别的模式（排班没开也这样））

- 【根因】“空闲”档在 GUI 里=空串 ""——旧版 `if (!batchTask.isEmpty())` 把空串拦掉 → 点“应用空闲”什么都不发，女仆自然保持旧模式
- 【修复】①应用任务按钮：空串 → 当作 `touhou_little_maid:idle`（TLM 空闲任务）发送；②批量任务循环**含空闲档回绕**（旧版 indexOf=-1 直接跳到第一个任务，永远选不到空闲）；③服务端任务读回校验（setTask 无守卫但客户端脱钩/任务被换回会静默失败，读回暴露“应用了但没生效”）

## 实测二百六十九（排班有日程但工作状态不符合日程安排）

- 【根因】休息时段（当前时间无段覆盖，如晚班女仆白天）applyNow 直接 return——**模式也不切**，女仆保持排班前的模式（如图：早班+空闲），“排班开了状态却与日程不符”
- 【修复】休息时段不再什么都不做：把作息切到班次模式（早班=DAY/晚班=NIGHT/全天=ALL），TLM 作息系统接管睡觉/待机；任务留给工作窗口内的段应用。女仆模式=班次模式=排班规定的状态，GUI 同步后与日程一致

## 实测二百六十八（排班生效后快捷设置页 GUI 应立即统一更新再锁定）

- 【现状】排班段应用成功只改服务端状态，客户端排班书数据来自打开那一刻的包 → GUI 永不刷新，观感“排班没生效”
- 【实现】新增 MaidStateSyncPacket（包 id 11，uuid/taskUid/模式/排班开关）：段应用成功（含休息时段切班次模式）→ 推给打开着排班书的主人 → `syncMaidState` 更新列表行数据 + 重建界面——快捷设置页立即显示排班规定的模式/任务并锁定

## 实测二百六十七（剪刀模式+解除排班+满血，全员模式不响应）

- 【根因】批量应用对“跳过/失败”完全静默——女仆被跳过无任何提示，排查无从下手
- 【修复】批量应用可观测化：①逐只诊断日志（latest.log 搜 `batch-apply`，记录 applied/skip+原因 preserve/combat/sched）；②应用后**读回校验**（TLM setSchedule/setTask 有守卫会静默拒绝，读回暴露）；③单只 try/catch 隔离——一只异常不再中断整队

## 实测二百六十六（满血时全员模式有时没应用（承接二百六十五））

- 【实现】isRealCombatActive：标记 true + ASSIGNED 匹配当前任务 = 真本系统战斗（跳过，防打断战斗还原链）；false = 残留标记/玩家手动攻击任务/第三方攻击任务 → 顺带清残留（clearMarkersForExternal：clearMarkers + restorePrevMode）→ 外部系统正常应用

## 实测二百六十五（全员模式有时无法有效应用（明明解除了排班））

- 【根因①·残留战斗标记】旧批量应用用 isReallyCombatActive 跳过——排班关闭时它含 IAttackTask 兜底（当前任务是任意攻击任务即判“战斗中”）→ 玩家手动安排攻击任务的女仆（满血、无排班）被**永久跳过**
- 【根因②·自保标记自愈不对称】进入自保要血<30%（enterRatio），自愈条件却是 70%（exitRatio）→ 血在 30%~70% 且无危险时会话不进、PRESERVE_TAG 残留永不清理 = 批量应用/排班/参战永久跳过。修复：自愈条件放宽到 ≥enterRatio(30%)——血≥30% 且无危险 = 不需要自保 = 标记是残留，清掉

## 实测二百六十四（药水动画效果给到之后立刻落地）

- 【根因】效果是触发即生效（瞬间给到），动画药水却还要飞半秒才落地——观感脱节（“效果先到、瓶子还在天上飞”）
- 【修复】①动画抛掷速度 1.0 → **4.0**（2~3 tick 内到达落地碎裂）；②追踪上限收窄为 **1 tick**（出生后立即停止追踪，自然落地）——动画与效果施加几乎同步

## 实测二百六十三（空闲状态排班也不生效）

- 【根因】MISC_SCHEDULE_AVAILABILITY_CHECK=true 时走 isAvailable 完整软探测（附近有没有矿/树/炉子/作物）——女仆空闲时通常在家附近没活 → 判“不可用”→ 每 10 秒重试**永不切换** = “排班开了不生效、锁定解除不了”
- 【修复】排班是**纯时间判定**（TLM-Sincerely 环境检测子系统不移植）：切换恒只查任务自己的硬闸（isEnable），任务状态跟着时间段落真实切换

## 实测二百六十二（女仆手上拿的是木头仍然会发光）

- 【根因】javap 实证 m_60739_ 是 **getLightBlock**（光阻挡值：完整不透明方块返回 15、实心返回 0）不是 getLightEmission——木头是完整方块恒返回 15 → 手持任何完整方块都被当成 15 级光源
- 【修复】改用 m_60791_（**getLightEmission**，自身发光，与光照引擎同源）：木头=0 不发光，火把 14/萤石 15/岩浆块 3 等正常；白名单路径原样保留

## 实测二百六十一（排班开启后女仆一直保持未排班状态，锁定还解除不了）

- 【根因①·战斗让位门过宽】排班女仆手头只要是攻击类任务（玩家手动安排的近战/弓/弹幕/残留标记）就被“战斗中”让位门永久挡死——模式/任务永不切换、守卫 mixin 又拦下一切手动调整 = 排班锁死。修复：isReallyCombatActive 判定收窄——排班开启的女仆不参与自主战斗（tryEngageMaid 直接跳过），只认 ASSIGNED 与当前任务一致才算本系统战斗
- 【根因②·跨维度静默失败】排班书列表全维度扫描，但开关/设置/保存只查**玩家当前维度**——女仆在别的维度时全部操作静默失败（客户端列表显示已改、服务端没执行）。修复：findMaid 跨维度查找（与列表口径一致），找不到才返回 null

## 实测二百六十（药水动画追踪上限改为半秒）

- TRACK_LIMIT_TICKS 20 → 10：动画药水最多追踪半秒，超过停止追踪、保持当前速度自然落地（碎裂/云动画完整）

## 实测二百五十九（滞留药水药水效果云还是没做好）

- 【根因】`cloud.m_19714_(maxDur)` 注释写“setDuration”——**实际是 setColor（写颜色数据槽）**！把时长当颜色写入 → 云永远是暗色怪云（看起来像无效云）。真正的 setDuration 是 m_19734_（字节码实证原版 ThrownPotion.m_37537_）
- 【修复】云参数全面对齐原版：setRadius 3.0 / setWaitTime 10（0.5 秒后开始生效/成长）/ setRadiusOnFall -0.5 / setRadiusPerTick -3÷时长 / setDuration m_19734_ / setPotion（云颜色自动按药水算出）+ addEffect 全部效果；自保侧的滞留云同口径

## 实测二百五十八（给女仆加一个不会踩坏农田的效果）

- 【实现】FarmlandGuard：注入 Forge 的 FarmlandTrampleEvent（农田踩踏入口，走路/跳跃/坠落都经过）——触发者是女仆 → cancel（农田保持原样，不变成泥土）。女仆移动走位频繁、容易把玩家的农业区踩坏，本守卫让女仆对农田完全“无痕”

## 实测二百五十七（滞留型药水突然没效果、药水云没了、投掷动画也没了）

- 【根因】动画药水无追踪上限：目标移动时永远追着飞（距离恒≥0.6）永不落地 → 看不到落地碎裂动画、也看不到落地生成的药水云（动画药水是实际滞留药水复制）
- 【修复】PotionAnimTrackMixin 加 1 秒（20 tick）追踪上限：超过停止追踪、保持当前速度自然落地——碎裂/云动画完整

## 实测二百五十六（喂食效果要跟女仆自己吃食物效果一样）

- 【实现】投喂改走 `LivingEntity.m_5584_`（eat 完整路径）——与女仆自己吃食物完全一致：食物效果（FoodProperties 原版施加）、吃/喝音效全部由原版路径触发；移除上轮手搓的 eat+heal+手搓音效（与“自己吃”观感不一致）；蜂蜜瓶解中毒 + 空玻璃瓶返还逻辑保留

## 实测二百五十五（药水动画落点偏移严重（效果给到了但落点不同很违和））

- 【实现】动画药水落点**时刻追踪**：动画实体带目标 UUID 标签（maid_smart_anim_target），每 tick 用当前实际位置重算抛物线瞄准目标当前位置——既有弧线又跟目标移动，落点与目标几乎重合；距离<0.6 格视为落点已到，保持速度自然落地（碎裂动画完整）

## 实测二百五十四（药水动画应该是投掷药水的动画效果（空瓶/白水花很违和））

- 【根因】动画药水用空喷溅药水（setPotion empty）：红瓶/白水花与“投掷再生药水”观感完全不符
- 【修复】动画药水改用**实际药水完整复制**（同样形态 + 实际 Potion 效果）：飞出去的就是再生药水瓶（紫色），落地自然呈现真实碎裂粒子/效果云；效果重复无害（触发逻辑 forceAddEffect 强制施加，动画落地溅射只会与原有效果合并取更长）

## 实测二百五十三（女仆根本连效果都没吃到）

- 【根因】旧版 `PotionUtils.m_43571_`（getMobEffects）只读 NBT 的 CustomPotionEffects 列表——字节码实证它**完全不读 Potion 字段**：再生/治疗/抗火等标准药水的效果存在 Potion 注册表（Potion.getEffects），不在 CustomPotionEffects → 读出来恒为空 → 效果从第一次就没加上
- 【修复】新增 potionEffectsOf：Potion.getEffects + CustomPotionEffects 合并读取，两处投掷与自保饮用同口径

## 实测二百五十二（被投掷的女仆连药水粒子效果都没有（效果压根没给到））

- 【根因】m_7292_（addEffect）在目标已有同类效果且不比现有更强时**拒绝施加返回 false**——再生药水第一次生效后后续全部被拒，效果从不刷新/可见，且直接 addEffect 不播粒子（日志实证 cast 每 3~5 秒连投但目标无粒子无回血）
- 【修复】①改用 m_147207_（forceAddEffect——跳过“不更强则失败”规则，强制施加/刷新时长）；②施加后播 entity_effect 紫色药水粒子（目标身上可见的“效果已给到”反馈）；③日志记录施加结果（aid-maid/owner effect: applied=N）

## 实测二百五十一（药水以抛物线方式从女仆飞到目标原有位置的动画（仅仅是动画））

- 【实现】新增 throwPotionAnimate：生成空喷溅药水（无任何效果，落地不施加东西），从女仆位置按弹道公式（速度 1.0/无散布/解仰角）抛物线飞向目标位置，纯观感；两处触发（治疗姐妹/治疗主人）在消耗+摆臂动画后调用；近距离（≤3格）/目标消失不抛

## 实测二百五十（显示扔出药水但看不到被扔出、效果也没给到）

- 【实现】触发即生效重写：只要触发扔药水事件就给予效果——消耗药水+摆臂动画（抛药水动作），滞留型→目标旁边生成 AreaEffectCloud（半径 3，setPotion+addEffect，持续到效果结束），其他形态→直接给目标施加所有效果；CD/气泡/日志照常
- 【删除】不需要再投掷实体：去掉 HomingPotionMixin/PotionHitFallbackMixin（历次方案均被实测否决：追踪乱飞/抛物线扔歪/落地不生效）

## 实测二百四十九（药水扔的路径仍然容易出错）

- 【根因】旧版 0.5 速度+inaccuracy 1.0：速度太慢（半秒只飞 5 格，远目标飞一半就消失）、随机散布（每次扔的方向随机偏转）
- 【修复】弹道精确瞄准——速度 1.0（射程 20 格，半秒飞 10 格）+ inaccuracy 0 + 弹道公式解仰角（2v²/g·len 二次方程），药水精确飞向目标位置，抛物线弧度自然

## 实测二百四十八（近距离时女仆一个劲往地上扔药水、玩家没吃到效果）

- 【根因】近距离（1~2格）初速 dx/len*0.5 极小，药水几乎原地落下→落地碎裂（onHitBlock）→原版落地不施加效果且实体移除→半秒超时强制生效永远等不到（实体已没了）
- 【修复】①近距（≤3格）不投掷，直接强制施加药水效果（与半秒超时同款逻辑）；②新增 PotionHitFallbackMixin：药水命中（含落地碎裂）时若带 homing 标签，强制给目标施加效果（原版落地溅射只覆盖 4 格内实体，目标可能不在溅射范围），施加后移除标签防重复

## 实测二百四十七（挖掘一定数量后不再搭方块（老存档可以新存档不行））

- 【根因】二百四十修复有遗漏：pillarUpStep 真实头顶检查（headPos/headPos+1）漏了**目标木材排除**——女仆搭了几块柱子后站在柱顶，目标木材就在头顶 1~2 格（headPos+1 恰好是目标木材）→ suffocates 拦死后续搭高
- 【修复】目标木材所在格排除（搭高就是为了够它），与 place+1/place+2 同口径

## 实测二百四十六（部署错版本根因确认 + 药水飞行逻辑重做）

- 【根因确认】游戏实际运行在 1.20.1-Forge_47.4.21（PCL2 启动实证），其 mods 里是旧 jar promaid-1.1.0(1).jar（4,780,756=实测二百三十九版），所有新修复（宠物保护加强/药水改动）都部署到了 47.4.23、游戏根本没加载——这就是“宠物保护没用”的真正原因。部署脚本改指向 47.4.21 并删除旧文件
- 【药水飞行重做】问题：过于鬼畜，改为和玩家一样向目标方向以抛物线方式抛出，强制半秒后消失并强制给予效果，不管先前是否命中：mixin 去掉每 tick 追踪修正（纯抛物线），超时从 20 tick 改 10 tick（半秒），两处投掷初速改玩家同款（0.5 速度+0.2 抬升）

## 实测二百四十五（宠物保护仍然没做好（命名铁傀儡女仆仍能伤到））

- 【排查】@Mod modid 与 EventBusSubscriber 一致（注册正常），m_5446_ 字节码实证含自定义名（理论上应命中），但 promaid.log 零 pet-hate-clear 记录 = 判定从未命中
- 【修复】①名字判定双保险——m_5446_（getDisplayName）之外再直接查 m_7770_（getCustomName）与 m_7755_（getName），任一命中即标记（防显示名被队伍/其他模组改写漏判）；②新增 pet-miss 诊断日志——伤害了名字含宠物字样的目标但判定未命中时，记录 display/custom/name 三个名字原文（60 秒限频），若仍不命中可凭日志精确定位

## 实测二百四十四（修回药水追踪）

- 【实现】恢复每 tick 修正速度方向锁定目标（追踪弹，速度 1.05 格/tick 低于穿箱阈值，距离<2.5 直接贴脸），保留出生 tick 记录（maid_smart_born），存活超过 20 tick（1 秒）强制给目标施加药水效果并 discard 药水（效果必达，药水最多飞 1 秒），目标消失直接 discard

## 实测二百四十三（药水追踪乱飞 → 改纯抛物线+一秒强制生效）

- 【实现】重写 HomingPotionMixin：①不再每 tick 修正速度方向（旧版目标移动时药水来回甩动乱飞），投掷初速就是抛物线自然下落；②出生时记录 tick（maid_smart_born），存活超过 20 tick（1 秒）强制给目标施加药水效果并 discard 药水——不管飞没飞到效果必达；③目标消失/死亡直接 discard；1 秒内自然命中走原版 onHit 不重复；两处投掷同步打 born 标签

## 实测二百四十二（女仆互助传递喝药水 + 战斗时传递食物）

- 【实现】①饮用型药水直接喂姐妹：旧版互助链只投喷溅/滞留型，普通药水从不传递（姐妹链完全没有）；新增 feedDrinkablePotionTo——背包找普通 PotionItem→extract→item.m_5922_（finishUsingItem）对姐妹强制饮用（效果即时生效）→空玻璃瓶返还喂食者背包→同种药水 CD；挂在低血链再生之后、喂食之前；②战斗时传递食物：新增 sisterFighting（脑内 ATTACK_TARGET 记忆存在=战斗中），姐妹正在战斗且血不满时喂食（战斗续航，低血链已喂则跳过）

## 实测二百四十一（女仆有时会用木头烧木头）

- 【根因】extractBestFuel 按数量最多选燃料：原木/木板/树苗既是燃料又是可烧制原料，伐木女仆背包原木数量碾压煤炭→原木进燃料槽、原木又进原料槽=用木头烧木头
- 【修复】①评分=燃烧时长优先（getFuel 映射，煤炭 1600 tick ≫ 原木 300 tick），同长再按数量；②纯燃料优先：可燃烧且无熔炉配方（煤炭/木炭/烈焰棒/干海带块/熔岩桶）先选，背包没有纯燃料才退而选可烧制燃料（原木烧原木总比炉子熄火好）

## 实测二百四十（伐木不会搭方块够上面的树木）

- 【根因】pillarUpStep 防窒息检查用 isAir：伐木场景头顶正上方就是目标木材（树干）或树冠树叶，都不是空气→搭高永远失败→女仆原地等待到超时弃置，从不垫方块
- 【修复】①放置格上方 2 格改 suffocates 判定（isSuffocating=false 的树叶/空气不拦，只拦真会闷住她的实心满块），目标木材所在格排除（垫块后头短暂插进目标木材属预期，下一 tick 挖掘入口立即挖掉）；②真实头顶检查同改 suffocates（树冠场景头顶就是树叶，旧 isAir 把树叶当堵头拦死搭高）；新增 suffocates 辅助方法

## 实测二百三十九

- 【应答·为什么最初没直接白名单】通用路径（物品→方块→方块自身 getLightEmission）理论上也覆盖火把系（StandingAndWallBlockItem 也是 BlockItem 子类），语义更"万全"；您实测证明**这条路径对特殊方块不成立**——白名单是这类"官方不按常理出牌"的最稳解，采纳
- 【实现·白名单优先】`LIGHT_WHITELIST`（注册名 → 光强，静态表）：torch 14 / soul_torch 10 / redstone_torch 7 / lantern 15 / soul_lantern 10 / campfire 15 / soul_campfire 10 / jack_o_lantern 15 / glowstone 15 / sea_lantern 15 / shroomlight 15 / magma_block 3——判定顺序改为【白名单命中→直接用登记亮度】，未命中才走通用 BlockItem 光强（灯笼/萤石等已验证路径原样保留）；登记一个不在表内的光源后其他发光方块依旧自动生效
- 测试：重启 → 主手拿火把 → `held light: level=14` + 地面/墙壁照亮；灵魂火把 10、红石火把 7、灯笼 15；面板开关可随时关闭

## 实测二百三十八（玩家问"光源问题呢"——持光功能验证化）

- 【现状说明】持光的两大死因已在二百三十五修掉：①注册时机（alive 日志 12:40 实证驱动活着）；②光块懒解析（类加载期注册表未冻结的 null 陷阱）。上一会话（12:56）驱动 alive 、无任何报错——但持光成功时**零日志**，无法确认到底亮没亮
- 【本次·探针日志】①每个女仆**首次点亮**落一条：`held light: maid=… level=N pos=… (first lit)`（一次/女仆）——亮没亮、亮度多少一目了然；②若 `minecraft:light` 光块解析不到，落 ERROR（`held light: light block unavailable`）
- 【连带】本次同样包含二百三十七的种树修复（jar 4,780,051→4,780,331，含两者）
- 测试：重启 → 女仆主手拿火把/灯笼/红石火把 → 搜 latest.log：`held light: … level=14`（火把）/`level=10`（灵魂火把）/`level=7`（红石火把）/`level=15`（灯笼；她周围地面/墙壁被照亮）；不拿光源 → 无该行、光块自动撤除

## 实测二百三十七（终局：种树不生效的全部真相——ClassCastException 每次扫描都炸，被静默吞掉）

- 【日志铁证】236 的 error 日志立刻抓到：
  ```
  java.lang.ClassCastException: class BlockItem cannot be cast to class ItemNameBlockItem
      at MaidPlanting.tick(MaidPlanting.java:230)
  ```
- 【根因】`#minecraft:saplings` 物品标签（参考 maid_useful_task 抄来的口径）除了常规树苗（ItemNameBlockItem）外还涵盖 **mangrove_propagule 等普通 BlockItem 子类**（模组树苗也可能是普通 BlockItem）——`isSaplingItem` 的物品标签分支不验类型把它放行，最后一步"(ItemNameBlockItem) 强转取方块"当场抛出 → 每次扫描（每 2 秒）都在这里崩溃、被 catch 静默 → **"驱动活着、认得到伐木任务、却一行 plant scan 都不出"** 的完整闭环
- 【修复】①`isSaplingItem` 物品标签分支加 BlockItem 类型校验（非方块物品标签命中不算树苗）；②种植取方块强转 `ItemNameBlockItem` → **`BlockItem`**（两者都是其子类，propagule/模组苗都能种）
- 【连带】此异常之前也破坏了"她包里到底有没有苗"的计数/搜索——修复后一切正常化
- 测试：退出游戏 → 部署 4,780,051 → 重启 → 给她手里/包里放任意树苗（云杉/红树幼苗均可）→ 应出现 `plant scan … result=planted` + `plant sapling`

## 实测二百三十六（235 重启复测：驱动活着且认到伐木任务，但 plant scan 仍零输出 → 诊断终极化）

- 【进展】235 修好注册后，日志实证：`plant driver: alive` + `held-light driver: alive` + `plant driver: sees woodcut maid … task=maid_smart:woodcut` ——驱动在跑、认到了伐木任务，但 `plant scan` 依然零输出 = **tick() 内部某处异常被静默吞掉**（235 的 catch 全是 `Exception ignored`）
- 【诊断升级】①`plant scan` 去掉 20 秒节流——每次扫描（每 2 秒）都落账；②驱动 per-maid catch 与整轮 catch、tick 的 catch 全部从"静默"改为 `LOGGER.error`（带堆栈）——任何异常直接现形
- 【护栏实证】本次部署时游戏正运行 → deploy69 **按设计拒绝**（"Minecraft/Java 进程正在运行（1 个）"），未覆盖运行中的 jar——混态崩溃路径已被堵死
- 待办：退出游戏 → 部署 4,780,029 → 重启 → 搜 `plant scan`（必有结果行）+ 任何 `plant driver error`/`plant tick error` 堆栈——下一步按日志定向

## 实测二百三十五（玩家复测：种树仍不生效；火把/灵魂火把/红石火把持在手中不发光 → 定位总根因）

- 【日志实证】12:00 会话伐木行为正常跑（wood behavior start 12:01/12:03、stuck-reset 12:02）但 `plant scan` 零输出、持光也无效果——两个自监听 ServerTick 的驱动（种树/持光）**根本没被触发**
- 【根因①·注册时机】两个模块此前在 ProMaidExtension 构造器里 ensureRegistered——但扩展由 TLM 扫描实例化、时机不可靠（"constructed"日志其实是女仆脑重建触发，并非扩展初始化铁证）→ 驱动可能从未挂上总线。修复：注册移到 **@Mod 构造器（ProMaidMod()，每次加载必然恰好执行一次）**，扩展侧保留（幂等）
- 【根因②·光块静态取值为 null】MaidHeldLight 的 `minecraft:light` 方块在【类加载期】查注册表——那时注册表未冻结，恒为 null → 整个持光模块早退（火把确实不会亮；火把物品本身是 BlockItem 子类 StandingAndWallBlockItem，判定没问题，纯被驱动死亡连累）。修复：**懒解析**（首次 tick 再取并缓存）
- 【加固】伐木行为重新挂回"每 20 tick 调一次 MaidPlanting.tick"的兜底钩子（任务级驱动 + 行为钩子双驱动，模块内部 5 秒冷却天然去重）——行为窗口内必然有机会
- 【诊断】两个驱动首次点火各落一条日志：`plant driver: alive` / `held-light driver: alive`；种树驱动见到伐木女仆再落 `plant driver: sees woodcut maid`——下次测试一眼确认"驱动到底有没有在跑/有没有认到伐木任务"
- 测试：重启 → 搜 latest.log：应有 `plant driver: alive` + `held-light driver: alive`；伐木女仆在场时应有 `sees woodcut maid`；给她手里/包里放云杉苗 → `plant scan`（含 result/bagSaplings/handSaplings/spots）+ `plant sapling`；主手拿火把/灯笼 → 脚下方块被照亮（火把 14/灯笼 15）

## 实测二百三十四

- 【诚实说明·机制不存在】javap 字节码实证：1.20.1 原版【没有】实体发光机制（SnowGolem 无任何亮度方法、Entity 无 getLightLevelDependentMagicValue、LightEngine 无实体分支——"雪傀儡戴南瓜灯发光"是基岩版特性）；TLM 本体亦无此工程
- 【实现·真实方块光】新建 `MaidHeldLight`（独立模块，自监听 ServerTick）：每 0.5 秒检测所有女仆的主/副手——持 BlockItem 时读取该方块自己的 getLightEmission（m_60739_，与光照引擎同源）：火把 14 / 灯笼 15 / 萤石 15 / 菌光体 15 / 海晶灯 15 / 灵魂火把 10 / 灵魂灯笼 10 / 岩浆块 3 / 模组发光方块自动同值。有光：在她脚底格放**隐藏的 minecraft:light 光块**（1.20.1 上唯一可靠动态光；不可见、无碰撞、可含水），0.5 秒节拍下位置变动即搬移（旧位仍是光块才撤——玩家自己放的光块绝不误删）；无光/离线/退服自动撤除
- 【不影响插火把】插火把读世界真实方块光——本光块与炉子/篝火等环境光源完全同等待遇（插火把逻辑零改动；亮处不插 = 与其"只在暗处插"意图一致，不浪费）
- 【配置】杂项节新增「手持光源发实光」开关（misc.heldLightMaid，默认开）+ 面板行
- 测试：重启 → 女仆主手拿火把 → 她周围方块被照亮（贴墙/洞穴可见光斑，亮度=火把 14）；拿灯笼/萤石更亮；双手拿工具类无光 → 熄灭；面板可关

## 实测二百三十三（玩家提供参考 jar：[女仆实用任务] 1.20.1-maid_useful_task-1.4.2，参考其种树逻辑）

- 【参考解码】javap 反编译 maid_useful_task 的 MaidTreeTask：它实现了 TLM 原生 IMaidBlockPlaceTask——**种树是任务级**：只要女仆选着伐木任务，TLM 原生放置机就持续扫描/行走/放置（shouldPlacePos 的判定 = 目标格下方是 #minecraft:dirt + 格内空气 + 周围 5×5 校验；物品用 #minecraft:saplings 物品标签）
- 【对照出的真根因】我们的种树检查挂在伐木【行为 tick】里——行为只在"实际砍树窗口"运行（日志实证每 20~30 秒启停一次），窗口外检查根本不跑：玩家自检"超平坦地表草方块（判定链条覆盖）+ 包里确实有苗"却一直不种，唯一合理解释就是**检查没挨到（行为窗口外）**——与"判定错误"无关
- 【修复·任务级驱动】MaidPlanting 改为自监听 ServerTickEvent（每 2 秒扫全部加载女仆，任务 UID == maid_smart:woodcut 才检查——触发语义 = 伐木模式，与行为运行窗口完全解耦）；伐木行为里的旧调起点与字段移除（单驱动源）
- 【物品判定升级】优先 #minecraft:saplings **物品标签**（参考模组同款，云杉/橡/桦等原版全树种 + 登记标签的模组苗自动兼容），ItemNameBlockItem+方块标签保留为兜底
- 测试：重启 → 女仆选伐木任务（无论她在不在砍）→ 手里/包里放云杉苗 → 2~5 秒内 latest.log 应出现 `plant scan`（result/bagSaplings/handSaplings/spots）+ `plant sapling`；之前的挂靠窗口限制（行为停止期不检查）已消除

## 实测二百三十二（玩家选句"拾苗高度放宽到 -6..+12"要求改回）

- 随手种树的"拾身边树苗"垂直范围改回 ±6（与水平同半径），撤销二百三十的 -6..+12 放宽（原意是够头顶树冠上的苗——按判定口径不作此放宽）
- 其余（主副手识别/自己格允许/plant scan 诊断）不变
- 测试：重启后拾苗行为与二百三十一前一致

## 实测二百三十一

- 【审计+修复·主副手识别】全工程排查"只扫背包、漏手部栏"的同类死角（与种树同根因——女仆的手在独立手部栏，getMaidInv 看不到）：
  - 种树（MaidPlanting）：主手→副手→背包 （二百三十已修）
  - **垫脚方块（MaidBuildBlockFilter 共享过滤器，挖矿/伐木/搭路三处一次性受益）**：计数与扣取均含手部栏，手里拿的方块优先入垫——手拿圆石也能搭高/铺桥
  - **插火把（MaidTorchPlacerBehavior）**：背包没有则用主/副手的火把（消耗对应格）
  - 其余自动装备/武器/喂食链本就手部感知（MaidToolAutoEquip/喂食走手部取物），无需改动
- 【种树诊断升级】每次冷却跳表都落完整状态（20 秒限频/女仆）：`plant scan: result=… bagSaplings=N handSaplings=N spots=… pos=…`——result 可取 no-sapling（有苗判定失败）/ no-spot / planted；一次日志同时回答"有没有挨到 tick、有多少苗、有没有土块"
- 【环境认知修正】超平坦 y=-60 = 地表草方块（脚下 #minecraft:dirt 或 grass_block 的判定本来就覆盖）——之前"洞穴深板岩"推断作废；若仍 no-spot，日志 spots=0 会证明是判定链条被别的因素挡住
- 测试：重启 → 伐木女仆：①手里/包里放云杉苗，等 5~10 秒看 latest.log `plant scan`（应有 bagSaplings>0、spots≥1、result=planted）+ `plant sapling`；②若两行都没有 = 本 tick 没挨到（发我"是否有 wood behavior start"与女仆任务状态）；③垫块/火把顺手验证：手拿圆石让她搭高应成功

## 实测二百三十（玩家两问：①是否识别所有可种地块（只认泥土没认草方块？）②有没有识别所有树苗——女仆当时手里拿的是云杉树苗 → 仍不种）

- 【回答①·地块判定】已覆盖：脚下 = #minecraft:dirt 标签（泥土/粗泥/灰化土/缠根土/泥巴/菌丝/苔藓，1.20.1 该标签的全部成员）**或 草方块**（草方块不在 dirt 标签里，是显式或条件）；石头/深板岩/沙**有意不认**——那上面树苗长不了（云杉/橡树等只长在泥/草上）。若你的洞穴树底是深板岩，日志会显示 reason=no-spot
- 【回答②·树苗判定+真 bug】云杉树苗本身识别没问题（ItemNameBlockItem + #minecraft:saplings，原版全部树种都在标签内）——**真 bug 是扫描只查背包（getMaidInv），漏了主手/副手**：女仆"手中拿的云杉树苗"被当成"没苗"。修复：主手 → 副手 → 背包 依次取用，消耗也走对应格（手部栏 extractItem 同款）
- 【附两处环境修复】①允许种在【女仆自己站的格子】（树洞/1×1 树桩窝里唯一有泥土的下方格就是她脚下——旧版排除它，洞穴场景永远找不到种点；种下后该格变非空气，后续扫描自动跳过，不会原地重复）②身边拾苗垂直范围放宽到 -6..+12（腐烂/掉落的苗常落在头顶树冠上，旧 ±6 够不着）
- 【诊断】失败原因每 60 秒/女仆落一条（latest.log 搜 `plant scan`：no-sapling / no-spot + 当前位置）——"不种"从此可定位是"没苗"还是"没土块"
- 测试：重启 → 伐木女仆：①给她手里放云杉苗 → 应优先拿手里的种（种子源=hand，日志 `plant sapling`）②地上/树冠的苗可被捡起③失败时看 `plant scan` 决定下一步

## 实测二百二十九

- 新增伐木面板「随手种树」开关（`wood.plantSaplingEnabled`，默认**开**，位于「补种树苗冷却」上方）：关闭 = 女仆只砍树不种树（树苗留在背包/地上）；开启（默认）= 现有行为（手上有苗+附近 6 格有可种土块 → 随手种一棵，触发=伐木模式）
- 实现：MaidPlanting.tick 顶部总开关门 + 配置注释/面板行同步（与冷却行同区，语义配对）
- 测试：面板「伐木 → 随手种树」取消勾选 → 重启（或即时生效）后女仆砍树不再种；勾回恢复

## 实测二百二十八

- 【架构】种树从伐木行为【完全剥离】成独立模块 `MaidPlanting`（新文件，自有冷却表；不读伐木目标/状态）——伐木行为只负责"每 20 tick 调起一次"（触发 = 伐木模式），两码事互不干扰
- 【行为】冷却（默认 100 tick ≈ 5 秒）跳表后：①背包有树苗（#minecraft:saplings，原版+模组都认）→ 直接用；没有 → 先扫身边（半径 6）树苗掉落物捡进背包（伐木中拾取任务让位，落地苗捡不到）；②身边（半径 6 格、垂直 ±2）找【可种土块】：空气格 + 脚下 #minecraft:dirt/草方块 + 格内无存活实体 + 不是她自己站的那格——取最近一处；③种下（音效粒子 levelEvent 2001 + 摆臂 + extractItem 消耗 1 个）并进冷却；找不到苗/土块只记冷却重试（不播报防刷屏）
- 【配置】新增 `wood.plantSaplingCooldown`（默认 100 tick，20~600 可调）——伐木面板「补种树苗冷却（tick）」行，tooltip 说明作用
- 【联动】树叶掉落物中的【树苗仍然永远直进背包】（二百二十六的供应修复保留）——随手种树的苗源；伐木内部的旧补种/树桩登记/地面拾苗全部移除（回归纯砍树+清叶）
- 【清理】冷却表挂在独立模块并与伐木的 per-maid 清理链打通（forget/purge 同款，不泄漏）
- 测试：重启 → 伐木女仆砍树：①有苗时每 5 秒左右在她身边空土块随手种一棵（latest.log 搜 `plant sapling`）；②砍树节奏不受影响（种树不打断挖，只在调起点夹缝执行）；③面板「伐木 → 补种树苗冷却」可调 CD

## 实测二百二十七

- 【核对/落地】四组默认值按玩家指定口径（防止联网玩家质疑"文档说的默认与实际不符"）：
  - 【连锁采集，全部默认开】挖矿「连锁采集」=开 （注释默认开）、伐木「连锁砍伐」=开 （注释默认开启）、农场「连锁收获」=开 （注释补注"默认开"）——实测兑现"所有连锁采集默认为开"，无改动仅补声明
  - 【伐木透视】「透视感知（隔墙找木材）」=开 （注释/面板 tooltip/手册均"默认开"）
  - 【挖矿透视】「透视感知（隔墙找矿）」**true→false**：代码默认翻转为关，配置注释/面板 tooltip（"开启（默认）"→"关闭（默认）"）/手册详细介绍（"默认开"→"默认关"）三处同步
  - 【搭路】=关 （默认关，注释/面板/手册一致）
- 说明：只改【默认值】，你现有配置文件里的值一律不动（想用新默认，删 promaid-common.toml 重新生成即可）；面板切换即时生效
- 测试：删配置启动或看面板——挖矿「透视感知」应显示关闭、其余四项按上表；手册挖矿章/面板 tooltip 与默认值一致

## 实测二百二十六

### ① 树苗不补种——日志实证：每次补种全部 `wood sapling skip: reason=no-sapling-in-inventory`（背包从来没苗）
- 【根因】树叶清理（burstNearbyLeaves）的掉落物：`wood.autoCollect`（默认关）开着才进背包，默认【落地】；而伐木期间拾取任务让位，落地树苗基本捡不到——"砍树→掉苗→补种"闭环在自动收集开关上断链（实测五的设计原本就假设苗直接进背包）
- 【修复①】树叶掉的【树苗物品永远直进背包】（其余掉落物仍遵循自动收集开关）：背包任何时候都有"苗"存货，砍完树立刻能补种
- 【修复②】补种时若背包仍无苗（概率掉率低/库存耗尽）→ 先扫描原址附近 XZ 8 格 × Y ±8 格的【地面/树冠树苗掉落物】捡进背包，同 tick 再验背包种下——站立的旧苗也能续上
- 效果：砍树收苗→补种；promaid.log 搜 `wood sapling planted` 应出现（仍无苗时日志 reason 会写明）

### ② 站高几格木头下不动——日志实证：`wood stuck-reset idle=200~256t` 反复触发（看门狗每 8~13 秒重置但循环复现）
- 【根因】approachWood 里"搭高失败→抬头找回目标"有两道缝：
  - ① `firstWoodAbove` 只扫【女仆自己头顶那一列】——她站在树干旁 1~2 格（走位/寻路落点偏一格/从树冠滑落）时头顶列是空气/树叶 → 找不到木材 → 跌回 walkToWoodBase（最近可站格恰是她脚下）→ 原地站桩到 15 秒目标超时再重选 = "站在木头下一动不动"
  - ② 找到目标后（above==t）旧版【无条件原地等待】——她水平偏开 1~2 格时"等"=干等超时循环
- 【修复①】firstWoodAbove 改【5×5 列簇】（离她最近的列优先、列内 dy 1~10）：站在树干旁 1~2 格也能找到头顶木材（找到即挖掘，无需垫块）；"树上高几格"的木头直接逐节挖下来
- 【修复②】目标已确认是头顶木材但水平偏开 >1 格 → 先走到【树干柱底最近可站格】（柱向下的第一个空气+有支撑格：她已挖穿的树洞底或柱下地面），站到正下方后挖掘入口自然接管
- 效果：树旁不再站桩；watchdog 不再为"够得着但没走位"的情况背锅

### 测试
- 重启 → 伐木女仆砍树：①树叶掉苗直接进背包（捡到后下一次砍树完成点补种，latest.log 搜 `wood sapling`）②让她砍一棵被树干旁站立/高几格木头的树——应走过去站到树洞正下方逐节上砍，不再原地发呆（promaid.log 搜 `wood stuck-reset` 不应再刷）

## 实测二百二十五（玩家复测：50 万上限+VBO 全量线框"内存不足渲染不出来了"，确认回退稀疏方式）

- 【回退】二百二十四的"点云上限 500000 + VBO 全区块线框缓存"整体回退到二百二十三的架构（已 git 检出 223 版三个渲染相关文件）：
  - 【原因】金字塔级蓝图 VBO 线框 = 133MB/块（345K 盒 × 12 棱），×LRU 3 块 + 构建期临时 ByteBuffer（133MB）+ 点云/解析 + 每帧上传 → 数百 MB 显存/内存峰值——玩家的机器"渲染不出来"，只能回到稀疏的方式
  - 【保留】二百二十四的【障碍物连锁】（与渲染无关，纯挖矿行为）与多包分片网络（12000 点 ≈170KB 时单包即可，回退后为 223 原版单包格式，同样正常工作）
- 回退后的渲染形态（与二百二十三一致）：点云上限 12000（洗牌+等距抽稀，无竖条纹）；每盒无条件 12 条棱线（lines 单缓冲成批）；近处（32 格内）最近 2400 盒满体积填充——近实心、远线框剪影；无 VBO、无额外显存占用（12000 盒 ≈ 几十 MB 量级）
- 若以后还想"全量+流畅"，正确路线是分片缓存+可见性剔除（Litematica 式区块网格），工程量大——见正式版需求再议；当前以稳定为准
- 测试：重启 → 金字塔「建造此图纸」→ 金色框内青色幽灵全貌（近处实心+远处线框）→ 确认建造后红色区块橙色幽灵同款；promaid.log 搜 `drawGhost` 盒数应 ≈12000、无 vbo 字段

## 实测二百二十四（玩家两项：①挖矿连锁采集对障碍物也生效，有触发上限 ②投影点云上限改 500000，保证所有方块都被渲染到）

### ① 挖矿连锁采集 → 障碍物（"挖一个旁边的障碍物，周围的同类障碍物也生效"）
- 【现状】连锁采集（FTB Ultimine 式同时破坏）只对【矿脉】生效（refillChainQueue 明写"只连锁矿，不连锁石头/泥土"）——挖开挡路的泥土/圆石/石头时，旁边的同类挡路块还需一块块挖
- 【实现】挖完判定分支：挖掉的是矿 → 走原矿脉连锁；挖掉的是【可挖穿障碍物】（isBreakable：内置名单+面板名单，扣除"已取消挖穿"排除项）→ BFS 找相连的同类可挖穿块，同样【同时破坏】（掉落直接进背包、连锁块静默、镐耐久只扣主目标一次、自己搭的方块不连锁、矿不混入障碍物链）
- 【触发上限】共用 mine.chainLimit（默认 16，面板「挖矿 → 连锁上限」可调）；BFS 展开上限 64 防超大土方卡顿；总开关仍为 mine.chainMining（默认开）
- 效果：挖穿土墙 → 一片土连锁塌进背包；挖碎石堆 → 一圈碎石连开，进度肉眼可见
- 日志（promaid.log）：`mine chain filled (blocking): … queued=N` / `mine chain burst (blocking): … broken=N`

### ② 幽灵方块 → 全量渲染（上限 500000）
- 【数据】取样上限 3000→12000→**500000**：包内最大蓝图（载具__BF7 星际航母 外壳 45.6 万、总统山 51.6 万外壳……）全部全量下发——**不再有任何抽稀截断，每个方块都在点云里**；>50 万外壳的自导入巨型蓝图仍按洗牌+等距兜底（保护网络与渲染）
- 【网络】点云上限到 50 万后单包装不下（≈6MB）——服务端改【多包发送】（每包 ≤16000 字符切片 + seq/total），客户端拼齐再应用；旧版 decode 只读 64 块（≈1MB）会截断
- 【渲染】免抽稀后 45.6 万盒的全区块棱线无法每帧即时重建（千万级顶点/帧）——红色区块（锚点固定）线框改【后台线程一次性构建 + 渲染线程上传成 GPU 显存缓存（VertexBuffer）+ 每帧一次 drawWithShader】：一次提交画全区块全部方块棱线，显存缓存按 LRU 上限 3 块（金字塔 ≈133MB/块）；VBO 就绪前与金色预览（锚点随玩家移动、不缓存）走 64 格内最近 6000 盒的即时回退；近处 32 格内最近 2400 盒满体积填充照旧
- 效果：全量点云 + 全量线框 + 近处实心——"所有方块都被渲染到"，金字塔/航母全貌完整
- 性能提示：显存占用随蓝图规模（百万级方块蓝图 ≈133~175MB/块线框缓存，最多 3 块）；帧率敏感可关「建造投影总开关」
- 测试：重启 → 金字塔「建造此图纸」→ 金色框内青色幽灵全貌（回退模式）→ 确认建造后红色区块橙色幽灵：promaid.log 搜 `drawGhost` 应见 `盒数=345xxx 填充=… vbo=true`（VBO 构建约 1~3 秒，期间为回退模式无 vbo 标志）

## 实测二百二十三（玩家复测：橙色幽灵方块只零星复刻大概形状，绝大部分方块没被覆盖——参考《传说之下》金字塔）

- 【日志实证完整链路】`projection: request 金字塔 q=0` → 服务端 `size=251x124x251 chars=170603 (sent)` → 客户端 `received 3000 blocks` → `drawGhost 盒数=3000 远距描边=true`：
  - 【根因①·数据封顶】金字塔蓝图 251×124×251、实际 37.6 万非空气方块、外壳 34.5 万——旧采样上限 3000 点 ≈ 外壳的 0.9%，且每点还附 blockId|state（实测一百四十七起渲染已不用 BlockState，客户端却逐点做 SNBT 解析——白费带宽）；去掉状态串后限流 3000→**12000**（同带宽 4 倍覆盖）
  - 【根因②·扫描序抽稀=竖条纹】抽稀按列表序（x 主序）每第 N 个——密集墙面恰好取成一格一格规则竖条纹（间隔 29 块）——"零星复刻一个大概形状"的直接来源；改为**确定性洗牌（种子=蓝图 id 哈希）+ 等距抽稀** ≈ 空间均匀点阵，剪影完整可辨
  - 【根因③·填充闸全局化】"距区块原点 >24 格整片只描边"——251 格宽的区块玩家站任意位置都算"远"，整片只剩线框；改为**逐盒按与相机 3D 距离判定**：≤32 格内盒全部填充（实心体积跟随玩家），超过 2400 盒取最近 2400（帧内开销封顶）；全部盒照旧无条件 12 条棱线（lines 单缓冲成批）
- 修复后效果：小/中蓝图（≤12000 外壳）全量无截断覆盖；金字塔这类巨图 12000 均匀点阵 + 近处满体积——从内到外、任意位置都有完整形态（远处为均匀线框剪影，不是竖条")
- 性能说明：12000 棱线 + ≤2400 填充/帧，为主流显卡可承受量级；帧率敏感可关「建造投影总开关」
- 测试：重启 → 蓝图书点「建造此图纸」（金字塔随意朝向）→ 走近白色框应显示完整青色幽灵体积，确认后走 20 格内红色区块应见橙色实心体积，远处轮廓完整；promaid.log 搜 `drawGhost` 应见 `盒数=12000 填充=…`

## 实测二百二十二（v1.1.0 beta 发布前自查）

- 搭路节奏默认值真正落地（自查发现二百一十五只改了注释、代码默认值没改）：
  - 【问题】`bridge.stepCooldown` 代码默认仍是 **2 tick/块（≈10 块/秒）**——注释/提交记录/二百一十五条目都写着"默认 4"，但值从未落下去；按 4 tick ≈5 块/秒（只比玩家手速快一档）才是二百一十五玩家原意（"降低默认搭建速度，只比玩家稍微快一点点"）
  - 【修复】默认值 2→**4**（范围 2~40 不变）；顶部遗留的旧注释块（"默认 8→5 tick/块 ≈4 块/秒"）一并清理；与你当前配置目录里的 stepCooldown=4 一致，删配置重新生成也是 4
  - 【连带】伐木搭方块冷却（wood.pillarCooldown）二百一十五已正确落为 4（代码默认=注释=4）；你配置目录里该值目前是 2，想同步可在面板「伐木 → 搭方块冷却」调到 4
- 排班可用性检测注释修正（二百零二"默认=你当前配置"同步后，代码默认已是开，注释仍写"默认关"——虚假宣传自纠）：注释改为"默认开，二百零二同步为你当前配置值"，并补一句排班任务不切换时的排查指引（先关掉本项）
- 无其他不一致：脚本复核关键默认（搭路开关关/最小高差3/平桥启动6/连锁采集开/透视感知开/穿透22/散步0.7/看门狗8/播报10/重力落地雪1×1 等）与注释、面板说明三方一致

## 实测二百二十一

- 部署安全护栏 + 启动崩溃解释（玩家崩溃日志：`NoClassDefFoundError: com/maidsmart/tool/MaidBuildBlockFilter` @ BridgeUpBehavior.hasBuildBlock）：
  - 【解释】不是代码 bug——游戏 07:48 启动**之后**我替换了 mods 里的 jar（UAC 部署成功覆盖）。运行中的 JVM 对已打开的 jar 做懒加载：类在首次使用时才按【旧目录索引】去读文件，jar 被替换后新类按旧偏移读不到 → ClassNotFoundException（类本身就在新 jar 里，已验证：源码/编译产物/部署 jar 三处均在）。重启游戏即恢复
  - 【护栏】deploy69.py 增加运行检测：存在 java/javaw 进程（游戏未关闭）直接拒绝部署（不再弹 UAC 覆盖运行中的 jar）——杜绝此类"半新半旧运行态"崩溃
- 另注：此会话日志 07:48:55「西行寺幽幽子从高处摔了下来」也是混态 jar 的附带现象，重启后不存在
- 测试：完全退出游戏 → 重新启动（当前 jar 4766545 已含全部改动）

## 实测二百二十

- 搭路竖直启动高差默认改为 3 格：
  - BRIDGE_MIN_DY 默认 6 → **3**（范围 1~8 不变）：她与你垂直高度差 ≥3 格才走垂直搭高——3 格 = 玩家手长：她搭到只差 3 格内你就能近身收回/互动，不用再爬着她搭的半空柱子够她
  - 配置注释/面板说明同步（说明"3 = 手长，收回方便"）；只改默认值——你现有配置目录里的 minDy 仍是你当前值，想用新默认删配置重新生成即可
- 测试：你比她高 4~6 格 → 她应开始垂直搭高并在 3 格差内停下等你收回

## 实测二百一十九

- 撤销二百一十七的"Y 轴拉回主人身边威胁检查"（玩家选句要求去掉）：
  - 移除 trySameDimPull 里 yPull 分支的 `anyVisibleMonsterAround`（主人身边 4 格有威胁→不传）检查与 sam-dim-ythreat 日志；`anyVisibleMonsterAround` 恢复为私有（仅自保传送内部使用）
  - Y 轴拉回的判定恢复为：搭太高(垂直超出门槛) + 搭路门豁免 + 唯一落点判定 findStand（主人身边 16 格内可站立格,找不到不传）——主人身边有怪不再拦截拉回
- 测试：搭高的女仆在主人身边有怪时也会正常传回（落在安全站立格）

## 实测二百一十八

- 平桥近距离拦截补强（玩家澄清："我指的是女仆水平方向两个轴（不含高度）距离玩家坐标太近时，不再进行平方向铺方块——这个没能触发"）：
  - 【兜底修复】水平铺块的入口集中在 tryAirBridgeStep（平桥）与 tryDiagStep（斜上台阶）——现在在这两个铺块函数【内部】再各加一道水平距离硬门槛 hDist < 平桥启动距离（默认 6）→ 直接不垫：调用侧（canUse 启动门/平桥腿/斜上腿）早有判断，内部兜底保证任何绕过路径（行为残留帧/触发顺序变化）都在落方块前被拦下——贴近玩家（水平 6 格内）绝不再水平方向垫方块
  - 垂直垫高 placeStep 保持豁免（你只是垂直爬高不受影响——近距离贴着爬塔仍可立柱）
- 测试：你站在沟/悬崖对面 5 格内 → 女仆应在沟边走走路，不再朝你水平垫块；拉远到 6 格以上才恢复铺桥

## 实测二百一十七

- 强制传送补充"主人旁安全地块"判定：
  - 说明：所有强制传送链路（Y 轴拉回/同维度拉回/跨维跟随/一键集合/单独传送按钮/自保传回/救援）本来就走同一个落点判定 findStand——主人身边 16 格内找"脚下有碰撞面 + 身体格无碰撞 + 无流体 + 无危险方块（岩浆/火等）+ 无实体占用"的可站立格，找不到不传
  - 【本次补充】Y 轴拉回在二百一十六放行搭路门后,落点"站得住"之外再加第二道判定,与自保传回同口径:**主人身边 4 格内(teleportSafeRadius)有可见威胁/怪物 → 不传**(落日志 sam-dim-ythreat,威胁清除后下一轮扫描自动再试)——站到主人身边却被怪围就是送死,同样属于"不安全地块"
- 测试:你身边站只僵尸时让女仆搭高 → 应不传(promaid.log 搜 sam-dim-ythreat);怪清掉后下一轮自动传

## 实测二百一十六

- 三项修正：
  - 【Y 轴拉回没生效·根因】实测二百零七 的"搭路中不拉"闸门把一百八十八 的【Y 轴拉回】连带拦掉了——"搭太高"恰恰发生在她自己垫的高柱/桥上，搭路闸门让 Y 轴拉回永远不触发。修复：Y 轴分支（yPull）豁免除搭路闸门——它是把她往主人旁边【已验证的安全落点】传下来，不是拉离，且二百零七②的近距刷新保证她脚下桥块不会立刻回收；水平远距拉回仍保留搭路门
  - 【近距离拦搭路没生效·根因】canUse 的垂直启动门槛写成了 `dy < min(minDy, 4)`——配置 minDy（默认 6）调到大于 4 完全无效，恒按 4 启动："Y 轴方向距离过小就阻拦搭路"形同虚设。修复：改为按配置值 `dy < minDy` 生效（主人至少高 minDy 格才走垂直搭高；默认 6 格）
  - 【合成表】排班表改为 纸 + 墨囊/荧光墨囊 均可合成（新增物品标签 `maid_smart:ink_sacs` 同时收录墨囊与荧光墨囊）
  - 【开局赠送】排班表与《Promaid 手册》同机制：首次进世界一次性把两本书都送到背包（共享"只送一次"标记，登入消息更新）
- 测试：①让女仆搭到 15+ 格高(你自己在旁边安全处)→ 应触发"搭太高传送下来"(promaid.log 搜「Y 轴距离")；②主人比女仆只高 1~5 格时不再垂直垫柱；③工作台 纸+荧光墨囊 → 排班表；④新存档/删标记后重登 → 两本书一起到背包

## 实测二百一十五

- 搭建失足摔死防护：
  - 【默认速度回调】搭路节奏 2 → **4 tick/块（≈5 块/秒，比玩家手速 4~6 块/秒略快一点点；原来 2 tick ≈ 10 块/秒）**；伐木搭方块冷却 2 → 4（与挖矿、搭路统一节奏）；注释同步
  - 【防摔落·潜行式收力】走到刚垫的方块上"到位"的瞬间——旧版只清推力标记，**0.22 的水平速度带着惯性继续滑行约 1.7~2 块**（0.22 × 0.91/(1-0.91)），1 格宽的桥块停不住，直接冲出边缘失足——速度越快越明显。现在两处 walk-on（搭路/挖矿/伐木"垫块踩上去"共用机制）到位即清水平速度（垂直保留，该落照样落到块面上）
  - 【防摔落·潜行式边缘守卫】每 tick 推送前检查目标格：目标格自身无碰撞 + 脚下一格有碰撞面（她搭的方块或地形都算）才推；无支撑（方块被回收/没放上）→ 水平收力原地站住，绝不在断崖边上把自己推下去
- 测试：搭路开 → 让她在悬崖/沟边朝你铺桥 → 应稳定一格一格走，不再冲出桥面；挖矿/伐木垫台阶同款保护

## 实测二百一十四

- 两项修正：
  - 【建造章过时建筑清理】手册建造章原写的"石砖小屋"示例与 7 台旧机器（仙人掌塔农场/超级熔炉组/无限岩浆泉/高效作物农场/自动伐木场/全自动养鸡场/自动熔炉组）来自早期的旧蓝图时代——当前随模组附赠的 81 套蓝图包里已经没有它们。已按实际蓝图包更新：示例改为「三层木制别墅」；机器清单改为当下包内的红石蓝图（中世纪刷石机/全树种树场（有红树）/国风刷铁机——黑金楼/优雅的村民交易所/彩虹信标/心形下界传送门 + 甘蔗牧场（已失效）注明不建议建）；蓝图来源①分类同步为 房屋/地形/天空/树木/武器/水/红石/节日/装饰/载具/雕像
  - 【搭路默认改回关】`bridge.enabled` 默认值 true→false（二百零六 曾按当时配置同步为默认开——按你现在的意愿改回关）；手册三处（被动技能章/面板导航章/这是什么速览）同步改回"默认关"；配置面板 SectionRow 本就是"默认关"无需改
- 测试：重启后配置面板/手册里的"搭路"应显示默认关；手册建造章的示例与机器清单与蓝图包一致

## 实测二百一十三

- 手册「详细介绍」按当前版本全面校对——逐章对照实际代码/配置逐项修正过时声明：
  - 「什么是」速览刷新：补入 v1.1.0 最新功能（模组武器专属模式、排班表单独传送按键、建造投影线框渲染、搭路默认开等）
  - 挖矿/伐木章：「透视感知（隔墙找矿/找木材）」默认关→**默认开**（当前实际默认开启——注释/面板同样修正，之前三处文本都写反了）
  - 杂项章：「挖矿中禁止拾取」默认关→**默认开**
  - 被动技能章 / 面板导航章：「搭路」默认关→**默认开**（二百零六 已按当前配置同步默认开）
  - 战斗章：逃生手段②补充「主人身边没有安全落点也不传」——自保传回有落地检查，不会把她扔半空摔死（二百零五/二百零七 行为）
  - 同步修正配置注释与面板 tooltip 中同样的"默认关/默认开"错误声明（透视感知 ×2 处文本）
- 其余章节（排班/烧制/贴身辅助/战术/武器/记忆/对话/感知/情绪/工具/语音/面板导航/术语/细则）已逐一核对——上一批更新已覆盖，此次无新增过时项

## 实测二百一十二

- 红色区块框挡住幽灵方块修复：
  - 【根因】红色区块框（和金色预览框）各自带一个 0.28/0.3 半透明的【大填充面】（整个区域一个大盒子 6 个面）：从外向内看时这层红面正好卡在相机与幽灵方块之间，后画的红面覆盖/混合掉幽灵——所以"外看是红盒子、内看正常"（内看时红面在相机身后被裁剪，挡不到）
  - 【修复】两块区域框的【大填充面】全部移除，只保留棱线框 + 悬浮标签（区域标识本来就靠它们）——从外向内看：红框轮廓 + 幽灵线框/填充全部穿透可见，不再有红色遮罩
- 测试：站在建造区块外回头看——红框应只是棱线（不再有红色雾面），幽灵方块轮廓清晰；金色预览同款

## 实测二百一十一

- 幽灵方块"未全渲染 + 从外看不到内"修复：
  - 【根因】幽灵只画填充盒（debugFilledBox 半透明面）：① 逐盒独立 flush 后无深度/距离排序，混合按提交顺序——站在区块外时外层盒面先画、内层后画，内层被外层盖糊（"从外看不到内"）；② 填充面受地形遮挡 / 盒面互相覆盖，部分方块在视角下不可见（"没全部渲染"）
  - 【修复】每盒**无条件画 12 条棱线**——走与红框同一的 lines 管线（实测隔地形/任意角度透显；LINES 每段独立原语，无条带连接问题）：所有搭建方块逐一成框，从外看内部结构线框清晰叠显（Litematica 式示意图观感）；**近处（24 格内）再叠加满体积填充**（专用源 + 每盒独立 flush，体积感与近距离清晰度保留）
- 测试：点「建造此图纸」→ 蓝色/橙色幽灵都应是"满框线 + 近处填充"：从区块外任意角度能看到全部方块轮廓与内部结构

## 实测二百一十

- 幽灵方块**二次崩溃**修复（玩家再次崩溃日志：`BufferBuilder not started`，堆栈经 TLM RenderHelper.renderLine → drawBoxEdges → drawGhost）：
  - 【根因】二百零九把"每盒 flush"直接放在了共享的主 bufferSource 上——字节码实证：1.20.1 的 `MultiBufferSource.BufferSource` 对【未入 map 的渲染类型】（debugFilledBox、lines 都在其列）统一使用同一个即时建造器（f_109904_），一次只允许一个活动类型；同一帧里描边（lines）与填充盒（debugFilledBox）在此建造器上互相切换/中途 flush → 本地描边指针指向的建造器已不在"开始"状态 → `BufferBuilder not started` 崩溃（实测二百零四的"Not filled all elements Index:14"是同一根因的另一面貌）
  - 【修复】幽灵填充改用【每帧独立的专用 BufferSource】（GhostBufferSource：子类化构造器，每次 drawGhost 新建，只写 debugFilledBox）：每盒 flush 只影响它自己，主 bufferSource（描边/红框/其他模组的渲染事件）完全隔离——两条崩溃路径都堵死
  - 渲染本体保持 DebugRenderer.renderFilledBox + 每盒 flush（满体积 1×1×1、无跨盒连接面、无距离剔除的视觉效果不变）
- 测试：进游戏 → 点「建造此图纸」→ 蓝色幽灵应正常显示不再崩溃；确认建造后走出区块看橙色幽灵；这次两个崩溃点（手动顶点/共享源 flush）均已按字节码级证据修复

## 实测二百零九

- 幽灵方块渲染崩溃修复（玩家崩溃日志：`BufferBuilder.endVertex` 抛出 "Not filled all elements of the vertex Index: 14"，堆栈直指 BlueprintAreaPreview.ghostQuad）：
  - 【根因】实测二百零四把手写 debugQuads 顶点渲染（逐面 4 顶点 QUADS）替换进幽灵方块——同一台客户机上 BufferBuilder 被 epicfight（MixinBufferBuilder）/ ponder 的 mixin 变换，手写顶点序列的校验不通过，第一次点"建造此图纸"（2469 块点云到达）即端掉整局
  - 【修复】回退到运行时验证过的 `DebugRenderer.renderFilledBox`（m_269311_——实测一百四十七~二百零一长期使用零崩溃；这也是 vanilla 自己的 debug 管线）；同时【每盒后立即 flush】（m_109912_ debugFilledBox）——原先多盒连续提交同一条 TRIANGLE_STRIP 会在盒间产生跨盒连接三角形（"太小/出区块消失/蓝影看不见"观感的来源），flush 后每个盒子独立成条：满体积 1×1×1、无跨盒连接面、无距离剔除，区块内外都能看到清晰幽灵方块
- 测试：进游戏 → 点「建造此图纸」→ 蓝色幽灵应正常显示、不再崩溃；确认建造后走出区块看橙色幽灵

## 实测二百零八

- 排班表新增**单独传送按键**：
  - 女仆详情页 → 第 1 页·快捷设置 新增「传送到我身边」——把**这一只**女仆传到你身边（不用再靠全员一键集合；跨维度/远处都行）
  - 与列表页「一键集合」同口径：她坐着/骑乘/在家模式（排班守家中）保持原位——服务端回复原因（"想强制召回先关闭排班/解除坐姿"）；你身边 16 格内无可站立点（高空/虚空）则不传并提示；她不在已加载区块就提示改用一键集合（那有强载区块自动召回）
  - 实现：新网络包 `MaidSummonPacket`（id 10）+ `MaidChunkLoadManager.summonOne`（跨维度搜索、人工意图优先）
- 测试：排班表 → 点女仆 → 第 1 页 → 「传送到我身边」；传成功的到她脚边，坐/家/高空的看聊天栏原因

## 实测二百零七

- 高空摔死真正根因（玩家质疑："当时女仆完全就不处于自保模式啊，为什么也会摔死呢？"——质疑正确，二百零五修错了对象）：
  - 【日志还原完整事故链】2026-08-31 05:11:28~05:12:01（你飞去高空，女仆 K螺诺亚 搭桥追你）：
    - 05:11:53 `bridge-up start owner-dy=19`——她正**搭路中**（站在自己铺的半空桥上）
    - 05:11:54 **我们自己的同维度 Y 轴拉回把她传送了**——"干活中不拉"的门**没有覆盖搭路状态**：`isTaskOccupied` 判的是挖矿/伐木/建造/站桩，搭路行为（BridgeUpBehavior）不在其列 → 拉回把站在半空桥上的她硬拉走
    - 05:11:59 她贴上主人（bridge-up stop reached），与此同时窒息/溺水自救把她顶出桥面——**她离开桥的瞬间，"3 秒后回收"的倒计时开始走**
    - 05:12:01 脚下的桥块按期消失 → 从约 25 格高处坠落 → "K螺诺亚从高处摔了下来"（落地 y=-60）
  - 【修复 ①：拉回不打搭路】trySameDimPull 新增搭路闸——`maid_smart_bridging` 标记（搭路行为活跃）一律不拉，落日志 `sam-dim-bridging`；搭路行为自己会铺到主人脚边，不需要传送兜底，拉走只会抽掉她脚下的桥
  - 【修复 ②：搭块回收防"刚离开就塌"】PlacedBlockTracker 的"站上面才延后"太窄——被顶出/上浮/传送扰动离开的刹那她很可能还依赖脚下那块；现在绑定女仆在块水平 ≤4 格、垂直差 ≤6 格内一律刷新寿命，真正走远/高度差拉开才回收（日志里的"离开 2 秒塌"窗口被封死）
  - 【备注】二百零五（自保传回安全落点）仍是正确的加固——那是另一条"直传主人坐标"的路；本次按日志修正为真正的死因

## 实测二百零六

- 默认参数重新同步 + 虚假宣传清理：
  - 【同步 9 处】代码默认值改为你当前配置的实值（脚本逐键对比 toml vs 代码，DIFF 归零）：
    - 搭路开关 默认关→默认开（你当前配置开着搭路）；挖矿：连锁采集 默认开、垂直向下搜索 4→12、深度惩罚 0→3、移动速度 0.3→0.6、搭方块冷却 2→4、搭方块清理 3→10 秒；伐木：搭方块清理 3→10 秒；散步速度倍率 1.0→0.7（你举例的这一条：注释写 0.7 实际 1.0——现在注释与默认值一致了）
  - 【清理 14 处虚假声明】解释字段里"默认 X"与实际默认值不符的（注释 12 处 + 面板提示 6 处中 4 处重复、2 处为 50 万/100 万单位误报）：
    - 搭路：触发距离 默认 7→32、空中搭桥 128→50、最小高差 2→6、最小球面半径 2→3、平桥启动距离 5→6、节奏 5→2 tick（相应"≈4块/秒"叙述更新）、清理时间 2→3 秒（联动叙述更新）
    - 挖矿：穿透预算 默认 6→22（并更新"22=无限穿墙"的旧叙述）；伐木：看门狗判定时长 默认 5→8；对话：工作播报间隔 默认 20→10
    - 连锁采集注释原写"v1.5.189 默认开启"但实际代码为 false——一并改为与同步后一致的"默认开"
  - 脚本复核：287 个标量键 toml 与代码默认值 0 差异；注释/面板"默认"声明全部与实际一致（含"50 万/100 万"单位换算核对）
- 测试：删除 `版本目录\config\promaid-common.toml` 再启动——生成的配置就是你现在的这套参数；手册→模组详细配置各条目说明与默认值一致

## 实测二百零五

- 女仆高空摔死修复：
  - 【根因】自保系统的两处"传送回主人身边"（自保中无怪时 `teleportHome` / 自保结束退出时 `teleportHomeOnExit`）是 `m_6034_(主人x, 主人y, 主人z)`——**直传主人的实时坐标，从不检查落脚点**。主人飞行/悬空（创造飞行、鞘翅、站平台边缘）时女仆被直接扔进半空 → 高空坠落摔死（有落地水+水桶才侥幸不死）。同维度拉回/跨维度跟随/Boss 拉链里的"安全落点检查"它一个都没走
  - 【修复】两处传送接入与拉回/跨维跟随**同一个安全落点判定**（findStand：主人格下 16/上 12 格 + 水平环 r≤3 内寻找"脚下的碰撞面 + 身体格空气 + 无流体 + 无危险 + 无占用"的可站立格）：找到落点 → 传到落点（不再传半空）；**找不到（主人悬空/虚空边缘）→ 本次不传**——宁可她走回来或等落地后由跟随/拉回链路归队，绝不冒险。传送改用 `teleportTo`（m_264318_，跨维度也安全）
  - 附带日志：拒绝传送时落 `self preserve teleport skipped (no stand near owner)`（latest.log）——之后"女仆没传回来"也有可查的明确原因
- 测试：创造飞行升到 60+ 格 → 让女仆被打进自保（或血回满退出）→ 她应停在原地不再瞬移；地面落地后跟随链路自行归队

## 实测二百零四

- 幽灵方块渲染根治：
  - 【根因】幽灵方块渲染用的是 DebugRenderer.renderFilledBox——字节码实证它走 `RenderType.debugFilledBox`（POSITION_COLOR + **TRIANGLE_STRIP** 三角形条带）：两百多个幽灵盒（大蓝图可达 2600 盒）按 24 顶点/盒连续提交进**同一条条带**，盒与盒之间自动补出跨盒连接三角形——整片幽灵变成"乱麻三角网"：单盒面被搅碎（看小）、走出区块后再看不到成型方块（连接面吃掉内侧面）、金色预览的蓝色幽灵直接看不清
  - 【修复】改走 `RenderType.debugQuads`（官方专为彩色半透明调试盒做的管线，POSITION_COLOR + QUADS）：每个盒子 6 面逐面独立提交 4 顶点——每盒**精确 1×1×1 满体积**（和真实方块一样大）、无跨盒连接面、双面不剔除、无任何距离剔除——红色区块内/外、远近都能看到完整幽灵方块；面透明度 0.45→0.55（橙）/ 0.40→0.55（青），远处仍补棱线描边
  - 附带诊断：每帧实际提交的幽灵盒数变化时落一条日志（promaid.log 搜 `drawGhost`）——若"还是看不到"，日志能直接区分"没进渲染层"还是"渲染了但客户端看不出"
- 测试：进游戏 → 点「建造此图纸」看金色框内青色幽灵（应清晰、与方块同大小）→ 确认建造后走出红色区块回头看橙色幽灵（应完整可见）→ 如仍异常把 latest.log 搜 `drawGhost` 和 `projection` 的两行发我

## 实测二百零三

- 手册「详细介绍」补写 v1.1.0 新功能使用方法：
  - 战斗与自保章：宠物免伤怎么用（命名牌改「玩家宠物/主人的宠物」、主人驯服的猫狗马等自动识别、模组宠物标记）、带保命道具（绀珠之药/不死图腾）低血不会逃跑、拿模组武器必定切它的专属战斗模式
  - 被动技能章：搭路新启动规则（有水平距离 + 前方脚下悬空就朝你铺方块踩上去；「平桥启动距离」默认 6 格可调；挖矿/伐木垫块也会踩上去不再卡住）
  - 农场与杂项章：回家与传送（跨维度跟随 / 同维度 48 格拉回 / 垂直 16 格拉回，主人旁有安全落点才传，排班与守家不响应）
  - 排班表章：排班活动半径默认 32 格（可调）、空闲散步参数（间隔/半径/速度在杂项调）；排班锁定后原版界面改任务会当场弹回
  - 烧制与酿造章：什么能烧就烧什么（矿石/沙子/原木/模组食材）、多只女仆各占一个炉子不挤人
  - 武器与工具章：剪刀任务自动从背包拿剪刀（带 forge:shears 标签的模组剪刀也认）
  - 对话章：击杀邀功气泡默认关（想听在「对话提示」打开「击杀邀功对话」）
  - 建造章：站在红色区块外也能看见橙色幽灵方块轮廓（距离越远越淡）

## 实测二百零二

- 当前配置同步为默认参数：
  - 从你当前游戏的配置（`版本目录\config\promaid-common.toml`，最近一次会话实值）同步 **40 处参数**写入代码默认值（建造速度 x1.5、极速模式开、建造上限 200、各类范围/倍率/开关等按你当前实值；10 个列表型配置保持内置默认）
  - 同步后：删掉这份配置文件再启动，生成的配置就是你现在的这套参数；不删的话现有配置继续生效，两者已一致
  - 测试：详见手册 → 模组详细配置面板各参数（默认值应等于你当前配置）

## v1.1.0 本次大更新（对照 v1.0.4）

**新增功能**
- **排班表（新物品）**：给女仆排日程——几点到几点做什么任务、什么作息，到时间自动切换；排班中的女仆活动范围更大、空闲时会自己散步，不会站桩发呆
- **自主战斗**：身边有威胁自动参战、打完自动回到原来的活；拿着模组武器（拔刀剑、史诗战斗等）会自动用它们专属的战斗方式
- **搭路**：你和女仆之间隔着沟/悬崖/高空时，她会朝你铺方块一路搭过来；挖矿、伐木垫台阶时也会踩上刚垫的方块
- **宠物免伤**：写着「玩家宠物」标签的生物、你养的猫狗等宠物——女仆不会攻击它们，溅射也不会误伤
- **烧制升级**：熔炉什么能烧就烧什么（矿石/沙子/原木/模组食材都可以）；多个女仆会各用各的炉子
- **回家与传送**：排班/守家中的女仆不会被跨维度传送拉走；搭得离你太高会自动传回你脚下（有安全落点才传）
- **界面细节**：建造确认时提示「按 Z 键旋转方向」、手册翻页/按钮重叠修复、排班表和手册换了新图标、击杀邀功气泡默认静音（需要可在设置里开回）

**修好的关键问题**
- 战斗结束后"还原回原任务"现在真的会生效（这个 bug 之前反复修了很久）
- 熔炉"第二只女仆只加燃料不烧东西"、搭路"只搭一块就停"、挖矿伐木"垫了块却踩不上去"、女仆气泡显示 `bubble.call...` 代码、建造投影走出区块看不见——都已修复
- 一次启动崩溃（宠物免伤功能的内部问题）已修复，游戏现在能正常进

**关于既有配置**：v1.0.4 的全部配置数值一个没改；新功能新增的设置项都给了合适的默认值（手册 → 模组详细配置里可调）。

---

**最近一次次版本的逐项记录**（编号是修复批次，倒序）：

- **二百零一**：建造投影——站在红色区块外也能清楚看到橙色建筑轮廓了（之前太淡几乎看不见）
- **二百 / 一百九十九**：搭路——离你 5 格以内时不再朝你铺方块（改为走路靠近）；这个距离可在「被动技能」里改
- **一百九十八**：女仆头顶气泡偶尔显示 `bubble.call...` 英文代码——现在显示正常文字
- **一百九十七**：启动崩溃修复（宠物免伤功能内部问题），游戏能正常进
- **一百九十六**：①带着保命道具（绀珠之药/不死图腾）的女仆不再惊慌乱跑，会留下来战斗或治疗 ②低血逃跑时不会被自动传送回你身边送死
- **一百九十五**：排班表和手册换了新图标
- **一百九十四**：女仆击杀敌人后不再不停冒"邀功"气泡（想要可在对话设置里开回）
- **一百九十三**：宠物免伤——不攻击「玩家宠物」标签生物和你养的猫狗，溅射也不误伤
- **一百九十二**：挖矿/伐木垫台阶后会自动踩上去（之前会卡在几个方块间来回）
- **一百九十一**：排班三处修复（排班日志不刷屏、散步速度正常、面板改任务被拦后立即弹回原值）
- **一百九十**：点击「建造此建筑」后会提示"按 Z 键旋转朝向"
- **一百八十九**：建造投影——走出红色区块也能看见幽灵方块（远景更清晰见二百零一）
- **一百八十八**：搭太高自动传送回你脚下（垂直距离超过 16 格且你旁边有安全落点才传）
- **一百八十七**：搭路启动要求可调：水平距离达到设定值才铺桥（默认 5 格，被动技能里可改）
- **一百八十六**：熔炉日志不再刷屏 + 女仆不再抢别人正在用的炉子
- **一百八十五**：排班/守家中的女仆不再被跨维度传送拉走（想带回来先解除排班）
- **一百八十四**：剪刀模式会自动从背包拿剪刀
- **一百八十三**：排班/守家活动范围扩大（默认 32 格）+ 空闲时会自己散步（频率/速度/范围可调）
- **一百八十二**：熔炉喂料升级——背包里什么能烧就烧什么（沙子/圆石/原木等都可）
- **一百八十一**：拿模组武器（拔刀剑等）时必定用它的专属模式，不再随机切到普通攻击
- **一百八十**：搭路修复——垫一块会自动走上去再垫下一块（之前只搭一块就停）
- **一百七十九**：搭路触发重做：与你有水平距离、前方脚下是空地就朝你铺方块（不再要求你比她高很多）
- **一百七十八**：手册界面排版修复（按钮重叠、页码压箭头、保存按钮错位等）
- **一百七十七**：配置面板分页修复（之前第 4 页翻不到第 5 页）
- **一百七十六**：排班切换重做——按时间段落真实切换任务，新增"最短持有"防频繁切换、切段后大脑自愈
- **一百七十五**：搭路没方块时提示更清楚（附诊断日志；想让她搭路请先给她包里放圆石/泥土）
- **一百七十四**：多女仆分炉修复——第二只女仆不再被搭路拉离炉子
- **一百七十三**：自主战斗还原修复——战斗结束真的回到原任务（这个 bug 前前后后修了很久）

## 实测一百七十二

- 还原扫描心跳诊断：
  - 【问题】所有会话 restore-scan（一百六十四）零条 + 无还原日志 + 无异常——卡点可能是：扫描没跑 / 女仆被排班门清理 / 还原动作静默失败，静态无法区分
  - 【本次】还原扫描新增**心跳 + 门统计**（每 10 秒一条，latest.log 搜 `auto-combat scan`）：`active=扫描看到的战斗态女仆数 / schedCleared=被排班门清理的`——一次测试定案：
    - 心跳完全不出现 → onServerTick 没跑（注册/事件问题）
    - active>0 且 schedCleared>0 → 女仆被排班门清理（排班优先设计使然，非 bug）
    - active>0 且 schedCleared=0 但无 restore-scan → 卡在威胁/安全计时门
    - active>0 且 restore-scan 出现 → 直接看威胁明细定位
  - 测试路径：重启 → 确认女仆排班**已关闭**（书里关掉，且手动改任务不再被拦截）→ 打怪 → 杀怪切和平 → 等 1~2 分钟 → latest.log 搜 `auto-combat scan` 和 `restore-scan` 发我

## 实测一百七十一

- 模组武器"选到又换走"根治（游戏重启后日志实证：00:59:19 候选池 `melee=[attack, feed_animal, true_power_of_maid:slashblade_attack, ef_tlm:fight_mode_task]`——拔刀剑/史诗战斗任务都在池里、第一次参战确实选了 slashblade_attack，但 10 毫秒后"自愈"触发重选、随机落回原版 attack = "不会拿出拔刀剑"的直接原因）：
  - 【根因①·参战重选抖动】刚参战 `setTask` 还没同步到 `getTask()`（DATA_TASK 抖动读到 idle）→ 下一次触发走"残留自愈"把标记清了又重选 → 随机数第二次落回原版武器。修复：**刚参战 3 秒内读到 idle 视为同步抖动**——不清标记、不重选，保持当前切换（拔刀剑的选择稳住了）
  - 【根因②·候选池被污染】TLM 喂动物任务（`touhou_little_maid:feed_animal`）也实现了 `IAttackTask`（javap 实证）——混进战斗候选池，女仆战斗中可能随机切去喂动物，还稀释模组武器权重。修复：**排除 feed_animal 出战斗池**
  - 效果：带拔刀剑/史诗武器的女仆参战时稳定切到模组武器任务并装备（mod 权重 2.0 vs 原版 1.0），不再被重选抖回原版

## 实测一百七十

- 排班任务切换设计修复：
  - 【根因】排班切换前的"完整可用性检测"（默认开）做软探测：切糖甘蔗/瓜/矿/树/炉子等任务前检查附近有没有活干——没有就"没活不切"。结果：① 排班一分配，段任务是糖甘蔗但附近没作物 → 任务切不过去，女仆钉死在原地（空闲），而排班锁了手动修改 → 动弹不得；② 时间流逝到新段落，每个段任务都因"没活"被拦 → 任务状态永远不随段切换 = 排班看起来完全失效
  - 【修复】可用性检测默认**关闭**（默认只查任务自己的 isEnable 硬闸）——排班切任务不再看附近有没有矿/树/炉子/作物，**任务状态跟着时间段落真实切换**（切过去没活就站桩在对应任务上，状态是变的、时间到了下段再切）；想用回完整检测可在面板重新打开
  - 附带：旧档存的 `scheduleAvailabilityCheck = true` 自动迁移为 false（重启生效）；toml 被运行中的游戏锁定无法直接改，迁移兜底
  - 测试：重启游戏 → 给女仆排班（多段）→ 用时间洪流怀表快进 → 任务应随段切换（promaid.log 搜「应用段」逐段出现）

## 实测一百六十九

- 战斗候选池内容诊断：
  - 【背景】ef_tlm 的 `isWeaponCap(ItemStack)` 反射签名已 javap 验证匹配（CombatTaskCompat 判定正确）；8月27日日志实证 true_power 拔刀剑任务曾被选中使用——模组武器链路本身通
  - 【本次】buildPools 新增候选池内容诊断（每 5 秒/女仆一条，latest.log 搜 `combat pools`）：直接列出近战/远程候选池里有哪些任务——池里只有原版任务 = 模组武器没被 isWeapon 认到（模组任务/武器判定问题）；池里有模组任务 = 权重随机或距离选择问题。一次日志区分两种根因
  - 【同时发现】运行中的游戏是旧 jar：日志实证 00:14/00:21 排班开启的女仆仍参战（一百六十三"排班中不参战"门没生效）、restore-scan 诊断零条（一百六十四没加载）——**当前 jar 需重启游戏才生效**（一百六十三排班优先 / 一百六十四还原诊断 / 一百六十五平桥追逐 / 一百六十六远程风筝 / 一百六十七战斗中不搭路 / 一百六十八多女仆共炉全在新 jar 里）

## 实测一百六十八

- 多女仆共炉分散：
  - 【根因】烧制行为找炉（`findFurnace`）按固定扫描顺序返回【第一个】炉子——两个女仆站在附近，各自扫描都先命中同一个炉子并绑定，另外两个炉子永远没人喂料（"只有一个炉子工作"）
  - 【修复】新增**炉子占用表**（维度|坐标 → 占用女仆 UUID）：绑定炉子时登记、行为停止/炉子丢失时释放；找炉时**跳过已被其他女仆占用的炉子**（自己占的不跳）——多个女仆自动分散到不同炉子，N 个炉子 N 个女仆各烧各的；占用者死亡/换维/被懒清理，不会占住炉子不放
  - 影响范围：烧制任务（熔炉/高炉/烟熏炉通用）；单女仆场景无感知（自己占自己用）

## 实测一百六十七

- 战斗中不再搭路：
  - 【根因①】搭路的任务占用门（`isTaskOccupied`）明确把【战斗任务】列为"不拦"（设计注释原文："不拦的情况：idle/跟随/战斗/喂食"）——战斗中的女仆照样被搭路行为接管移动，边打边往主人方向搭路跑；实测一百六十五放开搭路距离上限后更明显
  - 【根因②】搭路的威胁扫描只认 `Monster` 类 + 半径 8 格——史莱姆/岩浆怪等实现 `Enemy` 接口但不实现 `Monster` 的不算威胁；敌人站在 8 格外接战时搭路完全无感知
  - 【修复】① 战斗任务（`IAttackTask`）或脑内有存活攻击目标（ATTACK_TARGET 记忆）一律算占用，搭路不启动/立即中止；② 威胁扫描放宽到 `Enemy` 接口（与参战/还原同口径）
  - 效果：战斗中的女仆专心打架，绝不被搭路行为拉去追主人；威胁判定与全局统一（史莱姆等也算威胁）

## 实测一百六十六

- 远程走位修复：
  - 【根因】远程走位的理想射程 = 武器最大射程 × 0.6 倍率。三叉戟的射程走 TLM 配置 `TRIDENT_RANGE`（约 8~10 格）、弩走原版 `getDefaultProjectileRange`（8 格）——乘 0.6 后理想风筝距离只有 **4.8~6 格 = 贴脸**（近战怪 3 格就摸得到），看起来就是"远程还近身"；日志实证女仆在贴脸距离（2.9~4.4 格）反复跳劈
  - 【修复】理想射程强制**最小风筝距离 7 格**：任何远程武器（弓/弩/三叉戟/枪械）都保持 ≥7 格清晰射程——太近后退到 ideal+2、太远逼近、理想区间绕圈放风筝，近战怪完全摸不到；弓（射程 15 → ideal 9）与枪械不受影响
  - 附带：远程走位诊断日志（每 2 秒一条，latest.log 搜 `ranged kite`）——显示任务/主手武器/当前距离/理想射程，确认走位在跑、数值对不对
  - 若仍近身，把 latest.log 搜 `ranged kite` 和 `jump attack` 的行发我（jump attack=在打近战，ranged kite=在放风筝但距离不对）

## 实测一百六十五

- 搭路平桥追逐放开距离上限：
  - 【根因】搭路行为的启动（canUse）与保持（canContinue/stopReason）有三处距离上限：平路/低高差场景（主人不低于女仆）受 `maxDist`（玩家已调到 32 格）限制，超过就 `return false` 不启桥；追逐中途主人走远（>50 格）还会被 canContinue 掐断——水平距离一远就完全追不上
  - 【修复】平路/低高差追逐（主人不低于女仆、无需爬高）距离上限【全部放开】——水平方向多远都启动平桥追逐：前方脚下悬空就铺桥（tryAirBridgeStep，参考僵尸 BlockBuildBridGeGoal）、实心地面走导航，像僵尸持续逼近主人，方块耗尽/威胁出现/任务被占用自然停；只有主人【高于女仆】需垂直搭高时才受 `airMaxDist`（默认 128）约束（防搭到天上去）
  - 三处判定（canUse 启动 / canContinue 保持 / stopReason 中止）统一同口径；配置面板说明同步更新（「搭路触发距离」现只约束垂直搭高场景）
  - 注意：搭路仍要求女仆【非守家】（home 模式不追人）、非工作占用、背包有方块、周围无威胁

## 实测一百六十四

- 还原扫描门级诊断（追问："为什么会卡，就没有办法修复它吗？偏要加个硬性的时间调整"）：
  - 【诚实回答】90 秒硬超时是【安全网】不是根因修复。现状：整份日志零条还原记录 + 还原扫描零异常 → 扫描卡在威胁判定或安全计时两个【静默 continue】之一，静态分析无法区分（`pick dist=37.0` 是平方距离=实际 6 格，威胁判定半径本身没错）
  - 【本次】还原扫描新增门级诊断（每 10 秒/女仆一条，latest.log 搜 `restore-scan`）：`task=当前任务 / assigned=指派战斗任务 / lastThreatAge=距上次威胁刷新秒数 / threatDetail=威胁来源明细（fixed=固定半径内敌对生物距离 / ring=动态圈来源存活/已死 / none） / preserve=自保 / isOnSched=排班开启`
  - 测试路径：进游戏打怪→杀怪→切和平，等 1~2 分钟，把 latest.log 搜 `restore-scan` 的行发我——一条日志就能指出卡在哪道门（威胁一直 true？安全计时被续杯？任务判定异常？），然后针对性修根因
  - 已有兜底不变（排班优先方案 100 六十三 / 残留自愈 / 90 秒硬超时）——在根因修好前保证功能不卡死

## 实测一百六十三

- 排班优先级方案落地：
  - 【根因回顾】日志实证：8月26-27日排班正常应用段（"应用段 0:00~4:00 任务=sugar_cane"等），8月28日 14:53 起**排班再也没应用过任何段**，取而代之的是一连串"主动战斗中——排班让位"——女仆的 COMBAT_ACTIVE 战斗标记卡死在 true（战斗还原从没跑成功的同源问题），排班调度每次尝试应用都被"战斗中让位"挡掉 → 任务永不切换、女仆一直跟随主人
  - 【方案（按要求）】**排班 > 自主战斗**：排班开启的女仆【不参与自主战斗、不响应任何参战触发】（主人被打/女仆被打都不切战斗）——任务/模式全由日程表管理，从根上杜绝战斗标记与排班互相拉扯；想让她打 → 排班段任务直接配攻击任务（日程表驱动），或关闭该女仆排班
  - 【残留清理】老版本留下的残留 COMBAT_ACTIVE 标记自动清理：排班女仆在还原扫描中直接清标记（1 秒内）；非排班女仆的残留标记（无开始时间戳 + 当前任务非攻击）强制还原自愈；新参战仍受 90 秒硬超时兜底
  - 【让位收紧】排班让位判定改为"真实在战斗"（标记 + 当前任务确实是攻击任务）——残留标记不再拦截排班应用；批量一键应用同样跳过残留标记女仆
  - 效果：排班女仆每天准时切段（应用段日志恢复）、不再被战斗状态卡住跟随主人；非排班女仆的自主战斗/还原照旧工作

## 实测一百六十二

- 主动战斗"被吞 + 不还原"根治：
  - 【根因①·参战被吞】`tryEngageMaid` 的 COMBAT_ACTIVE 门：只要读到 idle（战斗早已结束、任务回落 idle）就 `return 0` 吞掉参战——老女仆身上残留的战斗标记（旧版本崩溃/半途退出留下的 `maid_smart_combat_active=true`）永远清不掉 → 参战被永久吞掉。新女仆没有残留标记所以能触发。修复：**只有真实还在战斗任务（指派或任意攻击任务）才吞**；idle 读数 = 残留标记 → 清掉并落日志「残留战斗标记自愈」后继续走参战评估
  - 【根因②·永不还原】整份日志【零条】还原日志（战斗还原/无需还原/兜底还原/还原未生效都没有）——还原动作从没被执行到，卡在还原扫描前面的某个门（威胁判定/安全计时）。修复：**硬性超时兜底**——参战瞬间记录 `maid_smart_combat_start` 时间戳，战斗会话超过 90 秒仍未还原 → 无论威胁是否仍在、安全计时是否被续杯、僵局阀是否失效，一律强制走还原（`forceRestore` 统一接管原来的"无武器强制还原"门，跳过远处切远程/还原开关门）
  - 还原动作本身已是 tlm_beyond_space 的 restoreTemporaryState 模式（快照任务+home+作息、先还原后清会话、读回校验、失败保留标记下轮重试），本次不再动
  - 测试路径：老女仆（之前卡在战斗态的）进游戏 → 打怪 → 参战应恢复触发；怪死后 90 秒内必然还原（promaid.log 搜「战斗还原」或「强制还原」确认）

## 实测一百六十一

- 烧制任务改名 + 烧不起来诊断：
  - 【改名】「烹饪」任务改名「烧制」（任务名/任务摘要/配置面板/手册/工作报告文案全量更新；`zh_cn`=烧制、`en_us`=Smelting）——任务已兼容矿石/高炉/烟熏炉，不再只是做饭
  - 【诊断】给烧制行为加断点日志（latest.log 搜 `cook `）：行为启动时绑定哪个炉子（`cook start`）→ 门控是否通过（`cook gate-blocked`）→ 每轮收了什么成品/喂了什么料/还是"背包无料可喂"（`cook round`）。日志能直接区分：行为没跑 / 找不到炉子 / 门控没通过 / 配方没匹配，四种断点一次定位
  - 【待验证】当前日志无法确定断点（旧版行为无日志）。请再测试一次：给女仆兔肉+燃料、切「烧制」任务、附近放熔炉/烟熏炉，跑 1~2 分钟后把 `logs/latest.log` 里搜 `cook ` 的行发我——按断点直接修

## 实测一百六十

- 修复跟随收紧 mixin 二次崩溃（崩溃日志实证：`InvalidInjectionException: Invalid descriptor ... CallbackInfoReturnable is required!`）：
  - 【根因】实测一百五十九把注入点换到 `m_6114_`（canUse）方向正确，但 **canUse 返回 boolean**——Mixin 规定注入带返回值的方法必须用 `CallbackInfoReturnable` 作回调参数，用 `CallbackInfo` 直接报 Invalid descriptor
  - 【修复】处理器签名改为 `CallbackInfoReturnable<Boolean>`（不调用 setReturnValue，原 canUse 判定结果完全不受影响）；注入目标仍是本类声明的 `m_6114_`（canUse，大脑每 tick 调用），4 格内不拉/守家/脑冻结/无主/跨维度/干活中不拉守卫与 `misc.followTighten` 开关不变
  - 已从部署 jar 验证：处理器签名 `(ServerLevel, LivingEntity, CallbackInfoReturnable)` + 目标 `m_6114_` 均正确

## 实测一百五十九

- 修复跟随收紧 mixin 导致游戏加载崩溃（崩溃日志实证：`InvalidInjectionException: Critical injection failure: @Inject ... could not find any targets matching 'm_6725_' in MaidFollowOwnerTask`——打开创造栏渲染女仆时 EntityMaid 加载触发 mixin 应用，直接崩）：
  - 【根因】实测一百五十一的 `MaidFollowOwnerTickMixin` 注入 `m_6725_`（tick）——tick 是**继承自父类 Behavior 的方法**，Mixin 的 `@Inject` 只匹配目标类【自己声明】的方法，继承方法匹配不到 → 注入目标校验失败（`"required": true` 配置下即致命错误）
  - 【修复】注入点改到 `MaidFollowOwnerTask` 自己声明的 `m_6114_`（canUse）：javap 实证本类只声明 `m_6114_`（canUse）与 `m_6735_`（start），且未覆写 `canStillUse`（父类默认 false）——大脑每 tick 对 STILL 状态行为调 tryStart → canUse（每 tick 调用），RUNNING 仅持续 1 tick 就 doStop 回 STILL，注入 canUse 入口即等价改版 jar 的每 tick 驱动。守卫逻辑（4 格内不拉/守家/脑冻结/无主/跨维度/干活中不拉）不变，总开关 `misc.followTighten` 不变
  - 若仍出现 mixin 应用报错，回退方案 = 外部每 tick 扫描器（HomePatrolHandler 同款）实现跟随重断言，不再依赖 mixin

## 实测一百五十八

- 烹饪任务兼容高炉与烟熏炉：
  - 【根因】行为只处理 `FurnaceBlockEntity`——烟熏炉/高炉是熔炉方块类（AbstractFurnaceBlock）但不是熔炉实体，女仆走到它们面前只会干坐（收成品/补燃料/喂料全不执行）
  - 【修复】三种炉子统一操作，按各炉子配方类型喂料：熔炉 = 食材白名单 → 矿物类可烧制物（实测一百五十七）；烟熏炉 = 有烟熏配方的物品（生食）；高炉 = 有高炉配方的物品（矿石/粗金属）。成品收取/燃料补充三种炉子通用
  - 约束关系：高炉只烧矿物 → 高炉喂料受「熔炉烧矿物」（`misc.cookSmeltOres`）开关约束，关掉后高炉只收成品/补燃料不喂料；烟熏炉喂生食不受该开关影响
  - 总开关 `misc.cookSmokerBlast`（默认开）+ 配置面板「兼容高炉/烟熏炉」行；关闭 = 旧行为（只操作熔炉）

## 实测一百五十七

- 熔炉兼容矿物类可烧制物：
  - 【根因】烹饪任务补食材只认 `FOODS` 内置白名单（生肉/鱼/土豆等）——背包里的铁矿石/粗铁/金矿石/远古残骸等矿物类可烧制物永远不会被放进熔炉，女仆守着炉子只烧食物
  - 【修复】背包没有食材时，新增矿物类可烧制物补料：物品**带矿物/原料标签**（标签路径含 `ores` 或 `raw_materials`——forge:ores、forge:ores/*、minecraft:*_ores、forge:raw_materials 等，模组矿物标签同样命中）**且当前世界有熔炉配方**（走配方管理器查 SMELTING，模组自定义烧制配方也认）才放入熔炉槽 0；食材优先、矿物兜底，成品收取/燃料逻辑不变
  - 总开关 `misc.cookSmeltOres`（默认开）+ 配置面板「熔炉烧矿物」行；关闭 = 只烧食材白名单

## 实测一百五十六

- 骑乘中禁止搭方块：
  - 【根因】挖矿模式的搭高/搭斜坡/搭桥（`pillarUpStep`/`slopeStep`/`bridgeToOre`）不检查骑乘状态——女仆骑在扫帚上（RIDE_WORK activity 任务照常运行）够不着矿时照样垫方块，留下一堆残渣
  - 【修复】三处搭方块入口统一加骑乘守卫（`isPassenger`）：骑乘中直接返回不垫块——移动由载具控制，垫方块只会留残渣；挖矿其他逻辑（找矿/挖矿/收集）不受影响
  - 总开关 `mine.rideNoPillar`（默认开）+ 配置面板「骑乘中禁止搭方块」行；关闭 = 旧行为

## 实测一百五十五

- 保命物品下保留逃跑开关：
  - 【背景】TLM 绀珠之药 = `ultramarine_orb_elixir`（ExtraLifeBauble，死亡复活，等价不死图腾，仅 `bypasses_invulnerability` 伤害不救）；原版不死图腾在饰品栏（UndyingTotemBauble）/双手/背包（MaidBaubleTotemMixin）都能救命。女仆带着这些保命物品时低血还在逃跑 = 死不了却丢下工作满场跑
  - 【修复】新增 `hasDeathSaveItem`（饰品栏绀珠之药/不死图腾 + 双手/背包不死图腾）与 `canFlee`：开关关闭（默认）时——有保命物品就不逃跑（继续战斗/垫高/治疗，不丢下工作），同时不再播报"我先撤了"；开关开启 = 照常逃跑
  - 总开关 `combat.fleeWithSaveItem`（默认关）+ 配置面板「保命物品下保留逃跑」行

## 实测一百五十四

- 溺水保护饰品识别：
  - 【根因】TLM 溺水保护饰品（`touhou_little_maid:drown_protect_bauble`，DrownProtectBauble）的机制 = 每次溺水伤害触发时**空气回满 + 伤害免疫**（javap 实证：`setAirSupply(getMaxAirSupply)` + 取消 `is_drowning` 伤害）——但女仆泡水时氧气仍会自然消耗，promaid 的溺水判定（泡水且氧气<60）照常触发 → "溺水了！我游上去！"上浮找空气，完全无视饰品
  - 【修复】溺水软危险判定 + 溺水分支 + 溺水兜底三处统一加饰品豁免：佩戴溺水保护饰品（且开关开启）时泡水不再喊溺水/上浮/喝水肺——饰品每 tick 自己补空气，她淹不死
  - 总开关 `combat.drownProtectBauble`（默认开）+ 配置面板「溺水保护饰品识别」行；关闭 = 旧行为

## 实测一百五十三

- 火焰保护饰品识别：
  - 【根因】TLM 火焰保护饰品（`touhou_little_maid:fire_protect_bauble`，FireProtectBauble）的机制 = 受到 `is_fire` 标签伤害时**给 15 秒抗火 + 喷灭火剂 + 伤害免疫**（javap 实证：MobEffects.FIRE_RESISTANCE 300 tick + EntityExtinguishingAgent）——但刚点燃还没吃到第一次火焰伤害的窗口期（或灭火剂未生效时）身上仍是 `isOnFire`，promaid 的着火判定照常触发 → 惊慌灭火/找水/往主人身边跑
  - 【修复】致命危险（泡岩浆）+ 软危险（着火）+ 着火分支 + 环境逃生兜底四处统一加饰品豁免：佩戴火焰保护饰品（且开关开启）时着火/泡岩浆不再惊慌——饰品自己会处理（受伤触发给抗火 + 灭火剂浇灭）
  - 总开关 `combat.fireProtectBauble`（默认开）+ 配置面板「火焰保护饰品识别」行；关闭 = 旧行为

## 实测一百五十二

- 有增益也喂牛奶：
  - 【根因】牛奶前提 = "无增益 + 有负面"（v1.5.281 设计：牛奶清全部效果，有增益时喝会连增益一起清掉）——但很多装备/饰品带**永久增益**（如 TLM 饰品/护甲附魔效果），女仆身上永远有增益 → 永远不满足"无增益" → 中毒/凋零/虚弱也不喝牛奶不解负面
  - 【修复】新增 `misc.milkFeedWithBuff`（默认开）：开启时女仆自己喝牛奶解负面（`hasOnlyNegativeEffects` 忽略增益）与给主人喂牛奶解负面（`feedMilkOrHoneyDirect` 无视主人增益）都照喂——有负面 buff 就喂牛奶，装备永久增益会被一起清掉；关闭 = 旧行为（有增益不喂牛奶，只喂蜂蜜解中毒）
  - 配置面板「有增益也喂牛奶」行（自保区）

## 实测一百五十一

- 跟随模式乱跑修复：
  - 【实证分析】反编译对比改版 jar（touhoulittlemaid-1.5.3-modified-all.jar）与官方 1.5.3 的 `MaidFollowOwnerTask`：距离逻辑（start 里的 teleport/walk 分支，`getRestrictRadius` 双带）**字节码完全一致**；唯一区别是改版把逻辑提取成 `tryFollowOwner` 并加了 **tick 覆写——每 tick 驱动**。官方版只在行为【启动时】设一次寻路目标：被其他行为覆盖、或 MoveToTargetSink 的 150~250 tick 刹车（走路满 7.5~12.5 秒清 WALK_TARGET + 停导航，实测一百二十九修过的同源问题）清掉后，女仆失去跟随目标 → 走走停停/乱跑
  - 【修复】新增 `MaidFollowOwnerTickMixin`：注入跟随任务的 tick（`m_6725_`，继承自 Behavior），每 tick 重断言——**4 格内不动（跟随已达成）；超过 4 格重新 setWalkAndLookTarget 指向主人**（speedModifier/stopDistance 与官方任务字段一致，跟随自然收敛在 4 格内）；守家/脑冻结/无主/跨维度不拉（与官方 maidStateConditions 同口径）；**干活中不拉**（防跟随目标覆盖工作寻路）
  - 总开关 `misc.followTighten`（默认开）+ 配置面板「跟随收紧」行；关闭 = 官方 1.5.3 原版行为

## 实测一百五十

- 和平模式（怪全清）后女仆仍不还原的兜底根治（玩家复现路径：简单难度 → 玩家攻击怪 → 女仆参战 → 女仆杀怪 → 切和平 → 女仆仍不切回原模式；参考 tlm_beyond_space 的 restoreTemporaryState：**先还原、后清会话——失败不清标记、下轮重试**）：
  - 【根因】promaid 旧版还原是【先 clearMarkers 再 setTask】——TLM 的 setTask 有守卫（睡眠/活动等）会**静默拒绝**（实测一百二十九的读回校验同源）：任务没切走、标记却已清 → 还原扫描不再处理她（COMBAT_ACTIVE=false）→ 永久卡在战斗任务 = "切不回原来的模式"的最后一道漏洞
  - 【修复】还原与兜底两条路径都改为**先还原、后清标记 + 读回校验**：setTask 后读回任务 UID，切走了才算还原成功并清标记；没切走（TLM 守卫拒绝）保留 COMBAT_ACTIVE、落日志"还原未生效…保留标记下轮重试"，下轮扫描继续重试直到成功——还原链再也不会被"半途清标记"丢掉
  - 附带：兜底（原任务不存在）路径同样读回校验 idle/排班切换，失败不清标记
  - 与实测一百四十八/一百四十九形成完整闭环：参战预检（不切入无武器任务）→ 威胁消失/无武器强制还原/僵局逃逸 → 先还原后清标记（读回校验+重试）→ 任务+home+作息全量恢复

## 实测一百四十九

- 主动战斗"切回之前模式"根治：
  - 【历史教训】日志实证整份 promaid.log 里【从来没有一条"战斗还原"日志】——还原从未跑成，反复出现的是"战斗中任务被接管…清标记退出"→ 重新参战的循环；且参战快照多为 idle
  - 【根因①·状态只存一半】参战只存了前任务 UID，没存 home 模式与作息（MaidSchedule）——"之前的模式"缺了一半。参考项目参战瞬间快照 任务+home+作息 三件套，结束时全量还原。修复：新增 `COMBAT_PREV_HOME_TAG`/`COMBAT_PREV_SCHEDULE_TAG`，参战捕获、还原时（排班关闭才生效，排班开启由日程表管理不覆盖）恢复 home+作息——"切回之前的模式"完整闭环
  - 【根因②·"接管"只清标记不还原】任务被外部改走时旧版只清标记退出——参考项目 `restoreAfterExternalTaskChange` 是【尊重新任务、但把 home/作息还原回战斗前】。修复：两处接管路径（参战入口/还原扫描）清标记的同时调用 `restorePrevMode` 兜底
  - 【根因③·同一 tick 多次读 getTask() 自相矛盾】TLM getTask() 读同步数据 DATA_TASK，uid 解析抖动会回落 idle（实测一百一十四）——日志实证"接管"判定时读的是非 idle、打印却变 idle，还原链被误判丢掉。修复：判定改【单次任务读取】（`isAssignedOrCombatTask`/`isIdleReadingTask` 传任务参数），参战入口与还原扫描共用同一读数
  - 【根因④·还原前不装备】参考项目还原前 `prepareSwitch`（把原任务武器/工具装回主手）——修复：还原 setTask 前调用 `CombatTaskCompat.prepareSwitch`（结果忽略，MISSING 也照常还原任务本身）
  - 还原链路现状：威胁消失（或武器被拿走强制还原/僵局逃逸）→ 先解析原任务（失败兜底 idle+排班）→ 清标记 → 排班开启交排班当前段，否则还原前任务 + home/作息 + 武器装备 → 全程日志

## 实测一百四十八

- 战斗武器两连修：
  - 【根因①·模组武器不用】`ef_tlm:fight_mode_task`（史诗战斗联动）**没有覆写 `isWeapon(maid, stack)`**——`IAttackTask` 默认实现恒 false（javap 实证）→ 该任务的武器永远进不了战斗候选池、也永远不被自动装备 = "切换武器时不用模组武器"。该任务只暴露 `isWeaponCap(ItemStack)`/`hasCapWeapon(maid)`——新建 `CombatTaskCompat`，用反射调用 `isWeaponCap` 补上判定（照搬参考项目的兼容层），接入战斗池判定（`hasWeaponForTask`）与模组任务自动装备（`MaidToolAutoEquip`）
  - 【根因②·再也不触发】切任务前从不【预检+自动装备】。TLM 官方 `onFunctionCallSwitch` 默认实现（javap 实证）= 主手有武器 → NO_CHANGE；没有 → `TaskEquipUtil.tryEquipFromBackpack` 自动装备 → OK；装不上 → `MISSING_REQUIRED_ITEM`。参考项目正是用它做切换前预检。修复三处：
    - 参战入口：`pickCombatTask` 后先 `prepareSwitch`，`MISSING_REQUIRED_ITEM`（武器被拿走/任务要求特殊物品）→ 不切入，绝不把女仆卡在打不出伤害的战斗任务上
    - 换战术（近远程切换）：同样先 `prepareSwitch`，装不上就不切保持现状
    - 还原扫描：当前战斗任务【已无可用武器】→ 强制还原（跳过威胁消失/安全时长等待）——武器没了打不死怪、威胁永不消失、还原永久卡住 = 女仆永远"战斗中"、之后一切参战触发全被跳过（"再也不触发"的根因）；同时"当前已是战斗任务"判定在无武器时不再视为已在战斗，允许重选
  - 附带收益：切任务瞬间武器自动装到主手（不再等下一 tick 的自动装备）；主世界/其他模组战斗任务行为不变

## 实测一百四十七

- 建造预览两连修（玩家问题：玩家周边的黄色区块框怎么都去不掉，原本要求显示的幽灵方块也没有了，整个建造链路出现了bug）：
  - 【金色框去不掉·根因】`BlueprintAreaPreview.clear()` 在实测一百三十二加诊断日志时把 `active = false` **误删**（日志替换了状态复位，git diff 实证）——金色预览开启后打开手册/确认建造都关不掉，框永远跟着玩家移动。修复：恢复复位，`clear()` 真正关闭金色预览（调用点 BlueprintBookScreen.open = "再次打开手册 = 关预览"，与 render 标签"（打开手册关闭）"一致）
  - 【幽灵方块消失·根因】实测一百零九的 renderSingleBlock 渲染方案对 1.20.1 不成立：Forge 版 7 参 `renderSingleBlock` 字节码实证——它遍历 `BakedModel.getRenderTypes` 按【模型自身图层】渲染、写进各图层自己的 buffer，**完全忽略传入的 translucent renderType**；幽灵顶点落入早已刷新的 solid/cutout buffer，透明混合与刷新时机都不对 → 方块不可见（实测一百二十九/一百三十二"轮廓又没了"反复复发、一百零一初次引入时同样不可见被一百零五/一百零六回退——同一根因）。修复：改回与【区块框同款】的 DebugRenderer 即时渲染——每点一个 `renderFilledBox` 半透明填充盒（区块框一直在正常显示：框能显示，幽灵必能显示）；删除废弃的 GhostBufferSource/GhostVertexConsumer 包装类
  - 投影数据链路（请求→服务端采样→分块回传）本就正常，未改动

## 实测一百四十六

- 下界彻底不用水桶（玩家问题：女仆在下界的时候仍然会使用落地水）：
  - 【排查结论】落地水本身（WaterClutchBehavior）下界早已有闸（`!isNether`，下界只会用细雪）——玩家看到的下界放水来自自保系统的 v1.5.250 改动：当时按"女仆喜欢用水桶"的要求，把着火灭火链和岩浆逃生链的下界跳过取消了，改为每 40 tick（2 秒）倒一次水（放置动作/蒸汽可见，实际水瞬间蒸发、灭火无效）——女仆在下界着火/坠入岩浆时看起来在乱放水，玩家误认为"还在用落地水"
  - 【修复】恢复 v1.5.210 设计，下界不再用任何水：① `placeClutchWater`（着火灭火）开头下界直接 return（双保险：调用侧 `fireBucketStep` 同时加回 `!isNether`）；② `placeWaterOnLava`（岩浆黑曜石逃生）下界 return false；③ 删除 `netherPourTick` 下界倒水节流字段与全部逻辑。下界着火/岩浆流转末影珍珠→抗火药水→金苹果→物理逃离，全部仍可用
  - 主世界（水会流动铺开/灭火/岩浆变黑曜石）行为完全不变

## 实测一百四十五

- 落地雪 3×3 → 1×1：
  - 【改法】`WaterClutchBehavior.ensureSnowPad` 不再铺 3×3 九宫格，只铺【落点中心一格】（落点格非空气/本轮已铺过则直接返回，不重置保持计时）
  - 【影响】细雪本就不流动、只认实体真正落进去的那一格——3×3 是容错垫，1×1 后无容差：能否接住完全依赖坠落途中逐 tick 的落点追补（击退/横移偏出一格、落点格非空气铺不进雪即空摔）。机制固有难度提升，追求稳请用水桶（水会流动自己铺开）
  - 同步更新：配置注释（combat.snowClutch）、配置面板文案、被动技能手册文案（"1×1 无容错，偏一格即空摔"）

## 实测一百四十四

- 排班表三连修：
  - 【不断瞬移·根因】受困救援的"真虚空"判定用错了方法——`needsRescue` 注释写的是"掉出最低建筑高度"，实际调用的 SRG `m_141928_` 是 `getHeight`（主世界 384）："y < 384" 对所有站立女仆恒真 → 救援系统把每只**距主人 >8 格**的女仆（含守家/排班女仆，无豁免）每 5 秒拽回主人身边一次（日志实证：排班锚点 (91,-60,139) 的女仆被当成"掉出世界"循环救援，14:58 起每 5 秒一条 `maid rescue`）。修正确认映射（javap `Level.getHeight` 实证 `m_141937_` = getMinBuildHeight，主世界 -64 / 下界与末地 0），只有真正掉出维度底部才触发救援；救援日志附带维度最低高度（min=）便于现场核对
  - 【瞬移连锁反应】救援循环还把排班女仆反复拽进主人身边的怪堆 → 每 5 秒重新参战 → 排班调度器因"主动战斗中"持续让位 → 排班"开了却一直不生效"观感加重。救援修复后此连锁一并消失
  - 【开启不切模式】排班开启瞬间 applyNow 让位于主动战斗（设计如此），但**零反馈**——玩家看不到任何提示，只看到"没切换"；现在保存/快捷开启排班时若女仆正在战斗，聊天栏明确告知「排班已开启，战斗中——战斗结束自动切换当前时段（模式/任务）」，不再静默
  - 【魂符收放不重放】根因：`APPLIED_TAG`（"本段已应用"去抖键）存在 persistentData，**随魂符保存**——同段内收进再放出，去抖键与当前段一致 → 调度器"本段已应用"跳过 → 排班不再重放。修复：女仆重新入世界（EntityJoinLevelEvent：魂符放出/区块重载/跨维度）时清持久化去抖键并立即按当前时段应用一次（任务/模式/在家锚点重放；SchedulePos 锚点随实体 NBT 保存，不会在放出位置错误重锚）；只清去抖键、不动内存里的"尊重手动选择/重试冷却"记录

## 实测一百四十三

- 半空女仆向飞行主人搭桥——两层根因一起修：
  - 【根因①·启动/收尾区重叠抖动】canUse 启动区（dist3 > minRadius=2）与 canContinue 收尾区（dSq<=6.25=2.5 格"reached"）重叠在 2~2.5 格 band：行为每秒 start→reached→stop（日志实证 bridge-up start/stop reason=reached 刷屏），tick 根本没机会铺桥——这就是"主人只有 5 格也不搭"的真凶（她一直悬在 2~2.5 空转）。修复：启动须离开收尾区（> max(minRadius, 2.5)），两区不相交 → 无抖动
  - 【根因②·多余门槛】实测一百四十一加的"水平 >4 格才启动"把 5 格左右的平地场景也挡在门外——去掉（近距离由根因①的收尾区兜底）
  - 【根因③·距离上限】女仆自己半空（isAirborne）时 distLimit=airMaxDist(24)，主人飞出 24 格 she 放弃——半空放开上限（参考僵尸索敌：方块耗尽自然停）；airMaxDist 默认 24→128 兜底地面女仆追空中主人
  - 三处口径统一：canUse / canContinue / stopReason 同规则

## 实测一百四十二

- 修复挖矿/伐木"每 5 秒重置一次状态"：
  - 【根因】矿/木材缓存 TTL = 100 tick（5 秒）到期强制全量重扫：挖完一块后 targetPos 置空 → findOre/findWood 发现缓存到期 → 启动分帧全量扫描（几十 tick，期间"无目标"站着等）→ 扫完重新选目标，可能换一块更"优"的矿/树重新走 → 观感 = 每 5 秒停一下/重置/换目标，像收放魂符
  - 【修复】去掉 TTL 到期强制重建——缓存轮（pickFromCache）每次本就逐格校验矿/木材是否还在（存在性/框内/可挖/挡路/弃置），到期重建纯属多余；缓存一直用到【轮空】才全量重建（轮空即框内确无矿 → 立即重建/迁移，实测五十五·一百二十七的"原地愣 2~3 秒"修复保留）。锚点变化/迁框时另有强制重建路径，框内新露头的矿在缓存轮空后的全量扫描里自然被发现
  - 挖矿与伐木两处同款修改（ORE_CACHE_TTL / WOOD_CACHE_TTL 逻辑统一）

## 实测一百四十一

- 平地搭桥意愿提升：
  - 【根因】实测一百三十的平地门槛要求"前方 1~2 格脚下悬空（hasGapAhead）才启动"——平地/低高差场景（dy<minDy）行为根本不启动，只有垂直搭高和空中铺桥在跑，平路追逐缺失
  - 【修改】平地/低高差场景去掉"必须前方悬空才启动"：主人【不在下方】且水平距离 >4 格即启动追逐——启动后每步冷却（默认 5 tick）在前方脚下悬空处铺桥、实心地面走路，与僵尸 BlockBuildBridGeGoal 行为一致（其 canUse 对玩家目标近乎无条件，铺桥逻辑同样是"前方脚下悬空才垫、实心走路"）
  - 【防抖动】hDist≤4 不启动（太近走路即可）+ 球面半径门槛保留——避免 2~2.5 格处的启动/停止边界空转（实测一百三十治的抖动不回归）
  - 头顶被挡 20 秒放弃/方块耗尽/威胁/自保等中止条件不变

## 实测一百四十

- 传送"无落脚点"根治：
  - 【根因】落点扫描判定过严：要求"站立格 isAir + 脚下 isSolid 满方块"——下界里脚下有火/火把/草丛、站在台阶/栅栏上、1 格窄道、或传送门平台边缘时必判失败 → "无可站立点" → 一键集合/跨维度跟随/同维度拉回全被卡（实测一百三十四加的三条传送链路全走这一个 findStand）
  - 【判定放宽，参考 tlm_beyond_space SafeTeleportService.canStandAt】新 `standableCell`：脚下有碰撞面（不限满方块）、站立格与头顶碰撞箱为空（无碰撞方块不挡）、站立格与头顶无流体、未加载区块不扫。附加保险：命中危险表（岩浆/火）不落、目标格无存活实体占用（防传进玩家身体被挤走）
  - 【失败落日志】findStand 全失败时（60 秒限频）记录 脚格/下格/头顶 方块 ID——万一还有误杀，日志直接指明
  - 生效范围：一键集合、跨维度跟随、同维度远距拉回、受困救援共用同一个落点查找，一并受益

## 实测一百三十九

- 自主战斗还原根治：
  - 【根因】关键链路用了不稳定的读数：①参战时原任务用 `getTask()` 记录——TLM getTask 读同步数据 DATA_TASK，uid 解析抖动时回落成 idle（日志实证每次参战都录成 idle → 还原目标恒为 idle = "切不回原来的模式"）；②还原扫描/参战评估拿"当前任务是 idle"当"玩家接管"→ 清标记退出、还原链被丢
  - 【原任务可靠快照】`resolvePrevTaskUid`：参战裸记录顺序改为 真任务(非idle) > 排班当前时段任务 > idle——排班女仆还原时回到的是排班段任务，不再被假 idle 带偏
  - 【还原不依赖 getTask 猜测】`isIdleReading`：当前任务读数为 idle/空 时不再判"玩家接管"清标记（继续走还原）；只有**真实的其他任务**（非 idle、非战斗、非指派）才算接管、尊重不还原
  - 军械齐：附带日志"战斗中任务被接管"现在只在真接管时出现，假 idle 不再刷

## 实测一百三十八

- 排班开启的女仆同步锁住【工作模式】（作息 DAY/NIGHT/ALL）——问题：排班名义上不允许玩家切换模式，但玩家用 TLM 本身能切，禁用调整变成无用：
  - 一百三十六只守了 setTask（任务），TLM 原版 GUI 切作息直接调 `setSchedule` 绕了过去 → 段内去抖键命中排班不纠正，作息调整形同虚设
  - 守卫 mixin 扩展：`setSchedule` 同样拦截（排班开启 + 非自动系统调用即拒绝），女仆气泡与日志统一提示"工作模式和任务都由日程表管理"
  - 排班调度器自己的 setSchedule 打内部标记放行（不误伤排班设工作时间）；未开排班的快捷设置/批量应用照常可切

## 实测一百三十七

- 切换至排班 = 强制接管：
  - 排班书快捷设置【开启排班】的瞬间：清掉可能残留的本段去抖键/尝试记录/重试冷却 + 立即 applyNow——手头任务**当场**强制改为当前时段排班的任务（旧版只等 1 秒扫描，且残留去抖键会直接跳过，要拖到下个时段边界才切；配合一百三十五保存路径已修的"改当前段保存不生效"）
  - 修复保存【关闭排班】的反向执行：旧版保存（无论开关）都无条件调 applyNow——关排班后 applyNow 仍会强制 home 模式并按日程应用段任务（等于排班没关）。现在仅开启时清残留+立即应用并进 home；关闭时只清残留记录、解除 home、不应用任何段任务，保持她当前状态

## 实测一百三十六

- 排班中的女仆【禁止手动改任务】——源头拦截：
  - 新增 `MaidTaskScheduleGuardMixin`：拦截 `EntityMaid.setTask`——排班开启（ScheduleData.isOn）且非【自动系统】调用（ScheduleSwitchGuard 内部标记）时直接拒绝。TLM 原版 GUI、蓝图书切任务、LLM 指令等一切手动入口在源头被掐掉，不再与调度重试互相顶（此前只拦了排班书自己的快捷设置/批量应用，TLM GUI/蓝图书/LLM 是绕过去的）
  - 主动战斗系统的 setTask 全部打上内部标记（参战/还原/换战术/idle 兜底 4 处）：战斗是自动系统，不因守卫误伤——排班女仆战斗中依然能正常切战斗、威胁解除后正常还原本职
  - smart_attack / smart_build 两个 LLM 工具加显式拦截：排班中的女仆直接拒绝并在工具结果里说明（而不是静默失败让 LLM 以为成功）
  - 被拦截时女仆气泡提示"排班中呢，任务由日程表管理~想手动改任务，先关闭我的排班吧"（5 秒限频防刷屏）+ logs/promaid.log 落盘
  - 配合一百三十五的"外部改动识别"双保险：即使有历史/漏网改动，调度也尊重手动选择不再死磕

## 实测一百三十五

- 修复"玩家修改排班状态下女仆的任务 → 排班卡死"：
  - 【根因】TLM 的 setTask 是无条件执行的（字节码实证：无睡眠/活动守卫），所以卡死不在 TLM；卡死出在我们调度器自己：段任务因"没活不切/暂不可用"进入 10 秒重试循环后，玩家手动把任务改走，排班每 10 秒仍想切回段任务 → 把玩家改的任务顶回/无限重试，观感 = 排班卡死
  - 【外部改动识别】新增尝试记录（记录"本段尝试时刻的任务"）：排班尝试过本段之后，当前任务 ≠ 尝试时刻的任务 = 外部改动（玩家 TLM GUI/命令/LLM）→ 排班**尊重手动选择**：写去抖键结束本段、不再重试、下个时段边界再接管，并落日志说明。尝试时刻任务没变 = 正常"没活不切"重试不受影响
  - 【整体隔离】applyNow 整段 try/catch——任一异常只落日志 + 10 秒重试，绝不击穿服务端 tick（防止某种异常每 tick 重演把排班整个瘫痪）
  - 【保存即时生效】排班书保存 = 清去抖键/尝试记录/重试冷却 → 立即按新日程落一次（修"改当前时段任务保存后不生效，要等下个时段边界"）
- 配套日志：外部改任务/应用异常都有 logs/promaid.log 落盘，排班"为什么不生效"可查

## 实测一百三十四

- 跨区块/跨维度传送根治——先定位再补刀：
  - 【根因】同维度（跨区块）的远距自动传送此前**完全依赖 TLM 自带机制**：MaidFollowOwnerTask 的 teleportToOwner 只对 非home+可脑动+主人同维度 的跟随女仆触发，且 10 次 ±3 随机试探可能全部落空（悬崖/窄道/主人飞行）而**静默失败**；排班自动 home 的女仆同维度更是永远不被 TLM 拉。历次修复（八十八区块保载/九十二接线/一百三十一 home 不拦跨维）改善的都是跨维度和"能否找到她"，**同维度远距拉回要么不存在、要么是 TLM 的随机静默失败**——所以怎么修都"还是不行"
  - 【新增】同维度远距拉回兜底（misc.maidSameDimPull，默认开，距离阈值 48 格可调）：与跨维度同款可靠链路（findStand + teleportTo 真传送），每 5 秒扫描中同维度超距即拉回；守家/坐姿/骑乘/干活中（挖矿/伐木/建造/站桩）不拉
  - 【新增】传送链路失败路径落日志（logs/promaid.log，60 秒限频）：跨维度无可站立点、"本该拉但守家/干活中不拉"、同维度拉回失败都有可见原因——"为什么不传"从此可查，不再玄学
  - 跨维度路径保持实测一百三十一语义：守家也照常跟随跨维度（排班女仆主人过门直接跟过来）

## 实测一百三十三

- 排班表三件套（学习 TLM-Sincerely）：
  - ① 切换前可用性检测（没活不切）：段任务应用前先查目标任务当前有没有活可干——硬闸 `IMaidTask.isEnable(maid)`（TLM 任务自身可用开关）+ 软探测（挖矿有矿 / 伐木有树 / 烹饪有熔炉 / 酿造有酿造台 / 农场有可收作物或可种空地）。没活就保持当前任务不切，约 10 秒后自动重试；战斗/待机/跟随/第三方附属任务不做扫描、只查 isEnable（保证其它附属工作模式兼容、夜班守家照常压阵）
  - ② 反向切换抑制（A→B→A 横跳冷却）：短窗口内同两任务来回反复到阈值即压制，防止战斗还原/可用性重试压任务导致的秒级来回覆盖；正常时段切换相隔约 2000 tick 天然不误判
  - ③ 自动 setTask 打内标记：排班自己的 `setTask` 用线程局部标记包住（`ScheduleSwitchGuard`），将来任何需要区分"排班自动切换"与"外部/玩家手动切换"的 mixin/守卫/兼容层都能识别，不再靠脆弱的启发式推断；每个服务端 tick 开头防御性清理残留标记
- 配置面板「杂项→排班表」新增 4 项：切换前可用性检测（开关）+ 反向切换窗口/阈值/冷却（tick）

## 实测一百三十二

- 新增建筑投影轮廓的诊断日志：判定到轮廓消失/靠的原因（服务端为空 RegionSync / 玩家主动打开手册清了金色预览 / BUILD_PROJECTION 总开关被关 / 渲染层前期空帧就退出）。及时收上来日志就能精准定位原因（或者你没发现这次发生）。性能影响零（仅变化瞬间记录）。
- 红色圈从主动拉跨维度到主人着落区域（已包含于实测一百三十一）。

## 实测一百三十一

- 修复跨维度跟随被 home 拦停：home 模式现在只拦【同维度跟随】（TLM 原生跟随照旧），不拦【跨维度跟随】——主人过门/到下界/到末地，排班/家用女仆都直接传送跟过来。测试清单的「跨维跟随」日志（PromaidLog，logs/promaid.log）也可直接查到成功结果（跨维：女仆名 跟随主人跨维传送至 x,y,z）

## 实测一百三十

- 搭路启动门槛从"最小竖直高差"改为"最小球面半径"（以女仆为圆心的 3D 欧氏距离，竖直+水平一起算）：主人在球面内不启桥靠跟随走路；球面外→高差够垂直搭高，竖直差不多+水平远+前方脚下悬空才平铺搭桥；实心地面平地纯走导航不启桥（根治平地反复启停抖动）
- 新增配置 bridge.minRadius（最小球面半径，默认 2 格），配置面板同步可改

## 实测一百二十九

- 排班失效排查：全链路诊断日志落盘 logs/promaid.log（反馈"排班表不停失效"）
  - 每步让位都留痕：总开关关/宽限期剩秒数/自保中/主动战斗中/日程空表/休息时段无段/去抖命中（60 秒限频）
  - 【应用后读回校验】TLM setTask 有守卫（睡眠/活动中）时静默拒绝，旧版去抖键已写→本段永不重试=应用了但没生效；现在应用后立即读回 getTask() 对比目标任务，不一致当场记录"任务未生效+实际任务"
  - 应用失败（任务UID非法/任务不存在/守卫拒绝/异常）不写去抖键 + 10 秒重试节流，条件解除后自动恢复
  - 保存日程审计：记录 谁保存/开关状态/段数/首段内容，对账"客户端以为开着、服务端实际状态"
  - 排班启用时自动锚定 home 也落盘（坐标）

## 实测一百二十八

- 右键喂女仆蛋糕补回血：女仆不是 Player 没有 FoodData，旧版只加好感+音效粒子不回复生命；现在吃完蛋糕按营养回血 14 点（m_5634_ heal，满血自动无效），玩家投喂与女仆自吃两路径统一

## 实测一百二十七

- 修复蛋糕吸引下女仆仍走岩浆块（玩家复测，日志实证 hp 掉到 25% 自保仍无人救）：
  - 【致命错配】DangerBlocks 危险表缓存 Set<Block> 但 isDanger/idIn 用 rl.toString() 字符串查询 → HashSet<Block>.contains(String) 恒 false → 寻路 mixin 与险境脱离全静默失效（脱离零日志的铁证）；缓存改 Set<String> 注册名
  - 【飞行缺口】1.20.1 FlyNodeEvaluator 自己覆写 m_7209_ → 旧 mixin 只注入 WalkNodeEvaluator，飞行女仆节点评估完全漏防；mixin 改为双类目标同时覆盖
  - 【脱离循环】险境脱离的安全格查找不排除玩家/实体占用的格 → 闭环岩浆测试把女仆传进主人脚底、碰撞挤回岩浆反复掉血；现排除占用格 + 搜索半径 4→8 + "无处可逃"静默路径补落盘日志
  - 【搭路直连腿】BridgeUpBehavior 的铺桥/斜上垫块腿（无导航直连施速）加危险表拦截——旧版会把台阶垫在岩浆/火上或踩上去

## 实测一百二十六

- 修复女仆互助"没道具还说话/系统提示喂了空气"：
  - 互助链入口加白名单预检——背包+双手没有任何可支援物品（药水/金苹果/牛奶/蜂蜜/食物）时整轮静默，不扫描不播报
  - feedSisterFood 手上食物由"活引用"改"扫描时刻快照"：同 tick 隐藏物品槽/任务换手清空手上的活引用时，旧版会把空栈当食物喂并播报"喂了空气"；现改为消耗前核对当前手仍是同种食物，喂空栈直接放弃
  - 系统字幕的物品名提前到进食（eat 会 shrink 栈）之前读取
  - giveFoodToOwner 副手哨兵 -1→-3（旧版只有副手食物时被误判"背包没有吃的"）；feedHealingFood 同款活引用防护

## 实测一百二十五

- 蛋糕投喂：女仆吃完蛋糕好感 +10（工作餐/家餐/玩家投喂同口径，每次一整块蛋糕）
- 玩家蛋糕右击自己的女仆 = 女仆立刻吃（音效+粒子）+ 玩家蛋糕消耗
- 触发蓝色系统消息与气泡（格式：女仆名：......）

## 实测一百二十四

- 空中搭桥距离上限 64 → 128 格（创造飞行远距离场景）

## v1.0.2(2026-08-17)正式发布

- 删除 v1.0.0 / v1.0.1 两个历史版本——原因:内置建筑蓝图未获原作者授权;
- v1.0.2 的建造目录已全部移除未授权内置蓝图,只保留玩家自己导入/生成的外部蓝图(建筑均为玩家自备授权);
- 修复 callresponse 2.0.4 兼容性(移除失效 mixin,消除启动崩溃与模组状态 broken);
- 修复 mixin 类被业务代码直接引用导致的 NoClassDefFoundError(农场冷却表迁出到普通类);
- 心契誓约对话/告白面板中文换行与居中重做(不再依赖 Font.split,逐像素切行);
- 玩家告白文案方向修正(玩家对女仆告白)+ 告白框文本完整居中;
- 女仆主动告白概率大幅提高(高好感基础 0.35、满好感 0.95);
- maidmarriage 调试面板新增红字免责声明;
- 内置蓝图目录全部移除后,手册建造目录仅显示外部蓝图。

### 测试版本声明

本版本经测试者验证,在以下模组的具体版本下可以正常运行;其他版本(含更新/更旧的 TLM 与各软联动模组)未必兼容:
- Touhou Little Maid(车万女仆,必装前置) 1.5.3 —— `[车万女仆] touhoulittlemaid-1.5.3-forge+mc1.20.1.jar`;
- maidmarriage(心契誓约,软联动) 2.3.0 —— `[车万女仆：心契誓约] maidmarriage-2.3.0-forge+mc1.20.1.jar`;
- callresponse(爱憎分明,软联动) 2.0.4 —— `Touhou Little Maid-Love  Loathe-1.20.1-forge-2.0.4.jar`;
- Heartfelt-connection(心契誓约×爱憎分明补丁,软联动) 1.0.0 —— `heartfelt_connection-1.0.0.jar`。

当前基线版本:v1.0.2。完整最新逐版本日志见 promaid_src/assets/promaid/guide/changelog.txt。以下为旧版开发条目(时间倒序)。


当前基线版本：v1.0.2。完整最新逐版本日志见 `promaid_src/assets/promaid/guide/changelog.txt`。以下为旧版开发条目（时间倒序）。

- promaid 1.5.252u：建造进度字段分行显示（状态文本分行：进度/缺料/参与女仆各行独立右对齐，进度条随行数自适应）
- promaid 1.5.252t：进度条上方字段出屏修复（右对齐改左锚点绘制、换行截断补省略号、蓝图名截断）
- promaid 1.5.252s：坐垫贴地修复（岸格改找最上面实心块，不再悬浮/放进水里）+ 手册进度条旁显示 块/秒速度与预计完成时间（防出屏钳制）+ HUD 广播限频验证日志
- promaid 1.5.252r：修复进世界第一 tick 即崩（普通代码直接引用 mixin 类被 JVM 判 invalid；钓鱼坐垫逻辑全部迁出到普通类 FishingChairService，mixin 仅留注入壳，普通代码零引用 mixin 类）
- promaid 1.5.252q：钓鱼坐垫重做（有坐垫直接坐/无则在最近岸边生成带标记坐垫并直接坐上/任务解除或脱离坐垫自动删除/被摧毁无掉落）+ 建造 HUD 多行折行显示与速度/剩余时间修复 + 清理 5 处调试日志刷屏 + 建造瞬移拦截恒 false 比较修复
- promaid 1.5.252p：创造模式材料显示 -21 亿修复（MAX_VALUE 溢出短路）+ 拟人化排序扩充装饰分类（灯笼/书架/陶罐/蜡烛/地衣等）
- promaid 1.5.252o：修复爱憎分明饥饿开关失效（2.0.2 作者包名 tartaricacid→JumDa5he,mixin 从未加载）
- promaid 1.5.252n：参与女仆数改为绑定数口径（手动绑定/全员加入计数一致,不再按区块附近统计）
- promaid 1.5.252m：悬空补支撑不再把树叶换成石头（树冠装饰恢复跳过，保护树冠外观）
- promaid 1.5.252l：世界存档提取不再把石头/砂岩等建材当地形过滤（天安门 32 块根因修复）
- promaid 1.5.252k：删除红石智能住宅·挖空版蓝图（挖空设计致附着物悬空、补支撑卡死下部设施,仅保留原版）
- promaid 1.5.252j：建造 HUD——左上角实时显示建造速度与预计完成时间（防错位防出屏）
- promaid 1.5.252i：钓鱼坐垫岸边判定修复（水面高 1 格才找岸边）+ 世界存档导入兼容带顶层目录的 zip + 悬空方块强制补支撑（不再大量跳过）
- promaid 1.5.252g15：治疗链只看血量（环境优先由分支顺序保证）
- promaid 1.5.252g14：治疗链跳过条件改为"正处于危险情境中"（不再看效果残留）
- promaid 1.5.252g13：情境药水改用【效果判定】做正门（按规则重做）
- promaid 1.5.252g12：投掷间隔按女仆记 + 主人有效果不投 + 情境中跳过治疗
- promaid 1.5.252g11：投喂优先级重排——情境保命（着火/溺水）优先于低血治疗
- promaid 1.5.252g10：蜂蜜瓶归为药物（只在中毒喝，不当食物）
- promaid 1.5.252g9：CD key 改为【效果种类】（变体不再各算一种）+ 投喂/自喝日志
- promaid 1.5.252g8：消灭药水浪费（饮用优先 / 喷溅优先 / 跳跃归情境）
- promaid 1.5.252g7：药水 CD 改为【按药水种类】记（异种互不阻塞）
- promaid 1.5.252g6：塞给主人的物品优先放快捷栏空位
- promaid 1.5.252g5：抗火药水也归为情境药水（不通用乱喝）
- promaid 1.5.252g4：投喂药水形态修正 + 情境药水不乱喝
- promaid 1.5.252g2：女仆投喂主人的药水补全（水肺/增益）
- promaid 1.5.252g：情境药水系统（溺水喝水肺 / 中毒喝蜂蜜 / 增益 CD=效果时长）
- promaid 1.5.252f：工作卡住播报降频（20 秒才说一次）
- promaid 1.5.252e：去掉冶炼，回归纯烹饪
- promaid 1.5.252d：回退复杂化，回归简洁
- promaid 1.5.252c：烹饪/酿造按女仆绑定调控 + 炉子/酿造台独占（v1.5.252d
- promaid 1.5.252b：手册新增"烹饪和冶炼"/"酿造"调控两页
- promaid 1.5.252：烹饪和冶炼重写（烟熏/高炉/熔炉 + 燃料修正 + 坐下绑定）+ 酿造台坐下 + 灵魂核心移除补全
- promaid 1.5.251e：灵魂核心功能移除 + 手册附魔光效验证
- promaid 1.5.251d：灵魂核心键位冲突修复 + 附魔光效 + 手册改蓝色
- promaid 1.5.251c：同存档双分身防护（一魂一仆）
- promaid 1.5.251b：记忆开关加固（磁盘优先 + 未加载女仆可关）
- promaid 1.5.251：物品纹理换色 + 记忆世界/时间标注 + 灵魂共享存储（双向同步/克隆继承）
- promaid 1.5.250：三处 SRG 错用根因修复（放水 / 记忆开关 / 记忆目录）
- promaid 1.5.249：记忆开关"关了又开"根因修复 + 放水双向诊断
- promaid 1.5.248：农场"走一步停一步"修复（批量种植/连锁收割性能优化）
- promaid 1.5.247：修正"包里什么都没有"误导文本
- promaid 1.5.246：放水失败定位诊断（有水桶却放不出水的排查版）
- promaid 1.5.245：自保会话进出防抖 + 高血量着火不喊救命（日志审查修正）
- promaid 1.5.244：WorldProbe 启动预热加载（"让女仆帮忙填坑"报错修复）
- promaid 1.5.243：着火链路补末影珍珠逃生（"跳入水坑和用末影珍珠都没有了"）
- promaid 1.5.242：记忆开关"关了一会儿又变回开"修复（显示 + 持久化双管）
- promaid 1.5.241：逃跑避让岩浆 + 搭高防跌落修复（getValue 全链替换为注册名比较）
- promaid 1.5.240：修复"有水桶却说没招了"——hasWaterBucket 改 getKey + 放水链
- promaid 1.5.239：修复启动崩溃（真正根因）——追踪药水 mixin 目标类改声明者
- promaid 1.5.238：修复启动崩溃——追踪药水 mixin 方法名漏写 SRG 名
- promaid 1.5.237：末影珍珠逃生改原版投掷特效（紫色轨迹 + 落地传送爆炸）
- promaid 1.5.236：岩浆误报"卡住"修复 + 农场批量种植（与连锁收割同设置格式）
- promaid 1.5.235：自保播报精简（不聒噪）——只留危险/求救，删光动作汇报
- promaid 1.5.234：阶梯判定思想推广——环境逃生补全阶梯 + 回血资源阶梯化
- promaid 1.5.233：判定阶梯（岩浆 300 > 着火 280 > 自保 250）+ 链路全失败让位恢复
- promaid 1.5.232：岩浆/着火链路按玩家规范重写 + 避让降级为仅移动 + 全失败"我没招了"播报
- promaid 1.5.231d：语言检测改自动重刷（直到中文才放行）+ 背包满明确"我给不了东西"
- promaid 1.5.231：着火不逃窜（朝主人走）/ 追踪药水弹 / 死亡传送全维度 / 女仆烧死兜底 / 记忆防注入污染
- promaid 1.5.230：放水判定补强（岩浆放水 fallback + 着火多位置）
- promaid 1.5.229：岩浆判定全面优化（感知半径/逃生顺序/抗火豁免/地狱特判）
- promaid 1.5.228：投喂/火把/盾牌从出生起就没运行过的根因修复（canStillUse）+ 死亡传送落点贴地 + 日文根治
- promaid 1.5.227：主人死亡传送安全落点（女仆"消失"）+ 系统消息刷屏根治 + 记忆开关显示修 + 投喂/火把运行时诊断
- promaid 1.5.226：AI 记忆开关真正生效（事件源守卫 + 客户端状态同步）
- promaid 1.5.225：修复编译链（5 个编译错误）+ 锄头黑名单 + 抗火豁免补全 + Brain 诊断
- promaid 1.5.224：手册"导入世界地图"按钮——玩家自助提取，无需手动放文件
- promaid 1.5.223：世界存档 ZIP 提取（"Tiananmen Palace.zip" 实测打通）
- promaid 1.5.222：构建链上限放开到 50 万 + ZIP 打包蓝图解析
- promaid 1.5.221：区块材料表单（0/88 页）公式修正——已建 + 女仆背包 + 玩家背包
- promaid 1.5.220：手册"导入建筑"按钮（文件选择器 + 版本警告确认 + 服务端解析校验注册）
- promaid 1.5.219：链路调试页按钮重叠修复（刷新 vs 重要度 / 页码 vs 标记删除恢复）
- promaid 1.5.218：建造悬空方块根治（门/活板门/中继器支撑判错）+ 材料缺口公式修正（已搭建+主人+女仆）
- promaid 1.5.217：图腾触发系统消息 + 盾牌诊断日志 + 语音包按钮反馈 + 玩家伤害模式 4 + 音量注释
- promaid 1.5.216：岩浆脱困五项修正（walk-out 时限/抗火豁免/逃跑防抖/范围云/卡墙误报）
- promaid 1.5.215：岩浆水桶时序修复 + TTS 音量 mixin FATAL 修复 + 投喂/火把诊断日志
- promaid 1.5.214：修复进入世界卡死（Server thread 自锁）——农场扫描/自保扫描加区块加载检查
- promaid 1.5.213：坐下受伤立刻站起来（锁移动判定保留，仅加受伤起身）
- promaid 1.5.212：删除施工区避让行为 + 坐下锁移动根治（每 tick 判定）
- promaid 1.5.211：着火分支尾部"贴脸反击"兜底（逃出岩浆带火的 15 秒窗口收口）
- promaid 1.5.210：着火分支"抗火/找水"位置互换
- promaid 1.5.209：岩浆逃生分支重构——walk-out 加时限 + 主分支 2 逃生链路重排 + 岩浆内/烧伤职责分离
- promaid 1.5.207：玩家对女仆伤害策略（面板可调——TLM 原版压制 / 完全免疫 / 无限制 / 有上限）
- promaid 1.5.206：投喂/治疗策略全面化——状态感知优先级链，不再只投喂单一药水/食物
- promaid 1.5.205：对话语言根治（TLM 聊天 GUI 也强制中文）+ 治疗药水判定修复 + 图腾扣减修正
- promaid 1.5.204：不死图腾真实消耗 + 岩浆感知/逃生大修（播报/无水自救/避让）+ 放水 3 秒回收 + 建造两步流程解锁
- promaid 1.5.203：搭高安全高度配置化（与落地水触发高度配对）
- promaid 1.5.202：轻量级自保（落地水格式）——边打边保命，不再与战斗冲突
- promaid 1.5.201：喂食按饱和度恢复量排序 + 金苹果改即时增益道具（不再当常规食物）
- promaid 1.5.200：自保退出稳定期（防 70% 上下浮动频繁进出）
- promaid 1.5.199：自保逻辑链修复（搭高积极性/传送保命/围殴喝药）+ 水桶垫水 + 岩浆逃生 + 环境提示 + 爱憎分明饥饿开关
- promaid 1.5.198：TTS 音量倍率 + 系统消息朗读（训练一次缓存复用）+ 系统语音包导入 + 记忆 API 绑定 + 对话语言强制
- promaid 1.5.197：自绘蓝图大小无限制 + 只入册不自动动工（防止拆周围）
- promaid 1.5.196：LLM 查询-行动闭环 + 感知查询工具 + 工作清单 + 画蓝图防超时（移植 PatchouliAI）
- promaid 1.5.195：威胁判定扩大——所有携带仇恨的生物（含中立反咬/主人被攻击）+ 文案"敌人"
- promaid 1.5.194：搭方块中途窒息封头（替换"搭完后糊脸"）+ 所有系统对话青色
- promaid 1.5.193：女仆说话的时宜——敌袭状态冻结情绪价值/主动对话 + 系统提示特殊颜色
- promaid 1.5.192：Promaid 手册——女仆工作链路调试面板 + 改名
- promaid 1.5.191：女仆大脑升级——7 阶段主动对话 + 配额改革 + 记忆维护周期 + 回复反馈学习
- promaid 1.5.190：AI 双手工具 + 记忆系统修复 + 面板修复（全链路排查）
- promaid 1.5.189：全能管家扩展——农业畜牧运维 + 玩家贴身辅助 + 环境交互
- promaid 1.5.187b/188：死循环修复 + 区块范围精确化 + 建造确认流程 + 战斗调整
- promaid 1.5.187：删掉站桩围壁，改为"拿方块糊怪物的脸"
- promaid 1.5.186：自保搭高统一——不再按敌人近战/远程划分
- promaid 1.5.185：大跳劈距离修复——触发距离与实际可达距离一致
- promaid 1.5.184：跳劈重构——普通跳劈 = 玩家同款垂直跳 + 玩家式暴击结算
- promaid 1.5.183：修复绑定失败 + 女仆管理显示问题
- promaid 1.5.182：女仆管理 UI 重构（详情页绑定 + 区块名单/工头）
- promaid 1.5.182：普通跳劈 = 玩家跳跃高度 + 修复"莫名其妙跳一下"（跳劈动作同步）
- promaid 1.5.181：大跳劈降低高度（不触发落地水）+ 大跳 10 秒 CD
- promaid 1.5.180：多区块共存 + 无女仆创建 + 女仆-区块显式绑定（架构级重构）
- promaid 1.5.179：缺料数字改为实时计算（需求 − 已建 − 绑定女仆/主人背包）
- promaid 1.5.178：区块内控制限制 + 女仆管理升级（区块列表/命名/绑定解绑）
- promaid 1.5.177：暂停 = 解除绑定——暂停后女仆可以停下来去干别的事
- promaid 1.5.176：修复游戏加载世界时崩溃（MixinTransformerError 致命错误）
- promaid 2.0：建造系统彻底重构（单计划状态机 + 统一已建感知 + 区块内右击进详情）
- promaid 1.5.175：跳劈斩改抛物线大跳（玩家指定"那种抛物线曲线"）
- promaid 1.5.174：跳劈触发重构（距离触发 + 人数分流）+ 取消"平A"概念
- promaid 1.5.173：建造不启动根因修复（TLM 时间表 activity≠WORK → 任务行为不被评估）
- promaid 1.5.172：连锁采集改为同时破坏（FTB Ultimine 式）+ 挖矿换镐改目标驱动
- promaid 1.5.171：控制按钮不再依赖玩家位置（修复"继续建造按钮消失"）+ canUse 诊断 + 清理残留存档
- promaid 1.5.170：建造死锁修复（被拒时看不到计划位置→无法取消）+ 恢复诊断日志
- promaid 1.5.169：手中正在使用的工具/武器/盾牌不触发切换，除非即将用坏自动换下一个
- promaid 1.5.168：附魔装备黑名单保护（即将用坏）+ 评分翻转（附魔>耐久）+ 镐切换挖掘等级对标
- promaid 1.5.167：武器切换优先级重写（DPS>耐久>附魔）+ 三叉戟自保确认覆盖 + 记忆调试面板
- promaid 1.5.166：弓/弩自保失效修复（StartAttacking 断源）+ 御币武器切换修复 + 自保诊断日志
- promaid 1.5.165：血机制解除自保后第一件事 = 定位主人并传送过去
- promaid 1.5.164：建造区块红色固定框 + 自保退出传送 + 跳劈连贯性 + 挖矿诊断日志
- promaid 1.5.163：连锁采集 / 连锁收获的范围可自定义
- promaid 1.5.162：建造区块标记机制 + 控制按钮按区块显示 + 去掉强制建造/强制续建
- promaid 1.5.161：进阶采集——挖矿连锁采集/自动收集 + 农场连锁收获/自动收集（默认关闭）
- promaid 1.5.160：详情页控制按钮随"有无进行中计划"动态显示
- promaid 1.5.159：手册建造面板 UI 重做 + 区块显示（建造范围预览）
- promaid 1.5.158：自保提示矛盾消除——威胁播报/退出播报区分 + 传送免触发窗口
- promaid 1.5.157：搭高不再扔珍珠 / 传送重试封顶 5 秒（旧配置 60 秒不跟随修复）/ 自保解除播报
- promaid 1.5.156：建造"瞬间完成"根因修复——建材等价不再算"已建"
- promaid 1.5.155：跳劈全程禁横扫 / 挥砍延迟 / 跳劈斩弧线加高 / 跳劈不后撤
- promaid 1.5.154b：跳劈斩改为"跳向"敌人（抛物线弧线，非贴地冲刺）
- promaid 1.5.154：跳劈斩（30% 概率冲向敌人 + 大击退），其余普通垂直跳
- promaid 1.5.153：单敌必跳劈 + 自保触发/取消机制重定义
- promaid 1.5.152：跳劈/横扫按敌人数分流 + 自保全程实时传送判定
- promaid 1.5.151：传送安全半径默认 4→5 格（防远程怪；魂符可兜底绝对安全）
- promaid 1.5.150：自保传送只判定主人身边安全（撤销"自己身边无怪"拦截）
- promaid 1.5.149：跳劈"只跳不打"根因修复——空中挥砍由行为亲自执行 + 明显暴击提示
- promaid 1.5.148：半成品原点识别改为"世界方块种类 ∩ 蓝图"交集锚点（方案）
- promaid 1.5.147：御币弹幕还击（遵循御币原版攻击方式）/ 取消后续建识别兜底
- promaid 1.5.146：跳劈高度回调（0.8 ≈4 格 → 0.5 ≈1.7 格，和玩家差不多）
- promaid 1.5.145：自保近身反击支持远程武器（弓/弩贴脸直接放箭）
- promaid 1.5.144：生存模式续建修复（缺料步骤不再被截断丢弃）/ 缺料播报中文名
- promaid 1.5.143：障碍播报带矿物名 / 自保近身回击 / 跳劈加高+暴击特效
- promaid 1.5.142：进阶装备控制（副手盾牌 + 饰品栏图腾）/ 跨维度跟随 / 蛋糕可吃 / 建造强制坐下
- promaid 1.5.141：平A横扫修复 / 后撤加强 / 蛇形走位 / 攻击词条过滤
- promaid 1.5.140：挖矿拾取/中文播报 / 删除建造传送 / 搭建速度档位硬限 / 删除伐木 / 搭方块边缘保护 / 换武器词条匹配
- promaid 1.5.139：挖矿耐久根治——软方块判定从"硬度"改为"镐正确工具"
- promaid 1.5.138：挖矿耐久对齐伐木 / 自动换武器只换空手 / 伐木补强（砍树叶+垫高+中文名）/ 建造传送根治
- promaid 1.5.137：修复两个 bug（虚空误判疯狂传送 / 手册按钮重叠）
- promaid 1.5.136：战斗指挥工具（smart_attack）——AI 对话直接下令开打
- promaid 1.5.135：自保盲区修复（自然伤害/无来源伤害/非 Monster 生物）+ 骷髅对策
- promaid 1.5.134：单兵作战战术（PVP 式战斗）——删除战斗协同与整理任务
- promaid 1.5.133：新增伐木任务（maid_smart:woodcut）——TLM 缺失的高需求任务
- promaid 1.5.132：战斗任务增强——协同作战（共享目标 + 护主集火）
- promaid 1.5.131：AI 聊天/记忆大脑链路全面核查 + 记忆可诊断性改进
- promaid 1.5.130：产出型任务专项增强（农场连收连种 + 钓鱼主动找水域）
- promaid 1.5.129：TLM 原生任务通用呆滞修复 + 干活不被打断（两个总开关可回滚）
- promaid 1.5.128：输入框 StackOverflowError 根治（真正的 setTextColor 是 m_94202_）+ 崩溃日志确认旧 jar 缺内部类
- promaid 1.5.127：挖矿"愣 2~3 秒"根治（直接导航 + 扫描重建提速）+ 整理任务大升级
- promaid 1.5.126：主人防窒息保护 + 材料提示中文名 + 输入框根治（照搬原版搜索框）
- promaid 1.5.125：创造模式"缺 21 亿材料"整数溢出根治（不建造链路之一）
- promaid 1.5.124：建造根治（暂停改内存态）+ 行为不再 3 秒重启 + 挖矿节流减半 + 输入延迟提交
- promaid 1.5.123：三 bug 根治（挖矿"一步一停"根因 / 配置面板输入 / 下达后不建造根因）
- promaid 1.5.110：配置面板每项注释 + 全部文本"偏左超出屏幕"修复
- promaid 1.5.109：上一版 4 个问题真正修复（换镐/搭高漂移/不停传送/安全区传送）
- promaid 1.5.108：建造"没规划就提前传送"修复（根因：存档静默恢复旧计划）
- promaid 1.5.107：挖矿按需换镐（不再无条件切最高级）+ 耐久核实
- promaid 1.5.105：记忆系统 ×（心契誓约 + 爱憎分明）联动全修（优雅降级）
- 1.5.122：挖矿"完全不动"修复 + 面板输入修复 + 建造诊断日志
- 1.5.121：建造恢复 home 模式（回归修复）+ 挖矿移动容差 + 面板输入聚焦
- 1.5.120：感知播报"分时宜"——危险时停止不合时宜的非紧急感知
- 1.5.119：敌对感知显示降频（感知检测照常，仅显示频率降低）
- 1.5.118：挖矿移速对齐 TLM 伐木（0.6）+ 部署 116/117
- 1.5.117：建造侧传送从根源停用（玩家决策）+ 撤销强制 home 模式
- 1.5.116：挖矿"一走一停"修复 + 建造被埋判定消抖（诊断预备）
- 1.5.115：确认建造 → 施工区【一次性清场传送】（按玩家预期修订）
- 1.5.114：建造逻辑深度优化（A 正确性 + B 性能 + C 手感 + D 清理 全组实施）
- 1.5.113：挖矿机制深度优化（A 性能 + B 正确性 + C 手感 + D 清理 全组实施）
- 1.5.112：自保传送节奏 + 死亡传送重定位 + 换工具评分分离 + 手册注释全面重写
- 1.5.111：标题居中模板 + 挖矿漂移根治 + 建造传送修复（3 大项）
- 1.5.104：审查建议实施——确认弹窗 + 建造完成通知 + 优化
- 1.5.103：全模组审查修复（1 高 + 4 中）+ 优化
- 1.5.102h：修复 heartfelt SmartPromptMixin（与 102g 同源的第二个崩溃点）
- 1.5.102g：修复 PromaidPromptMixin 注入非法导致女仆对话崩服
- 1.5.102f：模组镐评分兜底（模组武器/附魔兼容确认）
- 1.5.102e：修复耐久扣错对象 + 方块秒破（旧版残留问题）
- 1.5.102d：矿表面板/搜索/障碍物预勾选 + 换镐升级 + 拖拽削弱 + 无矿挡路上报
- 1.5.102c：进度文字右对齐 + 报点中文名 + 石质方块开路
- 1.5.102b：修复建造中进度条/进度文本贴出屏幕底边
- 1.5.102：全模组数值面板化——所有剩余硬编码数值全部纳入配置面板
- 1.5.101f 末影珍珠逃生 CD 缩短 + 数值全部进配置面板
- 1.5.101e 珍稀掉落物捡不到提示（气泡 + 主人聊天栏双提示）
- 1.5.101d 珍稀标记矿物（掉落物回收改为"标记格"机制 + 手册第三个创造物品栏名单）
- 1.5.101c 挖矿掉落物隔墙回收 + 拾取优先级按价值（原版低价值 0.5）
- 1.5.101b 挖矿黑白名单升级（目标矿物 + 障碍物 双名单，创造面板 toggle + 输入添加恢复）
- 1.5.101 修复 AI 记忆开关崩溃（TaskData ByteTag 强转 CompoundTag）
- 1.5.100d 修复女仆莫名传送（施工区防接近传送无冷却反复拽）
- 1.5.100c 开路白名单批量扩容（原木等自然方块）+ 矿物扫描加速
- 1.5.100b 挖矿目标拖拽 + 矿表创造面板 + 记忆页全面增强 + 面板细节修正
- 1.5.100 配置面板重构（手册式跳转+翻页）+ 手册目录按钮调小
- 1.5.99c 挖矿挡路判定根因修复（SRG 映射错误）+ 自动装备全面激活
- 1.5.99b 感知播报全局限频 + Blocker 崩溃排查
- 1.5.99 配置面板 UI 重构（可滚动 + 加大行距 + 标签对齐）
- 1.5.98c 修复站桩女仆溺水 + 主人注视感知刷屏
- 1.5.98b 启动崩溃修复（换回原版 TLM 后 mixin apply 失败）
- 1.5.98 关系记忆适配层（模组特色联动：记忆系统软感知心契誓约×爱憎分明）
- 1.5.97 Soul 灵魂系统（跨存档/换女仆迁移记忆）
- 1.5.96 全子系统精准开关体系（防未来 agent 冲突）+ Soul 系统评估
- 1.5.95 女仆大脑全面加强（记忆 6 项增强 + maidsoulcore 4 个子系统借鉴）
- 1.5.94 AI 画蓝图（子 Agent 设计器）+ 蓝图手册删除
- 1.5.93 站桩锁死位移（治"坐着/站桩女仆诡异地动"）
- 1.5.92 建造期传送抖动根治（建仆强制 home 模式，从 TLM 源头断跟随）
- 1.5.91 自保传送加固（双判定 + 珍珠落点安全）
- 1.5.90 挖矿穿墙 + 任务工具自动装备（修复"主手有镐仍不挖挡路方块"）
- 1.5.25 落地水（被动生存技能）——女仆自动落地水
- 1.5.25 续建修复（生存分批建造闭环）——超大型建筑（上万块）背包放不下，
- 1.5.9（BALANCED 补丁）：修复"第 6 格误判"——爱憎分明原版逻辑把女仆背包第 6 格
- 1.5.25（当前）：搭方块/挖矿搭高/酿造全自动/整理增强（测试反馈）——
- 1.5.24：五项测试反馈修复 + 海景房蓝图 + 材料展示重构 + 6 种风格建筑——
- 1.5.23：自保"可见性"修复（测试反馈反思）——
- 1.5.22：挖矿回归修复（测试反馈）——
- 1.5.21：自保优化（保命道具/威胁感知/智能逃生）——
- 1.5.20：自保状态机重构（新逻辑）——
- 1.5.19：挖矿渐进化（对齐 maid_useful_task 伐木逻辑）——
- 1.5.18：蓝图卷轴移除 + 触发方式重述 + 手册材料缺口——
- 1.5.17：四问题修复（测试反馈）——
- 1.5.16：三入口建造闭环（非 LLM 全流程 + LLM 状态查询）——
- 1.5.15：预制精美建筑（5 套，首次启动自动安装）——
- 1.5.14：复杂精美建筑能力（迁移 numen BlueprintFormats）——
- 1.5.13：建造体验优化 + 蓝图获取生存化——
- 1.5.12：numen 式结构文件蓝图迁移——
- 1.5.11：不依赖 LLM 的建造触发方式 + 建造种类/数量扩展——
- 1.5.10：建筑链路重构（借鉴 numen 蓝图设计）——
- 1.5.9
- 1.5.8：小女仆与父亲的剧情记忆——child_state/child_lineage 的
- 1.5.7：自保不再"抽空打怪"（每 tick 清攻击目标，Brain 各优先级组
- 1.5.6：工作状态播报——女仆选了 maid_smart 任务却不干活时，每 10 秒
- 1.5.5：记忆写入崩溃修复——TLM 的 TaskData 用 unboundedMap codec 解码
- 1.5.4：借鉴史诗攻城 + numen AI——
- 1.5.3：任务切换修复（createBrainTasks 必须返回可变列表——TLM 的
- 1.5.2：任务修复（行为有目标才启动，不再空转发呆，无目标时正常漫游）、
- 1.5.1：自保升级——覆盖一切状态（不限战斗）、优先级 250 全局最高（压过跟随/任务/睡觉/恐慌）、双退出条件恢复原状态、直接寻路逃跑
- 1.5.0：战斗自保（低血逃跑/进食/搭方块，纯规则零 LLM）
- 1.4.1：建筑材料判定升级——缺料时先问玩家"建一半还是放弃"（mode=full/partial）
- 1.4.0：建筑功能（smart_build 工具 + 3 个内置蓝图 + LLM 生成蓝图 + 材料判定）
- 1.3.0：心契誓约剧情记忆（结婚/怀孕/心情/丧子/告白 实时注入 AI 对话）
- 1.2.0：情绪价值动作（跑过来/递食物/看向/撒爱心）+ 家庭保护（防同主女仆互殴）
- 1.1.0：新增烹饪/酿造任务、自主决策（空闲自判任务）、长期记忆（事件写入+context 注入）
- 1.0.0：P1 四工具 + P2 挖矿/整理 + P3 主动对话
